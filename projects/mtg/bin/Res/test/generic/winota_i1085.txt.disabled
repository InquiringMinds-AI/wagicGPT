#Winota, Joiner of Forces (#1085; set id 479736 - comma name): a non-Human attacker triggers - look at top 6, put a Human
#from among them onto the battlefield tapped and ATTACKING (the complaint).
#Bears attack; eager cadet should arrive attacking -> opponent takes 2+1.
[INIT]
firstmain
[PLAYER1]
inplay:479736,grizzly bears
library:darksteel citadel,millstone,juggernaut,gold myr,silver myr,eager cadet
[PLAYER2]
[DO]
goto attackers
grizzly bears
next
choice 0
goto blockers
eager cadet
goto blockers
goto blockers
next
goto end
[ASSERT]
ENDOFTURN
[PLAYER1]
inplay:479736,grizzly bears,eager cadet
library:*,*,*,*,*
[PLAYER2]
life:17
[END]
#PARKED: reveal-machinery family, 5th witness (selvalas charge/enforcer,
#shifting_shadow, sigarda coven cousins; Reveal:6 variant). The attack
#trigger fires but the optionone target(human|reveal) machinery moves nothing
#- library untouched, no human enters, bears hit alone. The complaint's
#"must enter attacking" (readytofight) leg is unreachable behind it.
#UPDATE 2026-06-11 reveal round: option-oneshot fix landed; this @combat
#trigger reveal still never populates (trigger-context populate layer).
#Park stands.
#UPDATE 2026-06-11 trigger-populate round: the populate theory is DEAD - the
#@combat reveal constructs AND populates (6 cards) post earlier fixes. The
#real blocker: the AI auto-key in MTGRevealingCards::CheckUserInput (suite
#players are always isAI) self-advances multi-card displays before scripted
#clicks land. Infrastructure built: revealok/revealnext harness commands +
#open-display dispatch. A WAGIC_TESTSUITE guard on the auto-key let the picks
#drive (putbacks worked both legs!) but broke the tuned reveal:1 tests AND
#exposed the next layer: afterrevealed effects never fire when reveals QUEUE
#on the shared zone (the empty-branch sees the other reveal's card). REVIVAL
#RECIPE: re-apply the guard, retune enforcer/charge DOs with revealok, fix
#the shared-zone after-firing, then drive this with revealok + choice 0 +
#cadet click.
