# Finding 5 (CR 514.2): in cleanup, "remove all damage" and "until-end-of-turn
# effects end" happen SIMULTANEOUSLY as one non-stack turn-based action, with no
# state-based-action window between them. Test with a 1/1 (Var Brute Runner) that
# is pumped to 1/3 by a +0/+2 until-EOT effect (Var Bark Ward) and then dealt 2
# marked damage (Var Spark Jolt): 2 < 3, so it is alive during the turn.
#   CR-correct: cleanup removes the 2 damage AND ends the +0/+2 as one action ->
#               the creature never passes through a lethal 1/1-with-2-damage state
#               -> it SURVIVES into the next turn (this fixture asserts survival).
#   Divergence: if the engine ends the ueot pump first and checks SBAs before
#               removing damage, the creature would be a 1/1 with 2 damage and die
#               (moves to graveyard).
# PASS = REFUTED (observable is CR-correct); FAIL (creature in graveyard) = PROVEN.
[INIT]
firstmain
[PLAYER1]
inplay:Var Brute Runner
hand:Var Bark Ward,Var Spark Jolt
[PLAYER2]
[DO]
Var Bark Ward
Var Brute Runner
Var Spark Jolt
Var Brute Runner
eot
next
[ASSERT]
upkeep
[PLAYER1]
inplay:Var Brute Runner
graveyard:Var Bark Ward,Var Spark Jolt
[PLAYER2]
[END]
