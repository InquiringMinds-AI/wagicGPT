#W62-AA (R1, wave-62 codex review finding 1) - the CAST ROUTE's X announcement
#must carry the engine's own Cancel row onto the decision contract.
#
#DecisionContract.cpp has TWO ANNOUNCE_X branches. The `menu->announcing &&
#menu->announceCost` one (the pay[[{X}]] round) sets req.canDecline; the
#ordinary cast route - the route deck130 s21/s22 took - set req.kind and
#returned, leaving canDecline at its initialised false. A seat reading the
#contract therefore still saw a one-row X menu with no pass, and Starstorm at
#max affordable X = 0 was still burned.
#
#Ivy Elemental {X}{G} over {3}{G} arms that cast-route branch (maxX = 3, rows
#X=0..3, the engine's Cancel at index 4). `assertxdecline 1` reads the request
#the contract builds for that live menu: RED wherever canDecline is false.
#`choice 4` then takes the row and the card stays in hand, so the fixture also
#re-pins the engine fact the claim rests on.
[INIT]
FIRSTMAIN
[PLAYER1]
hand:Flicker,Ivy Elemental
inplay:plains,mountain
manapool:{3}{G}
[PLAYER2]
[DO]
Ivy Elemental
assertxdecline 1
choice 4
[ASSERT]
FIRSTMAIN
[PLAYER1]
hand:Flicker,Ivy Elemental
inplay:plains,mountain
manapool:{3}{G}
[PLAYER2]
[END]
