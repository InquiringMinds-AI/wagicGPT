# deck27 general-suggestions.md — core-prompt proposals (wave-19)

Layer-routing is enforced: single-seat items CANNOT add or cut a core line; engine/representation
items go to the notes.md ledger, never into core prompt text. This seat proposes **NO core-prompt
change.** Rationale and routing below.

## Core prompt: PASS (no change proposed)

Nothing this seat found is both (a) 2+-seat convergent and (b) properly core (not a guide off-case
or an engine fix). Detail:

1. **Free-block decline (deck49 s9)** — the model read the correct "(you kill it, your blocker
   lives)" annotation in its own reply and declined the block. This is single-seat. It is ALSO not
   clearly a core-prose fix: the representation already surfaced the favorable trade correctly, so a
   core line "take favorable blocks" would be teaching against a signal the model already has and
   ignored. Routed to the GUIDE (deck27 strategy.txt block rule) as the interim, and flagged as a
   MODEL-obedience watch in skill.md. Do not add a core line on one seat.

2. **Combat stale-board hallucination (deck14 s9)** — the model invented an attacker not in the
   offered set. This is a REPRESENTATION/model-grounding issue, not a strategy-judgment issue, so it
   does not belong in core strategy prose. Routed to the GUIDE (interim "only the listed attackers
   exist" line) and to the ENGINE/parser LEDGER (notes.md: salvage-valid-pairings-drop-illegal-index).

3. **Off-case losing spiral (deck35 s12) and ahead over-deliberation (deck110 s20)** — both are the
   giant-reply tax. The off-case-spiral doctrine is already handled at the GUIDE off-case layer
   (wave-18 ruling: do not double-layer into core). The ahead-giant mirror is single-seat and rare;
   handled with a guide line ("when ahead, keep it simple"). Neither is a core candidate.

## Confirmations for the synthesis agent (no text change)

- **Trust-the-rendered-number** held at this seat with ZERO mana fallbacks on a two-color
  hybrid base — a supporting data point that the mana false-belief risk scales with color
  COMMITMENT, not with two-color complexity. Reinforces the existing rule; no new prose.
- **The evasion-first / lords-are-the-engine pattern is deck-specific**, not portable core prose —
  it belongs entirely in the per-deck guide (this deck's win path). Correctly kept out of core.

## Engine/representation items produced (routed to notes.md ledger, NOT core)

- Bundled-block parser: on an out-of-range attacker index, drop that pairing but keep other valid
  pairings instead of discarding the whole reply (deck14 s9).
- (Reinforcement) TRADE OUTCOME annotations verified CORRECT at this seat — no ledger defect; the
  wave-19 annotation batch is clean here.
