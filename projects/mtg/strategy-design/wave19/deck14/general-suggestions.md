# deck14 (Deep Blue) — general-suggestions.md — core-prompt proposals (layer-routed)

## Verdict: CORE PASS from this seat. No new core-body line proposed.

Single-seat doctrine forbids adding/cutting a core line from one seat, and everything deck14 found
this wave routes BELOW the core (guide revision) or to the engine/representation ledger. Detail:

1. **Slow-clock soft-durdle (vs110, vs135) — GUIDE, not core.** The core's general "be aggressive"
   is already satisfied (the model declares attackers, even quotes the guide's attack-floor). The
   residual failure is a DECK-SPECIFIC clock-quality problem (deploy the flyer finishers, swing the
   whole board, don't poke) — exactly what the per-deck guide is for. Handled in strategy.txt Rule
   #1B. A core edit is neither warranted nor allowed from one seat.

2. **Combat format-drift (now DIED) — no core action.** The wave-18 concern is resolved by the
   guide's Rule #2 + the engine combat name-tolerance batch. The reply protocol in core already
   states the coded format; no third restatement (the sanctioned-against escalation). Core frozen.

3. **Menu/intent mismatch stale_echos (vs49 s26, vs135 s27, vs27 s15) — engine/representation, not
   core.** Core already carries the option-list-authority meta-rule verbatim (wave-17 synthesis,
   core line ~36). These are present-and-disobeyed instances (the model narrates one action and
   emits an index for another); a fresh core restatement hits the representation-beats-instruction
   wall. Guide off-ramp + the ledger item carry it. Keep core frozen.

4. **vs49 s31 unparsed coded block — engine, not core.** A well-formed `BLOCKS: B1:A2` discarded at
   the decisive turn is an engine parse/validation matter (notes.md E-NEW), not a prompt issue.

## OWNER-RULING check (effectBadOrGood suppression -> annotation replacement)
No spot at this seat where a legal play was HIDDEN or where a warning ANNOTATION (the new ed7d579e4
replacement for the old suppressions) misled the model into or out of a play. deck14's steal/bounce
are opponent-targeted and beneficial-to-self; they were offered and used normally (steals aimed at
bombs this wave). Nothing to flag for the annotation redesign from deck14.

## Brief focus item unverifiable here (not a core matter, noted for completeness)
TRADE-OUTCOME block annotations were not observable at this seat (empty `options_text` on combat
records) — see notes.md. No core implication; routed as an observability gap.
