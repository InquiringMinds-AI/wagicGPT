# deck27 general-suggestions.md — core-prompt proposals (wave-21)

Layer-routing enforced: single-seat items CANNOT add or cut a core line; engine/representation items
live in the notes.md ledger, never in core prompt text. **This seat proposes NO core-prompt change.**

## Core prompt: PASS (no change proposed)

Nothing this seat found is both (a) 2+-seat convergent AND (b) properly core. Detail:

1. **Answer-first (item b) validated — but it is HARNESS/PARSER, not core prose.** 135/135 clean coded
   first lines, 0 fallbacks, two 13-17k giants and one live repetition-degeneration all rendered
   harmless. This is the strongest positive result at the seat, and it belongs entirely to the
   harness/parser layer the reviewers do not edit. No core text follows from it; recorded as
   cross-seat confirmation for synthesis (every reviewer should be seeing the same 0-fallback effect).

2. **Wither annotation (item g) — ENGINE, closed.** Fixed and validated (notes.md #1). Not core.

3. **Ahead-position degeneration loop (vs135 s18, 3rd instance) — MODEL/DECODE, defused.** A core
   "keep it simple when ahead" line cannot prevent a decode repetition loop; and answer-first already
   makes it harmless. Routed to notes.md. Not a core candidate. The guide's existing ahead line is
   retained but is not expected to prevent decode loops.

4. **Late-stall durdle revision — GUIDE off-case, unexercised.** Owns the guide layer; its trigger did
   not occur. Not core.

## Confirmations for the synthesis agent (no text change)

- **Trust-the-rendered-number held again with ZERO mana fallbacks** on the hybrid {U/B} base (Nth data
  point that mana false-belief scales with color COMMITMENT, not two-color complexity). Reinforces the
  existing rule; no new prose.
- **The evasion/protection win-path is deck-specific**, not portable core — it drove all 5 wins and
  belongs in the per-deck guide. Correctly kept out of core.
- **The +2 record swing is mostly variance** (repeated-opponent control; 3 favorable + 1 unfavorable
  flip). A caution for synthesis: do NOT read deck27's 5/6 as evidence the guide/engine batch "worked"
  — the durable signal is the tax collapse (0 fallbacks), which is harness-layer and symmetric across
  seats, not deck27-specific.

## Engine/representation items produced (routed to notes.md, NOT core)
- Wither trade-annotation fix VALIDATED (item g) — full verbatim citations in findings.md.
- Answer-first + last-CHOICE-wins validated benign (items b/c).
