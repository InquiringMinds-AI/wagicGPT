# general-suggestions.md — deck49 (Dragons) wave-21 (core / general-prompt + decode items)

Single-seat cannot add/cut core lines. Candidates for synthesis to weigh; none is a deck49 guide
change (deck49 guide is a byte-copy this corpus).

## G-49a (carry, RE-SHAPED) — the reasoning-tax spiral now PARSES but relocated its consequence (CORE/decode)
Wave-20: the tax crossed the token cap at the develop fork and TRUNCATED to unparse (3x). Wave-21: the
develop-fork trigger is dissolved by the guide (0 unparses there), AND the answer-first protocol makes
the SURVIVING spirals parse — but the spirals themselves are undiminished in the non-develop pockets:
  - deck27 seq22 Shivan-cast 12939c/185s; seq23 blocks 13042c/185s (near the 16.9k max).
  - deck102 seq14 War-Paint value 9541c/135s.
  - deck35 seq18 War-Paint value 5730c/83.8s — this one tripped a stale_echo FALSE-POSITIVE (notes
    HARNESS-49a): the spiral re-stated its prior PLAN near-verbatim and the echo guard discarded a
    valid answer-first CHOICE. Zero harm here, but it is a NEW consequence class: **under answer-first,
    the tax no longer breaks the parser — it now risks tripping a same-state / repetition guard.**
The wave-20 levers still stand and are RE-CONFIRMED as the right layers (do not migrate to the guide):
  (a) A COMMIT brake in the GENERAL prompt: "once you have weighed both sides of a choice, state your
     CHOICE line — do not re-derive the same option set again." deck49's guide carries a deck-local
     version at the DEVELOP seam (validated dead this corpus); the tendency is universal (it now shows
     at combat/card-value/land-drop pockets, and at OTHER seats' giant replies). Strongest anti-spiral
     lever; belongs in core.
  (b) A decode-time REPETITION guard / early-stop on n-gram repeat (decode/harness, route to notes at
     the seat that owns the decode config). deck27 seq22/23's 13k-char tails are the strongest case.
NEW this wave: (a)+(b) also PRE-EMPT the HARNESS-49a false-positive — a shorter, committed reply would
not near-duplicate the prior PLAN and would not trip stale_echo. So the anti-spiral lever pays off at
BOTH the parse layer (wave-20) and the echo-guard layer (wave-21). Synthesis: the case for a core
COMMIT brake is now stronger, not weaker, post-answer-first — answer-first fixed the SYMPTOM (parse),
not the DISEASE (the spiral), and the disease found a new exit.

## G-49b (carry, VALIDATED as a template) — the develop-seam co-firing-fork fix generalizes
Wave-20 flagged "deploy a body vs throw burn at the face, no tiebreak -> paralysis" as a cross-deck
aggro shape and deck49's fix ("either is fine, COMMIT" + tighten the over-broad exception) as a
template. This corpus VALIDATED the template on deck49 (6/6 clean commits at the seam). Re-flag for
synthesis: any aggro-with-reach guide that states BOTH "deploy every turn" and "burn goes face" should
carry the same explicit early-turn tiebreak. The validation discriminator (reply LENGTH at the seam,
not fallback count) is the reusable audit method — see skill.md.

## G-49c (carry, ESCALATED) — trade-outcome annotations mask card-script stat drift, now GAME-MATERIAL
Wave-20 had no decision turn on a mis-rendered stat. Wave-21 DID: deck27 seq15, Ashenmoor Liege
rendered 4/1 (real 4/4), the "(both die)" block annotation accurate to the WRONG stat, deck49 lost its
anthem in a block a true 4/4 would have won (notes CARD-49-ashenmoor). This is the exact G-49c hazard
with a game cost. No prompt action — but it confirms card-script audits cannot rely on decisions
LOOKING wrong (the annotation renders a true trade over a false stat). Route: card/roster owner (notes).

## G-49d (carry, RE-CONFIRMED as the record's governor) — top-heavy curve / no reach (deck construction)
All THREE losses trace here, none to a decision: deck27 (burn-light + walled + the Ashenmoor drift),
deck62 (Shivan-flooded, creatureless from t7, vs an explosive Enchantress engine), deck102 (op to 8
then no reach vs a lifegain climb to 31, top-decking bare Mountains). Same lever as prior waves: a
3-drop flyer or a second cheap evasive threat would give deck49 a closer when burn runs out. DECKLIST
property, outside the strategy-guide and core surfaces; recorded so synthesis does not misattribute the
4/6->3/6 to a prompt effect. The record is noise around this construction ceiling — the wave-20 edits
IMPROVED decision quality while the record fell on draw/matchup variance. Not actionable from here.
