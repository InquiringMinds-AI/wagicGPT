# deck102 general (core-prompt) suggestions - wave 21

## PASS - no core-prompt change proposed.

The core prompt is symmetric and has passed 15 consecutive waves; deck102 produces no core-prompt proposal
this wave. Two context notes for synthesis:

1. Answer-first reply protocol (wave-21 item b, a core/reply-protocol change) is VALIDATED at this seat:
   0 fallbacks (down from 6 in wave-20), and every p95+ reply (14,310 / 14,243 / 13,867 chars) leads with a
   complete coded line before truncation. The behind/stalled giant-reply deliberation tax PERSISTS (the
   model still writes 14k-char replies at the same spots), but it no longer converts to a parse failure.
   This is the reply-protocol layer doing its job; the residual deliberation length is a decode/off-case
   concern (guide off-case branch + decode sampler), not a core-prompt line - do NOT double-layer it into
   the core.

2. The painland self-damage blind spot (Ancient Tomb / City of Traitors deal 2 to you per tap, invisible
   in the "Mana available" line) is a REPRESENTATION/option-rendering item (-> notes.md R-PAINLAND), NOT a
   core-prompt item. Flagged here only so synthesis routes it to the representation ledger, not the prompt.
