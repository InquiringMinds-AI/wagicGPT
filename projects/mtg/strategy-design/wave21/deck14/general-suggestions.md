# deck14 (Deep Blue) — general-suggestions.md — core-prompt proposals (layer-routed) — wave-21

## Verdict: CORE PASS from this seat. No new core-body line proposed.

Everything deck14 found this wave routes BELOW core (per-deck guide demotion) or to the engine/harness
ledger. Single-seat doctrine forbids adding/cutting a core line from one seat, and nothing here reaches
core. Detail:

1. **Bounce-on-the-stack (wave-20 #1 leak) — RESOLVED at the ENGINE, guide demoted, core untouched.** The
   wave-21 batch shipped the durable representation fix (item f: CAST-option annotation + TARGET-menu
   header). Confirmed fired + obeyed (25/26 declines; the 1 menu played correctly; 0 self-bounces). Core
   already carried the option-list-authority meta-rule; the residual was a deck-specific rules gap on a
   bounce-heavy deck, now carried by the engine annotation at the decision point. Core needs nothing.

2. **Truncation-before-line (E2) — RESOLVED at DECODE (answer-first, item b), core untouched.** Core
   already states the coded short-answer format; the failure was the reply exhausting the cap before ANY
   line, which no core prose fixes. Answer-first (emit coded line first) closed it — every p95 reply now
   parses. This is the reply-PROTOCOL layer, correctly handled in the harness, not core.

3. **spell-name-echo on a target menu (H1) — PARSER, not core.** The single fallback (vs27 s29): the model
   echoed the spell name ("Unsummon") ahead of the correct target on a target menu. Core and the menu
   header already instruct "answer with the chosen TARGET's name." A third copy in core would hit the
   representation-beats-instruction wall (the fix is the parser matching the "targeting <option>" tail or
   honoring the in-range index). No core action.

## ANSWER-FIRST protocol — one positive core-adjacent observation (owner: reply-protocol/harness, not this seat)
The wave-21 answer-first reply protocol (coded line first, then reasoning, then PLAN) is validated as a
NET POSITIVE at this seat with NO answer-quality regression: across the seat's longest replies (11k-15k
chars), the coded first line matches the reasoning that follows — the model did not commit to a worse
answer by answering before reasoning. This is evidence FOR keeping answer-first in the core reply contract;
flagged for the reply-protocol owner as a clean single-seat confirmation, not a core change request.

## OWNER-RULING check (effectBadOrGood / annotation-replacement) — the wave-20 "correct-but-reasoned-past" case is CLOSED
Wave-20 flagged the effectBadOrGood self-harm warning as CORRECT-but-reasoned-past (vs62 s18: the model
cast a self-harming Unsummon anyway, the on-stack false belief overriding the warning). Wave-21 the deeper
representation fix (the on-stack annotation naming WHY the target is illegal — battlefield-vs-stack) shipped
and the same-matchup same-seat s18 now READS the annotation and plays correctly. The "name WHY the excluded
target is illegal" routing recommended for the annotation-redesign owner is VINDICATED: the stack-vs-
battlefield naming is what flipped the play. Recorded closed for that lineage (matches the deck133
Fatal-Push-on-stack family the wave-20 note tied it to).

## Brief focus items owned by other seats — none route to core from deck14
Reveal partition, attacker-sparing, wither annotations, blocker-fallback gang-chump, cast-stall loop are
owned by other seats; deck14 saw no cast-stall (item i inert — mono-U, all casts payable) and no
attacker-sparing surface (no non-haste-attacker-vs-last-cast tension at this seat). Core frozen; 16th PASS
from this seat.
