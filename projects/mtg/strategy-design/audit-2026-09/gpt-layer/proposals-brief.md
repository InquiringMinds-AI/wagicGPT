# GPT-layer proposals lane — brief (owner-approved items of LEDGER.md §D per LEDGER-v2 §4/§6 AS CORRECTED BY §7)

Worktree: /home/magi/Projects/wagicGPT/worktrees/lanes/gpt-proposals (branch gpt-proposals, from master 9aa2c59ef). Game dir projects/mtg. Never touch the main checkout or other worktrees.

READ FIRST: LEDGER-v2.md §4-§6 (the rulings; your scope is EXACTLY the approved items), LEDGER.md §D (each proposal's evidence and cost from both reviewers), review-fable.md §4 and review-astra.md §4 (anchors), cleanup-lane.md (what the code looks like now — the cleanup merged at 6fc6f5947, the PARSETEST split at 7e3ef2d9f), split-parsetest.md (the platform build-list recipe). ~/.gatelogs/gpt-audit/audit-prompt.md carries the BINDING rulings: the golden protocol text (bin/Res/ai/gpt/reply-protocol.txt / kReplyProtocol) is untouchable EXCEPT the one extension approved for P10; the parser is robust; never remove, cap, or auto-answer a legal option; the surface owes truth ("a true statement in the wrong scope is a lie"); keys = board state + question; the model's reasoning stays in its native channel; the reply = PLAN then action. Corpus for measurement: ~/.Wagic/ai/gpt/selfplay-runs/matchups-20260912-074153-final/*.jsonl. Use /usr/bin/grep.

ITEMS — one commit each so any one can be reverted alone; in this order:
P3 Collapse identical hand entries (`Dream Fracture x2 [tag]`). Equality includes the entry's live facts, not the name alone; where a seam asks for ONE instance the instances stay distinguishable.
P4 Shorten the two dominant hand tags (the sorcery-speed tag ~cpp:31262; the HELD-instant tag ~cpp:31281) into one hand-line header + a short per-card tag; per-card exceptions survive; correct the "no legal target on the board" wording for stack targets.
P6 One clause per number on a row: fold the three overlapping mana clauses keeping BOTH sources-spent and sources-left (different numbers); print the fit tag only when a row is lost; fold the three-sentence ATTACK TOTAL into one; blockers header once.
P8 Emit only the range grammar the menu used (the renderer records which shorthand forms it emitted; `kOptionRangeNote` appends only those).
P5 NARRATION COMPACTION, as the owner-approved sample in LEDGER-v2 §5: a phase prints only when something happened in it; draw + land on the turn line; pay/cast/resolve → one line with the cost in parentheses; target/use/result → one line; damage carries the new life total; order within a phase preserved; NO FACT DROPPED (Astra: the sample fold must not drop the source count — it keeps "(N sources)"). The GAME LOG register ruling (2026-08-19) governs the register. Pin: a fixed recorded turn sequence renders to the exact sample text; a second pin that every event kind in the corpus's narration has a fold rule (enumerate the line families from the corpus; any family without a rule renders as before, never dropped). Measure narration bytes before/after over the corpus's recorded events (the emitters take the event stream; replay the stream from a seat log if the record has it, else from the fixed pin sequence) and report the fraction.
P10 Whole-order blocker ORDERING answer: `orderBlockers` asks once for the whole permutation instead of one pick per round. This is the ONE approved reply-shape extension: add the ordering answer form to the protocol text ONLY for that seam, minimal, stated verbatim in your report; the parser accepts the permutation; any partial/malformed answer falls back to the per-pick asks (no option lost). Count the round trips it saves on the corpus's ordering windows.
P9 LAND DROP FOLDED INTO THE CASTING MENU on the seat's own main phase (owner: "try it. Revert if it degrades behavior." — CORRECTED ruling, LEDGER-v2 §7): the land rows (`Play <land>`) appear in the same menu as the cast rows; one action per answer as today (a menu returns one action; after a land enters, the cast question re-puts on the changed board — no option lost, no auto-play); key-stable; behind a config/compile-time switch that restores the separate land ask; the probe must show land-drop windows answered from the combined menu. Report the round trips per own-main-phase before/after on the probe.
P11 Telemetry schema: one `window` record kind per decision with `seam` as a field; side records only for events with no window (`hold_event`, `recovery`, `gamestart`/`gameend`); fold `forced_close`/`wall_miss`/`menu_single_outcome` into fields of the window record they join to; KEEP every counter (they are consumed); this is a SHAPE change, not a deletion. UPDATE every tool that reads the old shapes (projects/mtg/tools/corpus-stats.py, regime-gate.py, runmanifest.py, selfplay-harness.sh's harvest, w81-dk-window-probe.sh, prompt-byte-census.py) and run each tool's --selftest.
P12 LAST: split AIPlayerGPT.cpp by responsibility (prompt facts + presentation; answer selection/parsing; answer lifecycle, reservations, transport; translog serialization; the decision seams) as a MOVE with zero behaviour change — byte-identical PARSETEST output and byte-identical prompts on a fixed record (pin both); every new source added to wagic.pri AND every platform build list (Makefile PSP, CMakeLists.txt Vita, Android/jni/Android.mk, Makefile.win) exactly as split-parsetest.md did; regenerate with `qmake6 wagic-SDL.pro CONFIG+=debug -o Makefile.sdl` (ALWAYS -o Makefile.sdl); move incident-narrative comments (seq citations, wave-by-wave measurements) into gpt-layer/comment-archaeology.md keeping every WHY/invariant beside the code.
NOT in scope: P1, P2 (A/B only, later), P7 (owner: NO — the discard prompt KEEPS its game log; closed), P13 (the next corpus).

STANDING RULES: every wagic invocation under `systemd-run --user --scope -q -p MemoryMax=4G -p MemorySwapMax=0 --`; build `make -f Makefile.sdl -j8` from projects/mtg (make -B before the final gate); Edit tool for UTF-8 sources; `git diff | /usr/bin/grep -c $'\357\277\275'` = 0; every new header scalar in the ctor list; NO AI attribution of any kind in commits; explicit paths in git add. Bytes: the corpus is fixed, so measure new renders by rebuilding the recorded boards/events through the new emitters in pins and report the per-item delta on those.

FINAL GATE (all required): (a) suite THREADS=1 as a detached unit `systemd-run --user --unit=gpt-proposals-suite -p MemoryMax=4G -p MemorySwapMax=0 --working-directory=<worktree>/projects/mtg/bin bash -c 'unset WAYLAND_DISPLAY DISPLAY; WAGIC_HEADLESS=1 WAGIC_TESTSUITE=1 WAGIC_TESTSUITE_THREADS=1 ./wagic > ~/.gatelogs/gpt-proposals-suite.log 2>&1; touch ~/.gatelogs/GPT_PROPOSALS_SUITE_DONE'`, waited with a FOREGROUND until-loop (no Monitor tool); count both `==Test Failed !==` and `==Test timed out`; baseline 1312/0 + 81 AI/0; known flakes lifeline / merrow_reejerey / intrepid_adversary_repeated_payment rerun solo. (b) PARSETEST from bin 0 failed (baseline 7231; report the count). (c) `python3 projects/mtg/tools/check-ctor-init.py projects/mtg/src`; `python3 projects/mtg/tools/check-reply-instructions.py projects/mtg/src projects/mtg/bin/Res/ai/baka`; every tool --selftest you touched. (d) Heuristic A/B: `bash projects/mtg/tools/baka-ab.sh <worktree>/projects/mtg/bin ~/.gatelogs/gpt-proposals-baka 10 4` then `python3 projects/mtg/tools/baka-ab-compare.py /home/magi/Projects/wagicGPT/archives/baka-ab/62a5733ec ~/.gatelogs/gpt-proposals-baka` — 0 unfinished, no flag (this layer must not move Baka). (e) LIVE PROBE: two full games on the pilot: `cd <worktree>/projects/mtg && ./tools/selfplay-harness.sh -u http://100.116.136.74:8084 -m qwen36-35b-a3b -k "$(cat <fresh scratchpad key file>)" --thinking on -r 1 -T 0 -j 1 -p 152,125 -o ~/.gatelogs/gpt-proposals-probe` as a detached unit (`-p MemoryMax=12G -p MemorySwapMax=0`); the key: `ssh spark 'cat ~/.config/vllm/qwen35-api-key' > <scratchpad file>` — NEVER print it, DELETE the file afterwards; wait with a foreground until-loop polling every 60 s for the harness's `results (` line (~20-40 min per game); both games natural end, 0 fallbacks of any kind, `reasoning_content` on every record, `plan_line_missing` ≤ ~5%, 0 unparsed; at least one land-drop window answered from the combined menu and one blocker-ordering window exercised (if the games produce none, SAY SO — never fake it); `python3 projects/mtg/tools/corpus-stats.py ~/.gatelogs/gpt-proposals-probe*` with the UPDATED tool; paste the census.

REPORT projects/mtg/strategy-design/audit-2026-09/gpt-layer/proposals-lane.md: per item — what changed (file:line), the pin(s), the measured byte / round-trip delta, P10's exact protocol text, P9's revert switch and round-trip delta, P5's before/after fraction; the gate numbers; the probe census and both game results; a weakest-evidence paragraph. Then `touch ~/.gatelogs/GPT_PROPOSALS_DONE`. Final message: per-item one-liners with deltas, gate numbers, probe results, commit hashes, weakest evidence.
