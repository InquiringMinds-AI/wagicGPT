# GPT-layer audit — `projects/mtg/src/AIPlayerGPT.cpp` + `include/AIPlayerGPT.h`

Read-only, 2026-09-15. Repo `/home/magi/Projects/wagicGPT` at `e71f73c5a` (NOTES commit on top of the audited master `62a5733ec`); no build, no run, no edit, no git write. Every `cpp:` anchor below is a line in the CURRENT 63,671-line `AIPlayerGPT.cpp`; `h:` is `include/AIPlayerGPT.h` (2,813 lines). The census (`strategy-design/audit-2026-09/gpt-layer-census.md`) was measured on the 106,056-line pre-split file — its line numbers are NOT valid here (production lines sit ~145 lower around 34k; I re-located every census item by symbol and cite the current line). Corpus = `~/.Wagic/ai/gpt/selfplay-runs/matchups-20260912-074153-final/` (42 seat logs, 2,473 records, 2,135 prompt-bearing decisions, 22 stderr files, `askreplay/` sidecar). Rules citations are grepped from `~/.gatelogs/engine-audit/cr.txt` (rule number + quoted text; Appendix A lists every rule used).

**One caveat that governs every corpus number in this report.** The corpus was played on the **wave-80** binaries (`strategy-design/wave80/reviewer-brief.md:9`; the 12 W81 gameend keys are absent from all 42 `gameend` records; 0 of 2,135 prompts carry the W81 `ONE TOTAL` fold). Every W81 mechanism in the file — the crack-back total fold, the cover-paragraph fold, the send-disposition counting, the SBA cache bypass, the stack-handle cast digest — has **no corpus measurement at all**; where I judge it I say "code only".

Method: I read the decision/parse/key/hold/assembly regions in full myself (§6 lists exactly which) and delegated four regions (board render, combat seams + verdict lineage, transport/telemetry/counters, non-combat seams) to fresh readers with the same rulings, then spot-verified their top findings against the source before including them. Corpus claims were computed with python over every record (scripts left in the scratchpad only, nothing written to the repo).

---

## 0. Baselines measured on the corpus (used throughout)

| fact | value |
|---|---|
| prompt-bearing decisions | 2,135 (ask 1,661 · priority 290 · attackers 80 · discard 52 · blockers 28 · reveal 20 · bottom 5) |
| prompt bytes | 28,033,436 total; p50 12,654 B, p90 21,254 B, max 27,189 B |
| prompt composition (median) | narration 5,590 B (**48.5 % of all prompt bytes**) · CURRENT SITUATION 3,424 B (26 %) · question+rows+notes 3,350 B (25 %) |
| system prompt (not in `prompt`, recorded via `system` record) | 46,695–51,793 B per seat-game (mean 48,506 B); it already carries the full rules text of every card in BOTH decks |
| round trips | 56.4 h of inference; latency p50 87 s, p90 165 s, max 418 s; every record `transport=curl=0,http=200,empty=0` |
| model non-compliance | `protocol_deviation`: compliant 2,072 · `unlabelled_plan` 57 · `plan_absent` 5 · prose 1; `plan_line_missing` 62; `coded_answers>1` **0**; `answer_replaced` **0**; `parse_note` on 2 records (both `multi_answer_first_taken`); `fallback` on 2 records (`unparsed_reply` 1, `engine_answered` 1); re-ask lanes fired **0** times |
| windows the model was NOT shown | `hold_windows_skipped` 1,051 (cast 860 / priority 191) · `ask_replays_reserved` 1,176 (cache 1,172 / cross-phase 4) · `own_turn_windows_skipped` 722 · `mana_only_windows_skipped` 78 · `main_phase_windows_skipped` 70 · `identical_option_asks_resolved` 13 · `single_outcome_menus_answered` 3 · `stop_reached_windows_skipped` 1 · `reserve_decline_windows_skipped` 1 · `sibling_window_asks_skipped` 0 · `chain_windows_collapsed` 0 |
| async | `async_drops` 9 (all "casting arm; question and board moved; re-asked"); `forced_close` 26 records = 13 armed + 13 `dropped_decision_moved`, **0 consumed**; `phase2_answer_recovered/missing` 0/0; stale-drop livelock breaker never fired |
| counters | 78 numeric gameend keys present; **27 are zero across all 42 seats**; 12 W81 keys absent; `crackback_verdict_lines_rendered` sums to 382 while 329 prompts contain the line and 291 records carry the face field (21 of 42 seats disagree) |

---

## 1. CRUD — code that does nothing for the model or the engine

Verdict key: **DELETE** = no production caller / no consumer / no render and no card-class excuse; **KEEP-DORMANT** = zero renders only because the 7-deck pool lacks the card class.

### 1.1 Helpers with no production caller (verified by token census over the comment-stripped source, then per-name grep across `src/`, `include/`, tools; the census's Tier-A list has false positives, see 1.9)

| # | symbol | anchor | evidence | verdict |
|---|---|---|---|---|
| C1 | `declineRowAtFact` | cpp:423 | extern, 0 references anywhere in the repo | DELETE |
| C2 | `shortDecodeGarbage` | cpp:19972 | static, 0 callers | DELETE |
| C3 | `loopCautionForBoards` | cpp:27536 | static, only comment mentions | DELETE |
| C4 | `w72HeldMenuShowedEveryRow` | cpp:37789 | static, 0 callers | DELETE |
| C5 | `w71EveryBranchIsANoOp` | cpp:44880 | static, 0 callers | DELETE |
| C6 | `combatDirectiveNegatedOnLine`, `combatDirectiveExampleEcho` | cpp:57986, 58016 | static, 0 callers (their consumer, the prose-reversal re-ask, was deleted in #W70/#W71) | DELETE |
| C7 | `wagicGptParseSelfTest` | cpp:62789 | extern, 0 references in the repo (the entry point is `runParseSelfTest`, h:663) | DELETE |
| C8 | `AIPlayerGPT::askReplayRefuse` | cpp:20904, h:734 | superseded by `askReplayRefuseScoped` (#W72-BT); both live sites use the scoped one (cpp:48866, 48923); kept only for a PARSETEST pin | DELETE with its pin |
| C9 | `AIPlayerGPT::loopAutoPassApplies` | cpp:38421, h:2054 | equals `chainAutoPassApplies(false,a,b)`; only the self-test calls it | DELETE |
| C10 | `AIPlayerGPT::w80CrackBackVerdictLineNow` | cpp:37528, h:2294 | both render sites (cpp:47453, 51788) call the W81 twin; only `AIPlayerGPTSelfTestAccess` reaches it | DELETE |
| C11 | `noAnswerClassFor` 3-arg overload | cpp:18951 | no production caller; `noAnswerClass()` (cpp:19149) calls the 6-arg form; the 4/5-arg forms are pure forwarders | fold the four overloads (cpp:18951/18968/18986/18999) into one |
| C12 | `transportOutcomeStamp` 3-arg | cpp:19029 | pure forwarder to the 6-arg form (cpp:19064) | fold |
| C13 | `w79BestBlockDamage` `vector<int>` overload | cpp:30764 | 3-line adapter reached only from the self-test access seam (cpp:63484); production uses the struct form (cpp:30676) | move to the self-test TU |
| C14 | `gptRetrySelectArmClose` | cpp:~20200 | exists only to thread one bool through `gptRetrySelectArm`; the flag is already a slot field (h:400) | fold |

### 1.2 Dead members, dead variables, dead assignments

| # | item | anchor | evidence | verdict |
|---|---|---|---|---|
| C15 | `bool retracted = false;` and the `retracted ? "retracted_choice" : …` fallback class | cpp:47680, 48125, 49104, 49334 | never set anywhere since #W70-BM deleted the retraction gate (the comment at cpp:48128-48131 says so); `retracted_choice` cannot be written | DELETE variable + class |
| C16 | duplicate `mPlanSetTurn = …; mCurrentPlan = plan;` | cpp:25744-25745 (repeats 25720-25721 verbatim; note the `}        mPlanSetTurn` join on 25744) | pure duplicate assignment left by an edit | DELETE |
| C17 | `mAskReplayKey`, `mAskReplayRun` | h:716-717; cpp:48652-48653, 49290-49291 | only ever cleared; superseded by `mAskReplayRuns` (#W72-BT) | DELETE |
| C18 | `mCeilingReasks` | h:848; cpp:20668 | write-only, never emitted (the one counter with no output at all) | DELETE or emit |
| C19 | `mStatedStopCount` | h:~1797; written cpp:20729, 25734, 47776 | never read | DELETE |
| C20 | `mStubReplyIndex` | h:~2243 | declared unconditionally; its only user is inside `#if defined(_DEBUG)||defined(WAGIC_DEVLOGS)` (cpp:19330-19368) | move inside the gate |
| C21 | `writeSingleOutcomeMenuRecord(..., commonPaid=0)` | cpp:55082 (call), 21145 (writer) | the `repeat_payments_paid` field is a literal 0 on every record — a field that cannot vary carries nothing | replace with the untapped-source count (needed by flaw F12) |
| C22 | `force_close_arms_deferred` gameend row | cpp:22961 | serialises `mForceCloseArmsRefused` — the SAME member as the row above it (cpp:22960); one of the two is a copy-paste artifact | DELETE the duplicate row |

### 1.3 Counters nothing reads (zero corpus-wide AND no consumer)

Consumer census: the only tool that interprets gameend counters is `tools/corpus-stats.py:465-489` (a generic dump of every int key plus a reconciliation of `protocol_replies`); `tools/w81-dk-window-probe.sh:25-28` reads four W81 keys; `corpus-compare.py` reads `won`; `regime-gate.py` reads `thinking`. Every other counter is read only by the wave brief that introduced it. Of the 27 zero-everywhere counters, these have BOTH no consumer and a per-record join that already carries the event, so deleting them loses nothing a reader can't recompute with `grep -c`:

| gameend row | member / increment sites | why dead |
|---|---|---|
| `ask_replays_refused` cpp:22874 | `mAskReplaysRefused` cpp:48868, 48925 | 0; max replay run in the sidecar is 39 of the 64 cap; the sidecar `cache_replay_refused` record is the join |
| `menu_pass_no_progress` cpp:22875 | `mMenuPassNoProgress` (AIPlayerBaka.cpp:6099) | 0 |
| `sibling_window_asks_skipped` cpp:22882 | cpp:37976 | 0 — see complexity X1 |
| `chain_windows_collapsed` cpp:22884 | cpp:38483 | 0 — see flaw F13 |
| `repeat_annotated_takes` cpp:22910 | cpp:22152 | 0 |
| `engine_reveal_floor_picks` cpp:22929 | cpp:21775 | 0 |
| `wall_miss_events` / `wall_miss_unrecorded` / `wall_miss_no_retry` cpp:22942-22944 | cpp:20604/20631, 20140, 20138 | 0; the per-record `wall_miss` field + `recovery` record is the join — three counters for one class |
| `action_before_plan_replies` / `_rejected_replies` cpp:22950, 22954 | cpp:22476, 22488 | 0; per-record `action_before_plan` / `_rejected` fields are the join |
| `phase2_answer_recovered` / `phase2_answer_missing` cpp:22955-22956 | cpp:19632, 19634 | 0/0 **by construction** while flaw F2 stands |
| `forced_close_unrecorded`, `force_close_arms_refused`, `force_close_arms_deferred` (dup), `force_close_defer_bound_hits`, `force_close_same_arm_deferred`, `forced_close_events` cpp:22958-22967 | cpp:20391/20440/20553, 20523, —, 20386, 20521, 21020 | the `forced_close` records (cpp:21018-21036) carry `outcome`/`park_armed`/`defer_ticks`/`unrecorded_so_far` per event — six counters for one record kind |
| `single_outcome_menus_answered` / `_rows_spared` cpp:22970-22971 | cpp:21148, 21150 | duplicated by `menu_single_outcome` records |
| `crossphase_board_unchanged` cpp:22976 | cpp:49048 | 0 |
| `hold_reopened_new_threat` cpp:22985, `hold_reopened_new_lethal` cpp:22990, `hold_events` cpp:22994 | cpp:38104, 38056, 21108 | duplicated by the 180 `hold_event` records |
| `ask_replays_repeat_latch` cpp:23011, `menu_pass_no_progress_suppressed` cpp:23012, `cached_replay_reasked` cpp:23016 | cpp:48943, 47532, 47570 | 0 |
| `put_gloss_stripped` cpp:23052, `answer_label_absent_read` cpp:23061 | cpp:22562, 22549 | 0 |
| `stop_reached_reputs_collapsed` cpp:22861 | cpp:47059 | 0 |
| `identical_ask_answers_reserved` cpp:22937 | cpp:48939 | 0 here (999 in wave 73) — keep only if the repeat latch survives complexity X1 |

Per-record fields with no tool reader (86 keys emitted by `writeTransLog` cpp:21908-22791; 33 are read by some tool; these 53 are read by nobody): `abandoned_after_s action_before_plan_rejected answer_replaced async_drop_events async_drops attempt_ms choice_meaning choice_source chosen_text_core coded_answers crackback_verdict deadline_pct dropped_assignments hold_check_ref_seq hold_check_ref_window http_error_body http_status ignored_line latched_coded_line latched_line latched_line_in_plan log_window log_window_kind log_window_turns_elided long_reply max_tokens_answer menu_pass_forced own_loop_verdict parse_note plan_echo_count plan_names_stranded_card plan_names_uncastable_zone_card plan_step_count plan_steps_done post_answer_overrun pruned_pairs reask_reason reasoning_budget_hit reasoning_degenerate reasoning_ngram_repeat repeat_n reveal_stall reveal_stall_phase reveal_wait_*(5) skips_since_last stack_death_verdict system_hash transport w81_render_events`. `transport` in particular: 2,135 identical strings, 0 readers, ~200 lines of stamp grammar behind it (cpp:18121-18176, 19012-19125, 20105-20126). Reviewer prose reads some of these by hand; that is the only consumer. Verdict: keep the load-bearing dozen (`kind seq window_seq prompt reply reasoning choice chosen_text options_text fallback latency_ms events`), move the rest behind `WAGIC_DEVLOGS` or delete (proposal I8).

### 1.4 Never-rendered emitters (measured 0 of 2,135; each verified against the current source)

| emitter | anchor | verdict |
|---|---|---|
| `cannotPayNowClause` `{you cannot pay this right now: 0 mana available}` | cpp:9426 | DELETE — restates the header's `0 total` on a row that already prints `[cost:]`; needs three coincident facts to fire |
| `paymentNoLifeCostClause` `{paying this costs you NO life: the auto-tap plan …}` | cpp:9347 | DELETE — 55 renders × 191 B of a non-event; the CAUTION line (cpp:31603) already says "MAY"; keep `paymentLifeCostClause` (cpp:9356) for the event |
| `menuFitTag` empty-`lostRows` branch `{leaves N sources - no other row on this menu needs more than N}` | cpp:40658-40669 | DELETE the branch — it renders on EVERY row of a menu whose rows all fit, so it discriminates nothing; keep the `rows 2 and 3 need more…` branch. 1,143 of 8,740 option rows carry this beside `{leaves N of your M untapped mana sources untapped}` (cpp:8417): the same N printed twice |
| the six own-loop verdict emitters (`w78OwnLoopVerdictLine` cpp:34410, `w79OwnLoopVerdictLineFor` 34468, `w77OwnLoopVerdictKey` 34580, `w79VerdictDangerRank` 34616, `w80LoopFaceOfLine` 36730, `w80ProvenWinLoopLine` 36756) | | `own_loop_windows_asked` = 1, lines rendered = 20, all 15 face strings 0 renders. NOT crud by the reach test alone — the class (the seat's own Sanguine Bond/Exquisite Blood loop mid-resolution) exists in the pool. KEEP the fact, FOLD the six into one (complexity X6) |
| `removalVictimTag`, `spellRemovalVerb`, `namedCastPenaltyScan`, `stackAnswerYesRowClause`, `w75ApplyChainFeed`, `w80SacrificeSpendsBlockerClause`, `zeroPowerAttackerTag`, `asyncDropTraceLine`, `w81FoldDuplicateCoverParagraphs` | cpp:41513, 41456, 41328, 39856, 34336, 29683, 6885, 19305, 40991 | census listed these as Tier-A deletable. Every one HAS production callers (grep: 3–5 call sites each). `asyncDropTraceLine` feeds `DebugTrace` (dev-only by JGE's `#ifdef _DEBUG`). `w80SacrificeSpendsBlockerClause` is 0-render with the card class PRESENT (Siege-Gang Commander, deck130) — that is a dead-emitter signal, not dormancy: verify its gate `w79SacrificeBlockerGiveBack`. `w81FoldDuplicateCoverParagraphs` is post-corpus, unmeasured. KEEP all; re-measure two |
| KEEP-DORMANT (pool lacks the card class, emitter cheap and correct): `changelingAnnotation` cpp:5530, `mutatedPileTag`, `attackerPoisonNote` cpp:13942, `hybridPipNote(Text)` cpp:49337-49401, `blockCountRequirementTag` cpp:8114, `attackerDestinationTag` cpp:58625, the `[battle` attacker branch, the MDFC/mutate menu annotations (cpp:54423-54446, `mutateHostAsk` 55384), `mdfcLandArrivalTagFrom` cpp:49814, `{adventure spell:` cpp:50464-50494, `[tapped - cannot attack this turn]`/`[tapped - blocking X]`/`[summoning sick … cannot block either]`/`[protected from this damage's source` cpp:5560/5558/5725/3266, `[no cast row now: a play restriction …]` cpp:31289, `zeroPowerBlockerTag` `[deals 0 … already deals 0]` cpp:6923, the `<upto>` / unlimited target loops, the Perimeter-Captain may-batch (2 renders) | | KEEP-DORMANT |

### 1.5 Wave-tag comment archaeology

- 22,186 of 63,671 lines are comment-only (34.8 %); 3,611 lines carry a `//#W<NN>-<lane>` tag; **73 comment lines say something "is/are DELETED"** describing code that no longer exists (`/usr/bin/grep -cE '^\s*//.*\bDELETED\b'`). These are provenance, not description; a future agent reading the seam pays for them on every visit.
- Comments that are now false about the code beside them (each verified):
  - cpp:50358-50360 "same engine call the Mana available line counts with, so the row's remainder and the line's total can never disagree" — rows count with `GptManaPolicy` (cpp:50361), the header with `ManaEngine::FreeProducerPolicy` (cpp:31541-31544); they DO disagree (flaw F5).
  - cpp:31194-31197 "same untapped-source count the Mana available line is built from" — the verdict beside it uses a different pool.
  - h:2386-2389 "`mRetryPark` … never read anywhere else" — read at cpp:20375, 20503-20505, 21031, 22959, 52356.
  - cpp:20349-20365 "Kept as an assertion … rather than as the gate it used to be" — `retryArmMatches` is still `&&`-ed into the gate at cpp:20364.
  - cpp:19583 "`reasoning_tail_answers` DELETED" — sits inside a branch that still takes the tail (cpp:19582-19602).
  - cpp:47655 and cpp:49092 "`multi_answer_first_taken` DELETED" — the counter was re-added in #W81-DM (cpp:22462, 22897).
  - cpp:33926-33932 (the hold row's own comment) describes a fold "tried and REVERTED"; cpp:50324-50327 "until c5 moves payment+clickstream into the manager" — `DecisionManager::planCastSpell` exists (c5a) but this seam still rides `aiForcedCandidate`.
- `stripNarrationDecoration` (cpp:33211-33449) holds a hand-maintained mirror of 58 emitter head literals, including retired spellings (`{it is released at the star` cpp:33375, `{identical in effect right now` cpp:33390) — dead vocabulary that must be kept in sync by hand (complexity X8 proposes the rule that makes the list unnecessary).

### 1.6 Duplicated helpers (real ones; the census's "duplicate definition" pairs are overloads or forward declarations — `parseAttackerSet` cpp:57627 is a declaration of 58244, `w77OwnLoopVerdictKey` cpp:35405 declares 34580, `w74ClockSourceRecurs` and `w79BestBlockDamage` are overloads)

| pair | anchors | note |
|---|---|---|
| `codedChoiceOrdinal` / `firstCodedChoice` | cpp:38522-38560 / 46207-46240 | the same line walk (same `* # -` prefix skip that `scanCodedAnswerLines` cpp:19891-19897 removed as a tolerance), differing only in return value; both exist to make `answer_replaced` truthful — a field that reads `false` on all 1,661 records |
| `offProtocolBytes` / `protocolLinesOnly` / `replyActionBeforePlan` | cpp:21289 / 21349 / 21423 | identical preamble, opposite keep/drop predicate, a third wrapper over the first |
| `w80CrackBackVerdictLine` / `w81CrackBackVerdictLine` | cpp:36546 / 36612 | W81 delegates to W80 when `addenda==0` and otherwise re-states W80's four faces verbatim plus one sentence |
| `holdStillStands` / `w78HoldStillStands` | cpp:36045 / 36184 | same two directions, same two `why` strings; the second is a list-keyed twin kept so PARSETEST can diff waves |
| `drawStepForecastText` / `theirDrawStepForecastText` | cpp:16075 / 16012 | mirrored bodies, dispatched at cpp:16138 |
| `w76HoldReopenNote` / `holdReopenNote` | cpp:34957 / 35495 | the member is a 15-line wrapper that stamps two record fields — fine, but the pair is why the census counted "twins" |
| `lineIsActivatedCost` / `w80ScriptLineHasActivationCost` | cpp:16331 / 29366 | J=0.52 near-duplicates (census §6), both live |

### 1.7 Diagnostics: gating verified

Every diagnostic-shaped block is inside `#if defined(_DEBUG) || defined(WAGIC_DEVLOGS)` (cpp:18099, 18195, 18252, 18905, 19130, 19330, 19372, 23116, 25972, 26165, 43906, 52286, 53749, 54060 — each opened and checked), and `DebugTrace` is `#ifdef _DEBUG` in JGE. Not gated, compiled into release: the env switches `WAGIC_GPT_NOTHREAD` (GptConfig.cpp:1637-1640), `WAGIC_GPT_TRANSLOG_STREAM=0` (cpp:21231-21236), `WAGIC_GPT_TRIM_V1` (cpp:24464-24472), `WAGIC_GPT_AUDIT_M_OFF` (cpp:33768-33776), `WAGIC_INFLIGHT_BOUND` (cpp:18829-18838). Three of those five switch back to behaviours replaced waves ago — archaeology in `getenv` form. (The "ship with a disable flag" rule justifies the last two; the first three have nothing left to A/B against.)

### 1.8 Reply-parser branches that cannot fire under the thinking-on regime (dormant, not dead — kept as safety net, listed so the owner sees their size)

`parseChoice` cpp:45233-46156 (≈920 lines) — echo/name reconciliation, stale-echo discard, numeric disambiguation, superstring fallback, amount-menu relaxed pass; `choiceLineIsClean`/`choiceLineIsRejection` tables cpp:24896-24940; the three re-ask lanes at the ask seam (cpp:49158-49222) and the four at the priority seam (cpp:47826-47920); `latchedCodedChoiceLine` cpp:39414; `latchedRowMismatch` cpp:44577. Corpus: 0 stale echoes, 0 conflicts, 0 re-asks, 0 rejections, 2 parse notes in 2,135 decisions. See complexity X4 for the fold.

### 1.9 Corrections to the census's Tier-A list (so nothing gets deleted on a false premise)

`revealSinglePickDeclineLegal` (cpp:61240) is called from `src/AllAbilities.cpp:1348-1350` (the reveal driver), not only from PARSETEST; `gptNoteEngineRevealFloor` (cpp:21787) and `noteRevealStall` (cpp:22791) are called from `AllAbilities.cpp`; `asyncDropTraceLine` (cpp:19305) has three callers; `gptPadlogFile` (cpp:18100) and `gptTracePromptDrift` (cpp:19131) are used through the `GPTASYNCLOG`/`GPT_DRIFT_TRACE` macros inside the dev gate; `landEntersTappedTagFrom` (cpp:49966) has four callers. The census's "26 no-caller helpers / 825 lines" is really ~14 functions (1.1); its "zero-render verdict variants" include latch MARKERS (`[crack-back verdict: none/LETHAL/you survive]` cpp:36031-36037, `[stack death verdict: nothing lethal…]` cpp:35946-35952) and RECORD faces (cpp:36692-36703) that are never meant to render — a category error, KEEP.

**Section total: 22 named crud items (C1–C22) + 27 dead counters + 53 unread record fields + 73 archaeology lines + 5 stale-switch envs.**

---

## 2. NEEDLESS COMPLEXITY

### X1. Fourteen gates on one window, six of which saved six round trips in 21 games

The priority seam (`chooseOrderedAction` cpp:46513-48193) and the casting seam (`FindCardToPlay` cpp:50098-52132) each decide whether to SHOW a window through a stack of independent latches, evaluated in this order:

| gate | anchor | corpus effect (42 seat-games) |
|---|---|---|
| repeat-N plan dispatch | cpp:47138-47216 | — |
| pass-decline cap (row withheld after 2 declines / 1 for fetches, same board) | cpp:46743-46758 | 2 windows auto-passed (stderr `all actions pass-declined`), rows withheld: unrecorded |
| animation-row withhold for the turn | cpp:46702-46711 | unrecorded |
| flip-toggle cap | cpp:46689 | unrecorded |
| stop-reached collapse (W72) + re-put collapse (W78) | cpp:47041, 47054 | **1** + 0 |
| all-toggle auto-pass | cpp:47237 | unrecorded |
| mana-only auto-pass | cpp:47275 | 78 |
| model's own HOLD row (per seam) | cpp:47335 / 51829 | 1,051 |
| sibling-window cover (W72-BU) | cpp:37970-38015 | **0** |
| reserve-decline latch (W67) + turn-span note | cpp:51850, 38366-38410; note cpp:51879-51893 | **1** skipped, 9 notes |
| loop/chain auto-pass | cpp:47342 / 51863 | 0 (`chain_windows_collapsed`) |
| deadlock breaker (forced pass on unchanged key + unmoved board) | cpp:47516 | 0 (`menu_pass_no_progress`) |
| priority last-answer replay (`askKey == mLastAskKey`) + W81-DI replay bound | cpp:47494, 47543-47582 | cached replays (not on the record); reasks 0 |
| ask cache (state+question) + repeat latch (K10) + cross-phase replay (U8) + SBA bypass | cpp:48854, 48912, 49009-49030, 48848 | 1,172 / 0 / **4** / — |
| `mStuckCastLines` (cast row withheld for the turn after a no-op) | cpp:50368, 51541, 52101 | unrecorded |
| land-face decline latch (`faceDeclinedThisTurn`) | cpp:50142 (DecisionContract.cpp:640-666) | 0 in pool |

**Simpler equivalent.** One window identity per seam — `(turn, phase, stack handles, untapped-source count, action-key row set)` — with one memory per seam `{last answer, wasHold}`; a window is not sent iff the identity is unchanged and (wasHold or a cached answer exists). That single test subsumes the hold latch, the sibling cover, the reserve decline (a hold-row case: same rows, same step, same sources), the cross-phase replay (same rows at another phase over an unchanged board is the same identity), the stuck-cast latch (an unchanged board after a take is the deadlock breaker's case) and the pass-decline cap. The stop-reached collapses are answered by the model's own hold row (it exists on every priority window since #W55-A). **What it loses, honestly:** 6 skipped round trips per 21 games (sibling 0, cross-phase 4, stop-reached 1, reserve 1, chain 0) and the per-cause skip counters — against ~2,000 lines of latch code plus their PARSETEST pins. The pass-decline cap and the animation/flip withholds are not "loss" but a doctrine breach (flaw F3), so their removal is a fix.

### X2. Ask-key construction — eight normalisers of one row set, and a drift from the ruling

The owner's rule is "ask/replay keys are BOARD STATE + QUESTION". The current key is not that: `w79AskScopeKey(turn, phase, continuationDigest) + w77KeyTailOf(tail)` (cpp:48805-48807, 47563) — the serialised board was REMOVED from the key in #W79-CZ (T3) "the seam scope, not the whole serialised board" and replaced by a digest of legal continuations (`w79ContinuationDigestPriority` cpp:37138, `…Cast` 37166). The consequences are visible in the code that had to be added afterwards: the deadlock breaker now reads the board separately (`mLastProgressBoardKey`, cpp:47504-47535, "the breaker therefore reads the board itself, which is the only thing that can say whether the game moved"); the W81-DI replay bound exists because "a cached answer … stood once for this window and the board moved under it" (cpp:47543-47582); the cross-phase entry keeps its own `boardKey` (cpp:49005); the stale-drop breaker keys on `mStaleDropBoard` (cpp:19717-19725). Row normalisation runs through `w77KeyTailOf` → `w78StripSharedCardTextHeader` → `stripDeclineReaskTags` → `w76StripBalancedAnnotationGroups` → `w79ActionNormalisedKeyTail` (cpp:37235-37244), while the hold latch uses `holdActionKeyRow` (cpp:35913-35937: `w76StripBalancedAnnotationGroups` → `w77StripTargetPreviewNumbers` → `stripRenderAnnotationsLc` → `holdKeyLifeProjectionsNormalised` + `w75CostGroupsKey`), the bracket uses `holdKeyRow` (cpp:35652), the decline count uses `optionSetKeyOf` (cpp:34020), the repeat latch `repeatAskKey` (cpp:48462), the cross-phase entry `w76CrossPhaseListKey` (cpp:48525), and the sibling test `w73SiblingBoardKey`. **Simpler equivalent:** one `actionKeyOf(row)` (strip every `{…}` and `[…]` group except `[cost: …]` and mana symbols) and one window key = `serializeGameState() + question + join(actionKeyOf(rows))` — which is the ruling's key, byte for byte. **Honest loss:** the 233 same-phase re-asks #W79-CZ measured when a life total ticked under an unchanged menu — i.e. more round trips on loop boards. That is the trade the ruling already chose; the owner should say whether #W79-CZ's departure from it stands.

### X3. The two-arm retry park/restore + forced-close deferral machine

`GptRetrySlot` (h:385-411), `gptRetrySelectArm`/`…Close` (cpp:~20191-20211), `gptForceCloseArm/Outstanding` (cpp:~20213-20224), `w76ForceCloseDecision`, `w78ForceCloseBusy/SameArmBusy` (cpp:~20275-20295), `selectRetryArm` (cpp:20297-20328), plus 11 seat members mirroring the 10-field struct, an 8-tick deferral bound (`kW76CloseDeferMaxTicks` cpp:20266), four counters and a record kind. **What it compensates for:** flaw F2 — the armed leg is gated on byte-equality of the whole prompt, so it is dropped on the next tick 13/13 times. Waves 74–78 built parking, deferral and same-arm bounds around a symptom whose cause none of them touch. **Simpler equivalent:** one `RetryLeg {tag, slotKey, prefill, firstLatencyMs, budgetMs, phase1Length}` INSIDE each `AsyncState` (the arms already have separate slots `mAsyncState`/`mAsyncLandState`, h:1127/1135), gated on `slotKey == asyncSlotKey(userMsg)`; no swap, no park, no ownership stamp, no deferral (two slots hold two legs). **Loss:** the `forced_close` outcome taxonomy and its interleaving pins.

### X4. Reply parsing tuned for a regime the owner has ruled out

Under thinking-on the reply is two lines and the corpus shows exactly that (0 conflicts, 0 stale echoes, 0 re-asks). What remains resident: `parseChoice`'s five-tier echo reconciliation (cpp:45233-46156), `findAnswerLabelLine`'s clean/rejection grammar (cpp:24896-24940, 25137-25236), `gptInlineChoiceOnPlanLine` (cpp:25072), the label-less reader `w79LabellessAnswerLine`, three walks over the same coded lines for telemetry (`firstCodedChoice` 46207, `codedChoiceOrdinal` 38522, `latchedCodedChoiceLine` 39414), and seven one-shot re-ask latches (`mAskReaskKey/Line/Kind`, `mAskNoopReaskKey`, `mPriorityReaskBoard/Line/Kind`, `mPriorityNoopReaskBoard`, `mDiscardReaskKey/Line`, `mCeilingReaskDoneBase`, `mRetryDoneBase`: h:~1815-1817, ~1837-1838, ~1843-1844, 847, ~2379-2380 — each "at most one correction of kind K per question"). **Simpler equivalent:** keep (a) the label walk with the order rule (answer must follow PLAN), (b) the label-less reader, (c) exact short-name binding with number-vs-name disagreement → ONE re-ask, and (d) one `map<askKey, map<kind,int>>` for re-ask budgets consulted in `pollCompletionRetry`; drop the word-tier matcher, the stale-echo discard (it hands a legal in-range answer to the heuristic — flaw F9), the affirmation/rejection tables and the ordinal/latched walks. **Loss:** protection against a thinking-off model writing prose around its answer — a regime the owner has ruled out; and the `answer_replaced`/`latched_line` telemetry (always false / unread).

### X5. The hold contract spelled on five surfaces

One mechanism (the model's HOLD row) is stated by: three row spellings (`kHoldPriorityRowText` cpp:33939, `…Activation` 33955, `…Cast` 33965, ~420 B each) + `holdRowBenefitClause` (cpp:~34040) + `holdContractParagraph` `[HOW A HOLD ENDS …]` (cpp:34098-34111, 711 B) + the `[hold check: …]` bracket (`w76HoldReopenNote` cpp:34957, 220 B) + `{closes ONLY this window …}` on the plain decline (`plainDeclineScopeClause` cpp:36004-36011, 144 B). Per prompt on 65 % of prompts: ~1.65 KB; corpus total 2.3 MB = **8.2 % of all prompt bytes** on one rule. The bracket's arithmetic (rows new/gone "since the last window I asked you at this seam") is computed through `W76HoldMemory`'s nine parallel maps (h:336-379) and a commit-on-record protocol (`w76HoldWindowAsked`, `w76HoldWindowNotAsked`) whose only purpose is to make that sentence true. **Simpler:** the rule once in the system prompt (cached prefix), the row as `N. Hold priority (rule: HOW A HOLD ENDS)`, and drop the bracket (its value to the decision is a count the model cannot verify — §5). **Loss:** the bracket's "N rows new" hint; nothing else.

### X6. The W68→W81 crack-back verdict lineage (one fact, nine resident functions, four re-walks per window)

`crackBackTotalOver` (cpp:29064) → `crackBackVerdictKey` (36030: threshold → English marker) → `crackBackVerdictNow` (37355) → `w79VerdictDangerRank` (34616: marker → int by substring-sniffing "LETHAL"/"none") → `w80CrackBackBestBlockFloorNow` (37470: re-walks the board, runs the O(3^n) DP) → `w80CrackBackVerdictLine` (36546) → `w81CrackBackVerdictLineNow` (37552: re-walks `crackBackTotalOver` AGAIN, calls `crackBackFloorSources` which the header and `crackBackScreenTotal` (29196) already called) → `w81CrackBackVerdictLine` (36612) → `w80CrackBackFaceOfLine` (36692: greps the rendered English back into a record face) → `w80CrackBackThreatIdentityNow` (37414) → `w81ApplyRenderEventsAtSend` (37572). The same shape exists for the own-loop verdict (six functions, §1.4) and the stack-death verdict (three). **Simpler equivalent:** compute ONE `CrackBackFacts{rawCombat, addenda, addendaUnsized, compulsoryDraw, bestBlockFloor, attackerIds, selfActive}` per window in `serializeGameStateImpl`; derive the marker (an enum), the rank (its ordinal), the line (one renderer) and the record face (the enum) from it; hand the struct to the rows. **Loss:** byte-for-byte PARSETEST diffs between waves' twins — which is the only reason the twins exist; and the "face recovered from prose" discipline, replaced by something strictly more reliable.

### X7. Three untapped-source counters, two policies (owner of flaw F5)

Header cpp:31541-31544 (`ManaEngine::FreeProducerPolicy`), cast rows cpp:50361 (`GptManaPolicy`), `windowReach()` cpp:43095 (FreeProducer, memoised, feeds `{spends…}` and the fetch clause). One `mWindowReach` + one sources list per render, one policy, printed by header/rows/X-pricing/strand/hand tags. Loss: none — the split is what produces F5.

### X8. Emitter vocabulary mirrored in the stripper

`stripNarrationDecoration` (cpp:33211-33449) lists 58 `{…}`/`[…]` heads that must be removed from narrated decisions — one entry per emitter, maintained by hand, already carrying retired spellings (§1.5). The rule it encodes is "every brace group except a mana symbol is decision-time furniture" (its own comment cpp:33195-33210 says the option furniture is by construction decision-time). **Simpler:** strip every `{…}` whose body is longer than a mana symbol; keep the bracket rule. Loss: none today.

### X9. Per-row emitter triples for one number

`leavesUntappedTag` `{leaves K of your M untapped mana sources untapped}` (cpp:8417) + `strandsHandCardTag` `{spends K of your M …; X in your hand needs N}` (cpp:40618) + `menuFitTag` `{leaves K sources - …}` (cpp:40658): three clauses, one K, on the same row (corpus: 1,143 rows carry ≥ 2). Fold to one clause: `{leaves K of M; rows 2,3 need more}`.

### X10. Replay-run bookkeeping ×3 and transport-outcome stamps

Three re-serve caches (ask cache, repeat latch, priority `mLastChoice`) each with their own run counter (`mAskReplayRuns` h:727, `mRepeatAskRuns` h:870, `mCachedReplayRuns` h:2239) and refusal (`askReplayRefuseScoped` ×2 sites, `w81CachedReplayMustReask`), plus the dead pair (C17). One map keyed by ask key, one cap. — `transportOutcomeStamp` ×2, `transportPhaseFor`, `transportRecordField`, `wallMissClassFor`, `attemptMsField`, `deadlineTenthsPct(OfAttempts)`, `retryableTransportFailure`, `remainingTransportRetryMs`, `gptDeadlineMissed`: ~200 lines to produce `curl=0,http=200,empty=0` (2,135 identical, 0 readers). Four JSON scalars (`curl`, `http`, `timed_out`, `attempt_ms[]`) carry the same facts.

**Section total: 10 complexity items (X1–X10).**

---

## 3. FLAWS — ranked by effect on decisions

Rank order: (1) a legal option removed or a model answer discarded; (2) a rendered fact that is false or in the wrong scope on a decision the corpus actually put; (3) loss of round trips / waste; (4) latent-but-armed (dormant in this pool).

### F1 [HIGH — false render on 43 real windows] The `[crack-back verdict:]` line is rendered on the OPPONENT's turn with own-turn semantics
- The `CRACK-BACK NEXT TURN` line is gated to the seat's own turn: `crackBackNextTurnDue(selfActive, …)` cpp:29032-29035 (`if (!selfActive …) return false;` — "on the OPPONENT's turn the line is still silent"). The verdict LINE has no such gate: `w81CrackBackVerdictLineNow()` is called unconditionally at cpp:47453 (ask/priority) and cpp:51788 (cast menu), off `crackBackTotalOver` (cpp:29064) → `crackBackBodyContribution` (29049) → `crackBackBodyUntaps` (28226), which counts their TAPPED and summoning-sick bodies as "untap first".
- On their pre-combat windows the text says "if you pass this window and they attack with everything that can … a hold here is taken over a board that kills you **next turn**" — the attack is THIS turn's, and their tapped creatures do not untap first: CR 502.3 "the active player determines which permanents they control will untap"; sick bodies cannot attack: CR 302.6. On their post-combat windows the seat's own tapped creatures WILL block next turn, but `w80CrackBackBestBlockFloorNow` (cpp:37470-37526) filters blockers with `canBlock()` (cpp:37484, excludes tapped), so "LETHAL EVEN THROUGH YOUR BEST BLOCK" can print over blockers the seat will have.
- Corpus: 43 ask/priority records carry the verdict on the opponent's turn (30 "survive", 13 "LETHAL"). `1789216931-…deck162-…-vs-…deck152.jsonl` seq 24-29 (their Combat damage → Cleanup, seat at 10, 0 creatures) prints "LETHAL if it is UNBLOCKED … kills you next turn" five windows in a row, during and after a combat the `INCOMING` line already priced at 7; `…deck125-…-vs-…deck130.jsonl` seq 101 (their **Blockers** step, seat at 1 life) prints "if you pass this window and they attack with everything that can" while blockers are being declared; `…deck123-…-vs-…deck152.jsonl` seq 66/68 (their Main 1, seat at 14) counts tapped Sigarda (4) + summoning-sick Augur/Wolf — 10 and 12 power where the bodies that can attack this turn total 2 and 5; "you survive" came out true only because life was 14.
- Effect: a lethal-scare during the opponent's turn argues for exactly the panic play the W80-DH comment (cpp:36530-36545) says the verdict must never argue for. Fix: gate on the same `selfActive` predicate (one line), or drop the opponent-turn render (the INCOMING forecast cpp:28133 already states this combat from `canAttack()`-now bodies).

### F2 [HIGH — 13 model answers discarded, 23 min of inference lost] The forced-close (phase-2) rescue has 0 % yield because its leg is gated on byte-equality of the whole prompt
- Gate: cpp:20366 `if (userMsg == mRetryBase)`; abandon branch cpp:20434-20456 (`dropped_decision_moved`). `mRetryBase` is the FULL assembled prompt (cpp:20527, 20549, 20616, 20672, 20686). The async slot itself abandoned full-prompt equality in #W56-A/#W62-fix (cpp:19230-19236, 19312-19327, 24043-24056) precisely because the prompt carries per-tick-moving bytes (hold-check bracket, decline notes, prompt-only verdict lines, narration appends). The retry gate never got the same fix.
- Corpus: 13/13 armed closes dropped on the very next record (`seq 10 → 11`, same turn/phase; e.g. `…deck123-…-vs-…deck126.jsonl` seq 10-11, `…deck152-…-vs-…deck125.jsonl` seq 87-89, `…deck162-…-vs-…deck146.jsonl` seq 25-26). Every drop was followed by a fresh full-price phase-1 ask on the same window (13 re-asks, latencies 35–186 s ≈ 23.4 min), and the wasted phase-1 decode's latency is discarded (`mRetryFirstLatencyMs = -1`, cpp:20452) so the corpus cannot even price it. The phase-1 reasoning trace (5–20 K chars the model paid for) is thrown away each time — an answer discarded, and recorded only as a `forced_close` row with no slot key. Wave 73 measured the same ratio (33 armed / 2 consumed, comment cpp:20356-20358) and "fixed" it with per-arm parking (#W74-CF), which was not the cause. The wall-miss/transport retry leg (cpp:20083-20086) has the identical gate (wave-68: "4 wall misses … 1 consumed, 3 abandoned when the decision drifted"), unexercised here.
- Fix direction: key the leg on `asyncSlotKey` and let phase 2 send the CURRENT prompt with the stored prefill (`buildRequestBody` already rebuilds from `baseMsg`, cpp:44160-44166) — see X3.

### F3 [HIGH — legal options withheld, doctrine] The priority seam silently withholds rows the engine offers
1. **Pass-decline cap**, cpp:46743-46758: a row pass-declined twice this turn (once for a fetch crack) over an unchanged board is `continue`d out of the menu; when every row is capped the window auto-passes with no model call (cpp:46862-46864; corpus stderr: `all actions pass-declined this turn; passing` ×2). The row is withheld because the model previously declined it — that is the engine deciding a legal play is not worth offering, the thing the ruling forbids, and the HOLD row (which is the model's own, explicit version of the same promise, cpp:47070-47110) has made the cap redundant since #W55-A. No record, no rendered bracket; only `DebugTrace`. Wave 65 (#W65-AM G7, comment cpp:46731-46742) narrowed it to "unchanged board" after it cost a game; the narrowing does not make it legal.
2. **Animation row withheld for the rest of the turn** once activated, cpp:46702-46711 (`isAnimationRow` + `aaa->counters >= 1 && isAnimatedLand` → `continue`). A second activation of a fixed-size animation is usually a no-op — "usually" is the judgment the ruling says the engine may not make (a `becomes` line with an "until end of turn" pump rider, or one that adds a counter, is not a no-op).
3. **Flip-toggle cap** `mFlipDoneCount[fcard] >= 2` cpp:46689 — a no-op face toggle, but the same shape.
4. **`mStuckCastLines`** cpp:50368 (armed when the board serialisation is byte-identical after a cast pick), cpp:51541 (row dropped from the cast menu for the TURN, `mStuckCastTurn`), cpp:52101: a legal cast row vanishes for the turn on a trigger that can be the model's own decision (the cast-mode sub-menu's `Decline - do nothing` before any payment: `…deck130-…-vs-…deck146.jsonl` seq 9), with no prompt-side receipt. The row's identity is a hash of the fully annotated text, so any `{leaves N…}` change un-sticks it by accident.
5. **ETB pay-or-tap auto-answer**, cpp:54621-54632: on a shockland-style ETB menu whose permanent is already tapped the seat clicks the `tap` row itself (`act.choice = i; return 0;`, no model call), justified as "paying is a strictly-dominated no-op". Paying life is losing life — CR 119.4 "If a player pays life, the payment is subtracted from their life total; in other words, the player loses that much life" — so "whenever you lose life" triggers make the payment desirable on some boards; and the tapped-state fallback (`etbPayOrTapLand`, cpp:~52741-52761) is "the newest fresh land", a guess. Corpus: never fired (both Agadeem/Emeria menus were asked). Keep the annotation (cpp:~52704-52710), delete the auto-answer.
6. **Target choosers**: an `<upto:N>` chooser cannot be answered with zero targets — `mayStop = multi && !picks.empty() && !tc->targetMin` cpp:55653 (the `Done` row appears only from the second pick) while CR 115.6 permits zero ("may allow zero targets to be chosen") and the engine accepts it (GameObserver.cpp:2330-2332); `DecisionContract.cpp:783` stops enumerating candidates at 40 (`req.targetCandidates.size() < 40`, across hand/library/graveyard/exile); cpp:56321 breaks an unlimited chooser at 12 picks ("sanity bound"). All three cap the legal option set. Dormant in this pool (72 target asks, 0 multi-target).
7. **Land-face decline latch** (DecisionContract.cpp:~640-666 → cpp:50142 `faceDeclinedThisTurn`): declining a land's own face menu removes that land from the land ask for the turn, while `landDropStatusLine` (cpp:~31603-31629) is computed WITHOUT the latch and still prints "Land drop: NOT yet used this turn - you can still play a land"; if it was the only land, no land ask is issued (cpp:~50156-50170) under a line that says one is available. CR 305.1: the play is legal. Dormant in this pool; a true-in-the-wrong-scope render is armed.

### F4 [HIGH — verdict from a different total] The hold-latch marker and the rendered verdict are computed from different totals, and the W81 fold has residuals
- On the W80 binary the corpus itself shows the two totals: `…deck162-…-vs-…deck146.jsonl` seq 30 (= `prompt-ask_mid-1.txt`): header "14 from combat … ADD THOSE UP … 17 … you would be at -10"; the cover clause on rows 2 and 4 subtracts from 17 → "-2"; the bracket verdict "LETHAL EVEN THROUGH YOUR BEST BLOCK … lets 10 through and you are at -3" (14 − Fog Bank's 4). Three "you would be at" numbers for one attack on one screen. Six more prompts have the header saying "that would KILL you" while the verdict says "you survive" (`…deck130-…-vs-…deck152.jsonl` seq 17, 19, 22, 24, 25; `…deck162-…-vs-…deck130.jsonl` seq 43).
- Current code (W81-DL V5, unmeasured): the LINE now folds `floorExtra` (cpp:37552-37565) but (a) the latch MARKER `crackBackVerdictNow()` (cpp:37355-37360) still uses raw combat only, so a hold taken at "you survive" (raw 14 < life 16) is not re-opened when the animated land makes the screen's own total lethal (17 ≥ 16) — the one window the marker exists for; (b) `floorUnsized` is computed and never passed (cpp:37556-37561 → `w81CrackBackVerdictLine` cpp:36612 has no unsized parameter), so when the header ends "and lower still - something listed above adds power this render cannot size" the verdict still prints an unhedged "you survive … leaves you at N" (`…deck130-…-vs-…deck152.jsonl` seq 6 is that board shape); (c) the best-block floor takes every untapped `canBlock()` body while the row cover (`crackBackCoverFacts` cpp:30298) excludes non-vigilance bodies in Main 1 because they may attack — verdict and cover can name different blockers on one screen; (d) the `none` short-circuit (cpp:36618-36619) returns nothing when combat is 0 but a compulsory draw loss alone is lethal.

### F5 [MED — rows contradict the header] `{leaves K of your M untapped mana sources}` and `{X pricing … (M mana total)}` disagree with `Mana available: N total` on the same screen
- Rows price with `GptManaPolicy` (cpp:50326/50361); the header counts with `ManaEngine::FreeProducerPolicy` (cpp:31541-31544); `windowReach()` (cpp:43095) is a third count. The comment at cpp:50358-50360 claiming they "can never disagree" is false.
- Corpus (9 rows): `…deck146-…-vs-…deck152.jsonl` seq 28 (header 2 / rows "of your 3"), 32, 37 (header **5** / rows "of your **6**", `{X pricing: max affordable X=3 (6 mana total)}`), 38 (1 / 2). Board holds `Treasure [artifact]` — the sacrifice-for-mana source the planner spends and the header's source list omits. The `[cannot pay now: needs N mana, you have M untapped sources]` hand tag (cpp:31256) prints the FreeProducer count beside a `payable` verdict computed with `castPool` (cpp:31775).
- Also: `Mana available: N total` is a SOURCE count printed as an amount; with a variable producer (Overgrown Battlement, 22 prompts carry `(VARIABLE output:`) the head number is wrong as mana (`…deck126-…-vs-…deck130.jsonl` seq 10: "3 total" over a Battlement, rows say `{leaves 0 of your 3}` for a {3}{b}{b} and a {4}{b}). Guides key on the head number ("hold up 4 of 7").

### F6 [MED — wrong-scope render, 310 occurrences] `[tapped - cannot attack or block this turn]` on the OPPONENT's creatures during the seat's own turn
- `tappedCreatureTag` (cpp:5553) is unscoped; the summoning-sick twin was scoped to the active player's board for exactly this reason (N-166f, cpp:12497-12499). On your turn "cannot attack this turn" is vacuously true of every opposing creature (only the active player attacks, CR 508.1a) and reads as a statement about the coming crack-back — which the CRACK-BACK line then contradicts with "(tapped ones untap first)". `prompt-ask_mid-1.txt`: `Silverquill Silencer #1 … [tapped - cannot attack or block this turn]` on their board, Main 1, your turn; those two Silencers are 6 of the 14 crack-back power. The fact in scope is "cannot block this combat" (CR 509.1a).

### F7 [MED — false rules claim on a real screen] "Declaring attackers taps only the attacking creatures" beside a `[vigilance]` attacker
- `kAttackersTurnFacts` cpp:302-307 ("Declaring attackers taps only the attacking creatures - never your lands") on every attackers prompt (80), and `bin/Res/ai/gpt/system_prompt.txt` line 3 ("Declaring an attack taps the attacking CREATURES only … and everything you control untaps at the start of your turn"). CR 702.20b: "Attacking doesn't cause creatures with vigilance to tap." `prompt-attackers-0.txt` (`…deck152-…-vs-…deck126.jsonl` seq 12) offers `A2. Briarbridge Tracker (4/3) [vigilance]` under that sentence; nothing on the screen or in the system prompt explains `[vigilance]`, so the model is told its attacker will be tapped for the crack-back it is also being told to weigh.

### F8 [MED — latent, unconditional code path] Double strike is not doubled on the blockers seam; planeswalker-aimed attackers are priced as face damage
- Attackers seam doubles (`rowPower *= 2`, cpp:59087); blockers seam does not: A-row `" deals " << power` (cpp:60271), `combatDamageForecast` (cpp:60141-60152), the INCOMING walk (cpp:32019, 32104). CR 702.4b: a double striker assigns combat damage in both steps. The 1-on-1 kill test (cpp:57019-57020) uses single-step power — a 2/2 double striker vs a 2/3 prints "neither dies". No double striker attacked a GPT seat in this corpus.
- `willattackpw` (the engine's record of an attack aimed at a planeswalker, MTGRules.cpp:2523) is read nowhere in AIPlayerGPT.cpp or DecisionContract.cpp; `buildDeclareBlockers` collects every `isAttacker()` and every A-row/forecast counts it against the seat's LIFE. CR 510.1b: an unblocked creature assigns its damage "to the player, planeswalker, or battle it's attacking". The attackers seam handles the mirror (cpp:58625, 58637, 46384); the blockers seam does not. 3 blockers prompts with an own walker in the corpus; Baka aimed at the player each time.

### F9 [MED — parser discards an answer that is unambiguously there] Three shapes the robust-parser ruling covers are refused
- `parseBlockAssignments` cpp:59860-60047: the range form is accepted only glued and with both labels (`B4-B9:A2`, cpp:59882-59893 requires `-` followed by `B`); `B4 - B9:A2` drops B4 (stamps `blockerInAttackerSlot`) and assigns only B9; `B4-9:A2` drops the whole pair — while the attackers grammar accepts `A4-9` and `A4 - A9` (cpp:58312-58335) and the same range note (`kBlockerRangeNote` cpp:10394) teaches the shape. `B1 blocks A1` (coded pass fails, the name pass's `significantWords("B1")` is empty at the ≥4 floor, cpp:59966-59972) → 0 pairs → heuristic blocks.
- `parseChoice` stale-echo discard: an in-range coded number whose parenthetical matches no row is returned as -1 (head branch cpp:~46016 and the trailing-scan twin cpp:~46120) and the HEURISTIC plays — the number the model wrote was a legal answer. The ruling says the parser reads the answer where it unambiguously is and rejects only an answer that precedes the plan; a number-vs-name disagreement earns a re-ask (the seams already have the `named_row` lane), not the heuristic.
- All dormant in this corpus (0 fires), which is why they rank here and not higher.

### F10 [MED — waste on every decision; a hang risk on handhelds] The whole prompt is rebuilt on every AI tick while the model is thinking
- Both seams build the entire prompt BEFORE polling: priority cpp:47380-47492 (tail, key tail, continuation digest, hold note, three verdict builders, `assemblePrompt` at 47492) → `pollCompletionRetry` at 47618; `askModel` cpp:48568-49061 likewise (`assemblePrompt` at 49039, poll at 49061); `FindCardToPlay` re-renders the whole cast menu (`describeAction` per candidate) every tick. `assemblePrompt` (cpp:24043-24130) runs `serializeGameState`, `logWindowApply` + `narrationBucketRuns` + `narrationFoldPaidSources` over the whole narration and `w81TrimNarrationToBudget` each time; `pollCompletion` returns `kChoicePending` (cpp:19418-19419) only after all of that; `selectRetryArm` (cpp:20297-20328) copies up to ten 20 KB strings per tick when a leg is armed; the crack-back best-block DP (`w79BestBlockDamage`, 3^n × n residual evaluations, ~410 KB of fresh vectors at n=12) runs on every rebuild (cpp:32234 in the blockers header, cpp:37470 in every ask/priority/cast window).
- Cost: `AIPlayerBaka::Act` ticks every 0.07–0.1 s (`initTimer` AIPlayerBaka.cpp:3993-3998); a p50 decision is 87 s → ~900 full 12–27 KB prompt builds per decision on the desktop harness; on Vita/PSP the DP alone is seconds per frame on a 12-body board. Not a hang (all bounds verified: in-flight abandon cpp:18840-18848, `kW76CloseDeferMaxTicks` 8, `kStaleLivelockLimit` 6 — but that last one is 6 FULL round trips ≈ 9 min and resets on any board change, cpp:19717-19725). Fix: check `asyncBusy()` (cpp:18725) at seam entry and return `kChoicePending` before building anything.

### F11 [MED — telemetry stamped on the wrong record] Cross-record consumption of "last" members
- `flushWallMissRecord("wall_miss_no_retry")` (cpp:20601-20609) → `writeTransLog("wall_miss", …)` consumes `mLastLatencyMs`, `mLastTransportOutcomes`, `mLastHttpStatus`, `mAsyncDropStamps`, `mLastReasoning*` (cpp:22155-22160, 22712-22716); the decision record the heuristic then writes for the same window reads `latency_ms -1`.
- `computeActions` cpp:52245-52285 writes the `defer` record through `deferSeat->writeTransLog(…)`, which may be the OPPONENT seat — consuming that seat's pending latency, reasoning, parse note, drop stamps, hold-check refs and skip-trace onto a record about a different seat's menu.
- `mAsyncDropStamps` land on the next record of any kind (corpus: a `casting/…` drop stamped on a `blockers` record, `…deck130-…` seq 25-26); `mAbandonedInFlightSecs` (cpp:18930 → 22422-22427) and `mLastReasoning*` (set on consume, cleared only by a record write, cpp:19640-19648) ride to the next record of any kind.
- 38 ask/priority records render `[crack-back verdict:` with an EMPTY `crackback_verdict` field (e.g. `…deck125-…-vs-…deck152.jsonl` seq 37, 46, 258, 287; `…deck130-…-vs-…deck162.jsonl` seq 21, 25, 27, 30, 38, 40, 44, 51, 53); `crackback_verdict_lines_rendered` sums to 382 against 329 prompts that carry the line and 291 records that carry the face — three numbers for one fact, 21/42 seats disagree. W81-DK (V9) targets this and is unmeasured.

### F12 [MED — an engine answer that cannot be audited] Single-outcome repeat-pay menus answered by the engine on the mana planner's word
- cpp:~55074-55089 answers the menu when `w80SingleOutcomeRepeatMenu` (cpp:~53118-53145) finds every `add N` row priced `paid == 0`; `paid` is `canAfford` OR `ManaEngine::planPayment` under `GptManaPolicy` (cpp:~54793-54826). If the planner under-prices (a producer the policy refuses — the exact W73-CB F1a shape at cpp:~38455-38466), two rows that look identical are not, and a real choice is answered silently. The three corpus records (`…deck152-…-vs-…deck130.jsonl` seq 6; `…deck152-…-vs-…deck146.jsonl` seq 6, 8; all Intrepid Adversary, 21 rows, answered 0) are consistent with 1 source left and 0 payments of {1}{W}, but the record carries `repeat_payments_paid: 0` as a literal and no source count (C21), so a reviewer cannot check it after the fact.

### F13 [LOW — a gate that is either dead or a breach] The cast-seam loop/chain auto-pass
- cpp:51863 calls `loopAutoPassWindow()` inside the cast loop, reached only with `candidates` non-empty (cpp:~51385-51386); `loopAutoPassFor` (cpp:38447-38475) passes only if `!hasAnyLegalAction(p, GptManaPolicy)`. A non-empty cast menu built by `buildCastSpell` under the same policy (cpp:~50328) means a legal cast exists — so either the two oracles agree (dead code) or they disagree (a rendered cast row auto-passed). `LegalActions.cpp:689-748` prices with its own mana computation, so agreement is not by construction. Corpus: `chain_windows_collapsed` 0. The priority-seam call (cpp:47342) is the legitimate one.

### F14 [LOW — wrong scope / misleading wording on real screens]
- `DRAW PUNISHERS` paragraph (cpp:14646-14650): "Every card the OPPONENT draws costs them 1 life to yours" — Ob Nixilis, the Hate-Twisted deals 1 DAMAGE ("deals 1 damage to that player", `planeswalkers.txt:2686-2688`); "to yours" reads as a drain and the DRAW FORECAST two lines down then says "you gain none of it" (`prompt-ask_mid-1.txt`). 208 prompts.
- `Opponent life trend: turn 22: 5` (`opponentLifeTrendLine` cpp:31107) samples the FIRST render of the turn and never says "at the start of"; `prompt-ask_mid-0.txt` prints "turn 22: 5" while the narration shows 5 → 2 during turn 22.
- `{right now: takes 2 damage - you would be at 0 - this row feeds their chain}` (`kChainFeedTail` cpp:34262 via `damagePlayerVerdict` cpp:39710) at life 20 (`…deck130-…-vs-…deck126.jsonl` seq 29 row 7, 43, 50; `…deck125-…-vs-…deck126.jsonl` seq 180): a loop verdict wearing the arithmetic clause's clothes.
- Counterspell hand tag "no legal target on the board" (cpp:31281-31287) for Cancel/Dream Fracture whose targets live on the stack (`prompt-priority-0.txt`).
- `heldBackBlockTag` (cpp:7015-7040) lists defenders as "could not block ANY of their creatures" (8 of 12 renders name Fog Bank, CR 702.3b "A creature with defender can't attack").
- `combatDamageForecast` W80-DF wording "before you act again" (cpp:13893-13897) — the seat receives priority several times before its draw step; "before your next draw step" is the scope. 0 renders here.
- `attackerBlockerCountLine` (cpp:7236) prints "declaring more than 3 attackers leaves at least (your attackers − 3) unblocked" on 22 of 80 prompts where ≤ 3 attackers are offered (`prompt-attackers-0.txt`: 2 attackers vs 3 blockers) — vacuous.
- `buildPregameBottomAskText` cpp:61863 promises "bottomed one at a time in the order you give" while `pregameChooseBottomInner` (cpp:61906 ff., queue fill at ~61961-61967) fills the queue in HAND order, so `PUT: 6, 2` bottoms card 2 first; CR 103.5 says "in any order" — the order is a real choice. `cleanupDiscard` trims an over-pick to the lowest hand positions rather than the model's first-named (silent). 5 bottom records, all single-card.

### F15 [LOW — pointer/lifetime; nothing dangling found]
- No `MTGCardInstance*`/`OrderedAIAction*` crosses the async gap: every seam rebuilds its option vector per tick and maps the index onto that tick's rows (priority cpp:47640-47700; askModel returns an index; `applyChooseTarget` re-validates, DecisionContract.cpp:795-806). Members that hold card pointers (`mLoopAbility`, `mRepeatAbility`, `mLandFacePreCard`, `mCounteredSpell`) are compared, never dereferenced, except that `mLandFacePreCard` is armed before validation (cpp:~50279-50283) and stays armed for the turn if the heuristic then plays something else — an address reused within the turn could answer a face menu from a stale pick (bounded).
- Worker lifetime is safe for seat destruction (`WorkerCtx` owns a `shared_ptr<AsyncState>`, cpp:18178-18190; generation bump cpp:18262-18274). Process exit: `gptShutdownWorkers(2000)` (GptConfig.cpp:392-410) is a 2 s wait; a worker still inside libcurl races `exit(0)` static destruction (h:1118-1120's "cannot leave the thread writing into freed memory" is only a 2 s bound). No process-wide "one call in flight" guard: a duel ending mid-call and a new one starting launches a second concurrent request (`asyncBusy()` is per seat, cpp:18725-18737).
- `aiPatiencePromptAnswer(false)` (cpp:52392-52408) clears `mEndpoint` but leaves `mRetryActivePrompt`/`mRetryPark`/`mForceCloseArmed` armed; `logGameEnd` then counts a `forced_close_unrecorded` for a close the user cancelled.
- May-batch verdict (cpp:~54058-54160) is decremented per consumed ask; a sibling trigger that never arms a menu leaves `mMayBatchRemaining > 0`, and a later window this turn with the same `source|effect|turn` key (DecisionContract.cpp:751-755) inherits the verdict with no ask.

**Section total: 15 flaw groups — HIGH 4 (F1–F4), MED 8 (F5–F12), LOW 3 (F13–F15).**

---

## 4. IMPROVEMENTS FOR THE OWNER'S REVIEW (proposals only)

Owner criterion: "Does the composed document read in a way that optimally helps the model make good decisions, with the minimum tokens needed to provide the full value." Numbers are over the 2,135 corpus prompts (28.03 MB). The system prompt is a cached prefix on every provider that supports it; the user message is not.

| # | proposal | evidence | cost / honest loss |
|---|---|---|---|
| I1 | **Move every invariant sentence out of the per-decision message into the system prompt** (the primer/strategy sections are user-editable and NOT owner-ruled; only the reply-protocol text is). Candidates with their measured per-prompt cost: `[HOW A HOLD ENDS …]` 711 B ×1,383; hold row 571 B ×1,383; `{closes ONLY this window …}` 144 B ×1,093; `[hold check: …]` 220 B ×1,715; the mana line's counting-rule parenthetical + "mana of ANY colour pays a generic cost" ~250 B ×1,826; the per-ask format sentence's "(a worked example of the FORMAT only …)" ~330 B ×1,661; `kAttackersTurnFacts` 392 B ×80, `kBlockersTurnFacts`, the "their untapped blockers" scope paragraph 675 B ×36, `kBlockTradeTrustNote` 290 B ×28, the "AT MOST ONE attacker" rule 197 B ×28; the cover clause's blocking-mechanics prose (`HOW A BODY REMOVES POWER FROM THAT TOTAL …`, ~1 KB per occurrence, 86 occ); `(THEIR card, written from THEIR seat …)` 115 B ×2,238 → once in the opponent-battlefield header. | hold family alone = 2.3 MB = 8.2 % of all prompt bytes; boilerplate total ≈ 13 % (3.6 MB). At p50 87 s/decision this is prefill, not decode — the win is tokens billed and attention, not latency. | Each fact stays in the model's context (system prompt) — nothing is lost; the risk is the one W80-DG U16 recorded: a row that no longer restates its own re-opener "in its own words". Test by A/B (the census's experiment). |
| I2 | **Stop re-shipping rules text the system prompt already carries.** `{effect: "…"}` on battlefield permanents 911 KB (3.25 %, 4,631 occ), `{card text: "…"}` on cast rows 337 KB (1.2 %), `{target text: …}` 80 KB — all copies of `card->text` that `describeDeckCards` (cpp:23176) already put in the system prompt for every card of both decks. Gloss only what the decklist cannot know: tokens, stolen/copied permanents, live text that differs from printed (granted abilities), and the one-line engine gloss on the seat's own non-creature engines (W47-R7's Sanguine Bond lesson). | 1.33 MB = 4.7 % | locality — the model must look up the deck list instead of reading the row. The W47-R7 incident says own-board engines need the local gloss; opponent creature glosses (W77-CT) can go first. |
| I3 | **Collapse identical hand entries**: `Dream Fracture (copy 1 of 2 …) [258-byte HELD tag]; … (copy 2 of 2 …) [same tag]` → `Dream Fracture x2 [tag]`. `handCastTags` is already keyed by name (cpp:31675). | 4,467 duplicate entries, 461 KB = 1.6 % | none |
| I4 | **Shorten the two hand tags that dominate the hand line**: `[no cast row now: sorcery speed - only in your own main phase with an empty stack; right now the phase is X, not a main phase]` (cpp:31262-31268, 2,251 occ × ~400 B) → one hand-line header "nothing sorcery-speed is castable in this phase" + `[sorcery-speed]` per card; `[no cast row now: it must have a target … HELD …]` (cpp:31280-31285, 1,776 occ × 258 B) → `[no target now - HELD: castable when one appears]`. | `[no cast row now:` family = 892 KB = 3.2 % | none (the word HELD keeps T8's scope lesson) |
| I5 | **Narration compaction — the biggest lever.** Narration is 13.6 MB = 48.5 % of all prompt bytes. Line families by bytes: `- Phase: X` 78,209 lines / 1.40 MB (10.3 % of narration — a phase marker is written whenever ANY event lands in the phase, so "Phase: Draw / You drew X" is two lines every turn); "used:" 1.52 MB; draws 1.15 MB; turn headers 0.97 MB; cast 0.93 MB; "resolved and entered/went" 1.38 MB; `Paid {…} for X (N sources)` 0.68 MB. A cast currently costs three lines (`Paid …`, `You cast X`, `Your X resolved and entered the battlefield`); folding to `You cast X ({1}{r}); resolved` and folding the draw step (`Draw: X`) saves ~25 % of narration ≈ 12 % of all prompt bytes. The `WAGIC_GPT_LOGWINDOW` lever (cpp:23765-23854, elide old turns to a summary) exists and was `full` on every corpus record — never A/B'd. | 13.6 MB; ~3.4 MB recoverable by folding alone | the narration is the model's only memory; folds must keep every fact (they do — the fold is syntactic); eliding old turns (the log window) is the lossy one and needs the A/B |
| I6 | **One clause per number on a row**: fold `{leaves K of your M …}` + `{spends K …; X needs N}` + `{leaves K sources - …}` (X9); print `menuFitTag` only when a row is lost; drop `{paying this costs you NO life…}`; fold the three-sentence `ATTACK TOTAL` (7660-7700 region: "puts them at 5. That is NOT lethal: they survive at 5 … they would be at 5"); print the blockers header once (`INCOMING THIS COMBAT` in the board frame duplicates `Unblocked, these attackers deal up to …` in the tail, cpp:60141-60164 vs 27895); `{after this combat: you control 1 fewer blocker …}` (cpp:57462) once per row, not per pairing. | ~0.5 MB | none |
| I7 | **Verdict lines computed once, numbers only** (X6/X7): a `CrackBackFacts` struct feeding header, cover rows and verdict; the cover row becomes `{cover: 17 total; this body + Fog Bank cover 8 → 9 through → you at -2 (floor, not a verdict)}` (~90 B) instead of ~1 KB; the rules prose moves to the primer (I1). Fixes F4 by construction. | crack-back cover 89 KB in this corpus, but 1.9 KB per creature row on wide boards | the "HOW A BODY REMOVES POWER" paragraph exists because a seat once misread cover arithmetic (W64-AK); one primer line covers it |
| I8 | **Telemetry: one fact, one field; a schema a future agent can read.** One `window` record kind per decision with `seam` as a field; side records only for events with no window (`hold_event`, `recovery`, `gamestart`/`gameend`); fold `forced_close`/`wall_miss`/`menu_single_outcome` into fields of the window record they join to (`window_seq`); drop write-only fields (§1.3) or move them behind `WAGIC_DEVLOGS`; keep on `gameend` only counters that are not `grep -c` of a record field; record the slot key and the first leg's latency/reasoning on a dropped retry (today `mRetryFirstLatencyMs` is zeroed, cpp:20452) so F2's cost becomes visible; one header line per translog naming the schema version. The load-bearing dozen: `kind seq window_seq prompt reply reasoning choice chosen_text options_text fallback latency_ms events`. | 86 record keys → ~15; 93 gameend keys → ~20; `crackback_verdict_lines_rendered` 382/329/291 becomes one number | the per-cause counters (which nobody reads) |
| I9 | **Fold the land drop into the casting menu on the seat's own main phase.** 1,369 land asks vs 20 declines (98.5 % "play it"), every one a full round trip over the same board the cast menu re-renders a tick later. Rows `Play Plains` beside `Cast X` remove no option; the cast question re-puts after the land enters (the mana line changes). | ~halves own-main-phase calls; at p50 87 s that is hours per corpus | the land arm's separate async slot (#W57-A) becomes unnecessary — a simplification, not a loss |
| I10 | **Poll first, build second** (F10): `asyncBusy()` at seam entry; cache the assembled prompt + slot key per window and rebuild only on consume/idle. | removes ~900 prompt builds and DP runs per decision | none |
| I11 | **Slot-key the retry legs** (F2, X3): 13 phase-2 requests (~400-token answers over a cached prefix) replace 13 full phase-1 re-asks; `phase2_answer_*` becomes measurable for the first time. | 23 min per 21 games in this corpus | the forced-close taxonomy |
| I12 | **Legibility for the next agent**: keep every WHY, move the incident narrative (seq citations, wave-by-wave measurements, "DELETED" epitaphs) out of the code into `strategy-design/` — the same ruling the owner gave for the deck guides ("provenance goes in review.md"). 22,186 comment lines / 73 epitaphs / 3,611 tags is the dominant reading cost of the file; a future agent needs the invariant, not the biography. Rename wave-prefixed functions to what they do (`w81CrackBackVerdictLineNow` → `crackBackVerdictLine`). | ~15–18 K lines of prose | git history keeps the biography; the risk is losing a WHY that was only in a comment — mitigated by moving, not deleting |
| I13 | **Cleanup-discard prompt in the hand-only frame.** `prompt-discard-0.txt` is 15.9 KB (full game log from turn 1) for an 8-card hand-size discard; `kAskWindowCleanupDiscard` can take the frame the pregame bottom uses. | 52 prompts × ~12 KB | the discard sometimes depends on the board (it stays: hand + CURRENT SITUATION, no log) |
| I14 | **Re-measure before trusting**: the W81 folds (`w81FoldDuplicateCoverParagraphs`, `ONE TOTAL`, send-disposition counting, SBA bypass) have zero corpus measurement; `w80SacrificeSpendsBlockerClause` reads 0 with its card class present; the census's A/B experiment (same commit, Tier A+B removal behind a compile flag, 21 games, judged on per-class misplays not win rate) is the right instrument, sized per the rare-event caution — several removed families (own-loop windows asked: 1) will need more than 21 games to discharge either way. | — | — |

**Section total: 14 proposals (I1–I14).**

---

## 5. WHAT I COULD NOT DETERMINE

1. **Anything about W81 behaviour on a real board.** The corpus predates every W81 lane; the crack-back `ONE TOTAL` fold, the cover fold, the send-disposition counters, the SBA cache bypass and the stack-handle cast digest are judged on code only.
2. **Whether the `[hold check: N rows above are new …]` bracket is ever true.** I re-implemented the row delta with a naive annotation strip and got 891 agreements / 417 disagreements with the printed bracket over 1,308 records; my normaliser is not `holdActionKeyRow` (ordinal realignment, occurrence indexing, cost-group retention), so the disagreements prove nothing either way. The bracket can only be adjudicated by re-running the engine's own key over the corpus — which is the instrument the wave-77 reviewer also lacked (cpp:34860-34870's own comment). Whether the model derives any decision value from a count it cannot verify is a question for the A/B, not for reading.
3. **How often the pass-decline cap, the animation withhold and `mStuckCastLines` actually withheld a row** (F3 1-2, 4). They write no record and no rendered bracket; the only evidence is `DebugTrace` (absent from the release build the corpus ran) — two `all actions pass-declined` stderr lines is the floor, not the count.
4. **Whether the 9 stale-answer drops were justified.** All nine are classed "question and board moved"; the slot key's board half is now the continuation digest (cpp:19321-19326), so a changed legal target set mints a new key — I did not verify that the rows of those nine windows actually changed.
5. **The single-outcome menus** (F12): the three records are consistent with 0 payable {1}{W} but carry no source count, so they cannot be confirmed from the corpus.
6. **Whether `mPregameBottomAsked` (h:615) is reset between games when a seat object is reused** — not checked against the harness's seat lifetime.
7. **The exact per-tick cost on a handheld** (F10): I measured tick cadence and per-tick work from the source; I did not profile.
8. **Delegated-region anchors**: I spot-verified the top finding of each delegated region against the source (F1's gate and call sites, F2's gate, F3-5's auto-answer, F9's range grammar, F13's call, F12's writer) and corrected one (the ETB auto-answer is cpp:54621-54632, not 54704-54717 as first reported). Anchors I did not personally open are marked `~` above; locate by the named symbol before acting.
9. **Rules claims inside the strategy guides** (`bin/Res/ai/baka/deck<N>_strategy.txt`) — out of scope; only `system_prompt.txt` and the compiled primer were checked.

---

## 6. COVERAGE

**Read fully by me (line by line):** the file head — constants, `kRulesPrimer`, `kStrategyPriors`, `kReplyProtocol`, the fact-line constants (cpp:97-500); `assemblePrompt` ×3 and the log-window block (cpp:23758-24130); the answer-selection region — clean/rejection grammar, `findAnswerLabelLine`, `gptInlineChoiceOnPlanLine`, `gptAnswerSegment`, `findPlanMarker`, `planBlockEndOffset`, `w80AnswerPrecedesPlan`, `consumePlan` (cpp:24789-25785); `parseChoice`, `isTemplatePlaceholderLine`, `isExampleEchoLine`, `firstCodedChoice`, the fetch-clause helpers, `naturalTextLess` (cpp:45233-46513); `chooseOrderedAction` (cpp:46513-48193); `askModel` and its neighbours `hybridPipNote*`, `altCostPrintedNote`, `pitchCostNote`, the land-tap gate family (cpp:48568-50098); the hold family — `holdReopenNote`, `holdKeyRow`, `holdActionKeyRow`, `w76StripBalancedAnnotationGroups`, `w77StripTargetPreviewNumbers`, `holdStillStands`, `w78HoldStillStands`, `w79RealignRankShifts`, `stackDeathVerdictKey/RowClause`, `crackBackVerdictKey`, `declinedListNote`, `reserveDeclineCarryNote`, `declineRowReaskTag`, `stripDeclineReaskTags` (cpp:35495-36330); `releaseHoldIfUntapPassed`, `holdHonoured`, `markCastDecisionAnswered`, `takeHold`, `reserveDeclineStillStands`, `reserveDeclineHonoured`, `takeReserveDecline` (cpp:37911-38421); `asyncSlotKey`, `w81StubReplyNext` (cpp:19312-19370); `optionSetKeyOf`, `holdRowBenefitClause`, `w77KeyTailOf`, the hold-row constants, `holdContractParagraph`, `holdRowLine`, `w76HoldReopenNote` (cpp:33923-34135, 34020-34060, 34957-35060, 37235-37260); `crackBackVerdictNow` … `w81ApplyRenderEventsAtSend` (cpp:37355-37600); `describeDeckCards`, `loadStrategyGuide`, `gptTextHash`, `recordSystemPrompt`, `buildSystemPrompt` (cpp:23176-23334); `menuFitTag`/`strandsHandCardTag` and `w81FoldDuplicateCoverParagraphs` (cpp:40610-40680, 40991-41030). The whole of `include/AIPlayerGPT.h` (2,813 lines) and `include/DecisionContract.h`. `bin/Res/ai/gpt/system_prompt.txt`, `reply-protocol.txt`, `README.txt`. The census in full. Eleven sampled prompts in full (`prompt-*.txt` in the scratchpad: 4 asks incl. the largest, 2 priority, 2 attackers, 2 blockers, 1 discard).

**Read fully by the delegated readers (each named its own coverage; I spot-verified as in §5.8):** board render — `serializeGameStateImpl`, `serializeGameState(Pair)`, `describeAction`, `describeZoneCards`, the mana/land/hand/battlefield-header emitters, the crack-back header family, incoming-combat family, draw-punisher family, ~35 helpers; combat — `chooseAttackers`, `chooseBlockers`, `orderBlockers`, both parsers, `combatTradePreviewStats`, the gang/menace/zero-power/held-back tags, the verdict lineage, `DecisionContract.cpp` builders/applies; transport — `WorkerMain`, `AsyncState`, `pollCompletion`, `pollCompletionRetry`, the retry-slot helpers, all record writers, `logGameEnd`, `buildRequestBody`, `computeActions`/`decisionPending`/`decisionArmed`/`Render`, `GptConfig.cpp` worker/spawn/shutdown; non-combat seams — `FindCardToPlay`, `chooseMenuAction`, `chooseCostTarget`, `chooseTarget`, `decideReveal`, the pregame trio, `cleanupDiscard`, the auto-pass family, `DecisionContract.cpp:388-939`, `LegalActions.cpp:689-748`, `AIPlayerBaka.cpp:3410-3545`.

**Sampled (opened in part):** `dynamicMagnitudes` (cpp:4219, first ~110 of ~580 lines), `drawStepExtrasScan`, `drawForecastBlock`, the narration emitters (`describeEvent`, `receiveEvent` cpp:25785-27043 — judged from the corpus line census, not read), `w77TargetedRemovalCoverTag`, `crackBackFloorSources` tail.

**Skipped:** cpp:500-3200 (register/narration constants and the zone-change narration builders), cpp:12178-18033 except the pieces named above (the second anonymous namespace: card-text snippet budgets, dungeon/venture, converters, punisher scans), cpp:27043-27860 (`serializePregameState` and the situation-line helpers), cpp:38514-39400 and 41030-43095 (X-pricing, kill rows, chain feed, loop notes), cpp:44577-45233 (`latchedRowMismatch`, `rowSaysNoOp` internals), `AIPlayerGPTSelfTest.cpp` (not in scope), the Codex/SSE and Android transports, PSP/Vita platform branches, the strategy guides.

**Corpus instruments run:** record-kind and field census; fallback/parse-note/deviation counts; latency and byte budgets; narration line-family census; annotation-family byte census; duplicate hand-entry census; gameend counter sums per seat and corpus-wide; per-seat verdict-counter vs prompt-line vs record-field reconciliation; askreplay sidecar counts; stderr greps for every silent gate; `forced_close`/`recovery`/`hold_event`/`menu_single_outcome` record dumps; a rough hold-check delta check (inconclusive, §5.2); the delegated readers' arithmetic checkers (crack-back life 441/441, ADD THOSE UP 59/59, INCOMING 28/28, ATTACK TOTAL 80/80, 182 printed 1-on-1 trades 0 contradictions, X pricing, `{leaves}` 384 pass / 30 fail on Treasure-Battlement boards, header counts 2,064/2,064).

---

## Appendix A — Comprehensive Rules cited (grepped from `~/.gatelogs/engine-audit/cr.txt`)

- 101.4 — active player makes choices first (turn order).
- 103.5 — London mulligan: "put that many cards … on the bottom of their library in any order".
- 107.4c — {C} colorless mana symbol (generic vs colorless).
- 115.6 — "A spell or ability that requires targets may allow zero targets to be chosen".
- 116.1 — special actions; 116.2a — playing a land is a special action.
- 117.1 — priority; 117.3b — active player receives priority after resolution; 117.4 — all players passing resolves the top object / ends the step.
- 119.3 — life adjustment; 119.4 — "If a player pays life, the payment is subtracted from their life total; in other words, the player loses that much life".
- 120.3 — results of damage (120.3c — damage to a planeswalker removes loyalty).
- 302.6 — "A creature can't attack unless it has been under its controller's control continuously since their most recent turn began" (and {T} abilities).
- 305.1 / 305.2 — one land per turn as a special action.
- 307.1 — "A player who has priority may cast a sorcery card from their hand during a main phase of their turn when the stack is empty".
- 500.1 — turn structure; 502.3 — "the active player determines which permanents they control will untap".
- 506.3 / 506.4 — attacking planeswalkers/battles; removal from combat.
- 508.1a — the active player chooses attackers; 508.1f — "The active player taps the chosen creatures".
- 509.1a — "The chosen creatures must be untapped … chooses one creature for it to block"; 509.1b — blocking restrictions; 509.1c — minimum-blocker requirements.
- 510.1a — combat damage equals power; 510.1b — an unblocked creature assigns damage to "the player, planeswalker, or battle it's attacking"; 510.1c — a blocked creature assigns damage to its blockers.
- 514.1 — cleanup: "they discard enough cards to reduce their hand size to that number".
- 601.2b — X is chosen as the spell is cast.
- 700.2a — modal spells.
- 702.2b — deathtouch; 702.3b — "A creature with defender can't attack"; 702.4b — double strike deals damage in both combat damage steps; 702.9b — flying can be blocked only by flying/reach; 702.12b — indestructible; 702.17b — reach; 702.19b — trample; 702.20b — "Attacking doesn't cause creatures with vigilance to tap"; 702.140a — mutate.
- 704.5a/b/c — life ≤ 0, empty library draw, 10 poison; 704.5g — lethal damage; 704.5j — the legend rule.
- 712.1 / 701.18a — double-faced cards / transform.
