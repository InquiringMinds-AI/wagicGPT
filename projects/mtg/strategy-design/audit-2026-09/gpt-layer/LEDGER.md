# GPT-layer audit LEDGER (2026-09-15) — every finding with BOTH reviewers' verdicts

Sources in this directory: review-astra.md (Codex Astra first pass), review-fable.md (Fable first pass), cross-fable-seat.md (Fable verifying Astra's findings), cross-astra-seat.md (a second Fable instance standing in for Astra — Codex pool exhausted — verifying Fable's findings and defending Astra's). Owner's concern this answers: "possible that they will agree on findings with no overlap" — after the cross round every item below carries two verdicts. Verdict shorthand: C = confirmed by the other reviewer; R = refuted; ? = could not determine; the reviewer's own rank noted where they differ.

Corpus caveat (both): the wave-80 corpus predates the W81 lanes; every W81 mechanism is judged on code only.

## A. FLAWS — two-reviewer CONFIRMED (the cleanup lane's FIX list; ranked by effect on decisions)
| # | defect | anchors | verdicts | cleanup action |
|---|---|---|---|---|
| L1 | LEGAL OPTIONS WITHHELD / AUTO-ANSWERED against the ruling — one family, nine shapes: pass-decline cap (+ all-declined auto-pass), animation-row withhold, flip cap, `mStuckCastLines` row drop for the turn, ETB pay-or-tap auto-answer (CR 119.4), `<upto:N>` chooser cannot answer zero (CR 115.6), 40-candidate target cap, land-face decline latch (+ `landDropStatusLine` lies about it, CR 305.1), single-outcome repeat-pay menu answered on the planner's word, one-option / byte-identical-row asks answered without asking | cpp:46689, 46702-46711, 46743-46758, 46862-46864, 50368/51541/52101, 54621-54632, 55653, DecisionContract.cpp:783, 50142, 55074-55089, 48661, 48675-48679 | Astra F1+F10 ≡ Fable F3.1-7+F12; C both ways, HIGH both | REMOVE every cap/withhold/auto-answer; where the engine offers a row the model sees it. The pass-decline cap's "last offer" bracket goes with it. EXCEPT the one-option ask (a menu with exactly one legal row is not a choice — owner's own mechanism-vs-choice line; keep, but it is listed in §D for his eye) |
| L2 | ASK/ASYNC KEYS ARE NOT BOARD STATE + QUESTION: key = turn+phase+continuation digest+normalised rows; `boardStateKey` computed and omitted; three compensations built around it (deadlock breaker, W81-DI bound, cross-phase boardKey) | cpp:48796-48807, 47393, async 19321-19326 | Astra F2 ≡ Fable X2; C; Fable conceded HIGH | RE-KEY on board state + question (the ruling); the compensations that exist only because the board was missing come out |
| L3 | THE IN-FLIGHT WATCHDOG NEVER RUNS on the ordinary path: `AIPlayerBaka::Act` returns at 5878 when `decisionPending`, `pollCompletion` (where the abandon lives) is never reached; the bound "was born unreachable" (b192afd1c 07-12 / b9feba4e0 09-03) | AIPlayerBaka.cpp:5875-5879, cpp:52317-52334, 18855 | Astra F3; Fable C (and conceded its own F10) | Move the deadline check into `decisionPending` (the gate that IS reached every tick) |
| L4 | WORKER CAN DESTROY A LOCKED MUTEX: generation-mismatch branch `delete ctx` under `lock_guard` on `ctx->state->mtx` when the ctx holds the last shared_ptr | cpp:18263-18274 | Astra F4; Fable C (narrowed its F15) | Release the guard before the delete (scope the lock) |
| L5 | CRACK-BACK VERDICT FAMILY — three defects, one fix: (a) rendered on the OPPONENT's turn with own-turn semantics (47 windows, 13 LETHAL; CR 502.3/302.6); (b) verdict, cover clause and header computed from different totals (three "you would be at" numbers on one screen; latch marker still raw combat); (c) W81 addenda (animated lands, joint affordability) folded in as "damage no block removes" | 29032-29037 vs 47453/51788; 37355-37360 vs 37552-37565; 28817-28847, 36612-36627 | Fable F1+F4 ≡ Astra F5 (+I1); C both ways, HIGH | ONE `CrackBackFacts` computed once per window, gated on own turn, feeding header, cover rows, verdict and latch; addenda stay blockable-or-not by their own rule |
| L6 | ROW MANA vs HEADER MANA: rows price with `GptManaPolicy`, header with `FreeProducerPolicy`, `windowReach` a third; the comment says they "can never disagree" | 50326/50361, 31541-31544, 43095 | Fable F5+X7; Astra C (4 records) | One policy, one count, one comment |
| L7 | FORCED-CLOSE RESCUE HAS 0% YIELD: phase-2 leg gated on byte-equality of the whole prompt; 13/13 closes dropped; 23 min of inference lost; loss unrecorded (`mRetryFirstLatencyMs=-1`) | 20366, 20434-20456, 20452 | Fable F2; Astra C | Slot-key the retry legs (Fable I11) or delete the rescue machine (Fable X3) — the lane picks the smaller |
| L8 | PARSER refuses answers the robust-parser ruling covers: label-less answer rejected when prose follows (Astra F6); blocker range form `B4 - B9` / `B4-9` dropped while the attackers grammar accepts them (Fable F9); stale-echo coded number returned as −1 → heuristic plays; legal no-op rows re-asked as a semantic veto (Astra F7) | 9020-9022, 9075-9076; 59882-59898; ~46016/46120; 39300-39303, 49143-49160 | C both ways, MED | The parser reads it; the no-op re-ask goes |
| L9 | WRONG-SCOPE / FALSE RENDERS: `[tapped - cannot attack or block this turn]` on the OPPONENT's creatures on our turn (310); "declaring attackers taps only the attacking creatures" beside a vigilance attacker (CR 702.20b); double strike not doubled on the blockers seam (CR 702.4b); "enters TAPPED — no mana this turn" turn-long (Astra F8); `landDropStatusLine` says ALREADY USED with no land played (14 prompts) / NOT yet used under the face latch (Astra F9 ≡ Fable F3.7); DRAW PUNISHERS "to yours" for damage; counterspell "no legal target on the board" when targets are on the stack; defender "could not block ANY"; life-trend sample; pregame bottom order not honoured (CR 103.5) | 5553-5562; 302-307 + system_prompt.txt:3; 60167-60283; 49992-50005; 9694-9720/31612-31632; 14645-14650; 31281-31287; 7015-7040; 31107; 61863/61961 | Fable F6/F7/F8/F14, Astra F8/F9; C both ways | Fix each; every fix is a truth fix, none removes an option |
| L10 | TELEMETRY on the wrong record / unbounded: wall-miss record consumes the window's "last" members (Fable F11); `mAskScopeDigest` never retired (Astra F11) | 20601-20609, 22155-22160; h:2226, 48815-48823 | C both ways | Stamp per window; retire per turn |
| L11 | Cast-seam loop/chain auto-pass is dead-or-breach (Fable F13) | 51562-51563, 51863, 38447-38475 | Astra C | Delete the call (a non-empty cast menu means a legal cast exists) |

REFUTED and dropped: Fable F10/I10 (prompt rebuilt every tick — the seam is not re-entered in flight); Fable's "worker lifetime safe" sub-claim.

## B. CRUD — two-reviewer CONFIRMED DELETE
Helpers with no production caller: `loopCautionForBoards` 27536; `combatDirectiveNegatedOnLine`/`combatDirectiveExampleEcho` 57986/58016 (+ block 58033); `askReplayRefuse` 20904/h:734 (+ its pin); `loopAutoPassApplies` 38421/h:2054; `w80CrackBackVerdictLineNow` 37528/h:2294 (superseded by L5's fix anyway); the `vector<int>` `w79BestBlockDamage` 30764 (self-test only → move); one of `lineIsActivatedCost`/`w80ScriptLineHasActivationCost` (EXACT duplicate, 16331/29366). Folds: `noAnswerClassFor` 4 overloads → 1 (18951-18999); `transportOutcomeStamp` 3-arg (19029); `gptRetrySelectArmClose` (20205). Dead members/vars: `retracted` + `retracted_choice` class (47680/48125/49104/49334); duplicate `mPlanSetTurn`/`mCurrentPlan` assignment 25744-25745; obsolete `planStart/planEnd` in `consumePlan` 25581-25583; `mAskReplayKey`/`mAskReplayRun` h:716-717; `mCeilingReasks` h:848; `mStatedStopCount` h:1796; `mStubReplyIndex` → inside the dev gate; `repeat_payments_paid` literal-0 field; duplicate `force_close_arms_deferred` gameend row 22960-22961; `plan_echo_count`. Comments verified FALSE about the code beside them (Fable §1.5 list, Astra §1.4): 50358-50360, 31194-31197, h:2386-2389, 20349-20365, 19583, 47655/49092, 33926-33932, 50324-50327, 46626, 25556, 48903 — rewrite to the invariant or delete; the 73 "DELETED" epitaphs → delete (provenance lives in strategy-design/).
REFUTED (KEEP, they have callers): `declineRowAtFact`, `shortDecodeGarbage`, `w72HeldMenuShowedEveryRow`, `w71EveryBranchIsANoOp`, `wagicGptParseSelfTest` (SDLmain entry), `revealSinglePickDeclineLegal` (engine-called), `gptNoteEngineRevealFloor`, `noteRevealStall`, `asyncDropTraceLine` (gated). KEEP-DORMANT (pool lacks the card class): changeling, mutate, poison, hybrid pips, block-count requirement, attacker destination, MDFC, battles.
Zero counters (26, both reviewers): NOT deleted on "no consumer" alone (the generic dump reads them); each is deleted only where a per-record field already carries the same fact (Fable §1.3 names the joins) — the lane applies that rule, not the list.

## C. NEEDLESS COMPLEXITY — two-reviewer CONFIRMED, folded into the lane where a flaw fix already touches the mechanism, otherwise §D
Folded into A: X2 (keys) → L2; X6/X7 (verdict lineage, three mana counters) → L5/L6; X3 (retry machine) → L7; X4 (parser lanes for the ruled-out regime, the re-ask lanes) → L8; the duplicate scanners / forwarders → B. Owner's review (§D): X1 (fourteen gates on one window — most ARE L1 and go; the rest, the hold family, are a design), X5 (hold contract on five surfaces, 8.2% of bytes), X8 (emitter vocabulary mirrored in the stripper), X9 (per-row triples for one number), X10 (replay bookkeeping ×3), Astra's typed-verdict / "render only at the boundary" shape.

## D. IMPROVEMENTS FOR THE OWNER'S REVIEW (proposals only, both reviewers' evidence; nothing here is done)
| # | proposal | evidence | cost / loss |
|---|---|---|---|
| P1 | Move invariant sentences out of the per-decision message into the (cached) system prompt: hold contract paragraph 711 B×1,383, hold row 571 B×1,383, hold-check bracket, mana counting-rule parenthetical, seat-voice reminder (2,238 occ) | Fable I1, Astra C; hold surfaces = 8.2% of all prompt bytes (X5) | the per-window copy is what the model reads when it decides; a primer sentence is further away |
| P2 | Stop re-shipping card text the system prompt already carries (`{effect:}` on permanents, `{card text:}` on cast rows, `{target text:}`); gloss only what the decklist cannot know (tokens, stolen/copied) | Fable I2; Astra: 0.98%+1.2%+0.3% by balanced-brace census (Fable's 3.25% not reproduced) | a permanent's current text can differ from its printed text |
| P3 | Collapse identical hand entries (`Dream Fracture x2 [tag]`) | 3,824-4,467 duplicate entries, ~1.6% | none |
| P4 | Shorten the two dominant hand tags (`[no cast row now: sorcery speed…]` 4,542 occ / 3.18%; the HELD-instant tag) into one header + a short per-card tag | Fable I4 ≈ Astra I2 | per-card exceptions must survive |
| P5 | Narration compaction: narration = 48.5% of all bytes; `- Phase:` marker written on every event (78,209 lines, 10.3% of narration); "used:" 1.5 MB; turn headers 1 MB | Fable I5, Astra C on totals | the owner's GAME LOG register ruling governs the shape |
| P6 | One clause per number on a row; print `menuFitTag` only when a row is lost; fold the three-sentence ATTACK TOTAL; blockers header once | Fable I6 ≡ Astra I1 | none claimed |
| P7 | Fold the land drop into the casting menu on the seat's own main phase (332 land asks, 0 declines; every one a full round trip) | Fable I9; Astra corrected the numbers, proposal stronger | the land question re-puts after the land enters |
| P8 | Telemetry schema: one `window` record per decision with `seam` as a field; side records only for events with no window; drop write-only fields or gate them | Fable I8 | the tools that read the current shapes |
| P9 | Cleanup-discard prompt in the hand-only frame (52 prompts, p50 17 KB with the full log) | Fable I13, Astra C | the discard sometimes depends on the board — keep CURRENT SITUATION, drop the log |
| P10 | Emit only the range grammar the menu uses (`kOptionRangeNote` 1,675 B on 99 prompts, 0 use the X forms) | Astra I3, Fable C | renderer metadata |
| P11 | Whole-order answer for blocker ordering (one decision instead of N) | Astra I4; single-sourced, uncontested | an action-format extension = owner approval of a reply shape |
| P12 | Make the layer navigable by responsibility (facts/presentation, parsing, lifecycle+transport, telemetry) with typed facts instead of prose→key→rank→telemetry; move incident narrative out of the code | Astra I5 ≡ Fable I12; 22,186 comment lines / 3,611 wave tags | a large mechanical move; behaviour-free if done as a move |
| P13 | Re-measure before trusting the W81 folds (zero corpus measurement) | Fable I14 | one corpus |

## E. SINGLE-SOURCED after the cross round (not in the lane; listed so the owner knows)
Fable's `parseChoice` internals (~920 dormant lines), the transport stamp grammar, the full 53-field unread list, the narration line-family byte split, several cited example prompts, may-batch inheritance, the `hasAnyLegalAction`-vs-`buildCastSpell` agreement (Fable F13's dilemma), Astra's I3/I4. Neither could determine: how often the withholds fired (no record), whether the nine stale drops were justified, W81 on a real board, per-tick cost on a handheld.
