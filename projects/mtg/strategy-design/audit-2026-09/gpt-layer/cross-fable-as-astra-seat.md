# GPT-layer audit — cross-review of fable.out (by the astra.out reviewer's seat)

Read-only, 2026-09-15. Repo `/home/magi/Projects/wagicGPT` at `e71f73c5a` (the two audited files are byte-identical to `62a5733ec`). `cpp:` = `projects/mtg/src/AIPlayerGPT.cpp`, `h:` = `projects/mtg/include/AIPlayerGPT.h`. Corpus = `~/.Wagic/ai/gpt/selfplay-runs/matchups-20260912-074153-final/` (42 seat logs, 2,473 records, 2,136 prompt-bearing incl. one with no latency). CR = `~/.gatelogs/engine-audit/cr.txt` (line numbers cited). Every corpus number below was recomputed by me with python over every record; every source claim was opened at the cited line. No build, no run, no edit, no git write. No new findings.

Verdict key: **CONFIRM** / **REFUTE** / **CANNOT DETERMINE**, verdict first on every entry; rank agreement stated where a flaw is confirmed.

---

## A. Fable's findings I did not make

### §0 Baselines
- **CONFIRM** every number I could recompute: 2,135 prompt-bearing decisions with latency (ask 1,661 · priority 290 · attackers 80 · discard 52 · blockers 28 · reveal 20 · bottom 5); prompt bytes 28,033,442, p50 12,654 / p90 21,254 / max 27,189; latency p50 86,952 / p90 165,195 / max 418,017 ms, 56.4 h; `protocol_deviation` compliant 2,072 / unlabelled_plan 57 / plan_absent 5 / prose 1; `plan_line_missing` 62; `coded_answers>1` 0; `answer_replaced` 0; `parse_note` 2 (`multi_answer_first_taken`); `fallback` 2; `transport` = `curl=0,http=200,empty=0` on all 2,135; gameend sums `hold_windows_skipped` 1,051 (cast 860 / priority 191), `ask_replays_reserved` 1,176, `own_turn_windows_skipped` 722, `mana_only` 78, `main_phase` 70, `identical_option_asks_resolved` 13, `single_outcome_menus_answered` 3, `stop_reached` 1, `reserve_decline` 1, `sibling` 0, `chain` 0, `async_drops` 9; `forced_close` 26 = 13 armed + 13 dropped; `crackback_verdict_lines_rendered` 382 vs 329 prompts with the line vs 291 records with the field. W81 keys (`sba_windows_cache_bypassed`, `hold_reopen_answer_invalidated`, `cast_decision_reopened_new_stack`, `verdict_faces_dropped_unrecorded`) absent from all 42 `gameend` records — the wave-80-binary caveat stands.
- Minor: I count **72** numeric gameend keys (excluding seq/turn/my_life/opp_life) and **26** zero-everywhere, not 78/27 (list in §1.3 below). Narration = 13,586,184 B = 48.5 % — exact match.

### §1.1 Helpers with no production caller
- **C1 `declineRowAtFact` — REFUTE.** Called at cpp:443 from `declineFactForMenu` (cpp:434), which `askModel` calls at cpp:48782. Live on every ask prompt's format sentence.
- **C2 `shortDecodeGarbage` — REFUTE.** Called at cpp:20029 from `isDecodeGarbage` (cpp:20016), which `unparsedReplyClass` (cpp:20011) reaches; that class is the fallback string at cpp:48125 and cpp:49334. Live.
- **C3 `loopCautionForBoards` — CONFIRM** (I made it: astra §1.1). Definition cpp:27536; only comment mentions at cpp:27556, 32563. DELETE.
- **C4 `w72HeldMenuShowedEveryRow` — REFUTE.** Called at cpp:37904 from `gptHoldCoversSiblingWindow` (cpp:37889), which the sibling cover calls at cpp:37970. Zero corpus fires (`sibling_window_asks_skipped` 0) is a reach fact, not a caller fact — it belongs with X1, not with dead code.
- **C5 `w71EveryBranchIsANoOp` — REFUTE.** Called at cpp:45053 from `AIPlayerGPT::rowSaysNoOp` (cpp:45038), which has five production callers: cpp:39302 (`noopRowEarnsReask`), 41100, 48338, 48346, 55047.
- **C6 `combatDirectiveNegatedOnLine` / `combatDirectiveExampleEcho` — CONFIRM** (I made it). cpp:57986, 58016; no callers in src/include/tools. DELETE with their explanatory block (cpp:58033).
- **C7 `wagicGptParseSelfTest` — REFUTE.** Referenced outside `projects/mtg`: `JGE/src/SDLmain.cpp:1043` (`extern void wagicGptParseSelfTest();`) and `:1046` (call). It is the PARSETEST entry point from the SDL main; `runParseSelfTest` (h:663) is what it wraps (cpp:62791). Not dead — a repo-wide grep was needed.
- **C8 `AIPlayerGPT::askReplayRefuse` — CONFIRM.** cpp:20904 / h:734; only callers are `AIPlayerGPTSelfTest.cpp:29049, 29066, 29076, 29877`; both live sites use `askReplayRefuseScoped` (cpp:48866, 48923). Deleting it touches the excluded self-test TU (its pin) — the owner should know that cost.
- **C9 `loopAutoPassApplies` — CONFIRM.** cpp:38421 `return oppLoopProven && !anyLegalAction;` ≡ `chainAutoPassApplies(false, a, b)` (cpp:38441 `(chainOnStack || oppLoopProven) && !anyLegalAction`). Only self-test callers (`AIPlayerGPTSelfTest.cpp:24714-24722, 30989`).
- **C10 `w80CrackBackVerdictLineNow` — CONFIRM.** cpp:37528 / h:2294; both render sites call the W81 twin (cpp:47453, 51788); no other reference in `AIPlayerGPT.cpp`.
- **C11 `noAnswerClassFor` 3-arg — REFUTE the premise, CONFIRM the fold.** The 3-arg overload (cpp:18951) is the terminal table; the 4-arg (18968) forwards to it at 18973, the 5-arg (18986) to the 4-arg at 18991, the 6-arg (18999) to the 5-arg at 19006, and `noAnswerClass()` calls the 6-arg at cpp:19151. So the 3-arg IS reached from production on every no-answer record — "no production caller" is wrong. Collapsing the chain into one function is still a fair simplification.
- **C12 `transportOutcomeStamp` 3-arg — CONFIRM.** cpp:19029 forwards to the 6-arg (19064); production calls at cpp:19457 and 22076 both use the 6-arg form; the 3-arg is reached only from the self-test (`AIPlayerGPTSelfTest.cpp:14146, 20006`).
- **C13 `w79BestBlockDamage` `vector<int>` overload — CONFIRM.** cpp:30764; both production calls pass `vector<W79BlkFact>` (cpp:32234 with `soak` declared `vector<W79BlkFact>` at 32203; cpp:37525 with `std::vector<W79BlkFact> soak` at 37510); the int form is reached only via `AIPlayerGPTSelfTestAccess` (cpp:63484).
- **C14 `gptRetrySelectArmClose` — CONFIRM** as a fold candidate. cpp:20205; one production call cpp:20311 (plus self-test calls); it wraps `gptRetrySelectArm` (cpp:20191, 20209) to carry one bool. Judgment item, correctly anchored.

### §1.2 Dead members / variables / assignments
- **C15 `retracted` — CONFIRM.** `bool retracted = false;` at cpp:47680 and 49104; never assigned; read only in the fallback-class ternaries cpp:48125, 49334; the comment at 49102 says so itself.
- **C16 duplicate `mPlanSetTurn`/`mCurrentPlan` — CONFIRM** (I made it; fable's anchor 25744-25745 is the exact duplicate line — the `}        mPlanSetTurn = …` join is visible; mine, 25722, was the block start). First assignment cpp:25720-25721.
- **C17 `mAskReplayKey` / `mAskReplayRun` — CONFIRM** (I made it). h:716-717; only `clear()`/`= 0` at cpp:48652-48653 and 49290-49291.
- **C18 `mCeilingReasks` — CONFIRM.** h:848; only write cpp:20668 (`++`); no gameend row, no reader.
- **C19 `mStatedStopCount` — CONFIRM.** h:1796; writes cpp:20729, 25734, 47776; no read anywhere in src/include.
- **C20 `mStubReplyIndex` — CONFIRM.** h:2242 unconditional; the only uses (cpp:19379, 19390) sit inside the `#if defined(_DEBUG) || defined(WAGIC_DEVLOGS)` block opened at cpp:19372; also initialised unconditionally in the ctor initialiser (cpp:20698).
- **C21 `writeSingleOutcomeMenuRecord(..., commonPaid=0)` — CONFIRM.** Call cpp:55082-55083 passes literal `0`; the three corpus `menu_single_outcome` records all carry `repeat_payments_paid: 0` (deck152 files, seq 6, 6, 8; all Intrepid Adversary, 21 rows, answered 0).
- **C22 `force_close_arms_deferred` duplicate row — CONFIRM** (I made it: astra §1.3). cpp:22960-22961 both serialise `mForceCloseArmsRefused`.

### §1.3 Counters nothing reads
- **CONFIRM the zero list, NARROW "no consumer".** 26 counters are zero across all 42 seats (my list = fable's minus one; I did not find a 27th): `action_before_plan_rejected_replies action_before_plan_replies answer_label_absent_read ask_replays_refused ask_replays_repeat_latch cached_replay_reasked chain_windows_collapsed crossphase_board_unchanged engine_reveal_floor_picks force_close_arms_deferred force_close_arms_refused force_close_defer_bound_hits force_close_same_arm_deferred hold_reopened_new_threat identical_ask_answers_reserved menu_pass_no_progress menu_pass_no_progress_suppressed phase2_answer_missing phase2_answer_recovered put_gloss_stripped repeat_annotated_takes sibling_window_asks_skipped stop_reached_reputs_collapsed wall_miss_events wall_miss_no_retry wall_miss_unrecorded`. A per-name grep over `tools/` finds no reader for any of them (checked: ask_replays_refused, menu_pass_no_progress, sibling_window_asks_skipped, chain_windows_collapsed, repeat_annotated_takes, engine_reveal_floor_picks, wall_miss_events, action_before_plan_replies, phase2_answer_recovered, forced_close_unrecorded, single_outcome_menus_answered, crossphase_board_unchanged, hold_reopened_new_threat, ask_replays_repeat_latch, put_gloss_stripped, stop_reached_reputs_collapsed, identical_ask_answers_reserved — all empty). BUT `tools/corpus-stats.py:465-473` dumps every int gameend key generically (`GAMEEND SUMS`), so "no consumer" is true only in the named sense — this is my astra §1.3 point and fable's own §1.3 acknowledges the generic dump. The per-record joins fable cites are real (26 `forced_close` records carry `outcome`/`park_armed`/`defer_ticks`/`unrecorded_so_far`; 180 `hold_event` records; 3 `menu_single_outcome`; askreplay sidecar 1,176 `ask_replay` records, max run 39). Disposition (delete vs keep-as-generically-dumped) is the owner's criterion call; the facts are agreed.
- **Per-record fields with no tool reader (53 names) — CANNOT DETERMINE in full.** I spot-checked `transport` (2,135 identical strings; no tool reads it) and `plan_echo_count` (129 records; no tool reader — my astra §1.1 item). I did not verify the other 51 names against every tool; fable's list is single-sourced beyond those two.

### §1.4 Never-rendered emitters
- **`cannotPayNowClause` — CONFIRM 0 renders** (`{you cannot pay this right now` 0 of 2,136 prompts); production call cpp:43673-43674 (inside `if (picks.empty())`, reach + floating pool). **Disposition disputed** — see B.
- **`paymentNoLifeCostClause` — CONFIRM.** 55 occurrences in 30 prompts (`paying this costs you NO life`); call cpp:50833; the CAUTION line (cpp:31603-31605) does say "MAY spend them". Token-value judgment; the count holds.
- **`menuFitTag` empty-`lostRows` branch — CONFIRM the shape, count differs.** cpp:40658-40669; corpus: `no other row on this menu needs more than` 660 occurrences in 271 prompts; **660** option rows (of 8,740, `options_text` census) carry it beside `{leaves K of your M untapped mana sources`, not 1,143. Same K printed twice on the same row is confirmed.
- **Six own-loop emitters — CONFIRM the facts, and this is the same item as my astra §1.2 row.** `[own loop verdict:` in 18 prompts (I counted 18 in astra.out too); gameend `own_loop_verdict_lines_rendered` 20, `own_loop_windows_asked` 1. Fable's verdict (KEEP the fact, FOLD the six) is compatible with mine (KEEP — live); see B.
- **Nine census-listed helpers all have production callers — CONFIRM** (I made the same corrections: astra §1.2). Verified again: `removalVictimTag` 41513, `spellRemovalVerb` 41456, `namedCastPenaltyScan` 41328, `stackAnswerYesRowClause` 39856→43490, `w75ApplyChainFeed` 34336→46807/51612/55754, `w80SacrificeSpendsBlockerClause` 29683→43579 (gate `w79SacrificeBlockerGiveBack` 29570→43385/43573), `zeroPowerAttackerTag` 6885→58743, `asyncDropTraceLine` 19305→19740 (under `DebugTrace`), `w81FoldDuplicateCoverParagraphs` 40991→51658. The claim that Siege-Gang Commander (deck130) is in the pool with 0 renders of the sacrifice clause: **CANNOT DETERMINE** — I did not check the deck list.
- **KEEP-DORMANT list — CONFIRM** (matches my astra §1.2 KEEP-DORMANT list at the same anchors: changeling 5508/5530, mutated 9818, infect 13942, hybrid 49337-49401, three-blocker 8114, PW/battle destination 58625/59605, MDFC 49814/50016, adventure 50464-50494).

### §1.5 Wave-tag archaeology
- **CONFIRM.** Counts: `/usr/bin/grep -cE '^\s*//'` = 22,186 comment-only lines; `DELETED` epitaph lines = 73; `//#W<NN>-` tags = 3,542 (fable 3,611 — regex difference, immaterial). Each "now false" comment verified: cpp:50358-50360 says rows and header use "the same engine call" — rows pass `GptManaPolicy policy(this)` (cpp:50326→50361), the header passes `ManaEngine::FreeProducerPolicy` (cpp:31541-31544): same function, different policy, so they can disagree (F5); cpp:31192-31197 same species; h:2386-2389 "never read anywhere else" — `mRetryPark` is read at cpp:20375, 20503, 20505, 21031, 22959, 52356; cpp:20360-20364 "kept as an assertion rather than the gate" while `retryArmMatches` is still `&&`-ed at 20364; cpp:19583 "`reasoning_tail_answers` DELETED" sits inside the branch that still takes the tail (19582-19602); cpp:47655/49092 say `multi_answer_first_taken` DELETED while it is emitted at 22897 and appended at 45997/46069. The stripper's retired spellings are at cpp:33375 (`{it is released at the star`) and 33390 (`{identical in effect right now`) as stated.

### §1.6 Duplicated helpers
- **CONFIRM the seven pairs exist at the cited anchors; DISPUTE one characterisation.** `lineIsActivatedCost` (cpp:16331-16360) and `w80ScriptLineHasActivationCost` (cpp:29366-29401) are **byte-identical in logic** — the only difference is the flag's name (`sawCostGroup` vs `topLevelBraceGroup`) and the comment block; "J=0.52 near-duplicates" measures the comments, not the code. My astra §1.1/§2 "exact duplicate" stands. The other six pairs were located (`codedChoiceOrdinal` 38522 / `firstCodedChoice` 46207; `offProtocolBytes` 21289 / `protocolLinesOnly` 21349 / `replyActionBeforePlan` 21423; `w80CrackBackVerdictLine` 36546 / `w81…` 36612 — the W81 one delegates when `addenda<=0` at 36621; `holdStillStands` 36045 / `w78HoldStillStands` 36184; `drawStepForecastText` 16075 / `theirDrawStepForecastText` 16012 — my astra §2 row; `w76HoldReopenNote` 34957 / `holdReopenNote` 35495) but I did not diff their bodies line by line.

### §1.7 Diagnostics gating
- **CONFIRM.** All 14 `#if defined(_DEBUG) || defined(WAGIC_DEVLOGS)` opens at cpp:18099, 18195, 18252, 18905 (also `|| WAGIC_TRANSCRIPT_ON`), 19130, 19330, 19372, 23116, 25972, 26165, 43906, 52286, 53749, 54060. Ungated `getenv` switches: `WAGIC_INFLIGHT_BOUND` cpp:18834, `WAGIC_GPT_TRANSLOG_STREAM` 21234, `WAGIC_GPT_TRIM_V1` 24469, `WAGIC_GPT_AUDIT_M_OFF` 33772, `WAGIC_GPT_NOTHREAD` GptConfig.cpp:1639/1693. (`WAGIC_GPT_WEDGE` at cpp:18206 is inside the 18195 gate — correctly dev-only.)

### §1.8 Parser branches dormant under thinking-on
- **CANNOT DETERMINE the ~920-line claim; CONFIRM the corpus side.** I did not read `parseChoice` (cpp:45233 ff.) end to end (astra §5 said so). Corpus: 0 stale echoes, 0 `[RE-ASK]` prompts, 0 re-ask lanes fired, 2 parse notes — confirmed. The latches fable names exist at h:847 (`mCeilingReaskDoneBase`), 1785 (`mPriorityReaskBoard`), 1814 (`mAskReaskKey`), 1836-1837, 1842, 2379.

### §1.9 Corrections to the census Tier-A list
- **CONFIRM — and it corrects my astra §1.2 row.** `revealSinglePickDeclineLegal` (cpp:61240) is called from the engine: `src/AllAbilities.cpp:1348-1350` (`extern bool revealSinglePickDeclineLegal(const string&)` then the call), feeding `singlePickDeclineLegal` into the seam at :1364. See B. `gptNoteEngineRevealFloor`/`noteRevealStall` from AllAbilities.cpp, `gptPadlogFile`/`gptTracePromptDrift` via macros, `landEntersTappedTagFrom` four callers (cpp:49966 def; 50011, 50045, 50095) — all confirmed.

### §2 Needless complexity
- **X1 — CONFIRM the gate inventory and its corpus yields.** Gates opened at the cited lines (flip cap 46689; animation 46702-46711; decline cap 46727-46758; all-declined auto-pass 46862-46864; stuck-cast 50368/51541/52101; face-decline 50142; loop auto-pass 47342/51863). Corpus: mana-only 78, hold 1,051, sibling 0, reserve 1, stop-reached 1, chain 0, deadlock 0, cross-phase 4, cache 1,172; stderr `all actions pass-declined` = 2 lines across the 22 `.stderr` files. The "simpler equivalent" is a design proposal; its stated loss (6 skipped round trips per 21 games + per-cause counters) is consistent with the numbers. Its first three gates are my astra F1 — merge.
- **X2 — CONFIRM; this is my astra F2 under a complexity heading** (merge; rank disagreement in B). Key = `w79AskScopeKey(turn, phase, digest) + keyTailStr` (cpp:48805-48807), `boardStateKey` computed at 48800-48801 and used only by the cross-phase entry; the #W79-CZ (T3) comment at 48803 says "the seam scope, not the whole serialised board"; the "233 same-phase re-asks" figure is in `strategy-design/wave79/lane-CZ.md:63,72` and `lane-DC.md:51`. The eight normalisers are at the cited lines (37235-37244, 35913-35937, 35652, 34020, 48462, 48525).
- **X3 — CONFIRM anchors, proposal not adjudicated.** `GptRetrySlot` h:385; `kW76CloseDeferMaxTicks = 8` cpp:20266; `mRetryPark` h:2389; the gate it compensates for is F2's `userMsg == mRetryBase` (cpp:20366). Whether one `RetryLeg` per `AsyncState` reproduces every current outcome I did not trace — it is a design claim.
- **X4 — CONFIRM the resident machinery; note it carries my astra F6 and F7.** Latches at h:847, 1785, 1814, 1836-1837, 1842, 2379; `w79LabellessAnswerLine` cpp:9030 (callers 19629, 25265); three coded-line walks 46207/38522/39414. Fable's proposed keep-list (b) keeps the label-less reader — with my F6 unfixed unless the `nonBlankAfterCand` refusal (cpp:9075-9076) is also dropped.
- **X5 — CONFIRM.** `[HOW A HOLD ENDS` 1,383 prompts (984,696 B ≈ 711 B × 1,383), `[hold check:` 1,715, `{closes ONLY this window` 1,093; constants at cpp:33939 (`kHoldPriorityRowText`), 34098 (`holdContractParagraph`); `W76HoldMemory` h:336 holds 14 map/set members in h:336-379 (fable says nine parallel maps — count differs by what is counted, the shape holds). My astra §2 "two hold stores" (h:1922, cpp:38124/38242) is a sibling of this item — merge.
- **X6 — CONFIRM the lineage anchors** (36030, 36546, 36612, 36692, 37355, 37414, 37470, 37528, 37552, 37572, 34616); `w81CrackBackVerdictLineNow` (37552-37565) re-walks `crackBackTotalOver` and `crackBackFloorSources` per call as stated. My astra §2 "typed verdict" row is the same proposal — merge (and my F5 is the correctness defect the fold would fix).
- **X7 — CONFIRM.** Header `FreeProducerPolicy` cpp:31541-31544; cast rows `GptManaPolicy` cpp:50326→50361; `windowReach()` cpp:43095-43104 `FreeProducerPolicy`, memoised on `mWindowReach`.
- **X8 — CONFIRM.** `stripNarrationDecoration` cpp:33211-33449 holds 66 `in.compare(i, …)` head entries (fable: 58 — counting method), two retired (33375, 33390). My astra §2 "vocabulary mirrored in the stripper" row — merge.
- **X9 — CONFIRM.** 660 rows carry `menuFitTag`-empty beside `{leaves K of your M`; `strandsHandCardTag` 40618, `leavesUntappedTag` 8417.
- **X10 — CONFIRM.** `mAskReplayRuns` h:727, `mRepeatAskRuns` h:870, `mCachedReplayRuns` h:2239; `askReplayRefuseScoped` at 48866/48923; `w81CachedReplayMustReask` 36940→47561; C17 is the dead pair; `transport` 2,135 identical, 0 readers.

### §3 Flaws
- **F1 — CONFIRM; rank HIGH agreed.** `crackBackNextTurnDue` (cpp:29032-29037) returns false when `!selfActive`; `w81CrackBackVerdictLineNow()` is called with no such gate at cpp:47453 (priority/ask) and 51788 (cast menu) — the enclosing blocks (47418-47444, 51760-51781) test nothing about whose turn it is. `crackBackBodyUntaps` (cpp:28226-28231) counts a tapped body unless `doesNotUntap|shackler|frozen`; `w80CrackBackBestBlockFloorNow` filters own blockers with `canBlock()` (cpp:37484). Corpus (my recount): **47** prompts carry `[crack-back verdict:` under `Phase: … | It is the opponent's turn.` (36 "survive", 11 "LETHAL"; 42 ask + 5 priority) — fable said 43 (30/13); the phenomenon is the same. Examples verified: `…deck162…-vs-…deck152` seq 24, 25, 26, 27, 29 (Combat damage → Cleanup, life 10, all "LETHAL if it is UNBLOCKED"); `…deck125…-vs-…deck130` seq 101 (Blockers, opponent's turn, life 1, "LETHAL if it is UNBLOCKED"); `…deck123…-vs-…deck152` seq 66 and 68 (Main 1, opponent's turn, life 14, "you survive"). CR 502.3 (cr.txt:4315) and 302.6 (cr.txt:3263) as cited.
- **F2 — CONFIRM; rank HIGH agreed.** Gate cpp:20366 `if (userMsg == mRetryBase)`; `mRetryBase = userMsg` at cpp:20531, 20544, 20620, 20676, 20686 (and `= live.base` 20315) — the full assembled prompt; abandon branch cpp:20434-20456 writes `dropped_decision_moved` and zeroes `mRetryFirstLatencyMs` (20452). The async slot key is `asyncSlotKeyOf(…, mPromptTail, w79AskScopeKey(...))` (cpp:19312-19327), not the prompt. Corpus: all 13 `armed` records are followed by a `dropped_decision_moved` at the very next seq in the same file; the next prompt-bearing record after each pair carries latency 164,816 / 39,513 / 132,453 / 34,987 / 122,724 / 105,845 / 111,823 / 150,758 / 122,216 / 132,023 / 185,593 / 48,809 / 49,625 ms = **23.35 min**. `phase2_answer_recovered/missing` 0/0.
- **F3 — CONFIRM; rank HIGH agreed. Sub-items:** (1) pass-decline cap cpp:46743-46758 + all-declined auto-pass cpp:46862-46864 — **= my astra F1 items 3-4** (merge); stderr floor 2 confirmed. (2) animation withhold cpp:46702-46711 — **= my astra F1 item 2**. (3) flip cap cpp:46689 — **= my astra F1 item 1**. (4) `mStuckCastLines`: armed cpp:50368 (board byte-identical after a pick), row dropped cpp:51541, armed again on a conceded re-ask cpp:52101, turn-scoped 50353-50356 — CONFIRM the mechanism; the `…deck130…-vs-…deck146` seq 9 record is a Main-1 cast ask whose chosen row was "Cast Talisman of Impulse" — that the trigger there was the sub-menu's "Decline - do nothing" is **CANNOT DETERMINE** from the record alone. (5) ETB pay-or-tap auto-answer cpp:54621-54632 (`act.choice = i; return 0;` when `etbAlreadyTapped`) — CONFIRM; CR 119.4 (cr.txt:2133) as cited; corpus: 6 `Choose an option for Agadeem|Emeria` asks were sent, consistent with "never fired". (6) `mayStop = multi && !picks.empty() && !tc->targetMin` cpp:55653; `req.targetCandidates.size() < 40` DecisionContract.cpp:783; `picks.size() >= 12` break cpp:56320-56321 — CONFIRM all three; CR 115.6 (cr.txt:1717). (7) `faceDeclinedThisTurn` filter cpp:50142-50146 and the `lands.empty() → return NULL` at 50156-50170 — CONFIRM; `landDropStatusLine` inputs (cpp:31612-31632: `legalLandPlays` + `mdfcBackFaceLandStatus`) never consult the latch — CONFIRM. **This sub-item and my astra F9 are the same formatter** (merge note C).
- **F4 — CONFIRM; rank HIGH agreed.** Corpus: `…deck162-0x55be…-vs-…deck146` seq 30 (turn 16, Main 1, life 7): header "up to 14 from combat … you would be at -7 or lower" + ADD THOSE UP (→ 17), cover clause on row 4 "17 in total … puts you at -10 OR LOWER … leaving 9 -> you would be at -2", verdict "best legal assignment … lets 10 through and you are at -3" — three life figures on one screen, confirmed verbatim. Six prompts with header "that would KILL you" and `[crack-back verdict: you survive`: `…deck130-0x5591…-vs-…deck152` seq 17, 19, 22, 24, 25 and `…deck162-0x562b…-vs-…deck130` seq 43 — exactly fable's list. Code: (a) `crackBackVerdictNow()` cpp:37355-37360 uses `crackBackTotalOver` only; (b) `floorUnsized` computed at 37557-37558 and never passed — `w81CrackBackVerdictLine` (36612-36613) has no unsized parameter; (c) `w80CrackBackBestBlockFloorNow` takes every `canBlock()` body (37484) while `crackBackCoverFacts` (30298; 30320-30324) skips non-vigilance, non-defender bodies when `!attacksSettled`; (d) `if (face.find("none") …) return string();` at 36618-36619 precedes the addenda arithmetic. All four confirmed.
- **F5 — CONFIRM; rank MED agreed.** Corpus scan (header `Mana available: N total` vs every `{leaves K of your M untapped mana sources` on the same prompt): exactly **4** records disagree, all `…deck146-0x55f8…-vs-…deck152` seq 28 (2 vs 3), 32 (5 vs 6), 37 (5 vs 6), 38 (1 vs 2). Code: X7's three policies; hand tag `[cannot pay now: needs N mana, you have M]` (cpp:31256) prints `sources` from the FreeProducer walk (31544) beside a `payable` verdict computed with `castPolicy`/`castPool` = `GptManaPolicy` (cpp:31730-31731, 31775). The Battlement "3 total" example I did not open.
- **F6 — CONFIRM; rank MED agreed.** `tappedCreatureTag` (cpp:5553-5562) has no controller/turn scope; the summoning-sick tag is scoped to `card->controller() == currentPlayer` (cpp:12497-12499). Corpus: **310** occurrences of `[tapped - cannot attack or block this turn]` on `Opponent battlefield` lines during `It is your turn` prompts (440 occurrences total) — exact match. CR 508.1a (cr.txt:4531), 509.1a (cr.txt:4693).
- **F7 — CONFIRM; rank MED agreed.** `kAttackersTurnFacts` cpp:302-307 ends "Declaring attackers taps only the attacking creatures - never your lands"; `bin/Res/ai/gpt/system_prompt.txt` line 3 "Declaring an attack taps t…"; on all 80 attackers prompts, 31 of which offer a `[vigilance]` attacker. CR 702.20b cr.txt:8231.
- **F8 — CONFIRM; rank MED agreed (latent).** Attackers seam doubles (`rp *= 2` cpp:59085-59086); blockers seam: `lifeIncoming += p` with `p = power` (cpp:60167-60172), A-row `" deals " << power` (60283), gang stats fold DOUBLESTRIKE into `firstStrike` only (60385-60386); the 1-on-1 stat carries `s.doublestrike` (57360) but it is read only for the lifelink label (57185, 57200, 57232, 57246), not for lethality (57012 ff. uses power). `willattackpw` appears only in `MTGRules.cpp:2362, 2523` — not in `AIPlayerGPT.cpp` or `DecisionContract.cpp`. CR 702.4b cr.txt:7845, 510.1b cr.txt:4801.
- **F9 — CONFIRM (by trace, not run); rank MED agreed.** Range form requires `-` immediately followed by `B`+digit (cpp:59884-59886); separators consumed are `: - > = space` (59896-59898); after `B4 - B9:A2` the scan lands on `B` (not `A`/`none`) → `a = -1` → malformed pair; `B4-9:A2` likewise; `B1 blocks A1`: coded pass fails the same way and the name pass's `significantWords` drops tokens shorter than 4 chars (cpp:56353) so `leftSeg = "B1"` yields no words (59977). Attackers grammar accepts `A4-9`/`A4 - A9` (58305-58320). Stale-echo discards at cpp:46016-46020 and 46119-46124 both `return -1` on an in-range index. Same ruling as my astra F6 (label-less refusal) — sibling items, not the same defect.
- **F10 — REFUTE.** The premise ("the whole prompt is rebuilt on every AI tick while the model is thinking") is contradicted by the entry gate: `AIPlayerBaka::Act` (AIPlayerBaka.cpp:5875-5879) returns 0 when `decisionPending(dt)` is true, and `AIPlayerGPT::decisionPending` (cpp:52317-52334) returns true whenever `asyncBusy()` (cpp:18725-18737: either slot `status == 1`). While a request is in flight the seam is never re-entered, so `assemblePrompt` (47492 / 49039) and the best-block DP run once at launch and once at consume, not ~900 times per decision. `computeActions()` is reached only after that gate (AIPlayerBaka.cpp:5942-5948). Nothing outside the AIPlayer files calls the seams (grep over `src/` for `chooseBlockers(|chooseAttackers(|orderBlockers(|FindCardToPlay(` excluding AIPlayer*/tests: none). The per-window DP cost is real once per window; the "hang risk on handhelds" and I10 do not follow. **This same gate is the evidence for my astra F3** (B below).
- **F11 — CONFIRM; rank MED agreed.** `flushWallMissRecord("wall_miss_no_retry")` at cpp:20609 → `writeTransLog` consumes and clears `mLastLatencyMs`, attempt ms, http/curl, transport outcomes (cpp:22155-22160); `deferSeat` may be the opponent seat (cpp:52254-52262, `w78DeferRecordMoves`); `mLastReasoning*` set on consume (19640-19648) and cleared only by a record write. Corpus: **38** ask/priority records render `[crack-back verdict:` with an empty/absent `crackback_verdict` (e.g. `…deck125-0x55a5…-vs-…deck152` seq 37, 46, 258, 287; `…deck123-0x55c5…` seq 34, 48); 382 / 329 / 291 confirmed. The blockers-record drop stamp example (`…deck130…` seq 25-26) I did not open.
- **F12 — CONFIRM; rank MED agreed.** `w80SingleOutcomeRepeatMenu` (cpp:53118-53144) answers when every `add N` row has `paid == 0`; `paid` = `getManaPool()->canAfford(probe)` OR non-empty `ManaEngine::planPayment(this, GptManaPolicy, …)` (cpp:54913-54918); answer site cpp:55079-55086 with the literal `0` (C21). Three corpus records as listed. (This is also my astra F10 item 3 — merge.)
- **F13 — CONFIRM the dilemma; rank LOW agreed.** `if (candidates.empty()) return NULL;` at cpp:51562-51563 precedes the `loopAutoPassWindow()` call at 51863, so the cast-seam call is reached only with a non-empty menu; `loopAutoPassFor` passes only on `!hasAnyLegalAction(p, &GptManaPolicy)` (cpp:38447-38472). Whether `LegalActionsOracle::hasAnyLegalAction` (LegalActions.cpp:689 ff.) and `buildCastSpell` can disagree I did not trace — the either/or is sound as stated.
- **F14 — CONFIRM overall; rank LOW agreed. Sub-items:** DRAW PUNISHERS "costs them N life to yours" cpp:14645-14650 vs `planeswalkers.txt:2686` `@drawfoeof(player):damage:1 opponent` — CONFIRM (208-prompt count not recomputed). `opponentLifeTrendLine` samples on the first render of a new turn (`mOppLifeByTurn[2] = opp->life` cpp:31367 inside `curTurn != mOppLifeLastTurn`) — CONFIRM the mechanism; the "turn 22: 5" example — CANNOT DETERMINE (sampled file not re-derived). `kChainFeedTail` cpp:34262 "you would be at 0 - this row feeds their chain" — corpus: 3 prompts carry it at `my_life == 20` (fable cited 4 records; two of its four are in files I matched). Counterspell hand tag: **235** prompts carry `Cancel|Dream Fracture … [no cast row now: it must have a target and there is no legal target on the board` — CONFIRM (cpp:31281-31287 keys on `instantSpeed`, not on where the target lives). `heldBackBlockTag` "could not block ANY of their N creatures" cpp:7031-7033 — CONFIRM wording; corpus: 12 `[held back` occurrences total, 3 with the current head, 2 of those Fog Bank — fable's "8 of 12" is CANNOT DETERMINE (different spelling census). "before you act again" cpp:13902 — CONFIRM exists. `attackerBlockerCountLine` "declaring more than N attackers" cpp:7255-7257 — corpus: **22** of 80 attackers prompts print it with ≤ N `A<k>.` rows offered — exact match. Bottom ask "bottomed one at a time in the order you give" cpp:61862-61864 vs queue filled in hand order (`for j over hand … push_back(hand[j])` cpp:61965-61970) — CONFIRM; CR 103.5 cr.txt:591 ("in any order" is in the mulligan text there).
- **F15 — CONFIRM the audit, REFUTE one sub-claim.** `WorkerCtx` owns a `shared_ptr<AsyncState>` (cpp:18180-18190); generation check (18263-18274); `gptShutdownWorkers(2000)` called at `GameStateDuel.cpp:1734` (and from `JGE/src/SDLmain.cpp:1070`) — CONFIRM. **"Worker lifetime is safe for seat destruction" — REFUTE**: in the generation-mismatch branch `delete ctx` (cpp:18271) runs while `std::lock_guard<GptMutex> g(ctx->state->mtx)` (18263) is still in scope; if the player is gone and `ctx->state` is the last reference, the mutex is destroyed locked and the guard unlocks freed storage — my astra F4, unchanged. `aiPatiencePromptAnswer(false)` clears only `mEndpoint` (cpp:52405) and `logGameEnd` adds `gptForceCloseOutstanding(mForceCloseArmed, mRetryPark.forceCloseArmed)` (22958-22959) — CONFIRM. `mLandFacePreCard` armed before validation (cpp:50278-50283) — CONFIRM the arming; the reuse hazard CANNOT DETERMINE. May-batch inheritance (cpp:54058 ff.) — CANNOT DETERMINE (not read).

### §4 Improvements
- **I1 — CONFIRM the counts** (`[HOW A HOLD ENDS` 1,383 prompts / 984,696 B; `[hold check:` 1,715; `{closes ONLY this window` 1,093; `(THEIR card, written from THEIR seat` 2,238 occurrences / 1,325 prompts; `Declaring attackers taps only…` 80). Proposal; the loss statement is fair.
- **I2 — CANNOT DETERMINE the headline number.** By balanced-brace measurement over all prompts: `{effect:` 1,978 occurrences / 274,634 B (0.98 %), `{card text:` 2,855 / 337,253 B (1.2 %), `{target text:` 784 / 80,138 B (0.29 %). Fable's `{effect:` figure (4,631 occ / 911 KB / 3.25 %) I could not reproduce; card-text and target-text match. The proposal (gloss only what the decklist cannot know) stands on the smaller number too.
- **I3 — CONFIRM.** 3,824 `(copy N of M in your hand)` entries by my regex (fable 4,467 by a different join); the hand line is keyed by name at cpp:31675 (not re-opened). Proposal.
- **I4 — CONFIRM.** `[no cast row now:` 4,542 occurrences in 1,264 prompts, 891,833 B = 3.18 % — exact.
- **I5 — CONFIRM the totals** (narration 13,586,184 B = 48.5 %; `- Phase:` lines 78,209). Line-family byte split not recomputed. `WAGIC_GPT_LOGWINDOW` = `full` on every record (`log_window` field) — confirmed.
- **I6 — CONFIRM** (X9 numbers; the other folds are proposals at plausible anchors).
- **I7 — CONFIRM; this is my astra I1 + §2 "typed verdict" row, and it would remove my F5 by construction** (merge).
- **I8 — CONFIRM** the motivating facts (2,135 identical `transport`; 382/329/291; `mRetryFirstLatencyMs = -1` at 20452). Proposal.
- **I9 — CONFIRM the proposal, REFUTE its evidence numbers.** Land-drop asks in this corpus = **332** (`log_window_kind == land_drop`, every one with row 1 `Play …` and the `Land drop: which land do you play now, if any?` question), chosen "Play …" in **332 / 332**, declines **0** — not 1,369 vs 20. The direction is stronger than stated (100 % "play"); the round-trip saving is ~332 calls per 21 games, not ~1,369.
- **I10 — REFUTE** (depends on F10; the seam is not re-entered while in flight, so there are no ~900 builds to remove).
- **I11 — CONFIRM** (= F2 fix direction).
- **I12 — CONFIRM** (= my astra I5; counts from §1.5).
- **I13 — CONFIRM.** 52 discard prompts, p50 17,295 B, max 25,889 B (all carry the full narration); `kAskWindowCleanupDiscard` exists (cpp:23395, 62637).
- **I14 — CONFIRM** (W81 keys absent; own-loop windows asked 1).

---

## B. My (astra.out) findings that fable.out contradicts, disputes, ranks differently or corrects

- **§1.2 `revealSinglePickDeclineLegal` — CONCEDE the characterisation.** I said its only reference in this file is a self-test bridge and the live seam receives the flag as an argument. Both true of `AIPlayerGPT.cpp`, but the helper is called by the engine at `AllAbilities.cpp:1348-1350`. It is live production code, not test support. Disposition unchanged (KEEP), reason corrected.
- **§1.1 duplicate assignments anchor — NARROW.** Fable's cpp:25744-25745 is the exact duplicate; my 25722 pointed at the block. Same item.
- **§1.1 / §2 `lineIsActivatedCost` = `w80ScriptLineHasActivationCost` "exact duplicate" — HOLD.** Bodies cpp:16331-16360 and 29366-29401 are identical statement for statement; only the flag name and comments differ. Fable's J=0.52 is a comment-inclusive token metric.
- **§1.2 `cannotPayNowClause` KEEP vs fable DELETE — HOLD, narrowly.** It is live (cpp:43673-43674), truthful, and rendered 0 times in 2,136 prompts — so it costs nothing and buys nothing measurable. Fable's "restates `0 total`" argument is a token-value judgment on a clause that never printed. Owner's call; I would not spend a deletion on it.
- **§1.2 own-loop "six emitters" KEEP — NARROW.** Keep the FACT (18 prompts carry the verdict); fable's fold of the six functions into one (X6 shape) is compatible with everything I wrote, and I agree the six are a lineage, not six emitters.
- **§1.2 `asyncDropTraceLine` — HOLD.** One production call, cpp:19740, inside `DebugTrace(...)`; fable's "three callers" = definition + that call + the self-test bridge (cpp:62833). Gated diagnostic, as I said.
- **§1.3 zero counters "not unread" — HOLD the fact, NARROW the consequence.** `corpus-stats.py:465-473` does consume every int key generically; no tool names any of the 26. Whether generic-dump consumption counts as "read" is the owner's criterion; fable's per-record joins are real, so deletion loses nothing a `grep -c` cannot recompute.
- **F1 (legal-option caps) — HOLD; fable F3.1-3 is the same finding**, rank HIGH on both sides; fable extends it with `mStuckCastLines`, the ETB auto-answer, the target-chooser caps and the land-face latch, all of which I confirmed above. Merge under one ledger item.
- **F2 (ask/async keys are not BOARD STATE + QUESTION) — HOLD rank HIGH (flaw), not complexity.** Fable X2 describes the same key at the same lines and lists the compensations built around it (deadlock breaker reading `mLastProgressBoardKey`, the W81-DI replay bound, the cross-phase `boardKey`, `mStaleDropBoard`) — those are symptoms of a correctness gap (life totals and toughness are outside the key: cpp:36442's exclusions, my static counterexample), not merely eight normalisers. Same defect, different heading; the owner's ruling makes it a flaw.
- **F3 (watchdog unreachable on the ordinary path) — HOLD.** Fable F10 asserts the opposite premise (seams re-entered every tick) and F10's own anchor refutes it: `AIPlayerBaka::Act` returns at AIPlayerBaka.cpp:5878 when `decisionPending(dt)`, and `decisionPending` (cpp:52317-52334) returns true on `asyncBusy()`. `reapWedgedRequests` has exactly one caller, cpp:19410, inside `pollCompletion`, which is reached only through the seams. Fable F15's "in-flight abandon cpp:18840-18848 … bound verified" verifies the bound's VALUE (`inFlightAbandonMsFor`), not that anything ever evaluates it while a request is wedged. Static, not reproduced — as stated in astra.out.
- **F4 (mutex destroyed locked) — HOLD.** Fable F15 says worker lifetime is safe for seat destruction; the generation-mismatch branch at cpp:18263-18274 does `delete ctx` under `lock_guard g(ctx->state->mtx)`. The shared_ptr protects the state exactly until that delete; the guard holds no reference. Conditional (abandoned generation + player destroyed + late worker), unobserved in the corpus, as I said.
- **F5 (crack-back addenda "damage no block removes") — HOLD.** `crackBackFloorSources` adds `bestRungPower` of animatable non-creatures into `sizeable` (cpp:28836-28847); `w81CrackBackVerdictLine` adds `addUp` into `floorNow` and prints "and N of it is damage no block removes" (cpp:36623-36627, 36655-36666). Fable F4 is the neighbouring defect (marker vs line totals; `floorUnsized` dropped; cover vs floor blockers; `none` short-circuit) and fable I7/X6 is the fix that would remove both — merge note in C. Not contradicted, differently anchored.
- **F6 (label-less answer refused for trailing prose) — HOLD.** cpp:9075-9076 `if (nonBlankAfterCand) return false;`. Fable X4 keeps the label-less reader in its simplified parser without noting this refusal.
- **F7 (no-op rows re-asked) — HOLD.** cpp:39300-39303 predicate; trigger cpp:49143-49145; budget-exempt second re-ask cpp:49158-49160. Fable X4 lists the re-ask lanes as removable machinery; consistent with my disposition. 0 corpus fires on both sides.
- **F8 ("enters TAPPED — no mana this turn … from your next turn on") — HOLD.** cpp:49992-50005; not disputed.
- **F9 (`landDropStatusLine` infers usage from legality) — HOLD; fable F3.7 is the same formatter's other defect** (computed without the face-decline latch). Merge as one item with two facets: inputs are `myTurn, playable, haveLand` only (cpp:31612-31632, 9694-9712).
- **F10 (auto-answered legal menus) — HOLD.** Fable confirms two of my four shapes as flaws (F3.5 ETB auto-answer, F12 single-outcome repeat menu = my item 3) and reports the other two as baseline facts (`identical_option_asks_resolved` 13, one-option asks) without ranking them; the ruling's text does not carve out "same outcome". Owner decides; the facts agree.
- **F11 (`mAskScopeDigest` grows all game) — HOLD.** h:2226; insert cpp:48823, read 48815-48816, no `erase`/`clear` anywhere (grep). Not disputed.
- **§2 "two hold stores" — NARROW into fable X5** (the hold contract on five surfaces + `W76HoldMemory`'s parallel maps); same simplification target.
- **I4 (whole-order blocker answer), I3 (range grammar scoped to the menu) — HOLD, uncontested and single-sourced.**

---

## C. Merge notes (same defect, different names/anchors)

1. **astra F1 ≡ fable F3 (1-3)** — pass-decline cap / animation withhold / flip cap; fable F3 (4-7) extends the item. One ledger entry.
2. **astra F2 ≡ fable X2** — the ask/async key is scope + continuation digest + normalised tail, not board state. Rank it once (HIGH flaw by the ruling); X2's normaliser inventory is its complexity facet.
3. **astra F3 ⟂ fable F10** — contradictory premises about the same gate (AIPlayerBaka.cpp:5878 / cpp:52317); the ledger should carry astra F3 and drop fable F10/I10.
4. **astra F4 ⟂ fable F15 "worker lifetime safe"** — same lines (cpp:18263-18274); keep astra F4.
5. **astra F5 + fable F4 + fable X6 + astra §2 "typed verdict" + fable I7** — one crack-back verdict family: two correctness defects (different totals; addenda treated as unblockable) and one fix (compute `CrackBackFacts` once). Count the defects separately, the fix once.
6. **astra F9 ≡ fable F3.7** — `landDropStatusLine` (cpp:9694) is wrong in two ways (wording infers usage; inputs ignore the face-decline latch). One item.
7. **astra F10 item 3 ≡ fable F12** (single-outcome repeat menu); **astra F10 item 1/2 ≈ fable §0 baseline** (13 identical-row asks, one-option asks); **fable F3.5** (ETB auto-answer) is a fourth shape of the same rule.
8. **astra §1.1 `combatDirective*` ≡ fable C6; `loopCautionForBoards` ≡ C3; `mAskReplayKey/Run` ≡ C17; duplicate plan assignments ≡ C16; `force_close_arms_deferred` alias ≡ C22; `plan_echo_count` ⊂ fable §1.3 unread fields.**
9. **astra §1.2 corrections ≡ fable §1.4 (nine helpers) + §1.9** — same census rebuttal; fable's §1.9 corrects my `revealSinglePickDeclineLegal` reason.
10. **astra §2 draw-forecast twins ≡ fable §1.6 row 5; astra §2 stripper vocabulary ≡ fable X8; astra §2 two hold stores ⊂ fable X5; astra §2 ask cache→latch→replay ⊂ fable X1/X10.**
11. **astra I1 ≡ fable I7 (+I6's ATTACK TOTAL fold); astra I2 ≈ fable I4; astra I5 ≡ fable I12.**
12. **astra F6 and fable F9** are siblings under the robust-parser ruling (label-less trailing prose vs range grammar / stale echo), not the same defect.

---

## D. What I did not independently check (remains single-sourced to fable.out)

I did not read `parseChoice` (cpp:45233 ff., §1.8 / X4's word-tier and echo machinery) or the transport stamp grammar beyond confirming `transport` is 2,135 identical strings (X10's ~200-line claim); I did not verify the 53-name unread-record-field list beyond `transport` and `plan_echo_count`; I did not recompute the narration line-family byte split (I5) beyond the 48.5 % total and the 78,209 `- Phase:` lines, nor fable's `{effect:` byte census (my balanced-brace count is a third of it); I did not open the Battlement "3 total" example (F5), the blockers-record drop-stamp example (F11), the `prompt-ask_mid-0` life-trend example (F14), the Siege-Gang-present claim (§1.4), the may-batch inheritance (F15), or `LegalActions.cpp:689-748` in enough depth to say whether `hasAnyLegalAction` and `buildCastSpell` can disagree (F13); and I did not trace X3's claim that one `RetryLeg` per `AsyncState` reproduces every forced-close outcome. Fable's §5.2 hold-check delta and its delegated readers' arithmetic checkers (441/441, 59/59, 28/28, 80/80) I did not re-run. Everything else above I opened at the cited line or recomputed over every corpus record.

---

## Tally over fable.out's findings (top-level entries as keyed above)

- CRUD: C1-C22 → CONFIRM 16, REFUTE 6 (C1, C2, C4, C5, C7, C11); §1.3 counters CONFIRM (narrowed); §1.3 unread fields CANNOT DETERMINE; §1.4 six rows CONFIRM; §1.5, §1.6, §1.7, §1.9 CONFIRM; §1.8 CANNOT DETERMINE.
- Complexity: X1-X10 → CONFIRM 10.
- Flaws: F1-F15 → CONFIRM 14 (rank agreed on all 14), REFUTE 1 (F10). Sub-items refuted inside confirmed entries: F15's "worker lifetime safe".
- Improvements: I1-I14 → CONFIRM 12 (I9 with its evidence numbers corrected 332/0 vs 1,369/20), REFUTE 1 (I10), CANNOT DETERMINE 1 (I2's headline byte count).
- **Total: CONFIRM 63 · REFUTE 8 · CANNOT DETERMINE 3** (74 entries).
