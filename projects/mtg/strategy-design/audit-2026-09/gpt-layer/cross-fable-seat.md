# GPT-layer audit — cross-review of `astra.out` by the `fable.out` reviewer

Read-only, 2026-09-15. Repo `/home/magi/Projects/wagicGPT` at `e71f73c5a` (the two audited files are byte-identical to `62a5733ec`, as the other report also verified). `cpp:` = `projects/mtg/src/AIPlayerGPT.cpp`, `h:` = `projects/mtg/include/AIPlayerGPT.h`, `baka:` = `projects/mtg/src/AIPlayerBaka.cpp`, `cr:` = `~/.gatelogs/engine-audit/cr.txt`. Corpus = the 42 seat logs under `matchups-20260912-074153-final/`; every corpus number below was recomputed by me over all 2,135 prompt-bearing records this round. Git was used read-only (`git log`/`git show`) to date two commits. No new findings are introduced; where verification turned up evidence the other reviewer did not have, it is attached to THEIR finding.

Verdict key: **CONFIRM** / **REFUTE** / **CANNOT DETERMINE**; **SAME** = a finding I also made (listed only so the ledger can pair them; see §C). Rank agreement is stated on every confirmed flaw.

---

## A. Findings in `astra.out` that I did not make (or made differently)

### A.1 CRUD §1.1 — "Confirmed removable residue"

- **Two abandoned combat-restatement helpers** (`combatDirectiveNegatedOnLine`, `combatDirectiveExampleEcho`, cpp:57986/58016) — **SAME** as my C6. Merge (§C).
- **`loopCautionForBoards`** (cpp:27536) — **SAME** as my C3.
- **`mAskReplayKey` / `mAskReplayRun`** (h:716-717, cpp:48652-48653, 49290-49291) — **SAME** as my C17.
- **Obsolete plan-block calculation in `consumePlan`** (cpp:25576) — **CONFIRM.** cpp:25581-25583 computes `planStart = firstLineLeadingPlanPos(text)` and `planEnd = planBlockEndOffset(text, planStart)`; cpp:25586-25587 `(void) planStart; (void) planEnd;` under the #W70-BM (E2) comment saying the demotion they fed is DELETED. Neither name is used again inside the function (the `planEnd` at cpp:25666 is a separate local computed from `labelLineStart`). Dead computation; DELETE agreed. (`planBlockEndOffset` itself must stay — it has other callers.)
- **Duplicate `mPlanSetTurn`/`mCurrentPlan` assignments** (cpp:25722 / my cpp:25744-25745) — **SAME** as my C16.
- **`lineIsActivatedCost` / `w80ScriptLineHasActivationCost` are an EXACT duplicate** (cpp:16331-16360 / 29366-29401) — **CONFIRM**, and I concede my own wording. Side-by-side the two bodies are the same automaton: `depth` counter, `(`/`[` increment, `)`/`]` guarded decrement, `{` sets the flag only at depth 0 then increments, `}` guarded decrement, first depth-0 `:` returns the flag, fall-through `false`. Only the flag's name (`sawCostGroup` vs `topLevelBraceGroup`) and the comments differ. My "J=0.52 near-duplicate" was a token-Jaccard over the commented text, which the comment bulk dragged down; the code is identical. Callers: cpp:16502 and cpp:29411, 36848; both are also pinned through `AIPlayerGPTSelfTestAccess` (cpp:63098, 63526), so deleting one means redirecting a self-test bridge as well as the two production callers. DELETE-one agreed.

**Telemetry-only candidate `plan_echo_count`** (cpp:22444-22448, 25701, h:1598) — **CONFIRM**, evidence boundary as stated. `/usr/bin/grep -rn plan_echo projects/mtg/tools/` returns nothing; in-engine the member is written at cpp:25701 and reset at cpp:24174, 24274, 24284, 24294 (four reset sites the other report did not list — all part of the same deletion), read only by the record writer at cpp:22447-22448. The header comment (h:1594) and the writer comment (cpp:22444-22446) both say "REPORT field only". I listed it among the 53 unread record fields (§1.3 of my report); their treatment as a named DELETE candidate is the sharper one. Merge (§C).

### A.2 CRUD §1.2 — "Tier A candidates that are not dead"

- **Own-loop "six emitters" are live, 18 prompts carry `[own loop verdict:`** — **CONFIRM the count and the liveness.** Recount: 18 of 2,135 prompts contain `[own loop verdict:` (my report's "20" was the `own_loop_verdict_lines_rendered` counter sum; two prompts carry the line twice through a re-put, so both numbers stand). Disposition is not in conflict: they say KEEP-live, I say KEEP the fact and FOLD the six functions into one (my X6). The fold is a complexity item, not a deletion.
- **Crack-back LETHAL/none variants KEEP** (cpp:36546) — **SAME** as my §1.9 ("latch MARKERS … never meant to render — a category error, KEEP").
- **`zeroPowerAttackerTag` / `[deals 0…]` KEEP** (cpp:6885, 6916, 58743) — **SAME** (my §1.4 row).
- **`removalVictimTag` / `spellRemovalVerb` KEEP** (cpp:41456, 41513, 51141) — **SAME.**
- **`namedCastPenaltyScan` KEEP; also feeds a life surcharge** (cpp:41328, 50534) — **CONFIRM liveness** (3–5 call sites by my grep); the "surcharge used elsewhere" sub-claim I did not re-open this round — **CANNOT DETERMINE** that detail; it does not change the verdict.
- **`stackAnswerYesRowClause` KEEP** (cpp:39856, 43490) — **SAME.**
- **`w75ApplyChainFeed` KEEP** (cpp:34336, 46807, 51612, 55754) — **SAME.**
- **`w80SacrificeSpendsBlockerClause` KEEP pending correctness** (cpp:29683, 43579) — **SAME**, with my added measurement: 0 renders while the card class (Siege-Gang Commander, deck130) is present, so its gate `w79SacrificeBlockerGiveBack` needs re-measuring (my I14).
- **`cannotPayNowClause` KEEP, "not established dead"** (cpp:9426, 43673) — disputes my DELETE; answered in §B.1.
- **`asyncDropTraceLine` KEEP as gated diagnostics** (cpp:19305, 19740) — **SAME.**
- **`w81FoldDuplicateCoverParagraphs` KEEP** (cpp:40991, 51658) — **SAME** (post-corpus, unmeasured).
- **`gptPadlogFile` / `gptTracePromptDrift` reached via macros** (cpp:18116, 19144, 19705) — **SAME** (my §1.9).
- **`gptNoteEngineRevealFloor` / `noteRevealStall` have engine callers** (`AllAbilities.cpp:1489`, `:1122`) — **SAME** (my §1.9).
- **`revealSinglePickDeclineLegal`: "its reference in this file is a self-test access bridge, not the live reveal seam … It is test support"** (cpp:61240, 61511, 63291) — **REFUTE the characterization; the disposition (do not delete) stands for a different reason.** The helper is production code: `src/AllAbilities.cpp:1348-1350` declares it `extern` and calls it in the live reveal driver (`singlePickDeclineLegal = revealSinglePickDeclineLegal(oneSpec)`), and that value is the `singlePickDeclineLegal` argument the live seam receives at cpp:61518 (passed through `AllAbilities.cpp:1364` → `decideReveal`). The bridge at cpp:63291 is additional, not the only reach. So "evaluating whether that support is redundant would cross the excluded self-test scope" does not apply — it is a live rules gate (`<upto:>`/`anyamount` arity → a decline is legal), and my §1.9 correction of the census stands.

**KEEP-DORMANT list** (changeling cpp:5508, mutated piles 9818, infect 13942, hybrid pips 49361, three-blocker 8114, PW/battle destination 58625/59605, MDFC variants 50016, adventure 50492) — **SAME** as my §1.4 KEEP-DORMANT row (my anchors: 5530, 13942, 49337-49401, 8114, 58625, 49814, 50464-50494). Their caveat "an absent variant is narrower than an absent mechanic" — agreed; my list was scoped to variants.

### A.3 CRUD §1.3 — zero counters, duplicate alias

- **`corpus-stats.py` generically consumes every integer gameend field** (`tools/corpus-stats.py:465`) — **CONFIRM the fact.** `tools/corpus-stats.py:465-472` sums every `int` (non-bool) key of every `gameend` record except `seq/turn/my_life/opp_life` and prints `GAMEEND SUMS`; lines 475-489 then reconcile only `protocol_replies`. Their inference from it ("the 26 zero counters cannot be called unconsumed") disputes my §1.3 — answered in §B.3.
- **`force_close_arms_refused` / `force_close_arms_deferred` serialise the same member** (cpp:22960-22961) — **SAME** as my C22.

### A.4 CRUD §1.4 — comment archaeology (all four are new relative to my §1.5 list)

- **cpp:46626 "The staleness key is the full prompt text"** — **CONFIRM stale.** The block at cpp:46632-46640 (LIVELOCK ROOT FIX, 2026-08-21) describes a full-prompt staleness key; the live ask key is `w79AskScopeKey(turn, phase, mContinuationDigest) + keyTailStr` (cpp:47393-47394) and the async slot key is `asyncSlotKey` (cpp:19312-19327, digest-based since #W56-A/#W62). The comment's WHY (byte-stable menu order) is still valid; its stated mechanism is not.
- **cpp:25556 `consumePlan` says "Find the LAST label … Last line-leading answer-label line"** — **CONFIRM stale.** Selection runs through `gptSelectAnswerIndex` (cpp:25041-25050): first usable AND clean candidate, else first usable — first-clean, not last. The header comment at cpp:25030-25036 states the first-clean rule correctly; the `consumePlan` prose contradicts it.
- **cpp:48903 "The cache above wants the board too"** — **CONFIRM stale.** The cache key built at cpp:48805-48807 is scope + continuation digest + normalised tail; `boardStateKey` (cpp:48800-48801) is computed but, per the comment at cpp:48802-48804, kept only for the cross-phase entry. The repeat-latch comment describes the pre-#W79-CZ key.
- **cpp:58033 removed-mechanism block calls prose recovery "the purest form of the tolerance invariant 000 forbids"** — **CONFIRM.** cpp:58031-58035 describes `restatedCombatDirective` (DELETED) and cites an invariant that the owner's 2026-09-12 ruling superseded (quoted verbatim at cpp:8994-8998: "no such rule exists. Explicitly the rule is to 'make the parser robust'"). Archaeology that also mis-states current doctrine. Adjacent to my C6 (the two helpers it orphaned).
- "Blanket removal of wave tags is not justified" — agreed; my I12 proposes moving the incident narrative out, keeping every WHY.

### A.5 NEEDLESS COMPLEXITY §2

- **Duplicate activation-cost scanners** — merge with A.1 (exact duplicate, conceded).
- **Draw-forecast emitters + dispatcher** (cpp:16011, 16079, 16137) — **SAME** as my §1.6 row (`theirDrawStepForecastText` 16012 / `drawStepForecastText` 16075 / dispatch 16138). Their "compute one fact object, format with subject/possessive fields; a careless boolean merge could lose the ownership distinction" is the right shape of the fold.
- **Verdict prose → string keys → danger-rank substring parsing → telemetry by prose substring** (cpp:34580, 36730, 36612) — **SAME** as my X6 (the W68→W81 lineage, `w79VerdictDangerRank` sniffing "LETHAL"/"none", `w80CrackBackFaceOfLine` grepping English back into a face). Their typed-verdict proposal = my `CrackBackFacts` struct (I7).
- **Two hold stores with parallel lifecycle** (h:1922, cpp:38124, 38242) — **CONFIRM** (new). `mHoldRows` (`map<string,set<string>>`, h:1922) and `mHoldLatchRows` (`map<string,vector<string>>`, h:1925) are cleared together (cpp:37924-37925 "the two stores are retired together"), erased together (cpp:38169-38170), and populated together in `takeHold` (cpp:38242-38257) under the same `seam` key; the lookup at cpp:38124-38128 prefers the list and falls back to the set "for a seam with no recorded list". Because both are written in the same block under the same key, the fallback branch is unreachable in the current code — their "preserve it explicitly until checked" can be closed: nothing reaches it. One nuance for the fold: the set has a second reader, the sibling-window rule at cpp:37963-37964 (#W72-BU), which fired 0 times in this corpus (my X1 row "sibling-window cover … 0"); folding to one store must either port that reader or retire the sibling rule with it.
- **Ask cache → repeat latch → cross-phase replay with cross-invalidations and separate refusal maps** (cpp:48846, 48909, 49008) — **SAME** as my X1 (the fourteen gates) + X10 (three run counters, two refusal helpers). Their "what it loses" ("some current round-trip savings come from reusing answers after state changes … not lossless under the owner's key rule") is the same honest loss my X2 states.
- **Render a vocabulary, then maintain a parallel vocabulary to strip it** (cpp:33211, 35913, 37235) — **SAME** as my X8 (`stripNarrationDecoration`'s 58 hand-mirrored heads) and X2 (the eight normalisers of one row set).
- **"The keys include too little, not too much"** — **SAME** as my X2 ("the serialised board was REMOVED from the key in #W79-CZ … replaced by a digest of legal continuations"). Merge (§C).

### A.6 FLAWS §3

**A-F1 — legal actions capped or suppressed (HIGH)** — items 1-3 are **SAME** as my F3.1-3 (pass-decline cap cpp:46743-46758, animation withhold 46702-46711, flip cap 46689). Item 4 and the rules/corpus evidence are new:
- Item 4, **fetch consume-on-choose stamp** (cpp:48045) — **CONFIRM.** cpp:48040 sets `mPassDeclineCount[fetchLineKey(row)] = 2` (the cap) and cpp:48045 stamps `mPassDeclineBoard[...] = string()`; the re-opener at cpp:46748-46751 explicitly skips an empty stamp (`!db->second.empty() && …`), so the row stays withheld for the rest of the turn whatever the board does. `fetchLineKey` (cpp:46353-46357) strips only the `" targeting …"` tail, so the key keeps the rendered card name and ordinal; whether an ordinal-less second fetch of the same name can be caught by the first's stamp I **CANNOT DETERMINE** without a board that prints one. As stated for one physical fetch it is a second-crack-of-a-sacrificed-card case and costs nothing; the doctrine point (a withhold with no re-opener) stands.
- **Rules argument on the animation withhold** (cr:5983 613.4b, cr:6021 613.7, cr:6031 613.7b, cr:1859 117.1b) — **CONFIRM** all four exist as cited. 613.7b's "a continuous effect generated by the resolution of a spell or ability receives a timestamp at the time it's created" is the mechanism by which a re-activation is not a no-op when an intervening effect set the same characteristic — a sharper argument than my F3.2's "usually a no-op".
- **Corpus effect** — **CONFIRM.** 47 prompts carry `{if you pass here, this option is not offered again until the board changes}` (`lastOfferClause`, cpp:46413-46422, rendered when one more decline reaches the cap); `130v162` line 60 = `priority` seq 66, turn 18, Draw: rows 1-11 carry the clause, and the same prompt says "You will have priority again later this turn, so instants and activated abilities you hold stay castable this turn" — the contradiction they describe is on the page. This also corrects my own F3.1/§5.3 (see §B.9).
- Rank **HIGH agreed**; merge with my F3.

**A-F2 — ask/async keys are not BOARD STATE + QUESTION (HIGH)** — **SAME** defect as my X2; their sub-claims are new and **CONFIRM**:
- "only the cast seam stages the digest; target/cost/menu callers can use an empty one" — `mNextContinuationDigest` is assigned at exactly one site, cpp:51948 (`FindCardToPlay`), and swapped into `mContinuationDigest` at cpp:48584-48585 on every `askModel` entry; `chooseTarget`/`chooseCostTarget`/`chooseMenuAction` stage nothing, so their ask keys are scope + normalised rows only (the comment at cpp:48581-48583 admits it: "an unstaged seam keys on the scope alone").
- "its commentary explicitly excludes life totals" — cpp:36449-36450: "No life total, no log line, no annotation text and no stack TEXT enters it".
- The repeat latch "with the board moved under it" — cpp:48899-48903, as quoted.
- Rank: I filed this as complexity X2 and asked the owner to rule on #W79-CZ's departure; the other report files it as a HIGH flaw. I **concede the rank** (§B.8): under the brief's own order a cached answer served over a board the model never saw is a window that did not reach the model.

**A-F3 — the ordinary AI wait gate prevents the watchdog from running (HIGH)** — **CONFIRM** (new; it also overturns part of my F10, §B.4).
- `baka:5878` `if (decisionPending(dt)) return 0;` is the first statement of `AIPlayerBaka::Act`, before the timer (baka:5915-5921) and before `computeActions` (baka:5940).
- `AIPlayerGPT::decisionPending` (cpp:52317-52336) calls `releaseHoldIfUntapPassed()`, then on `asyncBusy()` extends the interrupt offer and returns `true`. `asyncBusy()` (cpp:18725-18737) is `status == 1` on either arm. No reap in that path.
- `reapWedgedRequests` (cpp:18855) has exactly one caller, cpp:19410 inside `pollCompletion`; `pollCompletion` is reached only through `pollCompletionRetry` from the seams (cpp:47618, 47840, 47914, 49061, 49178, 49230, 59643, 60742, 61588, 61938, 62658, 62712).
- Therefore while a worker holds `status == 1`, the Act-driven seams (priority, cast, target, menu, attackers, blockers) are never re-entered and the in-flight bound is never evaluated. The exits that remain are the transport returning (libcurl's timeout publishes an empty body, `status = 2`, and the next tick consumes it normally) and the patience dialog (`aiPatiencePromptAnswer`, cpp:52393-52410 — user-driven, clears `mEndpoint`, bumps no generation). The `WAGIC_GPT_WEDGE` emulation (cpp:18206) is dev-gated; nothing in the file shows it exercised through `Act`.
- The seams that poll from outside `Act` — reveal (cpp:61588, driven from `AllAbilities.cpp`), pregame bottom (61938), cleanup discard (62658) — do reach the reap; the finding is correctly scoped to "the ordinary AI path".
- History (read-only git): the entry gate is from `b192afd1c` (2026-07-12, "W3c/c5d: delete AIPlayerGPT's mirrored Act body - async gate becomes a hook"); `reapWedgedRequests` arrived in `b9feba4e0` (2026-09-03, wave-57 lane U). The bound was born behind the gate.
- Rank **HIGH agreed** as a hang class, with the caveat the other report itself gives: it needs a transport that never returns. Corpus: every record `transport=curl=0,http=200`, `abandoned_in_flight` 0 — consistent with both "never wedged" and "never reapable".

**A-F4 — a discarded worker can destroy a locked mutex (HIGH, conditional)** — **CONFIRM** (new; concedes part of my F15, §B.5).
- cpp:18263 `std::lock_guard<GptMutex> g(ctx->state->mtx);` then cpp:18270-18274 `if (ctx->state->gen != ctx->gen) { …; delete ctx; return; }` — inside the guard's scope. `WorkerCtx::state` is the `shared_ptr<AsyncState>` (cpp:18181); `AsyncState` holds the mutex by value (h-side struct at cpp:32820 ff., `GptMutex mtx`). If that `shared_ptr` is the last reference, `delete ctx` runs `~AsyncState` — and `~mutex` on a locked mutex — before `g`'s destructor unlocks it. `GptMutex` is `std::mutex` on desktop (`include/GptConfig.h:245`) and a kernel-mutex class on Vita (`GptConfig.h:231-241`); destroying a locked `std::mutex` is undefined behaviour.
- Preconditions, verified: the generation is bumped only by `reapWedgedRequests` (cpp:18885, in place on the same object — the seat keeps its `shared_ptr`), so the seat must also have been destroyed (h:1127/1135 are the seat's references). Given A-F3, a reap can only originate from the reveal/pregame/discard seams, which narrows the window further; the patience path does not bump `gen`, so it cannot trigger this.
- Rank: **HIGH-conditional agreed** as a source-level lifetime defect; practical frequency is low and the fix is a one-liner (copy the `shared_ptr` to a local before taking the guard, or unlock before `delete`).

**A-F5 — the W81 crack-back verdict adds blockable damage as unblockable (HIGH)** — **CONFIRM** (new sub-defects on the emitter my F4 already indicts; merge as F4(e)/(f), §C).
- `crackBackFloorSources` (cpp:28774) adds each animator's `bestRungPower` into `sizeable` (cpp:28842-28845) alongside pinger damage; `w81CrackBackVerdictLineNow` (cpp:37552-37565) hands that `sizeable` to the line as `addUp`; `w81CrackBackVerdictLine` (cpp:36612) computes `addenda = addUp + compulsoryDraw` (36620), `floorNow = bestBlockFloor + addenda` (36625) and prints "… and N of it is damage no block removes" (36649). An animated land's combat damage is blockable (cr:4805, 510.1c); only the pinger/compulsory-draw share is not. The best-block DP (`w80CrackBackBestBlockFloorNow`, cpp:37470) walks creatures only, so the animator is excluded from the block search and then added back as if unblockable — the static counterexample holds.
- Joint affordability (cpp:28817-28820): each permanent is priced against `oppNextTurnManaReach(opp, c)` on its own and the results are summed — two four-mana manlands over five sources both count. **CONFIRM.**
- Corpus motivation — **CONFIRM:** 60 prompts contain `ADD THOSE UP`; `130v152` line 7 = `priority` seq 6, whose FLOOR clause names "Lair of the Hydra (best rung their mana pays for: 4 power once animated …)" — the same record my F4(b) cites for the unsized shape.
- Rank **HIGH agreed**, code-only (the corpus ran wave-80 binaries; the W81 line has no measurement).

**A-F6 — an unambiguous label-less answer is rejected because prose follows it (MEDIUM)** — **CONFIRM** (new). cpp:9020-9022 (the third of the five clauses): "that line is the LAST non-blank line of the reply. An answer with anything said after it is not the reply's last word"; the reader `w79LabellessAnswerLine` (cpp:9033 ff.) enforces it via `nonBlankAfterCand`; `gptAnswerSegment` (cpp:25266-25268) then returns `false`. Their example (`PLAN: …` / `2 (Cast the creature)` / `Thank you.`) is refused although exactly one line has the answer shape and it follows the plan — the ruling quoted three lines above (cpp:8994-8998) asks for the answer "wherever it unambiguously is". Rank **MEDIUM agreed**; it is static — the corpus's single `unparsed_reply` fallback (`152v125` seq 101) is a different shape (`CHOICE: Cast Fateful Absence targeting Essence Scatter`, a name with no number), not this one.

**A-F7 — legal "no-op" answers are intercepted and re-asked (MEDIUM)** — **CONFIRM** (new; my X4 listed the lanes as machinery, not as a doctrine breach). Predicate cpp:39300-39303 (`rowSaysNoOp && !rowIsDeclineRow`); budgeted lane cpp:49185-49206 (`(namedRowFail || passOnNoPass || noopRowZero) && !reasked` → `[RE-ASK]`); budget-exempt lane cpp:49150-49182 (`reasked && noopRowZero && mAskNoopReaskKey != askKey0`). The code's own defence (cpp:49162: "the second answer executes as given, whatever it is") means no option is removed — the cost is a round trip and a nudge, and the trigger is the engine's judgment that a legal row does nothing, which is the judgment the ruling withholds from the engine. Corpus: `noop_row_zero_reask` fallback 0 records (re-ask lanes fired 0 times overall). Rank **MEDIUM agreed**, dormant here.

**A-F8 — "enters tapped" promoted to a turn-long prohibition (MEDIUM)** — **CONFIRM the wording, rank lower.** `landEntersTappedTagFrom` cpp:49992-49996 (unconditional branch "enters TAPPED - it makes no mana this turn") + cpp:50004-50005 ("- it taps for mana from your next turn on"); the MDFC twin hedges "unless something untaps it" (cpp:32959-32960); cr:7047/7051 (701.26a-b) as cited. Corpus: 67 prompts carry the absolute wording; `123v126` line 4 = `ask` seq 3, row 1 "Play Arcane Sanctum [enters TAPPED - it makes no mana this turn …]", answer `Play Underground Sea` — confirmed, and that choice is right regardless of the wording. Whether the 7-deck pool holds any effect that untaps a land the turn it enters I **CANNOT DETERMINE** from this file; absent one, the sentence is true on every board this corpus put. I would rank it **LOW** (a hedge to borrow from the MDFC twin), not MEDIUM.

**A-F9 — land-play availability mislabelled as land-drop usage (MEDIUM)** — **CONFIRM, and the corpus supplies what the other report called a static finding.** `landDropStatusLine` (cpp:9694-9720): `playable` → "NOT yet used this turn"; `!playable && haveLand` → "ALREADY USED this turn - you cannot play another land until your next turn". Inputs at cpp:31612-31631: `playable = !legalLandPlays().empty()`, then `mdfcBackFaceLandStatus` (cpp:32776-32800) sets `haveLand = true` for any held modal-DFC land face and `playable = true` only if its AAFlip ability `isReactingToClick` — which is sorcery-timed. So on the seat's own Upkeep (or any non-main window) with an MDFC in hand and no real land, the line reads ALREADY USED before any land play. Corpus: 14 prompts (all deck146 seats, Agadeem's Awakening in hand, no `[land` entry in the hand line) print "Land drop: ALREADY USED this turn" on a turn whose narration carries no `- You played …` line; four of them are Upkeep windows where a land play cannot yet have happened: `146v162` seq 18 (t11) and seq 53 (t17), `146v125` seq 68 (t23) and seq 136 (t31). cr:3391 (305.2a) as cited. Rank **MEDIUM agreed**, now on real screens. Merge note with my F3.7 (§C).

**A-F10 — some legal menus are auto-answered (ruling violation)** — **CONFIRM the four mechanisms**; two are mine. cpp:48661 `optionsIn.size() == 1 && !askEvenIfSingle → return 0`; cpp:48675-48679 `identicalInterchangeableRows` (cpp:11608-11620: rows byte-equal and ordinal-free); cpp:55074-55089 single-outcome repeat-pay (my F12); cpp:55164 `cands.size() == 1 → return cands[0]` on the cost seam. Nuances from the source: `declineRowOffered` (h:699 comment block) means a decline row is appended INTO the options vector by the caller, so a one-row vector has no hidden decline and the one-option shortcut loses nothing; `identicalInterchangeableRows` requires the fully annotated rendered text to match, which carries target identity and counters, so "rendered strings ≠ engine consequences" is right in principle but I found no pool case where it bites. The one that costs is the repeat-pay case (my F12: answered on the planner's word, unauditable from the record — C21). Corpus: 3 `menu_single_outcome` records; `152v130` line 7 = seq 6, 21 rows, Intrepid Adversary, answered 0 "don't add any counter" — confirmed. Rank: the other report gives none; as literal ruling conflicts these are the owner's call, and only F12 is load-bearing.

**A-F11 — `mAskScopeDigest` grows for the whole game (LOW)** — **CONFIRM** (new). h:2226; the only sites are cpp:48815 (find), 48816 (compare), 48823 (insert) — no erase anywhere, unlike `mAskCache` (cleared per turn at cpp:48648-48653). The key is `w79AskScopeKey(turn, phase, "") + keyTailStr`, so entries from earlier turns can never match again. Bounded by game length (one entry per distinct ask window; keys are the normalised row text, KB-scale). Rank **LOW agreed**.

### A.7 IMPROVEMENTS §4

- **A-I1 render the combat conclusion once** — **CONFIRM the cited document**: `152v125` line 21 = `attackers` seq 20; the ATTACK TOTAL line reads, in order, "puts them at 16. That is NOT lethal … At least 2 damage lands whatever they BLOCK, but that floor is over their BLOCKS ONLY … so it is NOT a floor on what lands - they would be at 16. Both figures are BEFORE their attack triggers … not folded into either number. FOLDED, for the all-in declaration this line prices: …". Overlaps my I6 (fold the three-sentence ATTACK TOTAL) and I7 (compute once, numbers only). Merge.
- **A-I2 consolidate repeated timing explanations** — **CONFIRM the cited document**: `125v123` line 38 = `discard` seq 73 (12,254 B): the sorcery-speed explanation appears 2×, the "it must have a target … HELD" explanation 5×. **SAME** as my I4 (the two hand tags, 892 KB corpus-wide). Merge.
- **A-I3 emit only the range grammar used** (`kOptionRangeNote` cpp:10967) — **CONFIRM** (new). The constant is 1,675 B; 99 prompts carry it, all 99 have at least one plain `N-M.` row, 0 use the X-range or "copies" forms it also explains. Cost 166 KB = 0.6 % of prompt bytes; the renderer already gates the note on a range existing, so the win is the unused paragraphs only. Agree, small.
- **A-I4 whole-order answer for blocker ordering** (`orderBlockers` cpp:52132) — **CONFIRM the mechanism** (new): cpp:52141-52150 "Sequential single picks … each round asks which blocker is dealt damage next". Corpus: 3 ordering asks in 21 games, so the round-trip saving is negligible in this pool; the protocol-extension cost they name is real. Low value; the owner's call.
- **A-I5 navigable by responsibility, typed fact objects** — **SAME** as my I12 (+ X6/I7 for the typed verdict). Merge.

### A.8 §5 "Could not determine" — nothing to verify; their limits 1-6 match mine (no runtime, no exhaustive 298-family disposition, generic gameend consumer, no full system message in the corpus, no causal estimate, no protocol change).

---

## B. Responses to disputes of my own findings

**B.1 `cannotPayNowClause` — mine: DELETE (§1.4); theirs: KEEP, "not established dead".** HOLD, narrowed. We agree it is live (caller cpp:43673) and 0-render in 2,135 prompts. My DELETE was never a dead-code verdict; it is a redundancy verdict under the owner's "minimum tokens for full value" criterion: when its three coincident facts do fire, the row already prints `[cost: …]`, the header already prints `Mana available: 0 total`, and the hand line already prints `[cannot pay now: needs N mana, you have 0 untapped sources]` (cpp:31256). Narrowed wording: "not crud by the caller test; a duplicate-of-three-lines candidate for the owner's I6 fold". Their KEEP is correct on their test.

**B.2 `lineIsActivatedCost` / `w80ScriptLineHasActivationCost` — mine: "J=0.52 near-duplicates, both live" (§1.6); theirs: exact duplicate.** CONCEDE. The bodies are the same automaton (A.1); my Jaccard measured comments. DELETE-one, redirecting cpp:16502/29411/36848 and the two self-test bridges (cpp:63098, 63526).

**B.3 The 27 zero counters — mine: "no consumer" (§1.3); theirs: the generic gameend dump is a consumer, so "unconsumed" is not established.** HOLD, wording narrowed. My §1.3 already named `corpus-stats.py:465-489` as "a generic dump of every int key" and rested the DELETE on a second test — each counter has a per-record join (`forced_close`, `hold_event`, `wall_miss`, `menu_single_outcome` records; per-record `action_before_plan*` fields) from which the number is a `grep -c`. Deleting one changes a printed zero in `GAMEEND SUMS`; nothing reads that zero. Narrowed claim: "no INTERPRETING consumer; the only reader is the generic dump; every deleted counter is recomputable from a record the same log already carries". The two counters I kept conditional (`identical_ask_answers_reserved`, `crackback_*`) stay conditional.

**B.4 My F10 ("the whole prompt is rebuilt on every AI tick while the model is thinking … ~900 full prompt builds per decision … Not a hang (all bounds verified: in-flight abandon cpp:18840-18848 …)") vs their F3.** CONCEDE on both counts. (a) `baka:5878` returns before the timer and before `computeActions` while `asyncBusy()`, so the Act-driven seams are NOT re-entered during flight; the prompt is rebuilt at launch and at the consume tick (plus each stale-drop/relaunch cycle), not per tick. The ~900 figure and the handheld per-frame DP cost are withdrawn; the residual is one wasted build+DP on the consume tick (cpp:47380-47492 / 48568-49061 build before `pollCompletionRetry`), and the `orderBlockers` comment "re-polling while a call is in flight replays the prefix" (cpp:52141-52143) is archaeology. I10 ("poll first, build second") shrinks to that residual. (b) "all bounds verified" was wrong for the in-flight bound: it is unreachable on the path it was written for (A.6 F3). My `kStaleLivelockLimit` and `kW76CloseDeferMaxTicks` bounds stand — they live on paths that are entered.

**B.5 My F15 ("Worker lifetime is safe for seat destruction … generation bump cpp:18262-18274") vs their F4.** CONCEDE for the discard branch. The publish branch is safe (the guard unlocks before anything is freed); the generation-mismatch branch deletes the context — and possibly the `AsyncState` — under the guard (A.6 F4). Narrowed: "safe on publish; UB on discard when the seat is already gone".

**B.6 `revealSinglePickDeclineLegal` — mine (§1.9): called from `AllAbilities.cpp:1348-1350`; theirs: bridge-only, test support.** HOLD; theirs is refuted on the source (A.2). Disposition agrees (keep).

**B.7 Own-loop emitters — theirs "KEEP—live, 18 prompts"; mine "20 lines rendered, fold six into one".** No contradiction; both numbers are true (18 prompts, 20 lines) and both keep the fact. The fold stays a complexity proposal.

**B.8 Ask-key ruling drift — mine X2 (complexity, owner to rule on #W79-CZ); theirs F2 (HIGH flaw).** CONCEDE the rank. By the brief's order (an answer served without reaching the model ranks first) a key that omits the board is a HIGH flaw, not a design question. My X2's honest loss (the 233 same-phase re-asks #W79-CZ measured) is the cost of the fix, not a reason to keep the drift.

**B.9 My F3.1 / §5.3 ("No record, no rendered bracket; only `DebugTrace`" for the pass-decline cap).** CONCEDE the detail. `lastOfferClause` (cpp:46413-46422) renders `{if you pass here, this option is not offered again until the board changes}` on the window BEFORE the withhold — 47 prompts in this corpus — so the model is warned once; the withheld window itself still carries no receipt, and "how often a row was actually withheld" (§5.3) remains unmeasurable from the record.

**B.10 Crack-back LETHAL/none variants; census Tier A "not a safe deletion list"; `w81FoldDuplicateCoverParagraphs`; macro-mediated diagnostics; engine-called reveal hooks** — agreement on all; no response needed.

**B.11 A-F8's MEDIUM vs my LOW** — not a dispute of mine (I did not make it); stated in A.6 for the ledger.

---

## C. Merge notes — same defect, different names/anchors (do not double-count)

| astra.out | fable.out | note |
|---|---|---|
| §1.1 combat-restatement helpers (57986/58016) | C6 | identical |
| §1.1 `loopCautionForBoards` | C3 | identical |
| §1.1 `mAskReplayKey`/`mAskReplayRun` | C17 | identical |
| §1.1 duplicate plan assignments (25722) | C16 (25744-25745) | identical; my anchor is the second, joined-line copy |
| §1.1 exact-duplicate scanners + §2 row 1 | §1.6 last row | one item; my "near-duplicate" wording conceded |
| §1.1 `plan_echo_count` | §1.3 unread record fields (`plan_echo_count` in the 53) | one item; theirs is the sharper named candidate |
| §1.2 whole table + KEEP-DORMANT | §1.4 rows 5-6 + §1.9 | same corrections of the census; only `revealSinglePickDeclineLegal`'s reasoning differs (theirs refuted) |
| §1.3 `force_close_arms_*` alias | C22 | identical |
| §1.3 generic gameend consumer | §1.3 consumer census | same fact, different conclusion (B.3) |
| §2 draw-forecast emitters | §1.6 `drawStepForecastText` pair | identical |
| §2 verdict prose→keys→rank→telemetry | X6 | identical lineage; their typed verdict = my `CrackBackFacts` (I7) |
| §2 ask cache→repeat latch→cross-phase | X1 rows 13-14 + X10 | identical |
| §2 vocabulary then stripper | X8 + X2's normaliser chain | identical |
| §2 "keys include too little" + F2 | X2 | identical defect; rank conceded to HIGH (B.8) |
| F1 items 1-3 | F3.1-3 | identical; F1 item 4 (fetch stamp) extends F3.1 |
| F5 | F4 (b)/(d) + X6 | same emitter lineage; F5's two defects are new sub-items — record as F4(e) blockable-addenda-as-unblockable and F4(f) joint affordability |
| F9 | F3.7 | different defects on the same line (`landDropStatusLine`): mine = the land-face decline latch makes NOT-yet-used false; theirs = the MDFC/`playable` substitution makes ALREADY-USED false. Keep both, cross-reference |
| F10 (repeat-pay + cost target + one-option + identical rows) | F12 (repeat-pay) + F3.5 (ETB auto-answer) | F10's repeat-pay = F12; the other three shortcuts are new sub-items under one "auto-answer" heading |
| I1 | I6 + I7 | same proposal (compute once, one conclusion) |
| I2 | I4 | same proposal (hand-tag/timing consolidation) |
| I5 | I12 | same proposal (responsibility modules, typed facts) |
| F3 (watchdog) | F10 (rebuild per tick) | NOT the same defect — but F3's gate is what falsifies F10's tick count (B.4); ledger F10 as narrowed, F3 as new |
| F4 (mutex) | F15 (lifetime) | F15's "safe" claim narrowed; F4 is the new defect inside the region F15 cleared |

---

## D. What remains single-sourced after this round

I did not independently re-read, and therefore the ledger should mark as verified only by `astra.out`: the internals of `namedCastPenaltyScan`'s life-surcharge path (cpp:41328 ff. — I confirmed callers, not the arithmetic); `w75ApplyChainFeed`'s early-return predicate on the opponent's loop state (cpp:34336 — callers confirmed, body not reopened); their reading of `chooseTarget` and the attacker/blocker declaration seams beyond the anchors named above (my own coverage of those seams came through delegated readers whose top findings I spot-checked, so those regions are two-sourced only at the finding level, not line by line); their I5 module boundaries (a design judgment, not a checkable claim); and their statement that the corpus's `system` records do not contain the whole sent system message (I relied on the census's own note for the same limitation). Everything else in `astra.out` — every crud row, all six complexity rows, F1-F11 and I1-I4 — was checked this round against the current source lines, the CR text where a rule is cited, and the corpus where a record is cited. Conversely, the regions of MY report that `astra.out` did not touch and that stay single-sourced are: F1 (opponent-turn crack-back verdict), F2 (forced-close byte-equality gate), F5 (three mana counts), F6-F8 (tapped-tag scope, vigilance sentence, double strike/planeswalker on the blockers seam), F11 (cross-record telemetry), F13 (cast-seam loop auto-pass), F14 (wording family), the counter/field censuses of §1.3, and X3/X5/X7/X9 — those await the other reviewer's cross-pass.
