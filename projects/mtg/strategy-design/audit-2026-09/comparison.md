# Engine-delta audit — Astra vs Fable comparison (2026-09-12)

Scope: rules-engine files only, 056055590 (2026-09-03) .. HEAD 4a4f17a25; 191 hunks, +1,934/−190.
Both reviewers: read-only, same prompt, CR text on disk (file says effective 2026-08-07; the WotC page labelled it 20260819).
Astra: 34 groups, KEEP 24 / REWORK 9 / REVERT 1 (raw hunks 100/90/1; 43 of the REWORK hunks = the ward script sweep). Scryfall fetches FAILED (sandbox) — card verdicts from script text.
Fable: 36 groups, KEEP 179 / REWORK 12 / REVERT 0 (by hunk). Scryfall fetched for 20 cards.

## AGREED — both flag, same mechanism (act on these)
1. EDICT AUTO-REPLACEMENT (AllAbilities.cpp:6756-6836 / :6808, ActionStack.cpp:437-466). Tribute to Hunger / Devour Flesh: fixes a REAL double-lifegain (CR 608.2h) but the engine now picks the victim (lowest toughness) when the HUMAN is the targeted seat — Oracle says "of their choice". Astra #1, Fable #2. Astra adds: can select phased-out candidates, never verifies the sacrifice happened. HUMAN-FACING RULES REGRESSION (Vita).
2. PARKED-REVEAL DRAW RESCUE (MTGGameZones.cpp:386-421). Symptom patch for the LLM seat's stalled Idyllic Tutor; hides a stalled reveal from the deck-out check. Astra: REVERT (its only one). Fable: KEEP but "dangerous as a pattern". Same diagnosis, different threshold.
3. DESIGNATION-MARKER GUARD at the universal chooser (TargetChooser.cpp:1646-1665, 1953-2015). Outcome rules-correct (CR 110.1/114.1 etc.), but a hand-kept name list on every canTarget; two shipped binaries looped (Day/Night) and each repair claimed fixed. Fable #1, Astra #10. Astra: "incomplete rollback is hazardous" — keep the final form.
4. DECLINED-LAND FINGERPRINT / FindCardToPlay latch (AIPlayerBaka.cpp:3117-3231, :3174, DecisionContract.cpp:633-660). LLM livelock breaker sitting in Baka's per-tick walk; fingerprint omits mana/taps/counters (Astra) ; ordinary Baka never arms the latch, yet pays an ostringstream per land per tick (Fable). Astra #8, Fable #5.
5. ACT() PASS DEFERRAL + FORCED-PASS FLOOR (AIPlayerBaka.cpp:5731-5841 / :5793). Third rewrite in nine days; middle version shipped unbounded (2.58 M iterations). Fable: keep, now bounded. Astra: count-based "no progress" can force Baka to pass over a productive action. Astra #4, Fable #6.
6. EXPIRED-MENU / MENU-ROW IDENTITY MAP (ActionLayer.cpp:585-650, :609). Fixes a real SIGABRT; both note a vanished mandatory row leaves no completion path. Astra #7, Fable #8.
7. IDYLLIC TUTOR MANDATORY FALLBACK (AllAbilities.cpp:1342). Astra #5: forces a find CR 701.23b lets the player skip. Fable: REAL for the LLM stall, treats the one-card choice as mandatory — Fable did not weigh 701.23b (fail-to-find). Astra's citation is the correct rule; count as agreed-on-fact, disagreed-on-verdict.

## DISAGREED
A. WARD SCRIPT SWEEP (43 hunks, _macros.txt:194-197 + borderline.txt + Elderwood Scion + Kasmina). Fable: KEEP — rules-correct narrowing (CR 702.21a) of a chooser that could pick a library card. Astra: REWORK #2 — 12 named cards (Boreal Elemental, Callaphe, Charix, Esior, Jubilant Skybonder, Kaervek's Torch, Kopala, Pursued Whale, Sphinx of New Prahv, Syr Elenora, Elderwood Scion, Kasmina) are COST INCREASES modelled as pay-or-counter, and the rider still binds an arbitrary stack host, not the offending spell. Fable concedes the cost-increase approximation is pre-existing and ward-vs-ability was non-functional both before and after. Checkable: the 12 cards' Oracle text.
B. BAKA OWN-TURN INSTANT ARM (AIPlayerBaka.cpp:3890-3952). Astra F16: KEEP, "a real Baka improvement". Fable #4: rationale CONTRADICTED — ActionStack.cpp:1347-1380/1530-1534 already offered the active player the NextGamePhase window; the deficit was the GPT seat's; the fix moved Baka's (Vita opponent's) decision order. Checkable by a Baka-vs-Baka A/B on instant-heavy decks (the running 840-game A/B partly covers it).
C. GLOBAL PARSER SUPPRESSION BIT (MTGAbility.cpp:5735, gAbilityParseZoneGatedNull). Astra #9: nonlocal suppression of validation, unsafe by construction. Fable G29: KEEP, validator-only blind spot, no gameplay effect.
D. ATTACK DESTINATION CONTRACT (DecisionContract.cpp:144, planeswalker/battle attacks). Astra #6: stale snapshot index applied to rebuilt choices, battle owner model wrong. Fable: not in its top ten (KEEP).
E. propagateSource EXTRA-COST REBIND (MTGAbility.cpp:7987-8016) and restrictionUsableNow (LegalActions.cpp:357-397): Fable ranks #9/#7 on breadth; Astra KEEP without ranking. Both say rules-correct.

## RATIONALE-CONTRADICTED-BY-CODE — union (Astra 15, Fable 5; overlap 4)
Overlap: edict "re-chooses" is not the player's choice; fingerprint does not move on mana/taps/counters; "single choke point" — ordinary Baka answers menus via ActionLayer directly; the designation-marker fix claimed fixed twice while looping. Astra-only of note: "emblems are in no zone" (CR 114.1: command zone); "every changed chooser was ward" (12 are taxes); "no legal option removed" by stale-row handling (vanished mandatory rows not rebuilt); "dev-only" replay bypass compiled without the dev guard. Fable-only: the L6 "narrow scope" claim disproved by the next commit's own fixture (Proper Burial lifegain suppressed); the Act() floor was conditional on hasAnyLegalAction and never fired during the livelock.

## Both could not determine
ward vs ABILITY on the stack (either side); human auto-tap sharing planPayment (Overgrown Battlement fix); reveal shapes outside the enumerated ownership set; edict lastController after control change; emblem runtime type; the GPT-side consumer of every contract change (excluded by brief); performance (no runs).

## Bottom line
Neither reviewer supports reverting the nine-day range wholesale (both name real fixes it would lose: stale-index SIGABRT, granted-cost ownership, counter-event grouping, double lifegain). Both name the same first intervention set: (1) edict victim = the affected PLAYER's choice at resolution; (2) remove the draw-rescue and fix the reveal resolution it hides; (3) tutor searches may legally fail to find; (4) expired menus get a completion/rebuild path and "no progress" stops being count-based; (5) separate ward from cost taxes and bind ward to the offending stack object. The pattern both describe: a symptom seen on the LLM seat fixed in a SHARED path (Baka / human / rules), with the shared consequence untested.
