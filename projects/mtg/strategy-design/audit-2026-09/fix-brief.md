# Engine-audit fix lane (2026-09-14) — six reviewer-agreed defects in shared engine paths

Owner rulings governing this lane: "if these make the heuristic play worse (gameplay outcomes, engine performance or crashing), then it's a problem" — every fix ships through the HEURISTIC A/B GATE (tools/baka-ab.sh + tools/baka-ab-compare.py vs archives/baka-ab/4a4f17a25): HARD FAIL on any unfinished game; a |z|>=2 per-deck flag must be explained. Rules truth = the official CR on disk (~/.gatelogs/engine-audit/cr.txt, grep the rule number; never from memory) + the card's Oracle (Scryfall) AND its `auto=` script. Never remove, cap, or auto-answer a legal option. No AI attribution. Suite THREADS=1 0 failed; PARSETEST 0 failed (count may stay equal — this is ENGINE work, pin with SUITE fixtures under Res/test/, not PARSETEST). Every wagic run under `systemd-run --user --scope -q -p MemoryMax=4G -p MemorySwapMax=0 --`.

Sources: audit-2026-09/FINDINGS.md, audit-2026-09/comparison.md (Astra vs Fable), ~/.gatelogs/engine-audit/astra.out (F02, F03, F04, F05, F07, F09) and fable.out (G17, G32, G10, G21, G8, G35, G20).

1. EDICT = THE AFFECTED PLAYER'S CHOICE (AllAbilities.cpp ~6756-6836, ActionStack.cpp ~437-466; Tribute to Hunger / Devour Flesh, script `dynamicability<!mytgt toughnesslifegain targetopponent!> sacrifice!$ targetedplayer`). Oracle: "Target opponent sacrifices a creature of their choice." Keep the double-lifegain fix (CR 608.2h). REPLACE the lowest-toughness re-pick: when the victim pointer is stale at resolution, the TARGETED PLAYER chooses again — human seat = a real sacrifice chooser (the same UI a plain `sacrifice` cost uses), AI seat = its own chooseCard. Also (Astra): exclude phased-out candidates; verify the sacrifice actually happened before paying lifegain. Suite fixtures: human-seat edict re-choice presents a choice (no auto-pick); AI-seat edict; Proper Burial lifegain still paid.
2. REMOVE THE PARKED-REVEAL DRAW RESCUE (MTGGameZones.cpp ~386-421) and fix what it hides: a reveal that cannot complete must resolve/close (cards return to their zone) at the reveal driver, not be papered over at draw time; a deck-out with cards parked in the reveal zone must not be silently survivable. CR 701.20b (revealing doesn't move the card) — the reveal zone is a modelling artifact; make the library count include parked-revealed library cards OR close the reveal first, and say which. Fixture: the Idyllic Tutor stall shape from the wave-53 corpus.
3. SEARCH MAY FAIL TO FIND (AllAbilities.cpp ~1342, the mandatory-reveal first-card fallback). CR 701.23b (grep `^701\.23` in cr.txt): a player searching for a card with a stated quality may fail to find one even if it is there. The fallback must offer "find nothing" as the answer, never force the first card; the LLM/AI seat's stall is fixed by making the empty choice legal and terminating, not by forcing a pick.
4. EXPIRED MANDATORY MENU GETS A COMPLETION PATH (ActionLayer.cpp ~585-650 / ~609): keep the row-identity map (real SIGABRT fix) but when a mandatory menu's rows vanished, REBUILD the decision from the current legal set instead of leaving an unanswerable menu. Fixture: a mandatory menu whose row is removed by a resolved trigger.
5. ACT() PASS FLOOR — REPLACE COUNT-BASED "NO PROGRESS" (AIPlayerBaka.cpp ~5731-5841, third rewrite): a forced pass may fire only on a proven identical-state repeat (same legal-action set + same board digest), never on a count of iterations; keep it bounded. Fixture: a productive repeated activation (e.g. a mana ability used twice) is NOT force-passed; the wave-71 livelock shape IS.
6. DECLINED-LAND FINGERPRINT OUT OF BAKA'S PER-TICK PATH (AIPlayerBaka.cpp ~3117-3231, ~3304-3318; DecisionContract.cpp ~633-660): the latch is a GPT-seat mechanism; ordinary Baka must not build a fingerprint string per land candidate per tick. Gate it on the seat that arms it (virtual hook, Baka default = no-op). Measure: Baka game wall time before/after on the A/B run (secs column).
7. (DESIGNATION-MARKER NAME LIST, TargetChooser.cpp ~1646-1665, ~1953-2015) — keep the CURRENT final form (it is what fixed the hang); do NOT touch unless a fix above requires it; if you do, the marker test must become a card PROPERTY (type/flag in the primitive), not a spelling list — and the A/B gate is the proof (the hang lived here).

Deliverable: branch `fix-engine-audit` in worktrees/lanes/fix-audit; commit with explicit paths; audit-2026-09/fix-lane.md with per-item: CR/Oracle citation, RED fixture on base, fix, GREEN, and the A/B gate output (paste baka-ab-compare.py's table). Touch ~/.gatelogs/FIX_AUDIT_DONE. Do NOT touch AIPlayerGPT.cpp except where a virtual hook (item 6) requires an override.
