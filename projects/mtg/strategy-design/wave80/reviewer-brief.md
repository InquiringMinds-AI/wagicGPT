# Wave-80 step-3 brief (lean; REJECT is the expected verdict for anything not paid for by a decision the corpus shows going wrong. Amendment 332 stands: guides are decision-time instruction only, hard ceiling 20,000 bytes, no citations/history in a guide — evidence goes in YOUR review.md. A333 (heading = scope declaration) and A334 (no concessive clause for a rule's hard case) are in wave68/strategy-writing-skill-v2.md §4.1 — the current edition.)

OWNER RULINGS 2026-09-12 (verbatim, all in force): (1) "no such rule exists. Explicitly the rule is to 'make the parser robust'. And that 'any plan, must precede action.'" — the parser reads the answer wherever it unambiguously is (lane DD, in this corpus's binary) and an answer that PRECEDES the plan is rejected/re-asked (lane DH F2, in this corpus's binary); the protocol TEXT is unchanged; (2) pilot config changes: "No."; (3) lategame specimens: "Stop producing these." — write none; (4) the strategy-writing skill is loop-managed.

Eleventh reasoning-on corpus (invariant 000). Wave 80 step one (wave79/known-bugs.md §U → wave80/lane-{DE,DF,DG}.md + wave80/codex-review.md → lane-DH.md, twelve findings all CONFIRMED; F1 was MERGE DAMAGE — nine ctor initializers dropped by the merge helper, repaired, a post-merge check and a header-vs-ctor guard added): DE windows/holds/keys — U2 the crack-back and stack-death verdicts RENDER as prompt lines (`crackback_verdict_lines_rendered`, `stack_death_verdict_lines_rendered`) and every hold clamp/re-open writes a `hold_event` record (DH F11; `hold_verdict_safer_ignored`, `hold_reopened_new_lethal` — DH F3 made a lethal face an EVENT keyed on threat identity — and `hold_reopened_new_threat`); the rendered crack-back line runs the blockers DP (DH F4: `crackback_lethal_blocked_away`); U8 cross-phase REPLAY of a declined, board-unchanged, byte-identical window not entering a sorcery-speed window (`crossphase_replayed`, traced in askreplay/ with a record-seq `replayed_from` — DH F12); U9 replay counters split (`ask_replays_cache` + `_repeat_latch` + `crossphase_replayed` = `ask_replays_reserved`); U10 the own-loop face read off the printed line, land-drop seam cleared, "proven win" requires a LIVE named starter (DH F6) or prints the gap; U15 every render counter increments at the send from inside askModel (DH F10); DF combat/cover/pricing — U3 the cover nets a real ETB self-leave (DH F7: trigger forms only, never an activated cost) and names its mechanism (DH F8: trample-aware), the spent-blocker clause on every sacrifice-cost row; U4 one tap bill per row; U6 cycling = discard + draw; U7 `board sweep:` names YOURS; U14 the blockers verdict folds COMPULSORY draw damage (DH F9: optional draws excluded); U13 a menu whose rows all resolve to one outcome is engine-answered (`menu_single_outcome` records, `single_outcome_rows_spared`); DG plan echo/menus/render — U1 the cast step stays in the PLAN echo until the CAST EVENT (DH F5), decline rows on completing menus print the stranded mana (`plan_cast_steps_closed`); U5 the ask seam's format exemplar is neutral (was row 1); U11 own-spell marker on target + cast rows, the stack pointer only with its block; U12 six gloss/render fixes; U16 the hold-row bracket folded (−91 B/prompt); U17 the PER-SEAM COST table in corpus-stats. Post-corpus (rerun binary only): DI — a cached ACTIVATION/CAST answer replays at most once per key then RE-ASKS (`cached_replay_reasked`, record `reask_reason`), object-creating rows carry the battlefield count in the digest, a replay never passes a stated stop; DJ — every mid-run harness check keys on the run's manifest. Records carry `thinking`, `reasoning_chars`, `off_protocol_bytes`, `plan_line_missing`, `hold_check_ref_seq`/`_window`, `protocol_deviation`, `own_loop_verdict`, `reask_reason`, plus the `hold_event`, `menu_single_outcome`, `forced_close`, `recovery` kinds; gameend carries the counters below. Wave-79 guide edits for 146/152/125/130/123 are the LIVE guides (installed at 36ae77199; 162/126 KEEP; each deck's before->after list is in wave79/deck<N>/review.md).

Repo /home/magi/Projects/wagicGPT, game dir projects/mtg. Use python for translogs; use /usr/bin/grep (bare grep drops matches). DO NOT edit anything under bin/Res, src/, or run git or wagic; write only under projects/mtg/strategy-design/wave80/. Never write mechanisms as the owner's positions. Report what you did NOT check.

CORPUS (wave 80, MIXED binary: 20 games on archives/wagic-a7a6b8942-w80step1 = source cf72a002f; the `162v123` game RERUN on archives/wagic-ba5cdc374-w80step1b = 337becbf2 (+ lanes DI, DJ) after the original hung in an engine dead loop (killed at 3,641 s — the Doomsayer + Intruder Alarm cached-replay loop, its stderr kept as `game-162v123-1789216936-HUNG-attempt0.stderr`; a first rerun attempt was killed by a FALSE regime gate reading a dev lane's fixture logs — fixed by DJ, that attempt's dir is `…-rerun-01-162v123-attempt1-killed`, not a corpus); `--thinking on`, WAGIC_GPT_TIMEOUT=900, no operator token ceiling). Corpus under review: /home/magi/.Wagic/ai/gpt/selfplay-runs/matchups-20260912-074153-final (42 seat logs all with gameend, 0 timeout-tainted, merged results.tsv, askreplay/ 29 files; census = `python3 projects/mtg/tools/corpus-stats.py <dir>`, output also at ~/.gatelogs/w80-stats.out — CENSUS CHECK RECONCILES). 21 games, 0 HUNG in the final set; record-kind sum 2,347 = ask 1,661 + priority 290 + hold_event 180 + attackers 80 + discard 52 + blockers 28 + forced_close 26 + reveal 20 + bottom 5 + menu_single_outcome 3 + recovery 2; `protocol_replies` 2,135 (wave 79: 2,203; 1.92 decisions per results.tsv turn), 100% with native reasoning (median 5,770 chars, max 19,629), **0 heuristic fallbacks on the ask seam** (1 `priority`/`unparsed_reply`, class `prose_outside_two_lines` — engine seat (a) quotes it; 1 `reveal`/`engine_answered`); 0 reply_truncated, 0 action_before_plan; CORPUS-WIDE off_protocol_bytes>0 58 (2.7%), plan_line_missing 62 (2.9%, wave 79: 4.5%), `protocol_deviation` compliant 2,072 / `unlabelled_plan` 57 / `plan_absent` 5 / `prose_outside_two_lines` 1, latency p50 87.0 s (ask seam p95 185.5 s; wave 79: 94.4 s); gameend sums: `ask_replays_reserved` 1,176 = `ask_replays_cache` 1,172 + `_repeat_latch` 0 + `crossphase_replayed` 4, `ask_key_continuation_differs` 112, `hold_events` 180 = `hold_verdict_safer_ignored` 71 + `hold_reopened_new_lethal` 7 + `hold_reopened_new_threat` 0 + ?, `hold_released_turn` 81, `hold_windows_skipped` 1,051 (cast 860 / priority 191; wave 79: 1,598), `crackback_verdict_lines_rendered` 382 / `crackback_lethal_blocked_away` 26 / `stack_death_verdict_lines_rendered` 36 (all NEW), `own_loop_verdict_lines_rendered` 20 / `own_loop_windows_asked` 1, `plan_cast_steps_closed` 376 (NEW), `menu_single_outcome` 3 / `single_outcome_rows_spared` 60 (NEW), `cached_replay_reasked` 0 (rerun binary only), `crossphase_board_unchanged` 0 (wave 79: 26) / `crossphase_identical_reputs` 129, `stop_reached_windows_skipped` 1 / `_reputs_collapsed` 0, `stack_drain_windows_asked` 30, `forced_close_events` 26 / `_unrecorded` 13, `async_drops` 9, `mana_only_windows_skipped` 78, `main_phase_windows_skipped` 70, `own_turn_windows_skipped` 722, `plan_names_uncastable_zone_card` 29, `protocol_deviation_replies` 63, `blocker_forecast_gang` 6 / multi 74 / collapsed 4 / rows 144, `chain_windows_only_selfharm` 11 / `chain_selfharm_rows` 212 / `chain_acting_rows` 598 (+ `_cast` 307 / 474 / 1,280); wins deck152 4/6, deck130 4/6, deck126 3/6, deck125 3/6, deck162 3/6, deck146 3/6, deck123 1/6 (wave 79: 152 5/6, 162/146/130 4/6, 126 3/6, 125 1/6, 123 0/6; hands are real, do not read variance as guide effect).

## Per-deck agent (one per deck: 146, 152, 125, 126, 162, 123, 130)
Purpose (owner's words): read the transcripts of your deck, compare the LLM's actions to the
instruction set (the guide projects/mtg/bin/Res/ai/baka/deck<N>_strategy.txt + the general guide
wave62/general-strategy.md (wave-59 edition + R331; NOTE from the wave-63..67 syntheses: this file is NOT loaded at runtime (only deck<N>_strategy.txt is opened, AIPlayerGPT.cpp:15332) — the deck guide is the live surface) + the reply protocol in the prompt), find issues in the game, the
interface, and the guide, and surface anything else worth the core loop's attention.
Method: for each of your deck's 6 games (your seat's translog; the opponent's for context),
trace the decisions that decided the game. Classify each misplay PERCEPTION (the model misread a
true surface, or the surface was false/missing -> ENGINE/RENDER item, cite file+seq) vs STRATEGY
(read correctly, decided against the guide or against good play -> guide item). Note fallbacks,
re-asks, and any decision where the prompt's information was insufficient or wasteful (what a
better interface would have shown). Hands are real (mulligans, bottoming): do not read variance
as guide effect.
Output: ONE file, wave80/deck<N>/review.md: (1) game-by-game outcome + the deciding decisions with
seq citations; (2) engine/interface/card items ranked HIGH/MED/LOW with a concrete repro (file,
seq, the rendered line) — this is the primary deliverable; (3) guide verdict: KEEP as is (a
success verdict, state why) or EDIT — if EDIT, also write wave80/deck<N>/strategy.txt (the full
revised guide, start from the live one) and list each edit before->after with the seq that
paid for it IN review.md — the guide itself carries NO citation, count, or history (Amendment 332); hard ceiling 20,000 bytes, and an EDIT that grows the guide must say which rule earned its bytes; (4) OPTIONAL proposals, only when
your evidence supports them: general-guide changes (wave80/deck<N>/general-proposals.md) and
strategy-writing-skill changes (wave80/deck<N>/skill-proposals.md; current skill edition =
wave68/strategy-writing-skill-v2.md (the ~40 KB distillation, owner-installed 2026-09-06 after six Astra adversarial rounds; read it in full — it is short; the 1.3 MB wave68/strategy-writing-skill.md is lineage only) — read it before proposing; a proposal that
restates an existing amendment is noise). No prediction tables, no rotation evidence, no
docket bookkeeping. Final message: <= 10 lines.

## Engine-narration seat (one agent)
You read the whole corpus for the engine's sake, not the guides'. Deliverable wave80/engine-seat.md: (1) corpus census as last wave (games, decisions, decisions/turn vs wave 79's ~2.1, fallbacks by kind with genuineness, `async_drops`, `transport`, `deadline_pct`, reply shape, `reasoning_chars`, `reply_truncated`, `askreplay/`, `plan_steps_done`); (2) REGRESSION WATCH on the wave-80 changes — the section that matters most (MIXED binary: 20 games on a7a6b8942 = cf72a002f, the rerun game `162v123` on ba5cdc374 = 337becbf2 which adds lanes DI/DJ — say per finding which games you read; LESSON OF WAVE 79: every counter has a per-record trace — use it; a predicted non-zero that reads 0 is a FAIL unless the state never arose): (a) FALLBACKS — 0 on the ask seam; ONE `priority`/`unparsed_reply` with class `prose_outside_two_lines` (quote the reply verbatim: this is the robust parser's first live test — was the answer unambiguous under the owner's rule 'make the parser robust' and 'any plan must precede action', i.e. should DD's parser have read it, or was it genuinely ambiguous/answer-before-plan? `action_before_plan_rejected_replies` 0); the `reveal`/`engine_answered` (zero-eligible?); `answer_label_absent_read` 0 (the label-less shape did not recur); (b) THE HUNG GAME (attempt 0, `game-162v123-1789216936-HUNG-attempt0.stderr` in the corpus dir; the partial seat logs were removed): an engine dead loop — the model answered `Create human with Thraben Doomsayer` once at seq 32 and the cached answer was replayed 3,674× under Intruder Alarm; lane DI's fix (a cached activation replays at most once per key, then re-asks; object-creating rows carry the battlefield count; replays never pass a stated stop) is in the rerun binary only — `cached_replay_reasked` 0 corpus-wide: check the rerun game's records for Doomsayer+Alarm windows and say whether the state arose; (c) U1/DG + DH F5 — `dropped after its X was announced` (count from the stderr files; predicted 0); `plan_cast_steps_closed` 376: sample 20 completing menus (X / mode / target after a cast row) — does the echo still carry the cast clause; any decline on a completing menu, and did it print the stranded mana; (d) U2/DE + DH F3/F4/F11 — `hold_events` 180 (NEW kind; reconcile with `hold_verdict_safer_ignored` 71 + `hold_reopened_new_lethal` 7 + `hold_reopened_new_threat` 0): read every `hold_event` — face, reason, seam; for the 7 `new_lethal` re-opens, was the threat real and did the seat act; for a sample of 20 `safer_ignored`, was ignoring right; `hold_reopened_new_threat` 0 — show whether a new equal-rank threat ever arose (DC F2/DH F3); `crackback_verdict_lines_rendered` 382 / `stack_death_verdict_lines_rendered` 36 — the lines exist now: sample 30, TRUE against the board (DH F4: the crack-back line runs the blockers DP — `crackback_lethal_blocked_away` 26: quote 5, is the 'with your best block' floor right); any verdict line on a SUPPRESSED window (DH F10); `hold_windows_skipped` 1,051 (wave 79: 1,598); (e) U8/DE — `crossphase_replayed` 4 (predicted 10-40) / `crossphase_board_unchanged` 0 (wave 79: 26) / `crossphase_identical_reputs` 129: explain — did the replay absorb the board-unchanged population or did the population vanish; each of the 4 replays traced in askreplay/ (`replayed_from` = a record seq now, DH F12) — was the replayed answer right for the new phase (Astra's phase-gated-option risk); `ask_replays_cache` 1,172 / `_repeat_latch` 0 / sum = `ask_replays_reserved` 1,176 (U9 identity holds?); `ask_key_continuation_differs` 112 — sample 20; (f) U10/DE + DH F6 — `own_loop_verdict_lines_rendered` 20 / `own_loop_windows_asked` 1: every rendered line, its face, TRUE (a named LIVE starter, or the gap stated), on an asked prompt; (g) U3/U4/U6/U7/DF + DH F7/F8/F9 — crack-back cover: any body credited that the row does not leave (Acererak class), `crack-back cover: NONE from this body` count, the mechanism sentence now trample-aware (quote 5 on tramplers); one tap bill per row naming every tapped attacker; cycling rows priced as discard + draw (quote all); `board sweep:` markers name YOURS (quote the 6+); the blockers verdict folding only COMPULSORY draw damage (Sylvan Library class = optional); ETB self-leave only on real ETB triggers (Fleeting Image class); (h) U13/DF — `menu_single_outcome` 3 records / `single_outcome_rows_spared` 60: each engine-answered menu — did every row truly resolve to one outcome (mana spent included); (i) U5/DG — the ask seam's exemplar is now neutral: the row-1 pick rate on the ask seam vs wave 79 (62/139 at deck162; compute corpus-wide both waves if cheap) — did it fall; (j) U11/U12/DG — own-spell markers on target + cast rows (any seat still countering its own spell); the stack pointer only with its block; the six gloss fixes (mid-clause `(...more)` count, Sigarda, Puzzle Box, `[legendary:` loyalty, Sorin, mode objects); (k) U16/U17 — per-deck prompt bytes (deck146 mean, % over 20 KB; the −91 B/prompt fold); the PER-SEAM COST table in the census (latency p50 87 s; r=+0.66 vs reasoning_chars, −0.37 vs bytes) — say what it means for the byte work; `plan_line_missing` 62 (2.9%, was 4.5%); (l) DJ — the harness: the first rerun attempt was KILLED by a FALSE regime gate reading a development lane's thinking-off fixture logs from the shared dir (`…-rerun-01-162v123-attempt1-killed`, not a corpus); DJ re-keyed six scan sites on the manifest; the rerun game passed on the new harness — confirm from its log; (m) `async_drops` 9; `forced_close_events` 26 / `_unrecorded` 13; `stop_reached_reputs_collapsed` 0 / `_windows_skipped` 1 (few stop chains this wave — say whether any arose); `mana_only_windows_skipped` 78; wins deck152/130 4/6, 126/125/162/146 3/6, deck123 1/6 (wave 79: 152 5/6, 162/146/130 4/6, 126 3/6, 125 1/6, 123 0/6); (n) the harness — HUNG tripwire true positive once (attempt 0); no pilot stall; the corpus took 5 h 58 min to the 20th game + the rerun; (3) adjudicate EVERY prediction in wave80/lane-{DE,DF,DG,DH}.md (DH = the twelve Astra findings) and, for the rerun game only, wave81/lane-DI.md and lane-DJ.md (mark UNTESTED where the state never arose) as PASS / FAIL / UNTESTED with counts and file+seq (UNTESTED is a real verdict), plus the carried UNTESTED docket (BS F2 Tribute second edict, (BS F3 retired), BS F5 blockers ceiling, BS F6 distinct-index re-ask, CF F1 pending-deadlock, F2/F5-nonlifelink/F8 haste/F9, the reasoning-body halves of N14a/N14d/O20; {X}-announcement payment loss — count `dropped after its X was announced`); (4) engine/render/narration defects ranked HIGH/MED/LOW with repro — especially any new render clause stating a false fact (the drain clause, the UNPROVEN/THREATENED loop faces, the conditional-counter clause, the GAINED/LOST split, the hoisted card text, the `(...more)` cuts); (5) RETIRED by owner ruling 2026-09-12 — write NO lategame specimen. Final message: <= 15 lines, HIGH items first.

## Synthesis (runs only if any deck agent wrote general-proposals.md or skill-proposals.md)
Read all proposals + engine-seat.md. Decide each proposal: ADOPT (edit wave80/general-strategy.md,
a copy of wave62's (the R331 edition; waves 63-67 wrote no edition; wave 68 installed wave68/strategy-writing-skill-v2.md as the current edition), or append a numbered amendment to wave80/strategy-writing-skill.md, a
byte-verbatim copy of wave59's with amendments appended — verify the prefix with cmp) or
REJECT with one line of reasons. Write wave80/synthesis-notes.md. If a deck agent's guide edit
contradicts an adopted general change, note it; do not edit deck guides yourself.

METHOD NOTE (from the wave-59 engine seat): the translog `phase` field is not the rendered phase — count phase-gated facts from the prompt text.
