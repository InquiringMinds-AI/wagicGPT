# Wave-80 known bugs (corpus matchups-20260912-074153-final: 21 games, MIXED binary — 20 games on archives/wagic-a7a6b8942-w80step1 = cf72a002f, the `162v123` rerun on archives/wagic-ba5cdc374-w80step1b = 337becbf2 after lanes DI+DJ; sources wave80/engine-seat.md + wave80/deck<N>/review.md; attempt 0 of `162v123` HUNG at 3,641 s and is not a corpus game)

Invariant 000 held: 2,135/2,135 protocol replies with native reasoning (`thinking` on every record), 21 natural finishes, 0 truncations, **0 heuristic answers on the ask seam**; the two fallbacks are a zero-eligible `reveal` and ONE `priority`/`unparsed_reply` whose refusal was correct (the model answered a row the window did not offer — see V4). The robust parser (DD) had its first live test: `prose_outside_two_lines` 1 (`126v162` seq 62, plan preceded action, read correctly); `action_before_plan_rejected_replies` 0 (the shape did not appear); `answer_label_absent` 0. Reply discipline improved on every shape (`plan_line_missing` 4.5% → 2.9%, off-protocol 3.9% → 2.7%) and latency p50 fell 94.4 → 87.0 s with reasoning median flat (5,770). Prediction scoreboard (engine seat): DE 9/2/1, DF 11/1/3, DG 8/4/1, DH 8/5/4, DI 3/0/2, DJ 2/0/0 (PASS/FAIL/UNTESTED). PAID: U1 cast step held through completing menus (deck130 13/13, deck146 81/81 echoes); U2 `hold_event` trace closes exactly at every seat that checked (deck126 36/36, deck123 12/12 clamps safer); U3 Acererak cover `NONE from this body` 3/3, mechanism sentence trample-aware; U4 tap bills whole (deck130 94/0 split, deck152 14/0); U7 sweep marker symmetric (deck123); U13 the three `menu_single_outcome` all genuinely zero-payable; U5 exemplar neutral (row-1 rate flat at 44% but zero traces cite menu position); DJ manifest keying PASS end to end; wave-79 guide edits paid at 123 (edit 3 won its only game), 125 (B-1), 146 (edit 1 fired 3/3), 152 (both HIGHs closed), 130 (B on the win path). Wins 152 4/6, 130 4/6, 126/125/162/146 3/6, 123 1/6. Bytes REGRESSED: mean prompt 12,029 → 13,124 B (+9.1%), share over 20 KB 5.6% → 13.4% (U16's −91 B against ~+1,095 B of new render).

Two hot fixes shipped mid-wave and are carried here as V1/V2. Both binaries' `dropped after its X was announced` = 2, BOTH in corpus games (`125v123` 12 mana, `152v125` 17 mana; 0 in the HUNG stderr — the wave-80 brief's attribution was wrong); the decline rows printed the stranded mana correctly and deck125's model declined anyway (guide edit 1 this wave targets it).

## V — routed to wave 81 (HIGH first)
- V1 HIGH — THE CACHED-REPLAY DEAD LOOP (fixed by lane DI at 337becbf2, LIVE-UNTESTED): `chooseOrderedAction` replayed one model answer 3,674× under Thraben Doomsayer + Intruder Alarm (`162v123` attempt 0: `Using Activated ability` / `the menu is unchanged after action N but the board moved`). DI bounds an activating/casting cached answer to ONE stand per key then re-asks, adds the battlefield count to the digest for object-creating rows, refuses replay at/past the stated stop. RED/GREEN by live probe only; the rerun never formed the Alarm board (`cached_replay_reasked` 0, `reask_reason` on 0 records). The step-one binary shows the SAME state family at other seats: cached-replay runs of **39** (`126v125` seq 96) and **17** (`126v162` seq 33), and a legend-rule state-based action answered from the ask cache (`146v162` seq 27, `replayed_from` 24). Wave 81: adjudicate DI against THOSE windows (fixture from `126v125` seq 96 through the live caller; predict `cached_replay_reasked` > 0 on the next corpus with `reask_reason: cached_replay_bound`), and pin that a state-based-action window (legend rule) is never served from the cache.
- V2 HIGH — THE FALSE REGIME GATE (fixed by lane DJ at 76e4e1a18, PASS): the gate scanned the shared `~/.Wagic/ai/gpt/logs` by recency and read DI's thinking-off fixture games, killing rerun attempt 1. Six scan sites now key on the run manifest (`$OUTDIR/.seatlogs` + `WAGIC_GPT_TRANSLOG_FILE` announcements, tools/runmanifest.py). Verified on the rerun (`regime-gate-state.txt` = exactly the run's two basenames). Residual: manifest-less outdirs degrade to the old scan — wave 81 makes the degraded path PRINT that it degraded.
- V3 HIGH — A `new_lethal` HOLD RE-OPEN IS A LOGGED NO-OP: `125v162` seqs 18/20/22/24 — four `hold_event` `reopen_new_lethal` on one `window_seq 14`, **no prompt-bearing record after seq 17**, seat dies at 0; stderr prints `hold re-opened … NEW lethal threat` immediately followed by `the model took the hold row … later cast windows are held` with no `-> chose`. DH F3 shipped the event, not the re-ask. Same family at deck125 (`125v126` stderr 1445-1447, records seq 92/93 `verdict_clamped`): the clamp suppressed the WHOLE TURN when Exquisite Blood hit the stack; `hold_reopened_new_threat` 0 across 53 hold_events / 19 clamps — DC F2 FAIL on rendering. Fix: a re-open INVALIDATES the retained hold answer for that key before the hold check runs (the way DI bounds the cache); a re-opened window is ASKED; fixtures from both seats through the live latch; predict a `hold_event` re-open followed by an ask record on the same window_seq, and `hold_reopened_new_threat` > 0.
- V4 HIGH — AN INSTANT IN HAND CANNOT BE CAST IN RESPONSE: `[no cast row now: you already answered this phase's Casting decision …]` on **250 prompts, 200 gating an INSTANT, 61 with a non-empty stack** (`152v125` seq 101: Fateful Absence with 6 mana up while the opponent's Essence Scatter targets Briarbridge Tracker — the corpus's only unparsed reply; 15 such windows in that game). The once-per-phase casting decision removes instant-speed interaction for the rest of the phase. Fix: a NEW stack object (or a board change after the answer) re-opens the casting decision for instant-speed rows — the decision is per (phase, stack state), not per phase; key-stable; pin the counterspell-on-the-stack board; predict the clause count on non-empty-stack windows → 0.
- V5 HIGH — THE CRACK-BACK VERDICT CONTRADICTS ITS OWN SCREEN: 11 windows where the verdict uses the raw combat total while the header's `ADD THOSE UP` adds animated lands / trigger power (6 `you survive` under `that would KILL you`: `152v130` seqs 17/19/22/24/25, `162v130` 43; 5 `blocked away` floors flipped: `146v162` 21/22/31/32/33); deck123: the verdict asserts survival on 20/80 windows whose DRAW FORECAST in the same prompt says KILLS (`162v97/137` for deck123) — DH F9's compulsory-draw fold reached the blockers verdict only; deck146: a COMPULSORY forecast reading "you would be at 0; that KILLS you" sits in the board dump while a moot crack-back verdict gets the bracket next to the rows (`146v162` seq 53). Fix: ONE total — the verdict takes the same figure the CRACK-BACK line publishes after `ADD THOSE UP` plus the compulsory forecast, and the lethal bracket next to the rows is whichever lethal is nearest; pins on the three boards; predict 0 contradicting pairs.
- V6 HIGH — EXQUISITE BLOOD NEVER TRIGGERS ON LIFE LOST TO DAMAGE (deck130, `130v126`): 11 combat damage over turns 20-22, trend line `life-gain EVENTS put +0 on them`, zero Sanguine Bond / Exquisite Blood triggers; the seat read the card correctly and was paralysed six turns, then won because the engine contradicted the render. Script `auto=@lifelostfoeof(player)` (mtg.txt:37966); trigger code UNREAD by the seat. Oracle: "Whenever an opponent loses life, you gain that much life" — damage IS life loss. Fix: verify the Oracle (Scryfall) and the `lifelostfoeof` trigger's damage path in the engine BEFORE touching the card; suite fixture: opponent takes combat damage with Exquisite Blood out → controller gains; PARSETEST none. This is an ENGINE rules bug, not a render item.
- V7 HIGH — THE OWN-LOOP STARTER IS WRONG WHENEVER IT PRINTS: deck126 18/18 lines name a BLOCK trigger (`Perimeter Captain` / `Pride Guardian`) while the opponent controls zero creatures, or a loop HALF (`Exquisite Blood`, `126v123` seq 16); `126v130` seq 39 contradicts the same prompt's `nothing you control can legally block`; the seat echoed the false starter into its PLAN for nine windows in the game it lost; DH F6's gap branch never printed; in 8 lines a real starter (Staff ping, Sorin +1, lifelink attack) was live and unnamed. Fix: a starter is an ACTION THE SEAT CAN TAKE NOW (untapped affordable activation, a castable spell, an attack with a lifelink/drain body when the attack is legal) or a trigger whose event this seat causes; a trigger that needs the OPPONENT to act (block, attack-into) never qualifies; no starter → print the gap; pin deck126's two boards.
- V8 HIGH — THE `[<- best X for this cast: X=N …]` MARKER OPENS AS A RECOMMENDATION ON A CAST IT REFUSES: `162v130` seq 75 `best X for this cast: X=6 - the only X that kills anything of THEIRS costs you more of YOURS than it takes - THEIRS 4 / YOURS 5`; the seat consumed the prefix and swept its own Siege-Gang + 3 Goblins + Blastminer with exact lethal on the board. The X-menu seam renders the same verdict refusal-only. Fix: the cast-row marker uses the X-menu wording (refusal first, no `best X` prefix on a refused cast); pin. Same family: six board-sweep markers on the X-pricing path print bare `THEIRS 4 / YOURS 5` with no roster (`130v152` seqs 49-75) — wire U7's roster into the X-variable marker.
- V9 HIGH — SEND-TIME COUNTERS OVER-COUNT AND FACES UNDER-STAMP (DH F10 FAIL): `crackback_verdict_lines_rendered` 382 vs 329 prompts vs 291 stamped records; `own_loop` 20 vs 18 (deck126 20 vs 18 rendered, two increments unlocatable); `crackback_lethal_blocked_away` 26 vs 24 lines vs 14 records; 38 prompts carry the verdict with no `crackback_verdict` field, 36 of them `priority`. One boundary: suppressed paths still count and the priority seam does not stamp. Fix: the scoped guard's `sent` is set by the transport call itself; the priority seam stamps faces the same way the ask seam does; pin counter = rendered = stamped on a mixed fixture.
- V10 MED — RENDER PLACEMENT THAT COST A GAME: the `[held back …]` tag rendering inside an OFFERED attacker's row burned ~1,300 of 1,500 reasoning chars at the window deck146 lost (`146v162` seq 52; Ob Nixilis at 3 loyalty reachable, never mentioned); `CRACK-BACK COST OF ATTACKING` / stay-home cover renders on 1 of 8 deck152 attack prompts that print `CRACK-BACK NEXT TURN` — the one where it is already lethal, absent at `152v146` seq 19 where it was needed. Fix: held-back annotations never sit inside an offered row; the cover clause prints on every attackers window that prints a crack-back; pins.
- V11 MED — THE PLAN ECHO STILL DROPS A CAST STEP WRITTEN AS ITS OWN SENTENCE (`152v125` 15→16 FAIL vs 20→21 PASS) and drops the TARGET clause at the target window (`152v126` 19→20); deck130 13/13 and deck146 81/81 PASS on the one-sentence shape. Fix: the latch keys on the cast OBJECT, not the sentence containing it; pin the two-sentence shape and the target-window echo.
- V12 MED — A MULTI-ROW ANSWER IS SILENTLY REDUCED TO ITS FIRST ROW: `152` seqs 217/237 `CHOICE: 1, 2, 7, 8, 9, 11 (…)` on Emrakul's Annihilator sacrifice-6, `parse_note: multi_answer_first_taken`, the model believes it chose all six; the seam asks one card per window and the prompt never says so; the counter is not in the gameend census. Fix: the sacrifice/discard-N seam says "one card per window, N windows" and the parser's first-taken path is COUNTED in gameend; pin.
- V13 MED — INTEL MISSING FROM THE ROWS: the `TOKEN ENGINE` marker renders on the creature-pick ask but not on the edict CAST row or the forced player-target ask (`125v123` seqs 238/239 vs `162v123` 79 — deck123 Devour-Fleshed its only Keeper); no rendered clause names a live counterspell threat when the opponent has already countered (the fact exists only as graveyard names, `125v123` seqs 143/190); dead-only cast menus (dead Tutor / `[second copy:` half) do not print `NO LIVE CAST ROW` (`126v130` seqs 15/21/22/32/38); the cleanup `{spare:}` verdict is colour-blind (a Swamp marked spare with `{B}{B}{B}` in hand, `162v130` seq 47). Fix each; pins.
- V14 MED — BYTES: mean +9.1%, over-20 KB 5.6% → 13.4% (deck152 85/280 in the 49-turn game; deck130 max 26,330; deck162 10/168); the `{crack-back cover:}` paragraph (416 B) reprints per ROW (`162v146` seq 30, 25,717 B); GAME LOG is 52% of the mean prompt (deck146) and the narration trim stabilises at ~15 KB independent of the board block (deck152). Fix: the cover paragraph prints once per prompt with per-row deltas; the narration trim budget is a function of total prompt size; measure with prompt-byte-census.py (fix its line-297 crash first, V17).
- V15 MED — TELEMETRY GAPS: six wave-80 counters have no per-record trace (`ask_key_continuation_differs` 112, `hold_windows_skipped` 1,051, `plan_cast_steps_closed` 376, `mana_only_windows_skipped` 78, `main_phase_windows_skipped` 70, `own_turn_windows_skipped` 722); `hold_event.window_seq` is on a different numbering from record `seq` (hold at record 89 → `window_seq 42`, deck125) — the wave-77 lesson again. Fix: each gets a record field or a per-event record; `hold_event` carries the record seq of the window it refers to.
- V16 MED — THE ECHO LABEL COLLIDES WITH THE PROTOCOL LABEL: `THE PLAN YOU LAST STATED` produced `THE PLAN:` replies (all 4 deck125 protocol deviations; `152` seq 111 echoes the render sentence instead of a plan). Fix: the echo heading is render text (loop-owned) — rename it so no substring of the protocol label appears; pin. NOT a protocol-text change.
- V17 LOW — `prompt-byte-census.py` crashes at line 297 (`tot["n"]` on an int, exit 1); the `x<N>` repeat-menu exemplar still quotes a live row (5 prompts, U5 missed this seam); cycling rows never say they discard when no punisher is out (U6's discard half gated on a punisher); DH F6's gap branch UNTESTED (never printed).
- V18 DOCKET (carried): 55 uninitialised scalars in SimpleButton/SimplePad/SimplePopup/TestSuiteAI/TextScroller (DH found-not-fixed); U14 draw-fold UNTESTED (0/26 blockers verdicts mention the forecast; one eligible window); DA DP trample/menace/12×12 arms; T16; BS F2/F5/F6 and CF F1/F2/F5/F8/F9 NOT ADJUDICATED (no repro definitions carried — wave 81's reviewer brief must carry them or retire them); reasoning-body halves N14a/N14d/O20.

FAILED predictions: `crossphase_replayed` 4 vs 10-40 predicted (deck123: the one replay was right and hid nothing — the low count is the U8 gate being strict, not a defect); `hold_reopened_new_threat` > 0 → 0 (V3); DH F10 exactness (V9); `dropped after its X was announced` 0 → 2 (guide-side, deck125). Census rule holds: 2,347 = 2,135 round trips + 212 no-round-trip records.
