# Wave-80 step-one lane brief (base = master after wave79 known-bugs; evidence = wave79/known-bugs.md §U + wave79/engine-seat.md + wave79/deck<N>/review.md)

RULINGS THIS WAVE TOUCHES: 000 — reasoning only in the native channel; reply = PLAN then action, nothing else; no instruction licenses prose; the PARSER IS ROBUST (owner 2026-09-12: "no such rule exists. Explicitly the rule is to 'make the parser robust'. And that 'any plan, must precede action.'") — it reads the answer wherever it unambiguously is and rejects only an answer that precedes the plan or is ambiguous; parser robustness is loop-owned; caps never bound reasoning; the golden protocol TEXT (reply-protocol.txt / kReplyProtocol) and the thinking regime are owner-ruled — render brackets and row text may change; U18 is REPORT-ONLY; the parser is robust by ruling (DD merged, untested by the wave-79 corpus). 00 — full games; a game answered by heuristics is not a corpus game. 3 — suite THREADS=1 0 failed (known flakes: lifeline, merrow_reejerey, intrepid_adversary_repeated_payment), PARSETEST 0 failed. Never remove, cap, or auto-answer a legal option (R1's loop guidance is an ANNOTATION plus the model's own hold row — nothing is collapsed or auto-answered; R9 is MEASURE ONLY). No VPK. No AI attribution. NO CORPUS from a lane; no lane sends requests to the pilot this wave.

Standing mechanics: as wave71/lane-brief.md (worktree-only; `make -f Makefile.sdl -j8`; both link guards OK; binary freshness; every wagic run under `systemd-run --user --scope -q -p MemoryMax=4G -p MemorySwapMax=0 --`; suite THREADS=1 as a detached unit `w80-<LANE>-suite` waited with a FOREGROUND until-loop (no Monitor); count both failure strings; PARSETEST from bin; Edit tool for UTF-8 sources, byte-splice for MTGRules.cpp/GameObserver.cpp/primitives; U+FFFD 0; RED-on-base evidence + a falsifiable prediction per fix; commit with explicit paths, `git add -f` under bin/Res; write wave80/lane-<LANE>.md; touch `~/.gatelogs/W80_<LANE>_DONE`; weakest evidence). LESSON OF WAVE 79: (1) a counter with no per-record trace cannot be adjudicated — `hold_verdict_safer_ignored` 59 / `hold_reopened_new_threat` 0 had to be called UNTESTED by default, the exact outcome the wave-78 lesson exists to prevent; every new counter ships with a record field naming the event it counted, and every render counter increments AT THE SEND; (2) a verdict that lives only in a key is invisible — `[crack-back verdict:` / `[stack death verdict:` rendered 0 times in 2,203 prompts (wave 77's lesson, re-confirmed); (3) the model follows what is ON THE SCREEN: the casting menu's format example built from row 1 was picked in 62/139 windows, and a plan echo with its cast clause trimmed made the seat decline its own announced X — fix the emitter the seat reads, not the model. LESSON OF WAVE 78: (1) a pin can pass while the live path renders NOTHING (S6: pinned rendered bytes, 0 renders in 42 logs) and a fix can be right at the helper while the caller's gate never admits the real case (S3: `planTurn == nowTurn`, and 40/44 windows lacked the repeat row the gate demanded) — every fix ships with a CORPUS-DERIVED fixture: the actual prompt/record pair that motivated the item, driven through the LIVE caller, plus a predicted NON-ZERO counter value in the next census; a counter that reads 0 after the fix is a FAIL, not "untested"; (2) a key-stability claim is proven only on the corpus's own re-asked pairs — the CG shape survived four waves of green per-row pins because no pin ever took a real re-asked prompt pair as its input. LESSON OF WAVE 74 stands: any NEW annotation carrying a board-derived number must be proven OUTSIDE every key (ask, async slot, hold latch, hold-check, option-set) with a PARSETEST pin that two windows differing only in that number yield identical keys, and the held set in a key pin is built the way the LIVE seam builds it (holdActionKeyRow via mLastMenuRows). LESSON OF WAVE 77: (1) a verdict the engine computes but pushes only into a KEY is invisible to the model — every verdict that should change a decision must be RENDERED, and its pin must assert the rendered bytes, not the key (S6); (2) a field named `_seq` that is not a record seq wastes eight seats — name telemetry by what it is and pin the join (S7); (3) the census must sum every record kind, including the new ones a lane adds. LESSON OF WAVE 76: (1) a bracket that prints a NUMBER or a COUNT must be pinned on the cases where the count is zero, one, and a reorder — Q1 shipped with the referent fixed and the count still wrong on reorders and disappearances; (2) a fix-lane finding whose fix is "compare more state" (CQ F4) can make a clause fail-closed into silence — pin that the positive clause still prints on the shape it was built for; (3) a clause that carries a number about LIFE must name the EVENT that moves it, or the model reads it as a statement about the row's mana cost (R3). LESSON OF WAVE 75: (1) an instruction's REFERENT must be something the reader can see — the hold-check bracket spoke of "the last window" meaning the last one the engine BUILT, while 3,402 built windows never reached the model; any bracket that refers to history refers to ASKED history; (2) a helper that hardcodes its input (the wave-74 stats script ignored its argument) reports the wrong corpus with full confidence — new tools take the run dir as an argument and echo it in their first output line; (3) a prediction's negative clause ("non-zero means lost") needs its own pin, not just the positive.

## Lane DE — windows, holds and keys (U2, U8, U9, U10, U15)
U2 per-record `hold_safer_ignored_face` / `hold_reopen_reason`; RENDER the crack-back and stack-death verdict rows; fixture from `125v162` seq 199 through the live latch; predict `hold_reopened_new_threat` > 0 with a record naming the threat. U8 cross-phase replay only when board-unchanged + identical action-key set + no phase-gated legality difference; pin the phase-gated case as still asked; measure `crossphase_replayed`; acceptance `123v130` seqs 72-80. U9 name the 22 residual pairs' differing bytes; make the replay counters comparable across waves. U10 "proven win" requires a live named starter or prints the gap; hold row and verdict agree; count rendered non-`none` lines only. U15 one send-time counter helper at every seam; pin per seam.
## Lane DF — combat, cover and pricing truth (U3, U4, U6, U7, U14, U13)
U3 the cover credits only bodies the row's resolution leaves (ETB bounce, dead modes, sacrifice cost excluded), names its mechanism, and the spent-blocker clause attaches to every sacrifice-cost row under a crack-back — fixtures `146v162` seqs 32/35/37, `130v152` seqs 52/54. U4 one tap-bill clause per row naming every tapped attacker — `152v125` seq 38. U6 cycling prices discard + draw — `130v162` seqs 35/37. U7 `board sweep:` names both sides' losses — `123v162` seqs 42/74. U14 the blockers lethal verdict includes unavoidable forecast non-combat damage — `146v162` seq 29. U13 a menu whose rows all resolve to one outcome is engine-answered (no option lost) — `152` Adversary. Every clause key-stable on the JOINED menu; corpus boards as fixtures.
## Lane DG — plan echo, menus and render (U1, U5, U11, U12, U16, U17 measure)
U1 a cast step stays in the echo until its completing menu resolves; every decline row on a completing menu prints the stranded mana — fixtures `130v162` seqs 31→32, `125v126` seqs 44→45, `125v152` seqs 9→10; predict `dropped after its X was announced` 0. U5 neutral format placeholder on every seam — predict the row-1 pick rate falls. U11 own-spell rows marked, target roster matches the cast row, the stack sentence prints only with its block — `125v146` seqs 104/105, `125v162` seq 199. U12 the six gloss/render residuals (mid-clause `(...more)`, Sigarda double marker, Puzzle Box feeds, `[legendary:` loyalty, Sorin −2/−6, mode liveness names the object). U16 hold-row boilerplate to one line; deck146 byte census before/after. U17 per-seam latency vs bytes vs reasoning_chars line in corpus-stats.py (measure only).
## Lane DH — fix lane for the Astra review (wave80/codex-review.md) — seeded after step-one gate 1

## Lane DH — fix lane for the Astra review (wave80/codex-review.md) — seeded after step-one gate 1 — base = master after the merge repair (gate 1 on a66df551f: 1296/0, 76 AI/0, PARSETEST 7035/0; the repair adds no pins)
Twelve findings. F1 is CONFIRMED and already repaired (the merge helper's auto-resolve took one side of the ctor-initializer hunk twice; nine members were left uninitialised; restored before this lane launched) — DH's F1 work is the GUARD: `check-ctor-init` (the link-time guard) must fail when a scalar member declared in AIPlayerGPT.h is absent from the initializer list (not only when one is commented out) — enumerate the pre-existing uninitialised scalars it will find, initialise the ones that are read before assignment, and whitelist by name the ones the ctor body sets; pin the guard on a fixture header. Every other finding: CONFIRM (fixture/pin RED on base reproducing Astra's trigger THROUGH THE LIVE CALLER PATH, then fix) or REFUTE (a pin proving the trigger cannot occur, with the reason). Every counter keeps a per-record trace and increments at the send.
- F2 HIGH (AIPlayerGPT.cpp:~24558, inherited; PARSETEST ~88753 pins it): a labelled answer that PRECEDES the PLAN (`CHOICE: 2 (…)\nPLAN: …`) is accepted — the owner's rule is "any plan, must precede action" (a plan after the choice is post-hoc justification). Fix: an answer line before the PLAN line is REJECTED and the window re-asked (the same path a missing answer takes), counted `action_before_plan_replies` (the counter exists and reads 0 — make it real); the old pin is inverted in place; predict `action_before_plan_replies` > 0 only if the model produces the shape (it has not in 3 waves — say so).
- F3 HIGH (AIPlayerGPT.cpp:~36368; lane DE): a hold survives a SECOND lethal stack after the first was answered — the remembered face stays `KILLS`, the new face is identical, rows unchanged, so the window is suppressed and the rendered line never reaches the model. Fix: a lethal face is not a rank to remember but an EVENT — the latch re-opens whenever the stack-death or crack-back verdict is `KILLS` and the stack/board object set differs from the one the hold was taken over (object ids, not text; the same rowless-fact rule DE respected: the IDENTITY of the threat, not a number, enters the comparison); fixture from Astra's Salve/Bolt board through the live latch; predict `hold_reopen_reason` records with reason `new_lethal_stack` > 0 when the state arises.
- F4 HIGH (AIPlayerGPT.cpp:~35597; lane DE): the rendered crack-back verdict says the board KILLS you next turn without subtracting blockers (4 life, their 5/5, your untapped 0/6). Fix: the rendered verdict uses the same blocker-aware floor the blockers header uses (the DA DP), and names it ("with your best block: N"); the marker's rank may stay threshold-based for the latch, but the LINE must not claim death a legal block prevents; pin Astra's board = not lethal.
- F5 HIGH (AIPlayerGPT.cpp:~21686; lane DG): U1's cast latch is retired at the FIRST completing menu, but a cast can have several (mode, then X, then target) — the second menu echoes with the cast clause dropped again. Fix: the latch retires when the CAST resolves onto the stack / is committed by the engine (the cast-issued event), not when any menu answers; a countered/fizzled cast retires it too; fixture = a two-menu cast (Starstorm mode then X) through the live echo; predict 0 leading-clause losses at ANY completing menu.
- F6 MED (AIPlayerGPT.cpp:~36014; lane DE): "live starter" = script substring, not an available action — a tapped Prodigal Sorcerer or a Soul Warden with nothing entering names a "proven win". Fix: a starter counts only if its action is available NOW (untapped + affordable activation, or a trigger whose event can occur this turn cycle); otherwise print the gap; pin both boards.
- F7 HIGH (AIPlayerGPT.cpp:~28564; lane DF): `w80EtbSelfLeavesLine` accepts `{1}{U}:moveto(hand)` (Fleeting Image's ACTIVATED bounce) as an ETB that removes the body — prints a false mechanism and subtracts a blocker that can block. Fix: an ETB self-leave requires a trigger on entry (`@movedTo(this)` / the engine's ETB trigger forms), never an activated-cost prefix; pin Fleeting Image = counts as a blocker, Acererak = does not.
- F8 HIGH (AIPlayerGPT.cpp:~28803; lane DF): the new mechanism sentence says a blocker absorbs the attacker's FULL power — false for trample (a 1/1 vs a 5/5 trampler stops one). Fix: the sentence states the spill for trample ("absorbs N, M tramples through") and the full absorb only for non-tramplers; re-derive the literal pins; pin the 1/1-vs-trampler board.
- F9 MED (AIPlayerGPT.cpp:~58014; lane DF): U14 charges OPTIONAL draws (Sylvan Library's "may") as compulsory lethal damage. Fix: `drawStepExtrasScan` distinguishes may-draws; the compulsory charge counts only forced draws; optional draws print as "up to N more if you choose"; pin Sylvan Library vs Howling Mine.
- F10 MED (AIPlayerGPT.cpp:~49854; lane DE): the casting caller counts and stamps verdicts BEFORE `askModel`, whose cache/cross-phase replay branches return without sending. Fix: the send-time helper is called from INSIDE askModel at the actual send boundary (after every cache/replay/suppression return), not by the caller; pin a cached-decline rebuild = 0 counted.
- F11 MED (AIPlayerGPT.cpp:~36377; lane DE): `hold_safer_ignored_face` / `hold_reopen_reason` are one shared string overwritten per clamp and consumed by a LATER unrelated record. Fix: per-event records (kind `hold_event`, with seam, face, reason, window seq) written at the event, or a list on the next ASK record of the same seam only — never carried onto a target/discard record; counters reconcile: increments == hold_event records; pin.
- F12 MED (AIPlayerGPT.cpp:~46978; lane DE): cross-phase replay records pass `windowSeq` as `replayed_from` where other paths pass a RECORD seq. Fix: store the source ask's record seq at cache time and pass that; pin the join on a diverged-ordinal sequence.
Astra's side-checks: the protocol text unchanged; PARSETEST tail union intact; the DA/CZ-style pin seam not found this wave.
Order: F3, F5, F2, F4, F7, F8 (suppressed lethal / lost cast intent / plan order / false death / false mechanisms), then F1 guard, F10, F11, F12, F6, F9. Gate: suite THREADS=1 + PARSETEST + harness --selftest + corpus-stats --selftest + instruction guard + the new ctor guard; lane report wave80/lane-DH.md with per-finding CONFIRM/REFUTE, RED evidence, fix, GREEN, prediction (a NON-ZERO counter with its record trace), weakest evidence.
