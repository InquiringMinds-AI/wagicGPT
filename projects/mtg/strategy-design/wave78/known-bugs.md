# Wave-78 known bugs (corpus matchups-20260911-125420-final: 21 games, ONE run, no pilot incident, SINGLE binary archives/wagic-47ef378c8-w78step1 = 7c0f15835; sources wave78/engine-seat.md + wave78/deck<N>/review.md)

Invariant 000 held: 2,629/2,629 protocol replies with native reasoning, 21 natural finishes, 0 truncations, 0 hung, 0 tripwires. 8 HEURISTIC ANSWERS: 6 `answer_label_absent` (all deck125 seats, all in games deck125 WON — the S1 shape the owner has not ruled on; one real cost: `125v130` seq 303, the heuristic cast a 12-mana Sphinx's Revelation against the model's stated hold and the model then declined the X menu, stranding all 12 sources), 1 no-label reply on deck162 (`162v146` seq 16, cost 0), 1 `engine_answered` reveal (`152v123` deck152 seq 22 — a zero-eligible window, not a lost decision). Wave-78 items PAID: S7 CLOSED (2,063/2,063 ref seqs join, 0 false hold-check claims at five seats), S2 CLOSED (8/8 repeat rows, 0 re-asks), S5 PASS (n=1, true, the double-counter did not recur), S9(b) `N gone` renders, S12/CY F7 PASS (294 splits, arithmetic clean), S14 loyalty/colour/second-copy PASS, S15 fold PASS, S4/CY F2 PASS on the annotation text, S8 PASS on Howling Mine, CY F10 PASS (counter 6 == 6). Wave-77 guide edits PAID at 146 (Kaya ladder 3/3), 125 (16/16 Revelations in main), 130 (B/C/D), 123 (CHECK -1 regressed 3/7 — re-edited). Wins 146/126/152/123 4/6, 125 3/6, 162 2/6, 130 0/6 (deck130 four-wave slide 4→1→0; the seat priced five of six losses to matchup and one to a decision — T9).

## T — routed to wave 79 (HIGH first)
- T1 HIGH, OWNER RULING STILL NEEDED (measure-only until ruled): the unlabelled answer line recurs — 6 this wave (5 in wave 77), all deck125; evidence per instance in wave78/engine-seat.md (2)(a) and wave78/deck125/review.md. deck125's guide was the only ~20 KB guide whose text never contained the literal `CHOICE:` (correlation, not proof; wave-77 instances predate that line) — the seat's edit 4 now pairs the label with the answer form; WATCH `answer_label_absent_heuristic_played` next wave: if it drops to 0 on deck125 the guide was the cause and no parser question remains. NO parser change.
- T2 HIGH — S3 RECURS, ROOT CAUSE NAMED: `stop_reached_reputs_collapsed` 0 of 44 asked stop-reached windows (4 deck123 chains, 90.8 min of decode for 3 non-pass answers; `123v152` seqs 16-25 is a ten-window repeat of the wave-77 shape). Two gates never admit the case: `w72StopReachedWindowCollapses` (AIPlayerGPT.cpp ~34029) requires `planTurn == nowTurn` while every chain spans a turn boundary from the PLAN that stated the stop (the model restates its PLAN each window, re-dating it); and CY F1's arm requires a REPEAT ROW while 40 of the 44 windows carry none. The board key also moves (life 20→19→18→17 across identical-row re-puts as their triggers drained — `123v162` seqs 36-41). Fix direction (a collapse may lose NO legal option): the stop statement persists across the turn boundary as the seat's own PLAN carries it; the collapse identity = the ACTION-KEY SET of the window unchanged since the decline + the printed M still at/past the stated stop — life and other board numbers that create no new row are NOT identity; no repeat row required; pin on the corpus-derived pair (`123v152` seqs 16→17 prompts as the fixture) THROUGH the live caller; predict `stop_reached_reputs_collapsed` > 0 and the chains gone.
- T3 HIGH — THE CG SHAPE, FOURTH WAVE (CY F8 FAIL): 419 of 1,454 same-seam same-turn re-asks were re-asked with byte-identical prompts (233) or prompts identical after stripping `{…}`/`[…]` (186) — 578.8 min of decode, 236 answered identically. AND the hold latch does not hold when the board moves: 87 holds taken at deck123, 56 followed at once by a byte-identical `options_text` at the same seam (`126` seqs 93→96: only life 13→14, one log line and the stack top differed), and `126` seqs 89-191: 77 windows / 1 h 47 min on ONE turn with `LOOP RUNNING`/`LOOP COMPLETE` printing "Nothing on this list stops the chain", 28 byte-identical ask windows in a row. Fix direction: take the ACTUAL re-asked pairs from the corpus as fixtures and drive each through the live ask-cache / async-slot / hold-latch key builders to find which byte differs (narration? life? stack top? the `[hold check:` bracket itself?); the ask key and the hold-latch key must be identical for two windows whose ACTION-KEY SET is identical at the same seam and turn; predict byte-identical re-asks → 0 and `identical_ask_answers_reserved` up by the difference.
- T4 HIGH — S6 STILL NEVER RENDERS: `[own loop verdict:` / RESOLVING / THREATENED / UNPROVEN = 0 hits in 42 logs while `own_loop_windows_asked` 2 (both deck126; the windows cannot even be located — no stack block, no per-record field). CV's pin asserted rendered bytes and passed; the live path renders nothing — the prompt-only channel the pin exercised is not the channel the corpus prompt is built from, or the gate ahead of it never admits a real board. Fix: find the live render path from the corpus window (`126v125`, the two own-loop asks — add a per-record `own_loop_verdict` field so the seats can find them), drive the corpus board through it, RED on base, then render; predict ≥1 rendered verdict line per own-loop window next wave.
- T5 HIGH — BLOCKERS `AT BEST` FALSE UNDER TRAMPLE/MENACE: "you would be at 14 AT BEST (no assignment of your blockers does better)" while the seat's own legal block ended at 15 (`146v152` seq 21); "12 AT BEST" vs actual 14 (`162v152` seq 19) — the trample/menace-as-unblocked floor charges the trampler's FULL power and asserts it as unbeatable; the `GANG BLOCK:` clause on a trample attacker omits the spill ("any 3 of yours together deal 6, enough to kill this attacker") while the lone-blocker row says "5 tramples to your face" — the model reasoned "0 trample", took 3, lost (`146v152` seq 26); one header says "no assignment survives" and then "this line LOSES THE GAME and the line above does not" with both lines at −6 (`146v123` seq 19). Fix: the AT BEST figure = the true knapsack over legal assignments with trample spill = power − assigned toughness (menace needs two), never "counted as unblocked"; the gang-block clause states the spill; a header never prints two verdicts that disagree; pins on the three corpus boards.
- T6 HIGH — `{crack-back cover:}` SUBTRACTS FROM THE CREATURE-ONLY FLOOR while the same prompt's header prints the added-up total (10, "that would KILL you"): row says "you would be at 4", the model said "Safe enough", the game ended at −2 (`162v146` seq 15). Fix: the cover clause subtracts from the SAME total the header states (creatures + non-combat clock), or names which total it subtracts from; pin on the corpus board.
- T7 HIGH — `{paying this taps: X, Y - they cannot attack this turn}` FALSE under opposing Intruder Alarm (`152v123` seqs 17/18; both attacked at seq 21) — the sole input to deck152's tap-bill rule, overridden correctly by the model for the third wave. Fix: the clause checks for any untap trigger/effect in play (opposing Intruder Alarm, own untap sources) and says "unless X untaps them" naming the source; pin.
- T8 HIGH — A HELD COUNTERSPELL READS AS "NOT IN HAND": `[no cast row now: … no legal target …]` on a counter held for their turn was read by the model ("I have no counterspells in hand right now" with two Fall of the Gavel on the hand line — `125v162` seq 52, lost the game) AND by CY F9's keep-X, which names Path to Exile as the cheapest instant in 10 of 47 clauses while counters sit in hand (`125v130` seq 315). Fix: the no-cast-row note on an instant with no legal target NOW says "held — castable when a target appears"; keep-X prices the cheapest COUNTERSPELL in hand by its printed cost whether or not it has a target now; pins.
- T9 HIGH — A `[cost: …, Sacrifice]` ROW NEVER PRICES THE BLOCKER IT SPENDS: prints `{removes 2 from the CRACK-BACK total … that still KILLS you}` and two windows later the blockers header computes exactly the lost blocker (`130v146` seqs 53 vs 55, "best case with every blocker assigned: you would be at -1") — deck130's one priced loss; and ATTACK TOTAL names Lightmine Field but does not fold it in — a 1/1 attacked into it 5× for 0 damage (`126v125` seqs 83/95/116/120/125). Fix: a sacrifice cost on a creature row prices the crack-back delta of losing that blocker (the same floor the blockers header uses); ATTACK TOTAL folds punisher/static damage to attackers that it already names; pins.
- T10 MED — S13 RESIDUALS: the hoist is instance-scoped, so 94 prompts still repeat one body up to 5× (11 rows share one `{card text:}` at `126v152` seq 23); deck125 44.4% of prompts over 20 KB (was 42%); deck130's max 29,641 B is 20,647 B NARRATION (57/321 windows over 24 KB, all in the 68-turn game; the trim retains 46 turns); deck162 max 19,112 B, 0 over 20 KB. Fix: hoist by BODY (card text) not instance; the narration trim keeps the last K turns by BYTES not turns, with the trigger at 16 KB of narration; measure per deck.
- T11 MED — `{effect:}` GLOSS RESIDUALS: the fallback cutter (no clause boundary fits) still truncates mid-clause with a bare `...` (Barrowin, Sigarda `126v152` seq 14; Ranger Class left with an unclosed `(`); `(...more)` drops the decision-bearing clause while keeping decorative keywords (Triumphant Adventurer's "Whenever … attacks, venture" cut — `125v146` seq 51). Fix: clause PRIORITY — triggered/activated ability clauses before keyword/static clauses within the cap; the fallback cut closes any open `(`/`"` and marks `(...more)`; pins on the four cards.
- T12 MED — S9 RESIDUALS: the ordinal strip now OVER-merges — `Staff of Nin` → `Staff of Nin #2` counted as the SAME row (`125v123` seq 263); the hold-check sentence calls a cost change both "the SAME row" and "a different cost" (`152v125` seqs 65→66); deck130 found 32 brackets where the engine counts FEWER than a naive row key (direction reversed from wave 77). Fix: occurrence index ⇒ a second occurrence is NEW (one row → two rows = 1 new); the cost-change sentence names it as "same action, cost changed" once; pin the Staff case.
- T13 MED — S4 residual: the drain annotation's `HOLD covers every link` promise fails — the hold did not cover every link (`125v123` seqs 379→382, 395→398). Fix: drop the promise or make the latch hold across the drain (T3's fix decides which); pin on the corpus pair.
- T14 MED — S11/forced-close: the identity reconciles (armed == stderr `unclosed <think>`, "unrecorded" == `dropped_decision_moved`) but stderr still prints `0 unrecorded`; every arm-bound counter 0 for the FIFTH wave (retire `force_close_defer_bound_hits` / `_same_arm_deferred` if 0 again); `phase2_answer_recovered` 0 / `_missing` 0 (wave 77: 15 / 1) — did the phase-2 path stop being exercised (UNTESTED; name why in the next engine seat).
- T15 MED — S12 residual: the gained/lost split prints only when BOTH directions have events (deck162: 9 split / 39 net-only) — print `+0 gained / -N taken` when one side is zero; deck130's wave-77 Hammer gate keys on the NET figure, not GAINED (guide item, deck130 next wave).
- T16 MED — `ON THE STACK: 3 damage to you` excludes punisher damage that two queued draw triggers on the same stack will cause while `DRAW PUNISHERS … costs you 2 life` prints on the same screen (`130v162` seq 49); `[doesn't untap during its controller's untap step]` reads as a tap STATE with no affirmative `[untapped]` (`146v123` seq 15, ~1,300 reasoning chars); the 21-row Adversary menu is folded but still 21 rows wide for a 3-outcome decision (`152`); Teferi +1 chains cost 21% of one game's calls (report).
- T17 MED — TOOLS: corpus-stats.py CENSUS CHECK is off by exactly the `engine_answered` reveal — a ROUNDTRIP kind that made no round trip: subtract per-record `fallback == engine_answered` (and any `latency_ms == -1`) from the round-trip sum, and stop counting a zero-eligible engine-answered window as a fallback in the harness banner; stderr's `0 unrecorded` line (T14); the late-check diagnosis read the seat log's `turn` field (62/81) while results.tsv says 85/68 — the record `turn` is not the results turn; use results.tsv or say which.
- T18 REPORT-ONLY (owner-ruled territory): `plan_line_missing` 103 (3.9%; deck152 9.3%), `protocol_deviation_replies` 109 = `unlabelled_plan` 90 + `plan_absent` 13 + `answer_label_absent` 6; latency p50 78 s (deck162 p50 126 s). `declined_face_latches` 0 for the 6th wave — RETIRED (remove the counter and the BS F3 docket line).

Docket: still UNTESTED, carried: S6 render (T4), S10 (0 defer records), CY F3 (no Mana Leak-class board), F4, F5, F11, CU F3/F5/F7, CS's `AT BEST once the` pin, BS F2/F5/F6, CF F1, the reasoning-body halves. Census rule: the record-kind sum must reconcile with `protocol_replies` after T17.
