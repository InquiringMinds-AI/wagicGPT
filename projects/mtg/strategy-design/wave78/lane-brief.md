# Wave-78 step-one lane brief (base = master after wave77 known-bugs; evidence = wave77/known-bugs.md §S + wave77/engine-seat.md + wave77/deck<N>/review.md)

RULINGS THIS WAVE TOUCHES: 000 — reasoning only in the native channel; reply = PLAN then action, nothing else; no instruction licenses prose; no parser reads outside the two labelled parts (a present `CHOICE:` label on the PLAN's physical line is inside — P9); caps never bound reasoning; the golden protocol TEXT (reply-protocol.txt / kReplyProtocol) and the thinking regime are owner-ruled — render brackets and row text may change; S16 is REPORT-ONLY and S1 is MEASURE-ONLY pending the owner's ruling — NO parser tolerance for an unlabelled answer line. 00 — full games; a game answered by heuristics is not a corpus game. 3 — suite THREADS=1 0 failed (known flakes: lifeline, merrow_reejerey, intrepid_adversary_repeated_payment), PARSETEST 0 failed. Never remove, cap, or auto-answer a legal option (R1's loop guidance is an ANNOTATION plus the model's own hold row — nothing is collapsed or auto-answered; R9 is MEASURE ONLY). No VPK. No AI attribution. NO CORPUS from a lane; no lane sends requests to the pilot this wave.

Standing mechanics: as wave71/lane-brief.md (worktree-only; `make -f Makefile.sdl -j8`; both link guards OK; binary freshness; every wagic run under `systemd-run --user --scope -q -p MemoryMax=4G -p MemorySwapMax=0 --`; suite THREADS=1 as a detached unit `w78-<LANE>-suite` waited with a FOREGROUND until-loop (no Monitor); count both failure strings; PARSETEST from bin; Edit tool for UTF-8 sources, byte-splice for MTGRules.cpp/GameObserver.cpp/primitives; U+FFFD 0; RED-on-base evidence + a falsifiable prediction per fix; commit with explicit paths, `git add -f` under bin/Res; write wave78/lane-<LANE>.md; touch `~/.gatelogs/W78_<LANE>_DONE`; weakest evidence). LESSON OF WAVE 74 stands: any NEW annotation carrying a board-derived number must be proven OUTSIDE every key (ask, async slot, hold latch, hold-check, option-set) with a PARSETEST pin that two windows differing only in that number yield identical keys, and the held set in a key pin is built the way the LIVE seam builds it (holdActionKeyRow via mLastMenuRows). LESSON OF WAVE 77: (1) a verdict the engine computes but pushes only into a KEY is invisible to the model — every verdict that should change a decision must be RENDERED, and its pin must assert the rendered bytes, not the key (S6); (2) a field named `_seq` that is not a record seq wastes eight seats — name telemetry by what it is and pin the join (S7); (3) the census must sum every record kind, including the new ones a lane adds. LESSON OF WAVE 76: (1) a bracket that prints a NUMBER or a COUNT must be pinned on the cases where the count is zero, one, and a reorder — Q1 shipped with the referent fixed and the count still wrong on reorders and disappearances; (2) a fix-lane finding whose fix is "compare more state" (CQ F4) can make a clause fail-closed into silence — pin that the positive clause still prints on the shape it was built for; (3) a clause that carries a number about LIFE must name the EVENT that moves it, or the model reads it as a statement about the row's mana cost (R3). LESSON OF WAVE 75: (1) an instruction's REFERENT must be something the reader can see — the hold-check bracket spoke of "the last window" meaning the last one the engine BUILT, while 3,402 built windows never reached the model; any bracket that refers to history refers to ASKED history; (2) a helper that hardcodes its input (the wave-74 stats script ignored its argument) reports the wrong corpus with full confidence — new tools take the run dir as an argument and echo it in their first output line; (3) a prediction's negative clause ("non-zero means lost") needs its own pin, not just the positive.

## Lane CV — the window/hold family (S3, S4, S6, S7, S9, S11)
S3 first (the stop-reached collapse under the ruling's allowance — pin the 12-re-put shape = 1 ask and pin that any other new row keeps the window). S4 the drain annotation + hold latch across a 7-link drain (annotation only). S6 render the own-loop verdict line (key marker kept). S7 emit the record seq + fix the two staged-no-render cases. S9 handle-stable count + disappearances. S11 the per-event `forced_close` record + the fixture that makes the same-arm bound fire. Weakest evidence names what the translog cannot show.

## Lane CW — render truth and pricing (S2, S5, S8, S12, S13, S14, S15)
S2 first (one instruction for repeat rows on both surfaces). S5 the redundant-counter annotation. S8 clause-boundary gloss cuts. S12 gain/loss printed separately. S13 card text once per shared-source target menu (measure bytes on the deck130 max prompt). S14 the X-pricing counterspell input, the `[second copy:` wording, the loyalty cost figure. S15 the no-op row fold (display-only, option set intact, answer→engine index pinned). Every clause with a number: key-stability pin set incl. ask + async-slot keys.

## Lane CX — records and transport (S1 measure-only, S10)
S1: the `answer_label_absent` class replaces the false `prose_outside_two_lines` on that shape; `recovered_seq` on `recovery` records; `answer_label_absent_heuristic_played` measure; NO parser change (owner ruling pending — say so in the report). S10: the defer record on the ACTING player's translog with `options_text`. Census helper (`~/.gatelogs/w77-stats.py` shape, copy into projects/mtg/tools/corpus-stats.py taking the run dir as its argument and echoing it) sums every record kind.

## Lane CY — fix lane for the Astra review (wave78/codex-review.md) — seeded after step-one gate 1

## Lane CY — fix lane for the Astra review (wave78/codex-review.md) — seeded after step-one gate 1 — base = master 818f4d338 (gate 1: 1291/0, 76 AI/0, PARSETEST 6547/0)
Twelve findings, each CONFIRM (fixture/pin RED on base reproducing Astra's trigger THROUGH THE LIVE CALLER PATH, then fix) or REFUTE (a pin proving the trigger cannot occur, with the reason). Astra's recurring charge again: pins that supply the predicate (`declinedN`, `busy=true`, per-row keys) instead of deriving it through the live seam. Every pin here exercises the live path; key-stability sets include the ASK key + async-slot key on the JOINED menu, not per row. S1 stays MEASURE ONLY (owner ruling pending) — F10 touches the counter, never the parser.
- F1 HIGH S3 (AIPlayerGPT.cpp:~41311, lane CV): the stop-reached collapse's decline ledger is keyed on `optionSetKeyOf`, which strips `[cost:]` — a token-maker whose real cost changed while the row names stay is still "declined", so a legal option is lost (the owner's collapse condition); the ledger carries no board key and no stop identity — a DIFFERENT already-reached stop restated this turn does not invalidate the earlier decline; the 12-window pin supplies `declinedN` directly. Fix: the collapse identity = the hold-latch action identity (`holdActionKeyRow`, cost groups kept) + the ask-cache board key + the stop statement (number and source) — all three unchanged or the window is asked; `declinedN` derived through the live path in the pin (no supplied count); pins: cost change → asked; a different stop restated → asked; the 12 re-puts still collapse to 1 ask.
- F2 HIGH S4 (AIPlayerGPT.cpp:~31145, lane CV): the drain annotation says the offered activation "stays available after" from a comparison with the PREVIOUS asked menu — a pending opposing trigger that removes the activation's source falsifies it; the counter counts every unresolved opposing stack object, spells included. Fix: the clause states only what is known — the menu is unchanged NOW and N opposing TRIGGERS (not spells) remain to resolve; never a post-resolution legality claim; pin: a pending trigger that removes the source prints no "stays available"; a pending opposing SPELL is not counted as a drain link.
- F3 HIGH S5 (AIPlayerGPT.cpp:~44709, lane CW): `fizzle` anywhere in the pending counter's script is read as a guaranteed counter — Mana Leak (`donothing?fizzle`, they can pay {3}) prints the second counter "spent for nothing unless yours is countered or removed". Fix: classify the seat's pending counter as CONDITIONAL when its script carries a pay/alternative clause (`?`, `pay`, `unless`) → print `your Mana Leak already targets this (they can pay {3} to keep it)`; the "spent for nothing" wording only for unconditional counters; pins Mana Leak vs Counterspell through the live stack join.
- F4 HIGH S6 (AIPlayerGPT.cpp:~31122, lane CV): the rendered RESOLVING verdict inherits a threat detector that recognises TARGETING only — an opposing untargeted sweeper (destroy all enchantments) above the loop trigger still renders "the loop closes without another action". Fix: an opposing untargeted stack object whose effect class removes permanents of a component's type (all/each + destroy/exile/sacrifice/bounce by type) → THREATENED; an opposing object above the trigger that is unclassifiable → a third face (`their <X> resolves before your trigger - the loop is not proven`), never RESOLVING; pin the enchantment-sweeper case and the unclassifiable case through the live state derivation.
- F5 MED S9 (AIPlayerGPT.cpp:~31441, lane CV): the note-reuse guard checks `unseen == 0` but not `gone` — suppressed-then-rebuilt `{A}` after B disappears returns the cached "no row is gone" note; and ordinal normalisation lives only in the bracket while the live latch compares ordinal-bearing keys — `Goblin #1` → `#2` prints "unchanged" while the latch reopens. Fix: guard on `unseen == 0 && gone == 0`; the latch and the bracket use ONE normalised key (ordinal stripped + occurrence index) so bracket-unchanged ⇔ no reopen; pins: the suppressed-then-rebuilt sequence (gone = 1 printed), `Goblin #1` → `#2` = unchanged AND no reopen, through the live latch.
- F6 MED S8 (AIPlayerGPT.cpp:~539, lane CW): the clause cutter tracks neither quotes nor parentheses — `Enchanted creature has "Tap: Draw a card. Activate only if you control a Wizard."` cut after the first sentence emits the activation without its condition and an unfinished quote. Fix: `"…"` and `(…)` are atomic units — cut only at depth-0 boundaries; if no depth-0 boundary fits, omit the whole unit and mark `(...more)`; pin the Wizard case (condition kept or the whole granted ability omitted, never half).
- F7 MED S12 (AIPlayerGPT.cpp:~28040, lane CW): GAINED/LOST are differences between turn snapshots labelled as events — samples 20, 15, 18 with real events +7/−12/+3 print +3/−5, and an all-negative-net game hides the gain entirely (the original S12 failure). Fix: accumulate gained/lost from the life EVENT stream the seat already narrates (WEvent life changes per turn); if no event source exists at the site, print the net only and DROP the split — never label sampled deltas as events; pin the 20/15/18 sequence with the event stream = +10/−12.
- F8 HIGH S13 (AIPlayerGPT.cpp:~10137, lane CW): the hoisted shared card text is emitted OUTSIDE annotation delimiters and enters the ask key and async-slot key via `w77KeyTailOf(joinNumberedRows(rows))` — a changed shared gloss on the same action rows = a different ask key = a re-ask (the wave-74 CG shape); the stability pin compares rows before/after erasure and never includes the emitted header. Fix: the hoisted header goes inside the annotation delimiters the tail stripper already removes (or the stripper strips the header line explicitly); pin EQUAL ask + async-slot keys across a changed shared gloss on the JOINED menu through the live key builder.
- F9 MED S14 (AIPlayerGPT.cpp:~44465, lane CW): the keep-X "cheapest castable instant" figure uses converted cost and source count — Essence Scatter with only Mountains counts as castable; Force of Will's alternative cost is ignored. Fix: payability from the mana forecast for the candidate from the remaining pool honouring colours (the same `ManaEngine` forecast the cast rows use, preserveOptions=false); an alternative cost the engine supports is stated as such (`or its alternative cost`); pins: Essence Scatter vs Mountains = not castable; a colour-satisfied instant = castable; through candidate selection, not a supplied cost.
- F10 MED S1 (AIPlayerGPT.cpp:~20640, lane CX): `answer_label_absent_heuristic_played` increments on the raw shape flag, not the FINAL deviation class — `2 (Hold priority)` with no PLAN classifies `unlabelled_plan` yet increments the counter. Fix: increment only when the final class is `answer_label_absent`; pin the no-PLAN case = `unlabelled_plan`, counter 0; the 5 wave-77 records still count 5. MEASURE ONLY — no parser change.
- F11 MED S11 (AIPlayerGPT.cpp:~92477 pin; live predicate ~18756, lane CV): the same-arm bound pin supplies `busy=true` repeatedly while the live continuation checks only the PARKED arm — with a deferred close, this arm still armed and an empty park, the live path arms immediately and supersedes this arm instead of deferring; the fixture's 1 is local simulation. Fix: the continuation predicate checks THIS arm's armed state as well as the park; pin the described state THROUGH `pollCompletionRetry` — `force_close_same_arm_deferred` reads 1 on the live path and the identity forced = recorded + deferred + unrecorded holds.
- F12 MED corpus-stats.py:~154 (lane CX): the CENSUS CHECK reconciliation lists recovery + defer but omits `forced_close` (this diff's new kind) — one ask + one forced_close + protocol_replies 1 prints a difference of one with an empty kind list. Fix: the explanatory kind list is computed generically = every kind not counted in `protocol_replies` (recovery, defer, forced_close, any future kind); a tool self-check on the mocked input.
Astra's side-checks (no action): ctor initializer survives the hand union (list not declaration-ordered — pre-existing `-Wreorder`, harmless); merged PARSETEST blocks close correctly; golden protocol unchanged; no new unlabelled-answer acceptance found; deck130's 30,125 B max = ~21,918 B narration (24,000 B trim trigger, no whole-prompt bound) — docket for wave 79.
Order: F1, F8, F4, F3, F2 (lost legal option / key drift / false loop verdict / false counter claim / false legality), then F5, F11, F6, F7, F9, F10, F12. Gate: suite THREADS=1 + PARSETEST + harness --selftest + instruction guard; lane report wave78/lane-CY.md with per-finding CONFIRM/REFUTE, RED evidence, fix, GREEN, prediction, weakest evidence.
