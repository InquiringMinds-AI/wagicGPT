# Wave-78 step-3 brief (lean; REJECT is the expected verdict for anything not paid for by a decision the corpus shows going wrong. Amendment 332 stands: guides are decision-time instruction only, hard ceiling 20,000 bytes, no citations/history in a guide — evidence goes in YOUR review.md. Amendments 333 (heading = scope declaration) and 334 (no concessive clause for a rule's hard case) are drafted in wave74/strategy-writing-skill-v2-amendments.md, not yet folded in by the owner — apply them anyway.)

Ninth reasoning-on corpus (invariant 000). Wave 78 step one (wave77/known-bugs.md §S → wave78/lane-{CV,CW,CX}.md + wave78/codex-review.md → lane-CY.md, twelve findings all CONFIRMED and fixed): CV window/hold — S3 the stop-reached RE-PUT collapse (a window whose only "new" row is the stop-reached repeat row, with the stop stated this turn and the list already declined, is not re-asked; CY F1 made the collapse identity = the hold-latch ACTION key (cost groups kept) + the ask-cache BOARD key + the stop statement, so a moving board or a changed cost re-asks; counter `stop_reached_reputs_collapsed`), S4 a drain annotation while N opposing TRIGGERS drain (`stack_drain_windows_asked`; CY F2 removed the "stays available after" promise and counts ACTION_ABILITY only), S6 the `[own loop verdict:]` line is now RENDERED (CY F4: an opposing untargeted type-sweeper above the loop trigger = THREATENED; any other opposing object above it = a new UNPROVEN face, never RESOLVING), S7 `hold_check_ref_seq` = the record seq + `hold_check_ref_window` = the ordinal (contract enforced off the prompt's `[hold check:` presence), S9 the count keys on the instance (ordinal stripped, occurrence index) and prints `N gone` (CY F5: the note-reuse guard reads `gone`; latch and bracket share one key), S11 a per-event `forced_close` record (`forced_close_events`; CY F11 the continuation checks this arm + the park); CW render/pricing — S2 the repeat-row carve-out in the closing sentence (golden protocol untouched), S5 `{your <counter> already targets this on the stack …}` from a live stack join (CY F3: Mana Leak-class conditional counters say "they can pay {N}"; "spent for nothing" only for unconditional counters), S8 the `{effect:}` gloss cuts at clause boundaries with `(...more)` (CY F6: quotes/parens atomic), S12 `Opponent life trend:` prints gained and lost beside the net (CY F7: from the WEventLife stream; net-only when no event source), S13 shared card text hoisted ONCE above shared-source target rows (CY F8: the hoisted header is stripped from the ask + async-slot keys — the wave-74 CG shape caught a THIRD time), S14 keep-X names the cheapest castable COUNTERSPELL (CY F9: colour-aware `selectAutoTapProducers` forecast; alternative costs stated), second-copy price de-emphasised, loyalty cost in its own group, S15 no-op rows folded display-only (option vector intact); CX — S1 MEASURE ONLY (owner ruling pending): the unlabelled `<n> (<name>)` answer line after PLAN: is classified `answer_label_absent` (replaces the false `prose_outside_two_lines`) with gameend `answer_label_absent_heuristic_played` (CY F10: keyed on the final class) — THE PARSER IS UNCHANGED, the heuristic still plays those windows; S10 the `defer` record goes to the menu card's controller with `options_text` (root cause docketed: `buildMenuChoice` takes the X ceiling from the ACTING seat's pool); `tools/corpus-stats.py` (dir as argument; the census tool used below). Records carry `thinking`, `reasoning_chars`, `off_protocol_bytes`, `plan_line_missing`, `plan_steps_done`, `hold_check_ref_seq`, `hold_check_ref_window`, `protocol_deviation` class, plus the new `forced_close` and `recovery` kinds; gameend carries the counters below. Wave-77 guide edits for six decks are the LIVE guides (installed 2026-09-11 at 031db297b; deck152 KEEP = its wave-76 edition; each deck's before->after list is in wave77/deck<N>/review.md).

Repo /home/magi/Projects/wagicGPT, game dir projects/mtg. Use python for translogs; use /usr/bin/grep (bare grep drops matches). DO NOT edit anything under bin/Res, src/, or run git or wagic; write only under projects/mtg/strategy-design/wave78/. Never write mechanisms as the owner's positions. Report what you did NOT check.

CORPUS (wave 78, SINGLE binary archives/wagic-47ef378c8-w78step1 = source 7c0f15835; `--thinking on`, WAGIC_GPT_TIMEOUT=900, no operator token ceiling). No incident: one run, 21 concurrent, all 21 games finished naturally (the last two ran to turns 62 and 81 — 8 h 30 min total); the pilot never stalled. Corpus under review: /home/magi/.Wagic/ai/gpt/selfplay-runs/matchups-20260911-125420-final (42 seat logs all with gameend, 0 timeout-tainted, results.tsv, askreplay/ 30 files; census = `python3 projects/mtg/tools/corpus-stats.py <dir>`, output also at ~/.gatelogs/w78-stats.out). 21 games, 0 HUNG/crashed; record-kind sum 2,670 = ask 1,906 + priority 532 + attackers 93 + discard 52 + forced_close 32 + blockers 22 + reveal 18 + recovery 8 + bottom 7; `protocol_replies` 2,629 (wave 77: 2,740; 1.88 ask-decisions/turn vs 2.02), 100% with native reasoning (median 5,871 chars, max 17,847), **8 heuristic fallbacks**: 7 `unparsed_reply` on the ask seam (6 = `answer_label_absent`, ALL on deck125 seats — `123v125` seqs 83/140/215/305, `125v126` seq 107, `125v130` seq 303 — the S1 shape the owner has not ruled on; 1 = `unlabelled_plan` on deck162 `162v?` seq 16: reply `\n\nCast nothing right now.\n3 (Cast nothing right now)` — no PLAN label AND no CHOICE label) + 1 `reveal`/`engine_answered` (`152v123` deck152 seq 22, latency −1, the engine answered without a round trip — it is why the census tool's reconciliation is off by exactly one: report, tool item); 0 reply_truncated, 0 action_before_plan; CORPUS-WIDE off_protocol_bytes>0 96 (3.7%, wave 77: 4.4%), plan_line_missing 103 (3.9%, wave 77: 5.1%), `protocol_deviation` classes compliant 2,520 / `unlabelled_plan` 90 / `plan_absent` 13 / `answer_label_absent` 6, latency p50 78.3 s (ask seam p95 179.8 s); gameend sums: `ask_replays_reserved` 1,344 / `identical_ask_answers_reserved` 953, `hold_released_turn` 136, `hold_windows_skipped` 2,816 (cast 2,310 / priority 506; wave 77: 2,128 = 1,433 / 695), `stop_reached_windows_skipped` 44 (wave 77: 7) / **`stop_reached_reputs_collapsed` 0** (NEW — the S3 collapse never fired), `stack_drain_windows_asked` 60 (NEW), `own_loop_windows_asked` 2 (NEW render), `forced_close_events` 32 (NEW record) / `forced_close_unrecorded` 16 (wave 77: 10) / `force_close_arms_refused` 0 / `_deferred` 0 / `_same_arm_deferred` 0 / `_defer_bound_hits` 0, `phase2_answer_recovered` 0 / `_missing` 0 (wave 77: 15 / 1), `crossphase_identical_reputs` 93 / `crossphase_board_unchanged` 4, `main_phase_windows_skipped` 83, `mana_only_windows_skipped` 367 (wave 77: 69), `own_turn_windows_skipped` 650, `reserve_decline_windows_noted` 20 / skipped 2, `plan_names_stranded_card` 2, `plan_names_uncastable_zone_card` 35 (wave 77: 52), `protocol_deviation_replies` 109, `async_drops` 46 (wave 77: 53), `repeat_annotated_takes` 4, `identical_option_asks_resolved` 6, `blocker_forecast_gang` 22 / multi 196 / collapsed 8 / rows 250, `chain_windows_only_selfharm` 11 / `chain_selfharm_rows` 411 / `chain_acting_rows` 1,612 (+ `_cast` 318 / 389 / 1,032), `declined_face_latches` 0 (6th wave — RETIRED next wave); wins deck146 4/6, deck126 4/6, deck152 4/6, deck123 4/6, deck125 3/6, deck162 2/6, deck130 0/6 (wave 77: 152 6/6, 146 5/6, 162 3/6, 125 3/6, 123 2/6, 126 1/6, 130 1/6 — deck130 1→0, deck126 1→4, deck152 6→4; hands are real, do not read variance as guide effect).

## Per-deck agent (one per deck: 146, 152, 125, 126, 162, 123, 130)
Purpose (owner's words): read the transcripts of your deck, compare the LLM's actions to the
instruction set (the guide projects/mtg/bin/Res/ai/baka/deck<N>_strategy.txt + the general guide
wave62/general-strategy.md (wave-59 edition + R331; NOTE from the wave-63..67 syntheses: this file is NOT loaded at runtime (only deck<N>_strategy.txt is opened, AIPlayerGPT.cpp:15332) — the deck guide is the live surface) + the reply protocol in the prompt), find issues in the game, the
interface, and the guide, and surface anything else worth the core loop's attention.
Method: for each of your deck's 6 games (your seat's translog; the opponent's for context),
trace the decisions that decided the game. Classify each misplay PERCEPTION (the model misread a
true surface, or the surface was false/missing -> ENGINE/RENDER item, cite file+seq) vs STRATEGY
(read correctly, decided against the guide or against good play -> guide item). Note fallbacks,
re-asks, and any decision where the prompt's information was insufficient or wasteful (what a
better interface would have shown). Hands are real (mulligans, bottoming): do not read variance
as guide effect.
Output: ONE file, wave78/deck<N>/review.md: (1) game-by-game outcome + the deciding decisions with
seq citations; (2) engine/interface/card items ranked HIGH/MED/LOW with a concrete repro (file,
seq, the rendered line) — this is the primary deliverable; (3) guide verdict: KEEP as is (a
success verdict, state why) or EDIT — if EDIT, also write wave78/deck<N>/strategy.txt (the full
revised guide, start from the live one) and list each edit before->after with the seq that
paid for it IN review.md — the guide itself carries NO citation, count, or history (Amendment 332); hard ceiling 20,000 bytes, and an EDIT that grows the guide must say which rule earned its bytes; (4) OPTIONAL proposals, only when
your evidence supports them: general-guide changes (wave78/deck<N>/general-proposals.md) and
strategy-writing-skill changes (wave78/deck<N>/skill-proposals.md; current skill edition =
wave68/strategy-writing-skill-v2.md (the ~40 KB distillation, owner-installed 2026-09-06 after six Astra adversarial rounds; read it in full — it is short; the 1.3 MB wave68/strategy-writing-skill.md is lineage only) — read it before proposing; a proposal that
restates an existing amendment is noise). No prediction tables, no rotation evidence, no
docket bookkeeping. Final message: <= 10 lines.

## Engine-narration seat (one agent)
You read the whole corpus for the engine's sake, not the guides'. Deliverable wave78/engine-seat.md: (1) corpus census as last wave (games, decisions, decisions/turn vs wave 77's ~2.1, fallbacks by kind with genuineness, `async_drops`, `transport`, `deadline_pct`, reply shape, `reasoning_chars`, `reply_truncated`, `askreplay/`, `plan_steps_done`); (2) REGRESSION WATCH on the wave-78 changes — the section that matters most (SINGLE binary, single run, no reruns): (a) FALLBACKS — quote all 7 `unparsed_reply` replies verbatim (raw reply bytes + the window's legal rows); for the 6 `answer_label_absent` (all deck125) confirm each IS the S1 shape (PLAN line, then `<n> (<short name>)` with no `CHOICE:` label, in-range, byte-exact name) and say whether the heuristic's play changed the game (this is the evidence the owner's ruling needs — the parser is UNCHANGED by ruling; do not propose parser tolerance, just state the cost); the 7th (deck162 seq 16, no PLAN and no CHOICE label) — which pin, and did the heuristic's play matter; the `reveal`/`engine_answered` record (`152v123` deck152 seq 22): what did the engine answer and why was no round trip made; (b) S3 — `stop_reached_windows_skipped` 44 (wave 77: 7) but `stop_reached_reputs_collapsed` 0: find every window carrying `ALREADY AT OR PAST your own stop` re-put in the same phase; for each, which of the three identity legs (action key / board key / stop statement) changed between re-puts — CY F1's own weakest evidence was that a MOVING board number (`M=31 → M=34`) keeps the collapse from ever firing; count the re-put chains and their decode seconds; say whether the S3 shape (12 re-puts, seqs 41-52 in wave 77) recurred; (c) S4 — `stack_drain_windows_asked` 60: quote 5 drain annotations against the stack at that seq (N = opposing TRIGGERS only — CY F2; no 'stays available' promise anywhere); did any seat hold across a drain and lose a legal option that a trigger's resolution created; (d) S6 — `own_loop_windows_asked` 2: quote both windows; is the `[own loop verdict:]` line RENDERED, which face (IDLE / RESOLVING / THREATENED / UNPROVEN), TRUE against the stack, and did the seat act on it; CY F4's own risk: the UNPROVEN face silencing a true RESOLVING verdict — check; (e) S7/S9 — `[hold check:` brackets: for every record-adjacent pair at one seam (cast, priority, land), is the claim true against the record named by `hold_check_ref_seq` (now a record seq — join on it and report any that does not resolve to the previous asked window at that seam); the `N gone` clause — every printed `gone` true; any `Goblin` → `Goblin #1` renumbering still counted as new; the 'first window asked' sentences after an asked predecessor; (f) S11 — `forced_close_events` 32 / `forced_close_unrecorded` 16 (wave 77: 10) with every arm-bound counter 0: read the 32 `forced_close` records — arm, seq, outcome — and reconcile 32 = recorded + deferred + unrecorded against the stderr `unclosed <think>` count; name what superseded each of the 16 (CY F11's continuation now checks this arm + park — did the bound fire? it reads 0); `phase2_answer_recovered` 0 / `_missing` 0 (wave 77: 15 / 1) — did the phase-2 path stop being exercised, and why; (g) S5 — every `{your <counter> already targets this on the stack …}` clause: true against the stack, conditional counters (Mana Leak class) worded with the pay figure, and did any seat still double-counter (the `125v146` seq 125 shape); (h) S8/S13 — the `{effect:}` gloss: sample 30 entries, 0 mid-clause cuts, `(...more)` only after a whole clause/unit, any false statement; S13: max prompt bytes (wave 77: 36,211; deck130 max 30,125 was NARRATION — CW), count over 20 KB per deck (wave 77: deck125 42%), and whether hoisted shared card text appears ONCE above shared-source target rows (and never inside a row); the deck130 narration size vs the 24,000 B trim trigger; (i) S12 — every `Opponent life trend:` line: gained and lost printed beside the net, figures true against the narrated life events of that turn span (CY F7 from WEventLife; net-only when no source — count those); (j) S14/S15 — `[X pricing]` keep-X names a COUNTERSPELL the seat can actually cast with the remaining colours (CY F9); `[second copy:` wording; loyalty rows' counter cost in its own group; folded no-op bands: option numbers intact, any answer naming a folded row; (k) S2 — every window carrying a repeat row: the closing sentence carves it out; `repeat_count_reask` 0 this wave?; `reasoning_ngram_repeat` incidents; (l) S10 — `defer` records 0 this wave? (`menu_not_askable`); any X-announcement payment loss (`dropped after its X was announced`); (m) `async_drops` 46 — all re-asked?; `crossphase_identical_reputs` 93 / `_board_unchanged` 4 (quote the 4); `mana_only_windows_skipped` 367 (wave 77: 69 — what grew and is every skipped window truly mana-only); `hold_windows_skipped` 2,816 (cast 2,310 / priority 506 — what grew); `protocol_deviation_replies` 109 = `unlabelled_plan` 90 + `plan_absent` 13 + `answer_label_absent` 6: classify 20 `unlabelled_plan` samples (a plan line with no `PLAN:` label — did the parser still find the action?); (n) CY F8 — the ask/async keys with the hoisted header stripped: any window re-asked with rows differing only in the shared card text or inside `{...}`/`[...]` (the CG shape) — count byte-identical-after-strip re-asks; (o) the harness — no tripwire fired; the late check found two long games (turns 62/81), not a stall; (3) adjudicate EVERY prediction in wave78/lane-{CV,CW,CX,CY}.md (CY = the twelve Astra findings; its own weakest-evidence list is §Weakest) as PASS / FAIL / UNTESTED with counts and file+seq (UNTESTED is a real verdict), plus the carried UNTESTED docket (BS F2 Tribute second edict, BS F3/land latch `declined_face_latches` 0 for the 6th wave (retire), BS F5 blockers ceiling, BS F6 distinct-index re-ask, CF F1 pending-deadlock, F2/F5-nonlifelink/F8 haste/F9, the reasoning-body halves of N14a/N14d/O20; {X}-announcement payment loss — count `dropped after its X was announced`); (4) engine/render/narration defects ranked HIGH/MED/LOW with repro — especially any new render clause stating a false fact (the drain clause, the UNPROVEN/THREATENED loop faces, the conditional-counter clause, the GAINED/LOST split, the hoisted card text, the `(...more)` cuts); (5) the owner's lategame specimen (standing rule, alert-only): ONE late-turn decision (turn >= 25), full `prompt` verbatim to wave78/lategame-specimen.txt with a 3-line header. Final message: <= 15 lines, HIGH items first.

## Synthesis (runs only if any deck agent wrote general-proposals.md or skill-proposals.md)
Read all proposals + engine-seat.md. Decide each proposal: ADOPT (edit wave78/general-strategy.md,
a copy of wave62's (the R331 edition; waves 63-67 wrote no edition; wave 68 installed wave68/strategy-writing-skill-v2.md as the current edition), or append a numbered amendment to wave78/strategy-writing-skill.md, a
byte-verbatim copy of wave59's with amendments appended — verify the prefix with cmp) or
REJECT with one line of reasons. Write wave78/synthesis-notes.md. If a deck agent's guide edit
contradicts an adopted general change, note it; do not edit deck guides yourself.

METHOD NOTE (from the wave-59 engine seat): the translog `phase` field is not the rendered phase — count phase-gated facts from the prompt text.
