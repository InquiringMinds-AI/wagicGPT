# deck137 -> general-strategy (core prompt) proposals -- wave 28

## PASS. No core-prompt change proposed. (23rd consecutive core PASS from this seat.)

Nothing this corpus routes to the SHARED core prompt. Rationale by layer:

- The one open defect (March-via-convoke X-announcement non-resolution) is ENGINE, not prompt --
  routed to notes.md (ledgered item, deck137 is the sole observation seat). A core-prompt line
  cannot make a deferred engine tap-payment resolve.
- The one behavior lever (anti-re-pick after an unresolved cast) is DECK-SPECIFIC to the convoke
  cards deck137 pilots; it lives in the deck137 guide, not the core prompt. No other deck in the
  pool casts convoke, so generalizing it to the core would add noise for every non-convoke seat.
- Comprehension surfaces the core prompt owns (instance handles #N, land-identity tags, adventure
  bodies, shockland ETB menus, blockers legality) all rendered clean and were bound correctly this
  corpus -- 0 unparsed, 0 stale_echo, 0 retracted, 0 illegal gang-piles. Nothing to add or fix.

The core prompt is doing its job at this seat: every decision surface the model needs was
present, legal, and correctly disambiguated. The residual failures are one engine defect and
zero prompt defects. Core: PASS.
