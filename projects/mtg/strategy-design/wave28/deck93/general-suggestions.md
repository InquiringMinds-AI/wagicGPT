# deck93 general-strategy (core-prompt) suggestions (wave 28)

## VERDICT: PASS (no core-prompt / deck-agnostic strategy change from this seat)

23rd consecutive PASS. Rationale:

- The one actionable residual (L2 phantom-attacker) is a PROMPT-REPRESENTATION / engine change
  - a per-creature summoning-sick tag on the board render - NOT deck-agnostic strategy prose.
  It routes to notes.md (engine/harness ledger, N-93a) per layer-routing, exactly as the e4
  land tag was an engine/representation change, not a general-strategy line. Putting "count
  from the A-lines" into the general prompt would repeat the demonstrably-failed guide-prose
  approach at a higher layer (the deck93 guide ALREADY says it and is ignored). Do not escalate
  a failed perception-prose teach into the core prompt.

- The validated teaches (mulligan land-count, lord-shrink block discipline) are deck-SPECIFIC
  (Relentless Rats lord-of-each-other; mono-land mana-count) and correctly live in the per-deck
  guide, not the core prompt.

No convergence-worthy core-prompt proposal originates at this seat this wave.
