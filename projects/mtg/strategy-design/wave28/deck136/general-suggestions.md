# deck136 -> general (core) prompt suggestions (wave-28, FIRST-GUIDED)
# Layer: general-suggestions.md = core-prompt proposals, layer-routed.

## VERDICT: PASS (core prompt). The 23rd consecutive core PASS.

deck136 produced NO core-prompt signal this wave:
- Parser/reply-protocol: HELD PERFECTLY. 0 fallbacks at the deck136 seat (down from
  wave-27's 4, which were all the engine phantom-chooser - now FIXED at the card-script
  layer, not the prompt). The corpus-wide 18 fallbacks are all OTHER seats
  (16 deferred_to_heuristic + 2 retracted_choice); none at deck136.
- Mana/casting: mono-black, zero color-anxiety, zero mana-veto tax. Core handles the
  deck's unconditional-value plays competently.
- Land identity tags (e4): rendered cleanly at this seat; no prompt-noise, no confusion.
- No first-line-vs-conclusion contradiction, no commit-lock, no decode-collapse, no
  representation-ambiguity wrong-commit.
- The one behavior arguably worth a core line eventually (a universal "you have no
  flyers/reach -> cannot block a flyer -> race" prior) remains a below-bar CORE candidate
  awaiting convergence; deck136 is a WEAK witness (it has Rankle, pool is flyer-light).
  Left to synthesis as a convergence tally, not a deck136 proposal.

IMPORTANT: the ONE misleading behavior this wave (the pilot chasing "Legion's End the
Rats") was a GUIDE FACT error (fabricated MV), NOT a core-prompt weakness - the core
reasoned correctly given a false premise and even self-corrected against the guide. No
core line fixes a bad guide fact; the fix is in strategy.txt. Route all deck136 work to
strategy.txt (guide) and notes.md (engine ledger). No core change proposed.
