# Wave-70 known bugs and the revert list (from corpus matchups-20260906-224849, the first reasoning-on corpus since w43; deck 152/126 items pending the rerun)

Sources: wave70/engine-seat.md (§1 census table = the revert evidence; §0/§5 defects), wave70/deck{146,125,162,123,130}/review.md. Invariants touched by any fix here: 000 (no instruction may license prose; no parser reads outside the two lines; caps never bound reasoning), 00-P (procedure changes replace + sweep), 3 (suite green; PARSETEST 0 failed).

## R — THE REVERT LIST (census DELETE rows; every mechanism wave 66-69 built to cope with in-band reasoning is DEAD: 0 firings in 2,119 decisions where wave 69 had 44 fallbacks and 1,588 overruns)
Delete the CODE and its PARSETEST pins (a deleted mechanism's inverse pins go with it); keep a COUNTER only where the row says so. Do NOT revert an UNTESTED row (0 windows arose) — that is not evidence.
- R1 `pre_answer_overrun`, `post_plan_overrun`, `reply_trimmed_bytes`: writers already removed in BM — delete any remaining reader/consumer and the record-trim re-key that still keys on the trailing quantity.
- R2 anything that REACTS to `post_answer_overrun`/`reply_overrun` (keep the counter: 2 of 2,119).
- R3 truncation machinery: `reply_truncated*` re-asks at every seam, `truncated_abandoned*`, `commitRetracted`, `salvageProsePutList` (0 truncations; classes unwritable). Then the owed ANSWER-ceiling re-fit (LOW-3 below): p99 of PLAN+action is < 300 B at every seam against 768/896/512-token ceilings — re-fit from THIS corpus's bytes (the wave-68 error was fitting to a prediction; this is a measurement).
- R4 reversal remains: `decision_reversed_in_prose`, `planSaysPassThisWindow`, `proseReversesInCorrectionZone`, `proseNegatesTakenRow`, `planChoiceConflict`/`plan_choice_conflict*`, `planArguesAgainstRow`/`plan_argues_against_row_seen` — computed-but-unused after BL/BN; delete the predicates, their state, their census counters and pins (`plan_choice_conflict_seen` 0 of 40; the wave-69 largest fallback class is dead).
- R5 `index_name_conflict`/`distinct_index_reask` RE-ASKS (keep the parse notes `echo_index_conflict`/`index_name_unique_name`/`name_over_index`: all 5 recovered without a re-ask).
- R6 `repeat_count_reask`/`repeat_past_stop` re-asks (keep `repeat_annotated_take`).
- R7 `reasoning_tail_answers` counter (0 of 40; BM prediction 5 FAILED — the parser never sees a tail answer).
- R8 plan paragraph-bound machinery (`plan_paragraph_bound_cuts` 0 of 40; keep the 400-char cut — it costs nothing: max PLAN 422 B, 2 records ≥ 400).
- R9 dead re-ask kinds whose strings appear in no log: `label_missing_reask`, `noop_plan`, `attack_*`/`blocks_*` re-ask kinds — confirm removed, delete residue.
KEEP (earn their keep under reasoning-on): `noop_row_zero_reask` (6 of 9 fallbacks — but see L3), HOLD/mana window skips, `identical_ask_answers_reserved` (INSTRUMENT — link 2 of L1), `identical_option_asks_resolved`, async drops, `answer_replaced`, multi-answer latching (thin, non-zero), `off_protocol_bytes` (as a meter; see L10), `reasoning_*` fields, the answer-locked retry (fix its marker, L9).

## L — ENGINE / RENDER (ranked)
- L1 HIGH — THE HANG (engine-seat §0, deterministic): land-drop seam picks an MDFC (Hengegate Pathway), the face menu opens, the model declines (`CHOICE: 3 (Decline)`, correct play at opp 1 life). The decline is NOT sticky (`AIPlayerGPT.cpp:~39920` cancels the menu; land drop unused; board byte-identical), Baka re-proposes the same card next tick, `mAskCache` (~34978) replays the cached decline with NO request and NO record, the W64-AK pass floor (`AIPlayerBaka.cpp:~5578`) takes the `hasAnyLegalAction` arm forever (`menu pass floor reached … not passing` 2,584,166 times, fired 0). 3,641 s, 1.23 GB stderr, game lost. Fix: latch the declined card for that land drop (like the seam's own `held the land drop` at ~36183); a no-progress arm on the pass floor; `mAskCache` refuses after N identical replays AND writes a record. Fixture RED on base (a deterministic livelock is fixturable).
- L2 HIGH — the silent replay path hides a total game stop (engine-seat HIGH-1): cache/repeat re-serve paths write no translog record, so a hung seat log is indistinguishable from "not asked". Every re-served answer writes a (compact) record, and the harness's no-progress watchdog (3,600 s) should also read stderr growth.
- L3 HIGH — `rowSaysNoOp` (`AIPlayerGPT.cpp:~27306`) matches the substring "does not apply" — Tragic Slip's MORBID clause — as a no-op verdict and ignores the `{kills: <name>}` clause on the same row: 3 of 3 seat fires false (deck123: 152 seq 11/13, 146 seq 4); at 152 seq 13→14 the re-ask's "says it does nothing" wording made the model cast nothing and lose two printed kills. Fix the predicate (a kills/verdict clause on the row defeats no-op), pin both.
- L4 HIGH — an instant printed `[castable now]` gets NO cast row at `priority`-kind windows (deck130: Starstorm 23 of 23; all 25 "Casting decision" asks offered it). Lost vs123 outright (seq 17: 4 mana, X=2 sweeps 19 of 20). Find why the priority-window legal set omits it (oracle vs seam filter).
- L5 HIGH — the two-step cast reverses itself: the model answers `Cast X`, then at the "Choose an option for X" modal takes `cycling` against its own PLAN (deck130: 7 cases, repro deck126 seq 35→36). Either the modal must carry the committed intent (the cast already chosen) or not re-offer cycling once cast was chosen.
- L6 HIGH — Tribute to Hunger: two Tributes both paid 15 life off ONE Emrakul that died once (deck125: deck126 seq 120; primitive `mtg.txt:124073`), opp 10→40, +15 turns. Verify the ORACLE text first (feedback memory: retrieve Gatherer/Scryfall before trusting the primitive), then card script before engine.
- L7 HIGH — animating lands are in no count on screen: header, sweeper K and Lightmine N all read 0 while Lair of the Hydra dealt 14 over two turns (deck125). Manlands must count as creatures where they can be animated (or the counts must say "+N animatable").
- L8 HIGH — `{feeds:}` converter count and `{reserve:}` verdict computed against a hand the seat's OWN pending Teferi's Puzzle Box trigger (on the stack in the same prompt) was about to bottom (deck162: 152 seq 21). Reserve/feeds must fold pending hand-replacing triggers, or say the hand is about to change.
- L9 MED — `reasoning_budget_hit` over-stamped ~4x (19 records, 15 with 4.2k-15.6k chars against a 6,000-token budget and clean replies; only 4 genuine, at 18.9k-23.8k) — a budget re-fit read off it would be wrong 4x; `phase2_answer_missing/recovered` 0 of 40 while 4 answer-locked retries happened (one HTTP 400 → heuristic). Fix the marker (finish_reason from the API, not a char estimate) and wire the phase-2 counters to the path that runs. Pin both.
- L10 MED — a MISSING PLAN line is invisible (11 replies, `off_protocol_bytes 0`) while a mislabelled one is charged (92 unlabelled + 7 `YOUR PLAN:`). Add `plan_line_missing` to the record and gameend census (the silent-instrument shape BK fixed for reasoning_chars).
- L11 MED — the prompt's own heading is transcribed as the label: 7 × `YOUR PLAN:` (engine-seat MED-4, BL's flagged composition-seam risk, recurring). Fix on the RENDER side (the heading over the carried plan must not be a label-shaped string) — never accept `YOUR PLAN:` in the parser. Same render review for the 92 unlabelled plan lines: what in the prompt makes the label droppable?
- L12 MED — seam label leak: a reveal window answered `BLOCKS: …` then `PUT: 1` (152v123 seq 25; parser latched the right line but the model believed it was in a blockers window). Check the reveal prompt's tail/label cue.
- L13 MED — the log narrates a modal spell as "resolved and went to your graveyard" BEFORE its mode choice is asked (deck146: 130 seq 20).
- L14 MED — cast menu promises a manland animation "is asked at its own window" while it is unpayable (silently false); the rung-ceiling explanation prints only on the affordable row (deck146).
- L15 MED — Tomb of Annihilation modal rows render with zero annotation (deck162; the model invented the missing row's cost).
- L16 MED — false `[DRAW PRICE]` on a CAST row (deck130: 162 seq 17; no life was lost).
- L17 MED — hold row's "you give up no cast" is misleading on cast menus; the batch `repeated N times` row appears ~5 windows after the repeat annotation (deck123). Hold row bounded to genuine P8 menus is a GUIDE edit (staged) — the render should not offer hold on a Casting decision at all: check.
- L18 MED — CRACK-BACK floor anchored the model at 9 when 14 was incoming; decline-vs-hold rows near-synonymous (15 misses of 75); 2 parser refusals with unambiguous intent (deck125 — cite seqs from its review).
- L19 LOW — `W69-BJ F1` self-diagnostic fires on a legitimate row with two DISJOINT tap groups (29 times, 152v162/152v126); LOW-2 Idyllic Tutor one-window library lag (carried; payload stranding did NOT recur — downgraded); LOW-3 the answer ceilings are ~10x oversized (see R3).
- WATCH (owner-ruled surface; report only): consecutive PLAN lines identical 7.1% — the protocol asks "write it out again word for word" and the model rewrites 92.9% of windows; mechanisms keyed on "the plan still stands" (`repeatAskAnswerStands`) compare a string that changes nearly every window. 239/245 deck130 traces end in redundant format-ceremony (reasoning cost, not a defect).

## G — GUIDES
Five EDIT verdicts staged in wave70/deck<N>/strategy.txt (146 19,927 B; 125 19,941; 162 19,963; 123 19,936; 130 19,998) — each seat's review.md carries the before→after and the paying seq. Orchestrator verifies ≤20,000 B + Amendment-332 grep 0 + the link-time guide guard, then installs. No general or skill proposals were filed by any seat → no synthesis step this wave.

## P — PREDICTIONS (engine-seat §4)
BK 1 PASS-with-exception (4 retry records at max_tokens 400), 2 PASS, 3/5 UNTESTED, 4 PASS; BL 1-5 PASS; BM 1 half, 2/4/5 FAIL (all zero — mechanisms dead), 3 PASS; BN 1-4 PASS, 5 UNTESTED. `plan_steps_done` absent from every record (BN's F10 advance was unmeasurable) → add it to the record (L10's sibling).
