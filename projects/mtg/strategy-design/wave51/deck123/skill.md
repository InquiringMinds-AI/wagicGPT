# deck123 - skill proposals (wave 51)

S1. A GUIDE'S FIRST CORPUS UNDER A NEW ROW TAG IS A TAG-COVERAGE CHECK, NOT A RULE CHECK. Lane E
shipped `[already owned:` (54 renders at 123v125) and `[from exile]` already existed; the pilot read
the first correctly 5 of 7 times and the second 0 of 4 (123v152 seq 20-27, "Intruder Alarm is exiled,
I cannot cast it" under the row). Proposal: the #128/#139 grep gains a third column - for every
bracket tag the emitter can print on a row family the guide's cards appear in, the guide names the
tag once with its meaning; a tag the pilot narrated FALSELY in a corpus (as here) is a first-failure
prose fix, not a third-failure shrink. Evidence: 123v152 seq 20/24/26/27 (A51-3).

S2. A MANA SOURCE THAT IS NOT MANA UNTIL ACTIVATED IS TAUGHT AS A COST, NOT A CHOICE. Marsh Flats sat
uncracked 6 windows (123v130) and whole games in wave 50 because the guide framed the fetch as
"WHICH dual" (a choice) while the pilot's plan counted the Flats as a land it already had. Rule
candidate: for fetch lands, treasure, mana rocks with a tap ability the engine offers as a priority
row, the guide's first sentence is the prompt's own count ("Mana available does not count it") and
the second is "the first window it is listed". The colour choice comes third. Evidence A51-1.

S3. THE OWN-UPKEEP LOOP RULE WAS A FLOOR IN ODDS CLOTHING. "Worst window of all" was true against the
wave-44 sweeper and false in 4 of 4 own-turn chains since (all won at the L + C + 3 stop). #118's
ceiling applied: the rule is now conditioned on the opponent's log showing a sweeper. General form:
a timing rule written from one catastrophic game names the card class that made it catastrophic, so
the pilot can see when the class is absent. Evidence A51-13.

S4 (confirms #138, no change). Tutor-with-Alarm-owned: the reply at 123v125 seq 105 wrote the
guide's dictated sentence verbatim and cast the Tutor in the same reply; the tag was on the row.
The guide is at one restatement now (A51-6); the residual is a render/protocol lever (seat H2 is
the index-conflict half). Counted here as the fourth corpus for the docket.

S5. CROSS-GUIDE: deck130's D50-6 GANG-BLOCK rule ported to deck123 by its tag string (A51-10, #144).
The `GANG BLOCK: any 2 of yours together deal N` header is pool-wide; every guide with 2+ bodies
should carry the "life minus deals N <= 6 -> all counted blockers" line.
