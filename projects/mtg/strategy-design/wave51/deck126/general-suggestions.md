# deck126 - general (core-prompt / render) suggestions, layer-routed (wave 51)

G1 (engine, HIGH - seat engine #1): the payment planner taps every Overgrown Battlement for a
cost one covers and the `{paying this taps:}` forecast disagrees with the receipt (`vs152` seq 18 ->
19: forecast 2, receipt 3, 13 {g} floated; seq 26 -> 27: three walls tapped into a combat that took
18). Engine item, not prompt text - listed here only to route it.

G2 (render, MED - seat engine #2): `[from exile]` cast rows carry the cause and the tax once:
`[from exile - your card, exiled by their Elite Spellbinder; castable from there at {2} more]`.
`vs152` seq 26 (248 s: "Option 2 is illegal ... a distractor"), seq 19, seq 30 (the win).

G3 (render, MED - seat engine #3): the Tutor row's `[already owned: <half> on your battlefield -
this finds only an enchantment card]` names the wrong half; print the one it can still find:
`[finds: Sanguine Bond - not on your battlefield or hand]`, and `[both halves already on your
battlefield]` when they are. `vs125` seq 21, `vs152` seq 31.

G4 (render, LOW - seat engine #5): rows paid from a floating pool print `{leaves 0 untapped
sources and N floating}` so the guides' `leaves N` keys still land (`vs152` seq 12, 19).

G5 (parser, LOW - seat engine #7): the off-menu-hand-card `[RE-ASK]` prints the pip that cannot
be paid (`vs146` seq 23) - the wave-50 proposal, still open; the re-ask itself works.

G6 (frontend/D31, INFO - seat engine #8): the Upkeep Staff-ping window's `{this hits YOUR
permanent}` rows (five of 33 at `vs123` seq 23) are never taken (0/8 wave 50, 0/5 here) and could
be collapsed to one line.
