# deck146 - skill proposals (wave 51)

S-1. **A MULLIGAN CLAUSE ABOUT ANYTHING BUT THE LAND COUNT IS A CHAIN SEED, AND THE #131 TWELVE-HAND
CHECK DOES NOT CATCH IT BECAUSE THE FAILURE IS THE CARRY, NOT THE LOOK.** deck146 STEP 2 carried "or
NO source of one of your colors when the header shows every spell needs it" - correctly scoped to
the untouched seven and (keeping 6), and #131's per-look check agreed with it. The pilot lifted the
clause's words into its PLAN at look one (`146 vs125` s1: "find a hand with at least one white
mana source"), quoted the clause by name at look two (s2: "a 'no source of one of your colors'
mulligan condition"), and the carried plan then decided looks three, four and five against STEP
1's own "keep with even one land" - a two-card keep, first spell turn 11, -14/45 at T80. Rule: a
mulligan section names ONE property the pilot may ship on (the engine's land count / coverage
line) and states every other property as a bottom-ask question; where a guide keeps a second
shippable property, the #131 check is extended by one step - write the look-N plan the clause
would produce and check that plan against the look-N+1 hand. deck152's RULE -1 ("Not a colour,
not a two-drop, not a curve: those are bottom-ask questions") plus its "THE CARRIED PLAN IS NOT
EVIDENCE ABOUT THIS HAND" paragraph is the form; #144's cross-guide grep found deck146 the holdout
of both and it is closed this wave (146-M).

S-2. **A CHECK LIST WITH AN ABSOLUTE TAG OUTSIDE IT LOSES THE TAG.** deck146's Hive section has
CHECK 0-3 as a numbered list and the `[repeat: ...]` sentence as a separate paragraph ("read it
before you read anything else"). Three replies this corpus (`146 vs125` s91, s112, s262) ran CHECK
0, 1, 2, 3 in order, in writing, and never mentioned the tag on the row they took. The executor
runs the list; prose beside the list is not in the sequence (#133/#138). Rule: an absolute keyed to
a printed tag goes INSIDE the numbered procedure as its first step ("CHECK -1: the row prints
`[repeat:` -> PASS, stop"), not beside it. Left unapplied to deck146 this wave because #138 routes
a third-corpus identical failure to the render (seat E-2) and forbids the fourth restatement; the
rule is for the next guide that adds a check list.

S-3. **THE PUT-vs-PLAN METRIC (#143) NEEDS A SECOND COLUMN: PUT-vs-EXECUTED.** At `162 vs146` s17
the reply's PUT line named the right card ("PUT: 3 (Forced Fruition)") and the engine discarded a
different one (Liliana's Caress, #2). #143 scores the pilot's list against its plan and would have
scored this window a pass; the executed list (`chosen_text`) diverged from the emitted one. Rule:
for every list-answer ask the reviewer compares the reply's numbers against the log's
`chosen_text` (0 mismatches is the pass) before scoring the plan - a mismatch is an engine item,
never a guide finding. (Evidence at deck162; proposed here because the metric is #143's.)
