# deck130 - core-prompt / render proposals (wave 51), layer-routed

R1 (render, cast rows). When the payment plan for a cast row must tap a source whose coloured mana
damages its controller (Talisman of Impulse), print it on the row: `{pays {r} with Talisman of
Impulse: you take 1 - life 1 -> 0, LETHAL}`. 130v126 seq 85 (`{leaves 0 of your 1 untapped mana
source untapped}`, receipt `Paid {r} for Spark Spray with Talisman of Impulse`, gameend 0/24). The
CAUTION line exists on the frame; the row that kills is silent. Seat item H3.

R2 (render, attacker tags). The converter clause `(... their converter takes 2 off you)` renders
per attacker; a per-window summary line above the A-rows - `WALL CHECK: N of your attackers meet a
lifegain blocker; blocking all of them costs you 2N life` - is the one number the pilot never
summed (130v126 seq 20, 28). Guide half D51-1.

R3 (core prompt / orchestrator). 130v123 seq 37: 592 s; 130v125 seq 25: 652 s; 130v126 seq 15:
602 s - three of the corpus's longest replies at this seat, all on windows with 9-14 rows of
`Deal 2 damage with Siege-Gang Commander targeting <own permanent>` self-target rows. Collapsing
own-permanent target rows (R2 in deck123's file for the equip family) would shrink these windows
by 5-7 rows each.

R4 (render, X menu). `X = 0 {X pricing: X=0 - this cast does NOTHING ...}` is the only exit on the X
menu once `Cast Card Normally` is answered (130v123 seq 40). Ask: carry the `Choose an option`
menu's `Decline - do nothing` row onto the X menu as well, so a pilot who reads the pricing late has
a way back that keeps the card.
