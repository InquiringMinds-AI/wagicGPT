# Wave-48 seat: deck125 (3/6) + deck126 (4/6)

Corpus `matchups-20260827-072004` - FIFTH fair-hand corpus, first on the E1 parser fix (5ce8f8a5a)
and lanes N/O/P/Q; binary master eccf8284c. Guides = wave-47 reviewer revisions (4b4ade1bc) +
boundary pass (8b25c325a). deck125 **406** decisions / 6 games, deck126 **174** / 6 games (580,
excluding gamestart/gameend). The deck123-vs-deck162 dead-loop game is excluded from every
corpus-wide number below (per the brief).

**Health (corpus-wide, 41 seat logs, 1,729 decisions).** Fallbacks **5 (0.29%)**, inside the
0.10-0.31% baseline: 4 `unparsed_reply` (none on my seats) + 1 `all_assignments_illegal`
(**deck126 `vs146` seq 24**, mine - F9). `commit_retracted` **0**; `answer_replaced` **21** (E1
replacements; my seats 4 - see E1 below and HIGH #1). Latency deck125 p50 10.0 s / p90 22 s / max
75.8 s; deck126 p50 22 s / p90 108 s / max **514.7 s** (`vs130` seq 11, a 5,651-char reply that
parsed). Corpus max 801.9 s (deck152 seat seq 6, parsed) - the ~800 s ceiling from wave 47 is
back, but this time the reply was complete. Prompt bytes: deck125 max 25,716 (`vs146` seq 74);
deck126 max **61,240** (`vs123` seq 15, blockers, 257 attackers - the brief's known item; my
measurement of that record is 61,240 chars = bytes, not 135 KB; the identical Pride Guardian
B-rows sit at B1/B3 as the brief says). Every other prompt on both seats is under 20 KB.

Every card fact below is read off `projects/mtg/bin/Res/sets/primitives/mtg.txt` /
`planeswalkers.txt` with `/usr/bin/grep`; every render string is quoted from the corpus.

**Results.** deck125 3W-3L: won vs146 (t36), vs130 (t54), vs123 (t33); lost vs152 (t18), vs126
(t25), vs162 (t29). deck126 **4W-2L**: won vs125 (t25), vs146 (t20), vs130 (t18), vs152 (t15);
lost vs162 (t12), vs123 (t11). Counts against n=6; deck126's 1-5 -> 4-2 is not attributed to
the wave-47 edits (two of its four wins were decided by the opponent's mull-to-4 and by a loop
the guide does not touch).

## NEW-BASELINE record (mulligans)

| seat | offers | mulls taken | kept-hand land counts | bottoming |
|---|---|---|---|---|
| deck125 | 6 | **5** (2 vs152, **3 vs126**) | 3 (after 2 mulls), 5, 4, **3 (after 3 mulls, then bottomed to 2)**, 5, 3 | 2 windows: vs152 seq 4 (both Sphinx's Revelations - rule 2 duplicate, guide-legal); **vs126 seq 5 (Island, Fall of the Gavel, Essence Scatter - two rule breaks)** |
| deck126 | 6 | 0 | 5, 3, **1**, 2, 4, 3 | 0 |

**DECISION (mine, under the owner's standing ruling that deck strategy is the reviewer's call):
deck125 `vs126` seq 1-5 is a repeated mulligan that reached a 4-card keep, and the guide's
mulligan section is what let it happen - edited (W13).** The chain: seq 1 `0 lands, 7 spells` ->
mulligan (right); seq 2 `4 lands, 3 spells`, coverage line names Dream Fracture and Supreme
Verdict, row `Mulligan (a keep after this one would keep 5 cards)` -> mulligan (the KEEP clause
covered this hand; the STEP 1 text keyed on "(keeping 5)", which the row does not print); seq 3 `2
lands, 5 spells`, no coverage, at the would-keep-4 row -> mulligan; seq 4 kept `3 lands, 4 spells`
at keeping 4; seq 5 bottomed Island, Fall of the Gavel and Essence Scatter, keeping Glacial
Fortress, Plains, Elixir, Path. At that ask the guide's three floors (three lands, last counter,
last Path) could not all be satisfied with three cards to send - an unsatisfiable rule set, and
the executor chose freely. Every reply was the bare plan line. Per the owner's ruling that guide
text must not impose mulligan floors or forced keeps as code-like constraints, W13 replaces STEP
1/STEP 2 and the NEVER-bottom lines with odds-based guidance (the price row as printed, three
reasoned branches, a bottoming PREFERENCE order plus the shape of the hand to keep: its lands and
one answer) that is satisfiable at any bottom count. The game was the seat's vs126 loss. No
mull-to-zero anywhere. deck126 kept a 1-land seven at `vs130` seq 1 (no ship rule in
its guide; won at t18) - recorded, not patched.

---

# deck125 - Revelation Fracture (UW draw-go). 3W-3L

## Prediction set A - wave-47 guide edits (W30-W37)

**W30 (X above 3: 0; X = 3 whenever the menu reaches 3) - PASS 8/8.** Menu maxima 5, 11, 1, 11,
6, 2, 4, 13 -> announced 3, 3, 1, 3, 3, 2, 3, 3. Regression detector satisfied; not edited.

**W31 (sweeper cast at a header second number of 0: 0) - FAIL 4/8 casts, 95 sweeper windows.**
`vs130` seq 45 (t28, life 13, opposing board with NO creature), `vs130` seq 72 (t40, same shape),
`vs126` seq 46 (t22, `5 are creatures, 0 of them without a restriction against attacking` - five
walls), `vs162` seq 21 (t18, main phase 2, after seq 20 in main phase 1 of the same turn was
correctly declined). The four correct casts (`vs152` 29, `vs146` 21/27/54) all read 2 or 3. The
header discriminates 8/8; the rule is obeyed for many windows then broken (each cast follows
several correct declines; in three of four the sweeper was the only cast row). Wave 47 was 4/13.
Fifth wave. Edited W9; render proposal G2 (the row itself should print what it would destroy).

**W32 (Lightmine Field at that number below 3: 0) - FAIL 1/44.** `vs123` seq 40 (t16, 0, `leaves
5`). Was 3/3. Same edit.

**W33 (own-turn spend at `leaves 4` or less / taps-out with a counter in hand: 0; report the Staff
sub-lane, cumulative n >= 10) - FAIL, 10 identifiable (was 12); Staff 2/6 (was 6/7; cumulative
13, above the n=10 floor).** Staff: `vs162` seq 27 `leaves 2` (Essence Scatter, Cancel, Fall of
the Gavel in hand) and `vs130` seq 47 `leaves 0` at 11 life (Gavel). The other four Staff casts
were at `leaves 1` with NO counter in hand (`vs146` 12 - legal), 9, 6, 6. Elixir casts at {1}
below the line: `vs152` 14, `vs126` 10 and 20, `vs123` 5, `vs162` 3 and 26 (six, none costly);
Elixir activations to 0-2 open with a counter in hand: `vs152` 26, `vs126` 22, `vs162` 29;
sweepers: `vs130` 45 `leaves 1`, `vs162` 21 `leaves 3` (both also W31 failures). The `vs162` seq
27 -> seq 29 pair is the wave's clearest consequence trace: no record exists between seq 29 (t20,
0 open) and seq 30 (t22 upkeep), and Underworld Dreams is on the line at seq 30 - it resolved on
t21 with no window offered because the seat could not pay for one. Edited W10 (gate into the plan
line; Elixir CAST exempted).

**W34 (Path taken in every window at life <= 12 with a real target; 0 Paths at 0/1 power or
[defender]) - UNTESTED on the first half, FAIL 2/7 on the second.** 44 Path windows; **none at
life 12 or below** (lowest 14, `vs130` seq 21 - Rorix Bladewing 6/5, Pathed, correct). Seven Paths
cast: Brutal Cathar 2/2, Rorix 6/5, Master of the Feast (text "At the beginning of"), Thraben
Doomsayer and Bloodline Keeper ("{T}:") - all correct - and **`Goblin (creature 1/1)` twice**
(`vs146` seq 9 and 31, life 20, `leaves 5` / `leaves 10`), a textless token that was the only
candidate. Edited W12 (one clause).

**W35 (Emrakul taken N/N) - PASS 2/2**, at `leaves 1` (`vs146` seq 71, then attacked at seq 78 for
the win) and `leaves 2` (`vs123` seq 80). W7 held.

**W36 (counter spent on a stack object tagged/printing Defender or power 0: 0; re-key to the tag if
it ships) - PASS 0/27, and the tag SHIPPED.** 27 counters cast; targets: 15 creatures of power 2+,
2 planeswalkers, Luminarch Aspirant 1/1 ("At the beginning of combat" - guide-correct), 6
non-creature spells, Dwarven Blastminer 1/1 ("{T}:"), Intruder Alarm. Five stack rows this corpus
carried `[defender]` (`vs126` seq 15, 30, 33, 37, 47) and none drew a counter. Edited W11 (rule
re-keyed to the tag; the "does not carry the tag" sentence deleted).

**W37 (land drop N/N) - PASS 84/84**, 0 `Play no land` / `Hold`. Not edited.

**W27 lineage (Revelation under a DRAW PUNISHERS line) - PASS 0/9.** `DRAW PUNISHERS on the
battlefield: theirs - Underworld Dreams...` rendered in 9 Revelation-bearing windows (`vs162` seq
32-46); none cast.

## The losses (deck125)

- **vs deck126 (t25) - the mulligan chain, then nothing to do.** After the 4-card keep the hand was
  Path, Path, Essence Scatter for the whole game; Sanguine Bond (t9, seq 18 - only Path offered,
  Scatter cannot target an enchantment) and Exquisite Blood (t13, seq 29 - same) resolved into
  windows with no legal counter. Twelve turns of correct `Cast nothing` against five walls; one
  W31 break at seq 46. The seat lost 30 -> 0 in one combat at t25 to a lifelink Vampire with both
  enchantments out. Nothing in-game to teach; the loss is the mulligan chain above (W13).
- **vs deck162 (t29) - Forced Fruition + Underworld Dreams, and the guide had no rule for it.**
  Draw punishers were answered on sight (seq 8 Cancel on Dreams, seq 10 Scatter on Fate Unraveler,
  seq 24 Cancel on Teferi's Puzzle Box, seq 38 Cancel on Unraveler, seq 51 Cancel on Dictate).
  Forced Fruition resolved on t13 with no window (4 open after the seq 13 Path at Master of the
  Feast - a guide-correct Path). From seq 22 every cast drew seven (hand 4 -> 11 -> 19 -> 25);
  from t21 Dreams made that 7 damage per spell. Seq 44 (life 15): Elixir cast - a guide-LEGAL spend
  ("activate at 15 or less") - drew 7, 15 -> 20 -> 13. Seq 51 (life 11): Cancel under two
  Fruitions and Dreams - drew 14 - dead. Two contributing misplays: seq 27 Staff at `leaves 2`
  (W33, the uncontested Dreams) and seq 21 Verdict into a 0 header (W31). New section W8.
- **vs deck152 (t18) - 5-card keep, five lands on turn 18, Bant beatdown.** Two mulligans
  (7 lands, then 0 lands - both guide-legal), correct bottoming, 20 -> 13 -> 3 across t12-t15
  against Intrepid Adversary and a countered Sigarda; Verdict at 3 life into `2, 2` (correct);
  died to Elite Spellbinder + Adversary at t18. No adjudicable rule failure beyond the Elixir
  cast at seq 14 (W33, harmless).

## Other counted behaviour (deck125)

- **Elixir library floor:** min library 5 (`vs162` seq 42, activated in that window; 1/1 at <= 16).
  Other games min 25-50. The `<= 16` rule fired once and correctly.
- **Own-hand count / X preview:** `Your hand (N cards):` on 406/406; X preview on every X menu.
- **`[DRAW PRICE:]` reached Dream Fracture** (`vs162` seq 38, under Underworld Dreams) - lane Q
  PASS on this seat's one draw-on-cast counter.
- **Counters at land destruction** (`vs130` seq 15, 39, 53 - Molten Rain, Lay Waste, Stone Rain):
  the guide is silent; the seat countered and won at t54 with 39 life. Left alone.

---

# deck126 - Sanguine Blood (WGB wall prison). 4W-2L

## Prediction set A - wave-47 guide edits (D29-D35)

**D29 (Tribute cast in the first window where the header reads `of which 1 is a creature`: >= 1/1;
taken N/N) - FAIL 0/2.** `vs146` seq 18 (t9, Silverquill Silencer 3/2 their only creature; cast
Sanguine Bond, reply: "Keep Tribute to Hunger in hand to remove the Silverquill Silencer if it
becomes the opponent's only creature"); `vs123` seq 9 (t6, **Thraben Doomsayer** their only
creature; cast Overgrown Battlement, reply: "Remove Thraben Doomsayer with Tribute to Hunger if it
becomes the only creature"). The second is the loss (below). 52 Tribute windows, 40 at a 0 header,
2 at exactly 1, the rest at 2+. F1 gave the rule a rank and the executor read "1" without
resolving it to "only, now". Edited F5.

**D30 (Tribute at `0 are creatures`: 0) - the ONE cast (`vs130` seq 25) is an ENGINE event, not a
guide failure.** The reply's first coded line is `CHOICE: 2 (Cast nothing right now)` with "Rule
#4 Zero Stop" cited; its re-answer is `CHOICE: 1 (Cast Sanguine Bond)`, a card not on the 2-row
menu; E1 executed index 1 = Tribute. The zero stop held in the model's own text 40/40. HIGH #1.

**D31 (one blocker on a menace A-line: 0; whole board on one attacker while another blockable one
goes through: 0) - FAIL 1/8 on the first half (`vs146` seq 24, `B1:A2, B2:A3` one Wall on each of
two menace Spiders -> `all_assignments_illegal`), PASS 0/8 on the second.** Three later menace
windows were right: seq 29 (`B2:A2, B3:A2` gang block on the Spider), seq 36 (three on it), seq 49
(two Walls on the menace Hive, three bodies on the Silencer, game won). Edited F9 (the two-menace
case).

**D32 (`BLOCKS: none` or a benched blocker at `you would be at 8` or less: 0) - UNTESTED.** No
blockers window on this seat read 8 or less except `vs162` seq 16 (`you would be at -1`, LETHAL),
where the only blocker blocked. `BLOCKS: none` 0/8. Inverse shape seen once: `vs146` seq 11 (t6,
life 20, no enchantment) Pride Guardian on a `your blocker dies` line - STEP 1 says bench; not
re-worded (the STEP 1 anecdote is that shape).

**D33 (Lantern at `{B} 2`+ or with one out: 0) - 3/3 casts at `{B}` 2, 3, 2; 0 second Lanterns in
14 windows - and reading the menus overturns the RULE, not the plays.** `vs130` seq 9 and `vs162`
seq 9: the only other row was Tribute at a 0 header; `vs152` seq 11: Sorin from exile at
{4}{b}{w}. Two of the three games were won. Edited F6 (the {B} clause dropped; Lantern = entry 7
when nothing above it is on the menu).

**D34 (unpaid `Cast nothing`: <= 4, N/M) - PASS 4/21.** `vs125` seq 39 and 41 (Pride Guardian, both
enchantments out, on the turn the game was won), `vs130` seq 18 (Sorin, would tap out, one turn
before the combo), `vs152` seq 7 (Battlement at 2 mana). The other 17 were Tribute at a 0 header
(entry 8 forbids), a second Exquisite Blood with one already out, or Tutor with both pieces out.

**D35 (mana-only skips per game; all-mana menus reaching the model 0) - PASS.** 0 / 0 / 0 / 103
(`vs152`) / 0 / 835 (`vs123`); leaks 0 (the single Battlement-bearing menu, `vs152` seq 20, also
carried Staff pings and the seat pinged). Battlement taps taken 0/1. The 835 is the
Doomsayer/Alarm loop game, lane N's class, 10x down from 8,610.

## The losses (deck126)

- **vs deck123 (t11, -226 life) - F5 is the whole game.** `vs123` seq 9 (t6): Thraben Doomsayer
  their only creature, Tribute on the menu, Battlement cast instead; seq 10 (t8): Intruder Alarm
  and a Human on the line; seq 12 (t9): 55 creatures; seq 15 (t11): 257 attackers, 61,240-byte
  blockers prompt, three blocks, dead. Seq 13 (t10) cast Tribute at 55 creatures expecting to
  remove Bloodline Keeper (the reply says so) - the edict text on the row says "of their choice";
  the guide says it too; a 1/1 Human died. STRATEGY at seq 9; the render was complete (header `1
  is a creature`, Doomsayer's `{T}: Put a 1/1 white Human creature token` on the line).
- **vs deck162 (t12) - the draw lock, with the right plays in it.** Tutor -> Bond (t4), Bond resolved
  t8 into Fate Unraveler + Shield Sphere (header `2 are creatures, 1 of them without a
  restriction`: a Tribute would have taken the Sphere), Ob Nixilis t10, life 17 -> 11 -> 8 -> 2 ->
  0 through draws. One priced draw: `vs162` seq 13 cast Wall of Omens with `[DRAW PRICE: ...]` on
  its row at 11 life. No Tribute window at exactly 1 creature ever arose; no guide content.

## Other counted behaviour (deck126)

- **Attack rule.** 5 attackers windows: 3 correct sends (no blocker / win button), 2 sends against
  the guide's N >= 2 / 1-vs-1-dies stops (`vs146` seq 35, 41), both unpunished (unblocked). The
  win-button send at `vs130` seq 32 into four untapped blockers won the game - the guide's "only
  if their side is empty" was wrong; F7.
- **Staff ping with Bond alone:** `vs146` seq 43 passed the ping at opponent 7 after reasoning "the
  ping does nothing without Exquisite Blood"; F8.
- **Multi-CHOICE replies:** 9/174 (deck125 0/406) - see MED #3.

---

# Prediction set B - engine lanes (where my seats had windows)

| lane | verdict | evidence |
|---|---|---|
| **E1** 5ce8f8a5a - retraction-to-Baka on replies carrying a final coded CHOICE: 0/N | **PASS on the prediction; FAIL on a shape it did not predict (HIGH #1)** | `commit_retracted` 0 corpus-wide; 21 `answer_replaced`, 4 on my seats (`deck126 vs146` 22, 43, 46; `vs130` 25). Three took the model's real final answer (seq 22 Tribute -> Wall, seq 43 ping -> pass, seq 46 ping -> Sorin +1). **`vs130` seq 25 took a re-answer whose NAME was not on the menu** and executed a different card at that index. The corpus's 4 `unparsed_reply` fallbacks are all a single out-of-range `CHOICE: n (<card not on the menu>)` - not retractions. |
| **N** 7a858904d - pass row on single-option windows; repeat row; no turn > 500 windows; 21/21 natural | **PASS on the render (139/139 single-option priority windows carry `0. Pass priority (take no action this window)`); PASS on the window count (max priority records in one turn corpus-wide: 9, `deck123 vs126` t10); the repeat row was TAKEN on another seat (`deck123 vs162` seq 28 `..., repeated N times, then stop, N=45`, seq 30 `x13`) - reported, not adjudicated here; 20/21 natural per the brief (the dead-loop game is this lane's own failure and is owned by the engine seat)** | my seats: 0 repeat rows offered (no self-tap loop ability), 33 `[repeat:` tags corpus-wide, none on my seats' own rows. |
| **O** 96a07e864 - no prompt > 60 KB; blockers/attackers p90 <= 60 s | **FAIL by one prompt (61,240 B, `deck126 vs123` seq 15 - the brief's known item); PASS on latency (blockers p90 57.9 s, attackers p90 56.4 s corpus-wide; my seats' blockers max 62.1 s)** | 9 prompts carry ranged A-rows; battlefield grouping seen everywhere (`Human #1-#255 (1/1) ...`, `Perimeter Captain #1-#3 ... x3`). |
| **P** 9b19fbfe6 - header attacking count below the `[tapped - attacking]` row count 0/N; stack rows carry the keyword set; `most expensive to attack into` | **PASS 0/26 on the header; PASS on stack tags (`[defender]` on 5 counter-target rows, `[flying]` / `[lifelink]` / `[vigilance]` on the rest - closes wave-47 MED #2); `most expensive` rendered once corpus-wide, not on my seats** | `deck126 vs123` seq 15 header `257 of them are attacking right now` over 257 `[tapped - attacking]`. |
| **Q** 7246c4a95 - `A choice is required` 0; `target controller` 0; `[DRAW PRICE:]` on controller-draw rows under a punisher; ANNOUNCE_X carries no plan block; stale-plan X 0 | **PASS on all measured: 0 / 0 / DRAW PRICE on Wall of Omens (`deck126 vs162` seq 11, 13) and Dream Fracture (`deck125 vs162` seq 38) rows, 11 tagged prompts corpus-wide / 8/8 ANNOUNCE_X prompts on deck125 carry no plan block and X = 3 or the menu max every time; `auto=choice` menu named its spell (`deck126 vs125` seq 25 `Exquisite Blood [enchantment] [your reveal]`). Mid-word `{card text:}` cuts NOT measured (no detector built).** | |
| Fair hands (carried) | **PASS** | kept-hand land counts 1-5 across 12 keeps; the two mulligan chains are real hands. |

---

# Engine / render items (ranked, with repro)

Corpus root: `~/.Wagic/ai/gpt/selfplay-runs/matchups-20260827-072004/`

**HIGH - #1: the E1 replacement executes a re-answer whose parenthetical NAME does not match the
option at that index.**
`1787833231-ai_baka_deck126-0x55ee9173e000-vs-ai_baka_deck130.jsonl` **seq 25** (t14, main phase
2, 2 options: `1. Cast Tribute to Hunger ... 2. Cast nothing right now`). Reply line 1: `CHOICE: 2
(Cast nothing right now)` ("Rule #4 Zero Stop"); 2,563 chars of reasoning; last coded line:
`CHOICE: 1 (Cast Sanguine Bond)`. `answer_replaced: true`, `choice: 1`, `chosen_text: Cast Tribute
to Hunger ... casting this taps you out` - cast at a `0 are creatures` header: nothing sacrificed,
tapped out. The short name is requested exactly so the index can be checked; on a mismatch the
replacement should be refused (keep the earlier coded answer whose name matches, else fallback),
and the event counted. The same name check re-classifies the corpus's four `unparsed_reply`
fallbacks - every one is `CHOICE: n (<card NOT on the menu>)` with n out of range (Acererak,
Rorix Bladewing, Fate Unraveler, "Attack with all creatures") - as "named a row that was not
offered", a countable model-belief failure distinct from a parse miss.

**HIGH - #2 (carried, cite only): the unbounded legal loop** - `deck123` vs `deck126`
(`1787833243-ai_baka_deck123-0x56097312ac50-vs-ai_baka_deck126.jsonl`) reached 257 attackers by
t11 under lane N (9 priority records in that turn, down from 1,550); the engine seat owns it. From
my seat: `vs123` seq 15, 61,240 bytes, the known B1/B3 duplicate.

**MED - #3: post-answer overrun is now the reasoning channel, unmeasured, and it is where E1 looks.**
`1787833230-ai_baka_deck126-0x5622b00b9650-vs-ai_baka_deck146.jsonl` seq 22 (`post_answer_overrun`
4,648, two CHOICE lines, 235.6 s), seq 43 (4,889, three CHOICE lines), seq 46 (4,779, two);
`...deck126...vs-ai_baka_deck130.jsonl` seq 11 (514.7 s, 5,651 chars). 9/174 deck126 replies carry
two or more coded lines; deck125 0/406. Proposal in `deck126/general-suggestions.md` G2 (a
`multi_choice_replies` counter, or a bounded pre-CHOICE reasoning block so there is one coded line).

**MED - #4: a cast row under an opposing "whenever an opponent casts a spell, that player draws N"
permanent prints no draw price.** `1787833227-ai_baka_deck125-0x55d9ed7b1350-vs-ai_baka_deck162.jsonl`
**seq 44** (t26, life 15): `Cast Elixir of Immortality {1} {leaves 8 of your 9 ...}` under `Forced
Fruition ... "Whenever an opponent casts a spell, that player draws seven cards."` + Underworld
Dreams; narration: cast -> seven `You drew` lines -> seven `Underworld Dreams dealt 1 damage`
lines. **seq 51** (life 11): `Cast Cancel ... {leaves 10 of your 13 ...}` under `Forced Fruition
#1-#2 x2` + Dreams -> 14 draws -> dead. Lane Q's `[DRAW PRICE:]` keys on the cast card's own
draw (`scriptSelfDrawCount`) and does reach Dream Fracture (seq 38) - the opposing-permanent
cast trigger is the missing source. `deck125/general-suggestions.md` G1; guide W8 meanwhile.

**MED - #5: sweeper rows carry no "what it destroys" preview; five waves of guide prose have not
closed a 4/8 cast rate into empty or all-defender boards.** `1787833215-ai_baka_deck125-0x55895edf7530-vs-ai_baka_deck130.jsonl`
seq 45 (t28, header `0 permanents ... 0 are creatures`, `Cast Supreme Verdict ... leaves 1`
taken); seq 72; `...vs-ai_baka_deck126.jsonl` seq 46; `...vs-ai_baka_deck162.jsonl` seq 21.
`deck125/general-suggestions.md` G2 (`{right now: destroys 0 of their creatures ...}`).

**LOW - #6: `{leaves N ...}` still absent on activated-ability rows** (carried): `Life with Elixir of
Immortality [cost: {2}, Tap]` at `...deck125...vs-ai_baka_deck162.jsonl` seq 29 took the seat to 0
open before the uncontested Underworld Dreams.

**INFO - stack rows carry keyword tags (lane P): wave-47 MED #2 CLOSED.** `1787833221-ai_baka_deck125-0x555f0b36e390-vs-ai_baka_deck126.jsonl`
seq 15: `can target on the stack: Perimeter Captain {w} (creature 0/4) [defender] {target text:
...}`; 0 counters spent on tagged walls in 5 windows.

**INFO - the ~800 s ceiling recurred (801.9 s, deck152 seat seq 6) with a COMPLETE 4,760-char
reply**, so it is not a truncation; whatever it is, it did not cost a decision this corpus.

**INFO - target menus (wave-47 MED #4):** largest Staff-target menu this corpus 11 rows
(`...deck126...vs-ai_baka_deck146.jsonl` seq 34); the 431-row case did not recur (its game is the
excluded loop).

---

# Rotation evidence

- **deck125 - KEEP IN ROTATION.** 3-3, both guide-content losses now have a rule: one new section
  (W8, a pool card the guide never named) and the fifth-wave tap-out gate moved into the plan
  line (W10). Ceilings hold: X 8/8, land drop 84/84, Emrakul 2/2, walls-on-the-stack 0/5, draw
  stop 0/9. The sweeper-at-0 rate (4/8, five waves) is now argued to the render (G2) with the
  guide edit as the side quest. Not a freeze candidate while W31/W33 are open.
- **deck126 - KEEP IN ROTATION.** 4-2. The one loss with guide content is F1's shape again (0/2,
  now with the executor's exact deferral sentence in the rule). Three wave-47 edits are at
  ceiling or improved (F2 zero stop 40/40 in the model's own text, F3 gang block right 3 of 4, F4
  untested), and one wave-47 rule was wrong and is reversed on evidence (F6). Re-test F5-F9
  before any freeze talk.

# Lategame specimen candidate

Primary: `1787833243-ai_baka_deck126-0x560971e0cf50-vs-ai_baka_deck123.jsonl` **seq 15** (t11,
61,240 bytes, 3 blockers vs 257 attackers, the only prompt over 60 KB this corpus; the brief's
known B-row item). Alternate (ordinary lategame): `1787833211-ai_baka_deck125-0x5561d7168560-vs-ai_baka_deck146.jsonl`
**seq 74** (t36, 25,716 bytes, 10-option upkeep priority window with two Staffs, an Emrakul on the
battlefield, and the win two records later).

# Falsifiable predictions for wave 49

deck125:
- **W38** - a spell cast while `Forced Fruition` and a `Whenever an opponent draws` permanent are
  both on the opponent battlefield line and 7 x (Fruitions) x (punishers) >= life - 1: **0**;
  own-turn casts (Elixir, Staff, sweeper, Revelation) under that pair: **0**. Report windows.
- **W39** - Supreme Verdict / Final Judgment / Lightmine Field cast at a header second number of 0:
  **0** (was 4/8 + 1/44). Report casts and windows; if G2 ships, adjudicate by the row's `{right
  now: destroys 0 ...}` string.
- **W40** - Staff of Nin cast from a row reading `leaves 4` or less / taps-out with a counter in
  hand: **0** (was 2/6); the plan line in replies carries "leaves 5 or more" **N/N**.
- **W41** - Elixir CAST windows below `leaves 5`: taken (no longer a violation); Elixir ACTIVATION to
  `Mana available: 2` or less with a counter in hand: **0** (was 3).
- **W42** - counter spent on a stack row tagged `[defender]` or printing power 0: **0** (was 0/5).
- **W43** - Path at a textless 1-power token above 12 life: **0** (was 2); Path taken in every
  window at life <= 12 with a power-2+ untagged candidate: **N/N** (UNTESTED twice; report).
- **W44** - X = 3 whenever the menu reaches 3: **N/N**; land drop **N/N**. Regression detectors.
- **W45** - a mulligan taken on a hand of 2-5 lands whose coverage line names a card: **0**; a keep
  after a bottoming ask that holds no counterspell and no Path while one was in the numbered list:
  **0**; any chain reaching a 4-card keep: report (was 1).

deck126:
- **D36** - Tribute cast in EVERY window where the header reads `of which 1 is a creature` and
  Tribute is on the menu: **N/N** (was 0/5 then 0/2); zero replies containing "if it becomes" about
  a creature the header counts as 1.
- **D37** - Tribute at `0 are creatures`: **0** by the model's own last coded line (was 0/40 by text,
  1 by E1).
- **D38** - Chromatic Lantern cast when a row from Rule #7 entries 1-6 is on the same menu, or with
  a Lantern on the battlefield line: **0**; Lantern taken when it is the only cast row and none is
  out: **N/N**.
- **D39** - with both enchantments on the battlefield, every offered Vampire attacks: **N/N**.
- **D40** - Staff ping at the opponent taken in every upkeep window it is offered with Sanguine Bond
  out: **N/N** (was 2/4 in `vs146`).
- **D41** - one blocker alone assigned to a menace A-line: **0** (was 1/8, a fallback); two menace
  attackers answered one-each: **0**.
- **D42** - `mana_only_windows_skipped` per game and all-mana leaks (**0**).

# What I did NOT check

- I did not run wagic, touch `bin/Res`, `src/` or git, or read `game-*.stderr`; JSONL +
  `corpus-results.tsv` only. All guide work is under `wave48/`.
- Lane Q's mid-word `{card text:}` cut prediction - no detector built; UNTESTED here.
- Lane N's `[repeat:]` tag reconcilability and the repeat row's take-rate - no window on my seats
  (no self-tap loop ability in either deck); the deck123 take is reported from that seat's log
  only, not adjudicated.
- Lane O's ranged-collapse mixed-body interleave watch - no attackers window on my seats was wide
  enough (max 1 attacker offered).
- Whether the blocked Vampire at `vs130` seq 32 was in fact blocked (the game ended in that
  combat; the win is what F7 rests on, and lifelink-on-blocker damage is the engine's standard
  lifelink, which I did not step).
- The E1 code path itself - HIGH #1 is adjudicated from the record (reply lines, `answer_replaced`,
  `choice`, `chosen_text`, the 2-row `options_text`), not from `AIPlayerGPT.cpp`.
- **Primitives verified by `/usr/bin/grep` this wave:** Tribute to Hunger, Sanguine Bond, Exquisite
  Blood, Chromatic Lantern, Forced Fruition (`@movedTo(*[-land]|opponentstack):draw:7`), Underworld
  Dreams, Elixir of Immortality, Staff of Nin, Supreme Verdict, Lightmine Field, Perimeter Captain,
  Pride Guardian, Overgrown Battlement, Idyllic Tutor, Sorin, Lord of Innistrad (token has
  lifelink), Path to Exile, Sphinx's Revelation, Wall of Omens, Dictate of Kruphix, Master of the
  Feast. Quoted only from the engine's own rendered strings: Thraben Doomsayer, Bloodline Keeper,
  Intruder Alarm, Fate Unraveler, Ob Nixilis, Teferi's Puzzle Box, Silverquill Silencer, Siege-Gang
  Commander, Rorix Bladewing, Brutal Cathar, Sigarda, Elite Spellbinder, Intrepid Adversary,
  Luminarch Aspirant, Shield Sphere. Nothing cross-checked against Scryfall.
