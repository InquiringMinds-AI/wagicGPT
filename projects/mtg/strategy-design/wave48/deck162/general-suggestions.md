# deck162 - core-prompt / render proposals (wave 48), layer-routed

Engine items are in the seat file (E-6 off-menu number at `vs123` s10). These are the render
halves; most of this seat's lanes PASSED and are recorded so they are not re-opened.

G-1 (render, pairs with 162-A). **A draw-engine cast row could carry the punisher count the way
`[DRAW PRICE:]` carries the draw cost.** `Cast Howling Mine {2} ...` / `Cast Master of the Feast
...` print no fact about what the card does on THIS board; the guide's Rule 2 derives it from the
battlefield line. If 162-A's plan-text metric fails in wave 49 the remedy is `{feeds: the
opponent draws N extra per turn; punishers on your battlefield: 0}` on the row - a true count
the engine already has (it prints `DRAW PUNISHERS ... theirs` for the other side). Filed early
per #86's routing half; not asked for yet.

G-2 (render, PASS / KEEP). Lane Q's `{right now: if you choose "the opponent": life -6, draws
...}` preview on Peer into the Abyss renders (`vs152` s17), and `target controller` is gone from
every label (0 occurrences corpus-wide). Do not re-open.

G-3 (render, PASS / KEEP). The GANG BLOCK clause (`- GANG BLOCK: any 2 of them together deal 4,
enough to kill this attacker; each result above is a LONE blocker only`) produced its first live
hold at this seat (`vs123` s17). Do not re-open.

G-4 (render, observation). Ob Nixilis's `-2` rows print `targeting <name> [opponent's
battlefield]` and the full card text, but not the target's own text; the guide now names the
token makers, and R25's `{right now: ...}` extension to target-dependent activations would carry
the target's `{card text:}` instead. No new ask.

G-5 (core prompt, observation). `0. Pass priority (take no action this window)` rendered on 153
prompts corpus-wide and was taken; the deck123 seats opposite this one closed their token turns
with `x50` repeat answers and a `0` pass. The lane-N affordances are the reason `vs123` is a
27-record game this corpus instead of a 9-hour one.
