# Wave-48 reviewer boundary pass (deck reviewer lane)

Scope: the six boundary findings B1-B6 raised in `wave48/synthesis-notes.md`, adjudicated by
the reviewer of each deck and applied to the LIVE guides in `projects/mtg/bin/Res/ai/baka/`
(the deployed wave-48 reviewer revisions). Governing amendment: **119** (a mulligan section is
odds reasoning satisfiable at every bottom count; NEVER-sets jointly satisfiable at the smallest
keep; the owner's ruling that guides impose no floors / forced keeps). Corpus records
re-checked under `~/.Wagic/ai/gpt/selfplay-runs/matchups-20260827-072004/` (42 logs). ASCII
only; `git diff -- projects/mtg/bin/Res/ai/baka/ | /usr/bin/grep -c $'\357\277\275'` = **0**;
a `[^\x00-\x7F]` sweep over all seven guides returns no file. No `src/` change, no commit, no
wagic run.

## Verdicts

| # | verdict | reason |
|---|---|---|
| B1 | **ACCEPT** | Confirmed at deck123:87: *"No other spelling is read."* is a negative claim about the parser's grammar (#99), and D3 (widen to `N=17` / `17 times` / bare integers) is at the top of the wave-49 docket, so the sentence goes false the day it lands. The positive `x<N>` instruction (line 86) and the DECIDING restatement (line 521 area) are correct and sufficient; both left unchanged. Re-phrased as history exactly per the suggested wording. Corpus note: this corpus's deck123 replies carry `x50` (23) and the receipts `you named 50/15 repeats` (58/6); the `N=17` one-token event the sentence cites predates this corpus and was not re-located here - the edit does not depend on it, only on the negative-grammar shape. |
| B2 | **ACCEPT (all four guides)** | Verified the model chain: `1787833236-...deck146-...-vs-...deck152.jsonl` runs seq 1-5 through `(keeping 6) (keeping 5) (keeping 4) (keeping 3)` to `You will be left with a 3-card hand`, i.e. the STEP 1 floor text was argued past on its first real test; `1787833221-...deck125-...-vs-...deck126.jsonl` runs to `(keeping 4)` then a 4-card bottoming ask (the unsatisfiable NEVER-set). deck123 (RULE -1 head + MULLIGAN STEP 1/2 + bottoming + two DECIDING lines + "AT FIVE CARDS OR FEWER"), deck152 (RULE -1 head + STEP 1 + the four-land floor paragraph + DECIDING line) carried the IDENTICAL failed text verbatim; deck126 carried the "Never mulligan more than once ... last look: answer 1" absolute (#97's collision shape) plus three NEVER-bottoms; deck130 carried two NEVER-bottoms (Starstorm + three lands) that exceed a 4-card keep holding Starstorm + 3 lands... or any keep below 4. All re-shaped on 146-F: each look priced as a fresh seven shaved by one more card at the deck's own land count (24 / 25 / deck126 and deck130 keep their existing counts unstated - they name no land count in the section today, and I did not add an unverified number); the bottom ask given as a PREFERENCE ORDER satisfiable at any N; "you are DONE" / "do not read another rule" / "last look: answer 1" / every NEVER-bottom deleted. Preventative: no seat among 123/152/126/130 went below `(keeping 6)` this corpus (deck123 twice at keeping 6; the other three never mulliganed). |
| B3 | **ACCEPT (both)** | Confirmed: `Exquisite Blood` = 0 mentions in deck146 (only `Sanguine Bond` at :250, in the fold's quoted "net zero" arithmetic) and 0 in deck162. Both face deck126. Added the pair line FIRST in each DECIDING list (amendment 114's ordering), on deck152's 152-A wording keyed to the battlefield line. deck146's tail carries the lifelink-block half; deck162's tail carries its own true fact (the punishers still fire on their draw step) instead, since deck162 runs no blockers doctrine of that shape. |
| B4 | **REJECT (no edit), as the finding itself proposes** | Verified deck126:148 (THE ZERO STOP) and :355 (DECIDING) say the same procedure twice, and deck123 RULE 5 restates the count in plan-sentence form. The count is docketed on the row (D10). Editing now would be prose at a fifth corpus against a render that is about to carry the number; the fork risk (#96) is real but the fix is the D10-conditional delete, not a rewrite. Recorded: when D10 ships, deck126 keeps :355 and turns :148 into a pointer to the `{edict:}` clause; deck123 deletes the derivation and keeps one pointer. |
| B5 | **PASS (no edit)** | deck130's X procedure: "answer the mark" is line one, the steps are the unmarked fallback. Noted D14 (mark echoed on row 1) changes nothing in the rule. |
| B6 | **ACCEPT (self-check performed; three gaps closed)** | deck152's DECIDING list: the pair line WAS first (:493). The ONE-name "race with your evasion" posture (rules :471-478) was NOT in the list - added as the second entry. Positive-branch audit (#98): `do not cast in Main 1` lacked its positive half (added "Attack first, cast it in Main 2"); `Fateful Absence ... do not cast it` lacked one (added "hold it for the next window that lists a creature or planeswalker of THEIRS"). The other prohibitions (Katilda -> Main-2 action; Clue -> the draw costs N; BLOCKS: none only when ...; stays home unless the lethal check clears) already carried theirs. |

## Edits applied, per guide

| guide | finding(s) | edit | byte delta |
|---|---|---|---|
| `deck123_strategy.txt` | B1, B2 | RULE 1: *"No other spelling is read. "N=17" and a bare "repeated N times, then stop" with no number were each carried out as ONE token"* -> *"Write it exactly that way: in this corpus "N=17" and the bare row label with no number were each carried out as ONE token"*. RULE -1 head M<=5 branch: *"answer "Keep this hand" the moment the hand holds ONE LAND ... You are DONE; do not read another rule"* -> odds at every look (24 lands; same odds one card fewer; only ZERO lands buys a look; colour/curve are bottom-ask questions). MULLIGAN STEP 1 "THE CHAIN STOPS AT THE SECOND LOOK" -> "WHAT DOES THE NEXT LOOK COST?" (146-F shape, the 42-0 cost kept as WHAT THIS COST); STEP 2 "may you mulligan at all" -> "a fair trade only by the comparison below"; the AT FIVE OR FEWER bullet re-keyed to the odds. Bottoming: entry 1 *"enough lands to reach FOUR mana"* -> the lands that cover the cheapest kept spell, sized by N (two lands + one spell at N=3); *"NEVER bottom a land ... fewer than THREE, and never bottom your last castable spell"* and *"Never bottom your only way to find or make a creature"* -> an order that avoids all-lands / all-four-drops at every N, creature-finder last. Both DECIDING lines re-keyed (no "stop reading" / "No exceptions"). | 43,086 -> 44,201 (**+1,115**) |
| `deck126_strategy.txt` | B2 | *"Never mulligan more than once. If the prompt contains the words "having already taken", this is your last look: answer 1"* -> odds at every look from the second on (keep at one land at keeping 5 or lower; ZERO lands buys a look at the price of a card). *"NEVER bottom Sanguine Bond, NEVER bottom Exquisite Blood, and NEVER bottom Idyllic Tutor"* -> a preference order (covering lands, one combo piece/Tutor, cheapest wall, second piece, rest by cost with duplicate walls first; a piece goes under only when nothing below it is left). The DECIDING pair (:385-388) re-keyed to match; the "fewer than three lands" rule-out at :330-334 left in place as it is a build instruction under the order, not a floor over the keep. | 32,684 -> 33,391 (**+707**) |
| `deck130_strategy.txt` | B2 | *"NEVER bottom your only Starstorm ... and NEVER bottom a land when the hand you keep would hold fewer than THREE"* -> preference order (covering lands, three if N allows; Starstorm; cheapest spells; rest by cost; a land or the Starstorm goes under only when nothing below it is left). The "NEVER go below 6 cards" keep line (:347) left: it is a KEEP rule keyed to one land + one spell with "mulligan only zero lands or all lands" beside it - it does not force a keep on a zero-land hand and cannot exceed the cards in hand. | 36,615 -> 36,845 (**+230**) |
| `deck146_strategy.txt` | B3 | Pair line inserted first in DECIDING SITUATIONS: *"Their battlefield line shows BOTH "Sanguine Bond" and "Exquisite Blood" -> any damage you deal them and any life they gain repeats without limit. ATTACK: none, whatever the tags say, until one is gone; block every "[lifelink]" attacker you are offered a block for."* | 45,409 -> 45,680 (**+271**) |
| `deck152_strategy.txt` | B2, B6 | RULE -1 head M<=5 branch -> odds (25 lands), same shape as deck123. MULLIGAN STEP 1: *"KEEP ... you are DONE, do not read another rule in this section"* + the "THIRD look ... price of two" paragraph -> odds at every look with the two four-card losses kept as WHAT THIS COST. Bottoming: *"NEVER bottom a land unless ... at least FOUR"* + the "FOUR-LAND FLOOR APPLIES ONLY WHILE ..." paragraph -> one preference order sized by N (three or four lands + two-drops at N 5-6; two lands + two cheapest spells at N<=4). DECIDING: keep line re-keyed; ONE-name "race with your EVASION" line added second; positive halves added to the Main-1 tap rule and the Fateful Absence rule. | 41,973 -> 43,112 (**+1,139**) |
| `deck162_strategy.txt` | B3 | Pair line inserted first in DECIDING SITUATIONS (same wording; tail: *"the punishers still fire on their draw step"*). | 30,557 -> 30,810 (**+253**) |
| `deck125_strategy.txt` | - | untouched (W13 already 119-shaped). | 39,015 (0) |

**Pool total: +3,715 bytes across six guides.** Every byte is either the owner's ruling (119)
applied to text verified verbatim-shared with the chain that failed, a coverage gap confirmed
at 0 hits, or a #98 positive half.

## Not verified / notes forward

- deck126 and deck130 mulligan sections name no land count; I did not invent one (amendment
  119 asks the section to state it). A reviewer with the deck list open can add the number in
  one clause each.
- deck123's colour clause now names Marsh Flats and Tundra only (the two multi-colour lands
  verified in `deck123.txt`); the earlier draft's "Snarls" was removed as unverified.
- B1's cited `N=17` one-token event was not re-located in this corpus (only `x50`/`x15`
  spellings appear); the sentence now reads as history and no longer asserts the grammar.
- B4's conditional deletes are owed the wave D10 ships (amendment 89's standing sweep).
