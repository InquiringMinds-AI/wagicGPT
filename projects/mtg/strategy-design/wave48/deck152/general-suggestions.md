# deck152 - core-prompt / render proposals (wave 48), layer-routed

Engine items are in the seat file (E-3 creature-tapping cast rows, E-4 `[DRAW PRICE:]` on Clue
rows, E-5 = R12 loop clause now a lost game). These are the render-affordance halves.

G-1 (render, = seat E-5 / carried R12, HIGH now). **Ship the loop clause.** `152 vs126` s25 is the
first game LOST to the one-direction converter block: both `{effect:}` texts were on the
battlefield line, the block named Sanguine Bond alone, and the seat's own plan named both cards
and still computed a finite race. R12's exact text (`both halves of a life LOOP are on their
battlefield (Sanguine Bond + Exquisite Blood): any life they gain repeats without limit, so ANY
nonzero payment on a tag is fatal`) plus, on every A-row on that board, the fold already used for
the converter: `(... and their loop makes any damage you deal fatal)`. Guide-side answer applied
(152-A) so the rule survives if this does not ship.

G-2 (render, = seat E-3). **A cast row whose payment must tap a creature names the creature.**
`{paying this taps: Intrepid Adversary, Brutal Cathar - they cannot attack this turn}` on the row
(`152 vs162` s24). The mana line already lists them under `Those sources`; the row is where the
decision is made. Same emitter as `{leaves N of your M untapped mana sources untapped}`.

G-3 (render, = seat E-4). **`[DRAW PRICE:]` on `Draw 1 with Clue` and every other activated-
ability draw row** (`152 vs162` s28/s29). Lane Q reached cycling and cast rows; this class is the
same summary-line-to-row shape R9 describes.

G-4 (render, activated-ability phase). **On an activated-ability row offered in UPKEEP whose
cost includes `Tap` of a creature, say the creature will not attack this turn.** `put 1/1 counters
with Katilda [cost: {4}{g}{w}, Tap]` was taken in Upkeep 8 of 9 times; the row prints the cost and
not its consequence for combat. `{tapping Katilda now: she cannot attack this turn}` is the same
shape as the Hive `[tapped]` clause asked for at deck146 G-2.

G-5 (core prompt, observation only). The pregame ask with no carried plan held: 0 deck152
mulligans, 6 keeps, and every keep's PLAN was about the hand in front of it. Nothing to change.
