# Wave-41 status (step-1 complete checkpoint, 2026-08-23)

STEP-1 MERGED AND PUSHED (HEAD a4b82a3f1, binary /tmp/wagic-a4b82a3f1). Baselines:
**ST 1086/0 + 33 AI/0, PARSETEST 784/0** (single-threaded ground truth, both failure
strings). Seven planned lanes (A X-commit seam, B narration channel, C block-outcome
prevention, D option render + mutate-pile text, E may-batching + labels, F alias
counters/Laces, G repeat-activation countable) ALL landed, plus FOUR owner-reported
fixes from his Vita live play:
1. Delver of Secrets — illegal in-hand flip (anyzone doubleside) removed (card data).
   OPEN FOLLOW-UP: 59 other transform-DFC fronts carry the same line (wave-42 sweep).
2. Black Vise — owner REJECTED the Iron-Maiden rescript as a workaround (binding: never
   script a card wrong to dodge an engine defect). Faithful notaTarget choose script
   RESTORED; engine fixed at MayAbility::Update (mandatory single-candidate player
   chooser auto-resolves). choose-vs-target pinned by the True Believer shroud fixture.
3. Sliver first strike — stacked same-keyword lord grants: snapshot-restore replaced by
   grant/deny count arithmetic (both defect directions fixed, class-wide, 850 lords).
4. (From lane A) X-announce window: stale setX cleared on zone moves; ANNOUNCE_X exempt
   from the single-option no-model-call shortcut.

IN FLIGHT: the PROBE RUNNER (one agent, sequential): probe 1 mutate-render (#W41-4
discharge, stub-driven) + probe 2 deck152 edit discharge (REAL serve :8084, behavioral,
hours). Corpus WAITS for it.

NEXT (the loop): corpus — pool 146,152,125,126,162,123,130 (130 guideless Step-0),
-j 21, no cap, standing serve FP8-35B :8084 (NEVER cycle), canonical systemd-run +
single done-file Monitor recipe. Then review: seat validations incl. the falsifiable
predictions shipped this wave (Hydroblast/Pyroblast/Lace response-window offers;
zero "(your blocker dies" on prevention blockers; used-lines symmetry 165/0 -> equal;
zero anonymous public-origin moves; may-batch verdict keys; repeat-countable rendering;
X-announce records = casts). Wave-42 docket seeds: #W42-1 loud-fail chooser default,
DFC flip-line sweep (59 cards), Burnout zoneless-teach, Baka-cannot-X=0 residual,
multi-candidate mandatory chooser driver default, trigger-internal-choice register
question (owner may want "Opponent chose..." vs "used").

Rulings this session (all in wave40/engine-ledger.md + anchor memory): no-workarounds
(fix mechanisms, keep scripts faithful); X=0 legal/never filter; may = rules-faithful;
narration origin+public-naming shape; effect-source attribution; mass may accept/deny
(shipped as lane E).
