# Wave-32 consolidated engine ledger -- the wave-33 step-1 docket

Every item below comes from a seat's `notes.md` with the seat's own repro. Corpus
`~/.Wagic/ai/gpt/selfplay-runs/matchups-20260727-115847`, binary `7fabd9bd0`, unless stated.
Ranked by (game-cost observed) x (mechanism located) x (repro quality). Severity and repro quality
remain SEPARATE fields (wave-31 HL4).

Four cross-cutting directives for the fix agents, before the list:

- **#1 IS A CORPUS-INTEGRITY ITEM, NOT A SEAT ITEM, AND ITS VALIDATION IS A POOL RE-RUN.** The
  poison pair invalidates the pricing of every record in the wave-32 corpus, including its headline
  result. It ships FIRST, and the deck105 6/6 carries an ASTERISK until the re-run lands. Do not
  read either half as "a deck105 problem".
- **PATH-SCOPING NOW HAS AN INPUT SIDE AND A FIXTURE SIDE (wave-32 HL5).** #3 (N-158m) exists
  because a fix scanned card data through ONE representation when the data has two; #4 (N-152f)
  exists because a fix's FIXTURE rode the branch that already worked. Before closing anything:
  enumerate the data's indirection layers (macros, `transforms(...)` payloads, `ability$!...!$`
  wrappers, alternative-cost faces) and state which representation the scan sees; and state which
  branch your fixture rides versus which branch the failure rode.
- **REPORT EVERY ANNOTATION FIX AS A COVERAGE FRACTION** (offers-with-annotation / offers-total,
  per card, on the next corpus), never as "it renders". `3 of 52` is what a passing sample looked
  like this wave.
- **RECORD THE BINARY HASH ON BOTH SIDES OF ANY OPEN-DEFECT CLAIM before treating it as live**
  (wave-32 HL10). This rung already retired one HIGH docket item this wave -- see #17.

---

## Tier 1 -- cost a game this corpus, mechanism located

| # | id | sev | one-line mechanism | file:line | repro | validated next by |
|---|---|---|---|---|---|---|
| **1a** | **N-105a** (= N-116b = N-139o = N-146l = N-152i = N-158j = N-36a-half) | **P0, CORPUS-INTEGRITY, 7 seats** | **Poison counters are rendered NOWHERE to the AI.** Life line emits only `Your life: N \| Opponent life: N`; players have no counter field; infect combat damage narrates in the ORDINARY damage form with the life-change line simply ABSENT (a MISSING line is the only tell); non-combat poison (`alterpoison`) narrates NOTHING. The engine HAS the number and renders it FOR THE HUMAN -- a textbook **ishuman-lens gap**. `AIPlayerGPT.cpp` contains zero "poison" outside a comment | life line **`AIPlayerGPT.cpp:2251`**; number at `Player::poisonCount` / `Player::isPoisoned()` / `Player::poisoned()`, **`include/Player.h:94-95`**; human render **`src/GuiStatic.cpp:138-140`**; silent non-combat emitter `alterpoison` **`AllAbilities.cpp:1464`** | Present in **100% of windows of 6/6 games**. deck105 vs36 s17 t12: *"I am at 20 life and need 10 poison counters to win; I currently have 0"* (actual 6). vs146 s23 t15: *"the opponent has 21 life. I need 20 poison. I have 11"* -- threshold conflated with LIFE. vs146 s22 t14, **8,827 chars / 143.6 s**: *"If they have 11 poison counters, they have already lost. The game should be over. ... If the game is still going, then my count is wrong."* | **deck105 POOL RE-RUN** (below), plus every seat. Emitters to fix TOGETHER (HL5 sweep): life line (both players), a narration line for combat poison, an event line for `alterpoison`, and the attacker/blocker seams (#1b). **Validation predicate:** a poison total visible in every prompt of an infect game at BOTH seats, and a non-combat poison gain that narrates |
| **1b** | **N-105b** (= N-36a) | **P0, HIGH, CROSS-SEAT, GAME-LOSING** | The blocker-seam life forecast is built from RAW attacker power **with no infect check**, so against infect attackers it emits three falsehoods in one sentence: the life will not change; "NOT lethal" is a claim about a resource the attack does not touch and is INVERTED on any swing crossing 10 poison; and *"taking damage while ahead is often correct"* is the exact inverse of correct play against a counter that never resets and that no card in the pool removes | **`AIPlayerGPT.cpp:6768-6770`** (`tail << "Your life: " << life << ". Unblocked, these attackers deal up to " << incoming << " - you would be at " << (life - incoming) ...`). Related: `Damage.cpp:237` reads `source->getToxicity()`, so **`toxic` exists in this engine and has the same bug -- check `toxic` and `wither` on the same pass** | **12 of 12** infect blocker windows across six games (deck36 s9/s13/s17/s20/s25, deck146 s28, deck158 s9, deck116 s8/s11, deck152 s11, deck139 s20/s34); 4 ended in `no blockers`. **GAME-LOSING repro, deck36 s25 t12** (the killing swing): *"Since the total damage (5) is not lethal (I would drop to 15), and blocking with the Pest achieves nothing ... I declare no blocks"* -> `BLOCKS: none` -> poison 6 -> 11 -> `winner=0 life0=20 life1=20 turn=12`. **SAME-SEAM CONTROL:** the identical emitter is CORRECT in every non-infect window in the same corpus (deck105 s18 t12: `you would be at 7 - NOT lethal`, and the life DID go 15 -> 11) | **deck105 POOL RE-RUN** + every opponent seat. Fix direction: partition attackers by whether their damage is REPLACED; for the infect/toxic partition emit a poison forecast against the 10-counter threshold and **DROP the "taking damage while ahead" clause** (an accurate number with stale advice attached is still a misteach); emit both halves on a mixed swing. **Validation predicate:** zero occurrences of `- you would be at <life-N>` in any window whose attacker list contains `[infect]`, and at least one block declared against an infect swing the old line called "NOT lethal" |
| **1c** | **deck105 POOL RE-RUN** | **MANDATE, not a defect** | deck105's 6/6 perfecta was set while all six opponents were being told at the decisive seam that its attacks were survivable. **A record set under a false surface is not a strength measurement.** | -- | Three losers finished at 21, 23 and 23 life having GAINED life above 20; deck105 itself finished at <=8 life in four of six games and won all four (at 2 life vs an opponent on 23) | **After 1a+1b land, re-run deck105 against the SAME pool before pricing it for the difficulty ladder.** If it still goes 5-6/6 that is a construction/roster question; if it drops, the perfecta was substantially a render artifact. **The wave-33 brief must state the 6/6 as ASTERISKED**, not repeat it as a headline result |
| **2** | **N-146j** | **HIGH, GAME-COSTING** | `[-multicolor]` compiles to **two colour-bit exclusions that reject every ARTIFACT and every LAND**, whatever its actual colour. `SetExclusionColor(0)` sets `kColorBitMask_Artifact (0x01)`; `SetExclusionColor(6)` (`MTG_COLOR_WASTE`) maps to `kColorBitMask_Land (0x40)` -- **the same bit lands carry** -- and `match_and` rejects on ANY intersection. The comments state the intent was to exclude COLORLESS; the implementation does not do that | predicate compile **`src/TargetChooser.cpp:1066-1074`**; colour indices **`include/MTGDefinitions.h:100-107`** (`MTG_COLOR_ARTIFACT=0`, `MTG_COLOR_WASTE=6`, `MTG_COLOR_LAND=7`); bitmask collision **`src/CardPrimitive.cpp:530-535`**; rejection **`src/CardDescriptor.cpp:293-297`**. **`src/CardGui.cpp:1822-1829` carries the IDENTICAL block** and needs the same treatment or the card browser disagrees with the game | Deterministic. deck146 vs36 **seq 17 / 19 / 23, all t10**: `Cast Vanishing Verse {b}{w} - the only legal targets are YOUR OWN right now` against an opponent board whose `Glaze Fiend` and `Salvage Slasher` are `mana={1}{B}`, `type=Artifact Creature` -- monocolored black, plainly legal. The pilot spotted it: *"Glaze Fiend is {1}{B} (Black) - that is monocolored ... Why are they not listed as legal targets?"* **deck146 lost that game at t11 holding THREE Vanishing Verses.** Second independent corroboration: corpus-wide the ONLY artifact ever listed as a Verse target is a **Clue token** (token creation does not set the artifact colour bit), and **no land appears in any Verse target list in any of the 21 games** | **deck146** (holds). **Grep `Res/sets/primitives/*.txt` for `[-multicolor]` BEFORE fixing -- the fix changes legality for every primitive using it.** Direction: keep `setisMultiColored(-1)` and replace the two `SetExclusionColor` calls with a real colorless test that does not consult the artifact or land bits (line 1072's own comment names the mechanism). **The guide's Verse teach becomes a CRUTCH the moment this lands and must be demoted then** |
| **3** | **N-158m** | **HIGH** | i14's amass preview reaches **3 of 52 live offers (5.8%)**. `amassCountersFromScript` scans `card->magicText` for the literal substrings `counter(1/1.` / `counter(1/1)`; macro expansion runs on a **LOCAL copy** inside the ability factory and is **never written back** to `CardPrimitive::magicText`, so every `_AMASSORCn_` card presents the scanner with an opaque token | scanner **`src/AIPlayerGPT.cpp:246-259`** (`dynamicMagnitudes` -> `amassCounters` -> `amassCountersFromScript`); macro expansion **`src/MTGAbility.cpp:6650`** (`magicText = AutoLineMacro::Process(magicText);`); raw append **`src/CardPrimitive.cpp:358-360`**; macros **`bin/Res/sets/primitives/_macros.txt:68-72`** | Static, deterministic, mechanism read from source. Per-card coverage at deck158: Foray of Orcs **3/3**; Easterling Vanguard 0/17, Mordor Muster 0/13, March from the Black Gate 0/9, Orcish Bowmasters 0/7, Dunland Crebain 0/2, Swarming of Moria 0/1 | **deck158** (holds). **Preferred fix: call `AutoLineMacro::Process(...)` inside `amassCountersFromScript` before scanning** -- it closes the CLASS and fixes every future macro-defined magnitude for free (`_TREASURE_`, `_FOOD_`, any later `#AUTO_DEFINE` carrying a number). The mapping-five-macro-names alternative closes only the instance. **NEGATIVE CONTROL THAT MUST NOT BREAK:** Assault on Osgiliath's nine `alternative if compare(fullpaid)` branches carry `counter(1/1)` through `counter(1/1.8)` -> `values.size() > 1` -> return 0; the multi-branch suppression is the guard that keeps a confidently-wrong number off an X-spell. **Validation = the COVERAGE FRACTION re-measured, not "Foray renders"** |
| **4** | **N-152f** (N-152c LAYER 2) | **MEDIUM-HIGH** | All-flexible land base -> **mutual deferral deadlock** in `deferFlexibleSource`: the SELF test `sourceHasOtherNeeded` aggregates over every ability with `amp->source == card`, but the OTHER test `competesElsewhere` is evaluated **per-ABILITY** on whichever `amp` the loop holds. Another Deserted Beach reached via its `add{W}` ability has `hasColor(U) == false`, so it is judged "dedicated to W", `otherUnusedCoversK` becomes true, and every Beach defers every pip. **This is exactly the deadlock the function's own comment at lines 146-149 claims to prevent** | **`src/ManaEngine.cpp:121-160`** (`deferFlexibleSource`); SELF test :136-142, OTHER test :143-156, the comment :146-149. The wave-32 colour-first gate in `ManaEngine::planPayment` (`6ca1f8411`) is UPSTREAM of this and never reaches it | Live, deterministic. deck152 vs116, seq17 t8 (identical at s19/23/25/28/30): five untapped `Deserted Beach #1..#4` + `Barkchannel Pathway`, `colours you can make: {g}{u}{w}`, `Teferi, Who Slows the Sunset {2}{u}{w}` in hand and trivially payable. **`Cast Teferi` appears ZERO times in the entire game log.** The model verbalized it against the core's completeness guarantee at deck116 seq18: *"I have Teferi in hand, I have the mana, it is my main phase. Why is it not listed? ... This is a contradiction."* **Same-seam positive controls (the fix DOES work where a MONO source exists):** deck146 s11/s14/s20 and deck139 s23 all OFFER Teferi -- each board has a `Plains` or `Hengegate Pathway` freeing the Beach for `{U}`; deck158 s19 offers Sigarda off Overgrown Farmland's SECOND ability, with s14/s16/s17 (one white source) as the negative control | **deck152** (holds). Candidate fix: aggregate `competesElsewhere` over ALL abilities of `amp->source`, symmetric with the self test -- **confirm with the fixture BEFORE editing.** **FIXTURE SPEC, both required:** (a) POSITIVE, currently failing -- `inplay: Scrubland, Scrubland, Darksteel Citadel` + a `{1}{W}{B}` spell in hand; PASS = cast and reaches the battlefield. (b) NEGATIVE GUARD -- the existing `dual_land_multipip_not_offered_n152c.txt` (`{2}{B}{B}` over the same board) must STAY green, since the wrong fix (routing the cast path onto `potentialManaPermissive`) double-counts each dual. **HL5 note: the shipped `dual_land_multipip_cast_n152c.txt` rides one dual + a MONO source, i.e. the branch that already worked** |
| **5** | **N-36b** ({0}-cost token deletion) | **HIGH, 3 EMITTERS** | A converted mana cost of **0 is falsy**, so the guard `if (cost && cost->getConvertedCost())` deletes the cost token entirely. **347 renders of three `{0}` cards at deck36; not one carries a `{0}`**, including the system-prompt decklist line | **`src/AIPlayerGPT.cpp`**, three emitters: decklist ~**:845**, cast option ~**:1658**, hand/board ~**:4215** | Deterministic. The pilot manufactured **four distinct false rules**, verbatim: *"they are not spells you can cast from your hand"* (deck116 s3), *"putting them onto the battlefield requires a land drop slot"* (s4), *"Moxes are artifacts that I can cast, but they cost mana to cast. I have no extra mana"* (deck139 s5), *"Ornithopter/Moxes are low priority"* (s3). **4 of the seat's 7 cast-nothing windows declined a free permanent**; both Moxes delayed a full turn; the seat's two largest cast-seam replies (deck152 s3 at 8,600 chars / 133 s and s4 at 7,040 / 107 s) are spent on whether a Mox is free | **deck36** (holds). Fix all three emitters together and print `{0}` explicitly. **The general shape is `if (value) print(value)`, which deletes every legitimate ZERO** -- sweep for the same guard on 0-power creatures, 0-counter permanents and empty subtypes while you are in there (wave-32 HL9's ABSENT-FIELD audit). The guide's "a card with no cost printed costs {0}" line is a STOPGAP and demotes when this lands |
| **6** | **N-146k** | **HIGH, RENDER/HARNESS** | The **pre-game mulligan prompt renders a battlefield mana line** -- `Mana available: 0 total (no untapped sources)` -- a battlefield fact emitted before a battlefield exists | the situation-block assembler, per-seam suppression (the fix is to STOP EMITTING the line in the pregame phase, not to explain it in prose) | deck146: the pilot declares a land-rich hand landless in **5 of 14 mulligan replies**, echoing the line almost word for word. deck139 seq1, the OPENING seven, verbatim: hand = `Hive of the Eye Tyrant (land: taps for {B}); Plains; Swamp; Triumphant Adventurer; Vanishing Verse x3` -> *"This hand has zero lands that can produce mana **right now** (all three are lands, but you haven't tapped them yet, and you have no mana in your pool)"* -> Mulligan. Same shape at deck158 seq4, deck139 seq3/4/6 | **deck146** (holds) + **deck139** + **deck116** (all three spiralled). Deliberately NOT a core-prompt sentence: "ignore the mana line during mulligans" is prose arguing with a contradicting surface, the losing side of the perception-vs-strategy routing |

## Tier 2 -- representation escalations and path-scoped residuals

| # | id | sev | one-line mechanism | file:line | repro | validated next by |
|---|---|---|---|---|---|---|
| **7** | **N-139n** | **HIGH** | The mulligan / bottom hand block has **no LAND or GREEN-SOURCE COUNT**. Per-item tags assert MEMBERSHIP, and membership is exactly what the pilot argues with; only an engine-computed AGGREGATE closes it (wave-32 HL6) | the mulligan/bottom hand-block render. Proposed header: `Your hand (7 cards): lands 3 (green sources: 1) \| creatures 4` | **Two-cycle evidence.** Cycle 1 (wave-31): "zero lands" with three duals -> fixed at the GUIDE layer by enumerating the offenders by printed name and quoting `(land: taps for {R}{G})`. Cycle 2 (wave-32): rate halved 2.50 -> 1.50/game and the belief **MUTATED** to "zero GREEN mana sources" -- **6 false mulligans of 9**, with vs105 s1 **reciting the guide's own five-card list while holding a literal Forest**. The invented disqualifier is new each time (*"Stomping Ground is red/green but you have no way to pay the life"*). **Controlled contrast, same seat same wave: i11's per-item creature markers produced ZERO misreads on the creature axis** | **deck139** (holds) -- and **deck139's EXIT IS CONDITIONAL ON THIS SHIPPING**; the guide layer has now failed twice on this belief. Also relieves deck146 and deck116, both of which spiralled on the same axis. Validation: zero replies asserting "zero lands" / "zero green sources" against a hand whose count header contradicts them |
| **8** | **N-152j** / **N-158n** | **MEDIUM, 2-seat convergence on the ROUTING** | The blockers parser rejects a **compliant `BLOCKS:` head line written with card NAMES** instead of `Bn:An` labels. **Both of the corpus's `unparsed_reply` fallbacks are this one shape** | blockers parser (the file-static tolerant parser); extend to resolve names against the offered B/A lists by UNIQUE match, dropping unmatched names, first-wins as today | deck139 s21 t11: `BLOCKS: Pollywog Symbiote blocks Elite Spellbinder` -- offer was `B1. Pollywog Symbiote` against `A1..A5`, intended assignment unambiguously `B1:A5`. deck158 s35 t16 at **6 life, GAME-LOSING**: `BLOCKS: Orc army: Sigarda, Champion of Light; Dunland Crebain: Moonrage Brute` -> unparsed -> Baka -> no blocks -> 8 damage -> **-2, game over**. Both replies obey the answer-first protocol on line 1 | **deck152 + deck158 + deck139.** Both seats independently classify this as a **PARSER COVERAGE gap, not a compliance gap**, and both explicitly DECLINE to route it to the core prompt (core already carries a correct positive example three lines above the failure; a negative clause cannot cover the next equivalent notation). **HONEST CAVEAT:** deck158's instance ALSO asserts a tapped creature as an available blocker and an illegal flying block, so a name-tolerant parser recovers a PARTLY-legal assignment there (Army -> Moonrage Brute survives 3 of the 8 damage), not a clean one |
| **9** | **N-152d layer 2** | **MEDIUM, PATH-SCOPED** | `(printed X/Y)` on a transformed werewolf is FIXED on the attacker/target enumerators and **STILL FALSE on the BOARD SNAPSHOT** -- the battlefield emitter reads the FRONT face's P/T | the battlefield-line emitter (the sibling of the already-fixed attacker/target enumerators) | `Moonrage Brute (3/3) (printed 2/2)` **x22** corpus-wide vs 12 clean; Brute is `power=3 toughness=3`, `subtype=Werewolf` (NOT a Human, so Sigarda's `lord(human)` cannot touch it), no counters, no anthem -> **delta 0, correct render is a bare `(3/3)`**; `(printed 2/2)` is Brutal Cathar's FRONT-face P/T. Witness seqs deck158 s34/s38/s40/s43. **Positive control same seat same tag:** `Brutal Cathar {2}{w} (3/3) (printed 2/2)` at s32 is CORRECT (Human + Sigarda), as are `Sigarda (5/5) (printed 4/4) [counters: 1x +1/+1]` and `Luminarch Aspirant (2/2) (printed 1/1)`. The annotation LOGIC is right; only the transformed-face selection on this emitter is wrong | **deck152** (holds). Note `Tovolar's Packleader (7/7) (printed 6/6)` is **0** occurrences -- but Packleader appeared only on the FIXED paths this corpus, so its zero is COVERAGE, not proof |
| **10** | **N-152h** | **LOW, cross-seat, PATH-SCOPED** | The retired MDFC wording survives on a **THIRD card class: MDFC SPELLS.** `Flip Side with Emeria's Call -> ... the Cast menu ... lists every face you can afford (the other face appears there as an alternative-cost cast)` -- false there too; Emeria's back face is a LAND | the MDFC-spell Flip Side emitter (siblings already fixed: MDFC lands h4, transform DFCs i15) | deck146 seq36. Cross-seat: found by deck152 at deck146's seat | **deck146 or deck152.** Close it in the same pass as any other Flip Side work -- this is the third emitter of one class |
| **11** | **N-158k** | **MEDIUM** | Feed the Swarm's per-target life cost renders on the **CAST line only**; the subsequent `- TARGET CHOICE` menu, **where the target is actually committed**, carries rules text and no cost (wave-32 HL5.3, the COMMIT seat) | the target-menu emitter | The cast line WORKS and won a game: deck158 vsk36 s17/s18 at 12 life with exact lethal, *"If I destroy Cathodion, I lose 3 life (12 -> 9). Ornithopter is {0} ... Destroying Ornithopter is strictly better"* -> took Ornithopter -> killed on the swing. But at deck152 s26 and s31, on **11 and 6 life**, the target menu was answered with no price visible at all (`Ornithopter (0/2) [flying] [opponent's battlefield] - "Flying"`) | **deck158** (holds). Cheap; same pass as any other target-menu work. Validation: the `(costs you N life)` fragment present on BOTH surfaces |
| **12** | **N-158l** | **LOW, latent** | Counter-source attribution emits an **empty `[from ]`** when the source is a sorcery already in the graveyard -- a rendered defect on i14's OWN new surface | the counter-narration source-attribution emitter | 38 distinct prompts. Always follows `- Your Mordor Muster: stack -> graveyard` / `- Your Foray of Orcs: stack -> graveyard`. Three variants coexist: named-permanent source (90 prompts, correct), no-bracket on amass-onto-existing-Army (63, correct), empty bracket (38). No reply verbalized confusion; no misplay traced | **deck158.** Clean repro, low severity, batch it with #11 |
| **13** | **N-116a / N-136a family** | **MEDIUM, TAX-ONLY, cross-seat** | Library tutors render the **WHOLE LIBRARY** (44-51 options) rather than the eligible set. Mitigated -- the render carries an ELIGIBILITY header and per-line `[does NOT qualify - goes to "shuffle"]` tags -- and the picks are correct, so the cost is TAX, not correctness | the `revealzone(mylibrary)` reveal path; the wave-30 g2 filter is still absent | deck116, 14 reveal decisions / 6 games. Eligible sets of **4** (Fabricate) / **5** (Time of Need) / **7** (Farseek) against **44-51** rendered options. **Cost measured:** the corpus's single largest reply is a Farseek reveal at **13,796 chars / 220,606 ms** on a 48-option list to pick a land; three of the seat's top six tax rows are reveals. Picks 9/9 correct on the `<1>` reveals; fetched card arrival-traced to hand 14/14 | **deck116** (holds). **NOT path-scoped -- deck116 verified BOTH emitter framings (`<1>` singular and un-tokened subset) are unfiltered, so the fix must not be path-scoped either.** Companion: **N-116d (MEDIUM)** -- an un-tokened `target(...)` renders SUBSET framing on a pick-EXACTLY-ONE effect, producing over-selection in **5/5** Farseeks (`PUT: 14, 23, 25, 26, 29, 36`); outcome-neutral here ONLY because every over-selected card was interchangeable |
| **14** | **N-36c** | **MEDIUM** | **SELF-EXCLUSIVE MAGNITUDE**: Master of Etherium's cast-line CDA is built while the card is still in hand, so its self-inclusive artifact count is ONE SHORT (wave-32 HL9.5; third member of the family after Gray Merchant's `{drains N}`) | the cast-option magnitude build (same lane as the GM `{drains N}` fix -- build against the POST-RESOLUTION battlefield) | 2/2 casts, both verified. deck139 s14 `Cast Master of Etherium {2}{u} (6/6)` at `you 6` -> s15 battlefield `(7/7) (printed 0/0)` at `you 7`. deck116 s10 `(5/5)` at `you 5` -> s11 `(6/6)`. The CDA itself is CORRECT and tracks live in hand across seven consecutive renders (0/0 -> 6/6 in lockstep). The pilot's estimates drifted in BOTH directions off the un-annotated number (planned a "4/4" off a printed (5/5); planned a "9/9" at `you 10`, i.e. an 11/11) | **deck36** (holds). The guide carries the +1 as a STOPGAP with an explicit removal condition and demotes when this lands |
| **15** | **N-139s** | **MEDIUM** | An **ALL-ILLEGAL blocker assignment set** exits as a silent `unparsed_reply` + heuristic, where the i5 machinery just installed a LOUD re-ask elsewhere | the blockers validation exit (co-evolve with #8 -- these two touch the same parser) | deck139 vs152 s21, 8,171-char reply. Head line compliant in FORM (`BLOCKS: ...`) but names `Pollywog Symbiote` (1/3, no flying, no reach) onto `A5. Elite Spellbinder (3/1) [flying]` while B1's own line reads `may block A1, A2, A3, A4` -- A5 absent. All assignments illegal -> all dropped -> nothing to parse. **The parser behaved correctly**; the observation is about the EXIT | **deck139.** Ship with #8, not before it |
| **16** | **N-36f** | **MEDIUM, REPRESENTATION** | *"Coloured mana cannot pay a generic cost"* -- **with the core prompt's own corrective PRESENT and QUOTED.** Repetition is not a mechanism; routes to representation by the standing rule | the `Mana available:` line. Concrete proposal: state what the mana BUYS -- *"any of them pays a generic {1}"* | deck152 s4/s5, verbatim, twice in consecutive replies: *"'Mana costs are written like {2}{R} (two generic plus one red).' '{1}' is one generic. If I have {W} and {B}, I cannot pay {1}."* Cost at deck152 s5 t1: `colours you can make: {u}{b}{w}`, hand holding Steel Wall {1}, Silver Myr {2}, Glaze Fiend {1}{B} -- **all three OFFERED, all three payable** -> `Cast nothing right now`. Recovered in Main 2. It OSCILLATES: the same model got it right at s3 (*"{W} counts as generic? Yes"*) and wrong at s4 | **deck36** (holds; 35 of its 48 artifacts are colourless-generic costs paid off a five-colour artifact-land base, so it is the structurally exposed seat). Related successor belief at deck158: `{2}{R}` read as TWO mana -- a COST-NOTATION misparse, the inverse of the wave-31 mana-pool error, currently carried as one guide line |

## Tier 3 -- instruments, probes and carried items

| # | id | sev | disposition |
|---|---|---|---|
| **17** | **N-139m** | **RE-SCOPED, HARNESS LANE -- live severity WITHDRAWN** | The wave-32 corpus arrival-traced **6/6** Pollywog-present discounted mutate casts on `7fabd9bd0` (discounted cost paid, loot fired, `[mutated pile]` arrived, Snapdax's +4 life and Illuna's reveal both RESOLVED). The three failing fixtures were **re-run on `7fabd9bd0` and still fail identically**, so the card-loss reproduces ONLY under the scripted test-harness click path. Hypothesis: the script pump's handling of the loot's mid-cast discard chooser diverges from live menu flow -- kin to the known scripted-fixture menu-arming landmines. **Keep the five fixtures with their STATUS headers as the harness-lane reproducer; deck139's exit is NO LONGER gated on it.** This item is the worked example of wave-32 HL10's binary-hash rung |
| **18** | **COMMIT-FAILURE COUNTER** | **INSTRUMENT, not a fix** | Every wave-33 seat reports, as a counted field: `replies whose text continues past their own PLAN: line`, and of those how many RETRACTED and how many were FATAL. **Two or more seats with fatal post-PLAN retractions promotes the reply-protocol COMMIT terminator to a core proposal with its measurement in hand.** Do NOT write the terminator from one seat. Witness: deck158 vs139 s16, 12,180 chars / 206 s at 2 life -- a complete, correct, protocol-compliant `CHOICE: 1` + reasoning + `PLAN:`, then ~10,000 further characters and no re-commit; a CORRECT decision discarded, Baka answered |
| **19** | **N-137c layer 2** | **CARRIED from the probe, HIGH** | The alternative-cost CONVOKE branch still abandons the cast after consuming the pick: across four deck198 probe games, **ZERO `March: hand -> stack`, ZERO `Announce the value of X` on any route**, despite convoke-route PICKS at three seats. The crash half is FIXED (`7fabd9bd0`) and F4 (the OFFER) PASSED live. Instrument `MTGAlternativeCostRule::reactToClick`, **`MTGRules.cpp:1150-1160`**, through the announce / `setExtraCostsAction` seam on a convoke pick. Validation = the deck198 probe pair (recreate from `probe-results-n139h.md` + `wave31/deck137/findings.md` §8). **Run probe one-shots SEQUENTIALLY** -- `-j2` halved each game's decision budget and capped games 3-4. Convoke arc remains OPEN; deck137 stays rotated out |
| **20** | **N-146h-probe** + **Agadeem layer 2** | **CARRIED, PROBE-GATED** | i3 is **UNEXERCISED** at deck146: 0 cast offers, 0 casts, 0 `Choose an option for Agadeem's Awakening` menus; every appearance is pre-game (1-of, `{B}{B}{B}{X}` triple black). The layer-2 question (does the cast now fail LOUDLY, or resolve?) **cannot be answered from a corpus** and carries forward un-discharged; the guide's "leave Agadeem's Awakening alone" crutch STAYS and its demotion is not eligible. **Discharge by probe deck, not by another corpus of absence** |
| **21** | **N-36h** (i10 probe spec) | **PROBE SPEC** | i10's attachment renders have **NO WITNESS LEFT IN THE POOL.** A sweep of all 42 translogs / 21 games returns **zero** `attached to:`, `{attached:`, `[equipment]` and `Equip ` at EVERY seat; the pool that was to validate the fix (deck148, mono-W Kor equipment) rotated OUT in wave-31. **Verdict: UNEXERCISED, not clean.** Trigger condition: an Equipment or Aura permanent attached to a creature, either side, at any render window; corpus count 0. Deliverable is a probe deck. **The INSTANCE-HANDLE half IS exercised heavily and PASSES** -- 0 unhandled duplicates across 144 prompts at an identical-name farm, handles binding to combat lines and reaching the model's own output |
| **22** | **N-116e** | **CONSTRUCTION / roster lane, LOW, informational -- do NOT act** | Sheoldred `{5}{B}{B}` is HARD-uncastable in a deck with no black source: a cheat-only card by construction. Arguably intentional for this shell, but state it as such |
| **23** | **N-139q** | **MEDIUM, CARD-SCRIPT** | Nethroi's `auto=@mutated(this): target(<anyamount>creature[power<=10]\|mygraveyard) moveTo(myBattlefield)` returns **ANY NUMBER** of creatures each with power<=10; Oracle is "total power 10 or less". Per the standing rule the GUIDE softens to an oracle-true description and the DEVIATION routes here as a card-script item |
| **24** | carried, unchanged | -- | **N-105d** (the `(creatures: N)` battlefield header read as a type claim about the list; clean repro, legibility, LOW) · **N-105e** (mulligan bottom-line card text truncates mid-word, one word before the payload; cosmetic) · **N-116c** (colourless-producing lands render as bare `(land)`; Glimmerpost singled out as *"only one source of mana"*) · **N-36d** (a bare mana ability offered as a standalone priority action -- Tolarian Academy burned in the UPKEEP in **6 of 8 activations**, measured cost this corpus ZERO, do not over-price it) · **N-36e** (the PLAN field absorbs the model's own quoted plan plus everything after it; **51 of 132 prompts >400 chars**, parser lane) · **N-36g** (a Clue token's card text renders as literal `()`) · **N-139p** (the bottom ask drops "You will be left with an N-card hand" when N would be 0) · **N-139r** (`@mutated(mytgt)` present on five mutants, absent on Snapdax and Illuna; DATA QUESTION, no mechanism asserted) · **N-139t** (the loot's DISCARD header attributes its source inconsistently) · **N-139l** (merged-pile cost pip drift; RECURS, decision-neutral) · **N-152g** (LOW, needs a probe -- Sigarda payable-and-unoffered at deck105 s28/s36 with a Sigarda already in play; leading hypothesis is legendary-duplicate suppression, **recorded as a trigger predicate, NOT as a diagnosis**) · **N-158d** (Foray's damage-target menu names the ARMY as the source; the new ability-name qualifier helps, latent) · **N-158e** (flash windows; KNOWN-OPEN, steady state confirmed, not counted against the guide) · **N-158f** (Assault emblem / double strike + haste; UNEXERCISED -- Assault never offered) · **N-152a** (MDFC back-face land placement; expected, Teferi NOT blocked) · **N-139g** (under-card statics: **ANSWERED POSITIVE, live-confirmed twice** -- promise discharged, line becomes a validated teach) · **L2** (cross-zone dup-name; 1 instance, recoverable) · **L-158-1** (whole-turn planning displaced onto the LAND-DROP seam; LENGTH-ONLY watch, not a decision defect) · **L-18-1** (planning tax, length-only watch) · N-134b/d · ETB header card-name · N8 · Azcanta · undying · commit-lock (no witness this corpus) · prose-reversal · untapped-tagging · I-4 |

## CLOSED this wave

- **N-139i** (London bottoming) -- **CLOSED corpus-wide**, 15 games with mulligans, 15 bottom records
  (one per game), `says_mulligans == must_put == named` in 15/15, hand size 7-N in 15/15, including
  the 7-mulligan / 0-card case. Cosmetic residual only (N-139p).
- **N-139j** (0-power attacker tag) -- **CLOSED**, 26/26 attacker windows, and the tag is DYNAMIC and
  read dynamically (a printed 2/4 Dryad reduced to 0/2 by infect counters was excluded by name 3x).
- **N-139k / N-122c** (summoning-sick and tapped-attacker tags) -- **CLOSED**, 851 renders corpus-wide,
  old form 0x, paradox pair `[tapped - cannot attack or block] [attacking]` **0x corpus-wide**, zero
  paradox verbalizations in any reply at any seat.
- **N-158g** (mana line) -- **CLOSED by i12**, false-decline class 0/33 at deck158 vs 4/55 fatal
  instances one wave ago; old string 0 occurrences. **Successor belief installed** (cost-notation).
- **N-158b** (counter narration magnitude) and **N-158c** (Foray `damage 0`) -- **CLOSED by i14**;
  Foray is now exact on both branches, ground-truth-verified against the narration.
- **N-158h** (Feed the Swarm constant life cost) -- **CLOSED by i14 on the cast line**; sibling gap
  #11 opened on the target menu.
- **N-148c** (lifelink-ueot) -- no witness in this pool; carried as fixed-pending-witness.
- **N-146i / i13** (card text on Cast lines) -- **VALIDATED, adopt permanently.** 100% coverage at
  every seat and **the flagged length risk did NOT materialise**: deck146 latency p90 115s -> 51s and
  >60s calls 37 -> 13; deck139 every latency statistic down; deck158 p95 reply length FELL 46% in the
  same wave the prompt grew. Only deck152 shows tail growth (prompt p90 +11.8%, max +36%) with no
  measurable degradation.
