# deck139 -> general (core) prompt suggestions, wave 32

## VERDICT: **PASS (27th consecutive).** No core-prompt edit is proposed from this seat.

Every leak this seat produced routes to a layer other than core, and I argue each routing
explicitly per the four-layer discipline.

### Why the four candidates do NOT go to core

**(a) The mulligan false-belief ("this hand has zero green mana sources", 6 of 9 mulligans).**
Core already carries a mulligan land-count default and the interface contract. The failing belief
is not deck-agnostic in its CONTENT (it is about shockland/dual-land colour production) and it is
already stated in BOTH the deck guide and the per-card render tag. Per representation-beats-
instruction, a rule stated twice and still bypassed does not earn a third statement in a THIRD
layer -- and a core line would make all seven decks pay for a class-membership belief that
happens to bite this seat's manabase hardest. Routed to **ENGINE/REPRESENTATION** as N-139n (a
count header on the hand block), with the guide keeping one falsity-fact aimed at the specific
invented disqualifier. See skill.md P1.

**(b) The mulligan FLOOR being deferred rather than obeyed** (five mulligans to a 2-card keep).
Tempting as a core line ("never take more than two mulligans"), and it must NOT go there: the
correct number is deck-dependent (a two-colour aggro deck and a five-colour ramp deck do not share
a floor), and the fix that actually works is to key the floor to the string the MULLIGAN PROMPT
already prints (`having already taken N mulligans`) -- which is per-deck wording over an existing
core surface, not new core doctrine. Routed to **PER-DECK**. See skill.md P2.

**(c) The deploy-choice at 8 life (engine over body, lost the vs152 game).** This is posture- and
named-card-dependent by definition: "cast the highest-toughness body when behind" is correct for a
midrange ramp deck and wrong for a combo or a racing deck that should be deploying its clock.
Exactly the case the layer-routing rule names ("a racing deck's 'creatures don't block' promoted
to core would misplay every control deck"). Routed to **PER-DECK**. See skill.md P3.

**(d) Poison counters are invisible.** This is the one item with a genuine cross-deck argument,
and it still is not core: the defect is that a LOSE-CONDITION RESOURCE is absent from the board
snapshot, and instruction cannot substitute for visibility. A core sentence ("remember poison
counters exist") would be a paragraph against an unrendered number -- the exact anti-pattern.
Routed to **ENGINE/REPRESENTATION** as N-139o (HIGH, cross-seat). The GUIDE carries an interim
stopgap keyed to the visible `[infect]` tag on attacker lines, tagged with its removal condition.

### One counterexample against a shipped core-adjacent surface, reported not escalated

i12's count-first mana line (`Mana available: 3 total (three untapped sources ... colours you can
make: {g}{u}{r}{b}{w})`) was PRESENT and LOST once at this seat: vs146 s21 declined two offered
payable creature casts reasoning *"I have no untapped lands left to pay for any of the cards in my
hand"*. One instance, in a game this seat WON, outcome-neutral. Per the wave-10 rung (score the
surface by outcome-neutrality, do not re-open a non-outcome-affecting residual with louder
wording) this is logged and nothing is proposed. If a second seat produces the same
count-present-but-denied shape, the pair becomes a real signal.

### Reconfirmations (no action)

- The restatement ban held: nothing in this seat's revised guide duplicates a core line. The
  interface-contract fact appears once, tied to the named surface ("a Cast line is listed"), which
  is the sanctioned reinforce-when-still-losing form.
- No core stopgap is implicated. Core carries no cast-anchor and none is wanted.
- Reply-protocol layer: the seat's single fallback (vs152 s21) is an all-illegal blocker
  assignment set, correctly rejected by the parser. The observation about its EXIT (silent
  `unparsed_reply` rather than an i5-style loud re-ask) is routed to notes.md as N-139s, not here
  -- it must co-evolve with the blockers parser, not with the prompt body.
