# deck133 wave-26 -- general-suggestions (core / general prompt)

**PASS.** Nothing at deck133's seat routes to the core (general) prompt this corpus. The single
new-work item (the retracted_choice false-fallback, findings 2) is a HARNESS/PARSER-layer item
(the `choiceRetractedNoReplacement` line-anchoring interacting with a premature-answer-then-correct
reply) -- reply-protocol/parser mechanism, NOT core prose. The Arena-edit validation is a DECK-GUIDE
item. Yawgmoth/Liliana/Gray Merchant behavior is deck-specific. Fallbacks at this seat: 1 (the
routed parser item).

One adjacent observation, explicitly NOT routed to core (recorded so synthesis can see it was
considered and declined): the answer-first reply protocol can induce a PREMATURE coded commit
("CHOICE: 0") that the model then overturns in reasoning (findings 2). A core-prompt nudge
("reason THEN commit your CHOICE line") was considered and REJECTED -- it would fight the answer-first
protocol that floors decode-collapse spirals (the wave-21/23 win), trades a proven benefit for a
1/1364 safe-direction non-deciding fallback, and the cleaner fix lives at the parser (findings 2's
PLAN-corroboration discriminator). Layer-routed to notes.md, not core.

Core prompt continues to hold -- **21st consecutive PASS from this seat.**
