# deck22 (Giants!) STEP-0 proposals — wave 26

**PROPOSED rule-3 face (tagged single-seat) — the KEYWORD-TYPE-CONSEQUENCE inference gap.**
Rule 3 names belief-that-VETOES (17/21/59), card-text FABRICATION (62), target-SUB-MENU seam (49),
SYMMETRIC-cost one-sided read (102), ALT-COST DUAL-OPTION mislabel (137, closed), and SYMMETRIC-in-
your-favor recursion (59). deck22 adds a distinct shape: a KEYWORD whose TEXT is correctly rendered
but whose TYPE CONSEQUENCE the pilot fails to infer. Universal Automaton's option line carries
`[changeling] "This card is every creature type."` yet the pilot reasons "it's a 1/1 with no impact"
and "we have no other Giant" while holding a changeling that IS a Giant — mulliganing keepable hands
and bottoming its own enablers (vs131 s1/s4, vs18 s1). Mechanism split from the others: NOT hidden
(changeling text is shown), NOT fabricated, NOT a mislabel (the P/T is right) — the model simply does
not chain "keyword -> type -> tribal role." Sibling to the FOURTH face (number right, sign misread):
here the KEYWORD is right, the TYPE CONSEQUENCE is unread. Corrective template: a rule-#1 flat
identity line ("card X IS a <type> because of <keyword>; it fills every <type> role: enables A,
counts for B/C") tied to the exact downstream roles, carried into mulligan+bottom (cross-phase). Pair
with a representation-ledger annotation (render the tribal-relevant type on changeling bodies where a
restriction/anthem keys on it). Tag single-seat; promote if a second tribal/type-matters deck
(another changeling, a type-restricted attacker, a type-devotion payoff) reproduces the inference gap.

**CONFIRMS (no new rung):** the cross-phase-fact rung (wave-25 headline 3) generalizes beyond mana:
the Automaton-is-a-Giant fact had to reach MULLIGAN + BOTTOM (where it cost games), not just cast.
The Step-0-bis attack-floor rung (rule 5) reproduced again — a tribal midrange deck durdled its 4/3+
bodies behind 1/1 chip (vs133), so the FIRST-pass attack floor over the eligible Giant list was
written, with the two non-Giant utility bodies (Mogg Sentry, Stinkdrinker) named as exclusions.
