# deck131 wave-26 -- general-suggestions.md

PASS.

No core / general-prompt (shared decision-prompt) change PROPOSED from this seat. The seat's findings route
cleanly by layer without touching the shared prompt:
- The favorable-block finding (I-2) is a PER-DECK GUIDE rules-correctness fix (the never-block absolute was
  still too tight), applied to strategy.txt -- not a shared-prompt change.
- The lethal-attack reconciliation (I-3) is a PER-DECK GUIDE clause -- not shared-prompt.
- The Guttersnipe tapped-read guard (I-1) had NO recurrence this corpus -- nothing to route.
- The two wave-25 edits (bottom ladder, lethal-only-blocker) VALIDATED against a CORRECT, complete shared-
  prompt representation (the bottom prompt enumerated the hand + costs cleanly; the blockers prompt printed
  the explicit "LETHAL if it all connects" line the model relied on at vs27 s31). No core representation gap.
- c1/c2/c3/c5 were UNEXERCISED at this counter/bounce/basics-only seat (no target-preview, no shock/pathway,
  no PT-pump, no may-ask). c4 corpus gate PASSED (zero fallbacks; no dropped/mis-picked legitimate reply).

The general prompt's battlefield, mulligan, bottom, and combat representations are CORRECT and complete
here; the residual items are per-deck guide-rule edge cases or stochastic model comprehension, not defects
the core prompt could address.

**TWO below-bar candidates flagged for the synthesis agent (NOT proposals; do NOT break this PASS):**

1. **Affirmative untapped-blocker tagging** (carried from wave-25 I-6, evidence did NOT strengthen). The
   tapped-read misread class had ZERO recurrence this corpus, so the case for tagging untapped opponent
   creatures affirmatively (e.g. "[untapped - can block]") is NO STRONGER than last wave. Still single-SEAT,
   still correct-not-defective representation. Surface to synthesis ONLY on cross-SEAT convergence (a 2nd
   seat reporting a combat tapped-read misplay this wave). Absent that, WATCH.

2. **Answer-first self-coherence (NEW below-bar, I-4).** Two instances this corpus where the model emitted
   its coded / answer-first line (ATTACK: none; PUT: 1,2,4,6) then reasoned in prose to a DIFFERENT
   conclusion it never re-emitted in coded form -- so the parser (correctly, per the answer-first precedence
   spec) took the first line. This is NOT a parser defect and NOT core-fixable without breaking the
   deliberate answer-first design (attack replies ramble AFTER the answer, which is WHY first-line wins).
   Both non-decisive. It is a MODEL-BEHAVIOR observation, single-seat, below the bar to touch the shared
   prompt. Surface to synthesis ONLY if another reviewer reports the same first-line/conclusion divergence
   flipping a DECISIVE decision (cross-seat convergence would justify weighing a "finish reasoning before you
   commit the coded line" protocol nudge against the answer-first rationale). Absent that, WATCH.

Neither candidate is a proposal; both are convergence-gated. The seat's answer is PASS.

This is the 21st consecutive general-prompt PASS.
