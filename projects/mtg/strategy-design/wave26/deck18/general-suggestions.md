# deck18 general-suggestions (core-prompt PASS/candidates) - wave-26 Step-0

Bar is high (20 consecutive core PASSes per wave-25 synthesis). Verdict: **PASS** at
the core layer. Every deck18 finding routes to the deck guide or the engine/harness
ledger; nothing demands a core-prompt edit. Two items logged as SUB-BAR candidates for
convergence-tracking only, neither proposed for this wave.

## PASS rationale

- Leak 1 (Goldmeadow Dodger evasion unused) and Leak 2 (illegal flyer block) are
  DECK-SPECIFIC card facts / a deck-specific board condition (this deck has no reach),
  not universal priors - they belong in the per-deck guide, not core.
- Leak 3 (TRUST-THE-RENDERED-NUMBER tax) already has its home: the representation renders
  "(printed X/Y)" and the guide carries the trust-line. This is the established
  guide-lane + representation-lane split; the wave-25 synthesis explicitly ruled a
  general "commit briefly, don't re-derive" brake a CORE/representation touch only if
  ever wanted, and deferred it as latency-not-correctness. deck18 adds no new argument to
  reopen that - it is one more confirming seat, correctness-neutral.
- Leak 4 (Mobilization interface confusion) is an engine/representation item (activated
  ability surfaced only at priority) -> notes.md, not core.

## SUB-BAR candidate A (NOT proposed) - a universal "you cannot block a flyer without
   flying/reach" mechanics fact in the core prompt.

The core currently carries the FIRST-STRIKE mechanics fact but (per the wave-5 core
summary in the skill) does NOT appear to carry an explicit flying-blocking restriction.
deck18 vs137 s10 is one witness of the model attempting an illegal ground-blocks-flyer
block and spending 9.9k chars on it. Flying-blocking is genuinely universal (unlike the
Dodger evasion), so IF a second seat in another deck reproduces an illegal flyer-block
attempt, a single core mechanics sentence ("a creature without flying or reach cannot
block a flying attacker") would save re-teaching it per ground-only deck - the same
economics as the FIRST-STRIKE core line. Single-seat now, and the deck guide covers
deck18's case, so it stays BELOW core this wave. Flagged for convergence.

## SUB-BAR candidate B (NOT proposed, tracking only) - printed-evasion facts do not
   transfer from mulligan to combat.

deck18's cross-phase failure (knows Dodger evasion at mulligan s1, forgets at attack
s17) is the deck-level instance of a general pattern the wave-25 cross-phase mana-fact
rung already named at the SKILL layer (a fact stated for one phase must be restated at
the deciding seam). This is a GUIDE-authoring discipline, not a core prompt line - the
core cannot carry per-deck evasion facts. No core action; logged so the synthesis agent
can weigh whether the skill-level cross-phase rung deserves a core-layer echo (it does
not, on this evidence - the fix is per-deck restatement, not a universal sentence).
