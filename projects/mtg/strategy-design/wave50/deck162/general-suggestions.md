# deck162 - core-prompt / render proposals (wave 50), layer-routed

G-1 (render, MED - carried R39 / docket D15, NOT SHIPPED this binary). **`{feeds: opponent draws N
extra/turn; punishers on your battlefield: 0}` on engine cast rows.** 0 `{feeds` strings in the 42
seat logs; Rule 2 breaks this corpus 5 of 9 gated windows (`162 vs123` s7/s10/s13/s14, `162 vs146`
s8), the fifth corpus of the shape. With 162-E three of the five are re-scoped as correct (Caress
converts the extra cards); the annotation should therefore also count Caress: `{feeds: +N/turn;
converters on your battlefield: <punishers> + Caress}` - the pilot's own count is the thing that
fails, and the row is where it should be printed.

G-2 (render, MED). **Price the opponent-draws abilities the way DRAW FORECAST prices the draw
step.** Ob Nixilis's -2 row reads `-2: destroy target creature and draw two cards ... targeting
<X>` and nothing about the 2 x P damage those two draws deal under the seat's own punishers; the
seat passed it at their 3 life with P = 4 (`162 vs126` s23/s24) and won a turn later on the draw
step. A `{their draw: 2 cards = 2 x P = <N> life}` clause on the -2 rows (and on Peer into the
Abyss's row: `= half their library x P`) is the same arithmetic DRAW FORECAST already prints for
the draw step, moved to the row that offers the burst. Falsifier: -2 passed with 2 x P >= their
printed life 0/N.

G-3 (render, LOW). **`DRAW FORECAST (theirs)` shipped and held**: 50/50 `DRAW PUNISHERS on the
battlefield: yours` prompts on my seats carry it (lane X prediction PASS; wave-49 D16 verified).

G-4 (core prompt, LOW). **Carried PLAN echo length on the punisher seat**: max identical-PLAN
streak 8 (`162 vs125` s2-s9) and 7 (`162 vs130`, `162 vs126`), all plans that still named
menu actions ("cast Underworld Dreams or Fate Unraveler") and were being executed; 0 verdict
plans ("the game is lost": 0 occurrences on 18 logs). Lane Y's content-keyed expiry is behaving as
designed; the raw-count half of the wave-49 P4 prediction should be retired, not re-filed.
