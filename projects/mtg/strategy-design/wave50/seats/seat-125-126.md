# Wave-50 seat: deck125 (2/6) + deck126 (3/6)

Corpus `matchups-20260827-115759` - SEVENTH fair-hand corpus, first on lanes V/W/X/Y/Z; binary
master 67aeffe45. Guides = wave-49 reviewer revisions (0dac414f2) + boundary pass (ce3c7a90c).
deck125 **298** decisions / 6 games, deck126 **186** / 6 games (484, excluding gamestart/gameend).
The deck123-vs-deck162 dead-loop game is excluded from every corpus-wide number; none of my games
touch it.

**Health (my 12 seat logs).** Fallbacks **3 / 484 (0.62%)**, all deck126, all ONE shape
(`stale_echo`, choice -1, Baka took the window): `vs125` seq 14, `vs162` seq 16, `vs130` seq 13 -
each a `CHOICE: 1 (Cast Sanguine Bond)` / `(Cast Exquisite Blood)` naming a card that was in the
HAND but not on the menu (row 1 was Overgrown Battlement / Chromatic Lantern / Tribute at 0). Not
executed (lane S held); not re-asked either - HIGH #1. deck125 0/298. `answer_replaced` 1 (`deck126
vs146` seq 37 - lane Y's last-clean-line took a mid-reply self-revision `CHOICE: 2 ... CHOICE: 1`,
the revised line was the model's stated final answer; correct outcome). `commit_retracted` 0,
`rejected_line_skipped` / `no_pass_*` / `repeat_count_reask` / `degenerate_decode` 0 on my seats.
Latency: deck125 p50 2.2-31 s per game / max 73.8 s; deck126 p50 12-36 s / max **587 s** (`vs125`
seq 18, a 10,054-char reply on an 8,673-char prompt - deliberation, not the engine), 228 s (`vs146`
seq 28), 178 s (`vs123` seq 19). Prompt chars: deck125 max 25,402 (`vs123` seq 96, a cleanup
`discard` ask at t52 with 22 permanents), deck126 max 25,793 (`vs146` seq 52). No prompt above
30,000; longest option row 786 chars (`deck125 vs152` seq 45); rows > 3,000: 0. Multi-`CHOICE:`
replies: 1 (the seq 37 revision above).

Every card fact below is read off `projects/mtg/bin/Res/sets/primitives/mtg.txt` with
`/usr/bin/grep` (Tribute to Hunger, Emrakul, Supreme Verdict `destroy all(creature)`, Final
Judgment, Shield Sphere, Bloodline Keeper, Peer into the Abyss, Elite Spellbinder `flying`,
Sigarda `flying,trample`, Fog Bank `flying,defender`, Lightmine Field) or quoted from the corpus's
rendered strings. Scryfall not consulted.

**Results.** deck125 **2W-4L**: won vs146 (t41), vs123 (t54); lost vs152 (t26), vs130 (t15), vs126
(t34), vs162 (t13). deck126 **3W-3L**: won vs146 (t25), vs125 (t34), vs130 (t20); lost vs152 (t14),
vs162 (t16), vs123 (t17, at -208). Wave 49 was 3-3 / 4-2 - real hands, n=6, not attributed to
edits.

## NEW-BASELINE record (mulligans)

| seat | offers | mulls taken | kept-hand land counts | bottoming |
|---|---|---|---|---|
| deck125 | 6 | **1** (`vs123` seq 1: a 1-land seven, `would not cover any spell`) | 4, 3, 3, 4, 2 (`vs162`, no coverage, kept - allowed by the odds text), 2 (after mull, coverage Scatter + Elixir) | `vs123` seq 3 Staff of Nin (order: Emrakul absent, no duplicate, most expensive spell) |
| deck126 | 6 | **1** (`vs146` seq 1: a 1-land seven, no coverage) | 2 (after mull, coverage Captain + Battlement), 4, 2, 2, 3, 4 | `vs146` seq 3 Staff of Nin (order: Lantern/Staff first) |

No chain, no mull to 5 or below, no floor text anywhere (both sections are odds-form since wave
49). Skill #131 twelve-hand check run on both sections at write time (see each deck's edit-texts):
no sentence pair disagrees. **No OWNER QUESTION arises.**

---

# deck125 - Revelation Fracture (UW draw-go). 2W-4L

## Prediction set A - wave-49 guide edits (W46-W51)

**W46 (Emrakul taken with Sanguine Bond / Exquisite Blood on their line, or an opposing edict in
the log: 0; taken with none present: N/N) - FAIL 1/1 on the first half, PASS 2/2 on the second.**
`vs126` seq 42 (t28, 33 life): `Cast Emrakul` taken while the opponent battlefield line printed
`Exquisite Blood {4}{b} [enchantment] {effect: "Whenever an opponent loses life, you gain that much
life."}` - and the same reply's PLAN line said "cast it unless Sanguine Bond/Exquisite Blood/Tribute
to Hunger are present". Rule stated and broken in one reply = #102's exhaustion shape. The cast
itself did not lose the game (no edict came); what followed did - see the loss. Clean takes:
`vs146` seq 60 (t37, Kaya/Barrowin board, no converter; extra turn, 18 -> 3) and seq 64 (t40, won).
Edit W20 (re-keyed to the printed `{effect:}` text, no restatement) + render proposal G1.

**W47 (Staff from `taps you out` / `leaves 4` or less with a counter in hand: 0; plan-line carry
while breaking: 0) - FAIL 2/5 casts as written; 0/5 under the rule as re-derived.** `vs162` seq 20
(`leaves 0`, Essence Scatter in hand - against a line of Underworld Dreams x2, Dictate of Kruphix,
Howling Mine: the Scatter could not have countered anything that deck cast; the seat died next
turn to Peer into the Abyss (`{4}{b}{b}{b}` sorcery, draws half the library under two Dreams),
which no Scatter stops); the plan line carried "leaves 5 or more" in the same reply (1 carry-and-
break). `vs123` seq 29 (`leaves 2`, two Scatters + Gavel in hand, bare reply; won t54). Legal:
`vs152` seq 22 (`leaves 0`, no counter), `vs146` seq 52 (`leaves 7`), `vs123` seq 86 (`leaves 12`).
Seventh corpus of the fixed-5 gate failing and the second in which the break cost nothing: the
gate is over-strict, not under-obeyed. Edit W22: N is priced against the printed cost of the
cheapest counter in hand that can hit what the opponent casts (Scatter = 2, creature spells only;
Cancel/Fracture = 3; Gavel = 5). Under that rule seq 20 (Scatter useless) and seq 29 (2 >= 2) are
both legal and the wave-49 `vs152` loss (taps-out with a Scatter against a creature deck) stays
illegal. Plan line re-worded to carry the new key.

**W48 (Revelation from `max affordable X=1/2` at life > 10: 0; X = 3 whenever offered: N/N) - PASS
0/16 windows at X=1/2 (all `Cast nothing`); X = 3 taken 6/6** (`vs152` 30, 37, 41; `vs146` 19;
`vs126` 24; `vs123` 48). Casts at 13, 10 and 8 life took X = 3 off menus reaching 4, 5 and 7 - as
written. Ceiling.

**W49 (Verdict / Final Judgment at a header second number of 0: 0; Lightmine off a `deals 0` row:
0) - FAIL 1 + PASS 0.** `vs126` seq 44 (t29): Supreme Verdict off `{right now: destroys 10 of their
creatures (all of them carry a restriction against attacking), 1 of yours}` - ten walls and the
seat's own Emrakul, cast the turn before. The row printed the `1 of yours`; the guide's sweeper
opener says a wipe "only ever costs the opponent". STRATEGY (read right, decided wrong; the guide
had no Emrakul-out clause). Edits W21 + the opener. Lightmine: 0 casts at `deals 0`; 41 sweeper
windows at 0 declined (`vs146` seq 27-48 x14, `vs123` seq 41-95 x13, `vs126` 39-40, others). Two
sweeper casts at a header of 1 belong to the exactly-1 branches: `vs152` seq 13 (Elite Spellbinder
3/1 flying, no Path in list, 17 life - the branch says power 4+ or life <= 10: break, recorded) and
`vs146` seq 46 (a Spirit 1/1 token with a Path in the list - the Path section forbids a 1-power
token and the sweeper section says "take the Path": a contradiction, W23 resolves it to "cast
nothing"). Lightmine DECLINED at `they control 3 creatures able to attack` (`vs146` seq 61) - the
reply read the `[tapped]` tags as "no creatures able to attack"; won next turn regardless. W24.

**W50 (Path at a `[defender]` of either form or a textless 1-power token above 12: 0; Path at
<= 12 with a power-2+ candidate: N/N) - PASS 0 + PASS 2/2 (first measurement after three UNTESTED
waves).** `vs162` seq 9-10: the row offered `Shield Sphere (artifact creature 0/6) [defender]` and
`Fate Unraveler (3/4)`; the target ask took Fate Unraveler. `vs152` seq 34-35 (10 life): Augur of
Autumn 4/5 and Luminarch Aspirant 1/1 (`At the beginning of combat`) offered, Aspirant taken - the
engine-text rule outranks power, as written. `vs152` seq 45-46 (7 life): Tovolar's Packleader 7/7
taken over Augur. `vs146` seq 53-54 Triumphant Adventurer (venture, correct), 56-57 Spider 2/1.

**W51 (land drop N/N; Elixir activation to `Mana available: 2` or less with a counter in hand: 0)
- land drop PASS 72/72; Elixir FAIL 2 (was 5).** `vs123` seq 8 (t2: `Mana available: 2` -> 0 with
Essence Scatter + Fall of the Gavel in hand, 20 life, library 53 - a spend with nothing to buy),
`vs126` seq 27 (6 -> 4 with Gavel). Neither cost a window. Elixir CASTS at `leaves 0-1`: 3, legal.
The activation gate is re-keyed with W22 (same "cheapest counting counter" price).

## The losses (deck125)

- **vs deck126 (t34) - Emrakul into a converter, then a Verdict that killed it.** Dream Fracture
  countered the first Exquisite Blood (t26, seq 37); the second resolved t28 with only Essence
  Scatters in hand (creature-only - no window). Seq 42 (t28): Emrakul cast with the Blood printed
  on their line (W46). Seq 44 (t29, the extra turn): Supreme Verdict off `... 1 of yours` - killed
  ten walls and the Emrakul (W49/W21). Sanguine Bond resolved t31 (hand: Scatter x2, Verdict, Path -
  nothing that targets an enchantment), Sorin t33, Staff t35 + ping -> loop, 33 -> 0. Two guide
  breaks and one no-answer hand; the breaks threw away the 15/15 that would have attacked for
  lethal on the extra turn (opponent at 20, no flier among ten walls: annihilator 6 + 15 damage).
- **vs deck152 (t26) - fliers and a Teferi lock, no guide lever.** Verdict at a lone 3/1 flier
  (seq 13, recorded), Staff at `leaves 0` with no counter (legal), Verdict #2 at 3 creatures (seq
  26), Revelations at X = 3, Paths at Aspirant and Packleader. Teferi's +1 tapped the Staff and a
  land each turn from t21; Augur 4/5 + Packleader 7/7 + Lair of the Hydra took 16 -> -5. Every
  counter window was taken on sight (Katilda t6, Aspirant t?); the hand ran out of Paths at 6
  creatures.
- **vs deck130 (t15) - land destruction on the only white source, no lever.** Molten Rain took
  the Seachrome Coast (t6, no counter in hand), Dream Fracture countered Stone Rain (t8, guide
  silent - correct), Lay Waste resolved t10 (no counter), Rorix Bladewing (6/5 flying haste) t12
  with Path + Verdict in hand and ZERO {W} sources on the battlefield; 18 -> 12 -> 6 -> dead to
  Siege-Gang. 12 decisions. Nothing in the guide reaches a hand whose only white land is destroyed
  on turn 6; the mulligan keep (3 lands, one {W} source) was inside the odds text.
- **vs deck162 (t13) - Peer into the Abyss under two Dreams.** Underworld Dreams t6 (no counter),
  Fate Unraveler t8 (Pathed t9 - W50), Dreams #2 + Dictate t10 (no counter that could target them:
  hand held Revelations, sweepers, Path), Gavel countered Ob Nixilis t12 (18 life), Staff at
  `leaves 0` t13 with a useless Scatter (W47), then Peer into the Abyss: half the library drawn
  at 2 per card. The seat kept a 2-land no-coverage seven (seq 1) and drew lands on time; not a
  mulligan loss.

## Other counted behaviour (deck125)

- Cleanup `discard` asks (new kind): 9 across 4 games, all rendered as `Cleanup discard (hand N,
  limit 7): you chose ...` before the `You discarded` lines (lane W). The choices had no guide text:
  `vs123` seq 52 sent two Essence Scatters from a hand of six counters + Path + Emrakul at 34 life
  on 16 lands (Emrakul castable in 0-1 turns - defensible), seq 76 a Cancel over a Lightmine Field
  (six counters in hand, Lightmine the dead card), seq 50 Final Judgment + Emrakul + Island (fine),
  seq 96 Lightmine + Verdict (fine); `vs130` seq 11 a duplicate Verdict, seq 12 a Revelation at 4
  life under LD (fine). W25 adds a short cleanup order.
- Counters cast: 14; targets by shape all inside the list (Katilda `{T}: Add`, Sorin, Exquisite
  Blood x2, Stone Rain, Ob Nixilis, Lightning Greaves?, ...); walls on the stack countered 0/N.
- Elixir library floor never fired (min library 24, `vs123` seq 94).
- `[DRAW PRICE:]` on the Revelation row under two Dreams (`vs162` seq 20: "at X=3: 3 cards = 6
  life") - read and obeyed (0 Revelation casts under a punisher, 5 windows).

---

# deck126 - Sanguine Blood (WGB wall prison). 3W-3L

## Prediction set A - wave-49 guide edits (D43-D50)

**D43 (both enchantments out + `Cast Tribute {right now: they control N}` N >= 1 on the menu: taken
in the FIRST such window N/N; 0 "wait for the opponent to attack") - PASS 1/1, 0 replies.** `vs130`
seq 33 (t20): first both-out window with N >= 1 (`they control 4 creatures`) - taken, won on the
spot (Goblin sacrificed, +1, loop to 0). Both-out windows at N = 0 (`vs130` seq 25-31, `vs125` seq
29-31): `Cast nothing` / Sorin / Staff, correct; `vs125` seq 31-32 cast Staff and pinged - Rule 7a
as written, won. `vs146` seq 54-57: both out, no Tribute in hand, attacked with the Vampire - won.
`vs162` seq 23-24: both out at 3 life, no Tribute in hand (both spent - see F17), Wall of Omens
priced `[DRAW PRICE: ... costs you 4 life right now]` - `Cast nothing`, correct; died to the draw
step.

**D44 (second copy of a name already out: 0; "both enchantments" with a name absent: 0) - PASS
0/0 + 0.** `vs125` cast Exquisite Blood twice (seq 24, 26) because the first was countered (Dream
Fracture) - the battlefield line did not show it. `vs130` seq 18/24, `vs146` 27/53, `vs162` 18/22,
`vs152` 19, `vs123` 19: every enchantment cast the turn its row appeared (Rule 7 entry 1 PASS 9/9).

**D45 (Blood/Bond declined for a wall or Sorin when missing and on the menu: 0) - PASS 0/9.** One
adjacent break: `vs162` seq 6 (t4) declined Idyllic Tutor (entry 2, no piece in hand) for an
Overgrown Battlement (entry 4) - the same window as the Tribute miss below; recorded under F17.

**D46 (Tribute at `1 is a creature`: N/N; at 0: 0) - FAIL 1 window, then 3/3; 0 at zero.** The
row now NAMES the victim: `{right now: they control 1 creature - Shield Sphere is sacrificed, you
gain 6}` (lane T/X). `vs162` seq 6 (t4): that row declined for a Battlement, with the reply's own
plan saying "use Tribute only if the opponent has exactly one creature"; seq 8 (t6) took it (+6).
`vs162` seq 11 (`Fate Unraveler is sacrificed, you gain 4`), `vs123` seq 8 (`Thraben Doomsayer ...
gain 2`, the FIRST window the Doomsayer existed), seq 12 (`Bloodline Keeper ... gain 3`) - all
taken in their first window. Zero-stop: ~40 windows at `at 0 this does nothing`, 0 casts. Edit F16
re-keys 7b to the row string; F17 carves out the [defender] victim (below).

**D47 (Lantern with an entry 1-6 row beside it or with one out: 0) - FAIL 1 + 1 (was 2 + 4).**
`vs130` seq 6 (t4): Lantern over a Pride Guardian row. `vs146` seq 37 (t19): a second Lantern with
`Chromatic Lantern` on the battlefield line, only row on the menu - the reply first wrote `CHOICE:
2 (Cast nothing)`, argued itself into "I do not have a CL on the battlefield" (false - the line
showed one) and ended `CHOICE: 1`. Only-row Lanterns taken with none out: `vs146` seq 24 (correct).
Third corpus; the rule is keyed to the printed line already. Counts updated (F20), no restatement.

**D48 (a blocker off an all-`your blocker dies` B-row at `you would be at 9` or more with no
enchantment: 0) - PASS 0/1.** `vs146` seq 9 (t6, `you would be at 18`): B1 Battlement `may block A1
(your blocker dies, attacker lives)` on a deathtouch Adventurer - `BLOCKS: none`. At `you would be
at -11` (`vs152` seq 20) both walls blocked, as the <= 8 clause says - but on the two `neither
dies` 2-power attackers instead of the 4-power lifelink `your blocker dies` one the clause names
("biggest deals N"); two fliers (5 + 6, `NONE of your available blockers can block`) made it dead
either way. Recorded.

**D49 (mulligans) - PASS** (table above).

**D50 (all-mana single-row windows reaching the model: 0; skips per game) - PASS 0 (was 6).**
`mana_only_windows_skipped`: 203 / 38 / 62 / 46 / 10 / 20 (`vs146`, `vs152`, `vs125`, `vs162`,
`vs123`, `vs130`). No `priority` window on any deck126 seat had a lone `Add N green mana` row -
including the three post-counter windows of the wave-49 shape (`vs125` seq 24-27 region). Lane Z's
D12 closed on this seat.

## The losses (deck126)

- **vs deck123 (t17, -208) - Bloodline Keeper #2 with no Tribute left, after a cleanup discard
  threw the third one away.** Kept 3 lands + Tutor + Tribute x3 (coverage named all three). Seq 7
  (t4 cleanup, hand 8 -> 7): `PUT: 1` = a Tribute to Hunger, from `Tribute x3, Chromatic Lantern,
  Wall of Omens, Perimeter Captain x2, Sanguine Bond` - the Lantern was the card to send (the
  bottoming order already says so; the cleanup ask had no text). Tribute #1 ate the Thraben
  Doomsayer t6 (seq 8, first window - the wave-49 255-Human lesson held), #2 ate Bloodline Keeper
  t11 (seq 12). Damnation t12 wiped the walls. Bloodline Keeper #2 resolved t14 under Intruder
  Alarm; t15 the seat Tutored for Exquisite Blood (seq 16: Lantern / Tutor / nothing on the menu -
  no Tribute); t16 upkeep: `Create vampire with Bloodline Keeper (x30)`, transform, `Lord of
  Lineage (x27)` - 57 Vampires, 20 -> -208 at t18. STRATEGY at seq 7 (the discard), F18. The 57-
  activation upkeep is the dead-loop class (engine INFO #3).
- **vs deck162 (t16) - the draw lock, with the second Tribute spent on a wall.** Seq 6 declined
  Tribute at `Shield Sphere is sacrificed, you gain 6` (D46 break), seq 8 took it (+6, 18 -> 24),
  seq 11 took `Fate Unraveler ... gain 4` (the right one). Underworld Dreams x3 + Howling Mine + Ob
  Nixilis from t8; Sanguine Bond t13 (seq 18), Exquisite Blood t15 (seq 22) at 3 life - BOTH OUT
  with the opponent controlling two Fog Banks (`2 creatures, 0 without a restriction`) and no
  Tribute in hand: a Tribute here wins on the spot (Fog Bank sacrificed, +2, loop). The Shield
  Sphere Tribute was the card. F17: a lone [defender] victim is entry 8, not 7b.
- **vs deck152 (t14) - two fliers, no lever.** Elite Spellbinder 3/1 flying (t7) and Sigarda 6/6
  flying trample (t13) - `NONE of your available blockers can block this attacker` on every A-line;
  Tribute at 4 creatures (seq 11, 14 life, opponent chose Briarbridge Tracker) gained 3. Staff t12,
  Bond t14 at 8 life. 17 -> 13 -> 8 -> -7. Rule 4's flier clause needs "their ONLY creature"; it
  never was.

## Other counted behaviour (deck126)

- The three `stale_echo` fallbacks (HIGH #1): each reply named an enchantment IN HAND whose pips
  the untapped lands could not pay (`vs125` seq 14: Sanguine Bond with `{B} 1` on the mana line;
  `vs162` seq 16: Bond with 4 sources; `vs130` seq 13: Exquisite Blood at 4 mana). PERCEPTION (a
  hand card read as a menu row) - F19 adds the one line Rule 7 lacked; the engine side is #1.
- Attack rule: 9 attackers windows, all on `[no creature they control can block this attacker]`
  or bare rows: sent 7, `no attackers` 2 (`vs146` seq 39, 43 - Nadaar 4/4 vigilance untapped on
  the tag; correct).
- Tribute at 2+ creatures (entry 8): `vs146` seq 15 (3 creatures, 15 life, only other row a
  Battlement - the reply called the Adventurer "their only untapped threat"; +1), `vs152` seq 11,
  `vs130` seq 21 (4 creatures, 20 life, Goblin) - three casts that removed a token or the worst
  body. Recorded; entry 8 stands ("nothing above it matched").
- Rule 7a with both out and 0 opposing creatures: `vs125` seq 29-33 Sorin, +1, Staff, ping -> win
  (the wave-49 "45 turns without a trigger" shape did not recur: the Staff was drawn).

---

# Prediction set B - engine lanes (where my seats had windows)

| lane | verdict | evidence |
|---|---|---|
| **V** d9144d62c - casts with tapped == 2x pips 0/N; counter-then-payment 0 | **PASS by receipt**: one `- Paid` per mana payment on all 12 logs (deck125: 14/14, 1/1, 18/16 casts + 2 Elixir activations, 8/7 + 1, 3/3, 24/20 + 4; deck126: 16, 6, 18, 12, 9, 8 = casts exactly); a countered cast (`deck126 vs125` t26 Exquisite Blood, `deck125 vs126` seq 37) shows ONE Paid line. Tapped-count arithmetic not traced (no stderr read). | `- Paid {15} for Emrakul, the Aeons Torn with Island #1, Glacial Fortress #1, ... ` (`deck125 vs126` log t29) |
| **Z** 5b6b44c44 - repeat-row arithmetic; mana-only leaks 0; one Paid per payment; generic-from-dual 0/N | **PASS mana-only 0 (was 6, D50); PASS one-Paid; repeat rows: no token-maker repeat row on these decks (Sorin +1 is a single activation) - UNTESTED; generic-from-dual: NOT traced by pip** (every Paid line names sources, e.g. `Paid {1}{u}{u} for Dream Fracture with Island #1, Glacial Fortress #1, Plains #1` - the Plains on generic is right; no line was checked against the untapped basics left over) | `mana_only_windows_skipped` 203/38/62/46/10/20 |
| **X** c37805358 - gain recipient 0 wrong; no row > 3,000 / prompt > 30,000; `(0 able to attack)` 0; named-card casts 0; FORECAST theirs; MDFC | **PASS on all my windows**: Tribute rows `you gain N` (caster gains - primitive `toughnesslifegain targetopponent` on the caster's side, and the narration `You gained N life` confirms 8/8); max row 786, max prompt 25,793; `(0 able to attack)` 0 occurrences - own-turn sweeper rows now read `(N without a restriction against attacking)` / `(all of them carry a restriction against attacking)` / `(it carries a restriction against attacking)` and the Lightmine row `they control N creatures able to attack - deals N to each if all N attack` / `deals 0 until they have an attacker`; `[named: Vampire]` printed on Silverquill Silencer (`deck126 vs146` seq 37 line). FORECAST-theirs and MDFC: no window on my seats. | the `, 1 of yours` tail on `deck125 vs126` seq 44 is the row carrying the fact the seat ignored |
| **Y** 7858a403f - verdict-tail executions 0; exemplar failures 0; `CHOICE: 0` escapes 0; "game is lost" carries 0; garbage decodes 0 | **PASS by absence**: 0 `CHOICE:` lines with a verdict/negation tail on 484 replies; 0 `CHOICE: 0` on a pass-less ask; "game is lost" 0; `degenerate_decode` 0; the one `answer_replaced` (`deck126 vs146` seq 37) executed the LAST clean line of a self-revising reply - the model's stated final answer. | |
| **W** b39b80375 - foreign-chooser reveal message 0; edicts with no sacrifice 0/N; every cleanup `You discarded` preceded by a `discard` record N/N; self-target rows taken 0 | **PASS 0 / PASS 0/8 / PASS 9/9 / PASS 0/8.** Tribute resolutions: `vs146` seq 15 Goblin (+1), `vs152` seq 11 Briarbridge Tracker (+3), `vs162` seq 8 Shield Sphere (+6), seq 11 Fate Unraveler (+4), `vs123` seq 8 Thraben Doomsayer (+2), seq 12 Bloodline Keeper (+3), `vs130` seq 21 Goblin #1 (+1), seq 33 Goblin (win) - each with `Opponent's <name> died` + `You gained N life`; `none was a legal target` 0 in the whole corpus (21 logs grepped). Cleanup: every `You discarded` on my 12 logs sits under a `Cleanup discard (hand N, limit 7): you chose ...` line and a `discard` record (deck125 5+1+2+1, deck126 1). `{this hits YOUR permanent}` rows: 8 offered (deck126), 0 taken. The wave-49 HIGH #1 (stale Predation reveal) did not recur - `deck126 vs146` again faced a reveal-heavy deck146 and its Tribute at seq 15 sacrificed. **Non-recurrence of the exact prior loss shape (#132): the deck126-vs-146 Tribute resolved correctly.** | |

---

# Engine / render items (ranked, with repro)

Corpus root: `~/.Wagic/ai/gpt/selfplay-runs/matchups-20260827-115759/`

**HIGH - #1: a `CHOICE: <in-range index> (<card not on the menu>)` reply is classified
`stale_echo` and falls back to Baka with NO re-ask - three times, same shape, one seat.**
`1787849909-ai_baka_deck126-0x5601b9f034e0-vs-ai_baka_deck125.jsonl` **seq 14** (t13): menu =
`1. Cast Overgrown Battlement ... 2. Cast nothing right now`; reply `CHOICE: 1 (Cast Sanguine
Bond)` (Bond in hand, `{B} 1` on the mana line, uncastable); record `choice: -1, chosen_text: "",
fallback: stale_echo, coded_answers: 1`; next events `- Paid {1}{g} for Overgrown Battlement` (Baka
cast the wall). The previous reply (seq 13) was `CHOICE: 1 (Play Savannah)` - nothing was echoed;
the label is wrong. Same at `1787849911-ai_baka_deck126-0x55fc821cb030-vs-ai_baka_deck162.jsonl`
seq 16 (`CHOICE: 1 (Cast Sanguine Bond)` on a Lantern/nothing menu; Baka cast the Lantern; here
the prior reply WAS `PUT: 31 (Sanguine Bond)`, so the echo heuristic had a hook) and
`1787849923-ai_baka_deck126-0x56221e2c00d0-vs-ai_baka_deck130.jsonl` seq 13 (`CHOICE: 1 (Cast
Exquisite Blood)` on a Tribute-at-0/nothing menu; events empty). Lane S's contract was "off-menu
name replacements executed 0" (held) and "index-past-menu -> `named_row_reask`"; an in-range
index with an off-menu name is the gap between them. Proposal: one `[RE-ASK]` that says the name
is not on the menu and, when it is in the hand, why (`Sanguine Bond is in your hand but its
{3}{b}{b} cannot be paid: {B} 1`) - the model's answer was a perception error the render can
correct in one line, and Baka's pick (a wall / a Lantern) is not what the model would have chosen
from the real menu. Rate: 3/186 deck126 decisions; 0.62% of my 484 against the 0.10-0.31% baseline.

**MED - #2: a countered spell leaves no trace on the battlefield line the model reads at its
next own-turn window, so "cast the copy" and "the pair is out" share a surface.** Not a bug -
recorded as the reason D44's `vs125` seq 24/26 double Blood is legal, and why `deck126 vs146`
seq 37's reply could argue "I do not have a Lantern on the battlefield" against a line that
printed `Chromatic Lantern` (that one is pure misread; no render item). No proposal.

**INFO - #3: the unbounded-legal-loop class, second instance this corpus.**
`1787849921-ai_baka_deck126-0x556ae9dc1060-vs-ai_baka_deck123.jsonl` last prompt, t16 opponent
upkeep: `Opponent used: Create vampire with Bloodline Keeper (x30)` -> transform -> `Create vampire
with Lord of Lineage (x27)` under Intruder Alarm - 57 activations in one upkeep by the deck123
seat, which then attacked for 228. Same class as the brief's deck123-vs-162 dead loop (there 1,867
activations); here the model stopped. The engine seat owns the item; this is the second citation.

**LOW - #4: `destroys 1 of their creature` (singular "creature" after "1 of their") on the
Supreme Verdict row** - `deck125 vs152` seq 13, `vs146` seq 46, `vs162` seq 16-20. Cosmetic.

**LOW - #5: latency 587 s on a 10,054-char reply** (`deck126 vs125` seq 18, kind ask, prompt
8,673 chars) - the model deliberated, the parse succeeded; no engine action. 228 s / 178 s siblings
on the same seat.

**INFO - lane X's `, 1 of yours` tail did its job and was overruled by the guide's own sentence**
("a board wipe only ever costs the opponent") - fixed in the guide (W21), not the render.

---

# Rotation evidence

- **deck125 - KEEP IN ROTATION.** 2-4; two losses had no lever (LD on the only white source; a
  Peer into the Abyss under two Dreams), one had a partial lever (fliers + Teferi), one was two
  guide breaks in two turns (Emrakul into a converter, then a Verdict that killed it - W20/W21).
  Ceilings: X = 3 6/6, Revelation-at-1/2 0/16, walls-on-the-stack 0/N, Path-at-defender 0, land drop
  72/72, sweepers at 0 (41 windows) 1 - and that one is the Emrakul-out shape. The Staff gate is
  re-derived, not restated (W22) - re-test before any freeze talk.
- **deck126 - KEEP IN ROTATION.** 3-3; the Tribute-at-1 rule reached 3/3 after one miss and the
  row now names the victim (F16); two of three losses trace to a Tribute spent or discarded that
  a later window needed (F17, F18 - both new shapes: a [defender] victim, a cleanup discard); the
  off-menu-name fallback (F19 + HIGH #1) is a third new shape. Re-test F16-F19 before freeze talk.

# Lategame specimen candidate

Primary: `1787849917-ai_baka_deck125-0x55f933f33530-vs-ai_baka_deck123.jsonl` **seq 96** (t52,
25,402 chars, kind `discard`: 22 own permanents incl. two Staffs, an 18-permanent opponent line with
two Intruder Alarms and 0 creatures, a 9-card hand priced for the cleanup ask - the first corpus's
largest `discard` window). Alternate: `1787849885-ai_baka_deck126-0x5621009938d0-vs-ai_baka_deck146.jsonl`
**seq 52** (t25, 25,793 chars, both halves out, Sorin + Vampire, the win-button turn).

# Falsifiable predictions for wave 51

deck125:
- **W52** - `Cast Emrakul` taken while the opponent battlefield line prints an `{effect:}` reading
  "you gain that much life" or "loses that much life", or the log shows an opposing `Gain life
  equal to its toughness with Tribute to Hunger` / Devour Flesh: **0** (was 1/1); taken with none
  present: **N/N**.
- **W53** - Supreme Verdict / Final Judgment cast from a row whose tail reads `1 of yours` (or
  more) with an Emrakul of the seat's on the battlefield: **0** (was 1); at a header second
  number of 0 otherwise: **0**.
- **W54** - Staff of Nin / Elixir activation / Revelation taken from a row whose `leaves N` (or
  post-activation `Mana available:`) is below the printed cost of the cheapest counter in hand that
  can hit what the opponent has cast (Scatter counts only against a creature-casting opponent): **0**
  (the old fixed-5 rule read 2/5 + 2); casts that the old rule forbade and the new one allows:
  report the count and whether any lost a window.
- **W55** - sweeper cast at a header second number of exactly 1 whose creature prints power 0 or 1
  with no engine text, or a Path at it: **0** (was 1 + 0); Lightmine declined at a row reading
  `they control 3 (or more) creatures able to attack`: **0** (was 1).
- **W56** - cleanup `discard` asks: a counterspell sent while a Lightmine Field, a duplicate land
  or a second sweeper is in the list: **0** (was 2/9); Emrakul kept over a counter with fewer than
  13 lands on the battlefield line: **0**.
- **W57** - land drop **N/N** (72/72); X = 3 **N/N**; Path at a `[defender]` or textless 1-power
  token above 12: **0**. Regression detectors.

deck126:
- **D51** - Tribute taken in the FIRST window whose row reads `<name> is sacrificed, you gain N`
  with the named creature NOT tagged `[defender]` on their line: **N/N** (was 3/4); replies in
  such a window saying "only if exactly one" / "if it becomes": **0**.
- **D52** - Tribute cast at a row naming a `[defender]` victim (Shield Sphere, Fog Bank) with an
  entry 1-6 row beside it, at life above 10 and without both enchantments out: **0** (was 1);
  both-out windows with the opponent at 0 non-defender creatures and a Tribute in hand: taken
  **N/N**.
- **D53** - cleanup `discard` sending a Tribute to Hunger while a Chromatic Lantern, a Staff of
  Nin, a duplicate wall or a land beyond the sixth is in the list: **0** (was 1/1).
- **D54** - `stale_echo` / any fallback whose reply names a card in HAND that has no `Cast` row:
  **0** (was 3); replies naming an off-menu card at all: report.
- **D55** - Chromatic Lantern with an entry 1-6 row beside it or with one out: **0** (was 1 + 1,
  third corpus); a reply asserting a name is absent from a battlefield line that prints it: report.
- **D56** - blocks at `you would be at 8` or less: the all-`dies` blocker placed on the attacker
  with the biggest `deals N`: **N/N** (was 0/1); D48 regression **0**; mulligan/bottoming as D49.
- **D57** - all-mana single-row windows reaching the model: **0** (0/0 this corpus); Tribute at
  `at 0 this does nothing`: **0**.

# What I did NOT check

- I did not run wagic, touch `bin/Res`, `src/` or git, or read `game-*.stderr`; JSONL +
  `results.tsv` only. All guide work is under `wave50/`.
- Lane V's tapped-count arithmetic and lane Z's generic-from-dual payment order: the `- Paid`
  receipts were counted (one per payment) and eyeballed, not parsed pip by pip against the
  untapped basics left over.
- Lane X's `DRAW FORECAST (theirs)` and MDFC `(text omitted)`: no window on my seats.
- Lane Y's exemplar and `CHOICE: 0` branches: absence only (0 occurrences), no positive test.
- HIGH #1's classifier path (`stale_echo` vs `named_row_reask`) - inferred from the three records'
  fields, not stepped in code.
- Whether deck125's `vs146` seq 60-64 Emrakul returned to hand between t38 and t40 (Kaya's ability
  is the likely cause; the second cast won the game).
- Corpus-wide fallback rate - not recomputed (my 12 logs only: 3/484).
- Scryfall: nothing cross-checked; every card fact is the primitive's text=/auto= line or the
  engine's rendered string.
