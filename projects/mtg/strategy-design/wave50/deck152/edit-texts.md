# deck152 - wave-50 edits (corpus `matchups-20260827-115759`, deck152 5-1)

Baseline = live `bin/Res/ai/baka/deck152_strategy.txt` (43,423 bytes, wave-48 revision + wave-49
boundary pass B2). After = `wave50/deck152/strategy.txt` (45,182 bytes, +1,759). Three edits.

| # | section | before -> after (abridged) | finding (file, seq) |
|---|---|---|---|
| 152-E | YOUR LORDS AND GROWTH (new bullet after Ranger Class) + DECIDING | new: *"LAIR OF THE HYDRA is a land that taps for {G} and, for {1}{G}, becomes a 1/1 Hydra until end of turn (the row reads 'becomes a 1/1 hydra with Lair of the Hydra [cost: {1}{g}]'). It is offered in your UPKEEP and again in your main phases. Two forms of that row are a PASS on sight: one printed '[this land is TAPPED: animated, it still cannot attack this turn]' ... and one printed '[repeat: activated this turn N times already]' ... Otherwise animate it in MAIN 1 only, only with mana left after the creature you are casting this turn, and only when its A-line would read '[no creature they control can block this attacker]' ... WHAT THIS COST: ... eight mana for no damage."* DECIDING: *"The Lair of the Hydra row prints '[this land is TAPPED: ...]' or '[repeat: activated this turn ...]' -> pass ..."* | The live guide had NO Lair text at all. Lair offered 34 times on the six seats (19 Upkeep, 13 Main 1, 1 Main 2, 1 Blockers), taken 5: `152 vs125` s16 (row read `[this land is TAPPED ...]`), `152 vs126` s24 (same tag), `152 vs125` s59 and s73 (row read `[repeat: activated this turn 1 times already ...]`, paid twice more for the same 1/1), plus one clean take (`152 vs125` s58). Row literal verified against the emitter: `becomes a 1/1 hydra with Lair of the Hydra [cost: {1}{g}]` (#128). Engine detail: the row fixes X at 1. |
| 152-F | #4 RULE, the Katilda sentence after the ladder + DECIDING | *"Katilda is a mana source, not a trading piece: she blocks to save your life, never to swap with a 1/1 mana creature - and she joins INFECT blocks only from '8 of 10' ..."* -> *"KATILDA'S B-ROW: a Katilda line whose parenthesis reads '(both die)' or '(your blocker dies, attacker lives)' is not assigned while the header's 'you would be at N' is 10 or more. She is your mana engine and every Human's mana ability, and a token is never worth her. At N of 9 or less she blocks like everything else, and she joins INFECT blocks only from '8 of 10' ... WHAT THIS COST: at 17 life this seat put Katilda in front of a 1/1 Goblin token on a '(both die)' line, and at 20 life in front of a 3/3 Human token on another; both times she died for a token ..."* DECIDING adds the same rule as one line. | `152 vs146` s22 (T14, 17 life, `B1. Katilda (1/1) - may block A1 (both die), A2 (your blocker dies ...)`, reply `BLOCKS: B1:A1`; Katilda died to a Goblin token, `you would be at 13`); `152 vs123` s19 (T9, 20 life, `B1. Katilda (3/3) - may block A1 (both die)`, blocked a 3/3 Human token, `you would be at 17`). The old sentence named "a 1/1 mana creature" (the story) - the failing window was a 1/1 token, so per #126 the key was the anecdote, not the surface; re-keyed to the B-row parenthesis and the header N, which is what the ladder already reads. |
| (B2 carry) | MULLIGAN | unchanged from the wave-49 boundary pass; #131 re-run on the twelve hands after this wave's edits: no disagreement. | - |

## Read but NOT edited
- **Rung-1 free kill declined once** (`152 vs146` s14, T10, `B1. Elite Spellbinder (4/2) - may block A1 (you kill it, your blocker lives)`, reply `BLOCKS: none`, 1 damage taken). The rung is absolute already ("no number on this screen can make this block wrong"); first instance on this seat; recorded, not restated (#102).
- **Intrepid Adversary sent into three `(neither dies (lifelink: ...))` walls with two attackers vs three blockers** (`152 vs126` s17): the outnumber rule says home; it gained 3 life from the lifelink block and lost nothing. Single, no cost.
- **Katilda's counter ability**: offered 4 (3 Upkeep, 1 Main 1, all `152 vs126` s21/s29/s35/s38), taken 0 -> 152-C holds a second corpus.
- **Converter board, one half only** (`152 vs126` s40, Sanguine Bond alone, opp 8): the reply's first line was `ATTACK: A1, A5` (fliers only, the guide's posture); its last coded line, after 3,600 chars of reasoning that correctly derived "Sanguine Bond alone loops nothing and their walls print no blocking trigger", was `ATTACK: A1, A2, A3, A4, A5` and lethal was dealt (32/-7). `attack_last_line_taken` executed the revision by design. No edit: the guide's one-name posture ("race it with your EVASION ... send every creature whose A-line prints no '(blocking trigger:' and no stop") already permits the all-in on that screen.
