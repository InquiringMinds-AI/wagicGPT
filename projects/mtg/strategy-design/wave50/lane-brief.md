# Wave-50 step-1 lane brief (from wave49/engine-ledger.md — THE DOCKET; read your items there IN FULL)

Base: master at the wave-49 close (engine 7e94544f1; guides/synthesis commits after it carry no
engine change). Baselines: PARSETEST 1783/0; suite 1139/0 + 37 AI/0, exactly 1 `W43-1 ASSERT`,
0 timeouts. Doctrine: enforce legality without constraining choice; every parse/render change
ships PARSETEST cases (positive, must-NOT-match negative, echo shape); every engine change ships
a suite pin; byte-splice Windows-1252 files; MEMORY CAP on every wagic run; wait for gates with
a FOREGROUND `until [ -f DONE ]; do sleep 10; done` loop (timeout 1200 s) — never a
Monitor/background watcher, never end your turn with the commit pending; state a falsifiable
prediction; report what you did NOT verify.

Lane V (IN FLIGHT, ~/Projects/wagicGPT-w50-v): D1 double-tap on answered/countered casts.
Lane W — engine correctness: D2 Tribute to Hunger resolves with no sacrifice (stale reveal
  zone from a prior Pelakka Predation suspected — step the chooser in code, pin with a suite
  fixture); D4 cleanup-step discard never routed to the seat (the engine discarded Intruder
  Alarm for the model); D6 target rows vanish main1->main2 / self-only target windows offered
  every window (legal-target set drift + a pass answer for one-row self-target windows).
Lane X — render: D3 Devour Flesh row `you gain N` — the TARGETED player gains (fix the
  attribution at the emitter, audit siblings); D5 `legal targets right now:` collapse (28,755-
  char row; same rule as the battlefield line); D13 sweeper `(K able to attack)` semantics for
  summoning-sick tokens; D14 "choose a name" menu never rendered; D16 DRAW FORECAST on the
  punisher's own seat (mirror); D17 MDFC `Choose an option` cast row lacks `(text omitted)`.
Lane Y — parsing: D7 last-clean-line rule executed a rejection (`CHOICE: 1 (...) is wrong
  because`) and took x19 over a later `x20).` — a line carrying a negation/verdict opener is
  not an answer; D8 the prompt's own worked example `CHOICE: 1 (Goblin (1/1))` is unparseable
  (nested parens) — fix the parser AND the example; D9 `CHOICE: 0` on a no-pass ask (damage
  order) escapes the re-ask; D10 plan-carry expiry keyed on CONTENT (verdict opener / names no
  action on the menu) — raw count retired to a report field (the loop-lockout echo is CORRECT);
  D19 degenerate-decode counter (mojibake replies classified, not `unparsed_reply`).
Lane Z — loop/mana UX: D11 the repeat row carries the current creature count and the `x<N>`
  spelling (guide-side stop arithmetic gets its M on the row); D12 mana-only gate leaks a single
  Battlement row with an EMPTY stack; D18 `- Paid {cost} with <sources>` narration receipt so
  auto-tap order (lane T) is adjudicable by string.
Not this wave: D20-D26 carried (frontend grouping D20 stays an owner item).
