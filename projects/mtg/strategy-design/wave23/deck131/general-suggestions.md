# deck131 wave-23 — general-suggestions.md

PASS.

No core / general-prompt (shared decision-prompt) change proposed from this seat. The one
game-deciding leak (Guttersnipe attack-traded into an untapped-but-misread blocker, vs27) is routed
correctly by layer: the mitigation is a per-DECK guide edit (strategy.txt — Guttersnipe attack guard
with a per-creature [tapped]-read), and the reviewer-method generalization is in skill.md. The root
(a per-creature tapped-state comprehension miss on a partly-tapped board) is a candidate cross-seat
WATCH (notes.md I-1) but is single-seat — below the bar to touch the shared general prompt on N=1. The
general prompt's battlefield representation is CORRECT and unambiguous here (the [tapped] tag was
present on the tapped creature and absent on the untapped one); the failure was model comprehension of
a correct representation, not a representation defect the core prompt could fix.

This is the 18th consecutive general-prompt PASS.
