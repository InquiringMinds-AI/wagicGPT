# deck27 general-suggestions.md — core-prompt proposals (wave-23)

Layer-routing enforced: single-seat items CANNOT add or cut a core line; engine/representation, decode
and parser items live in the notes.md ledger, never in core prompt text. **This seat proposes NO
core-prompt change (continuing the consecutive-PASS streak).**

## Core prompt: PASS (no change proposed)

Nothing this seat found is both (a) 2+-seat convergent AND (b) properly core. Detail:

1. **Free-kills-first block rule validated — but it is PER-DECK GUIDE, not core.** The wave-22 revision's
   target scenario resolved correctly at the snap layer (vs109 s13). It lives in deck27's blocking
   section (a real-blocker deck); it is NOT core. Only if synthesis sees the SAME allocation success/
   failure across multiple real-blocker decks would a core blocking-priority line be warranted — flag as
   a cross-seat watch, not core text on N=1.

2. **Wither annotation round 3 — ENGINE, closed.** Validated, zero false positives (notes.md #1). Not core.

3. **Decode-collapse / ahead-degeneration (3 unparsed at vs137) — MODEL/SAMPLING + HARNESS, not core.**
   Pure token-repetition collapse; the fix candidates are rep_penalty strength and a max-tokens fast-fail
   (notes.md #3/#7), plus the pilot verdict input. No core prose can prevent a token-level decode
   collapse. Recorded as cross-seat data for the synthesis pilot verdict. No core text.

4. **Late-stall durdle revision — GUIDE off-case, unexercised (4th corpus).** Owns the guide layer;
   trigger did not occur. Not core.

5. **No-answer-to-lifegain (vs140) — DECK CONSTRUCTION, not prompt.** Now a one-life miss (6/7) rather
   than a blowout; a little reach/disruption would likely flip it. Routed to notes.md #5 (roster owner).
   Not core, not guide-fixable.

## Confirmations for the synthesis agent (no text change)

- **Trust-the-rendered-number held again with ZERO mana fallbacks** on the hybrid {U/B} base — Nth data
  point. Reinforces the existing rule; no new prose.
- **CAUTION for synthesis:** do NOT read deck27's 1/6 -> 4/6 up-swing as the free-kills-first revision
  "working" — the guide is frozen, the revision touched one decision, and the swing is pool-swap +
  variance (three earned win-path wins; two losses to known worst archetypes). Symmetric to wave-22's
  caution about not reading its DOWN-swing as a batch failure.
- **PILOT input:** at this seat, rep_penalty=1.05 = SAME clean-reply prose, WORSE decode stability
  (0 -> 3 fallbacks, all ahead-degeneration collapses; the pilot did not prevent the failure it was
  theorized to fix). Hedged N=1-game/new-opponent. First-class input to the synthesis pilot verdict.

## Engine/representation/decode/parser items produced (routed to notes.md, NOT core)
- Wither round-3 validation (closed).
- Commit-lock: no 2nd harmful instance (stays 1, single-seat; no parser change).
- Decode-collapse ahead-degeneration: 3 harmful instances, decode-owner NOT parser-owner; pilot-tie.
- deck27-has-no-lifegain-answer (construction, roster owner; now a narrow miss).
