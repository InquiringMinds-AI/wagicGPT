# deck137 general-suggestions.md (core prompt) — wave 23

## PASS (would be the 18th consecutive core PASS)

No core-prompt edit is justified from this single guideless seat. Burden of proof is on core
(every deck pays for each line) and both candidate items are single-seat and better served at a
lower layer:

1. The adventure (P/T)-mislabel is a REPRESENTATION defect (notes.md R-ADV-FACE), not a
   deck-agnostic prompt fact — it routes to the option-annotation layer + an interim guide table,
   not core.
2. The convoke "trust the offered option, engine taps for you" fact is a plausible eventual core
   line (it is the same TRUST-THE-RENDERED-NUMBER / trust-the-offered-option family core already
   carries as the interface contract), but it is single-seat here. If a SECOND convoke or
   alternative-cost deck reproduces the "recompute the alternative cost and spiral" tax, propose a
   one-line COMPLETION of the existing interface-contract clause ("a 'with its <alternative/convoke>
   cost' option is payable as offered; the engine handles the alternative payment — do not
   recompute") rather than new doctrine. Not yet.

Core's existing act-floor, interface contract, and attack-floor language are all present and
correct for this deck; the deck's leaks (attack passivity, adventure mislabel, convoke tax) are
per-deck / representation, addressed in strategy.txt and notes.md.
