# deck140 wave-23 — core/general prompt: PASS

No core-prompt (general strategy) change proposed. (18th consecutive PASS at this seat's
lineage.)

Nothing this corpus routes to the core prompt:
- The (a) forced-loss framing is ENGINE-rendered and validated — it is not a general-prompt
  concern.
- The one general-prompt interaction is a NON-issue: the general "send burn at the face"
  line is correctly overridden by the deck guide's #1-Rakdos-hold rule, and the model honored
  the override (held Rakdos ~13x in vs27, fired only for lethal-intent or a grind-to-cap life
  race). No general-prompt edit needed.
- All remaining residual is engine (mana-display friction), harness (a transient outage), or
  deck-construction — see notes.md. None touches the core prompt.

Verdict: PASS.
