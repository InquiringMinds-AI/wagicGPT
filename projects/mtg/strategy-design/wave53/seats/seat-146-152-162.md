# Wave-53 seat: deck146 (4-2) + deck152 (2-3 +1 adj) + deck162 (4-2)

Corpus `~/.Wagic/ai/gpt/selfplay-runs/matchups-20260902-023342/`, binary master c628dd531 (lanes
M/N/O/P/Q + R/S/T), 18 seat logs on my decks, **1,145 decision records** (146: 811, 152: 199,
162: 135; excluding `gamestart`/`gameend`/`recovery`), **449.3 min of inference** (wave 52: 1,351
records / 579.8 min), **11 of the corpus's 15 fallbacks**, 4 of the 7 new `recovery` records.
20/21 natural + the hung `152v125`. Tenth fair-hand corpus; first carrying the HOLD row, kill
summaries, lethal tails, stack naming, timeout retry and the `recovery` record kind. Guides under
test = wave-52 revisions + the boundary pass. Card facts read off `bin/Res/sets/primitives/*.txt`
`text=`/`auto=`/`mana=` lines with `/usr/bin/grep`; translogs read with python.

**Records (corpus-results.tsv):**
- deck146 **4-2**: W vs123 (23/0 T33), vs126 (16/-10 T17), vs152 (21/0 T11), vs162 (17/-1 T11);
  L vs130 (0/2 T18), vs125 (14/39 **T85, 607 records, 88.2 min** - the corpus's longest game).
- deck152 **2-3, +1 adjudicated**: W vs130 (11/-4 T15), vs162 (11/0 T11); L vs123 (-25/16 T12),
  vs126 (0/21 T16), vs146 (0/21 T11); **vs125 HUNG** - no gameend on either seat, `adj0` 20/9 at
  turn 15 (excluded from every rate below).
- deck162 **4-2**: W vs123 (24/0 T16), vs125 (20/0 T21), vs126 (21/0 T16), vs130 (1/0 T24);
  L vs146 (-1/17 T11), vs152 (0/11 T11).
No rate is attributed to any guide edit in either direction (new-baseline rule).

**Files (short -> full, all prefixed `1788334`):**
146: v125 `427-...deck146-0x55be9ec17510-vs-...deck125` | v152 `433-...0x5620b0db1510...` |
v126 `439-...0x55fb77a8b520...` | v130 `445-...0x56380576b4a0...` | v162 `462-...0x55b22bf32450...` |
v123 `464-...0x55629e998440...`
152: v126 `429-...0x55797e0ac1c0...` | v146 `433-...0x5620b20e53f0...` | **v125 `437-...0x5583d9910230...`** |
v130 `447-...0x561efd71b260...` | v162 `449-...0x560ce9ffc130...` | v123 `453-...0x55bb4615e230...`
162: v123 `441-...0x5579a8b77510...` | v152 `449-...0x560ceb338100...` | v126 `455-...0x559196803b50...` |
v130 `459-...0x563cc8f7a490...` | v146 `462-...0x55b22d2668f0...` | v125 `465-...0x559dea81e380...`

---

## THREE HEADLINES FOR THE CORE LOOP

**1. ENGINE (HIGH) - lane N shipped the HOLD row and the pilot will not take it, because its verb
collides with the pilot's own word for the same act.** `Hold priority - do not ask me again this
turn unless the board changes` rendered **1,173 times corpus-wide and was taken 29 (2.5%)** - 21
of the 29 in a single game at one seat. `hold_windows_skipped` totals **102** across 40 gameends
and is **0 on 38 of them**. Meanwhile **all seven** `plan_choice_conflict` records at `146v125`
(and 8 of the corpus's 9) are one shape: the coded line names the HOLD row and the PLAN sentence
says *"I will pass priority"* (s150, s371, s392, s398, s470, s491) or *"I pass"* (s555). The
conflict detector re-asks; three re-asks flipped to `pass` (row 0), three re-confirmed HOLD. Lane
N's own falsifier fires: **consecutive identical-declined runs are 27.6% of corpus decisions
(904/3,273) and 25.2% of mine (265/1,051)** against a target of <10% and a wave-52 baseline of
23.0%, and opponent-turn casting windows went UP (884 -> **1,052**) while opponent-turn casts went
DOWN (68 -> **58**). The specimen is `162v125` s14-s22: nine consecutive asks in one opponent turn
(Upkeep, Draw, Main 1, Combat begins, Attackers, Combat ends, Main 2, End, Cleanup), the identical
three-row menu each time WITH the HOLD row present and the nag `[you declined this exact list 8
times already this turn]`, nine `Cast nothing right now` answers, 208 s. See E-1.

**2. ENGINE (HIGH) - a modal cast row prints no per-mode legality, and it cost the 84-turn game.**
`146v125` s240 (T47) and s528 (T79, **20 untapped sources**) plus ~18 siblings across turns 29-83:
`2. Cast Silverquill Command {2}{b}{w} {leaves 16 of your 20 untapped mana sources untapped}
{card text: "Choose two -- ... Return target creature card with mana value 2 or less from your
graveyard to the battlefield. ..."}` - answered `Cast nothing right now` every time. s240's
stated reason is false on its own screen: *"my graveyard has no creature cards with mana value 2
or less"*, with `- Your Triumphant Adventurer was COUNTERED ...` and `- Your Silverquill Silencer
was COUNTERED ...` in the log directly above. Verified: `Triumphant Adventurer mana={B}{W}`,
`Silverquill Silencer mana={B}{W}`, `Silverquill Command auto=choice name(Return creature and you
draw) target(creature[manacost<=2]|mygraveyard) moveto(mybattlefield) && draw:1 controller &&
life:-1 controller`. Targeted rows got kill summaries this wave (`{kills: <name>}` 40 renders,
`{kills 0 of the N legal targets ...}` 129); modal rows got nothing. See E-2.

**3. GUIDE - deck152 holds attackers a two-name blocker list cannot block.** `152v126` s26 (T11,
**31 life vs 17**), s32 (T13, 25 vs 16), s40 (T15, 17 vs 16): four A-lines each time, **two**
distinct names in `their untapped blockers`, two attackers declared each time. The held pair's
own tags read `Wall of Omens (0/4) (you kill it, your attacker lives)` and `Vampire (1/1) (you
kill it, your attacker lives (lifelink: they gain 1, and their converter takes 1 off you))` - a
printed price of ONE. Their life moved 17 -> 16 -> 16; the seat's moved 31 -> 25 -> 17 -> 0. The
guide sentence that produced it is *"hold every creature whose tag prints one [`(blocking
trigger:`]"* - an absolute keyed to a substring inside an enumeration. Wave 52 closed the
`ATTACK: none` half of this rule (P5 PASS, 0/22 windows) and this is the other side of it. 152-A.

---

## FAIR-HAND BASELINE - every mulligan and bottom on my seats

| seat | look | engine count / coverage | decision | verdict |
|---|---|---|---|---|
| 146v123 | 7 | 3 lands, covers Verse + Soul Shatter | KEEP (126 s) | right |
| 146v125 | 7 | **1 land** {W}1, "would not cover" | MULL (**595 s**, reply reverses once) | right (STEP 2 one-land branch); parser took the last coded line |
| 146v125 | (keeping 6) | 2 lands, "would not cover" | KEEP; bottomed Agadeem's Awakening | **right - the floor now beats the literal** |
| 146v126 | 7 | 2 lands, covers 2 Silencers | KEEP (198 s) | right |
| 146v130 | 7 | 2 lands, covers Verse + Silencer | KEEP (222 s) | right |
| 146v152 | 7 | **1 land**, "would not cover" | MULL (145 s) | right |
| 146v152 | (keeping 6) | 2 lands, covers Adventurer | KEEP; bottomed Silverquill Command | right |
| 146v162 | 7 | 4 lands, covers 3 | KEEP | right |
| 152v123 | 7 | 4 lands, covers 3 | KEEP | right |
| 152v125 | 7 | 3 lands, covers 3 | KEEP | right |
| 152v126 | 7 | **1 land**, "would not cover" | MULL | right |
| 152v126 | (keeping 6) | 3 lands, covers 3 | KEEP; bottomed Lair of the Hydra | right |
| 152v130 | 7 | 3 lands, covers 2 | KEEP | right |
| 152v146 | 7 | 4 lands, covers 3 | KEEP | right |
| 152v162 | 7 | **2 lands {G}2**, "would not cover" | **MULL** | **WRONG** - its own floor keeps it; reply = THE FORBIDDEN THOUGHT verbatim (152-B) |
| 152v162 | (keeping 6) | 1 land, "would not cover" | MULL | right |
| 152v162 | (keeping 5) | 5 lands, covers Katilda + Aspirant | KEEP; bottomed 2 Pathways | right; **won that game 11-0** |
| 162v123 | 7 | 4 lands, covers 3 | KEEP | right |
| 162v125 | 7 | **1 land**, "would not cover" | MULL | right |
| 162v125 | (keeping 6) | 1 land, covers Shield Sphere {0} | KEEP; bottomed Liliana's Caress | right |
| 162v126 | 7 | 2 lands, "would not cover" | KEEP | **right - the floor beat the literal** |
| 162v130 / v146 / v152 | 7 | 5 / 2 / 5 lands | KEEP x3 | right |

**deck146 mulligans at two or more lands: 0 of N (was 3 of 4) - P1 PASS.** Chains never went
past (keeping 5); no mull-to-zero.
**OWNER QUESTION (flagged as the brief requires, not patched):** `152v162` is a repeated mulligan
whose FIRST look was shipped against an explicit guide floor (`TWO OR MORE LANDS AND AT LEAST ONE
SPELL: KEEP. That is the floor and it is not a close call.`) - s1 at 2 lands, then s2 at 1 land,
keeping 5. I have not touched the floor; the edit I own is #171(a)'s missing scoping sentence on
the two-or-more rung (152-B), which is a re-key, not a change of ruling.
**Land drops: 46/46 (146), 33/33 (152), 39/39 (162) - 0 "Play no land right now" answers.**

---

## deck146 - Orzhov dungeon midrange (4-2), per game

- **v123 (W 23/0 T33)**: mulligan-free 3-lander; Silencer T3, dungeon chosen T5, Lost Mine of
  Phandelver **completed FOUR times** (dungeon picks at s7/s51/s72/s84, room forks at s11/s17,
  s58/s68, s75/s78, s91/s94), eight Acererak casts as the repeatable venture the guide names.
  Carries the corpus's longest prompt (s92, T33, **32,068 chars**).
- **v126 (W 16/-10 T17)**, **v152 (W 21/0 T11)**, **v162 (W 17/-1 T11)**: clean; every A-line sent.
- **v130 (L 0/2 T18)**: **no window.** Land-locked at two lands by Molten Rain (T7) and Stone
  Rain (T9). Silencer named Siege-Gang Commander at s6 and attacked alone T5-T15 taking them
  20 -> 2. Three cleanup discards (s15/s17/s18) with Emeria's Call on all three lists and never
  sent - **0 of 3 on the guide's own order** (146-C). Died at 0 with a Nadaar it never had a
  third land for.
- **v125 (L 14/39 T85)**: the 84-turn game, traced below.

### Why `146v125` ran 84 turns (this seat's assignment)
Five facts, each read off the render or the primitives; four of them are matchup, one is a window.
1. **Every threat above three mana was countered on the turn it was cast.** Kaya T41 (Fall of the
   Gavel), Kaya T49 (Cancel), Nadaar T23/T63, Barrowin, Lolth, three Silverquill Commands, two
   Acereraks, a Silencer, a Soul Shatter (T64), Emeria's Call. Kaya never once appeared on the
   seat's battlefield line - I checked, and confirmed it was counterspells and not an engine fault
   before filing anything.
2. **Elixir of Immortality is unanswerable by this deck.** `mana={1}`, `auto={2}{T}:life:5 &&
   moveTo(mylibrary) all(this) && moveTo(mylibrary) all(*|mygraveyard) && shuffle`; used T24, T29,
   T70, T78. `Vanishing Verse target=*[-multicolor;-colorless]|battlefield` cannot target a
   colourless permanent; `Soul Shatter` takes a creature or planeswalker. Only `Kaya ... -3:
   target(*[-land]|battlefield) moveto(exile)` reaches it, and both Kayas were countered. Their
   life went 20 -> 43 (T29) -> 45 (T31) and was never below 26 after T27.
3. **The seat's clock was 3-6 damage on its own turns only.** 44 attack windows across the deck,
   18 of them in this game; from T31 the only attacker was one Silencer, then one or two animated
   Hives at `{3}{B}` each (`Hive of the Eye Tyrant auto={3}{B}:name(Becomes beholder) ... ueot`) -
   136 beholder rows offered, 19 taken.
4. **The decisions were not the cause.** 18/18 attack windows sent every A-line (26 A-lines, 0
   `ATTACK: none`, 0 empty-tag misses); `at 0 this does nothing` 396 rows / **0** takes;
   `{this hits YOUR permanent}` 11 windows / 0 takes; `[Upkeep offer:` 24 windows / **0** takes;
   `[repeat:` **0 rows**; `- Paid` receipts 133 unique on my seats / **0 no-source**.
5. **The one window is the Silverquill Command return mode** (headline 2 / 146-A): ~20 declines
   between T29 and T83 at 8-20 untapped sources, on a board of two animated lands, with two
   mana-value-2 creatures sitting in the graveyard.
**Cost:** 607 records, **88.2 min**, of which the mulligan alone is 13.3 min (s1 595 s + s2 69 s +
s3 67 s) and the identical-run class is a large slice of the rest.

**Fractions (deck146).** Attack windows **44**, A-lines **80**, sent **78**, `ATTACK: none` **0**,
empty-tag A-lines **42 / 0 missed** (P2 PASS; was 1 of 39). Own-turn `Cast nothing` with any other
`Cast` row **165**, of which **139 held only engine-marked-dead rows**; genuinely live **26** (20
Command + 2 guide-legal Acererak + 4 guide-forbidden Agadeem's). Show-the-work sentence **15 of
165** (was 20/84) protecting **0** misroutes -> deleted, not restated. `[legendary:` 12 rows / 0
takes. Hive `[this land is TAPPED ...]` rows **47, taken 1** (`146v125` s443 - first instance,
unpunished, recorded). Cleanup discards **3, 0 on the printed order**. Blocks 1 window, correct.

---

## deck152 - Bant human midrange (2-3 +1 adj), per game

- **v130 (W 11/-4 T15)**: two `{paying this taps: Katilda, Dawnhart Prime - it cannot attack this
  turn}` takes in **Main 1** (s11 Ranger Class, s14 Sigarda) - technically P4 breaks, but both
  clauses hold ONE name and both rows put a bigger body down; the seat won. 152-C re-keys the stop
  to the clause's name count rather than restating it.
- **v162 (W 11/0 T11)**: the (keeping 5) game; four `{paying this taps:` rows with two names
  correctly passed (s29/s32/s43/s44 - the exact wave-52 break, now held).
- **v123 (L -25/16 T12)**: **no window.** deck123 assembled 20 flying 2/2 Vampires behind
  Bloodline Keeper + Intruder Alarm by T11; Fateful Absence killed Lord of Lineage at s18-s19 and
  the Spellbinder took an Idyllic Tutor at s40 per the guide's own ordered rule (a named
  citation - but one of three identical Tutors, with Damnation also listed). Carries the corpus's
  worst two latencies at this seat: s16 **630 s** for a land drop, s1 141 s for the mulligan. The
  14 identical-option damage-order asks and both `unparsed_reply` fallbacks are here (E-3).
- **v126 (L 0/21 T16)**: the 152-A game (headline 3). Blocks in the same game were correct
  (s37/s42 legal multi-blocker assignments, 0 repeated B-numbers).
- **v146 (L 0/21 T11)**: **no window.** Spellbinder exiled Soul Shatter from their hand at s9;
  the seat then went 19 -> 10 -> 2 over two combats against Nadaar + Adventurer + Goblins with one
  blocker, and the s17 block (Brutal Cathar on a Goblin at 2 life, header `you would be at -2`)
  was the only surviving-shaped answer and did not survive.
- **v125 (HUNG)**: see E-4. Excluded from all rates. Its last real decision (s26) was
  `ATTACK: A1, A2, A3, A4` at **13 power against their 9 life** - a correct lethal swing.

**Fractions (deck152).** Attack windows **22**, A-lines **47**, sent **41**, `ATTACK: none` **0**,
empty-tag A-lines **26 / 0 missed** (P5 PASS; was 2 windows / 5 A-lines). `{paying this taps:` 12
windows / 14 rows / **2 takes**, both one-name Main-1 body casts. `[legendary:` 6 rows / 0 takes
(the deploy-floor "breaks" of wave 52 are gone with the count). Blocks **6 windows**, `BLOCKS:
none` **2** (both at `you would be at` 15 and 19, above the floor) -> **P7 PASS 0/N**; 0 repeated
B-numbers, **0 `multiblock_reask`**. Own-main `Cast nothing` with a live untagged creature row:
**0** (all 19 are Fateful Absence held as an instant - branch (a) of the deploy floor).
`[Upkeep offer:` rows **1** -> P6 still effectively UNTESTED.

---

## deck162 - UB Draw-and-Die (4-2), per game

- **v123 (W 24/0 T16)**, **v125 (W 20/0 T21)**, **v126 (W 21/0 T16)**, **v130 (W 1/0 T24)**:
  punisher-first, K-string clean. `v126` is the cleanest citation of the wave: the Master of the
  Feast carve-out met at K=1 (s7) and K=2 (s10) and declined at K=0 against `of which 0 are
  creatures` (s6).
- **v146 (L -1/17 T11)**: **no window** - every menu in the game was two rows (Mine/nothing, then
  Ob Nixilis/nothing) and no wall was ever in hand. Carries the seat's `named_row_reask` (s13,
  reply `CHOICE: 1 (Cast Underworld Dreams)` on a menu whose row 1 was `Cast Howling Mine`) with a
  clean `recovery` at s14. Also five of the eleven first-Mine over-brake windows (162-B).
- **v152 (L 0/11 T11)**: **the one window.** s11 at 4 life - see `deck162/edit-texts.md` G1 and
  general-suggestion R-8. This is the deck's only loss with a better legal answer on the screen.

**Fractions (deck162).** Engine casts at `converters on your battlefield: K >= 1`: **9/9 legal**.
Engine casts at K=0 with a Howling Mine already on the line: **0** (was 2/2) -> **P8 PASS**.
Second-Mine rows (`[second copy:`) **11 offered, 4 taken, all at K >= 1**; 3 declined at K=0.
**First-Mine over-brake: 11 windows across three games** (was 1) - 162-B, the wave's one
regression. Walls: Shield Sphere 3/3 offers taken. Ob Nixilis -2 3/3 across the table.
`{this hits YOUR permanent}` **12 windows, 0 taken**. Attack windows 3, A-lines 3, all sent.
Blocks 2 windows, both correct. Cleanup discards 0.

---

## THREE-NUMBER LITERAL AUDIT (#168) - every literal lanes O/P changed, plus the ones my guides quote

| literal | old-string renders | new-string renders | takes under the new string |
|---|---|---|---|
| defender tap clause: `- it cannot attack this turn` -> `- it cannot block on their turn` | **0 on any defender row** (14 remaining renders are non-defenders: Katilda, Luminarch Aspirant, Intrepid Adversary, Elite Spellbinder - correct) | **27** (all deck126 Overgrown Battlement) | **8** | 
| `[DRAW PRICE: ...]` + `this KILLS you` | n/a | 85 DRAW PRICE rows, **16** carrying `this KILLS you` | **0 casts from a KILLS row**; 1 cast from a DRAW PRICE row without it |
| `{kills 0 of the N legal targets at -1/-1}` | n/a | **129** | 4 |
| `{kills: <Name>}` | n/a | **40** | 3 |
| `[second copy: ... a second copy changes nothing]` (Lantern partial) | n/a | **1** | 0 |
| `[second copy: ...]` (base form) | n/a | **86** (11 at deck162) | 12 (4 at deck162, all K>=1) |
| stack line `ability: <source>'s <effect> (aimed at <victim>) [from your/their <source>]` | bare `ability: <effect>` **0** | **56** (13 at my seats) | n/a (log line) |
| edict such-clause `- a sacrifice is already on the stack aimed at this permanent` | n/a | **0** (no window at my seats) | UNTESTED |
| `- and their <name> takes N off YOU: life A -> B` on edict gain rows | n/a | **0** | UNTESTED |
| HOLD row `Hold priority - do not ask me again this turn unless the board changes` | n/a | **1,173** (336 at my seats), LAST row, opponent's turn only - both confirmed | **29** (21 at `146v125`) |
| `[you declined this exact list N times already this turn]` | n/a | **859** (277 mine) | n/a (annotation) |
| PLAN header `(as you last stated it, N windows ago on turn T)` | unstamped PLAN blocks **0** | **3,329** | n/a |
| `{right now:` **on a `Cast Silverquill Command` row** (deck146's entry-5 key) | **0 of 28** | none exists | **0** - the guide condition was unsatisfiable (146-A) |
| `at 0 this does nothing` | n/a | **614** (396 mine) | **2 corpus-wide, 0 mine** |
| `[no creature they control can block this attacker]` | n/a | **119** (71 mine) | 0 missed at any of my seats |
| `{castable from exile ...}` | n/a | 2 | 2 |
| `0. Pass priority` | n/a | **194** | 128 replies wrote `CHOICE: 0` there, **128 executed row 0** |

---

## PREDICTION SET A - wave-52 guide edits on my decks

| id | deck | prediction | verdict | evidence |
|---|---|---|---|---|
| P1 (146-Q/R) | 146 | mulligans at 2+ lands 0/N; mulligans naming a colour/source 0/N | **PASS 0/N; PASS 0/N** | 2 mulligans, both 1-land under "would not cover"; both replies argue land count, not colour |
| P2 (146-S) | 146 | `ATTACK: none` on a window with an empty-blocker-tag A-line 0/N | **PASS 0/44** | 44 windows, 80 A-lines, 78 sent, 42 empty-tag lines, 0 missed (was 1 of 39) |
| P3 (146-T) | 146 | own-turn `Cast nothing` with a live row carrying the entry-N sentence N/N | **FAIL 15/165 (15/26 live)** | and it protected 0 misroutes - #181's gate says delete, which I did |
| P4 (152-I) | 152 | `{paying this taps:}` rows taken in Upkeep or Main 1: 0/N | **FAIL 2** (was 3 of 10) | `152v130` s11, s14 - both ONE-name clauses on body casts, in a game the seat won; re-keyed by count (152-C), not restated |
| P5 (152-J) | 152 | `ATTACK: none` with one converter name and an empty-tag A-line 0/N | **PASS 0/22** | 26 empty-tag A-lines, 0 missed - but the partial-hold shape replaced it (152-A) |
| P6 (152-K) | 152 | casts of a `[legendary:` row while the copy is healthy 0/N | **PASS 0/6** | 6 rows, 0 takes; and 0 own-main `Cast nothing` with an untagged creature row |
| P7 (152-L) | 152 | `BLOCKS: none` at "you would be at" <= 9 with an offer 0/N; repeated B-numbers 0/N; first-strike-beats-trigger claims 0/N | **PASS 0; PASS 0; PASS 0** | 6 block windows; the 2 `BLOCKS: none` are at 15 and 19 |
| P8 (162-I) | 162 | engine casts on a two-row ask at `converters ... : 0` with a Mine on the line 0/N; casts at K>=1 N/N | **PASS 0/N; PASS 9/9** | 3 second-Mine declines at K=0, 4 takes at K>=1. The mirror failure is the exempt first Mine (162-B) |
| P9 (E-1 edict) | 146 | edict resolutions after which the named victim is still on their line 0/N | **PASS 0/2** | `146v125` T50 and T63: one Soul Shatter resolved -> `Opponent's Emrakul, the Aeons Torn died`; the second copy at T64 was countered by Cancel. Wave-52's triple did not recur |
| P10 (E-2 runs) | all | consecutive identical option-set declines max 2 per turn | **FAIL - up to 9-15** | 25.2% of my decisions are in runs of 3+ (was 27.8%); corpus 27.6% |

## PREDICTION SET B - engine lanes where my seats had windows

| lane | prediction | verdict | evidence |
|---|---|---|---|
| **M** (blockers seam) | `multiblock_reask` with no follower 0/N; one `declared blocks` per ask; `multiblock_first_wins`; one-per-blocker clause on the BLOCKS line | **PASS 0 reasks; UNTESTED on the notes** | **0 `multiblock_reask` at my seats** (was 3 corpus-wide in wave 52), 9 block windows, 2 legal multi-blocker answers (`152v126` s37, s42). No `multiblock_first_wins` or `blocker_handle_in_attacker_slot` string appears - no window |
| **N** (window economy) | opp-turn windows down >=30%; opp-turn casts >=90% of 68; End/Cleanup/Attackers/Combat-ends 0 casts; identical-decline runs <10%; `pass_row_named` N/N; PLAN age stamps N/N; `latched_coded_line` on conflicts with `coded_answers >= 2` | **FAIL; FAIL; near-PASS; FAIL; PASS; PASS; UNTESTED** | windows 884 -> **1,052 (+19%)**; casts 68 -> **58 (85%)**; End 0 / Cleanup 0 / Combat-ends 0 / **Attackers 1** (`125v152` s15, a real Path to Exile); runs **27.6%** vs <10%; `CHOICE: 0` on a `0. Pass priority` menu executed row 0 **128/128**; **0 of 3,329** PLAN blocks unstamped; no `latched_coded_line` string on any of my 7 conflicts |
| **O** (pricing) | `[DRAW PRICE:` with damage >= life carries `this KILLS you` N/N, casts from such rows 0; targeted rows with knowable damage carry a kill count N/N, removal where every target survives 0; defender tap rows printing `cannot attack` 0/N | **PASS 16/16 and 0 casts; PASS 169 kill-summary rows; PASS 0 defender rows on the old string, 27/27 on the new** | no DRAW PRICE or kill-summary window at MY seats (deck146/152/162 run none of those cards) - adjudicated corpus-wide by the emitter's string, as the brief directs. The defender half is deck126's Overgrown Battlement, 8 takes |
| **P** (render/state) | bare `ability: <effect>` stack lines 0/N; edict rows missing the such-clause 0/N; no ask menu > 12 rows collapsing to <= 2 shapes, max non-log segment < 12 KB; `{castable from exile}` naming its source 17/17; `[second copy:` takes on non-stacking scripts 0; battlefield header counts lands | **PASS 0; UNTESTED; FAIL; PASS 2/2; PASS at my seats; PASS** | 56 named stack lines, 0 bare. **The >12-row / <=2-shape rule is violated 16 times corpus-wide (14 at `152v123`)** - see E-3. Battlefield headers now print `and N are lands` on every line I read |
| **Q** (observability) | every `choice: -1` + fallback followed by exactly one `recovery` record; `empty_reply` within 1 s of the wall 0; Baka-executed opening keeps 0 | **PASS 7/7; UNTESTED; PASS 0** | 7 `choice: -1` fallbacks corpus-wide, 7 `recovery` records, **7/7 paired, 0 unpaired**. **0 `empty_reply` and 0 `timeout` in the whole corpus**, so the retry path has no window. No Baka-executed mulligan: the 595 s keep at `146v125` s1 was the model's |
| **R** (Go-Shintai `may pay`) | any `may`-prompt fallback | **UNTESTED** | no `may`-prompt fallback at my seats; Dominating Vampire not in the pool |
| **S** (hand-refresh perf) | human-seat only | **UNTESTED by construction** | |
| **T** (Baka aggregate-lethal blocks) | any `blockers` fallback record's outcome | **UNTESTED** | **0 fallbacks of any kind on a `blockers` record** at my seats; Baka never executed a block here |
| **owner batches / evolve / autotap** | adventure halves, equipment, evolve, counters-matter | **UNTESTED** | none of those card shapes exists in decks 146/152/162 |
| **D-carry (receipts)** | `- Paid` on every payment, 0 no-source | **PASS 133/133 unique, 0 no-source** | |

---

## ENGINE / RENDER ITEMS (ranked; repro = file, seq)

**E-1 (HIGH, efficiency) - the HOLD row is the right mechanism with the wrong verb, and the
window economy got worse.** Repro `162v125` s14-s22 (one opponent turn, nine identical three-row
asks with the HOLD row present from s14 and the decline-count nag from s15, 208 s) and
`146v125` s150/s371/s392/s398/s470/s491/s555 (all seven `plan_choice_conflict` records at that
seat: the coded line takes HOLD, the PLAN says "pass"; three re-asks then answer `pass`). Numbers:
HOLD 1,173 renders / 29 takes / `hold_windows_skipped` 102 across 40 gameends (0 on 38);
identical-decline runs 27.6% corpus / 25.2% mine; opponent-turn cast windows 1,052 vs 884 last
wave with 58 casts vs 68. The suppression that IS working silently is
`mana_only_windows_skipped` = **406**. Two asks, both cheap: reword the row to lead with "Pass
priority", and stop treating "pass" in the PLAN as a conflict with it. See `deck146/
general-suggestions.md` R-3.

**E-2 (HIGH, render) - modal cast rows carry no per-mode legality.** Repro `146v125` s528 (T79,
20 life, 20 untapped sources) and s240 (T47). 28 `Cast Silverquill Command` rows corpus-wide,
**0** carrying any mode annotation, and the pilot's derivation from the narration log was wrong at
s240 in the direction that cost the game. Targeted and edict rows got exactly this treatment this
wave (`{kills:}`, `{right now: they sacrifice ...}`); modal rows are the gap. R-1.

**E-3 (MED) - an ask whose entire option list is byte-identical.** Repro `152v123` s23-s37: a
12-row, then 11-, 10-, 9-, 8-, 7-, 6-row damage-assignment-order menu in which every row is the
literal string `Vampire (2/2) [flying, doesn't untap during its controller's untap step]`.
Fourteen sequential model calls with nothing to distinguish; `s29` answered `CHOICE: 7` on a
six-row menu (`unparsed_reply`, one of the corpus's 15 fallbacks; lane Q's `recovery` fired
correctly at s30). Corpus-wide **16 such asks, 147 rows, 2.9 min**, 14 of them at this seat.
This is also lane P's ">12 rows collapsing to <=2 shapes" prediction failing. Ask: when the
de-duplicated option list has length 1, resolve internally and log it, as
`mana_only_windows_skipped` already does. R-6.

**E-4 (HIGH, owned by the ENGINE seat - my seat's half of the `152v125` hang, pinned).**
From `1788334437-...deck152-0x5583d9910230-vs-...deck125.jsonl`, 29 records, no `gameend`:
- **s26** (T15, Attackers, 43.8 s): `ATTACK: A1, A2, A3, A4` - Elite Spellbinder, Moonrage Brute,
  Sigarda, Wolf; 13 power against the opponent's 9 life. A correct lethal swing.
- **s27** (T15, **Blockers**, 43.8 s): the Ranger Class level-2 target ask, answered row 3 =
  `Sigarda, Champion of Light (4/4) [trample, flying] [your battlefield] [tapped]`. The reply
  misnames it ("Resolve Sigarda's Coven trigger") but the coded answer is the right row.
- stderr `game-152v125-1788334434.stderr` last engine line:
  `Action added to stack: StackAbility. (Source: Ranger Class)`.
- **s28** (T15, Blockers): `kind: "reveal"`, **`options: 5`, `prompt` length 0, `latency_ms: -1`,
  `chosen_text: "none (no legal target)"`** - the engine self-answered without a model call.
- Then nothing for ~13 h until `adj0` (20/9, turn 15). The opposing seat
  (`...deck125-0x5583dac4bbe0...`, 18 records) also ends with no `gameend`; its last record is a
  T12 Path to Exile target ask answered `Sigarda ... [tapped]`.
Primitives that fix the sequence: `Ranger Class auto=@each my blockers restriction{compare(
hascntlevel)~morethan~1}:name(Put 1/1 counter) target(creature[attacking]|battlefield) counter(1/1)`
(the level-2 trigger fires at the BLOCKERS step, which is why s27 is phase Blockers), and
`Sigarda, Champion of Light auto=@combat(attacking) source(this) restriction{coven}:name(Look top
5 cards) **reveal:5** optionone name(Get a human) target(<upto:1>human|reveal) moveto(hand)
optiononeend optiontwo name(put on bottom) target(*|reveal) bottomoflibrary and!( all(*|reveal)
bottomoflibrary )! optiontwoend revealend`. **s28's `options: 5` is Sigarda's `reveal:5`.** So the
freeze is: two attack triggers in one window, the Ranger Class StackAbility pushed, then Sigarda's
Coven reveal seam opening with FIVE options and ZERO prompt bytes, resolving to `-1 / "none (no
legal target)"`, after which neither the `optionone`/`optiontwo` branch nor the
`and!( ... bottomoflibrary )!` tail completes. A plausible second reading I could not separate
from the logs: `restriction{coven}` was not satisfied (Sigarda's `lord(human) 1/1` can collide the
attackers' powers) and the reveal opened anyway on an empty set. I did not read
`AllAbilities.cpp`; the ENGINE seat owns the mechanism.

**E-5 (MED, render) - a cast menu prices each row alone and never says which rows fit together.**
Repro `162v152` s11: `Ob Nixilis {leaves 0 of your 5}`, `Master of the Feast {leaves 2 of your 5}`,
`Shield Sphere {leaves 5 of your 5}` on one menu at 4 life; rows 3+4 were affordable together and
are two blockers. The seat took row 2, s12's menu had two rows, and it died at 0 the next combat.
R-8.

**E-6 (LOW, telemetry) - `plan_choice_conflict` records improved and are now readable.** Unlike
wave 52 (E-4 there), the record now carries `chosen_text: ""` - correctly signalling that nothing
executed - and the re-ask is a SEPARATE, normally-logged record immediately after (`146v125`
s150 -> s151, s371 -> s372, s555 -> s556). Wave-52's E-4 can be closed. What is still missing is
a field naming WHICH prose phrase triggered the conflict; all seven here are the word "pass".

**E-7 (LOW) - single-decision latency outliers.** `152v123` s16 **630 s** for a three-row land
drop; `146v125` s1 **595 s** for a two-row mulligan; `152v146` s15 252 s; `146v130` s1 222 s and
s10 212 s. The mulligan ask is systematically the most expensive decision of a game (five seats
above 140 s). With no `timeout` and no `empty_reply` in the corpus these never hit the wall, but
they are ~4% of my seats' 449 min in eight decisions.

**Narrated actions with no decision record on my seats:** 0. Every `You discarded` has a `discard`
record (4/4), every reveal a `reveal` record (7/7), every `choice: -1` fallback a `recovery`
record (7/7 corpus-wide).

---

## LATEGAME SPECIMEN CANDIDATE
`1788334427-ai_baka_deck146-0x55be9ec17510-vs-ai_baka_deck125.jsonl`, **seq 520-560** (turns
79-81): 20 untapped sources on an empty board, the live `Cast Silverquill Command` row declined
three times with two mana-value-2 creatures in the graveyard (E-2), the two-Hive animation and
attack, and at s555 a reply whose prose contains two coded lines (`CHOICE: 0 (pass)` then
`CHOICE: 1 (animate Hive 1)`) producing the seventh `plan_choice_conflict` of the game and a clean
re-ask at s556. 607 records, 85 turns, 88.2 min; it carries E-1, E-2 and E-6 in one arc.
Second choice: `152v126` seq 26-40 - three combats of the 152-A partial hold, a complete guide
specimen rather than an engine one. The brief's own baseline specimen (`125v146` seq 357) is the
same game from the other seat.

---

## ROTATION EVIDENCE (open lanes, not the record - #167)
- **deck146 - NO EXIT, one big lane opened.** 4-2. The wave-52 lanes closed: mulligan 0/N (P1),
  empty-tag attacks 0/44 (P2), Kaya self-target 0/11, `[repeat:` 0 rows, edict re-resolution 0/2.
  What opened is bigger than what closed: a cast-order entry keyed to a literal that renders 0/28
  (146-A), and a cleanup order with no recognise->do surface (146-C, 0/3). Both are first-corpus
  findings with a first statement, so this deck needs wave 54.
- **deck152 - NO EXIT, and the converter lane has now failed in two opposite directions.** 2-3
  (+1 adj). Losses: `v123` no window (20 flying tokens on T11), `v146` no window (no wall in hand
  at any point), `v126` WINDOW (152-A). Wave-52's fixes held (P5, P6, P7 all 0/N; `multiblock_reask`
  0) and P4's residual is a count, not a break. The mulligan floor broke for the third corpus on
  the same shape and is now scoped rather than restated. Re-check all three in wave 54.
- **deck162 - NO EXIT this wave, and that is a change from wave 52's "exit-eligible".** 4-2 with
  135 decisions. The lane that was going to close it (162-I / P8) PASSED clean, but the wave-52
  edit that closed it opened a mirror lane: 11 first-Mine over-brake windows (was 1), delaying the
  engine four turns in two games. And the deck's one loss with a window (`162v152` s11) is a new
  lane - survival at the cast window - with a first statement. Both need a corpus.

---

## FALSIFIABLE PREDICTIONS FOR WAVE 54

**P1 (146-A).** deck146 own-turn `Cast nothing right now` answers on a menu holding a
`Cast Silverquill Command` row while the log shows "Triumphant Adventurer" or "Silverquill
Silencer" in the pilot's graveyard and not on its battlefield line: **0/N** (was ~20 of 26 live
windows). Replies asserting an empty mana-value-2 graveyard against a log that names one: **0/N**.

**P2 (146-C).** deck146 `discard` records whose `PUT` line omits Emeria's Call while Emeria's Call
is on the numbered list: **0/N** (was 3/3).

**P3 (146-T, deletion control).** Removing the show-the-work ritual does not cost anything: own-turn
`Cast nothing` answers on a menu holding a live row the engine has NOT marked dead, where the cast
order names a matching entry: **0/N** (was 0 of 26 - the deletion must keep it at 0).

**P4 (152-A).** deck152 attack windows under a ONE-name converter line where the number of A-lines
declared is less than or equal to the number of distinct names in `their untapped blockers` while
at least one undeclared A-line's tag contains `(you kill it, your attacker lives)`: **0/N**
(was 3 of 3 windows / 6 A-lines).

**P5 (152-B).** deck152 mulligans taken at **two or more lands**: **0/N** (was 1 of 3 this wave,
and 1 of 3 in wave 52). Replies naming a colour or a source as the ship reason at any land count:
**0/N** (was 1).

**P6 (152-C).** deck152 rows taken in Upkeep or Main 1 whose `{paying this taps:` clause names
**two or more** creatures: **0/N** (was 0 this wave, 3 in wave 52 - this is the half that must
stay at 0). Rows taken whose clause names ONE creature and whose row is a level counter, an
activation or a noncreature spell: **0/N**.

**P7 (162-A).** deck162 cast asks where the pilot's life is 10 or less, its own header reads a
smaller `of which N are creatures` than the opponent's `M of them without a restriction against
attacking`, and a creature row is on the menu: noncreature rows taken **0/N** (was 1 of 1, and it
lost the game). Windows where two affordable creature rows existed and only one was taken: **0/N**.

**P8 (162-B).** deck162 `Cast Howling Mine` rows carrying **no** `[second copy:` tag, on a board
with no Howling Mine, declined: **0/N** (was 11 across three games). Rows carrying the tag, taken
at `converters on your battlefield: 0`: **0/N** (was 0 - must stay).

**P9 (E-1).** With the HOLD row reworded to lead with "Pass priority": HOLD takes as a fraction of
HOLD renders **> 25%** (was 29/1,173 = 2.5%); `plan_choice_conflict` records whose reply names the
HOLD row while the PLAN says "pass" **0/N** (was 8 of the corpus's 9 conflicts); consecutive
identical-declined runs **< 10% of decisions** (was 27.6%).

**P10 (E-3).** Asks whose de-duplicated option list has length 1: **0/N** (was 16 corpus-wide /
147 rows / 2.9 min, and one `unparsed_reply`).

---

## WHAT I DID NOT CHECK
- **Scryfall was not fetched.** Every card fact above is read from `bin/Res/sets/primitives/*.txt`
  with `/usr/bin/grep`: Silverquill Command, Silverquill Silencer, Triumphant Adventurer, Nadaar,
  Barrowin, Acererak, Kaya the Inexorable, Lolth, Hive of the Eye Tyrant, Emeria's Call, Vanishing
  Verse, Pelakka Predation, Soul Shatter, Elixir of Immortality, Ranger Class, Sigarda, Katilda,
  Moonrage Brute, Ob Nixilis, Howling Mine, Master of the Feast.
- **E-4's mechanism is NOT pinned.** I read the two seat logs and the two primitives and did not
  open `AllAbilities.cpp` / the reveal seam, and I did not separate the two readings (a stalled
  `reveal:5` resolution vs an unsatisfied `restriction{coven}` opening an empty reveal). The
  ENGINE seat owns it.
- **Lane O's own row families have no window at my seats.** No `[DRAW PRICE:`, no `{kills:`, no
  `-N/-N` DIES/SURVIVES row and no fight row exists in decks 146/152/162; I adjudicated those
  predictions corpus-wide by the emitter's string, as the brief directs, and did not read the
  opposing seats' decisions around them.
- **Lanes R, S, T and the owner batches have no window at my seats** - reported UNTESTED, not
  passed. In particular there was **no Baka fallback of any kind on a `blockers` record anywhere
  in the corpus**, so lane T's sweep is untested here by absence of exposure, not by measurement.
- **The identical-run count** groups by (turn, exact option-set, answer) and requires 3+
  occurrences; pairs are excluded, so the true idle-window count is higher than 25.2%.
- **Opponent-seat replies** were read only where they narrate my seats' effects (the Soul Shatter
  resolutions at `146v125` T50/T63, the counterspell log, the `152v125` tail). The six opposing
  guides were not scored.
- I did not re-read the tail of the longest prompt (`146v123` s92, 32,068 chars) beyond its option
  block, and I did not measure per-seat prompt-size growth.
