# Wave-53 seat: deck125 (4/5 natural, 1 hung) + deck126 (2/6)

Corpus `matchups-20260902-023342` - TENTH fair-hand corpus, first on lanes M-T (docket D1-D14 plus
the Vita lanes). Binary merge head c628dd531, model qwen36-35b-a3b on Spark :8084. Guides =
wave-52 reviewer revisions (e68b7c7c6) + the boundary pass (2d83e169f).

**Volume.** deck125 **1,075** records over 6 seat logs, deck126 **267** over 6 - 1,317 decisions
excluding `gamestart`/`gameend`/`recovery`, 37% of the corpus's 3,572. The shape of the corpus
changed under my seats even though the turn count did not: deck125 played 261 turns last corpus and
261 this one, but `deck126 vs125` fell from 218 records for 39 turns to **123 records for 55 turns**
while `deck125 vs126` rose from 128 to 374. Largest prompt on my seats **31,553** (`deck125 vs126`
seq 369, t55, `priority`); longest seat log `deck125 vs126` (374 records, 55 turns).

**Results.** deck125 **4W-1L** natural (won vs123 t35 30/0, vs126 t55 19/0, vs130 t50 13/-14, vs146
t85 39/14; lost vs162 t21 0/20) **plus the hung `152v125`**, life-adjudicated against it at 20/9 on
turn 15 - excluded from every rate below per the corpus note. deck126 **2W-4L** (won vs152 t16 21/0,
vs130 t29 21/-2; lost vs125 t55 0/19, vs146 t17 -10/16, vs162 t16 0/21, vs123 t12 -63/20). Wave 52
was 5-1 / 1-5. Real hands, n<=6 per deck: **not attributed to guide edits in either direction.**

**Health (12 seat logs).** Fallbacks **2 / 1,317 (0.15%)** - back inside the 0.10-0.31% band and the
best figure in three corpora. deck125 1/1,075: `unparsed_reply` at `vs126` seq 365 (t55, a 2-row
Upkeep menu, 202.5 s, reply a multilingual degenerate decode). deck126 1/267: `named_row_reask` at
`vs125` seq 9 (`CHOICE: 4 (Cast Sanguine Bond)` on a 3-row wall menu; `parse_note
named_row_not_offered`). **Each is followed by exactly one `kind: "recovery"` record** (seq 366,
seq 10) and each recovered - deck126 seq 11 carries `named_row_reask_recovered` and answered the
right row. `commit_retracted` / `stale_livelock` / `plan_missing` / `decision_reversed_in_prose` /
`repeat_count_zero_pass` **0**. `answer_replaced` **1** (deck126 `vs125` seq 13). Other parse notes:
`multi_answer_first_taken` 2 (deck125), `echo_index_conflict` 1 (deck126 `vs130` seq 21 - engine
MED #5). `- Paid` receipts read out of `events`: **190 / 0 no-source**, 10 `from mana already
floating`. JSONL `turn` == the last `=== Turn N` in the prompt: **1,305 / 1,305**. Latency: deck125
p50 3.1-21.4 s per game, max 202.5 s; deck126 p50 10.3-41.8 s, max **868.7 s** - engine LOW #8.

**Card facts.** Every fact below is read off `projects/mtg/bin/Res/sets/primitives/*.txt` with
`/usr/bin/grep`: Exquisite Blood `mtg.txt:37966` `auto=@lifelostfoeof(player):life:thatmuch
controller`; Sanguine Bond `mtg.txt:99004` `auto=@lifeof(player) from(*[-lifefaker]|*):life:-thatmuch
opponent`; Overgrown Battlement `mtg.txt:83596` `{T}:foreach(creature[defender]|myBattlefield)
add{G}` + `abilities=defender` (**no life clause - it gains nothing on a block**, which is edit
D87a); Perimeter Captain `mtg.txt:85398` `@combat(blocking) source(creature[defender]|mybattlefield):
may life:2 controller`; Pride Guardian `mtg.txt:88356` life:3; Wall of Omens `mtg.txt:131252`
`auto=draw:1 controller`, defender; Tribute to Hunger `mtg.txt:124073` `toughnesslifegain
targetopponent`; Staff of Nin `mtg.txt:112425`; Sorin `planeswalkers.txt:3055`
`token(Vampire,...,1/1,black,lifelink)`; Sigarda, Champion of Light `borderline.txt:102243-102253`
(`auto=@combat(attacking) source(this) restriction{coven}: ... reveal:5 optionone
target(<upto:1>human|reveal) moveto(hand) optiononeend optiontwo ... optiontwoend revealend`, 4/4
flying+trample) and Ranger Class `borderline.txt:89046-89056` (`@each my blockers
restriction{compare(hascntlevel)~morethan~1}: ... target(creature[attacking]|battlefield)
counter(1/1)`) - both read for the hang, HIGH #1. **Scryfall not consulted; no primitive/Scryfall
disagreement is reported either way.**

**The engine loop question, settled empirically.** Rule #2's thesis (Blood + Bond drains the
opponent to 0 off one point of life) is **true in this build**: `deck126 vs152` seq 36 (t16, 4 life
vs 17) attacked one 1/1 lifelink Vampire into a 3/3 blocker - the Vampire was BLOCKED, the seq-39
`gameend` reads **21/0** - and `vs130` seq 33 (t29, 1 life vs 18) cast Tribute at Rorix and ended
21/-2. I initially read the `vs125` game as evidence the loop does NOT chain; that was wrong and the
events say so: Exquisite Blood was **COUNTERED** by Dream Fracture at t14 (`vs125` seq 20 events)
and the second Bond by Cancel at t18 (seq 23), so the pair was never assembled in that game and its
one-for-one drains are a single Sanguine Bond behaving correctly.

## NEW-BASELINE record (mulligans)

| seat | offers | mulls taken | kept-hand shapes (the engine's own counts) | bottoming |
|---|---|---|---|---|
| deck125 | 6 | **0** | **1L/6S** (coverage named only `Elixir of Immortality {1}` - `vs126` seq 1, kept, **won** t55), 2L/5S x4 (Scatter; Scatter; Path; Path), 3L/4S with `would not cover any spell in it` (`vs130`) | none |
| deck126 | 6 | **0** | 4L/3S (2 Captains + Guardian covered), 2L/5S (Omens+Captain+Guardian), 3L/4S (Tutor+Captain+Battlement), 5L/2S (Sorin), 6L/1S (Battlement), 3L/4S (2 Tutors+Guardian) | none |

Twelve offers, **zero mulligans, zero `bottom` asks, no chain, no mull-to-zero, no floor text**. #131
re-run on both sections at write time: deck125's had **no branch for a ONE-land seven whose coverage
line names a card** - the `would not cover any spell` branch needs no coverage and the `2 to 5 lands`
branch needs two - so W87 adds it, odds-form, recorded explicitly as a completeness fix and an
outcome (the hand won), **not** as a punished break. deck126's section covered all six of its hands
(its own one-land branch already exists). No sentence pair disagrees on the eleven other keeps.
**No OWNER QUESTION arises.** deck130's #132-UNTESTED mulligan section is not my seat.

---

# THREE-NUMBER LITERAL AUDIT (#168) - every lanes-O/P literal, on my 12 logs

| literal | old-string renders | new-string renders | takes under the new string |
|---|---|---|---|
| `{paying this taps: <defender> - it cannot block on their turn}` (was `cannot attack this turn`) | **0** cast rows | **27** (all deck126) | **8** |
| `[DRAW PRICE: ... deals you N - you would be at K]` (+ `; this KILLS you`) | **0** bare-price rows | **15 records / 24 row-instances**, of which **8 records carry `this KILLS you`** | **1** total, **0** under `this KILLS you` |
| `{kills 0 of the N legal targets at -1/-1}` / `{kills: Name}` | 0 | **0** | 0 - **UNTESTED-AT-THIS-SEAT**: neither deck owns a fixed-damage or -N/-N targeted spell (Staff of Nin's ping is 1 damage and prints no kill clause) |
| `[second copy: ... no legend rule, but the effect it gives your OTHER permanents is already on - this copy adds only its own abilities]` (Lantern partial form) | **0** of the retired `[already owned:`; **0** of the bare form for a Lantern | **6** | **0** (was **5 of 5 taken** last corpus under the bare string) |
| `[second copy: ... legal, but its effect is already on the battlefield and a second copy changes nothing]` (full-dead form) | - | **0 corpus-wide** (I grepped all 42 logs) | - ; the bare form rendered **41** times on my seats (35 Lightmine Field, 3 Staff, 2 Sanguine Bond, 1 Exquisite Blood) with **4 takes** |
| `ability: <source>'s <effect> (aimed at <victim>) [from your/their <source>]` | bare `ability: <effect>` **0** | **340** (deck125 295, deck126 45) | n/a (stack lines) |
| edict such-clause `- a sacrifice is already on the stack aimed at this permanent` | 0 | **0** - **UNTESTED-AT-THIS-SEAT** (Tribute to Hunger targets the player, never a permanent) |  |
| `- and their <name> takes N off YOU: life A -> B` on edict gain rows | 0 | **0** - **UNTESTED-AT-THIS-SEAT** |  |
| `Hold priority - do not ask me again this turn unless the board changes` | - | **497** (deck125 476, deck126 21), **LAST row 497/497**, opponent's turn only | **4** (deck125 3, deck126 1) - and see HIGH #2 |
| `[you declined this exact list N times already this turn]` | - | **451 prompts** (deck125 421, deck126 30); 0 in any option row, as specified | n/a |
| PLAN header `(as you last stated it, N windows ago on turn T)` | PLAN blocks with no age stamp **0** | **1,289 records** | n/a |
| `{castable from exile ...}` naming its source | bare `[from exile]` 0 | **1** (deck126 `vs152` seq 27, `- your card, exiled by their Elite Spellbinder, which lets you cast it from there`) | **1** |

Battlefield header counting lands (lane P): **every** battlefield line on both seats reads
`... and N are lands`. PASS.

---

# deck125 - Revelation Fracture (UW draw-go). 4W-1L natural

## Prediction set A - wave-52 guide edits (W71-W76)

**W71 (a cast taken from a row whose printed price is at or above the seat's current life: 0) -
PASS 0/24 rows.** Lane O shipped the finished subtraction. Eight `[DRAW PRICE:` row-instances on
`vs162` seq 79-88 (t19-t20, life 10 down to 6) read `deal you 14 - you would be at -4/-5/-6/-7/-8;
this KILLS you`, and **all eight were declined** - including seq 88, where all three cast rows were
counters aimed at an Underworld Dreams on the stack. Last corpus's identical menu at 3 life took the
counter and lost the game in that window. **The single most valuable render change on this seat.**
Declines while a counter-on-sight name is on the stack: **1/1** (seq 88).

**W72 (Staff / Elixir activation from a row whose `leaves N` is below the cheapest counter in hand:
0) - FAIL 8 breaks.** I recomputed each take against the seat's own hand line rather than the row's
`needs` clause. **Elixir activations 5/16**: `vs126` seq 10 (t7, 20 life, leaves 0, Scatter+Fracture
+Gavel in hand), `vs123` seq 35 (t17, 20 life, leaves 0, Scatter), `vs123` seq 53 (t23, 25 life,
leaves 1, Scatter), `vs162` seq 23 (t7, 20 life, leaves 0, Gavel), `vs162` seq 70 (t15, 21 life,
leaves 1, Scatter+Gavel). **Staff 2/7**: `vs126` seq 69 and `vs123` seq 27, both `taps you out` with
an Essence Scatter in hand. **Lightmine 1/2**: `vs130` seq 25 (t15, leaves 0, Gavel in hand). None
was punished - lane N/I keep the opponent-turn window open - but the count went 6 -> 8 and the gate
is now the seat's least reliable rule. W80/W85a/W86b/W86e re-key it to lane L's
`{spends K of your M untapped mana sources this turn; <card> in your hand needs N}` clause, **with
the warning the corpus forced**: at `vs162` seq 23 that clause named a *Path to Exile*, not a
counter, so the clause is a subtraction the pilot may trust and a card name it may not.
Own-turn tap-outs followed by an opponent-turn resolution with **no ask record between: 0/5 games**.

**W73 (`X = 3` N/N; Revelation from a max-X of 1 or 2 above 5 life: 0) - PASS 15/15 on X, FAIL 2/116
on the row.** Fifteen X menus, fifteen right answers: thirteen `X = 3` off menus topping out at 4,
5, 8, 11, 14, 15, 19, 20 and 21, plus the two menus whose maximum was 1 and 2 (the guide's
"largest below 3" branch). **116 low-X Revelation rows, 2 taken**: `vs126` seq 59 (t19, 25 life,
`max affordable X=2`) and `vs123` seq 17 (t9, 20 life, `X=1`). Both menus' only other rows were ones
the guide forbids - a sweeper at `destroys 0 of their creatures`, a Path at a `[defender]` candidate
- so this is the "nothing else was castable" excuse the guide already names, returning. W81.
Revelation at life <= 5 with a header second number >= 1: **UNTESTED a third time** - per #132 the
check now lives on the row (`vs130` seq 38 is the nearest: t23, **3 life**, but the header read 0).

**W74 (land drop N/N; sweeper at header 0: 0; Lightmine below 3: 0; Path at a defender or a textless
token: 0) - PASS, PASS, PASS, FAIL 4/18.** Land drop **85/85**. Sweeper rows **230**, of which
**155 at a second number of 0**, **0 taken** - fourth corpus at that ceiling; the six sweeper casts
were at 3, 5, 2, 1, 3 and 1 attackers, and both single-creature casts were correct (a 6/5 Rorix
Bladewing with no Path in the list, `vs130` seq 36; a Final Judgment exiling a graveyard-recursion
Barrowin, `vs146` seq 207). Lightmine **117 rows, 2 casts**, at `they control 4 creatures able to
attack` and `3` - exactly the branch. Path: **18 casts**; `vs146` seq 97 spent one on a lone 1/1
Goblin token at 35 life (a real break, W79b keeps the rule for it) and `vs126` seq 109/157/193 spent
three on 1/1 **lifelink** Vampire tokens at 37/47/45 life - forbidden by the guide's letter, **right
in substance**, because `LIFE-TO-DAMAGE CONVERTER on the battlefield: theirs - Sanguine Bond` was
printed in all 353 prompts of that game and each Vampire was 2 off the seat per combat. W79a adds
the carve-out; W86c/W86d make the two Path passages agree (#177).

**W75 (TARGET asks with 2+ candidates: engine text, else `[flying]`, else power) - PASS 7/7.**
`vs146` seq 65 Triumphant Adventurer (venture) over a Goblin; `vs152` seq 17 Sigarda (4/4
flying, "Whenever Sigarda attacks") over Elite Spellbinder (4/2 flying); `vs130` seq 23 Siege-Gang
Commander over three Goblins; `vs162` seq 44 Fate Unraveler over two Shield Spheres; `vs126` seq
110/158/194 the lifelink Vampire over five [defender] walls (the power rule, 1 > 0 - and the right
answer for the converter reason too). A candidate whose only text is "When this creature enters"
taken over one of those: **0**.

**W76 (cleanup sending a counter with a sweeper / Lightmine / duplicate / spare land still listed:
0; a `[second copy:` Staff or Elixir declined: 0) - PASS on the second half 3/3, MIXED on the
first.** `[second copy:` **41 rows, 4 taken - every take a Staff of Nin** (`vs126` seq 91, `vs146`
seq 301, `vs123` seq 50), and **35 Lightmine Field second-copy rows declined 35/35**. Cleanup: **25
`discard` asks**, three of which sent a counterspell (`vs126` seq 61 and 268 an Essence Scatter,
seq 312 a Fall of the Gavel). Seq 312 is correct (Lightmine went with it, Emrakul stays above 13
lands). Seq 61 and 268 sent the CHEAPEST counter, which the ordered rule forbids - but both were
against deck126, whose every creature is a wall, so an Essence Scatter is that hand's dead card. The
guide's own carve-out said "when the opponent casts no creatures", which never fires on a wall deck;
W82a sharpens it to "when every creature the opponent has cast so far is a wall" and rules the two
sends correct. One real gap remains and is fixed: at `vs162` seq 87 the seat sent its **only Staff
of Nin** from a fourteen-card hand at 6 life, and the ordered list had no Staff entry at all.
#143 PUT-in-PLAN: **0/25**.

## The loss (deck125)

- **vs deck162 (t21, 0/20) - one counter bought at fourteen life.** The seat led 24-20 at t18 with
  the opponent's board at Underworld Dreams + Ob Nixilis + Forced Fruition, and every menu row
  correctly priced. `vs162` **seq 78** (t18, **24 life**): the reply took `Cast Essence Scatter` off
  a row reading `[DRAW PRICE: casting this draws YOU 7 cards (their Forced Fruition), and their
  Underworld Dreams, Ob Nixilis, the Hate-Twisted deal you 14 - you would be at 10]`, to counter a
  *third* punisher (a second Fate Unraveler). The Scatter resolved; seq 79 opens at **10**. From
  there the seat cast nothing else in the game - correctly, because from t19 every row read
  `this KILLS you` - and died on draw-step pings at t21. **STRATEGY, and the guide licensed it**:
  its gate was "cast a counter ONLY when that price is below your life minus 1", i.e. 14 < 23, so a
  rule written to stop a lethal cast permitted spending 58% of the seat's life to deny 1 damage per
  future card. W78a/W78b replace the derived comparison with the row's own `- you would be at K` and
  a fixed floor of 15. Also in that game: `seq 23` and `seq 70` are two of the eight W72 gate breaks,
  and the seat never resolved a Staff of Nin all game (it discarded the only one at seq 87).

## The near-loss that was not (deck125)

- **vs deck126 (t55, 19/0) - the converter rule broken twice, at a cost of 30 life, in a game the
  seat still won.** `seq 294` (t49, **36/8**, two Staffs ticking) took `Cast Emrakul, the Aeons
  Torn` with `LIFE-TO-DAMAGE CONVERTER on the battlefield: theirs - Sanguine Bond` printed. The
  other seat's `seq 106` is the receipt: `Cast Tribute to Hunger {right now: they control 1 creature
  - Emrakul, the Aeons Torn (15/15) ... is sacrificed, you gain 15}`, then
  `- You gained 15 life (now 23)` / `- Opponent lost 15 life (now 21)`. 36/8 became **21/21**.
  `seq 332` (t52, 20/19) took the row again. The seat had declined the identical row on the identical
  board at `seq 254`, `255`, `262` and `309` - so this is recognition drift across repeats, not a
  missing rule, and W77a/W77b/W77c re-key it from "read the battlefield line for an [enchantment]
  whose {effect:} reads ..." to the prompt's own one-line CONVERTER block.

## Other counted behaviour (deck125)

- **Opponent-turn windows: 479, acted on 60, cast 47.** By phase: Main 1 **123** (45 casts),
  Upkeep 61 (1), Draw 51 (0), Main 2 41 (0), Combat begins 37 (0), Attackers 35 (1), End 35 (0),
  Combat ends 34 (0), Cleanup 34 (0), Combat damage 15 (0), Blockers 13 (0). **140 windows at
  End/Cleanup/Combat-begins/Combat-ends produced zero casts across six games**, and I opened two of
  them (`vs146` seq 27 and 41, opponent's End step): no `ON THE STACK` section at all, one cast row
  (a Revelation at `max affordable X=1`), and the HOLD row. Core-loop item G1.
- Counterspells cast **39**, **0 of them on a wall or a `[defender]`** (fourth corpus at that
  ceiling), 0 on a `power 0` stack line. Two on artifacts the guide tells the seat to let resolve:
  `vs126` seq 67 (Fall of the Gavel on a Chromatic Lantern) and `vs123` seq 22 (Dream Fracture on
  Lightning Greaves, an Equipment). W83.
- Staff pings: every `Deal 1 damage with Staff of Nin targeting the opponent` window taken.
  `{this hits YOUR permanent}`: **5 rows offered, 0 taken** (33 across both seats, 0 taken).
- Emrakul attacks: 3 windows, 3 declared. Sweeper rows tailed `1 of yours` while an Emrakul was out:
  0 taken.
- `mana_only_windows_skipped`: **0** in all six games.

---

# deck126 - Sanguine Blood (WGB wall prison). 2W-4L

## Prediction set A - wave-52 guide edits (D75-D82)

**D75 (windows with BOTH names on the battlefield line and none of Rule #2's five starters
available: a cast N/N, `Cast nothing right now` 0; replies asserting damage TO the seat triggers
Exquisite Blood: 0) - FAIL 0/2 and FAIL 2, PASS 0 on the false-direction sentence.** The
false-trigger sentence is **gone** - not one reply this corpus claims their ping starts the loop.
But the behaviour it caused survived it: `vs146` **seq 22** (t16, **4 life**, Main 1) and **seq 23**
(t16, Main 2) both answered `Cast nothing right now` with both enchantments printed on the
battlefield line and `Cast Wall of Omens {1}{w}` printed on both menus. The seat died at t17 to four
attackers with one blocker on its board. Second corpus, same shape, different reasoning: the seq-22
reply spends 700 characters re-deriving the opponent's untapped creatures and never reaches the
guide's own "CAST SOMETHING EVERY TURN in that state" line. D87c.

**D76 (`[second copy:` rows taken whose card is a Lantern or a duplicate enchantment: 0; whose card
is a Staff: N/N) - PASS 0/6 on the Lantern, FAIL 1/3 on the enchantments, UNTESTED on the Staff.**
The Lantern half is **fixed and the fix is measurable**: lane P's partial form rendered 6 times and
the seat declined **6 of 6**, including three windows in `vs162` (t11/t13/t15, at 18/10/1 life)
where the second Lantern was the ONLY cast row on the menu. Last corpus the bare string got 5 takes
of 5. The surviving hole is the enchantments, which correctly get the BARE tag because their auto
lines genuinely stack: `vs125` **seq 22** (t16, 20 life) cast a second Sanguine Bond off it on a
two-row menu. D86, D91a.

**D77 (a PLAN line naming a permanent NOT on the seat's battlefield line: 0; a decision taken on an
echoed plan whose board claim is false: 0) - PASS 0 + PASS 0.** The `vs146` false-board plan that
carried across three windows last corpus did not recur; lane N's age stamp
(`as you last stated it, N windows ago on turn T`) rendered on **248/248** deck126 records. One
adjacent failure remains and it is not a board claim: `vs146` seq 21's plan asserts that an Overgrown
Battlement's block "gain[s] 2 life (loop triggers)" with no Perimeter Captain on the line. D87a.

**D78 (land drop N/N; `CHOICE:` lines whose parenthesised name disagrees with the number: 0) -
PASS 52/52, FAIL 1.** The `name_over_index` land-drop miss is gone. One `echo_index_conflict`
remains (`vs130` seq 21) and it is an engine item, not a guide one - MED #5.

**D79 (Tribute at `at 0 this does nothing`: 0; Tribute in the FIRST named non-defender-victim
window: N/N; at a named [defender] victim above 10 life: 0) - FAIL 2/39, PASS 3/3, UNTESTED.**
The zero-stop broke for the first time in four corpora: `vs125` **seq 73** and **seq 74** (t42, 6
life), two windows of one turn, each a two-row menu whose other row was the decline. Both replies'
PLAN lines say not to cast it. D84. The named-victim rule is at its ceiling and produced the
corpus's best single decision: `vs125` seq 106 ate a **15/15 Emrakul** for 15 life; `vs130` seq 12
(Dwarven Blastminer) and seq 33 (Rorix Bladewing at 1 life, which won the game). Ten multi-creature
rows, 5 cast. No [defender]-victim row arose: **UNTESTED**.

**D80 (`reveal` picks naming a half already held: 0; Tutor cast with the missing half on the hand
line and its Cast row on the same menu: 0) - FAIL 2/9, PASS 0/22.** Nine picks: seven correct
against the printed clause. The two misses (`vs125` seq 15, `vs130` seq 22) are downstream of the
same cast: **the Tutor itself**, taken twice off
`[finds only an enchantment card - every enchantment left in your library is a copy of one you
already control or hold: ...]` with BOTH halves on the hand line (`vs125` seq 14 t10, `vs130`
seq 21 t21). The clause is accurate in all 22 renders; the guide had no stop for it. D85, D90, D91c.

**D81 (a blocker whose every line reads "your blocker dies" assigned while every attacker is already
blocked by a survivor and no enchantment is out: 0) - PASS 0/14.** Fourteen `blockers` windows, all
substantively right except the one D88 names, which is the opposite error: `vs146` seq 24 answered
`BLOCKS: none` at **4 life** against four attackers with an untapped 0/4 on the B-line, and the
reply wrote "I will concede this game". Rule #5's own carve-out says the bench is OFF at
`you would be at 8` or less.

**D82 (`{paying this taps:` source count == receipt count N/N; Battlements tapped for a payment one
covers: 0) - PASS 27/27 and PASS 0/27.** Lane H's residual is gone: every one of the 27 clauses
names exactly the sources its receipt then spends, and no receipt names two Battlements for a cost
one covers. Wave-52 engine #2 **CLOSED**.

## The losses (deck126)

- **vs deck125 (t55, 0/19) - the seat played the long game correctly and lost it on three rule
  breaks that each cost a turn.** deck125 countered Exquisite Blood (t14, Dream Fracture), the
  second Bond (t18, Cancel) and an Idyllic Tutor (t26, Fall of the Gavel), so Sanguine Bond alone
  carried from t14. With Bond out, Sorin's Vampires took deck125 from **49 to 19** over t34-t53 and
  the Tribute at Emrakul took 15 more - the deck's whole thesis working. The three breaks: seq 22
  (second Bond, D86), seq 14 (dead Tutor, D85), seq 73/74 (two Tributes at 0, D84). Each spent a
  main phase that Rule #7 would have spent on a wall or a Vampire.
- **vs deck146 (t17, -10/16) - the pair assembled at t14 and the seat stopped playing.** Bond t10,
  Blood t14, both printed on the battlefield line at seq 21-23. Seq 22/23 declined `Cast Wall of
  Omens` twice at 4 life (D75/D87c); seq 24 answered `BLOCKS: none` and wrote a concession (D88).
  Vanishing Verse exiled the Bond on the turn that killed the seat. Latencies on the two decisive
  windows: **718.2 s and 608.1 s**, both replies visibly self-arguing (engine LOW #8).
- **vs deck162 (t16, 0/21) - no lever.** Bond alone at t9; Exquisite Blood never drawn or tutored;
  Underworld Dreams took the seat 20 -> 1 on draw steps. From t11 the ONLY cast row on every menu
  was a second Chromatic Lantern, declined 3/3 - correctly.
- **vs deck123 (t12, -63/20) - the unbounded-token class, fifth citation.** Tribute at 2 creatures
  t9, then `{right now: they control 26 creatures}` at t11, then -63. INFO #7.

## Other counted behaviour (deck126)

- **Rule #7 entry 1: 10/10.** Every `Cast Sanguine Bond` / `Cast Exquisite Blood` row that was not a
  second copy was taken **the turn it first appeared**, in all five games where one appeared
  (`vs125` t12/t14, `vs152` t10/t16, `vs146` t10/t14, `vs162` t9, `vs130` t25/t27).
- Land drop **52/52**. Attackers: 12 windows - every Vampire sent whenever the opponent had 0 or 1
  potential blockers (deck125 has no creatures at all), `no attackers` twice against deck123's 26.
- Blockers 14 windows; Perimeter Captain / Pride Guardian life triggers accepted in every window
  they were offered (`accept ALL 2 triggers`, `accept ALL 6 triggers`).
- `mana_only_windows_skipped` per game: 276 / 0 / 76 / 0 / 54 / 0. The 276 in `vs125` is the largest
  figure on either seat and it is lane N doing its job.
- Opponent-turn windows: **32** total, 12 acted, 1 cast - against deck125's 479. The asymmetry is
  the decks (deck126 holds one instant), not the lane.

---

# Prediction set B - engine lanes (where my seats had windows)

| lane | verdict | evidence |
|---|---|---|
| **M** 614312289 (blockers seam) | **UNTESTED-AT-THIS-SEAT** | **0** `multiblock_reask` records on 12 logs (was 1 last corpus), so the "no follower, no `chosen_text`" prediction has no window. 14 `blockers` records, every one with a `chosen_text` naming real pairings or `no blockers`; `blocker_handle_in_attacker_slot` 0. The BLOCKS format line's one-per-blocker clause renders. I did **not** read stderr for `declared blocks from N assignment(s)` on my games. |
| **N** 3ab5c27c4 (window economy) | **SPLIT: PASS on the casting phases, FAIL on the window count and on the HOLD row** | End / Cleanup / Attackers / Combat-ends casts: **0** across 479 opponent-turn windows - PASS. `CHOICE: n (Pass)` on a `0. Pass priority` menu: no `pass_row_named` record and no mis-execution. PLAN blocks with no age stamp **0/1,289**; `plan_choice_conflict` records **0**. But opponent-turn windows per seat-turn went **UP**, not down >= 30%: deck125 played 261 turns in both corpora and took 198 opponent-turn windows last corpus against **479** this one. Consecutive runs of 3+ byte-identical DECLINED menus: **385 / 1,254 = 30.7%** (deck125 34.7%, deck126 11.9%), against the "< 10%" prediction and the 23.0% baseline. And the lane's own **FALSIFIER fires**: `hold_windows_skipped` reads **0 on all eleven `gameend` records**, with the row taken 4 times. HIGH #2. |
| **O** 0ba5ae2eb (pricing) | **PASS on every branch with a window** | `[DRAW PRICE:` rows with damage >= life carry `this KILLS you` **8/8** (was 0/41); casts from such rows **0**. Defender-naming tap rows printing `cannot attack` **0/27**, `cannot block on their turn` **27/27**. Kill-count clauses and converter-drain folding: **no window** (see the audit table). CARRY (fight rows): no window. |
| **P** 1057c7c9d (render/state) | **PASS on every branch with a window** | Bare `ability: <effect>` stack lines **0/340**. Edict rows naming a permanent already on the stack: no window. No ask menu > 12 rows collapsed to <= 2 shapes; largest non-log prompt segment well under 12 KB (max whole prompt 31,553, of which the log is the bulk). `{castable from exile}` naming its source **1/1**. `[second copy:` takes on non-stacking scripts **0** (was 4/14) - the Lantern's 6 rows were all declined; the Lantern is never called dead, it is called partially redundant, which is what its script says. Battlefield header counts lands: every line. |
| **Q** ba33a9adb (observability) | **PASS 2/2 on the recovery contract; the timeout branch UNTESTED** | Every `choice: -1` **fallback** record is followed by exactly one `kind: "recovery"` record (deck125 seq 365 -> 366, deck126 seq 9 -> 10) and by a real re-ask. **No `empty_reply` and no `timeout` on my seats**, so the `>= 95% of the deadline -> fallback "timeout" + retry: 1` stamping is UNTESTED-AT-THIS-SEAT - though deck126 `vs125` seq 13 **answered** at 868.7 s (96.5% of the 900 s wall) with no stamp of any kind, which is LOW #8. D20 (adjacent runs) declined on measurement: my 30.7% figure is the same measurement disagreeing - see HIGH #2. NOTE: the hung game's `choice: -1` at `deck152` seq 28 carries **no fallback field**, so it gets no `recovery` record either; a -1 with no fallback tag is currently invisible to this lane. |
| **R** 1bb08b2b1 (Go-Shintai `may pay({1})`) | **UNTESTED** | No `may` prompt and no Baka fallback answer on either seat. Dominating Vampire not in the pool. |
| **S** 6a4d5e119 (hand-refresh perf) | **UNTESTED by construction** | Human-seat only; no corpus exposure. |
| **T** 00549b391 (Baka aggregate-lethal blocking) | **UNTESTED** | **0** `blockers` fallback records on 12 logs, so Baka never blocked for either seat. The owner's Vita play is the test. |
| wave-52 owner batches, Vita evolve, Baka pump-targeting (5ca11d6e2), option-preserving autotap (c1a0bf814) | **UNTESTED** | No adventure/omen/morph/split/aftermath card, no Equip menu, no evolve or counters-matter card, no Baka take on either seat. The autotap is human-only and, unlike last corpus, **no forecast-vs-receipt mismatch arose at all** (0/27 on deck126, the seat that produced last corpus's 1/7). |

---

# Engine / render items (ranked, with repro)

Corpus root: `~/.Wagic/ai/gpt/selfplay-runs/matchups-20260902-023342/`

**HIGH - #1: the `152v125` hang is Sigarda's Coven `reveal:5`, not Ranger Class - and the seam that
swallowed it returned `-1` with an empty prompt.**
The corpus note points at Ranger Class; the stderr says Ranger Class *resolved*. Pinned sequence,
from `game-152v125-1788334434.stderr` (1,329 lines) and both seat logs:
1. `...deck152-0x5583d9910230...jsonl` **seq 26** (t15): `ATTACK: A1, A2, A3, A4` - Sigarda,
   Champion of Light among them.
2. stderr line 1103-1106: the Ranger Class level-2 `TARGET CHOICE` is answered with Sigarda, the
   ability is pushed and **`Resolving Action on stack: StackAbility. (Source: Ranger Class)`** at
   1106 - the Ranger Class trigger completes.
3. stderr 1107: `Resolving Action on stack: StackAbility. (Source: Sigarda, Champion of Light)` -
   the first Coven trigger begins resolving.
4. `...deck152...` **seq 28**: `kind: "reveal"`, `phase: "Blockers"`, `options: 5`,
   `chosen_text: "none (no legal target)"`, `choice: -1`, `latency_ms: -1`, **`prompt` empty**,
   `reply` empty - **the model was never asked**. This is Sigarda's
   `reveal:5 optionone name(Get a human) target(<upto:1>human|reveal) moveto(hand) optiononeend`
   (`borderline.txt:102246`): a `<upto:1>` target set that is empty short-circuits the seam.
5. stderr 1101: `[combattrace] t14 ph7 seat=0x5583dac4bbe0 combat decision suppressed by
   unresolved-stack` - **`0x5583dac4bbe0` is the deck125 seat**, whose log therefore stops at
   **seq 17** (t12, a Path TARGET ask).
6. stderr 1145-1146: `ActionLayer::doReactTo 149` then
   `Action added to stack: StackAbility. (Source: Sigarda, Champion of Light)` - a SECOND Coven
   trigger is pushed and **never resolves**. After line 1146 the file contains nothing but repeated
   `AttemptNew failed to load (404): ZNR/491910.jpg` (Sigarda's own art) until the harness ends the
   duel ~13 h later. No ticks, no further engine line: an engine hang, as the note says.
Suite pin: a `reveal:N` sub-choice whose `optionone` target set is empty must finish its trigger
(taking `optiontwo`, or resolving to nothing) rather than leaving the StackAbility unresolved; and a
`reveal` record with `choice: -1` and an empty prompt should carry a fallback class so lane Q sees it.

**HIGH - #2: the HOLD row cannot be honoured on the opponent's turn, because the hold key contains
the opponent's hand size and both libraries - which their draw step changes one phase later.**
`hold_windows_skipped` reads **0 on all eleven of my `gameend` records** while the row was taken 4
times. Repro: `1788334424-ai_baka_deck125-0x55c56d4af610-vs-ai_baka_deck126.jsonl` **seq 128**
(t30, opponent's Upkeep, `priority` seam, 2-row menu, HOLD taken) -> **seq 130** (t30, opponent's
**Draw**, the byte-identical 2-row menu, asked again), and `game-125v126-1788334422.stderr` logs
exactly one `hold re-opened at the priority seam - the board changed`. Twelve more windows followed
in that same turn. Mechanism, read out of `AIPlayerGPT.cpp`: `holdBoardKeyOf()` (line 12904) drops
only the leading phase line, so the key retains `Opponent hand size: N | Opponent library: M cards`
and `Your library: N cards`; `holdStillStands()` (12917) treats any key change as a re-opener. The
opponent's draw step changes two of those counters on **every** opponent turn, one phase after the
Upkeep window where a hold is naturally taken. Proposal: exclude hidden-zone counters (opponent hand
size, both library counts) from `holdBoardKeyOf` - a card drawn into a hidden zone is not a board
change the hold was about - or take the hold from the first post-draw window. Suite pin: a hold taken
at the opponent's Upkeep survives their draw step when nothing else on the board changed.
Consequence: lane N's window economy did not reach these seats at all (479 opponent-turn windows,
140 of them in phases that produced zero casts in six games, 30.7% of all decisions inside runs of
3+ identical declined menus).

**MED - #3: the annihilator sacrifice is framed as `your own effect`, and it cost the seat its only
lifelink body.** `1788334425-ai_baka_deck126-0x55c56e7c5d20-vs-ai_baka_deck125.jsonl` **seq 111**
(t53): `SACRIFICE ONE OF YOUR OWN CARDS: your own effect (Emrakul, the Aeons Torn) makes you
sacrifice a card from the list below ... Choose card 1 of exactly 6`. Emrakul is on the **opponent's**
battlefield. `buildHandRemovalAsk()` (`AIPlayerGPT.cpp:20105`) already has the correct branch -
`byOpponent` prints `FORCED SACRIFICE OF YOUR OWN CARD: the opponent's effect (<name>) forces YOU
to ...` - and it was not taken here. The reply picked row 23, its lone 1/1 lifelink Vampire, out of
a 23-row list holding 15 lands; at seq 112 (with the same framing) it reasoned correctly and started
sacrificing Sunpetal Groves. The record's `phase` also reads `Blockers` for an annihilator trigger.
Suite pin: an annihilator/edict sacrifice sourced from a permanent the seat does not control takes
the `byOpponent` framing.

**MED - #4: a reply whose PLAN contradicts its own CHOICE is executed silently, twice in one turn,
on a row the engine itself labelled a no-op.**
`1788334425-ai_baka_deck126-0x55c56e7c5d20-vs-ai_baka_deck125.jsonl` **seq 73** and **seq 74**
(t42, 6 life). Both menus are two rows: `1. Cast Tribute to Hunger {2}{b} {right now: they control
0 creatures - at 0 this does nothing}` / `2. Cast nothing right now`. Seq 73's reply:
`CHOICE: 1 (Cast Tribute to Hunger)` + `PLAN: ... Avoid casting Tribute to Hunger as there are no
creatures to target.` Seq 74's: `CHOICE: 1 (Cast Tribute to Hunger)` + `PLAN: The opponent has no
creatures, so Tribute to Hunger does nothing. Pass the turn.` Both executed; `answer_replaced`
false, `parse_note` absent, no `decision_reversed_in_prose`. Lane J's prose-verdict detector reads
the prose BEFORE the CHOICE line; the contradiction here lives in the PLAN line AFTER it. Proposal:
extend the detector to the PLAN line when the chosen row's own annotation says the action does
nothing (`at 0 this does nothing`, `deals 0`, `destroys 0 of their creatures`) - stamp it, at
minimum, so the ledger can count it. Guide half is D84.

**MED - #5: the echo matcher binds a card name that appears only inside another row's annotation.**
`1788334457-ai_baka_deck126-0x55e818418360-vs-ai_baka_deck130.jsonl` **seq 21** (t21). Menu:
`1. Cast Idyllic Tutor {2}{w} [finds only an enchantment card - every enchantment left in your
library is a copy of one you already control or hold: Exquisite Blood, Sanguine Bond] ...` /
`2. Cast Overgrown Battlement` / `3. Cast nothing right now (combat comes next this turn)`. Reply:
`CHOICE: 3 (Cast Sanguine Bond)`. `Sanguine Bond` is on no row's label and appears exactly once on
the whole menu - inside row 1's `[finds only ...]` list. Result: `parse_note: echo_index_conflict`
(no `name_over_index`), **row 1 executed** - neither the index the model wrote nor a row that offers
the card it named. The same shape at `deck126 vs125` seq 9 correctly produced `named_row_not_offered`
and a re-ask, so the seam resolves one shape two ways. I did **not** isolate which of the four
`echoRemap` branches fired (`AIPlayerGPT.cpp` 15147 / 15327 / 15391 and the subset fallback at
15268); the file already has `stripRenderAnnotationsLc()` and applies it on the exact-head path.
Proposal: match echoed names against annotation-stripped row cores on **every** tier, so a name that
only appears inside `[finds only ...]`, `{kills: ...}`, `{right now: ... <name> is sacrificed}` or
`[second copy: you already control <name>]` can never bind. Cost this time: one wasted Tutor in a
game the seat won.

**MED - #6: 140 opponent-turn windows in six games at phases that have never produced a cast.**
deck125: opponent's End 35, Cleanup 34, Combat begins 37, Combat ends 34 - **0 casts in any of
them**, this corpus or the two before. Two opened at random (`1788334427-ai_baka_deck125-
0x55be9ff4b380-vs-ai_baka_deck146.jsonl` seq 27 and seq 41) carry no `ON THE STACK` section at all
and one cast row: a Sphinx's Revelation at `max affordable X=1`, which the guide forbids at any life
above 10. Routed as a core-loop efficiency item (`general-suggestions.md` G1), **not** a legality
change - lane I's windows are why this seat can counter at all. The narrow ask: an opponent-turn
window with an empty stack, at a phase where the seat has never cast, is the natural companion to
`mana_only_windows_skipped`. It is also the cheapest way to reach HIGH #2's 30.7% figure without
touching the hold key.

**LOW - #7 (INFO): the unbounded-legal-loop class, fifth citation.** `deck126 vs123`:
`{right now: they control 26 creatures - they choose which one}` at t11 under Intruder Alarm +
Thraben Doomsayer, from an opponent at 20; the seat died at **-63** on turn 12. Same class as the
34-Vampire and `(x25)` citations. The engine seat owns it.

**LOW - #8: three replies between 600 s and 869 s, none of them stamped, and two of them decided a
game.** `deck126 vs125` **seq 13** answered at **868,700 ms** - 96.5% of the 900 s `WAGIC_GPT_TIMEOUT`
- on a **3-row, 5,780-char** menu; its reply is a visible self-argument
(`... to find the second Sanguine Bond (since we already have one in hand and one on the
battlefield? Wait, we have Sanguine Bond in hand and Exquisite Blood in hand. We do NOT have
either...`) and it is the seat's only `answer_replaced`. `deck126 vs146` **seq 22** (718.2 s) and
**seq 23** (608.1 s) are the two windows that lost that game (D75/D87c), and both replies are the
same runaway shape. Lane Q stamps EMPTY replies at the wall; a non-empty reply that eats 96% of the
budget is currently indistinguishable from a 5-second one in the ledger. Proposal: stamp any reply
above the same `>= 95%` threshold with a `long_reply` note, and consider a soft per-ask deadline
scaled to prompt size - 900 s for a 5.8 KB, 3-row menu is three orders of magnitude of headroom.

**INFO - #9: `multi_answer_first_taken` is benign here.** `deck125 vs126` seq 236 and `vs123` seq 90:
the model wrote two `CHOICE:` lines to ping with both Staffs of Nin. The seam took the first; the
second Staff got its own window immediately (seq 238) and was pinged. No damage lost. Recorded so
the note is not mistaken for a dropped decision.

---

# Rotation evidence

- **deck125 - KEEP IN ROTATION.** 4-1 natural, and the single loss has a single named cause with a
  render behind it (W78a). Ceilings this corpus: sweeper at header 0 **155/155**, land drop
  **85/85**, `X = 3` **15/15**, counters on walls **0/39**, `{this hits YOUR permanent}` **0/5**,
  Lightmine below 3 attackers **0/117**, `[DRAW PRICE: ... this KILLS you]` casts **0/8**. Two rules
  are drifting and both are now re-keyed to literals rather than to arithmetic: the tap-out gate
  (8 breaks, up from 6) and the Emrakul converter stop (2 breaks, 30 life, in a game it won).
  Re-test W77, W78 and W80 before any freeze talk.
- **deck126 - KEEP IN ROTATION.** 2-4, and the two wins are the deck's thesis executing exactly as
  written from 4 life and from 1 life. Of the four losses, two had no lever (a draw lock with one
  combo half; a 26-body token loop) and two are the same two turns of passivity that D75 named last
  corpus - now with a second corpus of evidence and four edits (D84/D85/D86/D87). The lane-P Lantern
  fix converted a 5-of-5 failure into a 6-of-6 ceiling with no guide change at all, which is the
  clearest render-to-behaviour result on either seat. Re-test D84-D88 before freeze talk.

# Lategame specimen candidate

Primary: `1788334424-ai_baka_deck125-0x55c56d4af610-vs-ai_baka_deck126.jsonl` **seq 369** (t55,
**31,553 chars**, kind `priority`): the deepest prompt on my seats, at 19/0 on turn 55 of a 55-turn
game, with a 24-land seat line, two Staffs of Nin, an Emrakul on the battlefield and an opponent
board of five walls, a Sorin at 14 loyalty and a Sanguine Bond - so the CONVERTER block, the
`[second copy:` tag, the HOLD row and the declined-count literal are all live in one record.
Alternates: the corpus baseline `1788334424-...deck125...` **seq 357** (t84 in the 85-turn `vs146`
game, 28,481 chars, an `ask`), and `1788334425-ai_baka_deck126-0x55c56e7c5d20-vs-ai_baka_deck125.
jsonl` **seq 120** (t54, 30,210 chars, the Sorin -6 window at 2 life).

# Falsifiable predictions for wave 54

deck125:
- **W88** - Emrakul rows taken while the prompt carries `LIFE-TO-DAMAGE CONVERTER on the
  battlefield: theirs`: **0** (was 2/6). Emrakul rows DECLINED on that line: **N/N** (4/6).
- **W89** - casts taken from a `[DRAW PRICE:` row whose `- you would be at K` is below 15: **0**
  (was 1, and it was the loss). Casts from a row carrying `this KILLS you`: **0** (0/8).
- **W90** - tap-out-gate breaks (Staff, Elixir activation, Lightmine, Revelation) counted against
  the cheapest counter on the hand line: **0** (was 8: Elixir 5/16, Staff 2/7, Lightmine 1/2).
- **W91** - Revelation cast from a `{X pricing: max affordable X=1` or `X=2` row above 10 life:
  **0** (was 2/116). `X = 3` announced whenever 3 is on the menu: **N/N** (13/13).
- **W92** - Path cast at a candidate whose every option reads printed power 0-1 or `[defender]`,
  with **no** `CONVERTER ... theirs` line printed: **0** (was 1/18). Path at a `[lifelink]`
  candidate WITH that line printed: **N/N** (3/3).
- **W93** - counters cast at a stack line printing `(artifact)` with no P/T and no damage/draw/token
  text: **0** (was 2/39). Counters cast at a `[defender]` or power-0 stack line: **0** (0/39).
- **W94** - cleanup asks sending the seat's only Staff of Nin: **0** (was 1/25). Land drop
  **N/N** (85/85); sweeper at header second number 0 **0** (0/155).

deck126:
- **D92** - `Cast nothing right now` taken in a window where both enchantment names are on the
  battlefield line and any `Cast` row is printed: **0** (was 2). A win-button entry taken in the
  first window it appears: **N/N** (2/2 - both wins).
- **D93** - Tribute cast off `at 0 this does nothing`: **0** (was 2/39). A `CHOICE:` line naming a
  row its own PLAN line argues against: **0** (was 2).
- **D94** - Idyllic Tutor cast off `[finds only ... every enchantment left in your library is a copy
  of one you already control or hold: ...]`: **0** (was 2/22). Tutor cast off the
  `still in your library, not on your battlefield or in your hand:` form when a half is missing:
  **N/N** (8/8).
- **D95** - `[second copy:` rows taken whose card is a Sanguine Bond, an Exquisite Blood or a
  Chromatic Lantern: **0** (was 1 Bond of 3, 0 Lanterns of 6).
- **D96** - `BLOCKS: none` answered at a header reading `you would be at 8` or less with a blocker
  on the B-line: **0** (was 1/14). Replies containing the word "concede": **0** (was 1).
- **D97** - replies asserting that a block by an Overgrown Battlement or a Wall of Omens gains life
  with no Perimeter Captain or Pride Guardian on the battlefield line: **0** (was 1).
- **D98** - `{paying this taps:` source count == receipt source count: **N/N** (27/27, wave-52
  engine #2 stays closed). Land drop **N/N** (52/52).

Both seats:
- **D99/W95** - `hold_windows_skipped` on a `gameend` record where the HOLD row was taken at least
  once: **> 0** (was 0/4 takes on 11 records). This is HIGH #2's pin and it does not need a guide
  change to move.

# What I did NOT check

- I did not run wagic, did not touch `bin/Res` or `src/` (beyond **reading** `AIPlayerGPT.cpp`,
  `MTGAbility.cpp`, `AllAbilities.h` and `Player.cpp` for the emitter strings and the hold/echo/
  sacrifice branches named above), did not run git, and did not deploy. All guide work is under
  `wave53/`.
- **Scryfall: nothing cross-checked.** Every card fact is a primitive's `auto=`/`text=`/`abilities=`
  line or an engine-rendered string. No primitive/Scryfall disagreement is therefore reported.
- I read `game-152v125-1788334434.stderr` in full for the hang and grepped three other stderrs for
  hold lines. I did NOT read stderr for the remaining eight of my games, so lane M's
  `declared blocks from N assignment(s)` check is unverified at this seat.
- I did not isolate which `echoRemap` branch produced MED #5; I established that the echoed name
  occurs nowhere on the menu except inside row 1's annotation, and that the executed row was row 1.
- Colour-pip auditing: I checked `- Paid` receipt COUNTS, no-source counts and forecast-vs-receipt
  agreement (27/27 on deck126), not pip-by-pip source choice.
- I audited deck126's 14 blocker replies against their A-line `deals N` numbers; deck125 has no
  creatures until an Emrakul lands and I did not audit the opponents' blocking in any game.
- Lanes A, F, K's life-cost and `[legendary:` branches, L's Equip-block and `makes no mana`
  branches, the adventure/split/morph and evolve/equip owner batches, lane R's `may` prompt, lane T's
  Baka blocking and lane S entirely: **no window on my seats** (UNTESTED-AT-THIS-SEAT, not absence).
- I counted `mana_only_windows_skipped` off the `gameend` records; I did not open the 276 skipped
  windows in `deck126 vs125` to confirm none reached the model.
- I did not determine why `deck126 vs125` seq 13 took 868.7 s beyond reading its reply; no server-side
  data was consulted.
