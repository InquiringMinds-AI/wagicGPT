# Wave-75 step-3 brief (lean; REJECT is the expected verdict for anything not paid for by a decision the corpus shows going wrong. Amendment 332 stands: guides are decision-time instruction only, hard ceiling 20,000 bytes, no citations/history in a guide — evidence goes in YOUR review.md. Amendments 333 (heading = scope declaration) and 334 (no concessive clause for a rule's hard case) are DRAFTED in wave74/strategy-writing-skill-v2-amendments.md, not yet folded into the skill by the owner — apply them anyway.)

Sixth reasoning-on corpus (invariant 000). Wave 75 step one (wave74/known-bugs.md §P → wave75/lane-{CI,CJ,CK,CL}.md + wave75/codex-review.md → lane-CM.md, nine findings all CONFIRMED and fixed): CI window/hold — the O11 in-row decline tag REVERTED (the fact rides the prompt-only note only; P1a), the hold-check bracket keys on the row's ACTION identity now WITH its `[cost:]` group (P1b + CM F2), the main-phase-skipped meter's offered-slot overwrite fixed (P13: wave 74's 28 false counts were (turn,4)/(turn,10) pairs), P8 post-counter re-ask REFUTED (the mana went with the countered spell; a trace now names the cast seam's silent exit), both MDFC faces as rows on the first land-drop window with the answer carried into the face menu (P18), the hold-row sentence braced and the once-per-prompt `[HOW A HOLD ENDS:` paragraph rewritten TRUE (P19; row 1,074-1,287 B → 493 B median), casting-seam chain census (P12, measure only), the early-check keyed on the run outdir + PILOT STALL requiring every live seat's NEWEST record to be a failure (P24 + CM F9); CJ transport — the phase-2 forced-close HTTP 400 root-caused to the Qwen chat template's `|trim` on the prefill (P2: prefill body now trimmed with Python's Unicode whitespace set, think markers stripped; non-200 bodies logged as `http_error_body`; `forced_close_unrecorded` on gameend, armed flag PER ARM — CM F5/F6), the 64-char truncation root-caused to `max_tokens` bounding the whole decode (P21: force-close arm now fires on cap-with-no-coded-answer; one coded-answer reader shared with P9 — CM F4), inline `CHOICE:` on the PLAN's physical line read when it has exactly one label, whitespace-anchored, a DIGIT head and a clean tail (P9 + CM F3), the repeat row's refusal-and-re-ask restored on the one-per-board ladder (P14), `reasoning_ngram_repeat` 8-gram meter (P20, measure only); CK engine truth — planPayment skipped foreach-wrapped VARIABLE producers in every pass so a Battlement could pay generic in no pass (P3: Exquisite Blood `[cannot pay now]` fixed at the shared payability path), P4 premise REFUTED (Sorin's -6 charged six; the real bug was Sorin targeting ITSELF — card data now `other!creature *[creature;planeswalker]` via a new target-DSL form (CM F1: an animated Sorin stays a legal creature target), new suite command `assertcantarget`; NO re-entry latch — a returned walker is a new object), crack-back cover no longer counts a legend-rule duplicate, tokens included (P7 + CM F8), pathway parse-error noise (P22: the `{t}:add{g}` line was innocent — the parser printed the consumed text); CL render truth — a loop-fed self-damage row prints `you would be at 0 - this row feeds their chain` (P5; infect sources print a POISON forecast instead — CM F7), `CRACK-BACK NEXT TURN` in every own-turn window (P6), own-clock gate takes the worst stated loss combat-or-stack and any stated loss under a proven loop (P10), keep-X names `another copy of` (P11), range paragraph decodes ascending AND descending X ladders (P15), `repeatShortName` cut on a word boundary (P16 — the reviewer's "O18 not applied at Equip" was wrong), `plan_names_uncastable_zone_card` (P17, measure only), one `protocol_deviation_replies` class (P23d). Records carry `thinking`, `reasoning`, `reasoning_chars`, `off_protocol_bytes`, `plan_line_missing`, `plan_steps_done`, `http_error_body`, `reasoning_ngram_repeat`; gameend carries the counters below. Wave-74 guide edits for all seven decks are the LIVE guides (installed 2026-09-10 at cce04d4af; each deck's before->after list is in wave74/deck<N>/review.md).

Repo /home/magi/Projects/wagicGPT, game dir projects/mtg. Use python for translogs; use /usr/bin/grep (bare grep drops matches). DO NOT edit anything under bin/Res, src/, or run git or wagic; write only under projects/mtg/strategy-design/wave75/. Never write mechanisms as the owner's positions. Report what you did NOT check.

CORPUS (wave 75, SINGLE binary archives/wagic-7a04cd6da-w75step1 = source 14fea99f3; `--thinking on`, WAGIC_GPT_TIMEOUT=900, no operator token ceiling; harness early check at +25 min: 42 seats, 263 asks, max 4 asks in any phase, no loop signature; the one game past the 8 h mark, 125v123, ran 82 turns and finished naturally): /home/magi/.Wagic/ai/gpt/selfplay-runs/matchups-20260910-003556-final (21 games, all completed naturally, 0 HUNG/crashed/timeouts, 0 reruns; 2,919 decisions (wave 74: 2,396) = ask 2,118 (wave 74: 1,866) + priority 603 + attackers 102 + discard 53 + blockers 27 + reveal 10 + bottom 6, 100% with native reasoning (median 5,678 chars, max 26,686), 0 fallbacks of ANY kind (wave 74: 4), 0 reply_truncated (wave 74: 1), 0 action_before_plan, 71 off_protocol_bytes>0 (3.4%, wave 74: 4.6%), 83 plan_line_missing (3.9%, wave 74: 5.0%), latency p50 80.4 s / p95 185.6 s, decisions/turn 2.15 ask-seam (wave 74: 2.04); gameend sums: `ask_replays_reserved` 1,801 / `identical_ask_answers_reserved` 1,332 (wave 74: 584 / 1 — explain the jump, see engine seat (l)), `hold_released_turn` 115 (wave 74: 70), `hold_windows_skipped` 1,601 (cast 1,075 / priority 526; wave 74: 1,262 = 1,068 / 194), `stop_reached_windows_skipped` 19 (wave 74: 30), `sibling_window_asks_skipped` 0 and `chain_windows_collapsed` 0 (again), `chain_windows_only_selfharm` 11 / `chain_selfharm_rows` 1,108 / `chain_acting_rows` 2,355 at the priority seam + `_cast` 482 / 692 / 1,445 at the casting seam (P12 — first casting-seam census), `main_phase_windows_skipped` 94 (wave 74: 72 with 28 false), `mana_only_windows_skipped` 232, `own_turn_windows_skipped` 650, `reserve_decline_windows_noted` 21 / skipped 3, `plan_names_stranded_card` 5 (quote all five), `plan_names_uncastable_zone_card` 68, `protocol_deviation_replies` 107, `async_drops` 70 (wave 74: 84), `phase2_answer_recovered` 15 / `phase2_answer_missing` 0 (wave 74: 10 / 2), `forced_close_unrecorded` 9 (CJ predicted 0 — engine seat (c)), `repeat_annotated_takes` 2, `blocker_forecast_gang` 18 (wave 74: 6) / multi 76 / collapsed 28 / rows 132; askreplay/ sidecar 33 files; wins deck152 5/6, deck146 5/6, deck162 3/6, deck125 3/6, deck126 2/6, deck130 2/6, deck123 1/6 (wave 74: deck125 1/6 → 3/6, deck123 3/6 → 1/6 — hands are real; do not read variance as guide effect).

## Per-deck agent (one per deck: 146, 152, 125, 126, 162, 123, 130)
Purpose (owner's words): read the transcripts of your deck, compare the LLM's actions to the
instruction set (the guide projects/mtg/bin/Res/ai/baka/deck<N>_strategy.txt + the general guide
wave62/general-strategy.md (wave-59 edition + R331; NOTE from the wave-63..67 syntheses: this file is NOT loaded at runtime (only deck<N>_strategy.txt is opened, AIPlayerGPT.cpp:15332) — the deck guide is the live surface) + the reply protocol in the prompt), find issues in the game, the
interface, and the guide, and surface anything else worth the core loop's attention.
Method: for each of your deck's 6 games (your seat's translog; the opponent's for context),
trace the decisions that decided the game. Classify each misplay PERCEPTION (the model misread a
true surface, or the surface was false/missing -> ENGINE/RENDER item, cite file+seq) vs STRATEGY
(read correctly, decided against the guide or against good play -> guide item). Note fallbacks,
re-asks, and any decision where the prompt's information was insufficient or wasteful (what a
better interface would have shown). Hands are real (mulligans, bottoming): do not read variance
as guide effect.
Output: ONE file, wave75/deck<N>/review.md: (1) game-by-game outcome + the deciding decisions with
seq citations; (2) engine/interface/card items ranked HIGH/MED/LOW with a concrete repro (file,
seq, the rendered line) — this is the primary deliverable; (3) guide verdict: KEEP as is (a
success verdict, state why) or EDIT — if EDIT, also write wave75/deck<N>/strategy.txt (the full
revised guide, start from the live one) and list each edit before->after with the seq that
paid for it IN review.md — the guide itself carries NO citation, count, or history (Amendment 332); hard ceiling 20,000 bytes, and an EDIT that grows the guide must say which rule earned its bytes; (4) OPTIONAL proposals, only when
your evidence supports them: general-guide changes (wave75/deck<N>/general-proposals.md) and
strategy-writing-skill changes (wave75/deck<N>/skill-proposals.md; current skill edition =
wave68/strategy-writing-skill-v2.md (the ~40 KB distillation, owner-installed 2026-09-06 after six Astra adversarial rounds; read it in full — it is short; the 1.3 MB wave68/strategy-writing-skill.md is lineage only) — read it before proposing; a proposal that
restates an existing amendment is noise). No prediction tables, no rotation evidence, no
docket bookkeeping. Final message: <= 10 lines.

## Engine-narration seat (one agent)
You read the whole corpus for the engine's sake, not the guides'. Deliverable wave75/engine-seat.md: (1) corpus census as last wave (games, decisions, decisions/turn vs wave 74's ~2.1, fallbacks by kind with genuineness, `async_drops`, `transport`, `deadline_pct`, reply shape, `reasoning_chars`, `reply_truncated`, `askreplay/`, `plan_steps_done`); (2) REGRESSION WATCH on the wave-75 changes — the section that matters most (SINGLE binary this wave, no split needed): (a) the loop family stays dead — for every seat, asks per (turn, phase); any phase with > 10 asks: genuine chain (board moves each link) or re-put window (board unchanged)? `[hold check: ...]` brackets on consecutive same-seam windows (CI P1b/CM F2: the check keys on holdActionKeyRow WITH the `[cost:]` group — quote any `N rows above are new` that is false, and any window where a genuinely new legal row — new cost, new target, new X — was covered by a hold); the prompt-only `[you declined this exact list N times already this turn]` note maxima per seat (wave 74: 1 after CG); (b) EVERY `hold_released_turn` (115) — no hold outlives the holder's untap; (c) `forced_close_unrecorded` 9 (CJ predicted 0; CM F5 moved the armed flag per arm and predicted 0 wherever recovered+missing equals the stderr close count) — for each of the 9: name the game, the arm, and whether a decision was actually lost (a genuinely lost close on one named arm = HIGH), and reconcile `phase2_answer_recovered` 15 + `phase2_answer_missing` 0 against the stderr forced-close count; (d) P2 — 0 records with `http_status: 400` / the `continue_final_message` body (CJ) — count; any `http_error_body` at all — quote; (e) P21/F4 — every `reply_truncated` record carries `retry` + `reasoning_forced_close` (there are 0 reply_truncated this corpus — say whether the arm ever fired: count `reasoning_forced_close` records); (f) P9/F3 — replies with an inline `CHOICE:` on the PLAN line: how many, all consumed correctly, none negated; (g) P13 — `main_phase_windows_skipped` 94 (wave 74: 72): cross-reference every counted (turn, phase) against `Casting decision (Main phase N, YOUR turn)` headers — CI predicted 0 false counts; (h) P18 — every land-drop menu with an MDFC in hand lists FRONT and BACK rows; 0 `Choose an option for <card>:` prompts with an `OTHER FACE of` row; (i) P19 — hold row < 650 B at every window, `[HOW A HOLD ENDS:` exactly once per prompt that renders a hold row; hold takes per window vs wave 74's 11/322; (j) new/changed render clauses stating a FALSE fact: P5 `you would be at 0 - this row feeds their chain` (every instance: is the loop live and does THIS row feed it), P6 `CRACK-BACK NEXT TURN` on own-turn Upkeep/Draw windows (rate vs the actual board), P10 own-clock gate (`reaches 0 in N more turns` must never print beside a stated lethal), P11 `another copy of`, P15 ascending/descending X ladders, P16 word-boundary short names (any uncopyable / ambiguous name), P7/F8 `{crack-back cover: ...}` vs `[legendary: you already control ...]` agreement, P3 `[cannot pay now]` on deck126's Exquisite Blood with a Battlement untapped (CK predicted 0), P4 Sorin -6 (no self-target; no graveyard->battlefield narration pair), P22 (0 `Parser returned NULL {t}:add{` in any stderr); (k) `async_drops` 70 (wave 74: 84) — sample 10: re-asked? any decision to the heuristic? (l) `identical_ask_answers_reserved` 1,332 / `ask_replays_reserved` 1,801 (wave 74: 1 / 584) — name the replay path each counter counts and whether 1,332 is the CG-era replay restored or a new window being re-put; (m) `protocol_deviation_replies` 107 vs `plan_line_missing` 104 / `off_protocol_bytes>0` 71 (CL P23d: one event class — confirm 96%+ overlap; quote the residue), `plan_names_uncastable_zone_card` 68 (CL P17: an upper bound — sample 10, how many are real graveyard/exile plans), `reasoning_ngram_repeat` vs `reasoning_degenerate` (CJ P20: not rank-correlated); (n) the harness — PILOT STALL / WINDOW LOOP never fired (confirm from ~/.gatelogs/w75-corpus.log), the early check reported 42 seats; (3) adjudicate EVERY prediction in wave75/lane-{CI,CJ,CK,CL,CM}.md (CM = the nine Astra findings) as PASS / FAIL / UNTESTED with counts and file+seq (UNTESTED is a real verdict), plus the carried UNTESTED docket (BS F2 Tribute second edict, BS F3/land latch `declined_face_latches` 0 for the 5th wave, BS F5 blockers ceiling, BS F6 distinct-index re-ask, CF F1 pending-deadlock, F2/F5-nonlifelink/F8 haste/F9, the reasoning-body halves of N14a/N14d/O20; {X}-announcement payment loss — count `dropped after its X was announced`); (4) engine/render/narration defects ranked HIGH/MED/LOW with repro — especially any new render clause stating a false fact (the loop-scope sentence, `[cost:]` on reveal rows, the ghostform gloss, the cannot-block subject); (5) the owner's lategame specimen (standing rule, alert-only): ONE late-turn decision (turn >= 25; 125v123 ran 82 turns), full `prompt` verbatim to wave75/lategame-specimen.txt with a 3-line header. Final message: <= 15 lines, HIGH items first.

## Synthesis (runs only if any deck agent wrote general-proposals.md or skill-proposals.md)
Read all proposals + engine-seat.md. Decide each proposal: ADOPT (edit wave75/general-strategy.md,
a copy of wave62's (the R331 edition; waves 63-67 wrote no edition; wave 68 installed wave68/strategy-writing-skill-v2.md as the current edition), or append a numbered amendment to wave75/strategy-writing-skill.md, a
byte-verbatim copy of wave59's with amendments appended — verify the prefix with cmp) or
REJECT with one line of reasons. Write wave75/synthesis-notes.md. If a deck agent's guide edit
contradicts an adopted general change, note it; do not edit deck guides yourself.

METHOD NOTE (from the wave-59 engine seat): the translog `phase` field is not the rendered phase — count phase-gated facts from the prompt text.
