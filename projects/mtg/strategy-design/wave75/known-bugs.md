# Wave-75 known bugs (corpus matchups-20260910-003556-final: 21 games, SINGLE binary archives/wagic-7a04cd6da-w75step1 = 14fea99f3; sources wave75/engine-seat.md + wave75/deck<N>/review.md)

Invariant 000 held: 2,919/2,919 decisions with native reasoning, 0 fallbacks of ANY kind (first corpus ever), 0 truncations, 0 http≠200, 0 hung, 21 natural finishes, no tripwire. Wave-75 items PAID: P5 0 violations across 327 loop prompts — the deck125 seat that lost 42-7 last wave declined all 22 `feeds their chain` rows and won 38-0; P6 crack-back 103 Upkeep / 12 Draw (was 0/0); P3 0 false `cannot pay now` with a Battlement (15 Exquisite Blood casts taken); P4 6 `-6` rows, no self-target, the one consumed did all three things; P7/F8 0 of 41 cover rows overlap a legendary bracket; P18 28/28 face pairs on one window, 0 second windows; P19 hold row median 593 B (was 1,074-1,287), `[HOW A HOLD ENDS:` exactly once in 2,076/2,076; P2 0 HTTP 400, `phase2_answer_missing` 0 (was 2), identity recovered+missing+unrecorded == closes in 21/21 games; P8 REFUTED confirmed (23/23 post-counter turns re-asked); P10 0 of 174 beside a stack KILL; P11 0 of 88; P22 0 parser-NULL lines; P20 meters uncorrelated (Spearman 0.242); CI hold-latch fix cut per-game decline maxima 23 → 10; `identical_ask_answers_reserved` 1,332 is the CG-era replay restored (3,402 windows never reached the model). All 115 hold releases at minimum span; all 70 async drops re-asked. All seven wave-75 guide EDITs installed (21d2203cf). Wins 152 5/6, 146 5/6, 162 3/6, 125 3/6, 126 2/6, 130 2/6, 123 1/6.

## Q — routed to wave 76 (HIGH first)
- Q1 HIGH — THE HOLD-CHECK BRACKET MEASURES AGAINST A WINDOW THE MODEL NEVER SAW. `holdReopenNote` updates `mLastMenuRows[seam]` at every window whose prompt is BUILT, but 3,402 built windows were never sent (1,601 hold-skipped + 1,801 replayed), so `[hold check: every row above was also on the menu at the last window at this seam (N windows in a row now)]` names a menu the reader has no memory of — 214 of 2,000 record-adjacent windows (upper bound), e.g. `126v146` deck126 priority seq 28 (eleven Staff rows) → seq 33 (three Sorin rows, nothing shared) printing "every row above was also on the menu"; deck123 reaches `(17 windows ago)` across phase boundaries (deck123 MED-3). Residual false-NEW too: `162v152` deck162 Upkeep seqs 16/24 print `1 row above is new` when only `{leaves}`/`{spends}`/`{reserve}` braces moved (deck162 MED) — the action key must strip those brace groups as annotations. Fix: key the bracket's memory on windows that were ASKED (a record written) and word the referent as the last window the model was asked at this seam; strip every `{...}` annotation group from holdActionKeyRow while keeping `[cost:]` (CM F2); pin seq 28→33 = the bracket says nothing or "first window asked", pin 162 seq 16/24 = 0 new. Key-stability pin set for any changed key. (engine-seat HIGH-1, deck123 MED-3, deck162 MED)
- Q2 HIGH — AN 80,805-BYTE PROMPT: `125v123` deck125 seq 394 (discard, t77) carries five sweeper rows (`Supreme Verdict (copy 1|2|3 of 3 in your hand)`, `Final Judgment (copy 1|2 of 2)`) at ~9.5 KB EACH, every one re-enumerating all 100 opposing bodies inside `{right now: destroys 100 of their creatures ... - THEIRS: Lord of Lineage (5/5) [...], Vampire #1 ..., Vampire #2 ...}`; 74 prompts carry a `- THEIRS:` enumeration, 562 prompts exceed 20 KB (wave 74 max was 34 KB). Two defects: the enumeration has no cap, and O18's identical-row collapse does not reach `(copy N of M in your hand)` rows at the discard seam. Fix: cap the enumeration at a count plus the DISTINCT shapes (the way the board header summarises), and fold identical copy rows at the discard seam with the option set intact (the fold is display; every copy stays answerable). Pin: the seq-394 shape renders under 12 KB with all five rows answerable. (engine-seat HIGH-2)
- Q3 HIGH — THE LOOP-COMPLETE FALSE-VERDICT FAMILY OUTSIDE P5's `{right now:}` SCOPE. (a) The combat header prints `NOT lethal: block only where the trade favors you` on the same screen as a live `LOOP COMPLETE` paragraph whose INCOMING block refuses any survival claim — 8 renders corpus-wide, including the losing window `146v126` deck146 seq 70 (T22 Blockers; seqs 64/69 too). (b) The P10 own-clock gate FAILS on a stated crack-back lethal: `125v123` deck125 seqs 381-397 (t77) print `CRACK-BACK NEXT TURN: ... you would be at -84; that would KILL you` six lines above `{the clock you already control: ... reaches 0 in N more turns}` — 9 of 191 tags (deck125 B-2; P10 took stack+combat but not the crack-back line). Fix: one shared "stated lethal on this screen" predicate (stack KILL, combat lethal, crack-back KILL, LOOP COMPLETE incoming) that the combat header, the own-clock tag and any other survival/clock clause consult; pins per source. Under a proven loop with NO stated incoming loss the clock is TRUE (engine seat: 57 renders, all at 28-38 life) — do not suppress there. (deck146 HIGH-1 + MED-2, deck125 B-2)
- Q4 HIGH — ATTACK TOTAL's AGGREGATE COVER AND FLOOR ARE FALSE ON TWO BOARDS. (a) `Their N untapped blockers can cover every attacker you could send` ignores that a MENACE attacker costs them two bodies — `126v146` deck126 seq 45 (t20, LOOP COMPLETE, 4 attackers of which 2 menace vs 4 blockers = a guaranteed unblocked attacker = the game; the per-attacker tags DO model menace, only the aggregate does not). (b) `At least N damage lands whatever they block - they would be at X` is asserted as a floor while a declaration-time attack trigger (Lightmine Field) sits on their board — `125v123` deck123 seq 1208: `ATTACK: all` with 100 creatures, the whole board destroyed before damage, opponent life unmoved at 97. Fix: the aggregate cover counts menace attackers as two bodies (and evasion the per-attacker tags already know); the floor sentence is suppressed or requalified when an attack-trigger permanent of theirs is on the board (name it). Pins for both. (deck126 HIGH-1, deck123 MED-1)
- Q5 HIGH — CRACK-BACK ARITHMETIC IS ASYMMETRIC: a row that KILLS the crack-back attackers gets no crack-back clause while a row that ADDS blockers gets a full `{crack-back cover: ...}` paragraph — `130v152` deck130 seq 23 (rows 2/4 = Starstorm-class sweep vs row 3 = Siege-Gang); the seat chose the bodies and lost. Fix: under a stated CRACK-BACK line, every row whose `{right now:}` removes attackers prints the cover from the bodies it removes (`removes K of the N crack-back attackers -> you would be at M`), same clause family as the blocker cover; key-stability pin (life number inside a brace = outside every key). (deck130 HIGH-1)
- Q6 HIGH — THE FORCED-SACRIFICE ROW STATES HALF THE PRICE UNDER THEIR LIFE-TO-DAMAGE CONVERTER: `162v126` deck162 seq 10 (t11) prints `[you SACRIFICE this; they gain 6 life (its toughness)]` and the header's tie-break "the one that pays the least" — with Sanguine Bond on their battlefield the 6 life they gain is 6 off the seat's total, never printed. Fix: the sacrifice row's gain clause adds `- and with their <converter> that is N off YOUR life` when a converter of theirs is live (the O5 converter predicate already exists); pin. (deck162 HIGH)
- Q7 MED — P13 IS NOT CLOSED: `main_phase_windows_skipped` 94 is 34/94 = 36% false and the (turn,4)/(turn,10) pair signature is back on 46 of 47 counted turns (wave 74: 39%); `125v162` deck162 gameend reads 14 against 10 possible skips. CI's two pure predicates are pinned; the member plumbing that calls them is not. Fix at the plumbing with a PARSETEST/fixture that drives two main phases of one turn through the real member calls; repro `146v125` deck146 stderr `(turn 3, phase 4)` vs seq 5. The meter may not be cited until closed. (engine-seat MED-1, deck162 MED)
- Q8 MED — 9 FORCED CLOSES ARMED, SUPERSEDED, AND PAID A SECOND DECODE: `125v123` casting ×2, `125v130` casting ×2, `162v123` land ×1, `152v123` casting ×1, `162v130` casting ×3 — no decision lost (0 fallbacks, all re-asked) but ≈12 min of wall clock on discarded ~20 KB decodes. Fix: do not arm a close on a window whose seam already has one outstanding at a different arm unless the first is still live; pin the interleaving. ALSO: CM F5's second clause ("non-zero = a genuinely lost close") is WRONG — the identity holds with 9 unrecorded; rewrite that sentence in lane-CM.md as SUPERSEDED. (engine-seat MED-2, deck130 HIGH-2, deck162 MED)
- Q9 MED — `plan_names_uncastable_zone_card` NEEDS TWO EXEMPTIONS AND A PER-RECORD FIELD: self-recursion (Hammer of Bogardan returns itself — 12 of 47 re-derived triggers, plan correct; deck130 12/14 false) and cast-from-exile (`Cast Sphinx's Revelation [from exile]` — 4 of deck125's 5; Chromatic Lantern, deck126); the remaining hits are REAL (deck126 18/19: countered Staffs, exiled Bond planned for 15 windows). The counter is gameend-only so no seat can sample it — write it per record like `plan_line_missing`. Also `plan_names_stranded_card` UNDER-counts: 2 of 4 reserve-VERDICT overrides on deck123 (`162` seqs 30/39, `146` seq 35, `130` seq 140) — widen the match to the taken row's own `{reserve: ... STRANDS <card>}` verdict. Measure only. (engine-seat MED-3, deck125 B-4, deck130 MED-2, deck126 MED-4, deck146 LOW-4, deck123 MED-2)
- Q10 MED — `{removes: A, B, C}` ON A REMOVAL/SWEEPER ROW COUNTS A GHOSTFORM-COUNTERED CREATURE AS REMOVED while the same prompt's battlefield line says it returns — `125v146` deck125 seq 71 (t20): `{removes: ..., Nadaar, Selfless Paladin, ...}` vs `[counters: ghostform ... returns]`; 4 false clauses, and it misdirected the seat's only removal. Fix: the removal forecast reads liveness the way O1's gloss does (granted abilities), and says `returns` for a ghostform body; pin. (deck125 B-1)
- Q11 MED — LOYALTY ROWS: an `<upto:3>` row prints exactly ONE `targeting <name>` while resolving against three OTHER permanents — `126v146` deck126 seq 39/40 (Sorin -6 "targeting Silverquill Silencer", the Silencer survived, three others destroyed/reanimated); and Sorin's `-2: emblem` row prints `targeting Sorin, Lord of Innistrad [your battlefield]` — a target it does not have (13 windows). Fix: up-to-N rows print the target LIST the engine will resolve or `(choose up to 3 targets next)`; emblem/no-target abilities print no target; pins. (deck126 MED-2, LOW-7)
- Q12 MED — THE CASTING MENU'S CLOSING ROWS ARE NOT IN A STABLE ORDER while the boilerplate calls whichever is last "the row that declines": 127 menus `Hold priority` then `Cast nothing right now`, 32 the reverse (deck126). Fix: one stable order (decline last, hold before it); the option set is unchanged; PARSETEST pin on both seams. (deck126 MED-3)
- Q13 MED — A THREE-ROW MENU RE-PUT TEN TIMES ACROSS ONE OPPONENT TURN (Upkeep → Cleanup), rows byte-identical each time, each a full model call — `152v125` deck152 seqs 25-34 (t12), ~13 min; the hold-check said `11 windows in a row`, the note said `declined 9 times`, and `identical_ask_answers_reserved` fired 0 times inside it because the replay key includes the PHASE. Legal-option boundary: the window itself must stay (a spell's value differs by phase), so the FIX is a measure + a bracket, not a collapse: count cross-phase byte-identical re-puts answered by the same decline (`crossphase_identical_reputs`), and print on such a window `[this exact list was put to you N windows ago at <phase> and you declined; nothing on the board has changed]` — annotation only, outside every key (key-stability pin). Report the count. (deck152 HIGH-3)
- Q14 MED — THE 3-LIFE LAND ROW SAYS A PLAIN LAND CAN ATTACK: every `pay 3 life` row (8/8, all deck146: `123` seq 5, `152` seq 8, `162` seq 22, `130` seqs 15/20/26, `125` seqs 17/22) prints `[usable (tap for mana / attack) this turn]` for Emeria / Agadeem, which are lands. Fix: the usable tag names what the permanent can actually do (a creature-land gets `attack`, a land gets `tap for mana`); pin. (deck146 MED-3)
- Q15 LOW — cosmetics and narration, one lane: the own-clock tag's plural template for one source (`your Staff of Nin deal 1 damage a turn between them`, deck125 B-5, deck126 LOW-8, third wave); six hold rows at 723-727 B over CI's 650 (`123v125` seq 1276, `125v126` seqs 111/112, `126v125` seq 228, `130v146` seqs 49/50 — the overage is the `{card text:}` group; LOW-2); `prose_outside_two_lines` counts the forced-close rescue's own echoed prefill as a deviation (`126v146` deck126 seq 22, 823 B; exclude forced-close records — engine LOW-1); Lightmine Field narrates N×1-damage events instead of one N-damage event (`125v123` seq 1210; deck123 LOW-4 — engine narration, check the trigger's script); a reply with TWO `CHOICE:` lines was consumed first-wins silently (`125v126` seq 345; deck125 B-7 — owner-ruled protocol: report only, confirm the existing multi-answer pin covers it).
- Q16 REPORT-ONLY (owner-ruled protocol territory — no lane): the attackers seam is the one reply seam that got worse — `plan_line_missing` 16/102 = 15.7% (wave 74 10.8%), `off_protocol_bytes>0` 14.7%, both `plan_absent` ATTACK replies; `unlabelled_plan` is 89/107 = 83% of all deviations (a correct plan with the label dropped — fifth wave unchanged); the declined-count override "Cast nothing" share is 54% (six waves, one direction: 20/26/24/40/45/54) — the prompt-only note is not moving the seat. Surfaced for the owner alongside the lategame specimen; nothing in the protocol text or the parser changes.

Docket: BS F3 / the land latch — RETIRED (P18 removed the second face window; `declined_face_latches` 0 for five waves, the latch is no longer reachable). Still UNTESTED, carried: BS F2, BS F5, BS F6, CF F1 (proxy only), CF F2, CF F5, CF F8, CF F9, CM F1 animated-Sorin, F3/F4 inline CHOICE (0 occurred), F7 infect (0 occurred), P14 repeat-count (0 occurred), P16 range lines (0 occurred), the reasoning-body halves of N14a/N14d/O20. `sibling_window_asks_skipped` 0 / `chain_windows_collapsed` 0 for the fourth wave — structural (chain rows always affordable; cast menus always carry a Cast row) — stop listing them as anomalies.
