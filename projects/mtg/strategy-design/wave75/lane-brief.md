# Wave-75 step-one lane brief (base = master after wave74 known-bugs; evidence = wave74/known-bugs.md §P + wave74/engine-seat.md + wave74/deck<N>/review.md)

RULINGS THIS WAVE TOUCHES: 000 — reasoning only in the native channel; reply = PLAN then action, nothing else; no instruction licenses prose; no parser reads outside the two labelled parts (P9 reads a present `CHOICE:` label on the PLAN's physical line — inside, not tolerance); caps never bound reasoning; the golden protocol TEXT (reply-protocol.txt / kReplyProtocol) and the thinking regime are owner-ruled — render brackets and row text may change. 00 — full games; a game answered by heuristics is not a corpus game. 3 — suite THREADS=1 0 failed (known flakes: lifeline, merrow_reejerey, intrepid_adversary_repeated_payment), PARSETEST 0 failed. Never remove, cap, or auto-answer a legal option (P1 reverts a TAG, never the row; P12/P17/P20 are MEASURE ONLY). No VPK. No AI attribution. NO CORPUS from a lane; P2 alone may send single probe requests to the pilot.

Standing mechanics: as wave71/lane-brief.md (worktree-only; `make -f Makefile.sdl -j8`; both link guards OK; binary freshness; every wagic run under `systemd-run --user --scope -q -p MemoryMax=4G -p MemorySwapMax=0 --`; suite THREADS=1 as a detached unit `w75-<LANE>-suite` waited with a FOREGROUND until-loop (no Monitor); count both failure strings; PARSETEST from bin; Edit tool for UTF-8 sources, byte-splice for MTGRules.cpp/GameObserver.cpp/primitives; U+FFFD 0; RED-on-base evidence + a falsifiable prediction per fix; commit with explicit paths, `git add -f` under bin/Res; write wave75/lane-<LANE>.md; touch `~/.gatelogs/W75_<LANE>_DONE`; weakest evidence). LESSON OF WAVE 74 (two corpus-killing regressions from render annotations that carried board numbers): any NEW annotation that carries a number derived from the board (life, mana left, counts, turns) must be proven OUTSIDE every key — ask key, async slot key, hold latch key, hold-check key, option-set key — with a PARSETEST pin that two windows differing only in that number yield identical keys. Every lane that adds or changes a bracket ships that pin.

## Lane CI — the window/hold family (P1, P8, P13, P18, P19, P12, P24)
P1 first: revert the O11 in-row tag (keep the separate declined-count line outside every key); the hold-check bracket keys on the CH action key and says a row changing only in its annotations is the same row; pin `146v125` seqs 65/66. P8: the post-counter casting window — find the arm, fixture RED on base if reproducible. P13: the third casting arm that never calls `noteMainPhaseCastingOffered()`. P18: faces as rows on the first land-drop window, no second window. P19: hold-row sentences kept by evidence (count quotes in the corpus `reasoning` bodies, /home/magi/.Wagic/ai/gpt/selfplay-runs/matchups-20260909-104713-final). P12: the chain census at the casting seam, measure only. P24: corpus-early-check.py keyed on the run's own outdir/launch epoch (it lives in ~/.gatelogs; copy the fixed version to projects/mtg/tools/) and the harness K-pool per-seat bound with a selftest.

## Lane CJ — transport and reply plumbing (P2, P21, P9, P14, P20)
P2 first: log the 400 body; probe the live pilot (http://100.116.136.74:8084, model qwen36-35b-a3b, `--thinking on` shape; ONE request per shape, tiny prompt) to find a phase-2 continuation shape the template keeps; pin the request JSON; every forced close leaves a record (`forced_close_unrecorded` = 0 on the stub). P21: the 64-character truncation. P9: the one-line `CHOICE:` pin + MUST-NOT-MATCH. P14: the repeat row's answerable short name. P20: the near-duplicate degeneracy measure, instrument only.

## Lane CK — engine truth (P3, P4, P7, P22)
P3 first: the payability oracle under board-dependent producers (Overgrown Battlement per defender, Chromatic Lantern) — fixture RED on base, fix in the shared path the cast planner already gets right. P4: Sorin's `-6` cost parse and the once-per-turn loyalty latch after a fizzle/`upto` list — two fixtures RED on base; add the `{right now:}` clause to the `-6` row. P7: crack-back cover subtracts bodies the row's own text removes (legend rule). P22: the pathway `doubleside` and `{t}:add{g}` parse errors.

## Lane CL — render truth (P5, P6, P10, P11, P15, P16, P17, P23)
P5 first (lost a 42-7 game): under a live loop paragraph, rows that feed the chain print `you would be at 0 - this row feeds their chain`. P6: CRACK-BACK NEXT TURN in every own-turn window. P10: the own-clock gate on any stated lethal. P11: keep-X names another copy. P15: delete the largest-first boilerplate. P16: O18 at the Equip seam, option set intact. P17: `plan_names_stranded_card` widened, measure only. P23: the residue items as evidence permits. Every new or changed bracket ships the key-stability pin (see the lesson above).

## Lane CM — fix lane for the Astra review (wave75/codex-review.md) — seeded after step-one gate 1 — base = master 301a30a37 (gate 1: 1288/0, 76 AI/0, PARSETEST 5901/0)
Nine findings, each CONFIRM (fixture/pin RED on base reproducing Astra's trigger, then fix) or REFUTE (a pin proving the trigger cannot occur, with the reason). Every fix that touches a rendered row or a key ships the full key-stability pin set (ask / async-slot / hold-latch / hold-check / option-set), with the held set built the way the LIVE seam builds it (holdActionKeyRow).
- F1 HIGH P4 (planeswalkers.txt:~3059, lane CK): `other *[creature;planeswalker]` excludes Sorin from the CREATURE alternative too — Oracle's "other" binds only the planeswalker alternative; an animated Sorin (creature) is a legal -6 target. Express the restriction on the planeswalker alternative only (check what the target DSL supports; if it cannot express per-alternative `other`, say so and choose the reading that loses NO legal target while still forbidding the self-planeswalker case); pin animated-Sorin-as-creature legal + Sorin-as-planeswalker illegal. ALSO: lane-CK.md's "general re-entry latch hole" is misdiagnosed — a permanent that leaves and returns is a NEW object and may activate loyalty again (CR 400.7); rewrite that paragraph as SUPERSEDED with the correct rule, do not build any identity latch.
- F2 HIGH P1b (AIPlayerGPT.cpp:~28561, lane CI): holdActionKeyRow strips every `[...]` group including `[cost: …]`, so two same-name activations with different costs (`Draw with Tome [cost: {3}]` vs `[cost: {T}]`) key identically — a genuinely new legal activation reads as "seen" and the hold covers it. The COST is action identity, not a board forecast: keep the `[cost: …]` group (normalised) in the action key while still stripping the forecast/annotation groups; re-run the wave-74 146v125 seq 65/66/70 pins under the new key (they must still read 0 new rows — the action key must not regress into the CH re-open shape); pin the two-cost case = 1 unseen.
- F3 HIGH P9 (AIPlayerGPT.cpp:~20725, lane CJ): `choiceLineIsClean` accepts a payload with no numeric head (`PLAN: Do not take CHOICE: not 2 (Cast Rorix Bladewing)`), and the downstream parser can bind the name or rescue the integer. The inline label must be followed (after whitespace) by the coded head — a digit run — or it is not an answer; MUST-NOT-MATCH pin for the negated payload and for a bare-name payload.
- F4 MED P21 (AIPlayerGPT.cpp:~17107, lane CJ): `codedAnswerCount` uses the line-leading scanner only, so a cap-stop reply whose answer is the P9 inline form is force-closed although it is complete. One definition of "coded answer" for both (route codedAnswerCount through the P9-aware reader); pin: inline answer at the cap → count 1, no force-close.
- F5 MED P2 (AIPlayerGPT.cpp:~17117, lane CJ): `mForceCloseArmed` is seat-global while forced-close storage is per arm (land parked, casting arms) — `forced_close_unrecorded` increments for a close that is safely parked, and recording one close clears the flag for the other. Make the armed flag and the unrecorded accounting PER ARM; pin the interleaving (land arms, parked; casting arms; both complete → recovered 2, unrecorded 0, closes 2).
- F6 MED P2 (AIPlayerGPT.cpp:~15451, lane CJ): the trim models six ASCII whitespace chars; Jinja/Python `strip()` is Unicode-aware, so a trace ending in U+00A0 (or U+3000, U+2028…) leaves a prefill the template does not reproduce → the same 400. Trim the Python `str.strip()` whitespace set (UTF-8 encoded sequences), and make the PARSETEST template model use the SAME Unicode set independently (not by calling the helper); pin U+00A0 and U+3000 tails.
- F7 HIGH P5 (AIPlayerGPT.cpp:~28425, lane CL): the feeds-their-chain rewrite treats a rendered damage subtraction as life loss; infect damage to a player (Damage.cpp:~204) is poison, not life, so a Glistening Oil self-ping prints a false `you would be at 0`. Gate the rewrite (and the underlying `you would be at N` forecast — fix the source, not the symptom) on the damage actually being life loss: infect source → poison forecast, no chain-feed; pin infect self-ping under a proven loop = no rewrite + a truthful poison line; pin non-infect unchanged.
- F8 HIGH P7 (AIPlayerGPT.cpp:~41175, lane CK): the legend-rule duplicate walk excludes tokens — a legendary Rorix TOKEN plus a cast Rorix still counts two bodies (false SURVIVE at 3 life). The legend rule does not care about tokens: include tokens in the twin walk; a fixture whose existing legend is a token, RED on base through the battlefield walk (not by supplying `legendTwinControlled`).
- F9 HIGH P24 (selfplay-harness.sh:~257, lane CI): the bounded pool still reaches K from a subset of live seats (4 live: 3 with two timeouts + 1 succeeding → STALL 6; 21 live: 6 timing out + 15 succeeding → STALL 6) while the banner claims EVERY live seat. PILOT STALL must require that every live seat's NEWEST record is a failure (a seat whose newest record is a model-answered decision is evidence AGAINST a stall); keep the single-live-seat-20-timeouts case = STALL; add Astra's two healthy shapes to `--selftest` as OK.
Order: F2, F3, F7, F8 (legal-option / false-verdict), then F1, F9, F4, F5, F6. Gate: suite THREADS=1 + PARSETEST + harness --selftest + instruction guard; lane report wave75/lane-CM.md with per-item CONFIRM/REFUTE, RED evidence, fix, GREEN, prediction, weakest evidence.
