# Wave-52 seat: deck125 (5/6) + deck126 (1/6)

Corpus `matchups-20260901-223430` - NINTH fair-hand corpus, first on lanes G-L, the owner batches,
the option-preserving autotap and the Baka pump-targeting fix. Binary master dddb6f62b, model
qwen36-35b-a3b (FP8). Guides = wave-51 reviewer revisions (5d0c24d28) + boundary pass (6686392d3).
deck125 **857** decisions / 6 games, deck126 **405** / 6 games (1,262 excluding gamestart/gameend,
37% of the 3,384-decision corpus). Longest seat log on my seats is `deck126 vs125` (218 records for
a 39-turn game); largest prompt `deck125 vs152` seq 143 (28,823 chars, t54). Nothing on my seats is
the corpus's 353-record or 31,551-char maximum.

**Results.** deck125 **5W-1L** (won vs123 t32 46/0, vs130 t69 25/-5, vs126 t38 25/0, vs152 t53
28/0, vs146 t51 53/-10; lost vs162 t19 0/18). deck126 **1W-5L** (won vs152 t23 19/0; lost vs130
t13 -1/19, vs146 t16 -4/22, vs162 t14 0/11, vs123 t25 -108/5, vs125 t38 0/25). Wave 51 was 2-4 /
4-2. Real hands, n=6 per deck: **not attributed to guide edits** in either direction. What IS
attributable in mechanism (not in win rate) is lane I - see set B below - which handed deck125's
draw-go seat 40+ opponent-turn counterspells it had never been offered before.

**Health (my 12 seat logs).** Fallbacks **4 / 1,262 (0.32%)**, above the 0.10-0.31% band and in
line with the corpus's own jump (15 / 3,384). deck125 2/857: two `unparsed_reply` degenerate decodes
at ~3.2 s on ordinary 2-3 row menus (`vs146` seq 50, reply `跟 \.8`; `vs152` seq 136, reply a single
space) - no format cause visible, the same menus were answered normally around them. deck126 2/405:
`empty_reply` at `vs146` seq 1 (the MULLIGAN ask, **900.019 s** = the whole `WAGIC_GPT_TIMEOUT`
budget; the fallback kept the hand, so that game's first decision was never the model's) and
`multiblock_reask` at `vs152` seq 54 (`BLOCKS: B1:A1, B1:A2` - one blocker on two menace-free
attackers; seq 55 recovered with `multiblock_reask_recovered`). `answer_replaced` 3 (`vs146` seq 13,
`vs162` seq 27 and, as `attack_last_line_taken`, seq 29), all three recovering to the right play.
`commit_retracted` / `plan_choice_conflict` / `stale_livelock` / `index_name_conflict` /
`decision_reversed_in_prose` / `plan_missing` / `repeat_count_zero_pass` / `degenerate_decode` (as a
record key) **0**. `name_over_index` 1 (see lane J). Latency: deck125 p50 2.9-12.0 s per game / max
560 s; deck126 p50 8.1-34.5 s / max **900 s**, with 857 / 464 / 382 / 286 / 206 / 202 s siblings -
deck126's per-decision cost is 3-10x deck125's on every game. `- Paid` receipts read out of the
`events` field only: **183 / 0 no-source**; 1 read `from mana already floating`. JSONL `turn` ==
the last `=== Turn N` in the prompt: **1,248 / 1,248**.

**Card facts.** Every card fact below is read off `projects/mtg/bin/Res/sets/primitives/*.txt` with
`/usr/bin/grep`: Exquisite Blood `mtg.txt:37966` `auto=@lifelostfoeof(player):life:thatmuch
controller`; Sanguine Bond `:99004` `auto=@lifeof(player) from(*[-lifefaker]|*):life:-thatmuch
opponent`; Chromatic Lantern `:19630` (`lord(land|mybattlefield) transforms(...)` PLUS its own
`{T}:Add{G}/{R}/{U}/{B}/{W}`); Overgrown Battlement `:83596` `{T}:foreach(creature[defender]|
myBattlefield) add{G}`, `abilities=defender`; Wall of Omens `:131252` `auto=draw:1 controller`,
defender; Perimeter Captain `:85398` `@combat(blocking) source(creature[defender]|mybattlefield):may
life:2 controller`; Pride Guardian `:88356` life:3; Tribute to Hunger `:124073`
`target=opponent ... toughnesslifegain targetopponent`; Staff of Nin `:112425` `@each my
upkeep:draw:1` + `{T}:damage:1 target(anytarget)`; Idyllic Tutor `:57029`
`notaTarget(enchantment|myLibrary)`; Sorin `planeswalkers.txt:3055` `token(Vampire,...,1/1,black,
lifelink)`; Forced Fruition `:42754` `@movedTo(*[-land]|opponentstack):draw:7 opponent`; Underworld
Dreams `:125822` and Fate Unraveler `:39182` both `@drawfoeof(player):damage:1 opponent`; Vanishing
Verse `borderline.txt:123683` `target=*[-multicolor;-colorless]|battlefield auto=moveto(exile)`;
Nadaar `borderline.txt:75383`; Rorix Bladewing `:96836` 6/5 flying+haste. **Scryfall not consulted.**
One primitive contradicted a live guide sentence - see D65.

## NEW-BASELINE record (mulligans)

| seat | offers | mulls taken | kept-hand shape (engine's own counts) | bottoming |
|---|---|---|---|---|
| deck125 | 6 (7 asks) | **1** (`vs126` seq 1: 2 lands, coverage "would not cover any spell in it" - shipped, kept the 3-land six covering Revelation + Fracture, **won** t38) | 3L/4S (Scatter+Path+Cancel), 2L/5S (Scatter), 3L/4S after the mull, 5L/2S (Path+Lightmine), 2L/5S (Scatter+Elixir+Path), 4L/3S (Fracture) | 1 `bottom` ask (`vs126` seq 3, sent Sphinx's Revelation from a 3-land 4-spell seven) |
| deck126 | 6 | **0 decided** - and **1 never answered**: `vs146` seq 1 timed out at 900.0 s (`empty_reply`), the fallback kept a **4-land 3-spell** seven whose coverage line read "would not cover any spell in it" | 4L/3S (no coverage, fallback keep), 2L/5S (Battlement+Guardian), 3L/4S (Captain+Tribute), 3L/4S (Battlement+Lantern+Tribute+Tutor), 4L/3S (Tribute+Captain+Tutor), 5L/2S (Battlement+Tutor) | none |

No chain, no mull-to-zero, no floor text anywhere. #131 re-run on both sections at write time:
deck126's section had no sentence for a THREE-OR-MORE-land no-coverage seven (D72 adds it, odds-form,
explicitly recorded as a completeness fix and not a punished break, since the model never answered
that ask). No sentence pair disagrees on the eleven decided hands. **No OWNER QUESTION arises**;
the 900 s mulligan stall is routed as `general-suggestions.md` G8, not as a guide question.

---

# deck125 - Revelation Fracture (UW draw-go). 5W-1L

## Prediction set A - wave-51 guide edits (W58-W63)

**W58 (Revelation cast from a `max affordable X=1`/`X=2` row above 5 life: 0) - PASS 0/18.** 140
Revelation rows offered, 18 of them at max X of 1 or 2, **0 taken**. All 10 casts came off rows
reading X=3 or more (`vs146` seq 19 X=3, 31 X=5, 37 X=6, 60 X=5, 104 X=5; `vs152` seq 73 X=7, 81
X=11, 111 X=13; `vs130` seq 150 X=6; `vs123` seq 29 X=3). No reply beside such a cast said "spare
mana" or "draw a card or two". Ceiling; the guide's count was two corpora stale (W66 refreshes it).

**W59 (Staff / Elixir activation / Revelation from a `leaves N` below the cheapest counter's price:
0) - FAIL 4/11 on Staff, FAIL 2/11 on Elixir, PASS on Revelation; none punished.** Staff: `vs162`
seq 32 (t15, `leaves 1`, Essence Scatter in hand), `vs126` seq 24 (t19, `leaves 0`) and seq 28
(t21, `leaves 0`, the second Staff), `vs123` seq 32 (t13, `leaves 1`). Elixir activations: `vs162`
seq 7 (t5, 20 life, library 51, `{spends 2 of your 2 untapped mana sources this turn; Essence
Scatter {1}{u} in your hand needs 2}`) and `vs130` seq 36 (t13, 18 life, leaves 2 against a lone
Fall of the Gavel at 5); the other 9 were legal by life<=15 or by leaving 6+. The own-turn-tap-out
half - a tap-out followed by an opponent-turn cast of a counter-on-sight name with **no ask record
between** - is now **0/6 games**, because lane I opens a window on every opponent cast: the seat
answered 381 opponent-turn windows this corpus. The gate's breaks therefore stopped costing what
they cost in wave 51. Rule KEPT (W70 corrects the count, not the rule); re-test next corpus.

**W60 (TARGET asks with 2+ candidates: engine-text, else `[flying]`, else highest power) - PASS
5/5.** `vs146` seq 34 Nadaar (venture) over two 3/2 menace Spiders; seq 41 Silverquill Silencer
(3/2, "Whenever an opponent casts") over two 2/1 Spiders; `vs152` seq 18 Tovolar's Huntmaster (6/6)
over two 2/2 Wolves; seq 144 Elite Spellbinder (3/1 **[flying]**) over Briarbridge Tracker (2/3) -
neither has the engine text, so the `[flying]` tiebreak decided it, as written; `vs130` seq 45
Siege-Gang Commander (2/2, "{1}{R}, Sacrifice a Goblin") over two 1/1 Goblins. Zero candidates whose
only text is "When this creature enters" were taken over one of those. The wave-51 Sigarda miss did
not recur. 12 Path casts, every one at a real threat; **0** at a [defender] or a textless token.

**W61 (Revelation at life <= 5 with a header second number >= 1: largest X) - UNTESTED.** No
Revelation window arose at 5 life or less. The nearest is `vs152` seq 73-74 (t35, **6** life, max
affordable X=7): the rule did not apply, the pilot answered X=3, and the seat won that game.

**W62 (Emrakul) - PASS.** 3 rows, 3 taken, 0 declined: `vs146` seq 131 (t50, their 9) and seq 135
(t51, their 7) - the second-cast-after-the-extra-turn shape - and `vs130` seq 145 (t69, their 12).
No converter (`Exquisite Blood` / `Sanguine Bond`) was on either opponent's line and no opposing
Tribute/Devour Flesh was in either log; both games were won. The W29 refinement had no window.

**W63 (ceilings) - PASS on all four.** Land drop **98/98** ("you have no land you could play"
windows excluded). Sweeper at header second number 0: **220 rows, 220 declined** across the six
games. Sweeper casts: 4, all at second number 2 or 3 (`vs146` seq 21 at 3, seq 23 Final Judgment at
2; `vs152` seq 20 at 2; `vs130` seq 54 at 2). Lightmine Field: 102 rows, **1 cast**, at
`they control 3 creatures able to attack` (`vs146` seq 24) - the 3+ branch, exactly. `X = 3`
**11/11**, off menus topping out at 5, 6, 7, 11, 13 and 16. Cleanup: 37 `discard` asks; four sent a
counter (`vs146` seq 108 Gavel+Revelation, seq 115 Scatter, seq 137 Gavel; `vs123` seq 38 Gavel) -
of those, only `vs123` seq 38 still had a sweeper and a Lightmine in the list, so the ordered rule
broke **1/37** (was 1/15); the other three lists were counters and Paths only, where a Gavel or a
spare Scatter is the guide's own answer. #143 PUT-in-PLAN (a card the reply's PLAN means to PLAY):
**0/37**.

## The loss (deck125)

- **vs deck162 (t19, 0/18) - Forced Fruition plus Underworld Dreams, and the guide's own price rule
  broken in the window that ended it.** The seat led on every axis to t17 (23/20 with a Staff
  ticking). Then: `Forced Fruition` resolved, and from t18 the life total fell one point per drawn
  card - 20 -> 6 inside turn 18 (seq 60-82, twenty-three windows, each opened by a single ping).
  Seq 91 (t19, **3 life**) is the model at its best: a seven-row menu on which every row carried
  `[DRAW PRICE: casting this draws YOU 7 cards (their Forced Fruition), and their Underworld Dreams
  deals you 7]`, and the reply answered `CHOICE: 7 (Cast nothing right now)` - the Fruition rule,
  followed exactly. Seq 93 (cleanup, hand **21**, send 14) kept three counters and a Path. Then seq
  97 (t20, still 3 life): the same clause, and the reply cast `Essence Scatter` at a Fate Unraveler.
  The seq-98 events read seven draws and `Underworld Dreams dealt 1 damage to you (now 2)`; seq 99
  is at 1; the game ends at 0. The guide's rule ("cast a counter ONLY when that price is below your
  life minus 1") was correct and was **stated as arithmetic the pilot had to perform**, while the
  row printed the finished number. STRATEGY, and the edit (W64) re-keys the rule to the clause.
  Also in that game: seq 7 the Elixir gate break (W59) and seq 32 the Staff gate break (W59) -
  neither cost a window, because lane I kept offering them.

## Other counted behaviour (deck125)

- **Opponent-turn casting (lane I): 40 casts.** Essence Scatter, Cancel, Dream Fracture and Fall of
  the Gavel resolved on the opponent's turn at Bloodline Keeper, Intruder Alarm, Vision Skeins,
  Emeria's Call, Vanishing Verse, Silverquill Command, Kaya the Inexorable, Barrowin, Teferi,
  Sigarda, Katilda, Elite Spellbinder, Tovolar's Huntmaster, Luminarch Aspirant, Brutal Cathar,
  Ranger Class, Rorix Bladewing, Stone Rain, Lay Waste, Dwarven Blastminer, Thraben Doomsayer and
  two Sanguine Bond / Exquisite Blood halves. This is the deck's whole thesis and it had never been
  available; wave 51's 41-of-51 no-Cast-row windows are gone.
- Walls on the stack: **0 counters spent**, as in every corpus since the rule landed.
- Staff pings: every offered `Deal 1 damage ... targeting the opponent` taken; `{this hits YOUR
  permanent}` rows: 79 offered across both my seats, **0 taken**.
- `[second copy:` rows: 12 offered, 6 taken - 5 Staffs and (at `vs162` seq 91) 1 declined under the
  Fruition stop. All 5 takes were real second clocks; the deck has no dead second copy.
- Collapsed target-handle ranges `#a-#b`: 55 rows on `vs126`, all read correctly.

---

# deck126 - Sanguine Blood (WGB wall prison). 1W-5L

## Prediction set A - wave-51 guide edits (D58-D64)

**D58 (Tribute at a `[defender]` victim above 10 with an entry 1-6 row beside it: 0; "remove their
blocker" replies: 0; replies calling a PRINTED row unpayable: 0) - PASS 0 + PASS 0 + PASS 0.** No
Tribute was cast at a named [defender] victim this corpus. 206 `at 0 this does nothing` rows, **0
cast** (the zero-stop's third straight ceiling). Named-victim rows: 3, all non-defender, all taken
in their first window - `vs146` seq 5 (t6, Silverquill Silencer, +2), `vs123` seq 47 and seq 48
(t16, two separate windows, `Bloodline Keeper is sacrificed, you gain 3` each, 20 -> 26 with
Sanguine Bond turning both gains into 6 off the opponent). F22's plan-text check held.

**D59 (Lantern over a wall with no combo piece / at 5+ sources: 0; Lantern #2 with the owned tag:
0/N) - PASS on the first half, **FAIL 5/5** on the second, and the failure is the literal drift.**
Wave 51 declined 9 of 9 Lantern-#2 rows tagged `[already owned: Chromatic Lantern on your
battlefield]`. That string does not exist in this corpus (0 occurrences in 42 logs). Lane K prints
`[second copy: you already control Chromatic Lantern; both stay on the battlefield - no legend
rule]`, and the seat took **five of five**: `vs162` seq 10 (t7, taps out), `vs123` seq 41 (t15),
`vs125` seq 39 and seq 41 (t16, twice in one turn), `vs152` seq 40 (t16, taps out). The rule did not
weaken - its recognition string was retired underneath it. Guide edits D65/D73 re-key it; the
render half is `general-suggestions.md` G7.

**D60 (Tutor cast with the missing half on the hand line and its Cast row on the same menu: 0;
`reveal` picks naming a half already held: 0) - PASS 0 + PASS 0/6.** 8 `[finds only ...]` rows
offered, 7 taken. Six `reveal` picks, every one correct against the printed clause: `vs130` seq 9,
`vs123` seq 9, `vs125` seq 7 and seq 84, `vs152` seq 8 all picked **Sanguine Bond** with neither
half held (Rule #3.4, as written); `vs146` seq 8 picked Exquisite Blood off a row reading `every
enchantment left in your library is a copy of one you already control or hold: Exquisite Blood,
Sanguine Bond`, and seq 14 picked Sanguine Bond off `still in your library ... : Sanguine Bond`.
Lane K's tutor form fixed wave-51's engine #3 and the wave-51 F23 misread did not recur.

**D61 (Tribute in the FIRST named-victim window: N/N; both-out windows with a Tribute in hand at 1+
creatures: N/N) - PASS 3/3 + UNTESTED.** The three named-victim rows above. No both-out window ever
offered a Tribute at 1+ creatures: in the one game where both halves were out for 17 turns
(`vs125`), the opponent's board held **zero** creatures throughout - which is the D67/D68 finding.

**D62 (replies naming a hand card with no `Cast` row: 0; Baka takes: 0) - PASS 0 + PASS 0.** No
`named_row_reask`, no `stale_echo_in_range`, no `off_menu_name_in_range` on either deck. F19's
sentence is at its ceiling for a second corpus.

**D63 (mulligan) - see the NEW-BASELINE table.** No two-land no-coverage seven arose; a FOUR-land
no-coverage seven did and the model never answered it (900 s timeout). D53's cleanup check is
**UNTESTED a third time** - 0 `discard` asks on deck126 in 405 decisions (hands never reached
eight). Per #132 that moves the check to the row: D-guide's cleanup section is left as written but
is now the least-tested section in the file.

**D64 (`{paying this taps:` count == receipt count: N/N; Battlements tapped for a payment one
covers: 0) - PASS 6/7 and PASS 0/91.** Lane H fixed wave-51's HIGH #1: **zero** rows anywhere in my
12 logs name two Battlements in one `{paying this taps:}` clause (was 2/2 in wave 51), and no
receipt names two Battlements for a cost one covers - except once. `vs125` seq 86 (t22) forecast
`{paying this taps: Overgrown Battlement}` (ONE) and the seq-87 receipt reads `- Paid {3}{b}{b} for
Sanguine Bond with Overgrown Battlement #1; Overgrown Battlement #2; Chromatic Lantern #1; Sunpetal
Grove #1` (TWO). Engine item #2 below.

## The losses (deck126)

- **vs deck125 (t38, 0/25) - the pair assembled on turn 22 and the seat passed to death on a false
  trigger direction. This is the seat's decisive finding.** Exquisite Blood resolved seq 67 (t20),
  Sanguine Bond seq 86 (t22); the battlefield line printed both names, with their own `{effect: ...}`
  text, in all 132 prompts after that. The opponent (deck125) has **no creature in the deck**, so
  every Tribute row read `they control 0 creatures - at 0 this does nothing` (178 of this seat's 218
  decisions were that one two-row window) and no combat ever occurred. The seat's own Staff of Nin
  was cast at seq 167 (t32) and **countered in the same events block** (`Opponent cast Fall of the
  Gavel`); it controlled no Sorin and no Vampire. So none of Rule #2's five starters existed. The
  PLAN carried from seq ~140 onward read: *"Both Exquisite Blood and Sanguine Bond are on the
  battlefield. The win condition is active. The opponent has no creatures, so Tribute to Hunger does
  nothing. I will pass. The opponent's Staff of Nin pings in their upcoming upkeep will deal 1
  damage to me (triggering Exquisite Blood), causing me to gain 1 life, which triggers Sanguine Bond
  to deal 1 damage to them. This loop will drain them to 0. I do not need to cast anything
  further."* Exquisite Blood is `@lifelostfoeof(player)` - it fires when the **opponent** loses life;
  being pinged fires nothing. On that sentence the seat answered `Cast nothing right now` at seq
  152, 185, 200, 215 and 216 (9, 5, 3, 1, 1 life) with `Cast Wall of Omens` - the deck's only
  card-draw - printed on the menu at three of them. STRATEGY, and the guide had no sentence naming
  the wrong direction and no branch for "assembled but unable to trigger". Edits D67/D68/D74.
- **vs deck146 (t16, -4/22) - Vanishing Verse twice, a redundant copy, and a PLAN that carried a
  false board claim across three windows.** The mulligan timed out (fallback keep, 4 lands / no
  coverage). Sanguine Bond cast t10 -> **exiled by Vanishing Verse t11**. Seq 11's reply wrote a
  1,500-char self-arguing PLAN ending "Both combo pieces (Exquisite Blood and Sanguine Bond) are now
  on the battlefield" - false - and that text was re-served verbatim as `YOUR PLAN (as you last
  stated it)` at seq 12 and seq 13. At seq 12 (t14, **11 life**) the pilot cast `Exquisite Blood
  [second copy: ...] {taps you out}` over `Cast Tribute to Hunger {right now: they control 2
  creatures} {leaves 2}`; Vanishing Verse exiled the first Blood at t15 and Nadaar (4/4 vigilance)
  plus a Goblin finished it. Seq 13 (t16, 4 life, a 206 s `answer_replaced`) recovered to the Tutor,
  too late. In 17 turns the seat cast **no wall at all** - none was ever on a menu. Edits D66, D67
  (the plan half); the echo channel is `general-suggestions.md` G9.
- **vs deck123 (t25, -108/5) - the unbounded-token class again, with the opponent at 5.** Every
  guide rule was followed: Tribute took both Bloodline Keepers in their first windows (seq 47, 48),
  Sanguine Bond t9, Staff t21, Sorin t23, three Staff pings, Tribute at 2 creatures seq 62. The
  opponent went from 20 to 5. Then `Intruder Alarm` + `Bloodline Keeper` produced **34 flying
  Vampires**, seq 63's attackers window read `[held back, it CANNOT block ANY of their 34 creatures]`
  and `ATTACK: none` was correct, and the seat died at -108. Engine INFO #4; no guide rule reaches
  a board that grows 30 bodies in one main phase. Also here: 28 consecutive `at 0 this does nothing`
  windows across t12-t16 at 20/20 (seq 15-46), all passed.
- **vs deck130 (t13, -1/19) - Rorix Bladewing (6/5 flying, haste) and no Tribute.** Tutor at t7 took
  Sanguine Bond with neither half held (as written); Bond resolved t9. Rorix cannot be blocked by
  anything in the deck (Rule #4's flying clause names Tribute as the only answer) and all four
  Tributes were still in the library. 20 -> 14 -> 8 -> 2 -> dead. **No lever.** One note: seq 13
  (t9, 14 life) cast a second Overgrown Battlement off a row reading `{paying this taps: Overgrown
  Battlement - it cannot attack this turn}`, spending the wall that would have blocked Dwarven
  Blastminer - engine item #1's wording.
- **vs deck162 (t14, 0/11) - the draw lock, no combo piece drawn.** Underworld Dreams + Fate
  Unraveler; life fell on draw steps, 20 -> 16 -> 10 -> 4 -> 1 -> 0. Neither half was ever drawn or
  tutored (no Tutor in hand). Sorin, two Staffs and three Vampires were the whole plan and it was
  three turns short. Seq 21's block (D70 below) is the only decision worth an edit and it was
  substantively right. **No lever.**

## Other counted behaviour (deck126)

- Rule #7 entry 1: every Blood/Bond cast the turn its row first appeared - `vs146` seq 10/11,
  `vs130` seq 12, `vs123` seq 11, `vs125` seq 67/86, `vs152` seq 18: **7/7**. The two second copies
  (`vs146` seq 12, `vs125` seq 135) are the failure D66 addresses.
- Land drop: **47 windows, 46 lands played**. The one miss is `vs152` seq 45 (lane J - see set B).
- Blockers: only 6 windows all corpus. `vs152` seq 9 (Captain + Guardian on Briarbridge Tracker,
  +5 life, both "neither dies"), seq 15 (a lifelink Vampire on a "your blocker dies" line at
  `you would be at 25` - the lifelink carve-out, as written), seq 55 (Perimeter Captain on a 6/6
  first-striker at `you would be at 20` with **Sanguine Bond out** - the enchantment carve-out,
  correct, and the +2 took them 8 -> 6), seq 62 (`you would be at 8`, both blockers assigned).
  `vs162` seq 21 is D70.
- Attackers: 8 windows. `vs162` seq 29 sent three Vampires after a 382 s `attack_last_line_taken`
  self-revision; `vs123` seq 63 `ATTACK: none` against 34 blockers - as written.
- Sorin: cast 3, `+1` taken in every offered window (10/10). Staff pings: every window taken,
  always at the opponent.
- `mana_only_windows_skipped` per game: 0 / 126 / 111 / 459 / 0 / 37.

---

# Prediction set B - engine lanes (where my seats had windows)

| lane | verdict | evidence |
|---|---|---|
| **G** 8c3e10e28 (livelock, label echo, PUT own-label) | **PASS 0 + UNTESTED + UNTESTED** | `stale_livelock` 0 records on 12 logs; no repeat-N plan and no edict-row `Cast nothing` echo on my decks; no PUT seam consuming a `CHOICE:` line (deck125's 37 `discard` asks all parsed clean). |
| **H** d58aeba6b (foreach producers tapped ONCE) | **PASS 0/91 rows + 0 clauses** | **Zero** `{paying this taps:}` clauses anywhere in my 12 logs name two Battlements (wave 51: 2/2, the HIGH #1 that cost 18 life in one combat). 91 defender-tap rows, every one naming a single wall. One residual receipt mismatch: engine item #2. |
| **I** 4bdca64bf (opponent-turn instant priority) | **PASS, and it is the corpus's biggest change on my seats** | 381 opponent-turn windows across my 12 logs (deck125 198, deck126 183); 358 passed, **23 acted**. deck125 cast 40 counters/removal on opponent turns; wave 51's "opponent-turn windows with an instant in hand and no Cast row 41/51" is now **0**. Removal resolving on the opponent's turn: `vs146` seq 109 (Path at Hive of the Eye Tyrant), `vs152` seq 4/13/17/25/56/84/143, `vs130` seq 44, `vs123` seq 25. |
| **J** aa8509282 (name over index; prose verdicts; plan_missing) | **PASS on the mechanism, 1 costly outcome; `index_name_conflict` / `decision_reversed_in_prose` / `plan_missing` 0** | `name_over_index` recorded **1/1** and the NAMED row executed: `deck126 vs152` seq 45, `parse_note: echo_index_conflict;name_over_index`, reply `CHOICE: 3 (Play no land right now)` on a menu whose row 3 was `Play Sunpetal Grove` and row 4 `Play no land right now`. The lane behaved exactly as specified; the index matched the guide's one no-exception rule and the name did not, so the seat's only missed land drop in 405 decisions came from lane J's tiebreak. Not a bug report - a recorded cost, with the guide half at D71. |
| **K** b1fba8eff (life cost, self-hit, owned forms, from-exile) | **life-cost UNTESTED; self-hit PASS 0/79; owned forms MIXED; from-exile PASS 3/3 rendered** | `{paying this costs you N life}` / `- this KILLS you`: **0 rows** on either deck (no painland, no Talisman) - UNTESTED-AT-THIS-SEAT, while the analogous lethal `[DRAW PRICE:]` total DID arise and killed deck125 (engine item #3). `{this hits YOUR permanent}`: 79 rows offered (deck126's Staff windows), **0 taken**. `[already owned:` **0 occurrences corpus-wide** - retired as specified; `[legendary:` 0 windows; `[second copy:` 34 rows / 14 taken (deck125 5 Staffs = right; deck126 5 Lanterns + 2 Bloods = wrong, D59/D65). Tutor `[finds only ...]`: 8 rows, 7 taken, **wave-51 engine #3 fixed** - the clause now names the half you LACK (`still in your library, not on your battlefield or in your hand: Sanguine Bond`) and the both-out form (`every enchantment left in your library is a copy of one you already control or hold`). `[from exile]`: 3 rows on `deck125 vs152`, all carrying `castable from exile` prose - wave-51 engine #2's bare tag is gone. |
| **L** 0f7dff3b0 (collapse, repeat row, animated land, pricing) | **PASS on every branch with a window** | Collapsed `#a-#b` handles: 55 rows (`deck125 vs126`), all read correctly; longest non-log prompt segment well under 30 KB and no 20+-row Equip block anywhere (max prompt on my seats 28,823, of which the log is the bulk). `becomes` rows: 149 in prompts, **0** re-taken in the same window (wave 51: 3/9). Animated land tapped for mana the turn it was animated: **0**. `{spends K of your M untapped mana sources this turn; <card> in your hand needs K}` on CAST rows - the wave-51 G1 ask - **shipped and rendering**: 434 occurrences across my 12 logs, on cast and ability rows alike; edit W68 now points the Elixir rule at it. |
| **owner batches** | **UNTESTED / PASS** | No adventure, omen, morph, split or aftermath card on my decks (UNTESTED). Equip menus: none (UNTESTED). Evolve/counters-matter: none. Option-preserving autotap (HUMAN-only): forecast-vs-receipt mismatch **1/7** on my seats - engine item #2 - all seven at deck126, so the AI-seat `preserveOptions=false` guarantee did not hold in that one case. Baka pump-targeting: no Baka fallback take on my seats (deck126 Baka takes **0**), UNTESTED. |

---

# Engine / render items (ranked, with repro)

Corpus root: `~/.Wagic/ai/gpt/selfplay-runs/matchups-20260901-223430/`

**HIGH - #1: `{paying this taps: <name> - it cannot attack this turn}` states a consequence of zero
for a `[defender]`, and hides the only consequence that exists for a wall deck.**
`AIPlayerGPT.cpp` `paymentTapsClause()` appends `" - " << (one ? "it" : "they") << " cannot attack
this turn"` whenever the caller sets the flag, with no test on the source's abilities. All sixteen
creatures in deck126 carry `defender` (`Overgrown Battlement` `abilities=defender`,
`mtg.txt:83596`), so none of them could ever attack; what the pilot needs to know is that the wall
is tapped through the opponent's combat and cannot BLOCK. **91 such rows** on my six deck126 logs;
7 taken. Repro: `1788320110-ai_baka_deck126-0x557974bba060-vs-ai_baka_deck130.jsonl` **seq 13**
(t9, 14 life, `Cast Overgrown Battlement {1}{g} ... {paying this taps: Overgrown Battlement - it
cannot attack this turn}`, taken; life 14 -> 8 on the next combat) and
`1788320097-ai_baka_deck126-0x55b12e55f100-vs-ai_baka_deck125.jsonl` **seq 86** (t22).
Proposal: when the tapped source has `defender`, print `- it cannot block on their turn`; when it
has neither restriction keep the current words; when a row taps several sources with different
restrictions, name each with its own consequence. Suite pin: a cast row that taps one Overgrown
Battlement prints "cannot block on their turn" and never "cannot attack this turn".

**HIGH - #3: a `[DRAW PRICE:]` total at or above the pilot's life carries no lethal clause, while
lane K prints one for the same arithmetic on a mana cost.**
`1788320083-ai_baka_deck125-0x557cd81d6290-vs-ai_baka_deck162.jsonl` **seq 97** (t20, **3 life**):
every row on the menu carries `[DRAW PRICE: casting this draws YOU 7 cards (their Forced Fruition),
and their Underworld Dreams deals you 7]`; the reply took row 1 (`Cast Essence Scatter`), and the
seq-98 events show seven draws and the seat at 2, seq 99 at 1, `gameend` at 0. Lane K's
`{paying this costs you N life (...) - you would be at K}` / `- this KILLS you` is the exact clause
this row wants and it had **zero** windows on either of my decks. Proposal:
`[DRAW PRICE: ... deals you 7 - you would be at -4; this KILLS you]`. Seq 91 (the same menu at the
same life on the previous turn) is the positive control: the pilot declined all seven rows there,
so the clause is not fighting a rule the pilot ignores - it is supplying the number that decided
which of two identical menus was answered correctly.

**MED - #2: one `{paying this taps:}` forecast still disagrees with its receipt (lane H's
residual).** `1788320097-ai_baka_deck126-0x55b12e55f100-vs-ai_baka_deck125.jsonl` **seq 86** row:
`Cast Sanguine Bond {3}{b}{b} ... {paying this taps: Overgrown Battlement - it cannot attack this
turn}` (ONE named); **seq 87** events: `- Paid {3}{b}{b} for Sanguine Bond with Overgrown Battlement
#1; Overgrown Battlement #2; Chromatic Lantern #1; Sunpetal Grove #1` (TWO Battlements tapped). At
that moment the seat controlled four defenders, so Battlement #1 alone made {G}{G}{G}{G} and covered
the whole `{3}`; the second wall was tapped for nothing and was unavailable to block. 6 of 7 taken
rows matched their receipts exactly, so this is the tail of wave-51's HIGH #1, not its return.
Suite pin: with two Battlements and four defenders, `Sanguine Bond {3}{b}{b}` taps ONE Battlement
and the clause names exactly the sources the payment executes.

**MED - #5: `[second copy: ... both stay on the battlefield - no legend rule]` answers a rules
question and reads as an endorsement.** `1788320089-ai_baka_deck126-0x555c67c26ee0-vs-ai_baka_
deck162.jsonl` **seq 10** (t7, `Cast Chromatic Lantern {3} [second copy: ...] {taps you out}`,
taken) and four siblings (`vs123` seq 41, `vs125` seq 39/41, `vs152` seq 40). The tag is accurate;
the pilot's question is whether the copy DOES anything, and the engine already knows the answer for
a non-stacking lord. Proposal in `deck126/general-suggestions.md` G7. Not a bug - a wording item
whose measured effect was 9/9 declines -> 5/5 takes across the literal change.

**MED - #6: the opponent-turn window whose only cast row prints its own no-effect clause.**
206 of deck126's 405 decisions (51%) - and 178 of the 218 in
`1788320097-ai_baka_deck126-0x55b12e55f100-vs-ai_baka_deck125.jsonl` alone - are the two-row window
`1. Cast Tribute to Hunger {2}{b} {right now: they control 0 creatures - at 0 this does nothing}` /
`2. Cast nothing right now`, at prompt sizes up to 18,086 chars. Zero were taken, in this corpus or
either of the two before. Routed as a core-loop efficiency item (`general-suggestions.md` G4), not
as a legality change: lane I's windows are the reason deck125 improved and nothing here should
narrow them. The narrow ask is that a window all of whose cast rows carry an engine-authored
no-effect clause be skipped the way `mana_only_windows_skipped` already skips all-mana windows.

**LOW - #7: a 900 s `empty_reply` on a 2-row, ~1.5 KB mulligan ask.**
`1788320086-ai_baka_deck126-0x559639b50390-vs-ai_baka_deck146.jsonl` **seq 1**,
`latency_ms 900019`. The harness behaved as configured (`WAGIC_GPT_TIMEOUT=900`); recorded because
the cost fell on the one decision that shapes a whole game, and because a per-ask deadline scaled to
prompt size would have spent seconds instead. `general-suggestions.md` G8.

**LOW - #8: a Sorin Vampire token rendered `[lifelink, doesn't untap during its controller's untap
step]`.** `1788320091-ai_baka_deck126-0x55b679fe0ea0-vs-ai_baka_deck123.jsonl` **seq 63** A-line.
`planeswalkers.txt:3057` creates `token(Vampire,Creature Vampire,1/1,black,lifelink)` with no such
clause, so the restriction is coming from somewhere on deck123's board (Intruder Alarm was out) or
from a tag leak. Cosmetic this corpus - the answer (`ATTACK: none` into 34 blockers) was right
either way - but the token's printed abilities should match what put them there. Not investigated
further; flagged for whoever owns deck123's board.

**INFO - #4: the unbounded-legal-loop class, fourth citation.** `deck126 vs123`: 34 flying Vampires
on the opponent's battlefield by t25 under `Intruder Alarm` + `Bloodline Keeper`, from an opponent
at 5 life; the seat died -108 from 29. Same class as wave 51's `(x25)` citation. The engine seat
owns it.

**INFO - #9: two degenerate decodes.** `deck125 vs146` seq 50 (reply `跟 \.8`) and `deck125 vs152`
seq 136 (reply a single space), both ~3.2 s on 2-3 row menus at t32 and t52. `unparsed_reply`
fallback each time. No prompt-format cause visible; recorded so the corpus's fallback jump is
attributed correctly.

---

# Rotation evidence

- **deck125 - KEEP IN ROTATION.** 5-1, and the improvement has a named mechanism rather than a
  win-rate story: lane I gave the draw-go seat 198 opponent-turn windows and 40 counters it had
  never been offered, which is what a "keep the counter payable" guide is written to exploit.
  Ceilings this corpus: sweeper at header 0 **220/220**, land drop **98/98**, `X = 3` **11/11**,
  Revelation from a low-X row **0/18**, walls-on-the-stack **0**, Path at a defender or a textless
  token **0/12**, `{this hits YOUR permanent}` **0**. The single loss has a single named cause (the
  Fruition price rule stated as arithmetic instead of as the row's own number, W64), and the two
  gates that still break (Staff 4/11, Elixir 2/11) went unpunished for the first corpus because
  lane I keeps offering the window. Re-test W64 and both gates before any freeze talk.
- **deck126 - KEEP IN ROTATION, and it is now the seat that needs the work.** 1-5, but three of the
  five losses had no lever at all (a 6/5 flier with every Tribute in the library; a draw lock with
  neither combo half drawn; a 34-body token loop from an opponent at 5). The two that DID have
  levers are the same failure twice - a false reading of Exquisite Blood's trigger direction, and a
  PLAN line carrying a false board claim into the next window - and both are now guide sentences
  (D66/D67/D68/D74) plus a core-prompt ask (G9). The literal drift (D59/D65) turned a 9/9 rule into
  0/5 and is repaired. What the deck cannot answer at all remains: an exiled combo half against a
  deck with two Vanishing Verses, and an opponent with no creatures. Re-test D65-D68 and D71 before
  freeze talk.

# Lategame specimen candidate

Primary: `1788320100-ai_baka_deck125-0x55b04245ac70-vs-ai_baka_deck152.jsonl` **seq 143** (t54,
**28,823 chars**, kind `ask`: a Path-to-Exile cast ask at 28/2 on turn 54 of a 53-turn game - a
20-land seat line, a wide Innistrad board with Teferi, and the log of a game carrying 13 counters,
three Revelations and two Staffs). Alternates:
`1788320094-ai_baka_deck125-0x55c97d343470-vs-ai_baka_deck146.jsonl` **seq 133** (t51, 28,023 chars,
a `priority` window in the two-Emrakul endgame) and
`1788320112-ai_baka_deck125-0x55ded94e0310-vs-ai_baka_deck130.jsonl` **seq 154** (t70, 23,558 chars
- the corpus's longest game, 69 turns, ten land-destruction spells in the log).

# Falsifiable predictions for wave 53

deck125:
- **W71** - a cast taken from a row whose `[DRAW PRICE:]` (or any printed price clause) names a
  number at or above the seat's current life: **0** (was 1, and it was the loss). Replies declining
  such a row while a counter-on-sight name is on the stack: **N/N** (was 1/2).
- **W72** - Staff / Elixir activation taken from a row whose `leaves N` (or `{spends K ...; <card>
  in your hand needs K}`) is below the cheapest counter in hand: **0** (was 6/22). Own-turn tap-outs
  followed by an opponent-turn resolution with no ask record between: **0** (was 0 this corpus, 3
  the corpus before).
- **W73** - `X = 3` **N/N** (11/11); Revelation from a `max affordable X` of 1 or 2 above 5 life
  **0** (0/18); Revelation at life <= 5 with a header second number >= 1 taken at the LARGEST X
  **N/N** (UNTESTED twice - per #132 a third no-window corpus moves the check to the row).
- **W74** - land drop **N/N** (98/98); sweeper at header second number 0 **0** (220/220); Lightmine
  cast below 3 attackers **0** (0/102); Path at a [defender] or a textless token **0** (0/12).
- **W75** - TARGET asks with 2+ candidates decided by engine text, then `[flying]`, then power:
  **N/N** (5/5); a candidate whose only text is "When this creature enters" taken over one of those:
  **0**.
- **W76** - cleanup asks sending a counter while a sweeper, a Lightmine, a duplicate or a spare land
  is still in the list: **0** (was 1/37); a `[second copy:` Staff or Elixir row declined for being a
  second copy: **0** (5/5 taken).

deck126:
- **D75** - windows in which BOTH enchantment names are on the battlefield line and none of Rule
  #2's five starters is available: a cast taken **N/N** (was 0/5); `Cast nothing right now` taken
  there: **0** (was 5). Replies asserting that damage dealt TO the seat triggers Exquisite Blood:
  **0** (was 1 plan, carried across 9 turns).
- **D76** - `[second copy:` rows taken whose card is a Chromatic Lantern or a duplicate enchantment:
  **0** (was 7/9); `[second copy:` rows taken whose card is a Staff of Nin: **N/N** (2/2, correct).
- **D77** - a PLAN line naming a permanent that is NOT on the seat's battlefield line at write time:
  **0** (was 2 games' worth); a decision taken on an echoed plan whose board claim is false: **0**
  (was 3 windows in `vs146`).
- **D78** - land drop **N/N** (was 46/47, the miss from `name_over_index`); `CHOICE:` lines whose
  parenthesised name disagrees with the number: **0** (was 1).
- **D79** - Tribute at `at 0 this does nothing`: **0** (0/206, third corpus at the ceiling); Tribute
  in the FIRST named non-defender-victim window: **N/N** (3/3); Tribute at a named [defender] victim
  above 10 life with an entry 1-6 row beside it: **0** (0/0, UNTESTED).
- **D80** - `reveal` picks naming a half already on the battlefield or hand line: **0** (0/6);
  Idyllic Tutor cast with the missing half printed on the hand line and a Cast row for it on the
  same menu: **0** (0/8).
- **D81** - blockers: a blocker whose every line reads "your blocker dies" assigned while every
  attacker is already blocked by a surviving blocker and no enchantment of the seat's is out:
  **0** (D70's new limit; was 0 in substance, 1 against the old wording).
- **D82** - engine #2 pin: `{paying this taps:` source count == receipt source count **N/N**
  (was 6/7); Battlements tapped for a payment one Battlement covers: **0** (was 1).

# What I did NOT check

- I did not run wagic, touch `bin/Res`, `src/` (beyond READING `AIPlayerGPT.cpp` for the emitter
  strings named in engine #1 and set B) or git, and I did not read `game-*.stderr` or the new
  `User/transcripts/`. JSONL + `results.tsv` only. All guide work is under `wave52/`.
- **Scryfall: nothing cross-checked.** Every card fact is the primitive's `text=`/`auto=` line or
  the engine's rendered string. No primitive/Scryfall disagreement is therefore reported either way.
- Lane B's colour-pip audit was NOT re-run this corpus (wave 51 did it heuristically); I checked
  `- Paid` receipt COUNTS and no-source counts, not pip-by-pip source choice. The one over-tap I do
  report (engine #2) was found by comparing the forecast clause with the receipt, not by pip tracing.
- Blocker assignments inside each `blockers` reply were audited against the A-line `deals N` numbers
  only for the six deck126 windows; deck125 has no blockers (no creatures) and I did not audit the
  opponents' blocking decisions in any game.
- Lanes A (Soul Shatter-class edicts), F (`{feeds:`, `(text omitted)`), K's life-cost and
  `[legendary:` branches, L's Equip-block and `makes no mana` branches, the adventure/split/morph
  and evolve/equip owner batches, and the Baka pump-targeting fix: **no window on my seats**
  (UNTESTED-AT-THIS-SEAT, not absence).
- I did not open the `deck126 vs123` seq 63 record's full board to determine WHY the Sorin Vampire
  token rendered `[doesn't untap during its controller's untap step]` (engine #8) - I compared the
  A-line against `planeswalkers.txt:3057` and stopped there.
- Whether deck125's four Staff-gate breaks and two Elixir-gate breaks would have been punished on a
  board without lane I's opponent-turn windows: I read the windows that happened, not the ones that
  would have.
- deck126 `vs130`: I did not open every one of the 37 skipped mana-only windows to confirm none
  reached the model; I read the `gameend` counter.
