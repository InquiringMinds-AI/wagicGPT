# deck140 wave-10 — core-prompt (general-strategy.txt) proposals

## Verdict: PASS on the core prompt body.
No 2+-seat core candidate arises from the deck140 seat this wave, and the convergence gate forbids a
single-seat item cutting or adding a core line. Every residual is layer-routed OFF the core prompt body:

- **Reply-protocol PLAN/CHOICE mismatch (Finding 3)** — owns the code-appended reply-protocol layer,
  NOT the core prompt body. It is a harness-layer defect (the model's trailing index contradicting its
  own correct plan prose), and per standing doctrine a reply-protocol defect is never scored against or
  fixed in the prompt text. The durable fix is the reply-protocol A/B; deck140's specific contribution
  (see skill.md Proposal 2) is that the winning A/B arm should reconcile the trailing index against the
  plan's NAMED action, because the model reliably names its choice in prose. **Layer: reply-protocol
  A/B. Single-seat-CONFIRMING (2nd seat after deck135). Not a core prompt-body edit.**

- **Reach starvation (Finding 6)** — deck construction (notes.md #7). The core prompt cannot manufacture
  a threat the decklist never draws; louder exhortation would misplay. **Layer: deck construction.**

- **intent-collapse-metric.py miscalibration (Finding 4)** — tooling. **Layer: notes.md ledger /
  reply-protocol-ab.**

## Confirmed-shipped (no action, positive record)
- The **adjudicate-by-life-at-cap** infra (deck140's OWN wave-9 general-suggestion #1) SHIPPED and is
  confirmed working: the wave-9 AHEAD-but-timed-out grinds (vs110, vs131) now bank as adj0 WINS,
  verified against the translog life-at-cap (Finding 5). This closes deck140's only prior
  general-suggestion. No further infra ask this wave beyond the standing `-j`/latency note (notes.md #4),
  which no longer costs games now that caps adjudicate.

Nothing for the core prompt body. PASS (matches the pool's multi-wave core-PASS streak).
