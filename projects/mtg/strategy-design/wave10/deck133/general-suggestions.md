# deck133 wave-10 — core-prompt (general-strategy.txt) proposals

**Verdict: PASS (no core prompt-body edit), with one SINGLE-SEAT parked candidate.**

deck133 produced no 2+-seat core candidate. The convergence gate forbids a single-seat item cutting or
adding a core line, and the pause-aware freeze posture holds. The core reflexive-blocking floor is
untestable here (0 blocker windows, structural). Layer-routing note: the two engine/harness items this
wave (self-Fatal-Push number/parse leak; attacker name-vs-A# parse miss) go to notes.md, NEVER prompt
text.

## Single-seat parked candidate (NOT adopted — flagged for a 2nd-seat grep by synthesis)

**When the plan's desired card is UNOFFERED, the safe fallback is "Cast nothing", not an offered
harmful option — and the answer-after-plan protocol does not currently guarantee this.**

Grounded in deck140 seq19 (findings F): the pilot planned Phyrexian Obliterator (not in the 2-option
list), and instead of the list-anchor rule's mandate ("cast the best card that IS listed" = here, the
only safe option, Cast nothing) it emitted the digit for the self-Fatal-Push. The core/general list-
anchor already tells the pilot to confirm its named card is offered and otherwise cast the best LISTED
card; the failure is that "best listed card" was read as an OFFERED action rather than defaulting to the
passive/safe option when the desired card is absent. A candidate general-layer sharpening would be: *if
the card you named is not in the list AND every listed cast is useless or self-harming, the best listed
choice is "Cast nothing" — never a cast that targets your own permanents.* 

Why PARKED, not proposed:
1. **Single seat, 2 instances**, both in WON games (non-deciding) — fails the convergence gate.
2. It is fundamentally a **representation/parse residual**, not an instruction gap: the pilot REASONED
   correctly ("I will not cast Fatal Push") and the emitted number still slipped — this is the
   representation-beats-instruction wall, where louder core prose is the wrong lever. The durable fix is
   the harness one (notes #1): route the unoffered-desired-card fallback and the formatless-truncation
   case to a safe default rather than an int-grab.
3. The primary residual (deck110 seq20) is a truncation + format-noncompliance parse artifact, squarely
   a harness item, not a prompt-body one.

**Reactivation trigger:** a SECOND seat this wave showing a plan that correctly rejects a listed
self/own-target/harmful option in prose but casts it via the recorded number (grep other seats' own-
targets and unoffered-desired-card windows). If it recurs cross-seat AND the harness fix (notes #1) is
judged insufficient, promote to a general-layer "unoffered-card default is Cast nothing" line.
