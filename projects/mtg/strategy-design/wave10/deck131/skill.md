# deck131 wave-10 -- strategy-writing-skill proposals

**One closure-note contribution; no new method headline.** Every deck131 line freeze-checked clean;
the guide is byte-identical. Per the epistemics rule, a single seat cannot cut or add a core method
line -- the item below COMPLETES an existing rung (it is the closing observation on wave-9 method
headline 2), so it is recorded as a closure, not a new headline. The synthesis owner should confirm
against the other seats before promoting the wording.

## Closure -- a reply-protocol layer fix is verified the SAME way a representation fix is: the WITNESSED defect-class goes to zero AT THE SEAT THAT WITNESSED IT.

Grounded in the deck131 HEADLINE finding. Wave-9's method headline 2 established the reply-protocol
INTENT-COLLAPSE class, routed it to the code-appended reply-protocol layer, and scheduled an A/B
(accept the LAST bare number / move CHOICE after the PLAN). deck131 was one of that headline's named
witnesses (vs44 seq1 mulligan false-Keep: PLAN concludes Mulligan, head `1`=Keep committed first,
never revised).

This wave the answer-last protocol shipped, and the deck131 seat is the clean end-to-end close:
the SAME decision class (a no-Mountain mulligan whose plan concludes Mulligan) now LANDS on Mulligan
2/2 (vs135 seq1, vs133 seq1, both `CHOICE: 2`), and outcome-correct rose from wave-9's 0/1 to 2/2.

The method point (extends the confirmed-fired-representation terminus to the reply-protocol layer):
**a shipped reply-protocol change is a HYPOTHESIS until you see the witnessed defect-class go to zero
at the witnessing seat -- verify it the same way a representation fix is verified (grep the corpus,
count the defect class), and split reasoning-vs-outcome so the close is legible (wave-9 had reasoning
1/1 / outcome 0/1; this wave outcome caught up to 2/2).** This is the mirror of the wave-9
reasoning/outcome split: wave-9 showed the fix fired in the reasoning while the outcome stayed wrong
(leak in a lower layer); this wave shows the lower-layer fix closing the outcome gap. It confirms the
layer-routing discipline paid off -- the guide was correctly NOT re-worded over the collapse, and the
right layer (the protocol) resolved it.

No wording change to the skill is required unless the synthesis owner wants to add a single verifying
sentence to the reply-protocol-layer doctrine. Recommend: WATCH -> record as a validated closure of
the wave-9 A/B, kept as lineage.

## No other proposals.
The mulligan-gate STRUCTURAL-ordering method (wave-8 deck131 #2), the run-representation-on-the-MENU
method (wave-7 deck131 P1, land co-offers), and the pause-aware demotion terminus all held this wave
with no refinement needed. No new authoring lesson emerged.
