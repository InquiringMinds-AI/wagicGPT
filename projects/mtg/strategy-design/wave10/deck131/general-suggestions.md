# deck131 wave-10 -- core-prompt (general-strategy.txt) proposals

## PASS -- no core prompt-body edit from this seat.

No 2+-seat core candidate surfaced at deck131 this wave, and the convergence gate forbids a
single-seat item cutting or adding a core line.

Layer-routed items (none land in the core prompt):

1. **Reply-protocol layer -- CLOSED, not a proposal.** The wave-9 intent-collapse residual that
   this seat witnessed (vs44 mulligan false-Keep) is resolved by the answer-last protocol already
   shipped in this corpus's binary. It is NOT a core prompt-body item (it owns the code-appended
   reply-protocol layer, per wave-9 headline 2). Recorded as validated in findings.md / skill.md.
   No action.

2. **`damage >= toughness = dead` -- single-seat WATCH, do NOT promote.** Still one seat (wave-8
   vs140 only); zero recurrence this wave (its precondition -- a sweeper on the stack over a live
   2/2 Guttersnipe with a counter up -- did not occur). Below the convergence bar. If a SECOND seat
   ever reproduces a toughness-survival misjudgment, revisit as a core rules-fact candidate.

3. **Mana-availability confusion (spiral trigger) -- harness/model, single-seat, notes.md.** The
   truncation fallbacks at this seat are mostly triggered by the model distrusting "Mana available:
   (no untapped sources)" or miscomputing generic-mana payment. This is the SAME family as the
   core's existing "Mana in your pool: (none) read as I cannot cast" correction, so the core already
   speaks to it; no new core sentence is warranted from one seat. It manifests here as truncation,
   not a wrong pick, and changed no game. Routed to notes.md as a harness/model ledger item, NOT the
   core prompt.

Nothing else. Core status from this seat: **PASS** (consistent with the pool's multi-wave PASS run).
