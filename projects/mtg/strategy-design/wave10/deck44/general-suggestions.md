# Core-prompt (general-strategy) proposals — deck44 wave 10

## PASS — no core prompt-body edit from this seat.

deck44 returned a clean FREEZE. No 2+-seat core candidate originates here, and the two model-relevant
items this corpus are ENGINE-layer, not core prompt-body — they are routed to notes.md per
layer-routing discipline (engine items NEVER become prompt text), and are noted here only so the
synthesizer can see they were considered and correctly routed away from the core.

### Considered and routed to ENGINE (notes.md), NOT core:
1. **Auto-tap greediness stranded {U}{U} and cost the vs140 game** (couldn't counter Blightsteel
   Colossus). The core prompt ALREADY carries the mana-discipline fact ("a response window opens only
   while a response is PAYABLE; hold reaction mana open") and the guide carries "LEAVE two blue
   sources untapped." The failure is that auto-tap makes that instruction UNEXECUTABLE — the pilot
   cannot choose which lands tap. This is a Step-2 "a rule the executor cannot execute" case: the fix
   is the engine's auto-tap heuristic (spend colorless/off-color sources on generic costs first,
   preserving colored mana for held instants), NOT a louder core sentence. Adding core text about
   holding mana would displace the rule the reviewer should write (the engine one). notes.md #2.

2. **Counter-option mis-annotation ("NO legal target right now" over an occupied stack).** A
   representation/engine defect the pilot robustly overrides; fixing it is engine work. Any core/guide
   line teaching the pilot to distrust the annotation would be restatement of a transient engine
   artifact. notes.md #1.

### Single-seat items that do NOT reach the core:
- The category-#4 breadth (counter any creature at <=12 life) let a correct race-line decline a 1/1
  at L6 (vs109 s17). This is a per-deck guide nuance, not a core matter, and the pilot's judgment was
  correct — no action.

Core status from deck44: **PASS** (7th consecutive freeze at this seat; the wave-9 counter-#2
tightening is validated, not a core edit — it lives in the deck guide). The live general prompt is an
unchanged copy of the deployed post-A/B prompt as far as this seat is concerned.
