# General / core proposals — deck44 wave 8 (convergence-gated, layer-routed)

Frozen-spine seat. Single-seat evidence cannot cut/add a CORE line (epistemics rule). No core-prompt-body
edits proposed. Each item names its layer and burden of proof.

## 1. ENGINE/REPRESENTATION — targetless option is STILL offered bare (RE-FILE, correction to the ledger)

- **Evidence:** the validation-drop CLASS is now effectively dead — corpus-wide **1** `fails validation`
  (`land Glimmervoid`, the deliberate residual gate), down from wave-7's 15 (GFTT 9 + Liliana 4 + Fatal
  Push 2). BUT the underlying CONTRACT VIOLATION persists at the menu: `Cast Go for the Throat {1}{b}` is
  **still rendered bare with an empty opponent board and NO `[no legal target]` marker** (verified: no such
  marker renders anywhere in the corpus; repro vs133 s5/s7/s10/s12/s13). The 0-drop rate is the GUIDE
  fallback declining the option, not engine option-suppression.
- **Routing: ENGINE, unchanged.** Burden of proof for core is more than met (deck-agnostic, mechanically
  guaranteed). At option-build time, for a spell/ability whose only legal mode requires a target, SUPPRESS
  or mark `[no legal target]` the "Cast X" option when the legal target set is empty. **Correction for the
  synthesis ledger:** do NOT treat the target-check as fully shipped — the pick-time deferral was cured
  (validation-fails ~0) but the OFFER-SIDE suppression did not ship, so **every per-deck GFTT no-target
  stopgap remains LOAD-BEARING and must NOT be demoted** (it is not double coverage — the representation
  never arrived). Removal condition for all guide stopgaps of this class: option-build-time suppression/marking ships.
- **Also (data quality):** `deferred_to_heuristic` is now a translog field (brief) — good; deck44 had none
  (it never picked a targetless spell). Keep it.

## 2. ENGINE/REPRESENTATION — block-seam "deals 0 / not lethal / punisher" rider (single seat; per-deck floor exists)

- **Evidence:** deck44 `vs110 s19` (L11) — Archmage of Echoes (4/4, the deck's WIN CONDITION) reflexively
  blocked Ornithopter (0/2, **deals 0**). The guide already forbids it ("at 9+ life... Do NOT block even a
  0-power attacker"); the pilot disobeyed at distance. Harm ~0 (won 7/-11). This is the **recurrence of the
  wave-7 P2 reflexive-block watch, same deck (vs110)** and the same shape as the inherited 3-guise
  convergence (high-life chump / block-the-only-reach-creature / block-a-punisher).
- **Routing: PER-DECK this wave** (the guide floor exists and is reinforced with the named win-con card),
  **ENGINE/REPRESENTATION watch** for the durable fix: annotate a blocker option with a `[deals 0]` /
  `[not lethal]` / punisher rider at the block seam — the SAME surfacing that fixed the stack seam and that
  wave-7 asked for the Obliterator punisher. Present-and-losing-at-distance routes to REPRESENTATION, NEVER
  a second core paragraph. Do NOT add core text (still single-seat family across witnessing decks; every
  seat agrees the floor exists and the pilot disobeys at distance).

## 3. WATCH (no edit) — plan/choice mismatch on counter options is CLOSED for deck44

- wave-7's 1-seat watch did NOT recur: **0 mismatches / 15 counter windows** (the plan-binding line is
  obeyed perfectly). Retire the watch unless a second seat reopens it. REPLY-PROTOCOL / representation
  intervention is NOT needed. Recorded so synthesis can close it.

## 4. Cosmetic (ENGINE) — empty `[counters:]` bracket still renders once counters reach 0 (carried from waves 5-7).

## Summary of routing
- Targetless option still offered bare: ENGINE (offer-side suppression/marking). Ledger CORRECTION — do
  NOT demote the GFTT guide stopgap; the representation did not ship. Deck-agnostic, multi-wave.
- Reflexive block of a win-con at 0-power: PER-DECK now (reinforced); ENGINE/REPRESENTATION watch (block-seam rider).
- Plan/choice mismatch on counter labels: CLOSED (0/15).
- No CORE-prompt-body edits.
