# deck133 wave-8 proposals for the strategy-writing skill

Mode this wave: OBEYED-BUT-LOSING / TURNAROUND (record 3W/2L real games, wedge excluded; wins
execute the guide's own lines). The one guide change was engine-fix-driven (GM display fixed ->
delete the +2 stopgap). The proposals below are METHOD data points, each flagged single-seat vs
confirming-existing.

## 1. [CONFIRM — existing method fired end-to-end] The confirmed-fired-representation-fix terminus DELETED a live guide stopgap this wave — the second clean instance of that lifecycle end.
The skill's Step-0 terminus (a confirmed-fired representation fix DELETES the guide stopgap it
replaced) was authored on deck110's re-equip marker. deck133 wave-8 is a second, cleaner instance
at the DELETION end: the Gray Merchant "+2" workaround (carried since wave-5) is deleted because the
engine display fix SHIPPED and is confirmed at the seat (vs109 seq36: `{right now: drains 8}`
resolved as exactly 8; brief probe: drains 6 on devotion-4). No method change needed — the lifecycle
worked as written. Positive data point: the deletion trigger is "grep the new corpus + the engine
ledger, confirm shown == resolved," and it fired correctly off a SINGLE GM cast because it agreed with
the independent probe. CONFIDENCE: high.

## 2. [CONFIRM — two-uses-two-triggers, finally exercised] A keyed second-use trigger can sit UNEXERCISED across multiple waves and still be correct — do not cut it for lack of corpus evidence.
The GM <=10 stabilizer trigger (the "panic button" second use, wave-5 two-uses-two-triggers) was
flagged "STILL unexercised across two waves" in wave-6 and wave-7 findings — a card that reaches the
list in only 2/6 games and never during a low-life crunch. This wave it FINALLY fired (vs109 seq36,
cast at my2 -> gained 8 -> my10, opp -> 10) and WON the game. Reinforces the skill's "never-drawn !=
validated, and don't cut it either": a keyed trigger for a low-density card is under-powered evidence
for MANY waves, and cutting it would have removed the exact line that flipped vs109 L->W. No method
change; a data point for the "keep low-density keyed triggers at proportional prominence" stance.

## 3. [WATCH — do not promote] A rule PRESENT-and-DISOBEYED only in already-lost / construction-bound spots stays FROZEN, not louder.
Both past-turn-3 Thoughtseizes in the corpus were into <=1-card hands (vs110 seq23 into an EMPTY hand
at 2 life while dying; vs44 seq21 into a 1-card hand in a race lost at 0), violating the guide's
explicit "hand shows 0 -> don't Thoughtseize" rule. The temptation is to shout the rule louder. But
both disobediences were in games ALREADY lost on construction (no early blocker / no flyer answer /
threats undrawn), and the rule was OBEYED in the early game of every win. The skill already covers
this (turnaround freeze mode + "rule present-and-losing at distance routes to representation, not a
louder paragraph"). PROPOSAL: none — recording that the existing guidance correctly steered a "don't
churn the obeyed line" decision, and naming the right escalation IF it recurs in a DECIDING spot:
representation (surface opp-hand-size AT the cast decision so the empty-hand case is unmissable),
never more prose. Single-seat; keep as a watch under the existing method.

## 4. [CONFIRM — freeze mode sizing held] Freeze-check on a mixed-record wave produced a ONE-EDIT diff, and the one edit was engine-driven, not model-driven.
No new method, a positive data point echoing wave-7's item 4: applying freeze-check to a mixed 3W/2L
control-arm wave yielded a near-zero diff (only the mechanical GM `N+2 -> N` correction). The losses'
ceiling (both construction-bound races) does NOT justify any model-fixable #1-slot lever, so no
top-slot churn. The OBEYED-BUT-LOSING mode + "size the residual lever's ceiling" gate both fired
correctly. Keep the mode as written.
