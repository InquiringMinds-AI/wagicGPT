# deck140 wave-8 — core-prompt / layer-routing proposals

**Recommendation: NO core-prompt edit this wave (PASS).** deck140 is again the only seat that exercised
sweepers, so single-seat epistemics forbid a core add/cut on this evidence. The wave-8 work is entirely
a per-deck constrained diff (demote two false-belief kills the shipped representation now carries;
freeze the rest). Layer-routing below.

## Routed PER-DECK this wave (in strategy.txt)
- **Demoted** the tapped-bridge and artifact-bridge kills to a one-line-each nod anchored to the live
  marker strings `(creatures: N)` and `[tapped - untaps and can attack next turn]` — the representation
  now perceives what those paragraphs used to assert (validated: pilot swept tapped/artifact boards).
- **Kept (compressed)** the SHROUD kill — representation surfaces a creature COUNT, not TARGETING, so
  "shroud does not stop a non-targeted sweep / Cruel Edict targets the player" is still guide-only and
  load-bearing.
- **Added** one BSZ-X guardrail (don't cast Black Sun's Zenith for an affordable X below the target's
  toughness; use Wrath/Damnation instead) — deck140 sweeper-selection detail, not core.
- Countable SWEEP TRIGGER, Rakdos hold, X-menu, Staff face-ping, Lightmine deploy, Mulligan/Override —
  all FROZEN verbatim (obeyed this corpus).

## Convergence WATCHES for core (advancing, still gated on a 2nd seat)
1. **"A TAPPED attacker is still a threat — a sorcery-speed sweeper kills it the same, and it untaps
   and attacks again."** deck140 is STILL the only witnessing seat, but note the resolution changed:
   the ENGINE shipped `[tapped - untaps and can attack next turn]` (86 firings) and the pilot obeyed it
   — so this belief is now carried by REPRESENTATION, weakening the case for a core PROSE line. If a
   second control/removal deck reproduces a tapped-hold DESPITE the tag, the fix is representation
   coverage (extend the tag), not a core sentence. WATCH, leaning representation not prose.
2. **"Shroud/hexproof does not stop non-targeted effects (destroy/damage all, sacrifice-a-creature)."**
   Single seat (deck140). Representation does NOT carry this (a count says nothing about targeting), so
   IF a second deck reproduces shroud-conflation suppressing a non-targeted spell, THIS one is a
   genuine core mechanics-fact candidate (the general prompt already carries a mechanics-facts block).
   WATCH; promote on a second seat.

## Representation ask — CLOSED for deck140
The wave-7 ask ("surface `creatures: N` + tag tapped attackers") SHIPPED and FIRED: the count is on
every opponent-battlefield line and the tapped tag fired 86 times this corpus. It did the job — the
tapped/artifact false-belief holds are gone and the guide's inline kills for them are demoted. No new
representation ask for deck140; the only residuals are deck construction (notes.md).
