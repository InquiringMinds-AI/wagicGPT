# Proposed revisions to the strategy-writing skill (from deck109 wave-8)

**Net: no method CHANGE proposed. The machinery held; one cross-cutting escalation is worth flagging
to the synthesis agent, and one existing method line got a strong new confirmation.**

## 1. STRONG CONFIRMATION — a shipped REPRESENTATION fix can be PRESENT-AND-LOSING even after it fires
deck109 is a clean data point for the skill's "verify a shipped fix FIRED, then read whether it is
OBEYED" discipline extended past FIRING. Engine ledger #5 (blocker-seam lethal arithmetic) FIRED in
3/3 blocker prompts with the exact rider "you would be at M - NOT lethal: taking damage while ahead is
often correct" — and the pilot READ-AND-DISOBEYED it in both loss games (blocked a 6/6 at 19 life;
blocked Phyrexian Obliterator into its sac-punisher at 10 life, both flagged NOT lethal). This is the
representation-beats-instruction WALL applied to a representation that is CORRECT and SURFACED and STILL
bypassed. The skill already says "when the pilot reads the surfaced fact and disobeys, instruction has
failed at full strength — do not escalate wording; record it as a representation defect and stop." This
seat is that rule's blocker-seam instance. No new method text needed; it validates the existing rung and
gives the synthesis agent a concrete case that "surface the fact" (engine #5) did NOT close the reflexive-
blocking convergence — the convergence's shipped remedy is itself now present-and-losing.

## 2. FLAG for synthesis (not a method change) — the surfaced lethal line can FIGHT a per-deck chump rule
The wave-7 watch asked whether the new blocker math IMPROVES or FIGHTS each guide's blocking rules. On
deck109 it FIGHTS: the guide's chump branch fires at "projected life <= 8" while the surfaced math flags
only actual lethal (<= 0), so at me10->3 (surfaced NOT lethal) the deck's own chump rule re-authorized
the block. The fix is per-deck (re-anchor the chump threshold to the surfaced lethal line via threshold-
reuse; hoist the damage-punisher "never block" absolute as its explicit exception — the wave-7 deck44
intra-guide-contradiction method, now reproduced at a 2nd seat). This is a good exemplar for the existing
"re-anchor a projected-count brake to the engine-SURFACED number, don't keep a hand-computed threshold
that fights it" line — no new method, just a second witness.

## 3. WATCH held, not promoted — decline-a-listed-BODY did NOT recur
The wave-7 phantom-lethal-fixation -> decline-a-listed-body watch (deck109, 1 seat) got NO 2nd instance in
the creature-body shape this wave. A DIFFERENT false-cost leak appeared instead (declining a listed BURN
spell — Pillar of Flame read as "{1}{R}" when it is {R}); that is a card-specific corrective-fact case,
not the same pattern. Keep the body-decline watch at 1 seat.

No method line was found wanting; nothing to cut.
