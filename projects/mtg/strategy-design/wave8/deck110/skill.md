# Deck-110 wave-8 — proposals for the strategy-writing skill

Context: deck110 owns the re-attach marker saga (wave 5 designed it, wave 6 found it inert, wave 7
closed it fired-and-obeyed). Wave 8 has NOTHING new to add to that loop — it re-verified clean and is
DONE. The wave-8 signal is a DIFFERENT representation rung: an engine-surfaced count the pilot ignores
in favor of a broken hand-count. My proposals are small; the skill already contains the governing
method (deck133 P2 prefer-surfaced-number; verify-offset). This is a confirmation-with-one-sharpening
wave, not a method-expansion wave.

## P1 (sharpening, single-seat but reinforces an existing rung) — a NEWLY-surfaced count re-opens the "manual-recompute is a representation defect even in a win" check; run it against every value the engine STARTS surfacing this wave, not only ones flagged last wave.

The skill's prefer-engine-surfaced-number rung (deck133 P2, Gray Merchant drain) is framed around a
value a PRIOR wave already identified as hand-computed. deck110 wave 8 shows the same defect appearing
FRESH the moment the engine adds a NEW surfaced number (`Artifacts in play: you N`, new this wave):
the pilot keeps hand-counting artifacts and over-counts (folds in Glimmervoid + basic lands; vs140 s33
"6 artifacts (2 Glimmervoids, Island...)" vs surfaced `you 3`), 180/180 offset verified ZERO. The
guide edit is a pure representation READ swap. Proposed one-line addition to the "prefer an
engine-SURFACED number" bullet: "when THIS wave's engine ledger adds a NEW surfaced value the guide
used to hand-compute (an artifact count, a devotion, a damage total), re-run the manual-recompute
audit against it immediately — the pilot will keep hand-computing the old way until the guide points
at the new line, and the hand-computation is exactly where the deck-specific miscount lives (deck110:
Glimmervoid/land folded into the artifact count)." Single-seat; adopt as a sharpening or hold to
watch — it composes two lines the skill already has (prefer-surfaced-number + verify-fired-first) and
just names the NEW-surfaced-number trigger explicitly so a reviser doesn't wait for a loss.

## No-change confirmations (skill method validated this wave)
- **Verify-a-shipped-representation-fix-FIRED (deck110 wave-6 headline) + the DEMOTION terminus
  (deck110 wave-7 headline):** re-applied — the marker re-verified fired-and-obeyed (16 offered / 0
  taken, no-op 0/9) and the wave-7-compressed Rule #1 held with zero regression. The full trichotomy
  lifecycle (inert -> fired -> obeyed -> demoted-stopgap -> RE-VERIFIED-stable) is now closed across
  four waves; no skill change needed, method vindicated.
- **OBEYED-BUT-LOSING / variance-entry test (deck110 wave-6's own contribution):** re-applied cleanly —
  5/6 with healthy obedience; the one loss lacked a precondition the guide can't create (a live
  clock + un-countered threats vs an evasive counter deck). Freeze + route to construction worked.
- **Frozen-line diff / turnaround-preservation:** the guide was frozen except the one licensed
  representation-read edit; no churn of obeyed lines.
- **META SHIFT as a deck-construction flag (deck110 wave-6):** re-confirmed — deck44 countered Cranial
  Plating + Etched Champion (+ Master) this wave; correctly NOT "fixed" with guide wording.
- **Read-the-reply-of-the-lone-counterexample (deck110 wave-7 P2):** N/A this wave — there was NO
  marker counterexample (0 taken), so the junction wasn't exercised. Still-valid, untested this wave.
