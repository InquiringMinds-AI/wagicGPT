# Wave-55 seat: deck123 (1/6) + deck130 (4/6)

TWELFTH fair-hand corpus, `~/.Wagic/ai/gpt/selfplay-runs/matchups-20260903-074354/`, binary
`b228b8648` (wave-55 step-1 lanes A-E on the wave-54 base + P purge + Q autotap + R stall-floor).
Model `qwen36-35b-a3b` on every gameend at both seats. Guides = the wave-54 reviewer revisions
(`5160eb1cb`) plus the boundary pass (`dd0cd2d74`). Per the carry's binding rule nothing below
attributes a win rate, a game length or a curve to a guide edit.

**Files (short -> full).**
- 123v125 `1788439469-ai_baka_deck123-0x56451bc167a0-vs-ai_baka_deck125.jsonl`
- 123v126 `1788439449-ai_baka_deck123-0x55abf742ea40-vs-ai_baka_deck126.jsonl`
- 123v130 `1788439457-ai_baka_deck123-0x55926e7b57b0-vs-ai_baka_deck130.jsonl`
- 123v146 `1788439455-ai_baka_deck123-0x56234143a110-vs-ai_baka_deck146.jsonl`
- 123v152 `1788439441-ai_baka_deck123-0x55efaf30f3a0-vs-ai_baka_deck152.jsonl`
- 123v162 `1788439463-ai_baka_deck123-0x5566cbbb67e0-vs-ai_baka_deck162.jsonl`
- 130v123 `1788439457-ai_baka_deck130-0x55926fad1d40-vs-ai_baka_deck123.jsonl`
- 130v125 `1788439459-ai_baka_deck130-0x55cec2451870-vs-ai_baka_deck125.jsonl`
- 130v126 `1788439451-ai_baka_deck130-0x55f1e762ca70-vs-ai_baka_deck126.jsonl`
- 130v146 `1788439471-ai_baka_deck130-0x55cc7200f100-vs-ai_baka_deck146.jsonl`
- 130v152 `1788439443-ai_baka_deck130-0x5557d30cc320-vs-ai_baka_deck152.jsonl`
- 130v162 `1788439445-ai_baka_deck130-0x562d12630880-vs-ai_baka_deck162.jsonl`
Opposing seat logs read for the other side of the narration; the eleven `game-*.stderr` files that
belong to my games read for the drop/release/replay counts in the ENGINE list only.

**Records.** deck123: **433 decisions, 6 fallbacks** (ask 311 / priority 74 / discard 19 /
attackers 8 / blockers 4 / bottom 3 / reveal 1). **1 win** (v146 t10 12/-2), **5 losses**
(v152 t35 -2/20, v126 t13 -1/41, v130 t36 -3/20, v162 t14 0/20, v125 t39 0/32).
deck130: **440 decisions, 3 fallbacks** (ask 261 / priority 169 / attackers 18 / bottom 3 /
blockers 2). **4 wins** (v123 t36 20/-3, v125 t16 19/-2, v126 t22 20/0, v146 t30 14/0),
**2 losses** (v152 t23 -5/25, v162 t11 -1/15).
Combined **873 decisions, 9 fallbacks = 1.03 %** (wave 54 at these seats: 17/896 = 1.90 %), and
**9 of the corpus's 14 fallbacks are mine** (`named_row_reask` 2 of the corpus's 3,
`plan_choice_conflict` 6 of its 9, `wall_miss_unrecorded` 1 of 1). 0 empty replies, 0 timeouts,
0 `choice: -1` without a fallback, 0 `stale_echo`, 0 `unparsed_reply`.
Latency p50 **21.4 s**, p90 64.4 s, >60 s 106 of 873, max 818,064 ms (`123v130` s9, the
`named_row_reask` re-ask). Max prompt at my seats **29,214** (`123v152` s117, turn 34); prompts
over 20 K: 12, all in `123v152`. `130v123` at 159 records is my largest seat log.

**Opponent-turn windows, reported separately (carry obligation).**
| seat | own-turn windows | opp-turn windows | opp-turn casts | opp-turn declines | HOLD taken (opp / own) | own-turn casts |
|---|---|---|---|---|---|---|
| deck123 | 292 | **129** | **1** | 51 | **64 / 42** | 35 |
| deck130 | 351 | **77** | **0** | 16 | **37 / 27** | 78 |
206 opponent-turn windows produced **one** cast (`123v152` s31, Devour Flesh in their main phase
at N=1 - correct). That is the standing shape of these two seats and it is not a defect the guides
can move much: deck130 owns exactly one instant (Starstorm) and deck123's three instants are all
edicts or a Slip that want a target on the board.
HOLD row: **655 renders, 170 takes (26.0 %)**; my seats hold **170 of the corpus's 282 takes**.
`hold_windows_skipped` non-zero on **10 of my 12 gameends**, sum **414 of the corpus's 735**;
largest `130v123` **175**, then `123v125` **97**.

**Mulligans / hands (new-baseline record).** deck123 **10 looks, 4 ships, 6 keeps, 3 bottoms**;
keeps at 2,2,2,2,2,1 lands. Ships: `123v130` s1 (0 lands), `123v146` s1 (1 land,
`would not cover any spell in it (1 land is not enough for your cheapest spell at mana value 2)`),
`123v152` s1 (1 land, coverage line NAMED Tragic Slip), `123v126` - none.
deck130 **9 looks, 3 ships, 6 keeps, 3 bottoms**; keeps at 2,1,1,2,2,4 lands. Ships: `130v123` s1
(1 land, `would not cover`), `130v126` s1 (0 lands), `130v152` s1 (0 lands).
**No mull-to-zero, no repeated mulligan, no keep below the guide floor at either seat; nothing to
raise to the owner.** Two notes: (1) `130v123` s1 is the FIRST render at deck130 of the
`would not cover any spell in it` branch in five corpora - it shipped, kept a two-lander, and won
20 to -3; boundary B8 part 1 is finally **TESTED and PASSES**. (2) deck123 is inconsistent between
two one-land sevens whose coverage lines both NAMED a card: `123v162` s1 kept (guide-correct) and
`123v152` s1 shipped (guide-incorrect). One break in ten looks, and the ship still reached a
two-land keep, so I have not written a rule for it - I am recording it as the one open mulligan
question at this seat and predicting on it below (P5).

---

# deck123 - Intruders of Thraben. 1/6.

## L1 - v152 (dead turn 35, -2 to 20): the sweeper that swept its own five-creature board
**The decisive misplay at either seat this corpus, and it is pure STRATEGY - the row said the whole
thing.** At `123v152` **s104** (turn 28, 2 life against 18) the menu was:
```
1. Cast Damnation {2}{b}{b} {right now: destroys 1 of their creature (1 without a restriction
   against attacking), 5 of yours} ...
3. Cast Damnation {4}{b}{b} [from exile] {castable from exile - your card, exiled by their Elite
   Spellbinder ... it costs {2} more than printed} {right now: destroys 1 of their creature
   (1 without a restriction against attacking), 5 of yours} ...
4. Cast nothing right now (combat comes next this turn)
5. Hold priority for the rest of this turn: ...
```
Answered **3**, under `PLAN: ... Cast Damnation to wipe opponent's Katilda and their single
creature, saving my own 5 creatures. Main phase 2: Tap Thraben and Bloodline for tokens, attack
with army.` The board (3 Humans + Bloodline Keeper + Thraben Doomsayer, built over eight turns
against a counter deck) was gone in the next window - `s105`'s own plan reads "Board is empty of
creatures". Two rungs broke at once: RULE 3's **M at 4 or more is never**, and the fact that row 1
was the SAME card for two mana less. A55-1a, A55-1b, A55-15.
The rest of the game is clean and worth recording as such: **s30** cast Damnation on
`destroys 3 ... 2 of yours` at 6 life into three attackers (correct); **s31/s59/s87** three edicts
at `they control 1 creature` (correct, and s59's Katilda is the guide's mana-engine carve-out);
**s44** a Tragic Slip on a `{kills: Luminarch Aspirant}` row; **s75/s110/s117** three Marsh Flats
cracks, all duals; **s113** Tribute to Hunger at 1 life on `Intrepid Adversary (7/5) [lifelink]
is sacrificed, you gain 5`, which bought four more turns.
**CLASSIFICATION: STRATEGY x1, and it was the game.**

## L2 - v126 (dead turn 13, -1 to 41): the same assembled loop, and 26 asks inside it
deck126 assembled **Sanguine Bond + Exquisite Blood** again. This time the render says so in
words on every attacker row (`their life LOOP is in play: any life they gain or you lose in this
combat chains without limit - fatal to you, not a trade`, 6 renders) and the pilot obeyed it: at
**s53** it had **37 creatures** and answered `no attackers`, which is CORRECT - the four rows the
attack screen printed totalled 8 power against 20 life, and the Vampire blocker gains 1 on block.
Wave-54's A54-5 (Branch B2) is doing its job.
The game's real lesson is structural and is now in the guide (A55-6): **37 creatures, FOUR
attackers.** Intruder Alarm stops the untap step, so only creatures made on the opponent's turn
can ever attack, and this deck fired its whole chain in its own main phase 1 on turn 12.
Then **s57-s87: 26 consecutive asks in ONE Upkeep step**, life 20 -> -1 three points at a time,
menu a two-row `Cast Devour Flesh` / `Cast nothing right now` plus the HOLD row, declined-note
running 8, 11, 12, 13, 15, 17 ... 27. The HOLD row was taken **six times inside the run** (s57,
s61, s71, s72, s81-s85) and did not stop a single re-ask. **0 casts inside the run** - the pilot's
discipline is not the problem here. E1 (HIGH) below.
**CLASSIFICATION: matchup (a two-card loop neither sixty removes) + the corpus's largest single
block of dead LLM windows at my seats.**

## L3 - v162 (dead turn 14, 0 to 20): fifteen asks in one Draw step, and a wasted edict
Ob Nixilis + Underworld Dreams again. **s36-s50 is fifteen consecutive identical asks in the turn-14
Draw step**, life 10 -> 1, HOLD taken at s36 and s42 and neither take held. Zero casts inside it.
The one real break is **s18**: `Cast Tribute to Hunger {2}{b} {right now: they control 1 creature -
Fog Bank (0/2) [flying, defender] is sacrificed, you gain 2}` - taken, tapping out three sources,
against a deck whose threats are two enchantment-side punishers no edict can touch. Primitive:
Fog Bank `mtg.txt:42416`, 0/2 Wall, `abilities=flying,defender` plus
`preventAllCombatDamage to(this)` and `from(this)` - it could never have dealt a point. A55-3.
**CLASSIFICATION: matchup + one edict rung break + the window tax.**

## L4 - v130 (dead turn 36, -3 to 20) and L5 - v125 (dead turn 39, 0 to 32)
v130: Ponza stripped the mana; 24 decisions in 36 turns, eight of them Cleanup discards, no
creature ever cast. Two notes. **s11 `Cast Lightning Greaves {2}` on a battlefield line reading
"of which 0 are creatures"**, tapping out, off a THREE-row menu whose other rows were
`Cast nothing right now` and the HOLD row - the fifth corpus for this break, A55-2. And **s9**,
a `named_row_reask`: `CHOICE: 2 (Cast Thraben Doomsayer)` on that same three-row menu. **s17**'s
fetch row is the corpus's best render news at this seat: `... and it adds {B} (you cannot make {B}
right now)`.
v125: the counter deck plus Staff of Nin. 125 decisions in 39 turns (wave 54's same matchup: 303
in 64). **Two dead casts in 108 all-dead menus** - `s83` a dead Idyllic Tutor written up as
"dead, but only way to dig for Bloodline Keeper against counter deck" (A55-4) and `s87` a Damnation
at `destroys 0 of their creatures ... 0 of yours` (the P5 rung, still not closed). Against that:
**s65 is the engine saving a card** - the reply read `CHOICE: 1 (Cast Tribute to Hunger)` with prose
"Opponent has 0 creatures, so Tribute to Hunger does nothing. This is a dead cast", and
`answer_replaced` executed the HOLD row instead. `s4` fetched a **basic Swamp** off a menu whose
Scrubland, Tundra and Underground Sea rows each carried `(you cannot make {W} right now)`, against
a deck with no land destruction - A55-8.
**CLASSIFICATION: matchup ceiling in both; three rung breaks, all now written.**

## The win
**v146 (t10, 12 to -2).** Shipped a one-land seven under the `would not cover` branch (s1), kept a
two-lander (s2), bottomed Lightning Greaves (s3); Marsh Flats -> Scrubland t2, Doomsayer t6,
Alarm t8, **s15-s21 the chain to M=24 in one main phase**, and t10 `ATTACK:` twenty-three Humans.
One break inside a won game: **s22** sent a lone Doomsayer into
`[their untapped blockers: Triumphant Adventurer (1/1) (both die); Goblin (1/1) (you kill it, your
attacker lives); Silverquill Silencer (3/2) (both die)]` with 25 summoning-sick Humans at home,
under a PLAN whose only content was "stop reached". A55-13.

## Rungs, with denominators (deck123)
- **RULE -1 / mulligan**: 10 looks, 4 ships, 6 keeps, 3 bottoms. 9 of 10 agree with the header
  rule; the miss is `123v152` s1 (shipped a one-lander whose coverage line named a card). All
  three bottoms correct (Damnation with a second in hand; Lightning Greaves; Vision Skeins).
- **RULE 0 land drops**: **45 asks, a land played at 45. PASS 45/45.**
- **RULE 1 token lines**: 24 activations, 9 of them on the repeat row; **3 past the written stop**
  (`123v126` s58, s65, s70 - all inside the drain turn, M 36-37 against a stop of 25-28).
  The stop itself is being computed and written: `L <>, C <>, stop <>; M is <> now; this window:`
  appears on the PLAN line of every token window I read.
- **RULE 2 Marsh Flats**: 12 windows offered, **7 takes**, **5 duals + 2 basics**. One basic is
  the licensed carve-out (`123v130` s17, {B} missing against a land-destruction deck); one is a
  break (`123v125` s4). P1 **FAILS 1 of 7**.
- **RULE 3 Damnation**: 53 rows offered, 33 of them `destroys 0`. **4 casts**: 1 correct (`123v152`
  s30), 1 defensible (`123v152` s108, N1/M0), 1 dead (`123v125` s87 at 0/0) and 1 catastrophic
  (`123v152` s104 at M=5). P5 **FAILS 1 of 4**.
- **RULE 4 Tragic Slip**: 30 rows offered, 17 priced `kills 0 of the N legal targets`. **1 cast**,
  on a `{kills: Luminarch Aspirant}` row - **correct**. 0 takes on a dead row. **P2 PASSES, third
  corpus.**
- **RULE 5 edicts**: 261 edict rows. **171 at `they control 0 creatures - at 0 this does nothing`:
  0 taken. 52 at `- they choose which one`: 0 taken.** Both **PASS** (wave 54: 1 and 0). All **5
  casts** at N = 1; four correct, one on a defender (`123v162` s18).
- **Cast list**: `[second copy:` Intruder Alarm rows offered **2**, taken **0** - **P3 PASSES** on
  a thin denominator. Idyllic Tutor: 26 dead rows, **1 dead cast**. Lightning Greaves: 48 rows,
  3 casts, **1 at `of which 0 are creatures`** - **P7(b) FAILS, third corpus**.
- **All-dead menus**: 108 windows where every cast row carried a dead verdict; **2 casts**
  (wave 54: 5). **P2/A54-1 improves but does not close.**
- **Cleanup discard**: 19 asks, 0 breaks - every send was a duplicate, a spare land or a card
  whose row read `{dead right now: 0 legal targets on the board for it}`.
  **Blockers**: 4 windows, 0 breaks. **Attackers**: 8 windows, 1 break (`123v146` s22), and
  `no attackers` answered correctly on the life-loop board (`123v126` s53).

---

# deck130 - Budde's Ponza. 4/6.

## W1 - v123 (t36, 20 to -3) and W2 - v126 (t22, 20 to 0): two games without losing a life point
v123: Molten Rain t6 and t8, Stone Rain, Lay Waste, two Blastminer activations - deck123 was held
to two lands and never cast a creature. 157 decisions, 105 of them on its own turn; 49
opponent-turn windows, 30 HOLD takes and **`hold_windows_skipped` 175**, the corpus's largest.
v126: **the game this deck lost 0 to 44 last corpus, won 20 to 0.** The WALL CHECK is the whole
story: `no attackers` at s29 (t14) and s35 (t16) with Perimeter Captains up, then Rorix over the
top at s38/s44/s52 and a Hammer for the last 9 at s48 (3 + Rorix's 6 = lethal, the licensed
chain). Zero life lost. The wave-54 A54/D54 wall-and-converter work is holding.

## W3 - v125 (t16, 19 to -2) and W4 - v146 (t30, 14 to 0)
v125: Spellbomb t2 and t6, Lay Waste at a Tundra t12 (countered), Siege-Gang t10, four attack
windows with the full team, then the closing race - s45/s47 Spark Spray, s50/s51 Hammer, s60. One
break: **s32** (turn 10, opponent at 15) fired a Spark Spray at the face off a menu whose only
other rows were its own Commander and three Goblins. D55-5.
v146: a 30-turn grind won on Rorix. Three breaks, all in the guide now: **s17** a Spellbomb at
`Nadaar, Selfless Paladin ... takes 2 damage - SURVIVES (toughness 3)` under `PLAN: Kill Nadaar
with Pyrite Spellbomb` (D55-7); **s28** a Lay Waste cycled at three lands (D55-4); **s62** a second
**Rorix Bladewing** cast at 14 life against 13 off a row reading `[legendary: you already control
Rorix Bladewing - legend rule: casting this sends one copy to your graveyard (you choose which)]`,
with the first Rorix already attacking for 6 a turn - six mana for the identical board (D55-3;
`deck130.txt` runs `Rorix Bladewing *2`, `mtg.txt:96836` `type=Legendary Creature`).
And **s34-s40 is the X-menu pair that matters** - see the rungs below.

## L1 - v152 (dead turn 23, -5 to 25) and L2 - v162 (dead turn 11, -1 to 15)
v152: Path to Exile on the turn-6 Blastminer, three creatures answered one at a time, and from
turn 12 an empty board with no threat left. Two breaks: **s11** Spark Spray at the face at
opponent 20, paying 1 life through a Talisman (D55-5); **s50** Molten Rain at **7 life against a
SIX-land board** with `Cast nothing right now` on the same menu, under a plan that wrote the
reason out loud - "Land count is 6, which is >4, so land destruction is generally low value, but
this is the only playable card" (D55-6). Also **s18**, a Lay Waste cycled at three lands (D55-4).
Neither break changed the result; the deck was out of threats by turn 14.
The good news in the same game is the corpus's best render result: **s46** is the X menu with the
marker on it, taken. See below.
v162: Ob Nixilis + Underworld Dreams + Fate Unraveler. 29 decisions, Siege-Gang t10 for the only
attack, and **0 draws or cycles taken under the `DRAW PUNISHERS` line across 10 renders** - fifth
corpus at zero.

## Rungs, with denominators (deck130)
- **X menu**: **3 menus, 16 rows, and the marker rendered on ALL THREE** (wave 54: 0 renders in 42
  logs). Two marker strings exist: `[<- most kills at any affordable X that costs you nothing -
  at X=N that is THEIRS: ...; YOURS: none]` (2 renders) and the new `[<- best trade: the most of
  THEIRS at the smallest cost to YOURS - at X=N that is THEIRS: ...; YOURS: ...]` (1 render).
  **Marked row taken 2 of 3.** The miss is `130v146` s40: answered `X = 3 {kills THEIRS: Triumphant
  Adventurer; YOURS: none}` with the marker on `X = 1 {same kills as X=3, for 2 less mana}` two
  rows below. **0 takes of a `kills THEIRS: none` row** (1 offered) - **P9 PASSES**. **0 takes of
  the TOP of a collapsed run** - **P10 PASSES**, both takes were the bottom row. D55-1.
- **Face damage (#2 step 4)**: **6 face casts.** Four are the licensed lethal chains (`130v125`
  s47/s51/s60 closing from 9, `130v126` s48 at 9 with Rorix's 6 already declared, `130v146` s89 at
  1). **2 breaks above the 6-life floor**: `130v152` s11 (opp 20) and `130v125` s32 (opp 15), both
  Spark Sprays with no opposing creature on the menu. Wave 54: 1. **P12 FAILS 2.**
- **Land plan**: **19 land-destruction casts** (incl. 2 Blastminer activations). Against a line
  printing 5+ lands: **2** (`130v126` s32 at 5, `130v152` s50 at 6) - **P15 PASSES on both
  clauses** (0 casts at an opponent inside the damage in hand). Below the 8-life floor: **1**
  (`130v152` s50 at 7).
- **Cycling (#1 rule)**: **11 cycles, 4 wrong**, all Lay Waste: `130v126` s22 and s23 (back to
  back at five lands), `130v146` s28 and `130v152` s18 (three lands). Wave 54: 13 cycles, 0 wrong.
  **This is the only rung that lost ground this corpus.** D55-4.
- **#0 draw punishers**: 10 engine renders, **0 draws or cycles taken under them** - **P14 PASSES,
  fifth corpus.** (I re-ran this: my first predicate counted four "draws" that were the word
  "Draw a card" inside a row's own `{card text:}`, not draws taken.)
- **SURVIVES rows**: 1 take on a `- SURVIVES` damage row (`130v146` s17). Wave 54: 1.
- **Talisman life**: `{paying this costs you N life ... you would be at K}` 4 takes, none at 3 life
  or below - **PASS, fifth corpus**. `[second copy:` Talisman rows taken 4, all correct
  (`mtg.txt:117529`, three separate `{T}:Add` lines - Talismans stack).
- **Legend rule**: 1 row, 1 take, **wrong** - new rung, D55-3.
- **Land drops**: **44 asks, a land played at 44. PASS 44/44.**
- **Attackers**: 18 windows, 0 breaks. `no attackers` twice against Perimeter Captains
  (`130v126` s29/s35) and the Blastminer sent only into empty or `(neither dies)` boards
  (`130v123` s131/s138, `130v126` s13). **Blockers**: 2 windows, both correct (`130v152` s33 gang-
  blocked an Intrepid Adversary with three Goblins and killed it; `130v146` s32 a Goblin chump).
- **Race check**: 5 lone-Rorix windows, sent 5/5, no flying blocker on any - **no race arose;
  UNTESTED, seventh corpus.**

---

# ROTATION EVIDENCE (both seats, all six opponents)
| matchup | deck123 | what decided it | deck130 | what decided it |
|---|---|---|---|---|
| 123 vs 130 | **L** t36 -3/20 | mana stripped to two lands; no creature ever cast | **W** t36 20/-3 | LD on curve, then Blastminer + Rorix; never lost a life point |
| vs 125 | L t39 0/32 | counters + Staff of Nin drain; 2 dead casts in 108 dead menus | **W** t16 19/-2 | Siege-Gang t10, four attack windows, a clean closing race |
| vs 126 | L t13 -1/41 | Sanguine Bond + Exquisite Blood; 26 asks inside the drain | **W** t22 20/0 | WALL CHECK held: `no attackers` twice, then Rorix over the top |
| vs 146 | **W** t10 12/-2 | mull to six, Doomsayer + Alarm, 23 Humans | **W** t30 14/0 | a 30-turn grind won on Rorix; three rung breaks inside a win |
| vs 152 | L t35 -2/20 | **the Damnation that swept its own five-creature board** | L t23 -5/25 | Path to Exile on the Blastminer; empty board from t12 |
| vs 162 | L t14 0/20 | Ob Nixilis + Underworld Dreams; 15 asks in one Draw step | L t11 -1/15 | outraced before Siege-Gang could block |
deck130 went **2-4 -> 4-2** and deck123 **2-4 -> 1-5**. I am not attributing either to a guide
edit (the carry forbids it and the hands are real), but the mechanism of deck130's two flipped
games is legible and is not variance-shaped: v126 was lost 0/44 last corpus to a wall-and-converter
board and won 20/0 this corpus by answering `no attackers` on that exact board twice, which is the
rung wave 54 wrote; and v125 was won on a lethal chain the deck declined last corpus.
deck123's five losses are four matchup ceilings plus one game it threw at s104.

---

# PREDICTION ADJUDICATION

## Set A - the wave-54 guide edits, at these seats
| edit / prediction | verdict | evidence |
|---|---|---|
| **P1** (A54-7, cast list relabelled CL0-CL8): `named_row_reask` + `named_row_not_offered` 8 -> 0 | **PASS on the labels, FAIL on the total: 8 -> 2.** Zero replies named a CL entry number this corpus. Both survivors name a card or an ability that is not on the window at all: `CHOICE: 2 (Cast Thraben Doomsayer)` on a three-row menu of Greaves/pass/hold, and `CHOICE: 2 (Create human with Thraben Doomsayer)` on a CASTING menu | `123v130` s9; `123v126` s25 |
| **P2** (A54-1): casts off a menu where EVERY cast row is priced dead, 5 -> 0 | **FAIL 2 of 108 such menus** (wave 54: 5). A large improvement and not a closure; G1 (the ask for a menu-level dead verdict) is still the lever | `123v125` s83, s87 |
| **P3** (A54-2): `[second copy:` Intruder Alarm takes 3 -> 0 | **PASS 0** - but only **2** Alarm rows were offered (wave 54: 20). Thin denominator; the sibling Talisman-shaped rows were taken 4 times, all correct | - |
| **P4** (A54-3 + G2): `plan_choice_conflict` re-asks returning the identical reply, 4 -> 0; total 5 -> at most 1 | **FAIL on both counts, and the shape changed.** 6 firings at my seats (deck123 4, deck130 2). Two carry an EMPTY `chosen_text` with a live `choice` - the re-ask machinery is now executing a row the record does not name | `123v126` s42, s69; `123v146` s16; `123v152` s56; `130v123` s109, s122 |
| **P5** (A54-0): the declined-note N at the first HOLD take of a turn falls below 3 in half the multi-window turns; identical-declined runs of 3+ below 30 % | **PASS on both.** Identical-declined runs of 3+ at deck123 **27.5 %** (wave 54: 43.7 %), at deck130 **2.7 %**; combined **15.0 %** over all windows and **52.4 %** over opponent-turn windows only (see #208 note below). Max run **12** (wave 54: 50) | corpus-wide re-derivation |
| **P6** (A54-5, Branch B2): 0 attacks while both halves of the loop are live and any untapped body can gain them life | **PASS 0, and for the right reason** - `123v126` s53 answered `no attackers` on 37 creatures with the render's loop clause printed, and the guide no longer prints arithmetic that says otherwise | `123v126` s53 |
| **P7** (A54-6 / A54-R6): edict casts at `they control 0 creatures` 1 -> 0; Greaves casts at `of which 0 are creatures` 1 -> 0 | **SPLIT. (a) PASS 0 of 171 rows.** **(b) FAIL 1 of 3 casts**, third corpus | (b) `123v130` s11 |
| **P8** (engine-conditional, E1): decisions inside a mandatory life-loss loop 29 -> under 5 per loop | **FAIL - 26 in one Upkeep step and 15 in one Draw step.** The de-dup KEY is fixed (the declined-note now counts unbroken to 27, which it could not do in wave 54); the RE-OPEN predicate is not. E1 below | `123v126` s57-s87; `123v162` s36-s50 |
| **P9** (D54-1): takes of a `kills THEIRS: none` X row while another affordable row names an opponent creature, 1 -> 0 | **PASS 0** | - |
| **P10** (D54-2): takes of the TOP of a collapsed `same kills as X=N` run, 1 -> 0 | **PASS 0 of 2 collapsed-run takes** (both took the bottom row); fourth corpus, first clean one | `130v146` s36, `130v152` s46 |
| **P11** (engine-conditional, E3/G6): marker renders on X menus 0 of 2 -> N/N, and the marked row taken N/N | **SPLIT, and the render half is the corpus's best result. Renders 3 of 3 - PASS.** Marked row taken **2 of 3 - FAIL**, and the guide's own "the marker appeared ZERO times, do not wait for it" sentence is what licensed the miss. D55-1 | `130v152` s46, `130v146` s36 (taken); `130v146` s40 (missed) |
| **P12** (D54-3): face damage at opponent life 7+ outside a lethal chain, 1 -> 0 | **FAIL 2** (wave 54: 1, wave 53: 4, wave 52: 6). Both are Spark Sprays whose only opponent-side row was the player - a case the guide priced as a hold and not as a cycle. D55-5 | `130v152` s11, `130v125` s32 |
| **P13** (D54-4): attackers answers sending a lone body whose own tag reads `(your attacker dies, their blocker lives)`, 1 -> 0 | **PASS 0 at deck130** (18 windows). It moved to deck123 instead - `123v146` s22 sent a lone Doomsayer into two `(both die)` blockers | `123v146` s22 |
| **P14**: draws/cycles under `DRAW PUNISHERS`, 0 fifth corpus | **PASS 0 of 10 renders** | - |
| **P15**: land destruction against 5+ lands below 3 of 22; 0 casts at an opponent inside the damage in hand | **PASS on both** - 2 of 19, and 0 | `130v126` s32, `130v152` s50 |
| **boundary B8 part 1** (deck130 `would not cover` at 2+ lands) | **TESTED AT LAST, and it PASSES** - the branch rendered for the first time in five corpora, at ONE land, shipped, and the game was won | `130v123` s1 |
| **boundary B8 part 2** (deck123's completed hands) | **PASS with one miss** - 9 of 10 looks agree with the header rule; `123v152` s1 shipped a one-lander whose coverage line named a card | `123v152` s1 |

## Set B - the wave-55 step-1 lanes, by the emitter's actual string at these seats
| lane | verdict |
|---|---|
| **A** (D2/D4/D11/D19/D21) | **PASS on every string; SPLIT on the economy, and the split is now precisely located.** Row text is exactly `Hold priority for the rest of this turn: pass now, and do not ask me again unless the board changes (any change re-opens this window; you give up no cast) {taking this row skips the rest of this turn's identical windows}`: **655 renders, 0 of the wave-54 string, and it is now offered on the pilot's OWN turn (69 own-turn takes at my seats)**. Takes **170 (26.0 %)**. `pass_hold_ambiguous` **0** at my seats (wave 54: 4 false positives) - the reword worked. `hold_windows_skipped` non-zero on 10 of 12 gameends, sum 414. The option-SET key is demonstrably live: the declined-note now counts unbroken to **27** across a run whose per-row life annotations move every iteration, which is exactly what defeated the wave-54 key. **Takes in declined-note N>=3 windows: 48 of 144 = 33.3 % at my seats - the >= 20 % target is MET here**, and the corpus's 8.9 % is carried by the other seats: I re-derived the corpus figure (2,352 renders / 282 takes / 753 N>=3 windows / 67 takes) and **48 of those 67 takes are mine**. Identical-declined runs: **15.0 % of all my decisions, max run 12** - max-run target MET, share target missed. **The one FAIL is D2's contract**: 79 of my 170 HOLD takes were followed by at least one byte-identical re-ask in the same turn, 221 re-asks in total = 25.3 % of my decisions. E1. |
| **B** (D3/D13/D14) | **UNTESTED on its own subject.** `all_assignments_illegal` **0** at my seats (wave 54: 1), so the `[RE-ASK]`-follower and `_exhausted` predictions have no window; `_exhausted` **0 renders**. The menace header `N of the M attackers need TWO or more blockers each ...` **0 renders**, and `[menace` **0 renders** - no menace attacker reached either seat in 6 blockers windows. What the lane's machinery did do: `[RE-ASK]` appears in **8 prompts** across 5 games and the two `named_row_reask` fallbacks both refused a card that was not on the menu rather than casting a neighbour - **0 wrong casts from a named-row reply**, third corpus. `latched_row_mismatch` **0**, `stale_echo` **0**. |
| **C** (D6/D7/D10/D15/D16) | **MIXED - the best lane and the worst finding are both here.** **X markers: 3 of 3 menus (PASS)**, both strings verbatim, the mark on the BOTTOM row of a collapsed run in both collapsed cases. **Both-player edict rows with a one-sided verdict: 0/N** - 53 rows carry `YOU control N creatures - targeting yourself sacrifices one of them, your choice, and you gain its toughness` and 82 carry `targeting yourself does nothing`, none one-sided (wave 54: 178/180) - **PASS**. The commit clause `{this row does not pick a target yet - taking it asks you next which of the N legal targets above}` **219 renders** - **PASS**. Single-target `{kills:` lists now carry the ownership split: `{kills whichever you target: THEIRS - ...; YOURS - ...}` **71 renders** - wave-54's **E6/G8 is CLOSED**. `[NAMED BY THEIR` **0 renders - UNTESTED** (no opponent named a card at my seats). **AND the lane shipped a false number**: every ability row aimed at the opponent prints a life total computed from a constant 166. E2 (HIGH). |
| **D** (D8/D18/D22/D9) | **PASS on the collapse, FAIL on the discard annotations.** D8's duplicate collapse **SHIPPED and works**: my one library-reveal window renders 47 array options as ranged rows - `38-40. Damnation (copies 1-3 of 3 in this list) ... x3` - in a **9,073-character** prompt; wave-54's E8 (a 50-row uncollapsed Tutor menu) is **CLOSED**. Cleanup discard menus collapse the same way. D18's decode line `The same 4 options as N-M, with <source> #2 as the source instead of #1` renders in **62 prompts**; I could not build a clean residual-`#N` predicate that separates a SOURCE ordinal from a TARGET ordinal, so I am reporting the decode line's presence and **not** a residual count. **D22 FAILS at my seats: of 146 rendered discard rows across 19 windows, only 21 carry an annotation** (`{dead right now: 0 legal targets on the board for it}` 9 prompts, `{spare: you control N lands}` 6; `{you already control one}` **0 renders**). The gaps are systematic, not random - see E4. `chooseaname` / `{visible now:}` / `You named X with ...`: **0 renders - UNTESTED**, no such card at either seat. |
| **E** (D5/D23/D12) | **PASS on what arose; the mulligan clause UNTESTED.** D23: `130v125` s25 is a `kind: wall_miss` record (`latency_ms` 900,030, `choice: -1`, `fallback: wall_miss_unrecorded`, `options: 0`) and its gameend carries `wall_miss_events: 1` / `wall_miss_unrecorded: 1` - the record and the gameend fields both ship. My other 11 gameends read 0/0. `stale_livelock` **0**; `reveal_stall` **0 records** - my one reveal window resolved in 46.7 s, so I cannot say whether the field is a stamp or a park. D12's mulligan colour clause: `on turn one` and `even with every land in this hand in play` **0 renders each**; the `would not cover any spell in it` branch rendered **2**, both in the count-based form `(1 land is not enough for your cheapest spell at mana value 2)`. **The colour-family clause is UNTESTED at both seats** - and note this is now a *different* string from the one wave 54 saw (`no spell in it is castable off {W}{U} alone`), so wave-54's 4/4 PASS does not carry forward. |
| **R** (D1, the stall floor) | **DISCHARGED at my seats, and E2 of wave 54 is CLOSED.** Across my eleven stderr files: `releasing so the game advances` **0** (wave 54: 33/30/44/64 per game), `giving this decision to the heuristic` **0**, `dropping stale async answer` **34** (wave 54: 281). The residual 34 are the seam class the carry names, and the stderr pins its shape - see E1's second half. |
| **Q / P (autotap, purge)** | **No drift I can attribute.** `- Paid {2}{w} for Idyllic Tutor with Scrubland; Swamp; Underground Sea` receipts render throughout; the fit clause `{leaves N sources - no other row on this menu needs more than N}` renders in 69 prompts / 157 rows and was taken 11 times; 0 windows where a row I judged affordable was absent. I did not read `WAGIC_OPT_SOURCES_CAP` behaviour directly. |

## THREE-NUMBER LITERAL AUDIT (#188) - counted from the RENDERED `prompt` for headers and hints, and from `options_text` / `chosen_text` for row literals, never from the guide text inside the prompt
| literal (lanes A-E) | old-string renders | new-string renders | takes under the new string |
|---|---|---|---|
| HOLD row: `Pass priority, and do not ask me again this turn unless the board changes ...` -> `Hold priority for the rest of this turn: pass now, and do not ask me again unless the board changes (any change re-opens this window; you give up no cast) {taking this row skips the rest of this turn's identical windows}` | **0** | **655** (deck123 337, deck130 318); own-turn renders present, 69 own-turn takes | **170** (deck123 106, deck130 64) |
| `[you declined this exact list N times already this turn]` | n/a | **277** prompts; max N reached **27** | 0 decline -> cast flips after it |
| `[<- most kills at any affordable X that costs you nothing - at X=N that is THEIRS: ...; YOURS: none]` | **0 in wave 54's 42 logs** | **2** | **1** |
| `[<- best trade: the most of THEIRS at the smallest cost to YOURS - at X=N that is ...]` | n/a (new) | **1** | **1** |
| `{X pricing: same kills as X=N, for K less mana}` | n/a | **6** rows | **2**, both the BOTTOM row of the run |
| `{kills whichever you target: THEIRS - ...; YOURS - ...}` | 0 (wave 54 printed one unmarked list) | **71** | **18** |
| `{kills: <name>}` (single-target, unconditional) | n/a | **7** | **4** |
| `{this row does not pick a target yet - taking it asks you next which of the N legal targets above}` | 0 | **219** | **14** |
| `they control N creatures - <name> is sacrificed` / `- they choose which one` / `- at 0 this does nothing`, WITH the `YOU control N creatures - targeting yourself ...` half | 0 one-sided | **135** (53 + 82) | **5**, all at N=1 |
| ability row aimed at a player: `{right now: takes N damage - they would be at K}` | 0 (wave 54: 73 rows with no life total at all) | **194 rows** at my seats, **251 corpus-wide** | **6** takes. **EVERY opponent-aimed row's K is wrong** - see E2 |
| `{paying this costs you N life (Talisman of Impulse #1: 1 damage) - you would be at K}` | n/a | 12 | **4**, none at 3 life or below |
| fetch row `... and it adds {W} or {B} (you cannot make {W} right now)` | 0 (wave 54: no colour at all - G4) | **60** rows | **7** |
| `{dead right now: 0 legal targets on the board for it}` (discard) | 0 | **9** prompts / 21 rows of 146 | 3 |
| `{spare: you control N lands}` (discard) | 0 | **6** prompts | 2 |
| `{you already control one}` (discard) | 0 | **0** | - (**UNTESTED**) |
| `(copies 1-3 of 3 in this list) ... x3` (reveal/discard collapse) | 0 | **62** prompts | n/a |
| `The same 4 options as N-M, with <source> #2 as the source instead of #1` | 0 | **62** prompts | n/a |
| `N of the M attackers need TWO or more blockers each; you have K blockers ...` | 0 | **0** | - (**UNTESTED**, no menace attacker) |
| `[NAMED BY THEIR <src>: ... you would be at K]` | 0 | **0** | - (**UNTESTED**) |
| `chooseaname` header / `{visible now:}` / `You named X with ...` | 0 | **0** / **0** / **0** | - (**UNTESTED**) |
| `on turn one` / `even with every land in this hand in play` (mulligan colour clause) | 0 | **0** / **0** | - (**UNTESTED**) |
| `[legendary: you already control <name> - legend rule: casting this sends one copy to your graveyard (you choose which)]` | n/a (new to me) | **1** | **1 - and it was wrong** |
| `[repeat: you control M creatures; activated this turn N times already. This turn will not advance while you keep taking this option; you have taken it K times in a row with no other action in between.]` | n/a | **23** | **9** |
| `Cast nothing right now (combat comes next this turn)` | n/a | **184** | **68** |
| `while ahead on LIFE` | **0** | **0** | - (the falsehood stays gone) |
| `{modes live right now: ...}` / `CHOOSE A CARD TO PUT ONTO THE BATTLEFIELD` / `FORCED SACRIFICE` / `this KILLS you` | 0 | **0** each | - (**UNTESTED**) |

---

# ENGINE / RENDER ITEMS (ranked)

**E1 (HIGH, window lifecycle). The HOLD row's promise is not kept: a life total moving between
windows re-opens the byte-identical option set, and the row is offered again in the same phase.**
Repro, smallest first: **`123v125` seq 104-109** (turn 35, Upkeep). s104 takes the HOLD row; s105
through s109 re-ask the SAME four options - the option-set key matches (the declined-note counts
2, 3, 4, 5 across them) - while the only change on the board is `Your life:` 6 -> 5 -> 4 and
`Opponent life:` 27 -> 32 from a Staff of Nin trigger and an Elixir activation.
Larger: **`123v126` seq 57-87** (26 asks in one Upkeep, HOLD taken at s57, s61, s71, s72, s81-s85,
declined-note reaching 27) and **`123v162` seq 36-50** (15 asks in one Draw step, HOLD taken at
s36 and s42).
The stderr names the mechanism in the emitter's own words (`game-125v123-1788439466.stderr`
around line 2571):
```
AIPlayerGPT: Casting decision (Upkeep, opponent's turn): which card do you cast now, if any? -> chose 4 of 4
AIPlayerGPT: the model took the hold row at the cast seam on turn 32 - this turn's remaining cast windows are held until the board changes
Resolving Action on stack: StackAbility.  (Source: Staff of Nin)
Action added to stack: 10DrawAction
Resolving Action on stack: 10DrawAction
AIPlayerGPT: dropping stale async answer
```
The hold is armed and then immediately disarmed by a triggered ability that changes no permanent.
**Scale at my seats: 79 of 170 HOLD takes (46.5 %) were followed by at least one byte-identical
re-ask in the same turn; 221 such re-asks = 25.3 % of my 873 decisions.** This is also the residual
`dropping stale async answer` class (34 at my seats) that lane R's D1 discharge leaves behind, and
it is the whole of my remaining window tax now that the stall floor is gone.
**Ask (lane A, wave 56):** the hold's re-open predicate should be the same OPTION-SET key the
declined-note already uses, not "the board changed". If the option set the pilot held is
byte-identical, the hold stands - a life total, a stack item resolving and a card drawn are not
new information about that menu. A cheaper variant if the predicate must stay permissive: cap
re-opens within one phase step at 2 when the option set is unchanged, and stamp the cap.
**Not a guide item.** The pilot's behaviour inside these runs was correct at both seats: 0 casts
across all three repros, and it answered the hold row on the first or second window of most turns.

**E2 (HIGH, render falsehood). Every ability row aimed at the opponent prints the opponent's
post-damage life computed from a constant 166.**
Repro: **`130v146` seq 17, option 2** - `Deal 2 damage with Pyrite Spellbomb targeting the opponent
{right now: takes 2 damage - they would be at 164}` on a screen whose CURRENT SITUATION block reads
`Your life: 20 | Opponent life: 20`. Also `130v125` s4, `130v123` s6, `123v125`-family rows.
Corpus-wide the pattern is exact and total: **251 of 251** opponent-aimed ability rows print
`at 165` for 1 damage and `at 164` for 2 damage, at every real opponent life from 34 down to 1.
The **`Yourself (player, life N)` row on the same menus is CORRECT** (41 rows, K = my_life - N
every time), so the bug is confined to the opponent branch of the ability-row emitter; the SPELL
cast-row tail (`- and 1 to the opponent at life 15 leaves them at 14`) is also correct. This is
lane C's D16 fix ("ability rows pointing damage at a player without a life total 0, was 73")
landing with the wrong life source.
**Why it is HIGH and not LOW:** deck130's entire #2 rule is a comparison between `Opponent life:`
and a row's damage, and the rows this bug covers are precisely the deck's lethal-chain pieces
(Pyrite Spellbomb's sacrifice, Siege-Gang's Goblin sacrifice). A pilot reading the row would never
see a lethal face row. **123 of my 873 windows carried the false number.** No decision at my seats
was demonstrably made off it - the six face takes were all from SPELL target menus, whose
arithmetic is right - but by the trust doctrine this is a ledger item and I have written **no guide
sentence hedging against it**.

**E3 (MED, parser). `plan_choice_conflict` now fires six times at my seats and twice leaves the
executed row unnamed in the record.**
Repro: **`123v126` seq 25** - reply `CHOICE: 2 (Create human with Thraben Doomsayer)` on a CASTING
menu (rows: Devour Flesh / Cast nothing / Hold), record carries `choice: -1`, `chosen_text: ""`.
Same shape at `123v152` s56 and `123v126` s75, s125, s207 (`chosen_text` empty with a live
`choice`). Two asks are involved: the re-ask machinery is right to refuse, but a record whose
`chosen_text` is empty while `choice` is a real index cannot be adjudicated by any later reviewer -
I had to read the following record to learn what happened. **Ask:** always stamp `chosen_text`
with the row actually executed, and where nothing was executed stamp the reason string.
Guide half shipped as A55-TRIM (the cast-list warning re-aimed from CL numbers, which are fixed,
to naming an ability on a casting menu, which is what remains).

**E4 (MED, render). The Cleanup-discard dead-verdict predicate is much narrower than the cast-row
one, so a discard menu shows verdicts on some dead cards and silence on others.**
Repro: **`123v125` seq 48** - rows 2-4 (`Tragic Slip x3`) correctly carry
`{dead right now: 0 legal targets on the board for it}`; on the SAME menu, with the opponent's
battlefield at zero creatures, row 1 `Tribute to Hunger` and row 7 `Tribute to Hunger (copy 2 of 2)`
carry nothing (an edict at N=0 is as dead as a Slip at 0 targets), row 8 `Idyllic Tutor` carries
nothing with an Intruder Alarm already on the battlefield, and row 6 `Damnation` carries nothing on
a board where neither player controls a creature. **21 of 146 rendered discard rows across 19 windows carry any
annotation.** `{you already control one}`: 0 renders. **Ask:** run the SAME dead-verdict family the
cast rows already compute (`at 0 this does nothing`, `destroys 0 of their creatures`, the Tutor's
three brackets, `of which 0 are creatures`) over the discard menu, so the absence of a verdict
means "live". Until then a guide cannot teach "the unannotated row is the keep", and A55-10 says
so explicitly.

**E5 (MED, render). A card castable from two zones prints two rows at two prices with identical
verdicts and no marker on the cheaper one.**
Repro: **`123v152` seq 104** - `1. Cast Damnation {2}{b}{b} {right now: destroys 1 ... 5 of yours}`
and `3. Cast Damnation {4}{b}{b} [from exile] {castable from exile ...} {right now: destroys 1 ...
5 of yours}`; the pilot answered 3. The X menu already solves exactly this problem with
`{X pricing: same kills as X=N, for K less mana}` and its bottom-of-run marker. **Ask:** when two
rows on one menu name the same card with the same `{right now: ...}` verdict, tag the dearer one
`{same effect as row N, for K more mana}` - the identical shape, and it costs one string.
(The pilot's take is a guide break too and is written as A55-1b/A55-15; the render ask stands
because the fix is one line and removes the choice entirely.)

**E6 (LOW, render). The Lightning Greaves cast row carries no `{right now: ...}` verdict.**
Repro: **`123v130` seq 11** - a three-row menu (`Cast Lightning Greaves {2}` / `Cast nothing right
now` / hold) on a battlefield line reading `of which 0 are creatures`; taken, tapping out. This is
the fifth corpus for this break and the only repeat offender at deck123 whose row has never been
priced. Every other class in this deck's menu has one: Damnation `destroys N ... M of yours`,
edicts `they control N creatures`, Tragic Slip `kills N of the M legal targets`, Idyllic Tutor's
three brackets. **Ask:** `{right now: you control 0 creatures - this equips nothing}` on an
equipment cast row at zero own creatures, and `{right now: <name> already wears Lightning Greaves
#1}` on the equip line. Guide half shipped as A55-2.

**E7 (LOW, harvest). `reveal_stall` cannot be adjudicated from my seats.** One reveal window in
873 decisions (`123v125` s9, resolved in 46.7 s), no `reveal_stall` field on it. Recording the
denominator so the next reviewer does not read my silence as a pass.

**Closed from wave 54:** **E2** (the 488 stale drops = stall-floor releases) - **CLOSED**, 0
releases and 34 drops across my eleven games. **E3** (the X marker never printing) - **CLOSED**,
3 of 3 menus. **E6** (single-target `{kills:}` listing the caster's own permanents unmarked) -
**CLOSED**, the ownership split `THEIRS - ...; YOURS - ...` renders on 71 rows. **E7** (fetch rows
naming no colour) - **CLOSED and improved**: 60 rows carry `and it adds {W} or {B}` plus a
`(you cannot make {W} right now)` tag. **E8** (library-reveal menus not collapsing duplicates) -
**CLOSED**, 47 array options rendered as ranged rows in a 9 KB prompt. **E9** (ability rows with no
life arithmetic) - the arithmetic ships but the number is wrong; re-opened as **E2**.
**E5** (`pass_hold_ambiguous` false positives) - **CLOSED**, 0 firings at my seats.

**Card facts checked** against `projects/mtg/bin/Res/sets/primitives/mtg.txt` with `/usr/bin/grep`:
Rorix Bladewing (96836, **`type=Legendary Creature`**, 6/5, `abilities=flying,haste`) and
`deck130.txt` `Rorix Bladewing *2` - together these are the whole basis of D55-3; Starstorm
(112835, `auto=damage:X all(creature)` + `abilities=cycling` + `autohand=__CYCLING__({3})`);
Lay Waste (66043, `target=land auto=destroy`, `autohand=__CYCLING__({2})`, mana `{3}{R}`);
Pyrite Spellbomb (90168, `{R}{S}:Damage:2 target(anytarget)` + `{1}{S}:draw:1`);
Siege-Gang Commander (105071, `_GOBLINTOKEN_*3` + `{1}{R}{S(goblin|myBattlefield)}:Damage:2`);
Hammer of Bogardan (52046, `damage:3 anytarget`, `autograveyard={2}{R}{R}{R}:moveto(hand)
myUpkeepOnly`); Fog Bank (42416, 0/2 Wall, `flying,defender`, `preventAllCombatDamage` both ways);
Fate Unraveler (39182, 3/4, `@drawfoeof(player):damage:1 opponent`); Talisman of Impulse (117529);
Sanguine Bond (99004), Exquisite Blood (37966), Intruder Alarm (58850), Thraben Doomsayer (120583),
Damnation, Tragic Slip, Tribute to Hunger, Devour Flesh, Idyllic Tutor, Vision Skeins, Lightning
Greaves, Bloodline Keeper, Marsh Flats / Tundra / Underground Sea / Scrubland, Molten Rain (75759),
Stone Rain (113726), Spark Spray (110142), Dwarven Blastminer (33251).
**No primitive/Oracle divergence found; no Scryfall fetch was needed** - every fact I relied on is
a P/T, a supertype, a subtype, a trigger condition or a script line, and the render agreed with the
primitive in every case except E2, which is an arithmetic bug and not a card-text one.
Two opponent cards I lean on live in `primitives/borderline.txt`, not `mtg.txt`, and I checked both
there: **Nadaar, Selfless Paladin** (75383, 3/3, `abilities=vigilance`) - so `130v146` s17's
`takes 2 damage - SURVIVES (toughness 3)` is TRUE and the Spellbomb was wasted; and **Triumphant
Adventurer** (120341, **1/1 `abilities=deathtouch`** plus first strike on its controller's turn) -
so `123v146` s22's `(both die)` on a 2/2 Doomsayer is TRUE, and that attack was a real risk, not a
render artefact.

---

# LATEGAME SPECIMEN CANDIDATE (invariant 0)
1. **`123v152` seq 104-113** (turns 28-32, prompts **22.3-25.5 KB**, ten records on a 20-permanent
   board). In ten records it exercises, at once: **two rows for the same card at two prices with
   identical verdicts** (s104, the E5 shape, and the strategic break of the corpus at my seats);
   the `[from exile] {castable from exile ...}` clause; the Damnation `{right now: destroys N ...,
   M of yours}` verdict at a live M of 5; the fit clause `{leaves N sources - row 3 needs more mana
   sources than the N this leaves}`; a Marsh Flats crack with the new colour tag (s110); the HOLD
   row (s114 immediately after); and finally **s113, a correct `Cast Tribute to Hunger` at 1 life
   on `Intrepid Adversary (7/5) [lifelink] is sacrificed, you gain 5`** - the right edict at the
   right N, 109 decisions deep. It is a better late specimen than a pure window-economy run because
   every one of those literals is a lane A-E product and one of them decided a game.
2. Alternate, for the window-economy lanes specifically: **`123v162` seq 36-50** - fifteen records
   of the turn-14 Draw step at 10 life down to 1, three-row menus, the HOLD row taken at s36 and
   s42 and honoured by neither, the declined-note climbing, in a form small enough to diff.

---

# FALSIFIABLE PREDICTIONS FOR WAVE 56

**deck123**
- **P1.** With A55-1b (the M-is-the-last-number stop + the repro): Damnation casts at a printed
  `M of yours` of 4 or more, **1 -> 0**. This is the prediction I care about most at this seat -
  it is the only game deck123 lost that it could have won.
- **P2.** With A55-15/A55-1b: takes of a `[from exile]` row while the same card is on the same menu
  at a lower cost, **1 -> 0**. If E5's `{same effect as row N, for K more mana}` ships as well,
  0 with a margin; without it, this is a pure guide test.
- **P3.** With A55-2: Lightning Greaves casts at `of which 0 are creatures`, **1 -> 0**, sixth
  corpus. If it breaks again on a menu where the Greaves is the ONLY cast row, the rung belongs to
  the render (E6) and I will say so and stop writing guide text for it.
- **P4.** With A55-3: edict casts on an N=1 body whose row names `[defender]` or a printed power of
  0, **1 -> 0**. Edict casts at N=0 and at `they choose which one` stay at **0** (171 and 52 rows
  offered this corpus).
- **P5.** With A55-9 (RULE -1 retuned to the record): one-land sevens whose coverage line NAMES a
  card are kept N/N. This corpus: 1 of 2. Two looks is a thin denominator; if it splits again the
  answer is not more guide text, it is that the coverage line's two branches need different words.
- **P6.** With A55-4: dead casts justified in the reply by the counter-deck bait licence, **1 -> 0**,
  and total casts off an all-dead menu **2 -> 0** of what will again be ~100 such menus.
- **P7.** With A55-6: the deck fires its token chain on the OPPONENT'S turn in at least half the
  turns where a `[cost: Tap]` line is offered on both turns. This corpus the ratio is not
  measurable from the guide's old framing, so I am fixing the denominator now: this corpus it is
  **10 own-turn chain turns (21 activations) to ONE opponent-turn chain turn (3)**, and the attack
  list never exceeded 4 rows in any game with the Alarm out except the won one.
- **P8 (engine-conditional, E1).** If the hold's re-open predicate becomes the option-set key:
  HOLD takes followed by an identical same-turn re-ask, **79 of 170 -> under 10**, and decisions
  inside a single mandatory life-loss phase, **26 -> under 5**. If E1 does NOT ship, expect these
  numbers flat and read that as confirmation that the remaining runs are engine-generated - the
  pilot spent nothing in any of them this corpus.

**deck130**
- **P9.** With D55-1: takes of a marked X row, **2 of 3 -> N/N**, and takes of an unmarked row while
  a marker is printed, **1 -> 0**. **This is the prediction I care about most at this seat** - the
  marker took four corpora to start printing and the guide spent one corpus telling the pilot to
  read past it.
- **P10.** With D55-1's WHICH-vs-WHETHER split: Starstorm casts where the taken X's `THEIRS` list
  names fewer creatures than its `YOURS` list, **1 -> 0**.
- **P11.** With D55-4 (the cycling gate re-keyed to `Mana available:`): wrong Lay Waste cycles,
  **4 -> 0**, and two cycles of the same card in one turn, **1 -> 0**.
- **P12.** With D55-5: face damage at `Opponent life:` 7 or more outside a lethal chain,
  **2 -> 0**. Five corpora at 6, 4, 1, 2 - if this reaches 0 the rung compresses to one sentence.
- **P13.** With D55-3: casts of a card whose row carries `[legendary: you already control ...]`,
  **1 -> 0**. New rung, denominator will be small (the deck runs two Rorix and one gets there
  rarely) - report it as UNTESTED if no such row renders rather than as a pass.
- **P14.** With D55-7: takes of a damage row reading `- SURVIVES`, **1 -> 0**, and Pyrite Spellbombs
  left on the battlefield across a turn where their only targets survive, N/N.
- **P15.** With D55-6: land-destruction casts at `my life` 8 or less, **1 -> 0**; at 5+ opposing
  lands, **2 of 19 -> 0 or 1**. Draws/cycles under `DRAW PUNISHERS` stay at **0**, sixth corpus -
  if that holds the rung compresses to the line plus its two thresholds.
- **P16 (engine-conditional, E2).** If the ability-row opponent-life source is fixed: rows printing
  `they would be at 164/165` at a real opponent life other than 166/167, **251 -> 0** corpus-wide,
  and the pilot reaches a lethal face row from an ABILITY menu at least once (this corpus: every
  lethal it found came from a spell target menu).

---

# WHAT I DID NOT CHECK
- **The other seat files, the corpus-wide totals I did not re-derive, and the ten games neither of
  my decks is in.** Every count above is over my own twelve seat logs unless it says
  "corpus-wide", and there are exactly three corpus-wide numbers here, each of which I recomputed
  over all 42 `.jsonl` files myself: the HOLD economy (2,352 renders / 282 takes / 753 declined-note
  N>=3 windows / 67 takes there - which matches the brief exactly), the ability-row life falsehood
  (251 of 251), and the X-marker string search. The brief's other figures (3,171 decisions, 14
  fallbacks, 22.98 h inference, max prompt 30,031) are quoted, not recomputed.
- **The engine source.** Nothing under `src/`, `JGE/` or `bin/Res` was read or written except
  `Res/sets/primitives/mtg.txt`, `Res/ai/baka/deck123.txt`, `Res/ai/baka/deck130.txt` and the two
  live guides I copied as my starting point. Every E-item is stated from emitted strings, JSONL
  fields, stderr lines and the lane notes. E2's "constant 166" is an inference from the arithmetic
  (165 = 166 - 1 and 164 = 166 - 2 at every real life total); I did not read the emitter and cannot
  say whether 166 is an uninitialised value, a wrong player object or a different life pool.
- **stderr beyond five counted strings.** I counted `dropping stale async answer`,
  `releasing so the game advances`, `giving this decision to the heuristic`,
  `chose to cast nothing` and `wall_miss` across my eleven files, and read two ~10-line windows
  around drops in one file. No invariant line, no memory counter, no ASAN output, no
  `frames`/`avg_swp` line was examined.
- **The D18 residual-`#N` count.** I could not build a predicate that separates a multi-SOURCE
  ordinal from a TARGET ordinal in a rendered row, so I report the decode line's 62 renders and no
  residual number. Someone with the emitter in front of them should do this one.
- **`reveal_stall` as a stamp-versus-park question, lane B's menace header and exhausted branch,
  `[NAMED BY THEIR`, `chooseaname`, the mulligan colour clause, `{modes live right now:}`,
  `CHOOSE A CARD TO PUT ONTO THE BATTLEFIELD` and `FORCED SACRIFICE`** - **0 renders each at my
  seats.** Reported UNTESTED, not inferred.
- **Scryfall.** No web fetch. Every card fact I relied on is a P/T, a supertype, a subtype, a
  trigger condition or a script line, and the primitive was sufficient. I did NOT verify Nadaar,
  Selfless Paladin or Triumphant Adventurer against any source - neither appears under that name in
  `mtg.txt` and I make no claim about either beyond the P/T the render printed.
- **RSS, render caches, Vita parity** - not observable from seat logs.
- **The suite, PARSETEST, and any build.** No wagic run, no build, no deploy, no git.
