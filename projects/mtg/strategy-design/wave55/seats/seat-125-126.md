# Wave-55 seat: deck125 (3/6) + deck126 (2/6)

Corpus `matchups-20260903-074354` - TWELFTH fair-hand corpus, first on wave-55 step-1 (lanes A-E on
base 8a1768d1c), binary master **b228b8648**, model qwen36-35b-a3b on Spark :8084. Guides =
wave-54 reviewer revisions (5160eb1cb) + boundary pass (dd0cd2d74). 21/21 natural.
**This is the first UNCONTAMINATED window economy at this seat since wave 53, and the first
re-read of deck125 since its 1-5 was ruled contaminated by the stall floor.**

**Volume.** deck125 **947** decisions over 6 seat logs (wave 54: 405; wave 53: 1,075), deck126
**331** (wave 54: 281) - **1,278 of the corpus's 3,171 (40%)**. deck125's opponent-turn windows went
**109 -> 444** and its own-turn windows 296 -> 497 while its turn count went 169 -> 202. deck126:
own 156 / opp 169. Largest prompt on my seats **29,808** (`125v152` seq 218, t63, Upkeep,
`priority`); longest seat log `125v126` (266 decisions, 41 turns), longest game `125v152` (65 turns,
257 decisions).

**Results.** deck125 **3W-3L** - won vs123 (t39, 32/0), vs126 (t41, 44/0), vs152 (t65, 18/-11);
lost vs130 (t16, -2/19), vs146 (t13, -1/20), vs162 (t28, 0/20). deck126 **2W-4L** - won vs123
(t13, 41/-1), vs152 (t14, 24/-2); lost vs146 (t13, -14/22), vs130 (t22, 0/20), vs162 (t16, -6/20),
vs125 (t41, 0/44). Wave 54 was deck125 1-5 / deck126 4-2. **Real hands, n=6 per deck, and the
window economy changed by a factor of four between the two corpora: no part of either swing is
attributed to guide edits, in either direction.**

**Health (12 seat logs).** Fallbacks **2 / 1,278 (0.16%)**, down from 0.29%: `stale_echo` at
`126v146` seq 12 (the reply named "Cast Chromatic Lantern" on a menu that no longer printed it -
the seat had spent to 2 mana one window earlier; correct classification, and the Wall of Omens on
that menu was not cast) and `unparsed_reply` at `125v152` seq 146 (a 400-char degenerate token
salad, followed by exactly one `kind: "recovery"` at seq 147). `plan_choice_conflict`,
`named_row_reask`, `all_assignments_illegal`, `stale_livelock`, `empty_reply`, `timeout`,
`commit_retracted`: **0**. `answer_replaced` 2. `pass_hold_ambiguous` **0** (wave 54: 4 - the
false-positive class is gone at this seat). `echo_index_conflict` / `name_over_index` /
`latched_row_mismatch` / `multi_answer_first_taken`: **0**. `latched_coded_line` **5** (all
deck126, all benign). `dropped_assignments` 0 on all 5 blockers records. Latency: deck125 p50
5.0-26.1 s, max **767.8 s**; deck126 p50 6.7-49.4 s, max **1,391.2 s** (`126v162` seq 24, the
corpus's `long_reply` + `wall_miss`, `deadline_pct` 154.5). Total inference on my seats 5.87 h.

**Card facts.** Read off `projects/mtg/bin/Res/sets/primitives/*.txt` with `/usr/bin/grep`.
**Peer into the Abyss `borderline.txt:82570-82576`** (the deck125 loss): `auto=choice
name(Target opponent) draw:halfuptype:*:opponentlibrary opponent && life:-halfupopponentlifetotal
opponent`, `text=Target player draws cards equal to half the number of cards in their library and
loses half their life. Round up each time.`, `mana={4}{B}{B}{B}`, `type=Sorcery`. **Scryfall
checked for this one card** (`api.scryfall.com/cards/named?exact=Peer+into+the+Abyss`): oracle
text, mana cost and type line are byte-identical to the primitive - **no disagreement to report**.
**Rorix Bladewing `mtg.txt:96836-96843`** `abilities=flying,haste`, 6/5 - this is why `126v130`
was offered no blockers window after t6 and it is NOT an engine gap. Howling Mine
`mtg.txt:55935-55940`, Vision Skeins `mtg.txt:129546-129550` (`auto=all(player) draw:2`, Instant),
Chromatic Lantern `mtg.txt:19630-19637`, Pride Guardian `mtg.txt:88356-88363` (`abilities=defender`,
0/3, `@combat(blocking) source(this):life:3`), Perimeter Captain `mtg.txt:85398-85405`
(`:may life:2`), Overgrown Battlement `mtg.txt:83596-83603`, Sanguine Bond `mtg.txt:99004-99008`,
Exquisite Blood `mtg.txt:37966-37970`, Staff of Nin `mtg.txt:112425-112430`, Tribute to Hunger
`mtg.txt:124073-124078` all re-read unchanged. Nothing else was cross-checked against Scryfall.

## NEW-BASELINE record (mulligans)

| seat | offers | mulls taken | kept-hand shapes (the engine's own counts) | bottoming |
|---|---|---|---|---|
| deck125 | 6 | **0** | 5L/2S, 4L/3S, 2L/5S x3 (one with `would not cover any spell in it (2 lands are not enough for your cheapest spell at mana value 3)`), and **1L/6S** (`125v123` seq 1, coverage named Elixir {1} + Path {w}) | none |
| deck126 | 6 | **0** | 3L/4S x4, 4L/3S, and **1L/6S** (`126v162` seq 1, coverage `would not cover any spell in it (you have 1 land; no spell in it is castable off {G} alone on turn one)`) | none |

Twelve offers, **zero mulligans, zero bottom asks, no chain, no mull-to-zero, no floor text.**
Both guides carry an explicit "one land: SHIP IT on a seven and on a six" branch and **both were
broken, 0 ships of 2** - deck125's keep won at t39, deck126's lost at t16. Split outcomes at n=2,
so no rule is rewritten; both branches are REORDERED so the imperative ends them instead of a win
story (deck125 edit `MULL`, deck126 edit `D-F`). **No OWNER QUESTION arises: no mulligan was taken
anywhere at this seat, so there is no repeated mull and no floor to flag.**

---

# THREE-NUMBER LITERAL AUDIT (#188) - lanes A-E literals
# Counted from the RENDERED `prompt` (row counts too), never from the `options` array.

| literal | old-string renders | new-string renders | takes |
|---|---|---|---|
| lane A hold row `Hold priority for the rest of this turn: pass now, and do not ask me again unless the board changes (...)` | **0** of the wave-54 `Pass priority, and do not ask me again this turn...`, **0** of the wave-53 `Hold priority - do not ask me again...` | **1,086 rows** (deck125 820, deck126 266); the `{taking this row skips the rest of this turn's identical windows}` tail on **1,086 of 1,086** | **87** (deck125 83, deck126 4) |
| lane A own-turn HOLD row | - | **481 of the 1,086 are own-turn windows** (deck125 377, deck126 104) - the own-turn half SHIPPED | **46 own-turn takes** (deck125 42, deck126 4) |
| `[you declined this exact list N times already this turn]` | - | **654 prompts** (deck125 483, deck126 171); **0** inside any option row, as specified | n/a |
| lane B menace header `N of the M attackers need TWO or more blockers each` / `[menace - ...]` tag | - | **0** | **UNTESTED-AT-THIS-SEAT** (no menace attacker reached either seat) |
| lane B `[RE-ASK]` | - | **0** | UNTESTED |
| lane C `{removes: <name>}` | - | **326 rows** (all deck125 Path to Exile), **0 on an INDESTRUCTIBLE target** | **7** |
| lane C `{right now: ...}` verdict | - | **711 rows** (deck125 483, deck126 228) | **63** |
| lane C `[DRAW PRICE: ... - you would be at K]` | **0** bare-price rows | **5 rows** (deck125 3, deck126 2) | **3** (K = 35, 32, and one deck126) |
| lane C `; this KILLS you` | - | **0** | **UNTESTED-AT-THIS-SEAT** |
| lane C `{kills THEIRS: / YOURS:}` split, `{kills 0 of the N legal targets}` | - | **0** | **UNTESTED-AT-THIS-SEAT** (neither deck owns a fixed-damage or -N/-N targeted spell) |
| lane C `edictSelfClause` / `{this row does not pick a target ...}` | - | **0** | **UNTESTED-AT-THIS-SEAT** (Tribute to Hunger's `target=opponent` menus never rendered a both-player row here) |
| lane C `[NAMED BY THEIR <src>: ... you would be at K]` | - | **0** | **UNTESTED**; the `[named:` battlefield tag itself rendered on **2** deck126 prompts (wave 54: 54) and **0 casts of a named card** followed - D101 is at 0 with a denominator of 2 |
| lane C X markers | - | **11 X menus, 11/11 largest-first with the header `option 1 is the LARGEST X`, 11/11 carrying `{X pricing: X=N - ...}` on every row and `X=0 - this cast does NOTHING` on the bottom row** | 9x `X = 3`, 2x `X = 2` off menus topping out at 2 - **11/11 correct** |
| lane C ranked kill markers on an X menu | - | **0** - **UNTESTED-AT-THIS-SEAT** (Sphinx's Revelation is the only X spell here and it kills nothing) |
| lane D discard `{spare: you control N lands already}` | - | **29 rows** (deck125 20, deck126 9) | **19** |
| lane D discard `{dead right now: ...}` | - | **17 rows** (all deck125 Path to Exile at 0 legal targets) | **0** |
| lane D discard `{you already control one}` | - | **0** | UNTESTED |
| lane D **bare discard rows** (no verdict of any kind) | - | **267 of 313 (85%)** - **the prediction "bare discard rows 0" FAILS**; see HIGH #2 | n/a |
| lane D collapsed `(copies k-m of n in your hand)` on discard rows | - | **18 rows** (deck125); library-reveal `(copy k of n)` collapse present in **1,103 prompts** | n/a |
| lane D multi-source decode `The same 4 options as 8-11, with <source> #2 ...` | - | **42 prompts** (deck125 37, deck126 5); residual `#N` rows in rendered menus **170** (deck125 169, deck126 1) - **above the predicted <= 12**, and every one is a legitimate instance ordinal (`Staff of Nin #1/#2`, `Island #1-#5`), not a decode failure | 16 |
| lane D `chooseaname` CARD NAME header / `{visible now:}` | - | **0** | **UNTESTED-AT-THIS-SEAT** |
| lane E `reveal_stall` / `_secs` / `_phase` | - | **5 of 5 reveal records**, `_secs` = latency to the second (24, 42, 60, 148, 474), `_phase` 0 on all five | see MED #4 - it is an unconditional duration STAMP, not a park flag |
| lane E `wall_miss` + `wall_miss_events` / `wall_miss_unrecorded` on gameend | - | **1 record** (`126v162` seq 24, `deadline_pct` 154.5) and **1 / 0** on that seat's gameend; the other 11 gameends carry 0/0 | **PASS 1/1, 0 unrecorded** |
| lane E mulligan colour clause `alone on turn one` (1 land) | - | **1 of 1** one-land coverage line | n/a |
| lane E mulligan clause `even with every land in this hand in play` (2+ lands) | - | **0** - the 2-land no-coverage hand rendered a COST clause instead: `(2 lands are not enough for your cheapest spell at mana value 3)`. Both forms are true and cause-carrying; the predicted string is not the emitted one | n/a |
| lane E `while ahead on LIFE` at `my_life <= opp_life` | - | **0** (0 renders of the clause at all) | the wave-53 falsehood stays closed |
| lane E `ability: <source>'s <effect> [from their <source>]` | bare `ability: <effect>` **0** | present on every stack line opened (`125v162` seq 183 shows 19 of 19) | n/a |
| `{paying this taps: <defender> - it cannot block on their turn}` | - | **30 rows** (all deck126 Tribute to Hunger) | **0** - D98's receipt test is **UNTESTED** this corpus |
| `[second copy: ...]` | - | **51 rows** (deck125 3 Staff/Elixir, deck126 48) | **4** (3 deck125 sanctioned Staff/Elixir, **1 deck126 Chromatic Lantern** - see deck126 STRATEGY D-2) |
| `LIFE-TO-DAMAGE CONVERTER on the battlefield` | - | **27 prompts**, all deck126 `yours`; **0** on deck125 | n/a |
| `DRAW PUNISHERS on the battlefield: theirs` + `DRAW FORECAST:` | - | **85 prompts** (deck125 65, deck126 20), the FORECAST line on 85 of 85 | n/a |
| NEW `(blocking trigger: you gain N and may gain M more)` inside the B-line parenthesis | - | **5 rows / 3 prompts** (deck126) | 4 - see deck126 STRATEGY D-3 |
| NEW `Cast <card> [from exile] {castable from exile - your card, exiled by their <src>, ...}` | - | **1 row** (`126v152` seq 16, Exquisite Blood at {6}{b}) | **1**, correct |

Battlefield header land counting: **every** battlefield line on both seats reads `... and N are
lands`. PASS. Land drop **deck125 45/45, deck126 24/24**.

---

# deck125 - Revelation Fracture (UW draw-go). 3W-3L

## Prediction set A - wave-54 guide edits (W96-W102)

**W96 (counters on draw-engine stack lines: N/N; counters at a stack line with no P/T and no
damage/draw/token/land text: 0) - PASS on the engine half, FAIL 2 on the second half.**
39 counterspell casts (Dream Fracture 16, Cancel 12, Essence Scatter 8, Fall of the Gavel 3). Every
cast at a card whose `{target text:}` contains "draws an additional card" / "draws that many" /
"draws seven" was taken (Howling Mine x2, Teferi's Puzzle Box, Dictate of Kruphix, Forced Fruition,
3x Vision Skeins) - **8/8**. The two textless-target breaks are both in `125v126`, both at **44
life** in a game won 44-0: **seq 234** a Dream Fracture at a **Chromatic Lantern** (`{target text:}`
= "Lands you control have {T}: Add one mana of any color." and nothing else) and **seq 255** a Dream
Fracture at a **Pride Guardian (creature 0/3) [defender]** - the first counter-on-a-wall in three
corpora. Neither cost the game; both are recorded as breaks and cited into the guide (edits W-E1,
W-E2) rather than excused.

**W97 (Revelation or Elixir CAST under a `DRAW PUNISHERS ... theirs` line: 0) - PASS 0/11 on the
Revelation, and the Elixir half of the prediction was OVER-BROAD.** 65 prompts carried the punisher
line; **0** of the 11 Sphinx's Revelation casts is one of them. One Elixir CAST is (`125v162`
seq 141, t23, 26 life) and it is **correct**: an Elixir cast draws nothing, so it costs nothing
under Underworld Dreams, and the row read `leaves 10 of your 11`. W97 is re-scoped for wave 56.

**W98 (Path at a candidate whose `{target text:}` is empty and printed power 0-1 with no converter:
0; Path at a repeatable-ability or [lifelink] candidate: N/N) - PASS 0/7 and PASS 7/7.**
Seven Path casts: Luminarch Aspirant (repeatable), Tovolar's Huntmaster 6/6, Brutal Cathar 2/2,
**Lair of the Hydra (creature 3/3)** - an animated land, and the power rule handled it - Dwarven
Blastminer (`{2}{R},{T}:` repeatable), Triumphant Adventurer (venture), and Master of the Feast
(5/5 [flying]) chosen over a Fog Bank (0/2) [defender] on a two-candidate target ask. **Target picks
2/2 correct.** No Path was cast at a token or a defender. Ceiling held.

**W99 (cleanup sending a counterspell while a land is on the list at 6+ lands: 0; Lightmine first at
header 0: N/N) - PASS 0 real breaks (was 3 of 4), and the Lightmine half is 4/12 with no cost.**
**35 discard asks**, 313 rows. The one predicate hit (`125v123` seq 76, 10 lands, sent Fall of the
Gavel + Plains) is a **FALSE POSITIVE**: one land on the list, two cards demanded, steps 3-6 empty,
so step 7 was legitimately reached. **wave-54 HIGH #2 is closed on the guide side.** The Lightmine
step-1 literal is 4 of 12 - and all 8 misses sent a spare land or a dead sweeper instead, so the
cost is zero; steps 1-3 are relabelled a TIER rather than given more text (edit W-F2).

**W100 (lands sent == min(lands on list, cards demanded) at 8+ lands: N/N) - PASS 27/27.**

**W101 (tap-out-gate breaks with the three exemptions written in: 0) - FAIL 2, and both are the
same shape, and both were RIGHT.** Raw 4, exempt 2 (both Elixir casts under shape (c)). The two
live breaks are `125v123` seq 49 (t19, 24 life, `Cast Staff of Nin {6} ... casting this taps you
out` with a Dream Fracture in hand) and `125v126` seq 49 (t13, 23 life, same row with an Essence
Scatter in hand). **Both are the FIRST Staff** - no Staff on the battlefield line - both resolved
untouched, both pinged on the very next upkeep, and both games were won 32-0 and 44-0. Two corpora
running, every live break of this gate has been a first Staff and none has been punished. The gate
gets shape (d) (edit W101a); the second-Staff / Elixir-activation / Lightmine cases stay gated.
Sub-predictions: `X = 3` **11/11** (9 threes plus two correct `X = 2` off menus topping out at 2);
low-X Revelation **2 of 92** (was 0/51) - FAIL, see W-A below; sweeper at header 0 **0/40**;
Lightmine Field **0/84 rows, 0 casts**; land drop **45/45**; Staff pings **54 windows, every Staff
fired every turn**; Emrakul **1 row, 1 take** (t62, no converter line, opponent on 23 - it attacked
on t63 and t65 and won the game); attackers windows **2/2** declared.

**W102 (with the stall floor fixed: `interrupt window held ... ticks` 0, stale drops < 100) - PASS,
and wave-54 HIGH #1 is CLOSED.** Across my **11 stderr files**: `interrupt window held by ... for
N ticks` **0** (was 367 releases on my games alone, every one at exactly 12,002 ticks);
`dropping stale async answer` **27** (was 367); `giving this decision to the heuristic` **0**;
`chose -1 of N` **0**; no segfault / assert / sanitizer line. deck125's opponent-turn windows per
seat-turn are back above the wave-53 rate. No guide change was needed to move this and none was
made.

## The three wins (deck125)

- **vs deck152 (t65, 18/-11) - the corpus's longest game and the deck's thesis at full length.**
  A Staff of Nin from t42 pinged them from 34 to 12 while eleven counterspells answered Sigarda,
  two Elite Spellbinders, Katilda twice, Augur of Autumn and Intrepid Adversary; four Sphinx's
  Revelations at X = 3 and an Elixir activation held the life total; a Supreme Verdict at three
  real attackers cleared the Wolves; **Emrakul was cast at t62 off a 15-mana board with no converter
  line and attacked twice** for the last 30. Eight cleanup asks, no misordered send.
- **vs deck123 (t39, 32/0)** - two Staffs (the second t25) took them 20 -> 0 across fifteen turns
  under seven counterspells. Twelve cleanups, and the two lands sent at seq 60 / 95 / 118 / 161 are
  step 2 working.
- **vs deck126 (t41, 44/0)** - the wave-54 loss of this matchup reversed. The Staff landed t13 (the
  gate break that is now shape (d)), Sanguine Bond was Cancelled on the stack at t32, and the seat
  never fell below 23.

## The three losses (deck125)

- **vs deck162 (t28, 0/20) - ONE decision, and it is the one the guide had no rule for.**
  `125v162` **seq 180** (t28, 30 life, 12 sources): the opponent cast a Howling Mine and the seat's
  menu printed exactly one counter row - `Cast Cancel {1}{u}{u} {leaves 9 of your 12 ...}`. It took
  it. In the **same phase** they cast **Peer into the Abyss**; the menu at seq 181 no longer printed
  any counter (Revelation, Path, decline, hold), the seat answered "Cast nothing right now", the
  sorcery resolved, and seq 183 shows the whole receipt: **19 draws, "You lost 15 life (now 15)",
  and 19 `ability: Underworld Dreams's deal 1 damage` lines stacked on the board**. 30 -> 0 in one
  turn. Everything before it was right: 11 counterspells including a Forced Fruition, a Teferi's
  Puzzle Box and a Dictate of Kruphix, two DRAW-PRICE takes at K = 35 and K = 32, a Path at a 5/5
  flier over a Fog Bank, 0/40 sweepers at header 0. **STRATEGY, and the guide teach is the
  LAST-COUNTER rule (edit W96).**
- **vs deck146 (t13, -1/20) - the seat declined a life spell at 10 life against eleven power,
  twice.** `125v146` seq 32 (Main 1) and **seq 36 (Blockers)**: three attackers on the opponent
  line reading 5/5 + 4/4 + 2/2 with "3 of them are attacking right now", the seat at 10, and the
  only cast row `Cast Sphinx's Revelation ... max affordable X=2`. Declined both. Eleven damage,
  final -1. **X = 2 was 12 life and the game.** The old carve-out fires at "life 5 or less" and on
  the header COUNT, so it never reached this board. **STRATEGY (edit W-A1)**, plus HIGH #1 below -
  the render prints three P/T numbers and no total.
- **vs deck130 (t16, -2/19) - NO LEVER, real hands.** The seat played Tundra on t1, Plains on t3
  and Tundra on t9 and finished the game on **three lands** with two Supreme Verdicts, two
  Revelations, a Final Judgment and an Emrakul stranded in hand. Ten decisions in sixteen turns.
  Its four counters were spent correctly (two Lay Waste, two Hammer of Bogardan at 8 and 3 life).
  Nothing here is attributable to the guide, and the mulligan (2L/5S with a cost-reason coverage
  clause) is inside the branch as written.

## Other counted behaviour (deck125)

- **Opponent-turn windows 444, real casts 41.** By phase: Main 1 **121** (38 casts), Draw 54 (0),
  Upkeep 52 (0), Combat begins 41 (2), End 37 (0), Attackers 32 (0), Combat ends 31 (0), Main 2 31
  (1), Cleanup 24 (0), Blockers 18 (0), Combat damage 3 (0). **291 windows at nine phases produced
  zero casts** - wave-53 MED #6 / wave-54 MED #3 at four times the absolute size now that the
  windows are back. Core-loop item MED #3 below.
- HOLD row **820 renders / 83 takes (10.1%)**; in windows carrying `declined this exact list N` at
  N >= 3, **18 of 318 = 5.7%**. Lane A's ">= 20%" target FAILS here.
- Consecutive runs of 3+ byte-identical declined menus at the same turn: **313 / 947 = 33.1%**
  (max run 16) counting all windows; **296 / 450 = 65.8%** counting opponent-turn windows only.
- `mana_only_windows_skipped` 0 in all six games; `identical_option_asks_resolved` **0**;
  `hold_windows_skipped` 84 / 95 / 63 / 22 / 0 / 0, sum **264**.

---

# deck126 - Sanguine Blood (WGB wall prison). 2W-4L

## Prediction set A - wave-54 guide edits (D100-D103)

**D100 (`Cast Sanguine Bond` / `Cast Exquisite Blood` for a MISSING name at `casting this taps you
out`, declined: 0) - PASS 0/5, and entry 1 is 9 takes of 9 windows.** Nineteen Bond/Blood rows: 8
second-copy, 11 entry-1. The two entry-1 rows not taken (`126v123` seq 7, `126v130` seq 14) are the
OTHER half of a menu that printed BOTH halves at the same "taps you out" price, and one of the two
was taken in each - so per WINDOW entry 1 is 9/9 and the wave-54 finding is closed. What those two
windows exposed instead is that entry 1 has no tiebreak between the halves; see STRATEGY D-1.

**D101 (casts of a card named in an opponent `[named: <name>]` tag: 0) - PASS 0/2, denominator
collapsed.** The tag rendered on **2** prompts this corpus (wave 54: 54) and no named card was cast.
Idyllic Tutor reveal picks: **5 of 5 correct** (Sanguine Bond x4, Exquisite Blood x1) - every pick
named the half missing from both the hand line and the battlefield line, and the "neither half"
looks took Sanguine Bond per Rule #3.4.

**D102 (Tribute off `at 0 this does nothing`: 0; `[second copy:` takes on a Bond, a Blood or a
Lantern: 0; dead-tutor takes: 0) - PASS 0/218, FAIL 1/40, PASS 0/18.** Two hundred and eighteen
zero-Tribute rows, **zero taken** (was 0/99). Bond/Blood second copies **0 of 8**. Dead-tutor rows
**0 of 18**; live-tutor rows **8 of 8 taken**. The single break is a **second Chromatic Lantern**
(`126v125` seq 200, t38, 3 life) off a menu whose only other Cast row was a dead Tribute - the
window Rule #2's absolute told it to cast something on. See STRATEGY D-2.

**D103 (`BLOCKS: none` with a blocker on the B-line: 0; "concede": 0; land drop N/N;
`{paying this taps:` receipts) - PASS 0/5, PASS 0, PASS 24/24, UNTESTED.** Five blockers windows,
every one answered with real pairings, `dropped_assignments` 0 on all five. No reply contains
"concede". Thirty `{paying this taps:` rows, **all thirty Tribute to Hunger, all thirty declined**
- so there is no receipt to compare and D98's forecast-vs-receipt test has **no window** this
corpus. Five Tribute casts, five correct (a named Dwarven Blastminer, a named Master of the Feast
(5/5 flying), a 7-creature "they choose which one" that WON the game on the spot, a named Shield
Sphere that should have been held - see D-5 - and one more).

## The new deck126 findings

**STRATEGY D-1 - entry 1 has no tiebreak, and the wrong half was taken in the one window that
offered both.** `126v130` seq 14 (t11, 23 life): both halves missing, both rows printed at "casting
this taps you out", Exquisite Blood taken. Sanguine Bond landed t13 - and Rule #3 already says the
Bond alone is a clock while the Blood alone does nothing you can start. From t13 the pair sat out
for **nine turns** with no Tribute, no Staff (the Staff of Nin in hand needed {6} and the seat never
had more than 5 sources), no Sorin, no Vampire, against a 6/5 [flying] Rorix that sixteen defenders
cannot block. 23 -> 0. Edit D-A.

**STRATEGY D-2 - Rule #2's absolute contradicts Rule #4 and entry 7, and the pilot resolved it
correctly twice and wrongly once.** `126v125` seq 169 (t34, 7 life) and seq 185 (t36, 5 life): both
enchantments out, opponent at 44 with zero creatures, and the ONLY Cast rows were a Tribute at "at
0 this does nothing" and a `[second copy: you already control Chromatic Lantern` row. The seat
declined - right, and against the letter of "while both names are on your battlefield line, 'Cast
nothing right now' is NEVER your answer to a menu that prints any Cast row at all". At seq 200 it
took the Lantern. Edit D-B writes the two exclusions in.

**STRATEGY D-3 - the new `(blocking trigger: you gain N ...)` clause renders INSIDE the
"your blocker dies" parenthesis, and STEP 1 lost its first blocker to it.** `126v146` seq 6 (t5,
20 life, no enchantment out): a lone Triumphant Adventurer (1/1) [first strike, deathtouch]; both
B-lines read `may block A1 (your blocker dies, attacker lives (blocking trigger: you gain 3 and may
gain 2 more))`. STEP 1 benches both. The seat blocked with both, banked 7 life, and Pride Guardian
died. At seq 16 (t11) **one** blocker faced five attackers for 15, and the seat lost -14 on t13.
The clause is true and it is the exact pull STEP 1 exists to resist. Edit D-C. **This is the render
working as designed** - it is a strategy item, not an engine item.

**STRATEGY D-4 - the Lantern carve-out fired 5 of 12 and the two misses are the two that lost
tempo.** Twelve menus matched entry 7's "combo piece on the hand line AND `Mana available:` 4 or
less" shape. Five takes; five declines for an entry ABOVE the Lantern (4 live Idyllic Tutors, 1
Rule-7b Tribute) which are **correct**; two declines for a WALL. The live one is `126v146` seq 11
(t8, 4 sources, Sanguine Bond on the hand line, `Cast Chromatic Lantern {3}` printed beside
`Cast Overgrown Battlement {1}{g}`): the Battlement was cast, the Bond became castable on t12, and
the game ended -14 on t13. Edit D-D. (`126v162` seq 26 is the other, at **2 life**, where the wall
is defensible.)

**STRATEGY D-5 - the defender-victim Tribute stop went 0 for 1.** `126v162` seq 8 (t9, 20 life):
`{right now: they control 1 creature - Shield Sphere (0/6) [defender] is sacrificed, you gain 6}`,
cast. The second Tribute then correctly ate a Master of the Feast (5/5 [flying]) at t13, and there
was no third. Edit D-E adds the cite; no new rule.

## The four losses (deck126)

- **vs deck146 (t13, -14/22) - the tempo game, and both levers are guide items.** D-4 (the Lantern
  at t8) and D-3 (both walls thrown at a deathtouch 1/1 at t5) are the two decisions; the Bond
  landed t12, one turn too late, and five attackers finished it. A `stale_echo` at seq 12 also cost
  a Wall of Omens cast that turn.
- **vs deck130 (t22, 0/20) - D-1, then no lever.** Pair assembled t13; from t13 the seat's only
  starters were unavailable and Rorix flies. Its play from t15 on (Wall of Omens, two Perimeter
  Captains, then hold) is Rule #2's ordering executed correctly on menus that offered nothing else.
- **vs deck162 (t16, -6/20) - NO LEVER.** Teferi's Puzzle Box + Ob Nixilis, the Hate-Twisted: at
  t15 the seat's own draw step bottomed its whole hand, redrew ten cards and took **fourteen Ob
  Nixilis pings in one uninterrupted block**, 16 -> 2, with no window offered between them. The
  one-land mulligan keep (D-F) is the only guide-side item and it is n=1.
- **vs deck125 (t41, 0/44) - a structural matchup loss.** deck125's two Staffs pinged 20 -> 0 over
  26 turns from a board with **zero creatures**, so Tribute was dead all game, no block was ever
  possible, and Exquisite Blood - which needs the OPPONENT to lose life - was a blank. Bond arrived
  t32 at 9 life. 224 decisions, 162 of them opponent-turn windows, **zero casts** in any of them.

## Other counted behaviour (deck126)

- **Opponent-turn windows 169, casts 0.** Upkeep 49, Main 1 20, Main 2 14, Cleanup 14, Draw 13,
  Combat begins 13, Attackers 13, Combat ends 13, End 13, Blockers 7. A deck with no instant but
  Tribute to Hunger is offered 169 windows it can almost never use.
- HOLD row **266 renders / 4 takes (1.5%)**; in declined-note N>=3 windows **1 of 141**.
- Identical-declined runs: **165 / 331 = 49.8%** all windows (max run 15); **160 / 175 = 91.4%**
  opponent-turn windows only.
- `mana_only_windows_skipped` **499** (`126v125`) and **45** (`126v152`), 0 elsewhere - the
  Battlement-tap suppression is doing real work. `identical_option_asks_resolved` **0** on all six.
- Attackers windows **0** (no Vampire was ever made); `variable_output` Battlement line on 224
  prompts.

---

# Prediction set B - engine lanes (where my seats had windows)

| lane | verdict | evidence |
|---|---|---|
| **R** (D1, stall floor) | **PASS - and it is the wave's biggest single change at this seat** | `interrupt window held ... ticks` **0 / 11 stderr files** (was 367 on the same eleven matchups); `dropping stale async answer` **27** (was 367); `giving this decision to the heuristic` **0**. deck125 decisions 405 -> 947, opponent-turn windows 109 -> 444. The RESIDUAL 27 are the seam class lane E predicted, and the stderr context pins the shape: **15 follow a `Resolving Action on stack: StackAbility.`, 11 follow a `Resolving Action on stack: 10DrawAction` (a Staff of Nin upkeep draw), 6 follow a land-drop ask (`Target: <land>` / `Cost: NULL`)**. It is a RESOLUTION-BOUNDARY seam, not specifically the land-drop/cast alternation - name it that way for wave 56's lane A. |
| **A** (D2/D4/D11/D19/D21) | **SPLIT: PASS on the row, the keys, the own-turn offer and `pass_hold_ambiguous`; FAIL on the take rate, the run share and the life-loss loop** | New row string **1,086/1,086** with its skip-note tail, old strings **0**; own-turn windows now carry it (**481 of 1,086**) and 46 of the 87 takes are own-turn - lane A's own-turn half SHIPPED. `pass_hold_ambiguous` **0** (was 4, all false positives). `hold_windows_skipped > 0` on **6 of the 7** gameends carrying a take; the seventh (`126v125`, 1 take, 0 skipped) is **verified not a bug** - the board changed between seq 88 and seq 89 (the Idyllic Tutor the seat had just cast was Cancelled, and the Lantern row reappeared), so the hold legitimately re-opened with nothing skipped. FAILS: takes **87 / 1,086 = 8.0%**; in declined-note N>=3 windows **19 of 459 = 4.1%** against ">= 20%"; identical-declined runs **37.4%** of all my decisions against "< 12%" (max run 16, which DOES meet "< 20"); and **decisions inside one mandatory life-loss loop phase = 16**, against "< 5" - see HIGH #3. |
| **B** (D3/D13/D14) | **UNTESTED-AT-THIS-SEAT** | `all_assignments_illegal` **0/0**, `[RE-ASK]` **0**, `_exhausted` **0**, menace header and `[menace - ...]` tag **0 renders** (no menace attacker reached either seat). `latched_row_mismatch` **0**; `long_reply` fired **once** (`126v162` seq 24, `deadline_pct` 154.5) and carried its retry - the wave-54 UNTESTED half is now **PASS 1/1**. |
| **C** (D6/D7/D10/D15/D16) | **PASS on every branch with a window; four branches have none** | `{removes:}` **326/326**, 0 on an indestructible target. `{right now:}` **711 rows**, none missing on a cast row. `[DRAW PRICE:` **5 of 5** carry `- you would be at K`; **0 bare-price rows**. X menus **11/11** largest-first with a per-row pricing clause and the X=0 no-op marker. **No window**: `; this KILLS you`, `{kills THEIRS/YOURS}` splits, `edictSelfClause`, `[NAMED BY THEIR ...]`, ranked X kill markers. Ability rows pointing damage at a player without a life total: **0 of 258 Staff-ping rows** - every one reads `{right now: takes 1 damage - they would be at N}`. |
| **D** (D8/D18/D22/D9) | **PASS on collapse; the `#N` residual and the discard-verdict predictions both FAIL, and the second is HIGH #2** | `(copy k of n)` / `(copies k-m of n)` collapse present on **1,103** prompts and on 18 discard rows - the 254-row / 36,274-char waste of wave 54 is gone. Multi-source decode lines **42 prompts**; residual `#N` rows **170** against the predicted "<= 12" - but every one I opened is a real instance ordinal (`Staff of Nin #2`, `Island #1-#5`), so this is a predicate problem, not a decode failure. `chooseaname` header, `{visible now:}`: **no window**. Discard verdicts: `{spare: you control N lands already}` **29 rows**, `{dead right now: ...}` **17 rows**, `{you already control one}` **0** - and **267 of 313 discard rows are still bare** against a predicted 0. Cleanup discards sending a counterspell past a listed spare land: **0** - **PASS** (was 3 of 4). |
| **E** (D5/D23/D12) | **PASS on the wall-miss contract; the `reveal_stall` field is a stamp, not a park flag; the 2+-land mulligan clause did not render** | `wall_miss` **1/1** on the consuming record, `wall_miss_events` **1** and `wall_miss_unrecorded` **0** on that gameend, 0/0 on the other eleven - **D23 PASS at this seat**. `stale_livelock` on a `reveal` **0/5**. `reveal_stall` is on **5 of 5** reveal records with `_secs` equal to the reply latency (24 s, 42 s, 60 s, 148 s, 474 s) - none is near the max(1800 s, 3x deadline) poll-churn floor, so **it is stamping non-parked reveals**; see MED #4. Mulligan colour clause: **1/1** one-land hands read `alone on turn one`; the 2+-land form `even with every land in this hand in play` rendered **0 times** and the 2-land no-coverage hand got a COST clause instead. Mulligans under a colour-family clause at 2+ lands: **0** - PASS. |
| **M, P, Q** | **UNTESTED-AT-THIS-SEAT** | Cache hits, the option-sources cap and the autotap search are not written to the translog and I did not instrument. No anomaly attributable to them: every `- Paid` receipt I opened names real sources, and no menu I read was truncated. |
| **G-N, O, S, T** | **UNTESTED-AT-THIS-SEAT** | No new crash/ASAN signature in 11 stderr files; Baka executed no decision for either seat (`chose -1 of N` **0**); no `may pay` prompt, no equip menu, no adventure/split/morph, no evolve/counters card. |
| **KNOWN BROKEN LEVER** | not exercised | `WAGIC_GPT_BOARDINDEX=0` was not used (it segfaults); the board-index line is therefore unfalsifiable from a seat log, as the carry-list says. |

---

# Engine / render items (ranked, with repro)

Corpus root: `~/.Wagic/ai/gpt/selfplay-runs/matchups-20260903-074354/`

**HIGH - #1: a combat window prints every attacker's power and never prints the sum, so "this
combat kills you" is a derivation the pilot has to do - and it lost a game.**
`1788439461-ai_baka_deck125-0x564a020c7f70-vs-ai_baka_deck146.jsonl` **seq 36** (t13, Blockers,
`my_life` 10, 3 rows). The opponent battlefield line reads `9 permanents listed, of which 3 are
creatures, 3 of them are attacking right now, 0 more able to attack right now, and 5 are lands`
followed by `Barrowin of Clan Undurr {2}{b}{w} (5/5) (printed 3/3) [counters: 1x +1/+1] [tapped -
attacking]; Goblin (2/2) (printed 1/1) [tapped - attacking]; ... Nadaar, Selfless Paladin {2}{w}
(4/4) [vigilance] [attacking]`. Eleven power against ten life, and **nothing in the prompt says
eleven or says the seat is dead**; the seat had no creature, so no `blockers` ask with its
"you would be at N" header was ever generated - the combat reached it only as a `priority` window
where that header does not exist. Every other price in this engine is printed and finished
(`[DRAW PRICE: ... - you would be at K]`, `{right now: destroys 3 of their creatures}`,
`{leaves N of your M}`). Ask: on ANY window during the opponent's combat, print the same device -
`INCOMING THIS COMBAT: 11 unblocked damage from 3 attackers - you would be at -1`. It is the
`[DRAW PRICE:` pattern applied to the one number that ends games, and it costs one line. Same shape
at seq 32 (the Main-1 window one decision earlier, same board, same 10 life). Guide half is edit
W-A1 - and note the guide half can only ask the pilot to add three numbers, which is exactly the
derivation #188 says to avoid. **Core-loop decision-quality, not legality.**

**HIGH - #2: lane D shipped the discard verdicts for lands and for zero-target removal only, so 85%
of discard rows are still bare - and the ask is the one wave-54 named as the seat's losing seam.**
`1788439469-ai_baka_deck125-0x56451a8fe7f0-vs-ai_baka_deck123.jsonl` **seq 95** renders the good
case: `6. Island (land) {card text: U} {spare: you control 12 lands already}` and
`1-3. Path to Exile (copies 1-3 of 3 in your hand) {w} (instant) {card text: ...} {dead right now:
0 legal targets on the board for it} x3`. On the SAME ask, `4. Dream Fracture`, `5. Essence
Scatter` and `7. Cancel` are bare. Across 35 asks and 313 rows: **29 `{spare:}`, 17 `{dead right
now:}`, 267 bare**. The dead cards this deck actually holds get nothing: a Supreme Verdict or Final
Judgment while the opponent header's second number reads 0 (`125v162` seq 64, `125v126` seq 166,
`125v152` seq 216), a Lightmine Field at the same 0 (12 asks), an Essence Scatter against a deck
whose every creature so far is a wall (`125v126` seq 147), an Emrakul below 13 lands (`125v152`
seq 104). Every one of those verdicts is a number the engine already computes for the CAST rows on
the same turn. Ask: reuse the cast-row `{right now:}` evaluator on discard rows -
`{dead right now: they control 0 creatures}` on a sweeper, `{dead right now: they control 0
creatures able to attack}` on Lightmine Field, `{dead right now: every creature they have cast is a
wall}` on an Essence Scatter. Ranked HIGH because wave-54 HIGH #2 is only half-closed: the LAND
half shipped and took the guide from 3-of-4 wrong sends to 0.

**MED - #3: 291 opponent-turn windows at nine phases produced zero casts across six deck125 games,
and 162 of 169 produced zero across six deck126 games.**
Per phase (deck125, casts in brackets): Draw 54 (0), Upkeep 52 (0), Combat begins 41 (2), End 37
(0), Attackers 32 (0), Combat ends 31 (0), Main 2 31 (1), Cleanup 24 (0), Blockers 18 (0), Combat
damage 3 (0). Four corpora at zero for End / Cleanup / Combat-ends. The HOLD row is the partial
answer and it is being taken 8.0% of the time; the identical-declined run share at opponent-turn
windows is **65.8% (deck125) and 91.4% (deck126)**. Repro for the pure shape:
`1788439451-ai_baka_deck126-...-vs-ai_baka_deck130.jsonl` is 26 decisions in 22 turns, and
`1788439475-ai_baka_deck126-0x556d88ee7260-vs-ai_baka_deck125.jsonl` seq 16-27, 31-42, 46-56,
60-70, 76-86, 91-102 - six blocks of eleven to thirteen byte-identical `Cast nothing right now`
windows, one per opponent phase, on a board with nothing on the stack. Ask (unchanged from wave 53
MED #6, now with an uncontaminated denominator): an opponent-turn window whose `ON THE STACK`
section is EMPTY, at a phase where this seat has never cast, is the natural companion to
`mana_only_windows_skipped` - skip it and count it, exactly as the mana-only windows are
(`126v125` shows 499 such skips already working).

**MED - #4: `reveal_stall` is stamped on every reveal record with the reply latency, so it cannot
distinguish a park from a fast answer.** Five reveal records at my seats, five stamps:
`126v130` seq 7 (`reveal_stall` 3563 ticks, `reveal_stall_secs` **24**, `latency_ms` 23,623),
`126v162` seq 6 (6568 / **42** / 42,356), `126v146` seq 9 (9270 / **60** / 59,978), `126v152` seq 7
(23417 / **148** / 148,102), `126v162` seq 25 (87917 / **474** / 473,965). `reveal_stall_secs`
equals `latency_ms/1000` to the second in all five, and `reveal_stall_phase` is 0 in all five. The
poll-churn floor is max(1800 s, 3x deadline), so **none of these five is a park** - a 24-second
reveal carries the same field a parked one would. As an instrument it currently answers "how long
did the model take", which the record already carries. Ask: emit the field only above the floor, or
rename it `reveal_wait_secs` and add a separate boolean. (Silent-instrument rule: a field that
fires on every event cannot be used as an alarm.)

**LOW - #5 (INFO): the residual stale-drop class is a RESOLUTION boundary, not the land-drop/cast
alternation.** 27 drops across 11 stderr files; the three preceding-line shapes are `Resolving
Action on stack: StackAbility.` (15), `Resolving Action on stack: 10DrawAction` (11, every one a
Staff of Nin upkeep draw), and a land-drop ask with `Cost: NULL` (6). Repro:
`game-125v123-1788439466.stderr` lines 1188-1191 (land drop) and 2573-2576 (Staff draw). Named for
wave 56's lane A as the carry-list asks.

**LOW - #6 (INFO): the `{spare: you control N lands already}` and `(copies k-m of n in your hand)`
strings render ONLY inside the prompt; the translog `options_text` array still holds bare card
names.** Any reviewer counting lane-D literals from `options_text` will read 0 renders for both and
report a shipped feature as missing - I did, on the first pass. This is the wave-55 brief's own
warning made concrete, and it is worth a line in the harness notes rather than a code change.

---

# Rotation evidence

- **deck125 - KEEP IN ROTATION, and the wave-54 1-5 is now known to have been the stall floor.**
  Same guide, same model, uncontaminated windows: **1-5 -> 3-3**, decisions 405 -> 947,
  opponent-turn windows 109 -> 444. Ceilings held or improved almost everywhere: land drop 45/45,
  sweeper at header 0 **0/40**, Lightmine **0/84**, X = 3 **11/11**, Path **7/7 with 2/2 target
  picks**, counters at draw engines **8/8**, cleanup land rule **27/27** with **0** real W99 breaks
  (was 3 of 4), Emrakul **1/1**. Four open items, all narrow: the LAST-COUNTER rule (new, and it is
  the `vs162` loss), the lethal-combat carve-out (new, and it is the `vs146` loss), low-X Revelation
  **2 of 92**, and two textless-target counters at 44 life. The tap-out gate's only two live breaks
  in two corpora were both FIRST Staffs and both were right, which is why shape (d) exists.
- **deck126 - KEEP IN ROTATION, and read the 2-4 with the matchups in front of it.** Two of the four
  losses have no lever at all (`vs162`, a Puzzle-Box/Ob-Nixilis draw-step lock that took 14 points
  in one uninterrupted block; `vs125`, a creature-less opponent against which Exquisite Blood is a
  blank and Tribute is dead all game). Every wave-54 prediction that had a window PASSED and most
  went up: zero-Tribute **0/218** (was 0/99), dead tutor **0/18**, Bond/Blood second copy **0/8**,
  entry 1 **9/9 windows**, live tutor **8/8**, reveal picks **5/5**, blockers **5/5** with
  `dropped_assignments` 0, land drop 24/24. The five open items are each one clause (D-A entry-1
  tiebreak, D-B Rule #2's exclusions, D-C the new blocking-trigger string, D-D the Lantern
  carve-out, D-E the defender Tribute). This is still a guide near its ceiling; the thing that
  decides its games now is whether it draws a Tribute for the flier.

# Lategame specimen candidate

Primary, from the deck125 side of the 64-turn game as the brief asks:
`1788439453-ai_baka_deck125-0x5636d58a0090-vs-ai_baka_deck152.jsonl` **seq 218** - turn 63, Upkeep,
`kind: "priority"`, **29,808 chars, 5 rows**, latency 3,977 ms, at 22/12 with two Emrakul attacks
still to come. It is the right specimen for the size question because the 29,808 chars are almost
entirely the 63-turn GAME LOG in front of a five-row menu: the decision needs the last two turns and
the board, and it is being handed sixty. Alternates: **seq 250** (t65, Upkeep, `priority`, 29,267
chars, 9 rows - the widest menu at that size) and **seq 216** (t62, Cleanup, `discard`, 28,689
chars, 8 rows) - the latter is the ask kind HIGH #2 is about, so the size question and the
decision-quality question are the same record there.

# Falsifiable predictions for wave 56

deck125:
- **W103** - counterspell casts taken from a menu printing exactly ONE counter row, at a target
  that is neither a planeswalker, nor a creature of printed power 2+, nor a permanent with
  "At the beginning of" / "Whenever" text: **0** (was 1 of 1, and it was the `vs162` loss).
  Counters at a stack line with no P/T whose `{target text:}` reads only "Add ...": **0** (was 1).
  Counters at a `[defender]` or power-0 stack line: **0** (was 1 of 39).
- **W104** - Sphinx's Revelation declined on a window where the opponent battlefield line's
  attacking creatures' summed power is at or above the seat's printed life: **0** (was 2 of 2, and
  it was the `vs146` loss). Low-X (`max affordable X=1`/`X=2`) Revelation takes above 10 life with
  no such board: **0** (was 2 of 92).
- **W105** - Staff of Nin casts at `casting this taps you out` **with** a Staff already on the
  battlefield line: **0**. FIRST-Staff casts at that string, taken: **N/N** (2 of 2 this corpus,
  both now sanctioned). Elixir ACTIVATIONS and Lightmine Fields at "leaves 0/1" with a counter in
  hand: **0** (0 this corpus).
- **W106** - cleanup discards: land rule **N/N** at 8+ lands (27/27); counterspell sent while a land
  is unsent on the list **0** (0); a LONE dead sweeper sent ahead of a counterspell when the
  opponent header reads 0 and every creature they have cast is a wall: **N/N** (4 of 4, now
  sanctioned); a lone sweeper sent against a deck that HAS cast a real creature: **0**.
- **W107** - X = 3 **N/N** (11/11); sweeper at header 0 **0** (0/40); Lightmine below 3 attackers
  **0** (0/84); land drop **N/N** (45/45); Path at an empty-`{target text:}` power-0/1 candidate
  **0** (0/7).

deck126:
- **D105** - a menu printing BOTH missing halves: `Cast Sanguine Bond` taken: **N/N** (was 0 of 1).
  Entry-1 windows overall **N/N** (9/9).
- **D106** - `Cast nothing right now` answered while both enchantment names are on the battlefield
  line and a LIVE cast row (not a zero-Tribute, not a `[second copy:` row) is printed: **0**.
  `[second copy:` takes of any card: **0** (was 1 of 51, a Chromatic Lantern at 3 life).
- **D107** - blockers assigned to an attacker whose every B-line parenthesis reads "your blocker
  dies", with no enchantment out and the seat above 8 life: **0** (was 2 blockers in 1 of 5
  windows). `BLOCKS: none` with a usable blocker: **0** (0/5).
- **D108** - a wall taken over `Cast Chromatic Lantern` when a combo piece is on the hand line and
  `Mana available:` reads 4 or less: **0** (was 2 of 12, 1 live). Lantern takes in that shape:
  **N/N** (5 of 12, the other 5 correctly outranked).
- **D109** - Tribute to Hunger cast at a row naming a `[defender]` victim above 10 life with no
  enchantment pair out: **0** (was 1 of 1). Zero-Tribute takes **0** (0/218); dead-tutor takes
  **0** (0/18); reveal picks **N/N** (5/5); land drop **N/N** (24/24).

Both seats:
- **W108/D110** - one-land sevens SHIPPED: **N/N** (was 0 of 2, one per deck). This is the only
  mulligan number either guide now moves, and it is deliberately small.
- **W109/D111** - engine-side, no guide change can move these: discard rows carrying no verdict of
  any kind **0** (was 267 of 313); a window during the opponent's combat with no printed incoming-
  damage total **0** (was every one); opponent-turn windows with an empty `ON THE STACK` at End,
  Cleanup, Combat begins or Combat ends **0 casts and 0 asks** (was 133 asks, 0 casts);
  `reveal_stall` present on a reveal answered inside the poll-churn floor **0** (was 5 of 5).

# What I did NOT check

- I did not run wagic, did not touch `bin/Res`, `src/` or `JGE/`, did not run git, and did not
  deploy. All guide work is under `wave55/`. I did not read any engine source this wave - every
  emitter string above is quoted from a rendered `prompt`, and I did NOT confirm which C++ function
  emits any of them, so the lane attributions in set B are from the carry-list, not from code.
- **Scryfall: ONE card cross-checked** (Peer into the Abyss, which the `vs162` loss turns on).
  Every other card fact is a primitive's `auto=`/`text=`/`type=`/`abilities=` line or an
  engine-rendered string, and no primitive/Scryfall disagreement is reported either way because
  none was looked for.
- I grepped my 11 stderr files for the release/drop/heuristic/crash strings and read
  `game-125v123-1788439466.stderr` around its nine stale drops. I did NOT read any stderr in full,
  so lane M's cache lines, lane G/I signatures and any new warning class are unverified beyond
  those greps.
- I did not measure lane M's cache hits, lane P's purge, lane Q's autotap search, lane I's RSS or
  lane J/N at all - none is observable from a translog and I did not instrument.
- Colour-pip auditing: I read `Paid ...` receipts for the two Staff casts and the Battlement
  clauses only. I did not audit pip-by-pip source choice anywhere.
- I audited deck126's five blocker replies against their A/B lines; deck125 has no creatures except
  the one Emrakul and I audited neither seat's opponent's blocking.
- The 499 and 45 `mana_only_windows_skipped` are read off the `gameend` records; I did not confirm
  that none of them reached the model.
- I did not open every one of the 1,278 records. The counted claims above come from scripted passes
  over all 12 seat logs; the narrative claims come from records I opened individually, and every
  one of those carries its seq.
