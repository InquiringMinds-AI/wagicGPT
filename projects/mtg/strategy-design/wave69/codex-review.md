> Seat: FABLE (owner-named adversarial fallback; the Codex pool was exhausted at 6:03 PM reset). Same contract as the Astra reviews.

# Wave-69 step-1 adversarial review (Fable seat, standing in for Codex)

Diff: master 169ee41f3..HEAD (`~/.gatelogs/w69-step1.diff`). Read: lane-brief, lane-BF/BG/BH/BI, the full diff, and HEAD source around every hunk cited. Line numbers are HEAD `projects/mtg/src/AIPlayerGPT.cpp` unless noted.

## Findings

**F1 — HIGH — merge duplicate: `{paying this taps: ...}` prints TWICE on every row it applies to.** `AIPlayerGPT.cpp:31076-31082`. BG's hunk kept the pre-existing `out << paymentTapsClause(taps, tapRestrict);` (31076) as context; BI's hunk added `paidTapsClause = paymentTapsClause(...); out << paidTapsClause;` (31081-31082) after it. Trigger: any cast row the pool cannot afford whose auto-tap picks include a creature or the source itself (lands-only prints an empty clause, so it hides there). Every such row now carries the guide-keyed clause twice, which falsifies lane BI's own prediction 10 ("no row carries two tap clauses") and re-bloats the rows BD-6 was shrinking. Invisible to PARSETEST: it lives in `describeAction`.

**F2 — HIGH — K6a states a FALSE cover: the seat's untapped creatures are counted as certain crack-back blockers in the seat's OWN main phase 1, before it attacks.** `crackBackCoverFacts` 23179-23215; clause at 23011-23025 ("You ALSO already control N untapped creatures that can block on their turn without casting anything"). Caller is the cast-row render in `FindCardToPlay` (37795-37812), which runs in main 1 and main 2. A body untapped now that attacks this turn is tapped through the opponent's turn; a must-attack body cannot be held back. On the LETHAL screen (the only place the tag renders) the row now reads "you cover 13 of 13 ... you would be at 12" while the same turn's plan is to swing with those bodies. The comment's justification (untap in the seat's untap step) is true and irrelevant to attacking.

**F3 — MED — K4b's refill clause asserts "+1 this card" for cards that EXILE themselves.** `abilityShufflesGraveyardIntoLibrary` 28180-28204 gates on an `AAShuffle` in the chain plus the lowercased literals `moveto(mylibrary)` and `all(*|mygraveyard)` anywhere in the script. Feldon's Cane (`{T}{E}:moveto(myLibrary) all(*|myGraveyard) && shuffle`, mtg.txt:39576) and Campfire's `{2}{T}{E}` line (borderline.txt:15766) match and print `shuffles your N-card graveyard and this card back ... from L to L+N+1`; the card goes to exile, the true count is L+N. Only Elixir carries `all(this)`; the gate should require it.

**F4 — MED — K6d's ALTERNATIVE line can name a LETHAL assignment without saying so.** `blockKeepAlternativeClause` 23472-23485 takes no life; the caller at 24629-24651 prints it whenever `keepDamage > bestCase && blockersDying > 0`, including on a `lethalScreen` (24720). Trigger: life 6, life line lets in 4 spending a body, material line lets in 7 — the row says "lets in 7 instead of 4 ... Both lines are legal" and never that 7 kills you. (`trampleOverflow` is the already-blocked excess, constant across assignments — fine.)

**F5 — MED — composition BG x BH: after K1 the game continues with `library 0` and 41 parked cards, so every K4 surface lies for the rest of the game instead of the game ending.** `MTGGameZones.cpp:407-423` returns one parked card per draw; the stranded Idyllic Tutor payload (BG: unfixed) leaves `library->nb_cards == 0`. `xLibraryCeilingX`/`xLibraryReserveWhy`, `Your library: 0 cards`, K4b's `from 0 to N` and every deck-out warning read that count. Trigger: the corpus route exactly. Not a regression against base (base lost the game), but it must be named in the ledger so the next corpus's `library 0` lines are not filed as new bugs. Second answer to "what else parks cards there": `MTGScryCards::initDisplay` (AllAbilities.cpp:1728-1742) parks scry cards from the library; a scry that empties the library followed by a draw in the same resolution now draws the scry's top card instead of decking — rules-correct, LOW.

**F6 — MED-LOW — K3's row-naming negation is bag-of-words within a sentence and fires on approving prose.** `proseNegatesTakenRow` 27854-27922: any `kNegations` cue followed by the short name anywhere later in the same sentence, over the whole reply including the PLAN body. Row `Cast Damnation`: "You do not need to hold back, Damnation clears the board" and "I cannot let their board grow, so Damnation now" both fire, each buying a `plan_choice_conflict` re-ask. One round trip per board, second answer executes, nothing removed — cost, not doctrine. The deferral filter looks only 60 bytes past the cue.

**F7 — LOW — K5(c) "0 mana available" ignores floating mana.** 31079-31080; `potentialColorReach` (ManaEngine.cpp:1210) counts untapped producers only. Trigger: 1 mana floating after a ritual, cost 3, no untapped sources.

**F8 — LOW — K4c x K5(a): the priced tapped-animation row now also earns the no-op re-ask** (`rowSaysNoOp` 31869 matches "does nothing"); BH's ~14-asks estimate did not count BG's rows. Same class: the `_exhausted` stamps at 35043/36003 now fire when the second answer legitimately re-takes the row it was told it could re-take, so `plan_contradicts_noop_row_exhausted` no longer means what it says.

## Checked and found sound

- (a) `drawFromLibrary`: the reveal takes the library TOP first (AllAbilities.cpp:352), `addCard` push_backs, so `reveal->cards[0]` is the former top; the return push_backs it onto the library and the draw takes `cards[nb_cards-1]` — the right card. `previousZone` is stamped by `removeCard` (MTGGameZones.cpp:1121) on every move; hand reveals are excluded; `MTGScryCards` cleanup iterates the zone, not a private list, so a drawn card is never yanked back from hand.
- (c) `stackDrawRecipient` (4494-4505) is the same three-way mapping as `MTGAbility::getPlayerFromTarget` (MTGAbility.cpp:7927-7939) and `AALifer::resolve` (AllAbilities.cpp:7112); `who`-carrying drawers still resolve through `getTarget()` first. No wrong-controller path found.
- (d) every `mLastLatencyMs >= 0` write (14929, 15563) is paired with the same slot's `mLastHttpStatus`/`mLastCurlResult`, all consumed together at 16682-16687; the wall-miss path sets latency only with a non-empty failure list, so a success stamp is never minted from a failed trip.
- (2) the three small-seam truncation arms unwind as their seams' existing pending paths (`return 0` at 47244 vs 47290; `return 1`; `PREGAME_PENDING`); no hang shape. (4) classification order untouched. (6) `xLibraryCeilingX(12,1,8,2)==6` is the true s110 ceiling (X=7 decks at the draw step); the K8, fold-size and cap pins match the code.

## Verdict

Not safe to ship as-is. F1 is a mechanical merge defect that doubles a guide-keyed clause on the highest-traffic row family and falsifies a lane's own prediction before the corpus runs; F2 puts a false survival number on the one lethal screen the crack-back tag exists for, in the phase where the seat decides whether to attack. Both are one-line fixes (drop 31076 or 31081-31082; gate the standing-body fold on post-combat, or on bodies not being declared). F3/F4 are false or withheld facts on rarer rows and can follow; F5 is the declared, unfixed stranding wearing BH's numbers and needs a ledger line; F6-F8 are cost, within doctrine. Nothing in the diff removes, caps or auto-answers a legal option.
