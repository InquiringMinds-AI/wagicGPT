# Wave-57 seat: deck146 (2-4) + deck152 (4-2) + deck162 (3-3)

Corpus `~/.Wagic/ai/gpt/selfplay-runs/matchups-20260903-174505/`, binary master **67b2bb38a**
(wave-57 step-1 lanes A-H + S/T/U), model qwen36-35b-a3b on Spark :8084. 18 seat logs on my decks,
**807 decision records** (146: 236, 152: 391, 162: 180), **9.21 h of inference**, mean **41.1
s/decision** (146: 49.1, 152: 36.3, 162: 40.7). 1 of the corpus's 8 fallbacks (`162v130`,
`named_row_reask`), the corpus's only `recovery` on my seats (same file), 0 SEGV, 0 hangs, 21/21
natural. Fourteenth fair-hand corpus; the first carrying wave-57 lanes A-H. Guides under test =
the wave-56 reviewer revisions (1e1dcd5ab) + the wave-56 boundary pass (a93aad889). Card facts read
off `bin/Res/sets/primitives/borderline.txt` with `/usr/bin/grep`; translogs read with python;
**every count below is from the rendered `prompt`, never the `options` array**; HOLD takes are
EXACT-matched (lane A's D4 makes that legal again - see the audit).

**Records (corpus-results.tsv):**
- deck146 **2-4**: W vs125 (22/-1 T18), vs126 (22/-1 T19); L vs123 (-2/22 T9), vs130 (-2/20 T18),
  vs152 (-3/5 T23), vs162 (0/20 T19).
- deck152 **4-2**: W vs125 (19/-3 T20), vs126 (27/-1 T10), vs130 (22/-20 T26), vs146 (5/-3 T23);
  L vs123 (-81/6 T23), vs162 (0/21 T10).
- deck162 **3-3**: W vs123 (14/0 T18), vs146 (20/0 T19), vs152 (21/0 T10); L vs125 (8/30 T29),
  vs126 (-3/38 T12), vs130 (-2/16 T31).
No rate is attributed to any guide edit in either direction (new-baseline rule). deck146 went
3-3 -> 2-4, deck152 5-1 -> 4-2, deck162 5-1 -> 3-3; none of those numbers is evidence about
anything and none is used below. The admissible numbers are the window counts.

---

# HEADLINE 1 (HIGH) - THE MDFC LANE IS CLOSED ON THE ENGINE SIDE. EVERY WAVE-56 DEFECT IS GONE.

My assigned surface, re-derived from the rendered prompts, corpus-wide:

| | wave 56 | wave 57 |
|---|---|---|
| windows carrying a numbered `PLAY THIS AS A LAND` row | 62 (65 rows) | **30 (30 rows)**, all mine (146: 15, 152: 15) |
| takes / arrivals | 6 / 6 | **15 / 15** (146: 8, 152: 7) - 50% of rows taken |
| rows on the OPPONENT's turn | 18 | **0** |
| rows on an own turn outside a main phase | 13 | **0** |
| rows on a prompt reading `Land drop: ALREADY USED this turn` | 6 | **0** |
| rows on a prompt reading `Land drop: you have no land you could play right now` | 17 | **0** |
| land plays narrated inside an opponent's turn | 1 | **0** |
| `Flip Side` rows rendered to an AI seat | 109 (90 windows) | **0** - and `Flip Side` does not appear ANYWHERE in any prompt in the corpus |

**All 30 windows print `Land drop: NOT yet used this turn - you can still play a land` and
`It is your turn`.** Wave-56 predictions **P9 PASSES on all four halves with the control non-zero
(30)**, and **P10 PASSES 0 of 30**. Lane W's `restriction{assorcery}` fix and lane D's D14 are both
discharged at my seats; my wave-56 engine items **E-2, E-3 and E-4 are CLOSED**.

**D26's arrival clause is three-valued and correct on 30 of 30 rows**, verified against the scripts:
- `{taking this row then ASKS you to pay 3 life: ...}` on **6** rows - Agadeem (2), Emeria (4);
  `borderline.txt:1199 / :33980` both carry
  `auto=ability$!name(Choose one) choice name(Pay 3 life) life:-3 _ choice name(Tap) tap(noevent)`.
- `{it enters TAPPED - it makes no mana this turn unless something untaps it, and no menu follows}`
  on **1** row - Pelakka Caverns; `:82640` is a bare `auto=tap(noevent)`, and its printed text is
  "Pelakka Caverns enters tapped", matching D26's docket correction.
- **no clause on 23 rows** - the Pathway class (Grimclimb :49109, Mistgate :73167, Tidechannel
  :117883, Boulderloft :13228 - each a bare `auto={T}:add{X}`, no `tap(noevent)`, no choice).
  Correct: they enter untapped and ask nothing.
**All 4 pay-or-tap takes got their follow-up menu** (3 paid, 1 declined). Lane D's **prediction 3
PASSES 0 of 4** - no reply plan asserts an untapped arrival before the menu; one says "pay 3 life
to enter untapped if possible, otherwise tapped", which is the clause read back. **Prediction 3b
PASSES 0 of 1** on the unconditional class.

**D2 PASSES.** 6 pregame prompts corpus-wide carry `1 card is counted TWICE above, on purpose:`
and **all 6 are deck146's, which is every deck146 pregame prompt holding a land-backed MDFC**
(`146v123` s2/s3 Pelakka Predation, `146v126` s1 Agadeem's Awakening, `146v130` s1 Agadeem's
Awakening, `146v152` s1 Emeria's Call, `146v162` s1 Pelakka Predation). Headers omitting a
`forcetype(land)` card: **0 of 6** (wave 56: 4 of 4). The `Mana sources` line now carries the back
face's colour too (`146v123` s2 reads `{W} 2, {B} 1`; the `{B} 1` IS Pelakka Caverns). My wave-56
**E-1 is CLOSED and P1 PASSES on all three halves**. deck152's Pathway hands correctly carry NO
note - both faces are already lands, so nothing is double-counted (see HEADLINE 4 for what that
costs instead).

---

# HEADLINE 2 (HIGH, deck146) - BOTH OF deck146's -2 LOSSES ARE MDFC LIFE PAYMENTS, AND ONE IS A SUICIDE

The render lane closed; the deck's own use of it did not. deck146 paid 3 life to an MDFC arrival in
**exactly the two games it lost at -2, and in neither game it won**:

| repro | life before | what it paid for | result |
|---|---|---|---|
| **`146v130` seq 20** | **`Your life: 1`** | Main phase 2, own turn 19, played Emeria and answered `pay 3 life - Emeria, Shattered Skyclave enters UNTAPPED`. The plan reads "Survive this turn by casting Kaya the Inexorable using the mana from the newly untapped Emeria" - Kaya costs {3}{b}{w} against a printed `Mana available: 1 total` | **gameend `my_life: -2` in the same window. The seat killed itself; the opponent did nothing** |
| `146v123` seq 8 -> seq 9 | 20, turn 7, Thraben Doomsayer already on their battlefield line | one turn of tempo: a {b}{w} Triumphant Adventurer it could not otherwise cast | Turn 8: Intruder Alarm + Doomsayer made **19 Human tokens**; the swing took it 17 -> **-2**. At 20 it would have been at **1** and still playing |

Classify: **STRATEGY**, both. Every number the decision needed was on the surface - `Your life: 1`
two lines above the menu, `Mana available: 1 total` beside it, the row's own
`{taking this row then ASKS you to pay 3 life ...}` warning one window earlier. There is a real
render half too and it is E-1 below: **the engine renders a lethality verdict on every other
menu it prints (`they would be at K`, `best case with every blocker assigned`) and none on the one
row in the game that can kill you outright.** Guide half shipped as **146-A** (a default of "tap",
a two-clause release, and a hard floor at 3 life).

---

# HEADLINE 3 (HIGH, deck146) - THE `counted TWICE` NOTE IS PREGAME-ONLY, AND THE HAND-CULLING WINDOWS DO NOT REPEAT IT

D2 fixed the mulligan surface. It did not fix the three other windows that throw cards away.

- **`146v130` seq 7**, cleanup discard, turn 9. Battlefield line: `1 permanent listed ... 1 is a
  land` (a Swamp). Hand: five {w}-pip spells. Row 8 reads
  `Emeria's Call {4}{w}{w}{w} (sorcery) {card text: Create two 4/4 white Angel Warrior ...}` -
  **no land word anywhere on the row**. The seat answered `PUT: 8`. It was still on one land at
  turn 19; that is the game it then ended by paying 3 life at 1 life. This is wave-56's **P2
  FAILING 1 of N** and it is the same class as wave 56's break.
- **`146v123` seq 3**, bottom ask (keeping 6). The note IS present here (bottom is a pregame
  surface) and the header reads `{W} 2, {B} 1` - the `{B} 1` is Pelakka Caverns. The seat still
  answered `PUT: 2` = Pelakka Predation, bottoming the hand's only printed black source with a
  **duplicate Nadaar on the same list**, while keeping Triumphant Adventurer {b}{w} and
  Silverquill Command {2}{b}{w}. (The two Brightclimb Pathways later made the black instead, so it
  was not fatal - but the reasoning was.)

Guide halves **146-B** (the cleanup order was self-contradicting - it listed Emeria's Call and
Agadeem's Awakening as rung-1 sends and then excluded them in the same sentence; that ambiguity is
the shape of the `146v130` s7 answer) and **146-C** (the row will not tell you; recognise the three
names yourself at every culling window). Engine half: **E-2**.

---

# HEADLINE 4 (MED, deck152) - THE N>=2 DECLINED NOTE COUNTS MENUS, NOT BOARDS, AND deck152's WHOLE HOLD LANE IS A NULL

Wave-56's P6 asked for deck152's `Cast nothing right now` answers under an N>=2 declined note to
fall below 15 and its HOLD takes to reach 25%. Measured (HOLD row present, N parsed from the
rendered note):

| | N>=2 windows | `Cast nothing right now` | HOLD taken |
|---|---:|---:|---:|
| deck146 | 8 | 4 | 0 |
| **deck152** | **73** | **66 (90%)** | **7 (9.6%)** |
| deck162 | 5 | 3 | 0 |

**P6 FAILS on both halves** (was 32 of 35 / 5.7%). But the key is measuring the wrong thing, and
this is the finding: I diffed the `--- CURRENT SITUATION ---` block of each of those 66 windows
against the window before it. **0 of 66 were unchanged.** The stack was resolving under the seat,
life was moving, a permanent had arrived - in every single one. HOLD's own row says "any change
re-opens this window", so taking it would have bought **nothing**, and the 66 declines cost nothing
either. Corpus-wide the note fires on **801** windows at N>=2 while only **163** windows in the
whole corpus (56 at my seats, 39 of them deck152's) have a situation block identical to the window
before - the note over-selects by about 5x, and **0 prompts anywhere in the corpus are fully
byte-identical to their predecessor**.

So I am not sharpening 152-B, I am **re-keying it**: HOLD stays as a free tiebreaker (it is weakly
dominant once you have decided to pass), the false cost claim ("you will be answering it again
inside the same turn") and the stale scoreboard come out, and the guide now says plainly that the
N line counts identical OPTION LISTS and never means "nothing is happening". Engine half: **E-3**.

---

# HEADLINE 5 (MED, deck162) - THE ZERO-CONVERTER BRAKE IS BEING RESTATED AND VIOLATED IN THE SAME REPLY

deck162's brake fired **5 times** this corpus (wave 56: 1), every one in `162v125`, the long game
it lost, and every one inside the rule's own exclusion:

- `162v125` s40 and s41, turn 21: **Dictate of Kruphix** at `converters on your battlefield: 0`,
  hand = 2 Dictates, 2 Swamps, Master of the Feast, Underground Sea - no punisher name.
- `162v125` s57 (turn 26), s64 and s65 (turn 28): **Master of the Feast** at `converters: 0`,
  opponent battlefield line reading `of which 0 are creatures` at all three - the exact condition
  the guide's Master exception excludes.

s57's own reply reads: *"With no converters on the battlefield, we cannot safely stack more draw
engines ... We must wait for a converter"* - and its CHOICE is a draw engine. The rule is known,
restated, and not bound to the row. Opponent went 15 -> 25 -> 30 life over those turns; the seat
lost at 8. Wave-56 **P8 FAILS 5 of N** (control: no reply this corpus names Forced Fruition as a
reason to feed at zero converters - that half PASSES 0 of N). Guide half **162-A** converts the
brake from prose into a last-pass check on the row's own text before the answer is sent.
Wave-56 **P7 FAILS 1 of N** (`162v152` s4, declining a Fog Bank that was the only non-pass row at
turn 4, in a game it won) - handled with one clause on the Fog Bank rule, **162-B**, not a new key.

---

## FAIR-HAND BASELINE - every mulligan and bottom on my seats

| seat | look | engine count / note | decision | verdict |
|---|---|---|---|---|
| 146v123 | 7 | `1 land, 6 spells`, `would not cover any spell in it`, no MDFC on the hand line | **MULL** | right - the guide's own one-land rung |
| 146v123 | 7 (keeping 6) | `3 lands, 5 spells` + **counted TWICE (Pelakka Predation)**, `{W} 2, {B} 1` | KEEP | right |
| 146v123 | bottom 1 | same | **PUT: Pelakka Predation** | **wrong - bottomed the hand's only {B} source with a duplicate Nadaar on the list - HEADLINE 3** |
| 146v125 | 7 | 3 lands, 4 spells | KEEP | right |
| 146v126 | 7 | `4 lands, 4 spells` + counted TWICE (Agadeem's Awakening) | KEEP | right |
| 146v130 | 7 | `2 lands, 6 spells` + counted TWICE (Agadeem's Awakening) | KEEP | right (wave 56 mulliganed this class on a false count) |
| 146v152 | 7 | `3 lands, 5 spells` + counted TWICE (Emeria's Call) | KEEP | right |
| 146v162 | 7 | `3 lands, 5 spells` + counted TWICE (Pelakka Predation) | KEEP | right |
| 152v123 / v125 / v126 / v130 / v146 / v162 | 7 | 4 / 4 / 4 / 4 / 3 / 5 lands | KEEP x6 | right; **4 of the 6 hold a Pathway whose second colour the header does not print** - HEADLINE 4's sibling, 152-C |
| 162v123 / v125 / v130 / v146 / v152 | 7 | 2 / 4 / 2 / 3 / 2 lands | KEEP x5 | right |
| 162v126 | 7 | `2 lands, 5 spells` | **MULL** | inside the guide's rung |
| 162v126 | 7 (keeping 6) | 3 lands, 4 spells | KEEP; bottom **Forced Fruition** | right - the priciest non-converter |

**deck146: 1 mulligan (correct), 1 bottom (wrong). deck152: 0 of 6. deck162: 1 mulligan
(correct), 1 bottom (correct).** No mull-to-zero, no chain past (keeping 6), **no repeated mulligan
against a guide floor - the OWNER-QUESTION trigger did not fire.** Land drops: 0
`Play no land right now` answers at any of my seats.

---

## deck146 - Orzhov dungeon midrange (2-4), per game

- **v123 (L -2/22 T9)**: 13 decisions. HEADLINE 2's second case and HEADLINE 3's bottom. Thraben
  Doomsayer + Intruder Alarm made 19 tokens on turn 8 against one blocker; the 3 life paid on
  turn 7 is the whole margin.
- **v125 (W 22/-1 T18)**: 51 decisions. Clean. 1 MDFC window (Grimclimb Pathway, declined - it had
  a printed land to play), 0 life paid.
- **v126 (W 22/-1 T19)**: 45 decisions. Carries the deck's 6 `{X pricing: max affordable X=N}`
  renders (Agadeem's Awakening as a CAST, not the ANNOUNCE_X menu) - **0 takes, all correct**: at
  s25 the row printed `{right now: returns NOTHING - your graveyard has no creature cards ...}`
  and the seat cast Silverquill Command instead.
- **v130 (L -2/20 T18)**: **the window, twice.** 20 decisions; seq 7 is HEADLINE 3's discard and
  seq 20 is HEADLINE 2's suicide. Also the deck's cleanest MDFC positive: seq 3, turn 3, took
  Agadeem and paid 3 at 20 life to cast a two-drop - which is what rule 146-A's release clause is
  written to keep legal.
- **v152 (L -3/5 T23)**: 72 decisions, my largest deck146 seat and the source of my largest prompt
  (26,939 chars, seq 71). 0 life paid; lost on board.
- **v162 (L 0/20 T19)**: 35 decisions. Took Pelakka Caverns at seq 4 (correct - the plan reads
  "to untap mana for next turn", not "usable now") and Agadeem at seq 11, **declining** the 3 life
  at 10 life under Underworld Dreams - the right answer, and the one the new rule generalises.

**Fractions (deck146).** Attack windows **24**, `ATTACK: none` 1, blocks 2. Discards **6**,
bottom 1. HOLD **101 renders / 15 exact takes (14.9%)**, N>=2 windows 8. MDFC land rows
**15 windows / 8 takes / 8 arrivals**, `Flip Side` 0. `{visible now:` 9 windows (all of the
corpus's) / 5 takes. `[legendary:` 1 / 0. `{dead right now:` 4 / 0. `{spare:` 2 / 0.
`they sacrifice ONE of these` 2 / 2 takes.

## deck152 - Bant human midrange (4-2), per game

- **v125 (W 19/-3 T20)**, **v126 (W 27/-1 T10)**, **v130 (W 22/-20 T26)**, **v146 (W 5/-3 T23)**:
  clean; 7 Pathway back-face takes across them, all on the right colour.
- **v162 (L 0/21 T10)**: no window. Fate Unraveler drained it while Teferi's Puzzle Box shuffled
  its hand away at seq 19 (five cards to library, five drawn) - the same shape that beat deck146
  in wave 56. 20 decisions.
- **v123 (L -81/6 T23)**: **146 decisions, the corpus's largest seat, and the corpus's most
  extreme board.** deck123 assembled **Bloodline Keeper + Intruder Alarm** and at seq 126 the
  opponent battlefield line reads *"42 permanents listed, of which 32 are creatures, 32 of them
  without a restriction against attacking"* - 31 Vampire tokens made in one activation chain on
  the opponent's turn 22 (`- Opponent used: Create vampire with Bloodline Keeper (x31)`). The seat
  got **no window during the chain, correctly**: it was the opponent's turn and its hand held only
  sorcery-speed cards, so the engine had nothing legal to offer. Its one answer, Fateful Absence,
  had been bottomed by a reveal at seq 124. The one classifiable break is the reply at seq 126,
  whose plan says *"Attack with Packleader and Sigarda to deal lethal"* against 32 untapped
  blockers - a lethal claim the printed board refutes. It did not change the outcome. **This game
  also holds 36 of the deck's 66 N>=2 `Cast nothing` answers (HEADLINE 4).**

**Fractions (deck152).** Attack windows **26**, blocks **7**, reveals **7** (0 `reveal_stall`),
discards 0. HOLD **226 renders / 20 exact takes (8.8%)**, N>=2 windows **73**. MDFC land rows
**15 windows / 7 takes / 7 arrivals** (Boulderloft 5, Tidechannel 2), `Flip Side` 0.
`[legendary:` 11 rows / 2 takes (both correct - a legend-rule replacement on a healthy copy is
what the guide forbids and neither take was one). `[NAMED BY THEIR` 2 renders / 0 takes.

## deck162 - UB Draw-and-Die (3-3), per game

- **v123 (W 14/0 T18)**, **v146 (W 20/0 T19)**, **v152 (W 21/0 T10)**: the engine assembled on
  curve; `v152` also carries P7's one hit (seq 4).
- **v125 (L 8/30 T29)**: **the window - HEADLINE 5.** 68 decisions, the deck's largest seat, and
  the source of my largest prompt anywhere (**30,052 chars, seq 56**). Five zero-converter feeds.
- **v126 (L -3/38 T12)**: no window. Its own Teferi's Puzzle Box + Liliana's Caress engine landed
  on turn 12 and the opponent was already at 38 life.
- **v130 (L -2/16 T31)**: no window I can call a break. Rorix Bladewing (6/5 flying) attacked from
  turn 28; Shield Sphere and Fog Bank cannot block a flier, this deck has no removal but Ob
  Nixilis's -2, and it never drew one. 12 blockers windows, all wall blocks.

**Fractions (deck162).** `{feeds:` 60 renders / **20 takes**, of which **5 at `converters: 0`
with no punisher in hand** (HEADLINE 5). `[second copy:` 29 rows / 8 takes. `{feeds:` +
`[legendary:` collisions 0. Attack windows **3**, blocks **12**, discards 5, bottom 1.
HOLD **96 renders / 4 exact takes (4.2%)**, N>=2 windows 5. Carries my one fallback
(`162v130`, `named_row_reask`) and its paired `recovery`.

---

## THREE-NUMBER LITERAL AUDIT (#188) - measured on the RENDERED `prompt`

| literal (lane) | wave-56 renders | wave-57 renders (prompts) | takes |
|---|---|---|---|
| **D** `PLAY THIS AS A LAND` numbered row | 62 windows / 65 rows | **30 corpus / 30 mine** (30 rows) | **15 mine, 15 arrivals** |
| **D** `Flip Side` row | 109 rows / 90 windows | **0 corpus** (the literal is absent from every prompt) | - |
| **D** `{taking this row then ASKS you to pay 3 life` | n/a | **6 rows, all mine** | **4 takes, 3 paid / 1 declined** |
| **D** `{it enters TAPPED - it makes no mana this turn` | n/a | **1 row, mine** | **1 take** |
| **D** lowercased MDFC script token in a GAME LOG line | 54 renders (1 take) | **422 prompts corpus / 207 mine, 684 occurrences** | - **FAIL, see E-4** |
| **D** adjacent byte-identical GAME LOG lines | 1,767 in 670 records | **0 corpus** | - |
| **D** collapsed count line (`drew N cards` / `(xN)`) | n/a | **1,271 prompts** | - |
| **E** `1 card is counted TWICE above, on purpose:` | n/a (was 4/4 wrong headers) | **6 pregame prompts, all mine = 6 of 6 MDFC-holding hands** | n/a |
| **E** `Opponent life trend:` | 1 of 137 | **3,267 of 3,267 board frames (100%)**; 228/385/172 mine | n/a |
| **E** `[<- ` cast-row marker | n/a | 518 corpus / **0 mine** | **UNTESTED at my seats** |
| **E** `{X pricing:` (ANNOUNCE_X menu, D9) | 40 rows, 0 with a fit clause | 532 corpus prompts / **6 mine, all CAST-mode advisories, no ANNOUNCE_X menu reached my seats** | 0 mine - **UNTESTED at my seats** |
| **A** HOLD row | 2,018 renders / 63 mine (PREFIX) | **2,490 corpus / 423 mine**; corpus takes **450 (18.1%) by EXACT match** | **39 mine (9.2%)**; EXACT == the brief's number, so **D4 PASSES and prefix matching is retired** |
| **A** `[you declined this exact list N times already this turn]` | 877 corpus / 90 mine | **1,154 corpus / 133 mine**; at N>=2: **801 corpus / 86 mine** | n/a |
| **B** `INCOMING THIS COMBAT` | 38 corpus / 9 mine | **112 corpus / 26 mine**; **21 of 21** opponent-combat windows with a possible attacker | n/a |
| **B** `best case with every blocker assigned` | n/a | 34 corpus / **20 mine** | n/a |
| **B** `they would be at K` / menace header / `BLOCKING THIS COMBAT` | 133 / 3 / n/a corpus, 0 mine | 168 / 0 / 6 corpus, **0 mine** | **UNTESTED at my seats** |
| **C** `{their converter:` | n/a | 2 corpus / **0 mine** | **UNTESTED at my seats** |
| **C** `[NAMED BY THEIR` | 4 corpus / 0 mine | 16 corpus / **2 mine** | 0 mine |
| **C** `[RE-ASK]` | 3 corpus / 0 mine | 6 corpus / **1 mine** | - |
| **C** `{identical verdict right now to row ` | 0 | **0 corpus** - D30 still never renders | - |
| **C** discard rows with no braced clause at all | 357 of 426 | **0 of 74 rows at my 13 discard/bottom windows** (every row carries at least `{card text:`) | - |
| `{feeds:` / `[second copy:` / `[legendary:` / `{spare:` / `{dead right now:` / `{visible now:` | 0/18/21/1/1/6 mine | **60 / 29 / 12 / 5 / 5 / 9 mine** | 20 / 8 / 2 / 0 / 0 / 5 |
| `- Paid` without a source | 0 | **0** | - |

---

## PREDICTION SET A - wave-56 guide edits on my decks

| id | deck | prediction | verdict | evidence |
|---|---|---|---|---|
| P1 (146-A/H, E-1) | 146 | pregame `counted by the engine: N lands` disagreeing with the true count: 0/N; mulligans at a corrected count >= 2: 0/N; bottom PUT naming one of the three at a corrected count <= 2: 0/N | **PASS / PASS / PASS** | 6 of 6 MDFC hands carry the note and count the land; the one mulligan (`146v123` s1) is a genuine one-lander with no MDFC on the hand line; the one bottom (`146v123` s3) is at a corrected count of 3, so it does not trip this key - it is a colour break instead and gets its own key P2 below |
| P2 (146-E) | 146 | cleanup `discard` PUT lines sending Emeria's Call / Agadeem's Awakening / Pelakka Predation at fewer than four printed lands: 0/N | **FAIL 1 of 6** | `146v130` seq 7 - Emeria's Call sent at ONE land. HEADLINE 3. Control (highest-MV non-land-backed card sent) holds 4 of the other 5 |
| P3 (146-G) | 146 | takes of a `PLAY THIS AS A LAND` row: non-zero (control); takes on a turn the seat also had a plain land: 0/N | **PASS 8 (control); FAIL 2 of 8 by the key, both CORRECT plays** | `146v123` s5 and s7 each took Grimclimb over a plain `Play Brightclimb Pathway` row - the same card, and Grimclimb's {B} was the colour the hand needed. The key does not distinguish a Pathway (whose two faces are one land drop) from a spell-backed MDFC (whose take spends a card). **Re-keyed: the key is now about spending a spell face, not about having a land** |
| P4 (146-I) | 146 | `ATTACK: none` where every A-line carries `[doesn't untap]` AND one is vigilance AND the header reads 0 untapped blockers: 0/N; attacks under the tag by a non-vigilance creature below their life with blockers up: 0/N | **PASS 0/N; PASS 0/N** | 24 attack windows, 1 `ATTACK: none`; the wave-55 shape did not recur |
| P5 (152-A) | 152 | `Flip Side` takes on a land menu: 0/N (regression detector for R-3); takes of a back-face row whose colour the battlefield already makes twice while the other face's colour is at zero: 0/N | **PASS 0 of 0 - the row no longer renders at all; PASS 0/7** | all 7 deck152 takes were the colour the board was short of |
| P6 (152-B) | 152 | `Cast nothing right now` at N>=2 with HOLD on the menu: below 15 of N; HOLD takes in those windows >= 25% | **FAIL 66 of 73; FAIL 9.6%** - **and the key is invalid**: 0 of the 66 had an unchanged situation block, so HOLD bought nothing in any of them | HEADLINE 4. Rule re-keyed, not sharpened |
| P7 (162-A) | 162 | own-turn `Cast nothing right now` on a menu whose only non-pass row prints no `{feeds:}` and no `[legendary:` / `[second copy:`: 0/N | **FAIL 1 of N** | `162v152` seq 4, turn 4: only non-pass row was `Cast Fog Bank {1}{u} ... {leaves 0 ... casting this taps you out}`, opponent had creatures. Game won. Handled as 162-B, one clause on the Fog Bank rule |
| P8 (162-B) | 162 | takes of a `{feeds:}` row at `converters on your battlefield: 0` with no punisher on the hand or battlefield line: 0/N; replies naming Forced Fruition as the reason: 0/N | **FAIL 5 of 20; PASS 0/N on the second half** | HEADLINE 5. All 5 in `162v125` |
| P9 (E-2, engine) | all | `PLAY THIS AS A LAND` rows on an opponent-turn prompt 0/N; on an own non-main prompt 0/N; on an `ALREADY USED` prompt 0/N; land plays narrated in an opponent's turn 0/N; own-main rows non-zero | **PASS / PASS / PASS / PASS / PASS (30)** | HEADLINE 1 |
| P10 (E-3, render) | all | prompts carrying BOTH `Land drop: you have no land you could play right now` and a numbered `PLAY THIS AS A LAND` row: 0/N | **PASS 0 of 30** | all 30 read `Land drop: NOT yet used this turn` |
| P11 (E-4/R-3, render) | all | `Flip Side` rows at an AI seat 0/N; `only display-toggle ... auto-passing` stderr lines 0 | **PASS 0 of 109; the stderr line is 1,309 (was 760) and that is the fix working, not failing** | see the census below |
| P12 (E-5, observability) | all | stderr naming the slot rescue's outcome > 0; `dropping stale async answer` < 40 corpus-wide | **PASS on the first half (the drop line now names its ARM); FAIL 108 corpus-wide** | classification below |

### The `only display-toggle (Flip Side) options; auto-passing` census - the brief's question answered

1,309 lines, **all 1,309 in the 11 games that carry one of my decks** (146 seats 826, 152 seats
483; 0 in any other game). Up from 760. **These are now candidate-level drops with ZERO prompt
cost and zero inference cost**: 0 `Flip Side` rows reached any rendered prompt, so every one of
the 1,309 windows was auto-passed before `describeAction` ran. Wave 56 paid 109 of them at full
price (~650 chars each, ~65 KB of prompt at 42.8 s/decision); wave 57 pays none. The line count
rose because the drop now fires on every such window instead of leaking 109 of them into prompts.
**Lane D's prediction 2 PASSES.** Not the D17 livelock either: interleaved with normal engine
output, no run anywhere approaching the docket's >100 repro.

### D5 classification - all 108 stale drops, by arm (the brief asked for this before judging slot 2)

| line | corpus | in my 18 games |
|---|---:|---:|
| `dropping stale async answer (casting arm; the question (or turn/phase) moved)` | 51 | 19 |
| `dropping stale async answer (land-drop arm; the question (or turn/phase) moved)` | **45** | 6 |
| `dropping stale async answer (casting arm; the question and board moved)` | 12 | 8 |
| **total** | **108** | **33** |

`asyncSlotDriftKind` **never appears as a literal** in any stderr file - the arm word in the
parenthesis is the only classifier that shipped. **45 of the 108 are answers that belonged to the
LAND-DROP arm and were dropped anyway**, which is the case the second slot exists to hold; the
casting arm's 63 are answers whose own question moved. Engine seat owns the verdict; I am
reporting the split, not calling the slot ineffective. **0 `slotKey` / rescue / consume lines and 0
`giving this decision to the heuristic` lines in any of my games; `abandoned_in_flight` 0 (lane U
PASSES at my seats).**

## PREDICTION SET B - wave-57 engine lanes where my seats had windows

| lane | prediction | verdict | evidence |
|---|---|---|---|
| **D (D14)** | `Flip Side` rows whose card's other face is a LAND: 0 of N (was 108); the spell-face residue is not a miss | **PASS 0, and stronger than asked: 0 rows of EITHER class** - the one spell-face row lane D deliberately kept did not arise in this corpus | corpus-wide literal census |
| **D (D14b)** | option sets built and discarded for a land-backed toggle-only priority window: 0 | **PASS** - 1,309 auto-passes, 0 rendered rows | stderr + prompt census |
| **D (D26)** | plans asserting an UNTAPPED arrival for a pay-3 back face before its menu: 0 of N; plans asserting a usable land this turn from an unconditional-tapped face: 0 of N | **PASS 0 of 4; PASS 0 of 1** | replies at `146v123` s8, `146v130` s3/s19, `146v162` s11, s4 |
| **D (D27)** | replies declining a `[NAMED BY THEIR ...]` row while reasoning the price is one-off: 0 of N | **UNTESTED at my seats** - 2 renders at deck152, 0 takes, no decline reasoning about the price | - |
| **D (D28)** | lowercased script tokens in `chosen_text` or a narration line: 0 of N | **`chosen_text` PASSES 0. NARRATION FAILS: 422 prompts / 684 occurrences corpus-wide** | E-4. `- Opponent used: grimclimb pathway with Brightclimb Pathway` (`123v146` s2 and 421 others) |
| **D (D29)** | adjacent byte-identical GAME LOG lines: 0 of N (was 1,767); records carrying a collapsed count line: > 0 | **PASS 0; PASS 1,271** | `- Opponent drew 2 cards`, `- Opponent's Vampire died ... (x4)` |
| **E (D2)** | headers omitting a `forcetype(land)` card 0 of N; MDFC-holding pregames carrying the note N of N | **PASS 0 of 6; PASS 6 of 6** | HEADLINE 1 |
| **E (D9)** | X rows with no mana-fit clause: 0 of N | **UNTESTED at my seats** - no ANNOUNCE_X menu reached deck146/152/162; the 6 `{X pricing:` renders are CAST-mode advisories | `146v126` s25/s31/s36/s39/s41/s45 |
| **E (D15)** | board frames with `Opponent life:` and no trend line: 0 of N | **PASS 0 of 3,267** | corpus-wide |
| **E (D19/D20/D23)** | fetch-colour positive form; `[<- ` marker on X cast rows | **UNTESTED at my seats** (0 renders of either) | - |
| **A (D1/D4)** | same-turn byte-identical re-asks after a HOLD take < 20; `chosen_text` == the rendered row byte-for-byte at every priority seam | **PASS - EXACT-match HOLD census reads 450/2,490 corpus-wide, identical to the harvest number**, so the wave-56 prefix workaround is retired; 0 records at my seats with `choice >= 0` and an empty `chosen_text` | corpus census |
| **A (D5)** | stale drops < 20; drops on the land arm 0 | **FAIL 108; FAIL 45** | classification table above |
| **B (D6)** | opponent-combat windows with no `INCOMING THIS COMBAT` line: 0 of N | **PASS 21 of 21 where a line was possible.** The 11 omissions at my seats are all `Combat begins` / `Attackers` windows whose opponent line reads `of which 0 are creatures, 0 of them able to attack right now` - nothing incoming, so no line is correct | `152v123` s19/s60/s61/s88/s89, `152v125` s31/s32/s63/s64/s80/s81 |
| **B (D24)** | lethal-header blockers windows requiring the seat to subtract: 0 | **PASS 0** - 20 `best case with every blocker assigned` renders at my seats, all self-contained | - |
| **B (D22/D10)** | `BLOCKING THIS COMBAT` header; B-lines nesting a gain 0 | **UNTESTED at my seats (0 renders); PASS 0** | - |
| **C (D7/D8/D30/D12)** | unpriced creature rows under their converter 0; discard rows with a computable dead verdict and none 0; `{identical verdict right now to row ` >= 1; dead-menu header | **UNTESTED (0 `{their converter:` at my seats); PASS 0 of 74 discard rows are bare; `{identical verdict` renders 0 corpus-wide - STILL NEVER FIRES; dead-menu header 0 corpus-wide** | 13 discard/bottom windows |
| **F/S/T/U** | 0 SEGV, 0 phase-stuck games, `abandoned_in_flight` 0 | **PASS on all three at my 18 seats** | 21/21 natural, no stub exit in my games |
| **H (D43 control)** | `log_window` full on 100%, `elided` 0 | **PASS** - every record at my seats carries `log_window: full`; `log_window_kind` is `unclassified` on the pregame records I sampled, which is the control arm behaving | - |
| **G (D42)** | no AI-seat prediction | **N/A - render only, human seat** | - |

---

## ENGINE / RENDER ITEMS (ranked; repro = file, seq)

**E-1 (HIGH, correctness) - the pay-3-life MDFC arrival menu carries no lethality verdict, and a
seat killed itself on it.** Repro `146v130` seq 20: prompt reads `Your life: 1`, the two-row menu
reads `1. pay 3 life - Emeria, Shattered Skyclave enters UNTAPPED [usable (tap for mana / attack)
this turn]` / `2. tap - ... enters TAPPED`, the seat answered 1 and the gameend one record later
reads `my_life: -2, won: false`. The engine already prints a lethality verdict on menus that cannot
kill you (`they would be at K` on ability rows, `- best case with every blocker assigned: you would
be at K` on blockers windows) and prints none on the one row that can. **Ask: price the payment on
its own row from the player's own life - `{you are at 1 life: paying 3 puts you at -2 and you LOSE
the game}` when life <= 3, and `{leaves you at N}` otherwise.** The same emitter already computes
`life:-3` from the script (`borderline.txt:1199`), so the number is in hand. This is the same
species as D24's blocker verdict and belongs in the same channel. Note the second half of the same
record: the plan the payment bought needed 5 mana against a printed `Mana available: 1 total`.

**E-2 (HIGH, render) - the `counted TWICE` note is pregame-only; no hand-culling window repeats the
land half.** Repro `146v130` seq 7 (cleanup discard, turn 9, one land on the battlefield line, row
8 = `Emeria's Call {4}{w}{w}{w} (sorcery) {card text: Create two 4/4 white Angel Warrior ...}` and
nothing else) and, for the contrast, `146v123` seq 3, where the same card class DOES carry the note
because a bottom ask is a pregame surface. D2's detector (`mdfcHandBackLandName()`, the `autohand=`
bucket) is already data-only and already runs before any ability registers, so it is available at
every hand-listing seam. **Ask: tag the ROW, in the braced channel, at the cleanup discard, the
forced-discard and the reveal-to-bottom menus - `{this card is also the land <back face> (taps for
{X}) - discarding it discards that land}`.** 74 rows across my 13 such windows carry a `{card text:`
clause and none carries this one.

**E-3 (MED, render) - `[you declined this exact list N times already this turn]` counts option
lists, not board states, and over-selects about 5x.** Repro `152v123` seqs 69-82, turn 15: N climbs
2 -> 13 on a 3-row menu while the stack drains from 4 items to 1, life moves 22 -> 24 and a
Luminarch Aspirant trigger resolves - i.e. every re-ask is a genuinely new decision, and the note
tells the pilot the opposite. Census: **801 corpus windows carry the note at N>=2; only 163 windows
in the entire corpus have a `--- CURRENT SITUATION ---` block identical to the window before, and
0 prompts anywhere are fully byte-identical to their predecessor.** At deck152's seats, **0 of 66**
note-carrying `Cast nothing` windows had an unchanged situation. **Ask: key the note (or add a
second clause) on the SITUATION block, not the option vector - `[you declined this exact list N
times already this turn; the board has not changed since M of them]`.** Without the second number
the note cannot distinguish a wasted window from a live one, and the guide rule built on it (152-B)
had no gain available to it in 66 of 66 windows.

**E-4 (MED, render) - D28 fixed the row head and not the narration line; the lowercased script
token still rides every later prompt.** Repro `123v146` seq 2, `- Opponent used: grimclimb pathway
with Brightclimb Pathway`; census **422 prompts / 684 occurrences corpus-wide (207 prompts mine)**,
distributed grimclimb 79, boulderloft 313, tidechannel 181, agadeem 75, pelakka caverns 22, emeria
14. `chosen_text` is clean (0 hits), so `mdfcRowHead` is doing its job at the row; the defect is at
the WRITE seam, where `- You used: <engine menu text> with <front face>` is composed from the
engine's lowercased token instead of the head the row printed. **Ask: route that narration line
through `mdfcRowHead` too, so the log and the row cannot disagree.** Lane D's own prediction 5 for
this wave is the number above.

**E-5 (MED, observability) - the second async slot leaves 45 land-arm answers on the floor.**
Repro: `game-146v126-1788475518.stderr` and 13 other files; the census is in the D5 table. The line
now names the arm, which is a real improvement over wave 56 (where `slotKey` never printed at all),
but `asyncSlotDriftKind` does not render and the outcome (fired / seam-changed / no matching slot)
is still not stated per drop. **Ask: print the slot's decision beside the arm** so wave 58 can tell
a broken predicate from a correct refusal.

**E-6 (LOW, render) - the pregame `Mana sources among those lands` line counts only the face each
Pathway is showing.** Repro `152v125` seq 1: `Mana sources among those lands, counted by the engine:
{W} 1, {G} 3` on a hand line holding `Branchloft Pathway (land: taps for {G})` and two
`Barkchannel Pathway (land: taps for {G})` - each of which can instead be played as Boulderloft
{W} / Tidechannel {U}, and deck152 played exactly those back faces **7 times** this corpus. Lane E
called the Pathway (land/land) class deliberately untouched, and no decision turned on it here
(deck152 kept all 6 hands), so this is LOW - but it is the same defect D2 just fixed one card class
over, and the fix has the same shape. **Ask: add the other face's colour to `sources[]` for a
land/land MDFC too, and say which colours are alternatives rather than additions.**

**Narrated actions with no decision record on my seats:** 0. My one fallback (`162v130`,
`named_row_reask`) carries a paired `recovery` record.

---

## LATEGAME SPECIMEN CANDIDATE

`1788475544-ai_baka_deck152-0x558761cc7f70-vs-ai_baka_deck123.jsonl`, **seqs 119-145** (turns 21-24,
146 decisions in the file, lost -81/6). It is the corpus's most extreme board render - at seq 126
the opponent battlefield line reads *"42 permanents listed, of which 32 are creatures, 32 of them
without a restriction against attacking"* after `- Opponent used: Create vampire with Bloodline
Keeper (x31)` (D29's collapse renderer doing exactly its job on a 31-run), and the seat's reply in
that window still claims lethal. It also carries the corpus's densest N>=2 stretch (36 of deck152's
66) and a gameend at -81. Prompts run 20-25 KB.

Second choice, for the SHORTEST complete exhibit: `1788475506-ai_baka_deck146-...-vs-...deck130.jsonl`
**seqs 19-21** - a priority window that takes the Emeria land row, the two-row pay-or-tap menu at
`Your life: 1`, and the gameend at -2, in 3 records and under 8 KB. It is E-1 end to end.

Largest single prompt at my seats: `162v125` seq 56, **30,052 chars** (turn 26, the 23-permanent
board).

---

## ROTATION EVIDENCE (open lanes, not the record - #167)

- **deck146 - NO EXIT, and the lane MOVED from render to judgment.** Every engine defect this
  deck's guide was written around (E-1/E-2/E-3/E-4 of wave 56) is closed: the header counts, the
  row is legal, the toggle is gone, the arrival clause is right. What is left is the deck spending
  its own life and its own lands: two -2 losses that are 3 life wide, one discard that threw away
  a land, one bottom that threw away a colour. That is a cleaner lane than the deck has ever had -
  the guide now owns a decision instead of a workaround, and P2/146-A/146-C are all decidable in
  one corpus.
- **deck152 - NO EXIT, and the HOLD lane should be CLOSED next wave.** Its one measurable open
  rule (P6) turns out to have had no gain available in 66 of 66 windows; the re-keyed rule is a
  tiebreaker and should stop generating findings. What is left is a Pathway colour surface (E-6,
  render, not guide) and a matchup (`v123`) whose loss is a two-card combo the deck drew no answer
  to. If wave 58 shows the re-keyed HOLD rule producing no misplay and E-6 unfixed with no decision
  turning on it, this deck rotates.
- **deck162 - NO EXIT, and its one lane got WORSE, which makes it decidable.** The zero-converter
  brake went 1 hit -> 5, all in one game, all inside the rule's own exclusion, with the rule quoted
  verbatim in the reply that broke it. That is not a knowledge failure and it is not a render
  failure; it is a binding failure, and 162-A now expresses the brake as a check on the chosen
  row's own text run last, before the answer is sent. If that still fires in wave 58 the honest
  conclusion is that a prose brake cannot bind this model tier and the item belongs to the core
  loop (a row-level annotation the pilot cannot answer past), not to the guide.

---

## FALSIFIABLE PREDICTIONS FOR WAVE 58

**P1 (146-A).** deck146 answers of `pay 3 life - ...` on the MDFC arrival menu while the prompt's
`Your life:` is **3 or less**: **0/N** (was 1 of 4 - and it was the loss). Answers of `pay 3 life`
where the prompt's `Mana available: N total` plus one does not pay for any card on the hand line:
**0/N** (was 1 of 4). Answers of `tap`: **must stay non-zero** (the control; was 1 of 4).

**P2 (146-C, E-2).** deck146 cleanup-discard or bottom PUT lines naming Emeria's Call, Agadeem's
Awakening or Pelakka Predation while its battlefield line prints **fewer than four lands**:
**0/N** (was 1 of 6 discards). PUT lines naming one of the three while the pregame `Mana sources`
line gave its colour a count of **1**: **0/N** (was 1 of 1, `146v123` s3).

**P3 (146-G, re-keyed).** deck146 takes of a `PLAY THIS AS A LAND` row on a **spell-backed** MDFC
(Emeria's Call, Agadeem's Awakening, Pelakka Predation) on a turn whose `Land drop:` line also
offers a printed land or a Pathway: **0/N** (was 0 of 4 - the two wave-57 hits were Pathway takes,
which spend no spell and are exempt). Pathway back-face takes: **must stay non-zero** (was 4).

**P4 (152-B, re-keyed).** deck152 replies that give "the list has not changed" as the REASON for
passing while the situation block differs from the previous window: **0/N**. deck152 HOLD takes at
N>=2: **no target** - this is now a tiebreaker, and its count is a measurement, not a score.

**P5 (152-C).** deck152 mulligan replies naming a colour it cannot make while a Pathway whose OTHER
face makes that colour is on the hand line: **0/N** (was 0 of 6 keeps - the control is that deck152
keeps everything, so a hit is a real break). Pathway back-face takes whose colour the battlefield
line already makes twice or more: **0/7** (was 0/7).

**P6 (162-A).** deck162 takes of a row printing `converters on your battlefield: 0` with no
Underworld Dreams / Fate Unraveler / Ob Nixilis on the hand or battlefield line: **0/N** (was 5 of
20). Takes of **Master of the Feast** at `converters: 0` while their line reads
`of which 0 are creatures`: **0/N** (was 3 of 3). Replies that state the brake and then break it in
the same record: **0/N** (was 1 of 1, `162v125` s57).

**P7 (162-B).** deck162 `Cast nothing right now` on an own-turn menu whose only non-pass row is
`Cast Fog Bank` or `Cast Shield Sphere` while the opponent line shows a creature: **0/N** (was 1).

**P8 (E-1, engine).** The MDFC pay-or-tap menu rendered while the seat's life is at or below the
payment, carrying no clause naming the resulting life total: **0/N** (was 1 of 1, and that 1 was a
loss). Games ending because a GPT seat answered a life-payment row: **0** (was 1).

**P9 (E-2, engine).** Cleanup-discard, forced-discard and bottom rows for a `forcetype(land)` hand
card carrying no land-half clause: **0/N** (was 74 of 74 rows / 13 windows at my seats, of which 6
listed one of the three names).

**P10 (E-3, engine).** `[you declined this exact list N times ...]` notes rendered on a window whose
situation block differs from the previous window, with no second number distinguishing them:
**0/N** (was 801 corpus-wide at N>=2, against 163 genuinely-repeated situations).

**P11 (E-4, engine).** GAME LOG lines of the form `- <You|Opponent> used: <lowercased MDFC token>
with <printed name>`: **0/N** (was 422 prompts / 684 occurrences).

**P12 (E-5, engine).** `dropping stale async answer` lines naming the **land-drop arm**: **0/N**
(was 45 of 108). Total stale drops: **< 40** (was 108, was 65, was 43). Lines naming the slot's
own decision: **> 0** (was 0).

**P13 (D14 regression detector).** `Flip Side` rows rendered to an AI seat: **0/N** (was 0 of 109 -
this prediction exists only to catch a regression). `PLAY THIS AS A LAND` rows on an opponent-turn,
non-main-phase or already-used prompt: **0/N** (was 0 of 30).

---

## WHAT I DID NOT CHECK

- **Scryfall was not fetched this wave.** Every card fact above is read from
  `bin/Res/sets/primitives/borderline.txt` with `/usr/bin/grep` - Agadeem, the Undercrypt `:1198`,
  Emeria, Shattered Skyclave `:33979`, Pelakka Caverns `:82639`, Pelakka Predation `:82646`,
  Grimclimb Pathway `:49109`, Mistgate Pathway `:73167`, Tidechannel Pathway `:117883`, Boulderloft
  Pathway `:13228`. Lane D reports these Oracle-verified on 2026-09-03 and the scripts agree with
  the clause classes it built; I did not re-fetch. **Bloodline Keeper and Intruder Alarm I did NOT
  verify against a primitive** - my reading of the 31-token chain comes from the narration
  (`Create vampire with Bloodline Keeper (x31)`) and the rendered enchantment text, not from a
  script read.
- **I did not open `src/`.** Every engine item above is a corpus repro plus a reading of the lane
  notes; E-1's "the emitter already has the number" is an inference from `borderline.txt:1199`'s
  `life:-3` and from the existence of the other verdict channels, not a traced control flow. I did
  not check where `- You used:` is composed (E-4) beyond observing that `chosen_text` is clean and
  the log line is not.
- **The human / console seat, lane G's grouping, and the PSP/Vita sidecars.** Nothing in this
  corpus touches them.
- **The §D log-window A/B** is a separate run and is not adjudicated here; I confirmed only that
  this corpus is the control arm (`log_window: full` on every record at my seats).
- **Opponent-seat logs** were read only where they narrate my seats and for the corpus-wide literal
  censuses (which cover all 42 logs). The four opposing guides were not scored.
- **Lane B's `they would be at K`, the menace header, `BLOCKING THIS COMBAT`, lane C's
  `{their converter:` and `{identical verdict`, lane E's `[<- ` marker and the D9 ANNOUNCE_X fit
  clause are UNTESTED at my seats, not passed** - 0 renders each at deck146, deck152 and deck162.
- **I did not re-derive the corpus-wide identical-declined RUN statistics** (17.1% share / max run
  12) nor the `hold_windows_skipped` sum; my HOLD numbers are renders and EXACT-matched takes,
  which I compute exactly.
- **The 30 `otherrestriction` spell back-face gates** (D33, lane D's deliberate non-change)
  produced no window at my seats and I did not go looking for one; the one spell-face toggle row
  lane D expected to survive did not render in this corpus either, so its residue is untested here.
