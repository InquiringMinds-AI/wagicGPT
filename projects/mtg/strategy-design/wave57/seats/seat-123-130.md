# Wave-57 seat: deck123 (3/6) + deck130 (3/6)

FOURTEENTH fair-hand corpus, `~/.Wagic/ai/gpt/selfplay-runs/matchups-20260903-174505/`, binary
`67b2bb38a` (wave-57 step-1 lanes A-H + S/T/U). Model `qwen36-35b-a3b` on every gameend at both
seats. Guides = the wave-56 reviewer revisions (`1e1dcd5ab`) + the wave-56 boundary pass
(`a93aad889`). Per the carry's binding rule **nothing below attributes a win rate, a game length
or a curve to a guide edit**; §0 addresses the brief's question about deck123's 1/6 -> 3/6 and
answers it with a measurement, not with an edit.

**Files (short -> full).** All twelve are `1788475*-ai_baka_deckA-0x...-vs-ai_baka_deckB.jsonl`:
`123v125` (...512), `123v126` (...520), `123v152` (...544), `123v130` (...549), `123v146` (...554),
`123v162` (...556), `130v146` (...509), `130v162` (...518), `130v152` (...529), `130v125` (...532),
`130v126` (...534), `130v123` (...549). Opposing seat logs read for the other side of the
narration; my eleven `game-*.stderr` files read for the drop/rescue/toggle counts in the ENGINE
list only. Every count is over MY twelve seat logs unless it says otherwise, and every one is
taken from the rendered `prompt`, never from `options`.

**Records.** deck123: **445 decisions, 2 fallbacks** (ask 324 / priority 97 / attackers 10 /
discard 7 / blockers 4 / recovery 2 / reveal 1 / bottom 1; + 6 `gamestart`, 6 `gameend`).
**3 wins** (v146 t10 22/-2, v126 t16 17/-39, v152 t24 6/-81), **3 losses** (v162 t19 0/14,
v130 t28 -2/18, v125 t53 0/30).
deck130: **598 decisions, 0 fallbacks** (ask 365 / priority 217 / attackers 14 / blockers 1 /
bottom 1). **3 wins** (v146 t19 20/-2, v162 t32 16/-2, v123 t28 18/-2), **3 losses**
(v152 t27 -20/22, v125 t59 0/36, v126 t16 0/38).
Combined **1,043 decisions, 2 fallbacks = 0.19 %** (wave 56 at these seats 3/792 = 0.38 %), and
**2 of the corpus's 8 fallbacks are mine** (`named_row_reask` 2 of 6). 0 timeouts, 0 empty
replies, 0 SEGV, 0 `abandoned_in_flight`, 0 `releasing so the game advances`, 0 `giving this
decision to the heuristic`, `identical_option_asks_resolved` 2 (one each in `123v126`,
`130v125`). `hold_windows_skipped` non-zero on **12 of 12 gameends**, sum **1,012**; largest
`123v125` 272, then `130v162` 149.

**Mulligans / hands (new-baseline record).** **14 looks, 12 keeps, 2 mulligans, 2 bottom asks, 0
repeated mulligans, no mull-to-zero, nothing to raise to the owner.**
- Both mulligans are the same shape and both are guide-correct: a ONE-land seven whose coverage
  line reads `would not cover any spell in it` (`123v162` s1) or covers only a 1-mana spell
  (`130v152` s1, `Spark Spray {r}`). **This is the first corpus in three in which
  `would not cover any spell in it` was actually acted on** - my predecessor's P5 (wave 55) was
  reported UNTESTED twice and is now **TESTED and PASS 1/1**.
- The one look worth naming: `130v126` s1 KEPT a one-land seven whose coverage line named three
  castable spells (`Pyrite Spellbomb {1}, Pyrite Spellbomb {1}, Spark Spray {r}`). That is the
  guide's own "coverage line names a card -> keep" branch, so it is the rule working. That game
  was lost, but not to mana: it reached 8 untapped sources by turn 16 and died to a life loop
  (§2.4). **The rule is 14/14 guide-correct.**
- Both bottom asks were answered sensibly: `123v162` s3 bottomed the second Underground Sea from
  a 5-land 2-spell keep; `130v152` s3 bottomed Hammer of Bogardan from a 3-land keep holding two
  land-kill spells. 2 of the corpus's 4 `bottom` records are mine.

---

# §0. THE BRIEF'S QUESTION: deck123 went 1/6 -> 3/6. WHAT CHANGED?

**The hands did. It is measurable, and it is measurable on the exact number my predecessor made
falsifiable for this purpose.**

Wave 56 closed with P5: *"windows in which deck123 controls at least one creature, 46 of 290
(15.9 %) overall and 2 of 194 (1.0 %) in its losses. If a wave-57 guide edit is claimed to have
moved this deck, it must move THIS number."* Recomputed on this corpus, same predicate (`Your
battlefield (N permanents listed, of which M are creatures ...)`, M >= 1):

| game | windows | with >=1 creature of mine | max creatures | result |
|---|---|---|---|---|
| `123v125` | 105 | 0 | 0 | L |
| `123v130` | 49 | 1 | 2 | L |
| `123v162` | 63 | **55** | 6 | **L** |
| `123v146` | 13 | 10 | 20 | **W** |
| `123v152` | 68 | 35 | 32 | **W** |
| `123v126` | 70 | 50 | 247 | **W** |
| **total** | **368** | **151 (41.0 %)** | | 3-3 |

**15.9 % -> 41.0 % overall; 1.0 % -> 25.8 % in the losses (56 of 217).** The mechanism is one
line long and it is upstream of every decision: **deck123 cast a token-maker in 6 of 6 games this
corpus.** Wave 56 had three games with no maker on the board at any point (one where none was
ever cast, two where every one was countered). This corpus only `123v125` - the counter deck -
denied it (4 counters: Intruder Alarm, Thraben Doomsayer, Bloodline Keeper x2), and that is the
one game with 0 creature-windows and the one loss that looks like wave 56.

**What it was NOT.**
- **Not the guide.** The live deck123 guide changed by **-189 bytes** between the wave-55 and
  wave-56 revisions (71,002 -> 70,813, then +74 from the boundary pass to 70,887): a 0.3 % text
  delta. No rung it touched moved a game. The one rung that measurably improved - Greaves,
  45 rows / 0 takes against 62 / 1 - is worth 2 mana in one window.
- **Not the render, on my evidence, and if anything the render cost deck123 a game.** The lanes
  that fire at this seat are lane A (HOLD, and it is excellent - see §3), lane C (discard
  verdicts, excellent) and lane C's new menu-level all-dead header, which is **FALSE on 6 of my
  92 all-dead menus** and talked the pilot out of an N=1 edict on the 6/5 that was killing it,
  three windows running, in a game it lost (ENGINE E1). The X apparatus, the MDFC work and the
  menace header render 0 times at deck123.
- **Not a change in what the opponents drew being controlled for.** It is not controlled for. The
  harness pins no seed (`initRand(0)` -> `srand(time(0))`); the corpus's own band this wave is
  4/3/3/3/3/3/2, the flattest on record, which is itself what a variance-dominated corpus looks
  like.

So the honest statement for the wave: **deck123's rate moved because its makers resolved and
stuck. The deck is still the ceiling my predecessor described - fourteen of its seventeen
interaction cards only ever point at a creature, and three of its six opponents win without
one - but the ceiling is not a floor, and this corpus it drew the top half of its own range.**
I am carrying P5 forward as the measure and predicting on it (P5 below), because it is the only
number at this seat that a guide edit could ever be shown to have moved.

**A second, smaller thing did change and it is worth recording separately:** the two games
deck123 flipped (146, 152) were both won by an ALPHA STRIKE off a resolved engine -
`123v146` s18 nineteen Humans after `Tribute to Hunger` cleared the lone blocker, `123v152` s101
twenty-seven Vampires off a transformed Bloodline Keeper wearing Lightning Greaves. In both the
guide's rungs fired in sequence and in both the LAST decision was an edict or an equip that the
guide names. That is a rung chain working; it is not a rate.

---

# §1. deck123 - Intruders of Thraben. 3/6 (wave 56: 1/6, three waves at 1/6).

## 1.1 THE BREAK THAT COST A GAME, AND IT IS PERCEPTION
**`123v130` seq 45 (turn 22, 13 life), seq 49 and seq 50 (turn 24, 7 life).** The menu:
```
NO LIVE CAST ROW ON THIS MENU: all 1 cast rows below carry a verdict computed from the board
that reads zero - not one of them changes a number on the board as it stands. ...
1. Cast Devour Flesh {1}{b} {right now: they control 1 creature - Rorix Bladewing (6/5)
   [flying, haste, doesn't untap during its controller's untap step] is sacrificed, they gain 5
   - the sacrificing player gains, not you; YOU control 0 creatures - targeting yourself does
   nothing} ...
```
Answered `Hold priority ...` all three times. Rorix had resolved that turn and hit for 6 the same
turn (13 -> 7); the deck lost at -2 on turn 28. RULE 5 of its guide had already answered this -
N = 1, the body is killing you, cast the edict - and what it lost to is a menu-level line that is
FALSE about the row under it. **6 of my 92 all-dead menus carry a demonstrably live cast row**
(list in ENGINE E1). This is the wave's HIGH item at this seat and it is not a guide failure;
A57-1 only states which unit RULE 5 reads, with a removal condition.

## 1.2 THE BREAK THAT COST THE OTHER GAME, AND IT IS STRATEGY
**`123v162` seq 31, turn 12, 14 life against 20.** Five creatures on the attack list, every one
tagged `[doesn't untap during its controller's untap step]` (its own Intruder Alarm). Reply:
`ATTACK: A1, A2, A3, A4, A5` / `PLAN: L=20, C=1, stop=24; M=5 now; this window: attack with all
5. Fire chain on opponent's turn.` Under the Alarm a tapped creature untaps only when a creature
ENTERS, and only an untapped maker makes one enter - so attacking with every maker ends the
engine. At seq 67 (turn 19, 1 life) the same board reads `Thraben Doomsayer #1 (4/4) (printed
2/2)`, `Bloodline Keeper (7/7)`, `Human #1 (5/5)`, all `[tapped - cannot attack or block this
turn]`: **26 power, tapped for seven turns**, while two Underworld Dreams took the pilot from 14
to 0 at 2 a turn off its own draw step. The pilot cast NOTHING after turn 12 - correctly, since
two Forced Fruitions would have drawn it 14 cards at 1 life each - so the DRAW-PUNISHER rung
(47 renders, 0 draws/cycles taken) held perfectly while the deck died anyway.
Both of the guide's rules were obeyed. The exception between them was never written; A57-2a/b
writes it in both places. Primitives verified: Intruder Alarm mtg.txt:58850
(`auto=lord(creature) doesnotuntap`, `@movedTo(creature|myBattlefield):untap all(creature)`),
Forced Fruition mtg.txt:42754, Underworld Dreams mtg.txt:125822, Thraben Doomsayer mtg.txt:120583
(`auto=this(controllerlife < 6) lord(other creature|mybattlefield) 2/2` - the Fateful hour that
produced those printed sizes, and A57-4).

## 1.3 The third break
**`123v130` seq 55, turn 25**: `Cast Intruder Alarm {2}{u} [second copy: you already control
Intruder Alarm; ... a second copy changes nothing]` taken with an Alarm on the battlefield line.
**1 of 6 `[second copy:` rows** (wave 56: 0 of 24). The rung was on my trim list and comes off it.

## 1.4 Rungs, with denominators (deck123)
- **RULE -1 mulligan**: 6 looks, 5 keeps, 1 mulligan (a one-land seven with no coverage), 1
  bottom. **PASS 6/6**, and it is the first corpus with a real ship.
- **RULE 0 land drops**: **55 asks, a land played at 55. PASS 55/55**, fourth corpus.
- **RULE 1 token lines**: 53 token-ish takes, **20 `[repeat: you control ...]` row takes of 46
  rendered rows**; the PLAN carried `L <>, C <>, stop <>; M is <> now` on every token window I
  read, including the one at 1.2 that was wrong for a different reason.
- **RULE 2 Marsh Flats**: 4 fetch windows, **4 cracked at the first offer**, `(you cannot make X
  right now)` rendered on 4 prompts / 13 rows and **4 takes**. **0 colour breaks. P2 PASSES**
  (wave 56: 1 of 5).
- **RULE 3 Damnation**: **54 rows, 9 of them LIVE, 3 casts, every one at `M of yours` = 0** and
  two of them at `destroys 4`/`destroys 1` with 0 of its own. **0 casts at M >= 4** on a menu
  that printed `246 of yours` once (`123v126` s88). **P1-of-wave-55 PASSES on a live denominator
  for the first time** - wave 56's PASS was vacuous (25 rows, all `destroys 0`).
- **RULE 4 Tragic Slip**: 109 rows, **5 casts**, four of them on a `-13/-13 ... Morbid applies`
  row naming the kill (`{kills: Luminarch Aspirant}`, `{kills: Rorix Bladewing, Siege-Gang
  Commander, Goblin #1, Goblin #2}`), one at `-1/-1` killing a 1/1 Blastminer. **0 wasted.**
- **RULE 5 edicts**: **267 rows, 95 of them at N >= 1, 4 casts, every one at N = 1.** 0 casts at
  N = 0 and 0 at `their choice`. **PASS**, and the one from-exile take is right (below).
  Against that: **3 DECLINES at N = 1 under a false menu header** (1.1) - counted as a render
  break, not a guide break.
- **`[from exile]`**: `123v152` s16 took `Cast Tribute to Hunger {4}{b} [from exile]` over
  `Cast Devour Flesh {1}{b}` on the same menu, both at N = 1 on the same Intrepid Adversary.
  3 more mana for `you gain 2` instead of `they gain 2` - a 4-life swing, with 6 mana and nothing
  else wanted. **P2-of-wave-55 PASSES**: the render's `the sacrificing player gains, not you`
  clause is doing the work.
- **RULE 6 Greaves**: **45 rows carrying `you control 0 creatures - this equips nothing`, 0
  taken. P1 - the prediction my predecessor cared about most - PASSES, seventh corpus.** The one
  Greaves cast this corpus (`123v152` s97) was onto a live board and the equip that followed gave
  Lord of Lineage haste for the 27-Vampire lethal swing.
- **All-dead menus**: **92 of 345 casting menus, ONE cast off them** (`123v130` s7) - **and that
  cast was CORRECT**: the menu's header was false and the row it took carried `{kills: Dwarven
  Blastminer}`. **P6 is re-adjudicated PASS**; the letter-reading FAIL is an artefact of the
  header.
- **Cleanup discard**: 7 asks, **36 of 36 rows annotated** (wave 56: 357 of 426 bare), 0 breaks -
  every send was a duplicate or a card the board had made dead.
- **Blockers**: 4 windows, **0 breaks**, including the D24 specimen at 1.5. **Attackers**: 10
  windows, 1 break (1.2), 2 alpha strikes that won games.

## 1.5 The blockers window the wave-56 edit was written for, and it worked
**`123v152` s85**, turn 23, 12 life:
`INCOMING THIS COMBAT: 4 attackers, 15 unblocked damage - you would be at -3; this KILLS you
(your creatures may legally block every attacker in that total) - one legal assignment gets you
to 1`. Answered with four Vampire blocks and **survived at 6**, then won on turn 24. Last corpus
the same header shape produced a decline that A56-5 had to license after the fact; this corpus
the header printed the survivable total and the pilot found it. **D24 PASS, and A56-5 (P3) PASSES
on its only window.**

---

# §2. deck130 - Budde's Ponza. 3/6 (wave 56: 2/6, wave 55: 4/6).

## 2.1 The face-damage class, and the rule was on the wrong seam
**4 face-damage takes** (wave 56: 8).
- `130v123` s80: `The opponent (player, life 1) {right now: takes 3 damage - they would be at -2;
  THIS WINS THE GAME}` - the win. Correct.
- `130v162` s95: 1 damage to the face at their 19, on a menu whose other rows are three 0/2
  defenders all reading `SURVIVES` and its own tapped Blastminer reading `DIES`. Correct.
- **`130v125` s92 and s94, turn 38** - and this is the finding. Both are TARGET menus with
  exactly two rows, `The opponent (player, life 26)` and `Yourself (player, life 20)`. **There is
  no declining row.** The decisions were s91 and s93, the CAST rows, which printed
  `{no creature target - and 3 to the opponent at life 26 leaves them at 23}` and a `Cast nothing
  right now` row. At s93 the frame read `Opponent life trend: turn 34: 28, turn 36: 28, turn 38:
  28, now 31 (+3 since turn 34)` - they had gained 8 back inside the turn - and the pilot cast
  the second Hammer with the same sentence it used for the first: *"3 damage is not lethal, but
  it is the only active play"*, a sentence its own guide names as the failure. Add s88/s90 (a
  Pyrite Spellbomb into the face at their 28) and **that one turn spent three burn cards into a
  five-land creatureless board that was gaining faster than 3 a turn**. Lost 0 to 36 on turn 58.
**Adjudication.** Wave-56 P7 (face takes into 3+ lands with no lethal chain: 0) **FAILS 2 by the
letter**; re-scored at the seam where a decline existed it is **2 cast-row breaks**, and P8 (the
latch) **FAILS** - the latch never fired although the render printed the gain twice. D57-1 keys
the latch to the printed trend line and moves the whole floor/latch/release apparatus to the cast
row. The wave-56 release (`0 creatures AND <= 2 lands`) was never wrong here: the board printed
FIVE lands, so the floor bound, and the one release-licensed take (`130v123` s80) was lethal.

## 2.2 The X apparatus - the seat's best rung last corpus, and it regressed
**5 X-announce menus, 5 cast rows, 117 prompts carrying `{X pricing:`, 136 X rows, 0 rows without
a fit clause (D9 PASS, corpus-wide 0 of 55).** Marked rows: `[<- most kills at any affordable X`
3 renders / 3 takes, `[<- best trade:` 1 render / 1 take (first ever - wave 56 UNTESTED),
`[<- best X for this cast:` **7 cast-row renders / 4 takes - a NEW seam for the marker**,
`[<- no X on this menu kills anything of THEIRS]` 99 renders / 2 takes.
Two of the five are wrong, and both were decided at the cast row:
- **`130v162` s7, turn 4, two mana.** `Cast Starstorm {r}{r}{x} {X pricing: your mana affords
  only X=0 right now, which deals 0 damage and kills nothing}` - TAKEN, under `PLAN: Sweep their
  board with Starstorm (X=0 is fine ...)`. s8's X menu then had ONE row. **P11's "X = 0 answers
  stay at 0" FAILS 1 - but the X-menu answer was forced; the break is s7.**
- **`130v152` s31, turn 14, 19 life.** `... At X=4 - kills THEIRS: Sigarda, Champion of Light;
  YOURS: Dwarven Blastminer, Siege-Gang Commander, Goblin x3` with `[<- best X for this cast:
  X=4 - best trade: the most of THEIRS at the smallest cost to YOURS]`. TAKEN, in a reply whose
  own PLAN read `Sweep Sigarda with X=4. Attack with Siege-Gang Commander and 3 Goblins.` The
  sweep killed the attack. The guide's clause (a) names Siege-Gang Commander explicitly and lost
  to the marker printed beside the row. **`[<- best X for this cast:` is a NEW literal: the
  marker now renders on the CAST row, not only on the announce menu** - see ENGINE E3 and skill
  proposal S57-8.
Of the three correct ones, all three took the marked row and two were the bottom of a collapsed
`same kills as X=N, for K less mana` run. **P11's marked-row half: 3 of 3 where the marker was
sound; `kills THEIRS: none` takes 0; top-of-collapsed-run takes 0.**

## 2.3 `- SURVIVES`: the shape that broke twice is now at zero
**25 windows rendered a `- SURVIVES` row; 2 taken, and both are PLANESWALKERS** - `130v152` s36
Teferi `SURVIVES (loyalty 3, 1 left)`, `130v126` s43 Sorin `SURVIVES (loyalty 4, 1 left)`. Loyalty
does not heal. **Creature-shape SURVIVES takes: 0 - P10 PASSES on the shape it was written for,
third corpus of trying**, and D57-2 carves the loyalty form out so the rung stops mis-scoring
correct play.

## 2.4 The game that was lost inside one phase, and it is a window-economy item too
**`130v126`, turn 16, Main phase 1: 72 decisions in ONE (turn, phase)** - 37 priority + 35 ask,
of which **68 are pure declines** (24 hold takes, 23 `pass`, 21 `Cast nothing`) - for
**1,415 s = 23.6 minutes of inference** and four real actions. The narration for that phase is
eighteen consecutive `Opponent gained 1 life (now N)` / `You lost 1 life (now M)` pairs: the
opponent's Tribute to Hunger ate a Goblin, Sanguine Bond turned their gain into the pilot's loss,
Exquisite Blood turned that loss back into their gain, and the loop ran **18 to 0** while the
engine asked the model a question at every tick. `[you declined this exact list N times already
this turn]` reached **N = 43** here (wave 56 max 11).
The pilot's contribution is one decision: **s63, casting Siege-Gang Commander into that board**
off a row that priced it - `{their converter: this body has toughness 2 and they control 1
life-to-damage converter (Sanguine Bond) - ... takes 2 off YOU: life 18 -> 16}` - with the frame's
block naming BOTH halves. It was probably dead anyway (nothing in this sixty removes an
enchantment) but it handed them the trigger. D57-4. Primitives verified: Sanguine Bond
mtg.txt:99004, Exquisite Blood mtg.txt:37966, Tribute to Hunger mtg.txt:124073.

## 2.5 Rungs, with denominators (deck130)
- **Land drops**: 24 asks, 24 lands. **PASS.** **Mulligan**: 6 looks, 5 keeps, 1 ship (one-land),
  1 bottom. **PASS 6/6.**
- **#0 draw punishers**: 2 renders, **0 draws or cycles - PASS, seventh corpus**, but on a tiny
  denominator; reported as PASS (vacuous-adjacent), not as evidence.
- **Cycling**: **19 cycles taken; 1 wrong by the current letter** (`130v125` s101, Lay Waste at 15
  mana against 6 of their lands) **and that one is exactly the window D56-3 licenses. P11-of-wave-55
  PASSES.** P9 (cycle rows offered at opponent life <= 6 with 10+ untapped sources) - **the window
  never arose: 0 of 0. UNTESTED**, reported as such.
- **Land destruction**: **26 acts. At my life <= 8: 0 - P12's life floor PASSES** (wave 56: 1).
  Against a printed 5+ of their lands: **5 of 26** (wave 56: 3 of 25) - **FAILS the letter**, but
  three of the five are `130v123` s35/s44/s55, the game this deck WON by keeping deck123 off its
  mana, and one is `130v125` s102 at turn 40 in a game already lost. I am **not** tightening this
  clause on that evidence; see P12 below.
- **Talisman life**: 6 takes of a `{paying this costs you N life}` row, **none at 3 life or below
  - PASS, seventh corpus.**
- **`[second copy:`**: 21 rows, **9 takes, all correct** - 7 Talisman of Impulse (mtg.txt:117529,
  three separate `{T}:Add` lines, they stack) and 2 Pyrite Spellbomb (mtg.txt:90168, each is its
  own sacrifice).
- **`[legendary:` 0 renders - UNTESTED, second corpus.**
- **Attackers**: 14 windows, 0 breaks. Seven of them are `130v162`'s lone Dwarven Blastminer into
  a board of 0/2 defenders whose tag read `[their untapped blockers: Shield Sphere (0/6) (neither
  dies); Fog Bank (0/2) (neither dies (no combat damage is dealt either way - prevented))]` - a
  provable no-op, but free: the Blastminer could not block the flier anyway and the pilot never
  had the {2}{R} to activate it on those turns. Recorded, not ruled a break.
  **Blockers**: 1 window, `Goblin blocks Thraben Doomsayer` under a `best case with every blocker
  assigned` header. Correct.
- **Race check**: 3 lone-Rorix sends, no flying blocker on any - **no race arose; UNTESTED, ninth
  corpus.** Stays retired from the prediction set.

---

# §3. PREDICTION ADJUDICATION

## Set A - the wave-56 guide edits, at these seats
| prediction | verdict | evidence |
|---|---|---|
| **P1** (A56-2/3/16, Greaves re-keyed to `this equips nothing`): 1 -> 0 | **PASS 0 of 45 rows**, seventh corpus. The rung the predecessor said it cared about most | - |
| **P2** (A56-8, untagged dual-over-basic tie-break): 1 -> 0; first-window cracks N/N | **PASS both.** 4 fetch windows, 4 cracked at the first offer, 0 colour breaks; `(you cannot make ...)` rendered 4 prompts / 13 rows, 4 takes | - |
| **P3** (A56-5, blockers on a `this KILLS you` header) | **PASS 1 of 1.** Header named `one legal assignment gets you to 1`, four blocks declared, survived at 6, won next turn | `123v152` s85 |
| **P4** (the ~2.5 KB trim test) | **PASS on every clause.** Damnation casts at `destroys 0` **0 of 54**; edict casts at N != 1 **0 of 267**; `[second copy:` takes **1** (the Alarm - see below); `plan_choice_conflict` **0**; casts off an all-dead menu **1, and correct**. The trim holds | - |
| **P4 exception** | The **only** rung that broke is the one paragraph I would have trimmed and the predecessor kept: the Alarm second-copy bracket, **1 of 6** | `123v130` s55 |
| **P5** (board presence must move for any deck123 claim) | **MOVED, and not by an edit: 15.9 % -> 41.0 %, losses 1.0 % -> 25.8 %.** §0 | corpus-wide at my seat |
| **P6** (mulligan: keeps at 2+ lands N/N; `would not cover` not a ship at two lands) | **PASS, and the coverage branch is TESTED for the first time**: 14/14 correct, 2 ships both at ONE land | `123v162` s1, `130v152` s1 |
| **P7** (D56-1, face takes into 3+ of their lands with no lethal chain: 0) | **FAIL 2 by the letter**, re-scored as **2 cast-row breaks** at s91/s93 - the s92/s94 menus have no declining row | `130v125` s91-s94 |
| **P8** (D56-2, the latch as a per-turn comparison) | **FAIL.** The trend line printed `+3 since turn 34` and the pilot burned again in the next window | `130v125` s93 |
| **P9** (D56-3, cycle release at 10+ mana vs 5+ lands): 14 -> under 4 | **UNTESTED. 0 of 0** - no cycle row was offered at opponent life <= 6 with 10+ untapped sources | - |
| **P10** (D56-4, takes of a `- SURVIVES` row): 1 -> 0 | **PASS on the shape** (0 creature-form takes of 25 windows); 2 takes, both `SURVIVES (loyalty N, K left)` on planeswalkers and both correct | `130v152` s36, `130v126` s43 |
| **P11** (the ~2 KB X trim) | **SPLIT.** Marked-row takes 3/3 where sound, `kills THEIRS: none` 0, top-of-run 0 - all PASS. `X = 0` answers **1** - FAIL, and forced (the X menu had one row); the real break is the cast at s7 | `130v162` s7/s8 |
| **P12** (LD below 8 life 1 -> 0; against 5+ lands 3 -> 0 or 1) | **SPLIT: life floor PASSES 0 of 26.** The 5+ land clause reads **5 of 26** - worse by the letter, and three of the five are in the game this deck won by mana denial. Draws under `DRAW PUNISHERS` **0 - PASS** | `130v123` s35/s44/s55 |
| **P13** (engine-conditional, E1: stale drops 35 -> under 15) | **FAIL, and re-scoped.** 78 drops at my eleven games (corpus 108). The land-drop arm is **flat** - 30 of mine, 45 corpus-wide, against 44 last corpus - so lane A's second slot did NOT move that class; the growth is the CASTING arm. See ENGINE E2 | corpus-wide stderr |
| **P14** (lone-Rorix race check, retired) | Stays retired. **0 windows, ninth corpus** | - |

## Set B - the wave-57 step-1 lanes, by the emitter's actual string at these seats
| lane | verdict |
|---|---|
| **A** (D1/D4/D5/D16/D31) | **D1 is the cleanest result at my seats in the loop's history: 289 HOLD takes, 0 followed by a byte-identical same-turn re-ask** (wave 56: 8 of 119; wave 55: 79 of 170). HOLD renders **819 prompts / 289 takes (35.3 %)** at my seats against the corpus's 18.1 %. **D4 PASS and now TESTED on both halves**: 0 records with `choice >= 0` and an empty `chosen_text`, and `<refused: named_row_reask>` **renders twice** as `chosen_text` (wave 56: 0, UNTESTED) - both refusals were correct, the replies named rows that were not on the window, and 0 wrong casts followed. **D16 `plan_choice_conflict` 0, `plan_choice_conflict_narrowed` 0** - same ambiguity my predecessor flagged: I cannot distinguish narrowed-away from never-arose. **D5 FAILS at my seats and I have classified all of it** - see ENGINE E2; the carry's expectation that the drop line carries `asyncSlotDriftKind` is wrong, the literal is `dropping stale async answer (casting arm; the question (or turn/phase) moved)` / `(land-drop arm; ...)` and `asyncSlotDriftKind` renders **0 times corpus-wide**. |
| **B** (D6/D24/D22/D10) | **D6 PASS 9 of 9.** 11 opponent-combat-phase windows at my seats, 9 with an `INCOMING THIS COMBAT` line; the 2 without are `123v162` s50 and `130v126` s35, both Attackers-phase windows where the opponent's line reads `0 of them able to attack right now` (deck162's two creatures are a Fog Bank and a tapped flier; deck126 had 0 creatures) - the closed form is correct there. Forms seen: declared (7 shapes counted), `this KILLS you` **27 prompts**, `none of your creatures can block` **0 - UNTESTED**. **D24 PASS**: `best case with every blocker assigned` **7 prompts**, `one legal assignment gets you to` **2**, and the one lethal-header blockers window needed no subtraction by the seat - the header named the survivable total and the pilot took it (`123v152` s85). **D22 `BLOCKING THIS COMBAT:` 0 renders - UNTESTED.** **D10 PASS 1,025 of 1,025** prompts carrying an opponent battlefield block also carry `Their untapped sources:`. Menace header **0 - UNTESTED**, 5 blockers windows, no menace attacker. |
| **C** (D7/D8/D21/D30/D11/D12) | **D8 PASS and it CLOSES my predecessor's E2**: **36 of 36 discard rows carry a `{dead right now: ...}` or no-verdict-because-live annotation** (wave 56: 357 of 426 bare), and the one 8-row menu I read prices six dead rows and leaves the two live Idyllic Tutors clean. **D7 TESTED, 1 render, 1 take**: `{their converter: this body has toughness 2 and they control 1 life-to-damage converter (Sanguine Bond) ... takes 2 off YOU: life 18 -> 16}` on `130v126` s63 - the clause is correct and the pilot cast anyway (§2.4). **D21/D30 (the menu-level all-dead line) FAILS at my seats: 6 of 92 renders are false** - ENGINE E1, the HIGH item. **D11 `{identical verdict right now to row N}` 0 renders - UNTESTED. D12 dead-menu header: the literal that renders is `NO LIVE CAST ROW ON THIS MENU`, 92 prompts.** |
| **D** (D14/D26-D29) | **UNTESTED AT MY SEATS AND STRUCTURALLY SO**, second corpus. `Flip Side` **0**, `PLAY THIS AS A LAND` **0**, `{taking this row ` / `{it enters TAPPED` as an arrival clause **0** (the `{taking this row skips ...}` string I do count 819 times is the HOLD row's own clause, not D26's). Neither sixty runs an MDFC. `only display-toggle` stderr lines: **367 across three of my games** (`game-152v130` 195, `game-146v130` 154, `game-146v123` 18), **max consecutive run 4** - same as wave 56, so E3 stands: ordinary phase-7 re-entry, not a livelock, and these are candidate-level drops with **no prompt cost** (no translog record, no model call). |
| **E** (D2/D9/D15/D19/D20/D23) | **D15 PASS 1,025 of 1,025 board frames** carry `Opponent life trend:` (wave 56: 1 of 137) and it is the line my D57-1 edit is keyed to. **D9 PASS: 136 X rows, 0 without a fit clause** (wave 56: 40 of 40 without). **D2 UNTESTED** - `counted TWICE above, on purpose` **0 renders**, no MDFC in either sixty, so I can say nothing about the "every MDFC-holding pregame has it" check. `[<- ...]` markers: 6 renders / 4 takes; `[NAMED BY THEIR` **1 render, 0 takes** (wave 56: 4 / 1). |
| **F / S / T / U** | **All quiet at my seats. 0 SEGV in my eleven games, 0 phase-stuck games, `abandoned_in_flight` 0** (lane U's prediction, PASS at my seats), 0 `releasing so the game advances`, 0 `giving this decision to the heuristic`, `consuming an in-flight answer` **5** at my games (corpus 11). No `WAGIC_STALLPROBE` line. The Intruder Alarm + Thraben Doomsayer livelock the carry names from `146v123` did **not** recur: `123v146` ended naturally at turn 10 and `146v123` has 0 stale drops and 18 toggle lines. |
| **G** | Human-seat render only. No AI-seat prediction; nothing at my seats bears on it. |
| **H** | **PASS, control arm reproduces.** `log_window` = `full` on **1,041 of 1,041** records carrying the field, `log_window_elided` absent on all 1,067 records, 0 elided. Narration is **56.9 %** of my mean prompt (10,939 chars). |

## THREE-NUMBER LITERAL AUDIT (#188) - from the RENDERED `prompt`, per file, never from `options`
| literal | old-string renders | new-string renders (prompts / rows) | takes |
|---|---|---|---|
| `Hold priority for the rest of this turn: ...` | wave-54 string **0** | **819** prompts (deck123 350, deck130 469) | **289**; **0** followed by a byte-identical same-turn re-ask |
| `[you declined this exact list N times already this turn]` | n/a | **276** prompts; max N **43** (`130v126` t16; wave 56 max 11) | 0 decline -> cast flips |
| `NO LIVE CAST ROW ON THIS MENU` (D21/D30) | 0 | **92** prompts | **1 cast**, and it was correct - **6 of the 92 are FALSE** |
| `you control 0 creatures - this equips nothing` | 0 | **45** rows | **0** |
| `{dead right now:` on discard rows (D8) | bare rows **0 of 36** | **36 of 36** rows annotated | 1 discard menu read in full |
| `INCOMING THIS COMBAT:` | 0 | **10** prompts / 9 of 9 threat windows | n/a |
| `this KILLS you` | 0 | **27** prompts | 1 blockers answer, correct |
| `best case with every blocker assigned` / `one legal assignment gets you to` (D24) | 0 | **7** / **2** | n/a |
| `BLOCKING THIS COMBAT:` (D22) | 0 | **0** | **UNTESTED** |
| `none of your creatures can block` | 0 | **0** | **UNTESTED** |
| `N of the M attackers need TWO or more blockers each` (menace) | 0 | **0** | **UNTESTED**, 5 blockers windows |
| `Their untapped sources:` | 0 | **1,025** (100 % of board frames) | n/a |
| `Opponent life trend:` (D15) | 0 | **1,025** (100 %) | keyed by D57-1 |
| `{X pricing:` (D9) | n/a | **117** prompts / **136** rows, **0** without a fit clause | 5 cast rows, 5 X menus |
| `[<- most kills at any affordable X ...]` | n/a | **3** | **3** |
| `[<- best trade: ...]` | wave 56 **0** | **1** | **1 - and it was wrong** (§2.2) |
| `[<- best X for this cast: ...]` (NEW SEAM) | n/a | **7** cast rows | **4** |
| `[<- no X on this menu kills anything of THEIRS]` | n/a | **99** rows (72 of them inside the one phase at E4) | **2** |
| `{X pricing: same kills as X=N, for K less mana}` | n/a | **3** rows | **2**, both the bottom of the run |
| `{their converter:` (D7) | 0 | **1** row | **1** |
| `{identical verdict right now to row ` (D11) | 0 | **0** | **UNTESTED** |
| `Flip Side` / `PLAY THIS AS A LAND` (D14/D8-MDFC) | 0 | **0** / **0** | **UNTESTED**, no MDFC in either sixty |
| `counted TWICE above, on purpose` (D2) | n/a | **0** | **UNTESTED** |
| `<refused:` (D4) | 0 | 0 in prompts | **2** as `chosen_text` - first renders ever, both correct |
| `[RE-ASK]` | n/a | **2** prompts | n/a |
| `YOUR LAST PLAN is withdrawn` (D11) | 0 | **0** | **UNTESTED** |
| `[NAMED BY THEIR <src>:` | n/a | **1** | **0** |
| `[second copy:` | n/a | **27** rows (deck123 6, deck130 21) | **10 - 9 correct, 1 wrong** (the Alarm) |
| `[legendary:` | n/a | **0** | **UNTESTED**, second corpus |
| `[repeat: you control M creatures ...]` | n/a | **46** rows | **20** |
| `LIFE-TO-DAMAGE CONVERTER` | n/a | **89** prompts (all deck130) | 0 edicts under it |
| `their life LOOP is in play` | wave 56 **0** | **rendered** (`130v126`, inside the converter block) | see §2.4 |
| `DRAW PUNISHERS` | n/a | **49** prompts (deck123 47, deck130 2) | **0** draws/cycles |
| `{paying this costs you N life ...}` | n/a | **319** rows | **6**, none at life <= 3 |
| `- DIES` / `- SURVIVES` | n/a | **175** / **31** rows | **12** / **2** (both loyalty) |
| `{leaves N sources - no other row on this menu needs more than N}` | n/a | **955** rows | **117** |
| `The same N options as A-B, with <source> #2 as the source instead of #1` | n/a | **52** prompts | n/a |
| `copies 1-N of N in this list` (collapse) | n/a | **7** prompts | n/a |
| `would not cover any spell in it` | n/a | **1** | **1 mulligan** - first act-on ever |
| `even with every land in this hand in play` (mulligan colour) | n/a | **0** | **UNTESTED**, fourth corpus |
| `{visible now:` / chooseaname | 0 | **0** | **UNTESTED** |
| `reveal_stall` | 0 | **0** | **UNTESTED**; 1 reveal at my seats (`123v152` s6) |
| `for {K} more mana` (D15-of-wave-56) | 0 | **0** | **UNTESTED** |
| `their choice - all tied at MV` (D9-of-wave-56) | 0 | **0** | **UNTESTED** |
| `asyncSlotDriftKind` (stderr, carry's expectation) | n/a | **0 corpus-wide** - the literal is `(casting arm\|land-drop arm; the question (or turn/phase) moved)` | n/a |

---

# §4. ENGINE / RENDER ITEMS (ranked)

## E1 (HIGH, render correctness). THE MENU-LEVEL `NO LIVE CAST ROW ON THIS MENU` LINE IS FALSE ON 6 OF ITS 92 RENDERS AT MY SEATS, AND IT COST A GAME
**Repro, the game-losing one: `123v130` seq 45, turn 22.** Header:
`NO LIVE CAST ROW ON THIS MENU: all 1 cast rows below carry a verdict computed from the board
that reads zero - not one of them changes a number on the board as it stands.`
Row 1: `Cast Devour Flesh {1}{b} {right now: they control 1 creature - Rorix Bladewing (6/5)
[flying, haste, doesn't untap during its controller's untap step] is sacrificed, they gain 5 -
the sacrificing player gains, not you; YOU control 0 creatures - targeting yourself does
nothing}`. A forced sacrifice of their only creature is not "a verdict that reads zero".
**All six (file, seq, turn):** `123v152` s18 t15 (Tragic Slip carrying `{kills: ...}`);
`123v130` s7 t4 (BOTH rows live - Devour Flesh at N=1 and a Tragic Slip carrying
`{kills: Dwarven Blastminer}`); `123v130` s45 t22, s49 t24, s50 t24 (Devour Flesh on Rorix, three
times); `123v146` s16 t10 (Devour Flesh on Silverquill Silencer 3/2).
**Cost:** three declines of a forced kill on the 6/5 that was hitting for 6 a turn, in a game
lost at -2. The one CAST off an all-dead menu in this whole corpus (`123v130` s7) is the pilot
correctly ignoring the false header.
**Hypothesis for the row-level classifier, stated as a hypothesis** (I read no source): all seven
false-live rows carry a NEGATIVE substring inside a multi-part `{right now:}` verdict - the edict
rows carry `YOU control 0 creatures - targeting yourself does nothing` as their second half, and
the Tragic Slip rows carry `no creature has died this turn, so Morbid does NOT apply` while ALSO
carrying `{kills: <name>}`. 131 of my casting menus that were NOT flagged all-dead also contain
such a row, so the substring alone is not sufficient - the visible pattern is that the row-level
"reads zero" verdict is wrong for these two row shapes, and the menu-level header is only false
when every other row on that menu is genuinely dead.
**Ask (lane C):** the menu-level line should be derived from the same per-row verdicts it
summarises, so the two can never disagree; and a row carrying `{kills: <name>}` or
`they control N creature(s) - <name> is sacrificed` with N >= 1 must never be scored dead.
Guide side: A57-1, with a removal condition.

## E2 (MED, async lifecycle). D5's REGRESSION IS CLASSIFIED: THE LAND-DROP ARM IS FLAT, THE CASTING ARM TRIPLED, AND A TRIGGER LOOP OWNS MOST OF IT
The carry asked for all 108 drops to be classified by arm before the second slot is called
ineffective. My eleven games hold **78 of the 108**. Corpus-wide split, from the emitter's own
strings: **`(casting arm; the question (or turn/phase) moved)` 63, `(land-drop arm; ...)` 45.**
- **The land-drop arm is FLAT: 45 corpus-wide against 44 last corpus.** 30 of the 45 are mine, and
  `game-125v123` alone has 16 land-arm drops with 0 casting-arm drops. On this evidence lane A's
  second slot did not move the class it was built for. (I cannot see WHY from a translog; I can
  see the arm and the count.)
- **The casting arm is the growth, and one game is 28 of it.** In `game-126v130` 28 of 29 drops
  sit directly under a `Resolving Action on stack: StackAbility. (Source: Sanguine Bond)` /
  `(Source: Exquisite Blood)` pair, alternating with `AIPlayerGPT: hold re-opened at the cast
  seam - a printed row changed or is newly available`. That is the §2.4 life loop: every tick is a
  real board change, so the slot key correctly misses and the answer in flight is correctly
  dropped - but the deck is being asked, and re-asked, inside a deterministic unstoppable loop.
- `asyncSlotDriftKind` renders **0 times corpus-wide**; the carry's expectation about the drop
  line's contents does not match the emitter.
**Ask (lane A):** nothing is incorrect here - this is cost. The item is E4 below, and this entry
exists so the D5 number is not read as a slot failure.

## E3 (MED, render). THE `best trade` MARKER CAN MARK A 1-FOR-5, AND IT NOW PRINTS ON THE CAST ROW
**Repro: `130v152` seq 31 (cast row) and seq 33 (X menu), turn 14, 19 life.**
`[<- best trade: the most of THEIRS at the smallest cost to YOURS - at X=4 that is THEIRS:
Sigarda, Champion of Light; YOURS: Dwarven Blastminer, Siege-Gang Commander, Goblin x3]`.
The marker is correct by its own definition - no cheaper X kills anything of theirs - and it
points at destroying five of the pilot's own creatures to kill one. The pilot followed it, which
is what the trust doctrine says it should do, in a reply whose PLAN named the creatures the sweep
destroyed. **Two asks:** (a) print the two counts in the marker (`THEIRS 1 / YOURS 5`) so the
argmax and the net are both on the row, or suppress the `best trade` form when YOURS exceeds
THEIRS - the `most kills ... that costs you nothing` form already handles the free case;
(b) note that `[<- best X for this cast:` is a NEW seam for this marker (7 renders, 4 takes) -
every guide sentence that located the marker "one screen later" is now stale. Guide side: D57-5.

## E4 (MED, window economy). 72 MODEL DECISIONS AND 23.6 MINUTES OF INFERENCE INSIDE ONE UNSTOPPABLE TRIGGER LOOP
**Repro: `130v126` turn 16, Main phase 1, seq 63-134.** 37 priority + 35 ask; 68 of the 72 are
declines (24 hold takes, 23 `pass`, 21 `Cast nothing`); **1,415 s of latency**; 71 of the 72
option blocks are distinct, so no existing de-dup key can catch it. The underlying event is the
Sanguine Bond + Exquisite Blood loop resolving one point at a time, 18 times, to the pilot's
death - a sequence in which no legal answer changes anything.
This is the shape my predecessor's wave-55 E1 (the hold row re-opening on a life tick) was
declared CLOSED for, and the closure holds for its own case (0 byte-identical re-asks in 289
takes). It is NOT closed for a repeated trigger loop, because there the board really does change
each tick. **Ask (core loop):** while a triggered-ability loop is resolving with no legal
response available to a seat, that seat's windows are the cheapest thing in the engine to skip -
`hold_windows_skipped` already exists as the mechanism. Corpus-wide this game's phase alone is
2.2 % of the wave's decisions.

## E5 (LOW, UX). A DECISION MENU WITH EXACTLY ONE ROW STILL COSTS A ROUND TRIP
`130v162` seq 8: `1. X = 0 {X pricing: X=0 - this cast does NOTHING: it deals 0 damage and the
spell is spent} ... [<- no X on this menu kills anything of THEIRS ...]` - one row, one model
call. Same family: the two-row target menus at `130v125` s92/s94 whose only alternative is
shooting yourself. Legality is not the issue and nothing should be withheld; the ask is either to
collapse the N=1 case (the `identical_option_asks_resolved` machinery already exists) or to say
in the header that the menu has no declining row and the decline was the previous window - which
also fixes the seam problem for every guide that carries a "do not spend it" rung. Filed as
`deck130/general-suggestions.md` G57-5.

**Closed from wave 56:** **E2** (the Cleanup-discard menu showing a verdict on some dead cards and
silence on others) - **CLOSED by lane C's D8**, 36 of 36 rows annotated, the exact families that
were silent (Tribute to Hunger, Devour Flesh, Damnation) now priced. **E1** (the async drop class)
- re-scoped, not closed, as E2 above. **E3** (`only display-toggle` run lengths) - re-measured,
max run 4, confirmed as ordinary re-entry. **E4** (two edict rows for the same effect at two
prices, unmarked) - **effectively answered by the render**: `123v152` s16 shows the two rows
differing in their printed verdicts (`they gain 2 - the sacrificing player gains, not you` vs
`you gain 2`) and the pilot picked the better one for 3 more mana. Downgrade to closed.

---

# §5. CARD FACTS CHECKED
Against `projects/mtg/bin/Res/sets/primitives/` with `/usr/bin/grep`, and the deck lists
`Res/ai/baka/deck123.txt` / `deck130.txt`. Every fact I rely on above:
Intruder Alarm (mtg.txt:58850 - `lord(creature) doesnotuntap` plus BOTH `@movedTo(creature|
myBattlefield)` and `|opponentBattlefield` untap-all lines: the lock and the release are both
symmetric, which is why 1.2 is a real trap and not a quirk), Thraben Doomsayer (120583, the
`controllerlife < 6` Fateful hour lord), Bloodline Keeper (12582), Lightning Greaves (67192),
Devour Flesh (29133), Tribute to Hunger (124073), Tragic Slip (123128), Damnation, Idyllic Tutor;
Sanguine Bond (99004), Exquisite Blood (37966), Underworld Dreams (125822), Forced Fruition
(42754), Vision Skeins (129546), Shield Sphere (103832); Starstorm (112835), Hammer of Bogardan
(52046), Pyrite Spellbomb (90168), Spark Spray (110142), Lay Waste (66043), Molten Rain, Stone
Rain, Talisman of Impulse (117529 - three separate `{T}:Add` lines, so the `[second copy:` takes
are correct), Siege-Gang Commander (105071), Rorix Bladewing (96836), Dwarven Blastminer (33251),
Sigarda, Champion of Light (borderline.txt:102243, power 4, flying/trample).
**No primitive/Oracle divergence found; no Scryfall fetch was needed** - every fact I used is a
P/T, a supertype, a trigger condition or a script line, and the primitive agreed with the render
in every case.

---

# §6. LATEGAME SPECIMEN CANDIDATE (invariant 0)
1. **`130v125` seq 128-134** (turns 56-58, the corpus's longest seat log at 59 turns; seq 133 is
   my largest prompt at 25,772 chars). It carries, on one board: the `Opponent life trend:` line
   after 24 turns of history, `Their untapped sources:` against a live counter deck, the
   `{paying this costs you N life ... you would be at K}` Talisman rows on a 19-source board, a
   cycling menu with its `Decline - do nothing` third row, the `{leaves N sources - no other row
   on this menu needs more than N}` fit clause, and the HOLD row on a turn with 15 mana. It is
   the right specimen for the owner's "decision value per token" read because it is the largest
   prompt at my seats and its narration share is the corpus's problem, not its render's.
2. **Alternate, for the window-economy question: `130v126` seq 63-134** - the 72-decision phase
   of §2.4/E4. One phase, 23.6 minutes of inference, 68 declines, and a life loop the pilot could
   not answer. If the owner is to read ONE prompt about whether the loop asks too often, it is
   one of these.

---

# §7. FALSIFIABLE PREDICTIONS FOR WAVE 58

**deck123**
- **P1 (the one I care about most at this seat, and it is engine-conditional).** If lane C
  derives the menu-level all-dead line from the row verdicts: menus flagged `NO LIVE CAST ROW ON
  THIS MENU` that contain a row carrying `{kills: <name>}` or `they control N creature(s) -
  <name> is sacrificed` at N >= 1, **6 of 92 -> 0**; and declines of an N = 1 edict on the
  opponent's only creature while that header is on the screen, **3 -> 0**. **If nothing ships**,
  A57-1's scope paragraph must carry it alone and I predict **3 -> 0 or 1**; if it is still 3,
  the guide cannot beat a false aggregate at this tier and I will say so and stop trying.
- **P2 (A57-2a/A57-2b).** Attack answers that send EVERY token-maker while Intruder Alarm is on
  this deck's battlefield, **1 -> 0**. Denominator: attackers windows with an Alarm out and 2+
  makers on the list (1 this corpus, 3 across the last two).
- **P3 (A57-3 trim).** Greaves casts on a row carrying `you control 0 creatures - this equips
  nothing` stay at **0** of ~45 rows with the eleven-line block cut to four. Any cast falsifies
  the trim and I restore the text.
- **P4 (A57-5).** `[second copy:` takes on Intruder Alarm, **1 -> 0**; the Talisman/Spellbomb
  takes at deck130 stay correct N/N.
- **P5 (the ceiling measure, carried).** Board presence: windows in which deck123 controls at
  least one creature, **41.0 % overall / 25.8 % in losses this corpus.** This number moves with
  the DRAW, not with the guide. Any wave-58 claim that an edit moved this deck must move it, and
  any claim that the deck "improved" must survive its regression toward 16-25 %.
- **P6 (A57-4).** Attack answers of `none` at 5 life or less with a Fateful-hour board whose
  printed total power is at or above the opponent's life, **0**. Denominator this corpus: 1
  (`123v162` late), and the pilot could not attack there because 1.2 had already happened.
- **P7 (mulligan).** Ships stay confined to one-land sevens whose coverage line names nothing or
  one 1-mana spell: **2 of 2 this corpus.** Keeps at 2+ lands stay N/N (12 of 12).

**deck130**
- **P8 (D57-1, and it is the one I care about most at this seat).** Face-damage CAST rows taken
  at opponent life 7+ while the `Opponent life trend:` line's final parenthesis carries a `+`,
  **2 -> 0**. And, re-seated: takes on a target menu with no declining row are no longer counted
  as breaks at all - the denominator moves to the cast row, where it was 2 of 4 this corpus.
- **P9 (D57-3).** Starstorm cast rows taken while the row reads `your mana affords only X=0 right
  now`, **1 -> 0**; and cast rows taken while the YOURS list names Rorix Bladewing or Siege-Gang
  Commander, **1 -> 0**.
- **P10 (D57-5, guide half of E3).** Marked-X takes where the YOURS list is longer than the
  THEIRS list, **1 -> 0**. Marked-row takes where the marker is sound stay at **N/N** (3 of 3).
- **P11 (D57-2).** Takes of a `- SURVIVES (toughness N)` row stay at **0** (third corpus at zero);
  takes of a `- SURVIVES (loyalty N, K left)` row are no longer counted as breaks.
- **P12 (D57-4).** Creatures cast while the frame names BOTH halves of their life loop,
  **1 -> 0**. Denominator: 1 game this corpus.
- **P13 (LD, and I am predicting AGAINST tightening).** I did not change the 5+ lands clause. It
  reads 5 of 26 and three of the five are in a game this deck won by mana denial; if next corpus
  it is again 4-6 of ~25 with the same win pattern, **the clause is wrong and I will delete it
  rather than enforce it**. LD below 8 life stays at **0** (26 of 26 this corpus).
- **P14 (engine-conditional, E4).** If a loop-tick skip ships: decisions in a single (turn,
  phase) at my seats, max **72 -> under 20**; `[you declined this exact list N times already this
  turn]` max N, **43 -> under 15**. If nothing ships, expect both flat and read that as
  confirmation the class is the trigger loop, not the hold latch.
- **P15 (engine-conditional, E2).** If the land-drop arm's key is changed: land-arm drops at my
  games, **30 -> under 10**. If nothing ships, expect it flat at ~30, and the casting arm to
  track the number of converter-loop games in the corpus.

---

# §8. WHAT I DID NOT CHECK
- **The other thirty seat logs and the nine games neither of my decks is in.** Every count is over
  my own twelve seat logs unless it says "corpus-wide" and names the source. The corpus-wide
  figures I quote (3,317 decisions, 8 fallbacks, HOLD 2,490/450, `log_window` full on 3,317, the
  108 stale drops, 11 rescues, 224 `hold re-opened` lines) are either quoted from the brief or
  re-derived from `cat game-*.stderr` and are marked as such.
- **The engine source.** Nothing under `src/`, `JGE/` or `bin/Res` was read or written except
  `Res/sets/primitives/*.txt` and the two live guides I copied as my starting point. E1's
  classifier hypothesis is stated as a hypothesis from emitted strings only. I did not run wagic,
  the suite, PARSETEST, any build, any git operation or any deploy.
- **Whether the 5 slot rescues at my games consumed the RIGHT answer.** I can see
  `consuming an in-flight answer whose prompt text drifted` fired; a translog cannot show me
  whether the answer it landed was correct for the window.
- **`plan_choice_conflict_narrowed` vs never-arose.** Both stamps are 0 at my seats, same
  ambiguity my predecessor reported. Someone with firings last corpus has to answer it.
- **The D2 MDFC obligation, D22, D11, the menace header, `chooseaname`, `reveal_stall`,
  `[legendary:`, `their choice - all tied at MV`, `YOUR LAST PLAN is withdrawn`, `for {K} more
  mana`, the mulligan colour clause, `[<- best trade:`'s free form** - **0 renders each at my
  seats. Reported UNTESTED, never inferred.** Neither of my sixties runs a modal DFC, so nothing
  I write bears on the deck146/152 MDFC work.
- **stderr beyond eight counted strings.** I counted `dropping stale async answer` (with its arm
  split), `consuming an in-flight answer`, `releasing so the game advances`, `giving this decision
  to the heuristic`, `only display-toggle` (with run lengths), `hold re-opened`,
  `asyncSlotDriftKind` and `abandoned_in_flight` across my eleven files, and read ~10-line windows
  around the drops in one file. No ASAN output, no memory counter, no frame/timing line.
- **The log-window A/B (§D of the carry).** It runs after this corpus; my §3 lane-H numbers are
  the control arm only.
- **Scryfall.** No web fetch. Every card fact I used is in §5 and the primitive agreed with the
  render in every case.
