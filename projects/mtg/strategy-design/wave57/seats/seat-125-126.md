# Wave-57 seat: deck125 (3/6) + deck126 (3/6)

Corpus `matchups-20260903-174505` — FOURTEENTH fair-hand corpus, first on wave-57 step-1 (lanes
A–H + S/T/U, binary master **67b2bb38a**), model qwen36-35b-a3b on Spark :8084. Guides = wave-56
reviewer revisions (1e1dcd5ab) + wave-56 boundary pass (a93aad889). 21/21 natural, 0 SEGV.

**Volume.** deck125 **909** decisions over 6 seat logs (wave 56: 813), deck126 **588** (wave 56:
501) — **1,497 of the corpus's 3,317 (45%)**. deck125 own-turn windows 521 / opponent-turn 376;
deck126 own 217 / opp 355. Longest seat log `126v125` (448 decisions, 77 turns) and `125v126`
(298). **The corpus's largest prompt, 30,228 chars, is mine** (`125v162` seq 175). Inference on my
12 seats **6.43 h** (deck125 3.28, deck126 3.15). Latency p50 9.1 s / 6.6 s; max 137.5 s (deck125)
and **671.2 s** (deck126).

**Results.** deck125 **3W-3L** — won vs123 (t52, 30/0), vs130 (t58, 36/0), vs162 (t29, 30/8); lost
vs126 (t76, -8/80), vs146 (t18, -1/22), vs152 (t20, -3/19). deck126 **3W-3L** — won vs162 (t12,
38/-3), vs130 (t15, 38/0), vs125 (t76, 80/-8); lost vs123 (t15, -39/17), vs146 (t19, -1/22),
vs152 (t10, -1/27). Wave 56 was deck125 2-4 / deck126 3-3. **Real hands, n=6 per deck; neither
swing is attributed to a guide edit.** The corpus is the flattest win-rate band on record.

**Health (12 seat logs).** Fallbacks **5 / 1,497 (0.33%)** — `unparsed_reply` 1 (`125v126` seq
84), `named_row_reask` 3 (all deck126: `126v130` seq 8, `126v146` seq 29, `126v162` seq 11),
**`retracted_choice` 1** (`126v146` seq 34 — the corpus's only one, and it is my seat). 4
`recovery` records. `plan_choice_conflict`, `plan_choice_conflict_narrowed`, `stale_echo`,
`empty_reply`, `timeout`, `all_assignments_illegal`, `latched_row_mismatch`, `wall_miss`,
`abandoned_in_flight`: **0**. `wall_miss_events`/`wall_miss_unrecorded` **0/0 on all 12 gameends**.
`identical_option_asks_resolved` **0** on all 12. `hold_windows_skipped` deck125 534 (86/96/86/13/
16/237), deck126 **12** (all in `126v125`). `mana_only_windows_skipped` deck125 **0**, deck126
**385** (was 1,482). 0 SEGV, 0 sanitizer lines, 0 asserts in my 11 stderr files.

**Card facts.** Read off `projects/mtg/bin/Res/sets/primitives/*.txt` with `/usr/bin/grep`, or
quoted from a rendered `prompt`. Re-read and used below unchanged: Sphinx's Revelation, Staff of
Nin, Emrakul the Aeons Torn, Path to Exile, Supreme Verdict, Final Judgment, Lightmine Field,
Elixir of Immortality, Sanguine Bond, Exquisite Blood, Tribute to Hunger, Idyllic Tutor, Chromatic
Lantern, Perimeter Captain, Pride Guardian, Overgrown Battlement, Wall of Omens, Ob Nixilis the
Hate-Twisted (5 loyalty, 1 damage per opponent draw), Silverquill Silencer, Sigarda Champion of
Light, Elite Spellbinder. **No card was cross-checked against Scryfall this wave** and no
primitive/Scryfall disagreement is reported either way, because none was looked for. Two source
reads WERE made (`src/AIPlayerGPT.cpp` `rowSaysNoOp` and `assignableRemainderDamage` /
`incomingCombatLine`), read-only, and they are the root causes of HIGH #1 and HIGH #2 below.

## NEW-BASELINE record (mulligans)

| seat | offers | mulls taken | kept-hand shapes (the engine's own counts) | bottoming |
|---|---|---|---|---|
| deck125 | 6 | **0** | 2L/5S ×3, 4L/3S ×2, 3L/4S | none |
| deck126 | 6 | **0** | 2L/5S ×2, 5L/2S, 4L/3S, 3L/4S, and **1L/6S** (`126v162`, coverage `Pride Guardian {w}, Perimeter Captain {w}, Pride Guardian {w}`) | none |

Twelve offers, **zero mulligans, zero bottom asks, no chain, no mull-to-zero, no floor text, and
not one `would not cover any spell in it` line at either seat.** **No OWNER QUESTION arises.**
The one-land seven at `126v162` was KEPT against the guide's explicit "SHIP IT on a seven and on a
six" — the THIRD such keep in three corpora, and **this one WON, 38/-3 on turn 12**. W108/D110 now
reads 1 win / 1 loss / 1 (wave 55). The rule is not changed on n=3 in either direction; its cite
now carries both outcomes, and deck125's mulligan section is compressed (W57-7) precisely because
eighteen offers over three corpora have produced eighteen keeps.

---

# THREE-NUMBER LITERAL AUDIT (#188) — lanes A–E literals
# Counted from the RENDERED `prompt` (row counts too), never from the `options` array.
# HOLD takes are EXACT-matched against the row: D4 held, so exact matching is now correct (see LOW #7).

| literal | old-string renders | new-string renders | takes |
|---|---|---|---|
| lane A hold row `Hold priority for the rest of this turn: ...` | **0** of any wave-53/54 string | **1,248 prompts** (deck125 749, deck126 499) | **122** (deck125 119, deck126 3) |
| `{taking this row skips the rest of this turn's identical windows}` tail | – | **1,248 of 1,248** | present in **122 of 122 takes** — **0 tail-less** (was 23 of 138). **W120/D123 PASS** |
| own-turn vs opponent-turn HOLD | – | own **543** (125: 380, 126: 163), opp **705** (125: 369, 126: 336) | own **49** (125: 46, 126: 3), opp **73** (125: 73, 126: **0**) |
| `[you declined this exact list N times already this turn]` | – | **745 prompts** (deck125 399, deck126 346); 0 inside any option row | n/a |
| lane B `INCOMING THIS COMBAT: ...` | bare combat header 0 | **76 prompts** (deck125 32, deck126 44) across 5 forms | n/a — **D6 PASS, see below** |
| `; this KILLS you` | – | **9** (125: 4, 126: 5) | n/a |
| lane B/D24 `- best case with every blocker assigned: you would be at K` | – | **7 prompts** (all deck126) | 7 of 7 blockers windows maximised |
| lane B/D24 `- one legal assignment gets you to K` | – | **1** (`126v152` seq 14) | **THE NUMBER IS FALSE — HIGH #2** |
| lane B/D22 `BLOCKING THIS COMBAT:` header | – | **6 prompts** (deck126) | 0 declines-all-blocks under a converter — **D22 PASS 0 of 0** |
| lane B `Their untapped sources: N` | – | **1,457 of 1,457** board-bearing prompts | **D10 PASS** |
| lane B `{right now: takes N damage - they would be at K}` | K == a constant 0 | **67 rows**, K == `opp_life − damage` on **67 of 67** | 59 ping windows, 59 fired; **2 lethal rows rendered `- they would be at 0; THIS WINS THE GAME`, both taken** — **D2 PASS on both halves** |
| lane B `{blocking trigger, this combat: ...}` | – | **6 rows** (deck126) | every window blocked; see D114 |
| lane C `{removes: <name>}` | – | **378 prompts** (deck125 Path) | Path casts all at live targets |
| lane C `{right now:}` cast-row verdict | – | **668 prompts** (deck125 204, deck126 464) | see D12 below |
| lane C **`{their converter: ...}`** (D7) | – | **1 row** — `125v126` seq 293, the Emrakul row, priced `life 52 -> 37` | **TAKEN. D7's render PASSES; the guide half FAILS — HIGH-value guide item W-F4** |
| lane C `{... but an effect already on the stack is aimed at that same creature ...}` (D11) | – | **1 row** (`126v125` seq 438) | **DECLINED — D11 PASS 1 of 1**, and wave-56 MED #6 is CLOSED |
| lane C `{identical verdict right now to row N ...}` (D30) | – | **0** | **UNTESTED-AT-THIS-SEAT** |
| lane C cycle cross-price (`the cast this replaces` / `the alternative this replaces`) (D21) | – | **0** | **UNTESTED-AT-THIS-SEAT — neither deck runs a cycling card; 0 cycling rows in 1,469 prompts** |
| lane C `NO LIVE CAST ROW ON THIS MENU:` (D12) | – | **405 prompts** (the docket's grep read 0 corpus-wide; the literal is right) | **0 casts off any of the 405 — D12 PASS at a denominator of 405** |
| lane C discard verdicts (D8) | 53 of 282 tagged (wave 56) | **48 asks / 374 rows: 63 `{dead right now:}`, 24 `{right now:}`, 37 `{spare:}`, 250 bare**; legend on **44 of 48** asks | 85 sends; **8 sent a live row past a dead/spare row** (guide edit W57-5) |
| lane C `{dead right now:}` on the SWEEPER half | 0 (wave 56) | **Supreme Verdict 17, Lightmine Field 12, Path to Exile 34** | **wave-56 HIGH #2 SHIPPED for the sweeper half** |
| lane D `[NAMED BY THEIR <src>: ... - you would be at K]` | – | **13 prompts** (125: 6, 126: 7) | 2 taken; one of them is HIGH-value guide item D-F1 |
| lane D `(xN)` adjacent-event fold | – | **493 prompts** (125: 135, 126: 358); 19 folds in the 12 largest logs | **D28 PASS — 0 runs of identical adjacent events in the largest prompt of all 12 seat logs** |
| lane D `Flip Side` / `PLAY THIS AS A LAND` / `only display-toggle` | – | **0** | **UNTESTED-AT-THIS-SEAT** — neither deck runs an MDFC |
| lane E `{X pricing:}` cast-row clause | – | **415 prompts / 437 rows** at deck125 | 5 Revelation casts |
| lane E ANNOUNCE_X rows (D9) | 40 of 40 with no fit clause (wave 56) | **7 menus / 28 rows, 28 of 28 carrying `{leaves N of your M floating mana unspent}`** | **D9 PASS at this seat** — see the X adjudication below |
| lane E `[<- largest affordable X ...]` / `[<- best X for this cast: ...]` | – | **408 prompts**; marker on row 1 of **7 of 7** X menus | 4 of 7 menus answered the marked row |
| lane E `Opponent life trend: ... since turn N` (D15) | 1 of 137 (wave 55) | **1,457 of 1,457 board-bearing prompts** | **D15 PASS** |
| lane E pregame `counted TWICE` note (D2) | – | **0** | **UNTESTED-AT-THIS-SEAT** (no MDFC in either deck) |
| lane E `{feeds:}` | – | **0** | UNTESTED-AT-THIS-SEAT |
| lane C/E `[RE-ASK]` (D3) | – | **3 prompts**, all deck126, all `"Cast Sanguine Bond" is not on this list` | **D3 PASS on the emitter**; the model recovered on 2 of 3 — the third is D-F1 |
| `[second copy: ...]` | – | **23 rows** (126: 19, 125: 4) | **2 takes** (`126v125` seq 306 Lantern #2, seq 331 Bond #2) — was 4 |
| `{paying this taps: <defender> ...}` | – | **17 rows** (deck126) | 6 |
| `Cast <card> [from exile] {...}` | – | **3 rows** (deck126) | **1**, correct (`126v152` seq 13) |
| `[named: <card>]` battlefield tag | – | **44 prompts** (126: 23, 125: 21) | **0 casts of a named card** where the price read lethal |
| `LIFE-TO-DAMAGE CONVERTER on the battlefield` | – | **572 prompts** (125: 207 `theirs`, 126: 365 `yours`) | **the deck125 loss is a decision taken with this paragraph on screen** |
| `DRAW PUNISHERS on the battlefield: theirs` + `DRAW FORECAST:` | – | **52 prompts** (125: 43, 126: 9), FORECAST on all 52 | **the wave-56 vs162 loss is now a WIN off this line — W110 PASS** |
| `[DRAW PRICE: ...]` | – | **3 prompts** (deck126) | – |
| `[menace - ...]` | – | 3 prompts (deck125) | n/a |

Battlefield header land counting: every battlefield line on both seats reads `... and N are lands`.
PASS. **Land drop deck125 83/83, deck126 49/49 — 132 of 132, 0 declines, 75 of them single-land
menus with exactly one right answer.**

---

# deck125 — Revelation Fracture (UW draw-go). 3W-3L

## Prediction set A — wave-56 guide edits (W110–W114)

**W110 (Staff ping target) — PASS, and it is the wave's cleanest edit result.** 59 ping windows,
0 declined. **5 carried `DRAW PUNISHERS on the battlefield: theirs` and ALL FIVE shot Ob Nixilis,
the Hate-Twisted** (`125v162` seqs 27, 35, 48, 50, 66 — turns 13 to 17). **54 with no punisher
line, 54 at the face.** Predicted 0-at-the-face-under-a-punisher: **0 of 5**. Wave 56 was 8 of 8
at the face and lost that game; `125v162` this corpus was WON 30/8. (Real hands: the WIN is not
attributed to the edit; the 5/5 decision record is.)

**W111 (Emrakul under a converter) — FAIL 1 of 1, and it is the whole `vs126` game.**
`125v126` **seq 293**, turn 77, **52 life against their 20**, 21 lands. The row now prices itself —
lane C's D7 shipped on exactly this row — and the seat took it anyway. Full evidence under the
engine list's discussion of D7 and in `deck125/edit-texts.md` item 6/7. `126v125` seq 437 Tributed
it, the Bond converted 15, Exquisite Blood fed it back: **52 → −8 while they went 20 → 80, in one
turn.** Fifth corpus, fourth loss to this row.

**W112 (second-Staff gate) — PASS, no window.** 0 Staff casts at `casting this taps you out` with
a Staff already on the battlefield line. FIRST-Staff casts at that string: 2 of 2.

**W113 (counter discipline) — PASS on the wall half, FAIL 2 of 39 on the mana-rock half.**
39 counterspells cast; every stack line answered was a real threat except `125v126` seq 212 (Cancel
on a Chromatic Lantern at **47** life) and `125v130` seq 35 (Dream Fracture on a Talisman of
Impulse at 28). **Counters at a `[defender]` or printed-power-0 stack line: 0 of 39** (wave 56: 1).
Counters spent on land destruction while on 6+ lands: 0. Neither rock counter decided a game.

**W114 (ceilings) — PASS on every branch that had a window, and one of them is now a LIABILITY.**
Land drop **83/83**. Sweeper at a dead header **0 of 45 rows**. Low-X Revelation **2 taken of 104
rows**, and both takes are the low-life exception firing correctly. `X = 3` — see the X
adjudication: the rule was OBEYED 3 of 3 and the rule itself is wrong. Path at an
empty-`{target text:}` power-0/1 candidate **0**. Cleanup counterspell past an unsent land **0**
(fourth corpus). Elixir activations 12 of 23 rows. **Lightmine Field 0 casts of 24 rows — and one
of those 24 declines is the `vs146` loss** (W-F3).

## The X-menu adjudication (this seat owns it)

**Lane E D9 — PASS at this seat: 28 of 28 ANNOUNCE_X rows carry the pool-priced fit clause**
(`{leaves N of your M floating mana unspent}`, and on row 1 `- this X spends your whole pool`),
against wave 56's 40 of 40 with none. The mechanism lane E predicted is visible in the window
itself: `Mana available: 0 total (no untapped sources) | Already in pool: {u}{u}{w}{w}{w} (5 mana
ALREADY produced and floating right now)`. **7 menus at deck125, and these are the corpus's only
X menus I can see from here** (the docket's 55 X rows are corpus-wide and include cast-row
`{X pricing: max affordable X=N}` clauses, of which my seat alone renders 437 — the two shapes must
not be pooled).
Also **7 cast rows** read `{X pricing: your mana affords only X=0 right now; ...}` and carry no
`leaves`/`spends` clause. I count these as PASSING (the clause states the pool affordance in
words) and flag the wording split for the emitter's owner: three different fit-clause forms now
exist for one fact.
Largest-first: **7 of 7** menus print `option 1 is the LARGEST X` and mark row 1 `[<- largest
affordable X ...]`. Monotone collapse: **1 of 7** (`125v126` seq 80, `2-5. X = 5 down to X = 2`),
answered option 1 — in range. **0 out-of-range ANNOUNCE_X answers.**
**The behaviour, and it is a guide failure the render cannot fix:** four of the seven menus were
answered at option 1, the largest X (largest = 3, 6, 1 and 2 — `125v126` seq 43 and seq 80,
`125v146` seq 14, `125v152` seq 45). In the other three, whose largest X was **5, 4 and 4**, it
answered **X = 3 every time** (`125v126` seq 62,
`125v130` seq 68, `125v152` seq 50) — leaving 2, 1 and 1 floating mana unspent, i.e. **4 life and
4 cards binned**, one of them **at FIVE LIFE in a game lost at −3**. The guide's `YOUR X IS 3` rule
was obeyed exactly; the rule is wrong, because at the announce window the mana is already out of
the lands and a smaller X saves nothing it could spend later. Edit W57-1.

## D21 / D30 adjudication (this seat owns it)

**D21 — UNTESTED-AT-THIS-SEAT.** Neither deck runs a card with cycling or any other cast-mode
alternative: `the cast this replaces` and `the alternative this replaces` render **0 times** in
1,469 prompts, and **0 option rows in the whole seat pair mention cycling**. The 202 prompts
containing the word "cycling" carry it inside an opponent's `{card text:}` blob only. The docket's
"cycle takes at opp life ≤ 6 > 3 of 17" cannot be adjudicated from here.
**D30 — UNTESTED-AT-THIS-SEAT.** `{identical verdict right now to row ` renders **0 times**. My
menus do carry the shape the tag is for (`125v130` seq 14 prints two Final Judgments with
byte-identical verdicts) — but they are the SAME card, which is D15's tag, not D30's cross-card
one, and D15 did not fire on them either. So the lane's "renders at least once" prediction is
neither confirmed nor refuted here; the pair that would test it is a cheaper card with an identical
verdict, and neither of my decks contains one.

## The three wins (deck125)

- **vs deck162 (t29, 30/8)** — the wave-56 loss, flipped, and the flip is decision-level: 5 pings
  at Ob Nixilis under the DRAW PUNISHERS line (W110), 8 counterspells including two Underworld
  Dreams, a Peer into the Abyss and two Liliana's Caress, and 0 Revelations cast under a punisher.
- **vs deck123 (t52, 30/0)** — the specimen's own game. One Staff from turn 13, Intruder Alarm
  countered on sight twice, 13 cleanups with no misordered send.
- **vs deck130 (t58, 36/0)** — 8 counters, four of them on land destruction while on 2–5 lands
  (sanctioned by W-E5 last wave, and correct again), finished on 36 life.

## The three losses (deck125)

- **vs deck126 (t76, −8/80) — the Emrakul row, at 52 life against their 20, under a printed price.**
  `125v126` seq 293. STRATEGY, and the render is now complete. Edits W57-4 and W57-4b.
- **vs deck146 (t18, −1/22) — SEVEN declines of a life-gain row at THREE LIFE.** `125v146` seqs
  16, 17, 19, 20, 21, 22, 23. At 3 life against a board whose able attackers totalled 7 power, the
  menu carried `Cast Sphinx's Revelation ... max affordable X=2` (2 life) and, at seqs 19–20,
  `Cast Lightmine Field {2}{w}{w} {right now: they control 2 creatures able to attack - deals 2 to
  each if all 2 attack}` against a Goblin (1/1) and a Silverquill Silencer (3/2) — both dead to 2.
  The seat answered `Cast nothing right now` at every one and died at −1 to those same bodies.
  Two guide rules held it there: the max-affordable-3 floor whose exception was 70 lines away, and
  the Lightmine "never below 3 attackers" floor. Edits W57-2 and W57-3. **The `[NAMED BY THEIR
  Silverquill Silencer ... you would be at 0; this KILLS you]` Supreme Verdict row on the same menus
  was correctly declined 4 of 4** — the seat read that price perfectly.
- **vs deck152 (t20, −3/19) — real hands plus one X.** Lands on t2/t4/t6 then none until t12; three
  Revelations stranded at max X=0 for six turns. The one lever is `125v152` seq 50: at 5 life,
  X = 3 off a max-X=4 menu (W57-1). 1 life would not have covered the 4 damage that killed it, so
  this is classified **NO LEVER with a counted waste**, not a decided misplay.

## Other counted behaviour (deck125)

- **Opponent-turn windows 376, real casts 41 — and 237 of the 376 are at nine phases that produced
  ONE cast between them all corpus.** By phase (casts in brackets): Main 1 **112 (40)**, Upkeep 67
  (0), Draw 60 (1), Combat begins 35 (0), Attackers 28 (0), Combat ends 22 (0), Main 2 21 (1),
  End 15 (0), Untap 6, Cleanup 6 (0), Blockers 2 (0), Combat damage 2 (0). Sixth corpus running at
  zero for End / Cleanup / Combat-ends. MED #4.
- HOLD row **749 renders / 119 takes = 15.9%**; own-turn 46 of 380, opponent-turn **73 of 369 =
  19.8%**. Lane A's ">= 20%" opponent-turn target: **misses by 0.2 points** (wave 56: 23.6%).
- Identical-declined runs of 3+ (same turn+phase, byte-identical MENU ROWS): **32 / 897 = 3.6%,
  max run 6** (wave 56: 23.7% / 9). Targets `< 15%` **PASS**, `max < 10` **PASS**. My predicate is
  turn+phase+rendered option rows; wave 56's may have been the whole prompt, so the improvement is
  partly definitional — the max-run figure is the comparable one.
- **D1 byte-identical same-turn re-ask after a HOLD take: 0. PASS.** D1's stated risk — games lost
  while a hold stood with a legal answer: **0**; every HOLD take at a life total the following
  combat could reach was checked by hand.
- Decisions inside one mandatory life-loss loop phase: **max 3** (`125v162` t16 Draw). Lane A's
  `< 5`: **PASS** (was 6, and 16 the corpus before).

---

# deck126 — Sanguine Blood (WGB wall prison). 3W-3L
# **THE ZERO-HOLD-TEXT CONTROL (skill #233; #299 sunset recorded) — third corpus, and the gap widened.**

**The control reads. deck126's guide contains the string "Hold priority" ZERO times, the
declined-note zero times, and no instruction about the hold row of any kind — and my wave-57
revision adds none. deck125's carries a seven-line block naming the row. Same binary, same model,
same corpus, same emitter string on 1,248 rows:**

| | HOLD renders | takes | take rate | opp-turn take rate |
|---|---|---|---|---|
| deck125 (guide teaches the row) | 749 | 119 | **15.9%** | **19.8%** |
| deck126 (control, no text) | 499 | 3 | **0.6%** | **0.0% (0 of 336)** |

Wave 55 read 10.1% / 1.5%; wave 56 18.5% / 2.7%; wave 57 **15.9% / 0.6% — a 26× ratio, up from
7×**. `hold_windows_skipped` sums 534 vs 12. All three deck126 takes are own-turn.
**I am keeping deck126 text-free.** Three corpora at a stable, large ratio is now a settled result:
the row's uptake is overwhelmingly the GUIDE, not the row. Nothing in this seat's six games was
lost to a window deck126 declined, and its wasted windows are absorbed by
`mana_only_windows_skipped` (385). The control's remaining job is to stay a control.

## Prediction set A — wave-56 guide edits (D112–D117)

**D112 (Tribute into your own answer on the stack) — PASS 1 of 1, at the exact shape.**
`126v125` seq 437 cast Tribute at Emrakul; seq 438 re-offered the identical row now carrying lane
C's D11 clause `- but an effect already on the stack is aimed at that same creature; if it is gone
when this resolves they control 0 creatures and this does nothing`, and the seat **declined**.
Redundant second Tributes: **0 of 12 casts** (was 3 of 10). Wave-56 MED #6 is closed on both sides.

**D113 (dead rows) — FAIL 3, improved.** `[second copy:` takes **2 of 23** (was 4): `126v125` seq
306 (Chromatic Lantern #2 at turn 56) and seq 331 (Sanguine Bond #2 at turn 60), both in a game won
by 88. Dead Idyllic Tutor (`every enchantment left in your library is a copy of one you already
control or hold`) takes **1 of 10 rows** (was 3 of 17) — `126v146` seq 31, and it is the loss (D-F1).
Live tutor rows 13, taken 6. `Cast nothing right now` answered while a LIVE cast row was printed:
**0** (was 1).

**D114 (blockers) — PASS on both halves, 9 of 9.** Nine blockers windows, **every available blocker
assigned in all nine**, `BLOCKS: none` **0** (was 1 of 7), blockers left out **0** (was 2 of 7),
`dropped_assignments` 0. At the three `; this KILLS you` headers the assignment took the maximum
available every time — including `126v146` seq 33, where one blocker against 5 attackers stopped
the largest (3 of 7) exactly as the header's `best case ... you would be at 2` said. **The two
wave-56 blocker edits (D-E3/D-E4) are at their ceiling.**

**D115 (reveal picks) — FAIL 1 of 5.** 5 reveals, `reveal_stall` 0, wait fields 5 of 5 (D12/lane E
PASS again). Four picks fetched the missing half. `126v146` seq 32 named **Sanguine Bond while
Sanguine Bond was already on the hand line** — off the dead tutor of D-F1, so the bad reveal is
downstream of the bad cast, not a separate decision.

**D116 (Tribute shapes) — PASS on the zero stop at a huge denominator, FAIL on first-window.**
Tribute rows **456**, of which **445 read `at 0 this does nothing` and 0 were taken** (fourth
corpus at zero; wave 56: 0 of 296). 9 Tribute casts. 7b rows (`1 creature - <name> is sacrificed`,
victim not `[defender]`) taken in their FIRST window: `126v123` seq 11 (Thraben Doomsayer) yes;
`126v146` seq 7 (Triumphant Adventurer, 1/1 deathtouch) **declined** — 1 of 2. Tribute cast at a
`[defender]` victim above 10 life with no pair out: **0** (was 1). Zero-Tribute takes **0**.

**D117 (lone Vampire attack) — UNTESTED.** deck126 was offered **no `attackers` window at all**
this corpus: Sorin resolved in two games but no attack was ever declared. 0 of 588 records are
`attackers`.

## The three wins (deck126)

- **vs deck125 (t76, 80/−8)** — 448 decisions, the corpus's longest seat log. Won on deck125's
  Emrakul: `126v125` seq 437's Tribute, with Sanguine Bond ×2 and Exquisite Blood out, converted 15
  into a loop that ran 20 → 80 and 52 → −8 in one turn. The seat's own play in the preceding 70
  turns was patient and correct bar two second copies.
- **vs deck162 (t12, 38/−3)** — the one-land keep, and it WON: Sanguine Bond t11 off a live tutor
  reveal, Exquisite Blood, a 7b Tribute on a Fate Unraveler, and the pair closed it on turn 12.
- **vs deck130 (t15, 38/0)** — Idyllic Tutor → Exquisite Blood, Bond t11, Sorin, and two Tributes.

## The three losses (deck126)

- **vs deck146 (t19, −1/22) — FOUR consecutive breaks of the seat's own Rule #7, and the guide's
  entry 1 was printed on the menu for three of them.** seq 24 (t16, 13 life): row 1
  `Cast Exquisite Blood {4}{b} {... casting this taps you out}` → cast an Overgrown Battlement.
  seq 28 (t18, 10 life): row 1 `Cast Exquisite Blood {4}{b} {leaves 1 of your 6}` → cast a
  Chromatic Lantern, off SIX sources, outside the Lantern carve-out's own ≤4-source shape. seq 29/31
  (t18): a `[RE-ASK]` refusing `"Cast Sanguine Bond"`, answered with the identical name, so the
  NUMBER executed a dead Idyllic Tutor carrying a 3-life `[NAMED BY THEIR Silverquill Silencer]`
  price. seq 34 (t20, 2 life): the `retracted_choice` reply. **It died at −1 holding both
  enchantments.** Edits D57-1, D57-2, D57-3, D57-5.
- **vs deck152 (t10, −1/27) — six declined live Tribute rows under two unblockable fliers.** seq 5
  (t6, 17 life), seq 7 and 8 (t8, 9 life — cast a Perimeter Captain then an Overgrown Battlement),
  seq 11 (t10, 1 life — cast a Wall of Omens). Every wall it cast was printed as unable to block
  an Elite Spellbinder (4/2 flying) or a Sigarda (6/6 trample, flying), which carried 10 of the 13
  incoming. Edit D57-4. This is also the game carrying **engine HIGH #2**.
- **vs deck123 (t15, −39/17) — NO LEVER.** A Thraben Doomsayer + Intruder Alarm engine made 74
  attackers by turn 16 (`126v123` seq 25: `INCOMING THIS COMBAT: 74 attackers, 75 unblocked damage
  - you would be at -48; this KILLS you`). All 5 blockers were assigned. Second corpus running that
  this matchup produces the same unanswerable board.

## Other counted behaviour (deck126)

- **Opponent-turn windows 355, real actions 16 (10 of them blocks).** Upkeep 36, Draw 36, Main 2
  35, Combat begins 34, Attackers 34, Combat ends 34, End 34, Cleanup 34, Main 1 62 (6), Blockers
  10 (10), Untap 6. **279 of 355 at phases that produced nothing.** MED #4.
- Identical-declined runs of 3+ (same predicate as deck125): **17 / 572 = 3.0%, max run 8** (wave
  56: 41.2% / 13). `< 15%` **PASS**, `max < 10` **PASS by 2**.
- 0 `attackers` windows; 9 `blockers`; 5 `reveal`; 3 `priority`. The Battlement `variable_output`
  line renders on every prompt with a Battlement; the upkeep-tap window reached the model **0
  times** in six games (385 passed by the engine).

---

# Prediction set B — engine lanes (where my seats had windows)

| lane | verdict | evidence |
|---|---|---|
| **A** (D5/D4/D16/D31) | **SPLIT: D4 PASS outright; D1/D11/D16 PASS; D5 FAIL, and my seats own 79% of the corpus's drops** | **D4 PASS**: 122 of 122 HOLD takes are the rendered row byte-for-byte including the skip-note tail (was 115 of 138) — **exact-match censuses are trustworthy at this seat and the wave-55 boundary defect is CLOSED**. `chosen_text` empty with `choice >= 0`: 0. **D1 PASS**: 0 same-turn byte-identical re-asks after a take; 0 games lost under a standing hold. Identical-declined runs 3.6% / 3.0% against `< 15%` — **PASS at both seats** (was 23.7% / 41.2%); max run 6 / 8 against `< 10` — **PASS at both** (was 9 / 13). Mandatory-loop max 3 against `< 5` — **PASS**. **D16/D11 PASS**: `plan_choice_conflict` 0, `_narrowed` 0, `decision_reversed_in_prose` 0. **D5 FAIL**: **85 stale drops in my 11 stderr files** against a target of < 20 corpus-wide — **45 casting arm, 38 land-drop arm, 2 casting+board**. The second slot did NOT close the land-drop arm (wave 56 predicted 0 there; 38 remain). The rescue line fired **9 times** (`125v126` 4, `125v130` 4, `152v126` 1). See MED #6. |
| **B** (D6/D24/D22/D10/D13) | **D6 PASS, D22 PASS, D10 PASS, D13 PASS — D24 FAIL 1 of 8 and the failure is HIGH #2** | **D6 PASS, and wave-56 HIGH #1 (mine) is CLOSED.** 201 opponent-turn combat-phase windows across my seats; 76 carry a line, 125 do not — and **0 of those 125 had a creature able to attack**. deck125, which controls no creature by design, now sees the total on all 32 of its combat windows that could carry one (was 0 of 68). Forms observed: closed 54, declared 12, forecast 4, unblockable-split 2. **D24**: 7 `best case with every blocker assigned` lines, all 7 arithmetically consistent with the board; the 8th, `one legal assignment gets you to 1`, is **false and unreachable** — HIGH #2. **D22 PASS**: 6 `BLOCKING THIS COMBAT:` headers, 0 declines-all-blocks. **D10 PASS** 1,457/1,457. **D13 PASS**: 0 B-lines nest the gain inside the survival verdict. |
| **C** (D7/D8/D11/D30/D21/D12) | **D7 PASS, D11 PASS, D12 PASS, D8 PARTIAL; D21 and D30 UNTESTED-AT-THIS-SEAT** | **D7 PASS 1 of 1** — the tag rendered on the one creature cast row this seat pair ever printed under a converter of theirs, with the right toughness, the right converter count and a finished subtraction. (Denominator note: the docket's "80 of 82 creature cast rows lack `{their converter:`" is not reproducible at my scope — deck125 owns exactly ONE creature and deck126's converters are its OWN, so my seats contribute 1 row to that population, not 82.) **D11 PASS 1 of 1.** **D12 PASS**: 405 menus carrying the all-dead token, **0 spends off any of them**. **D8 PARTIAL**: bare rows 250 of 374 (66.8%, was 81.2%); the sweeper half SHIPPED (Supreme Verdict 17 dead / Lightmine 12 dead / Path 34 dead) and the legend prints on 44 of 48 asks — but the zero-predicate is verb-gated and misses "exiles 0", so **5 of 5 all-zero Final Judgment rows are tagged `{right now:}`**: HIGH #1. **D21/D30 UNTESTED** — see the adjudication above. |
| **D** (D14/D26/D27/D28/D29/D13) | **D28 PASS; D14/D26/D27 UNTESTED-AT-THIS-SEAT** | **D28 PASS**: 0 runs of identical adjacent events in the largest prompt of each of my 12 seat logs, with 493 prompts carrying `(xN)` folds. Neither deck runs an MDFC, so `Flip Side`, `PLAY THIS AS A LAND`, the arrival clause and `only display-toggle` render 0 times and are UNTESTED here; **0 `only display-toggle` lines in all 11 of my stderr files**. D13 narration is MEASURED at this seat: see LOW #8. |
| **E** (D2/D9/D15/D19/D20/D23) | **D9 PASS, D15 PASS, D20 PASS; D2 UNTESTED** | **D9 PASS 28/28** at this seat (was 0 of 40) — the full adjudication is above. **D15 PASS 1,457/1,457**. **D20 PASS**: the `[<- ...]` marker is on row 1 of 7 of 7 X menus and on every `best X for this cast` clause; marked-row takes 4 of 7 (the 3 misses are the guide's own X=3 rule, not the marker). **D2 UNTESTED** — no MDFC. |
| **F / S / T / U** | **PASS on every observable** | 0 SEGV, 0 sanitizer lines, 0 asserts, 0 `abandoned_in_flight`, 0 `giving this decision to the heuristic`, 0 `chose -1 of N`, 0 `0<name>` refusals, 0 phase-stuck games in 11 stderr files. Both my long games (`125v126` / `126v125`, 77 turns, 746 decisions between them) ran to a natural end. The `WAGIC_STALLPROBE` line never appears. |
| **G / H** | **G UNTESTED (human-seat render only); H control arm consistent** | `log_window` is `full` on every record I read and 0 records carry an elided-turn count. My per-kind prompt census (LOW #8) is the arm-verification data lane H asked for. |
| **M, P, Q, I, J, N, O, V** | **UNTESTED-AT-THIS-SEAT** | Not written to the translog and I did not instrument. Lane V (Baka blocker policy) is not in this binary. |

---

# Engine / render items (ranked, with repro)

Corpus root: `~/.Wagic/ai/gpt/selfplay-runs/matchups-20260903-174505/`

**HIGH — #1: `rowSaysNoOp` is a VERB WHITELIST, so a zero-magnitude row whose verb is "exiles" is
badged live — on the same menu as an identical zero badged dead.**
Repro: `1788475532-ai_baka_deck125-0x563d79b012b0-vs-ai_baka_deck130.jsonl` **seq 14**. The cleanup
menu prints, three rows apart:
```
1. Final Judgment (copy 1 of 2 in your hand) {4}{w}{w} (sorcery) ... {right now: exiles 0 of their creatures (0 without a restriction against attacking), 0 of yours}
5. Supreme Verdict {1}{u}{w}{w} (sorcery) ... {dead right now: destroys 0 of their creatures (0 without a restriction against attacking), 0 of yours}
```
Same board, same three zeroes, two different families. Root cause, read from
`projects/mtg/src/AIPlayerGPT.cpp:19862`: `kNoOp` is `{"does nothing", "deals 0", "destroys 0",
"kills 0", "removes 0", "drains 0", "does not apply", "gains 0", "draws 0"}` — **"exiles 0" is not
in the list**, and Final Judgment's Oracle verb is *exile*. Census at my seats: **5 of 5 all-zero
`{right now:}` discard rows are Final Judgments; 0 non-zero rows are mis-badged in the other
direction.** Blast radius is three surfaces, because lane C made this predicate the shared
definition of "dead": D8's discard badging, **D12's `NO LIVE CAST ROW ON THIS MENU` token (which
can never fire on any menu holding a Final Judgment)**, and the guard at `AIPlayerGPT.cpp:11561`.
Ask: the predicate should read the NUMBER in the clause, not the verb in front of it — every
verdict this engine writes has the shape `<verb> N of ...`. A verb whitelist will keep silently
acquiring holes as cards are added. **Core-loop correctness, and it is one function.**

**HIGH — #2: `assignableRemainderDamage` drops a trampler's damage from its own baseline, so the
blockers header prints a survivable number on a board that is lethal no matter what.**
Repro: `1788475557-ai_baka_deck126-0x557a691d3160-vs-ai_baka_deck152.jsonl` **seq 14** (turn 11,
5 life). Header: `INCOMING THIS COMBAT: 3 attackers, 13 unblocked damage - you would be at -8; this
KILLS you (of that, 10 from 2 attackers none of your creatures can block) - one legal assignment
gets you to 1`. **"1" is unreachable**: 10 of the 13 damage comes from an Elite Spellbinder (4/2
flying) and a Sigarda (6/6 trample, flying) that no blocker in the game can block, so the floor is
5 − 10 = −5 (−1 if both `may gain 2` triggers are counted as certain). Arithmetic of the defect:
`faceDamage[j]` is deliberately 0 for a trampler (the comment at `AIPlayerGPT.cpp:14991` says so),
and `assignableRemainderDamage` then computes `total` by summing that SAME clamped vector — so
Sigarda's 6 vanishes from the residual as well as from the matching weight. 13 − 6 (Sigarda) = 7
total; minus the 3 the two walls can stop = 4; 5 − 4 = 1. `bestCaseOptimal` correctly downgrades
the WORDING to "one legal assignment", but the NUMBER is optimistic by the whole trample damage,
which is the opposite of the safe direction on a lethal window.
**And the model caught it, at cost.** `126v152` seq 14's reply spends ~250 words re-deriving the
true number, writes "The prompt's hint ... seems contradictory to the math", "The hint is confusing
or I am missing a mechanic", and blocks correctly anyway. That is the trust doctrine being taught
in reverse by the engine: this is the first record I have seen of a rendered number training the
pilot to distrust the render. Ask for lane B: keep two vectors — `weight[j]` (0 for tramplers, for
the matching) and `face[j]` (true unblocked damage, for the baseline) — so the printed residual is
never below what the board can actually do to you.

**HIGH — #3: the `{their converter:}` price knows about the converter and not about the LOOP the
same prompt has already detected.** `1788475554-ai_baka_deck125-0x5639221d93e0-vs-ai_baka_deck126.jsonl`
**seq 293**. The row reads `... takes 15 off YOU: life 52 -> 37`, and forty lines above it the same
prompt reads `Both halves of a life LOOP are on THEIR battlefield (Sanguine Bond + Exquisite Blood)
... chains until YOU are at 0 - so ANY nonzero payment on a tag above is fatal`. The row's own
finished subtraction therefore UNDERSTATES by the whole game: 37 is a number you can look at and
accept at 52 life, and 0 is the truth. Every other price in this engine is finished at the row;
this one is finished at the paragraph. Ask: when the loop predicate that writes that paragraph is
true, the tag's tail should read `- and with <other half> also on their battlefield this LOOPS: you
would be at 0` instead of the single-iteration total. Guide item W-F4 stands either way — the seat
should not have taken the row — but this is the fourth corpus in which the row's own arithmetic is
the most reassuring number on the menu.

**MED — #4: 237 of deck125's 376 opponent-turn windows and 279 of deck126's 355 sit at phases that
produced ZERO actions across six games each.** deck125: Upkeep 67, Draw 60 (1 cast), Combat begins
35, Attackers 28, Combat ends 22, Main 2 21 (1), End 15, Cleanup 6, Blockers 2, Combat damage 2.
deck126: every phase but Main 1 and Blockers. Repro for the pure shape:
`1788475554-ai_baka_deck126-0x5639234f1300-vs-ai_baka_deck125.jsonl` (448 decisions, 77 turns).
Sixth wave running. The HOLD row is the partial answer and it is now taken 19.8% of the time on
deck125's opponent turns — and **0.0% on deck126, which is not taught it**. Ask, unchanged: an
opponent-turn window whose `ON THE STACK` section is EMPTY, at a phase where this seat has never
acted in this game, is the natural companion to `mana_only_windows_skipped`, which deck126 already
uses 385 times with no ill effect.

**MED — #5: a reply that writes `CHOICE: n` and then reasons its way to a different row is REFUSED,
and the refusal spends the window.** `1788475522-ai_baka_deck126-0x561708c8f820-vs-ai_baka_deck146.jsonl`
**seq 34**: line 1 is `CHOICE: 3 (Cast Staff of Nin)`; ~900 words later the same reply closes
`So CHOICE: 1.` and its PLAN line describes casting row 1. The engine stamped `retracted_choice`,
`choice: -1`, `chosen_text: <refused: retracted_choice>`, and a `recovery` followed. The seat was
at 2 life on turn 20 and died that turn; **row 1 (Cast Exquisite Blood) was the play its own
reasoning had reached.** The current policy (first line wins, retraction refuses) is defensible and
the guide half is edit D57-2 — but on a reply that terminates naturally, the LAST `CHOICE:` line is
the model's considered answer and the first is its snap one. Ask for the core loop: when a reply
carries exactly two `CHOICE:` lines, both naming legal rows, and the second is the last thing
written, take the second and stamp it (`choice_revised`) rather than refusing the window. Corpus
frequency is 1 in 3,317 — the ranking is MED because the cost per occurrence is a whole decision at
2 life, not because the rate is high.

**MED — #6: the second async slot did not close the land-drop arm, and my seats own 79% of the
corpus's stale drops.** `game-126v130-1788475528.stderr` (29), `game-125v123-1788475508.stderr`
(16), `game-125v126-1788475542.stderr` (15), `game-125v130-1788475526.stderr` (8),
`game-125v162-1788475520.stderr` (8). Classified by the drop line's own arm token across my 11
files: **45 `(casting arm; the question (or turn/phase) moved)`, 38 `(land-drop arm; ...)`, 2
`(casting arm; the question and board moved)`** — 85 of the corpus's 108. The land-drop drops are
followed immediately by the same land-drop question being re-asked and answered
(`AIPlayerGPT: Land drop: which land do you play now, if any? ... -> chose 1 of 3`), so each is a
full round trip spent twice. The rescue line fired 9 times, so the slot works and its key still
does not match at this boundary. **The cheaper ask is still the wave-56 one and this corpus prices
it: 132 land-drop asks at my seats, 132 lands played, 0 declines, 75 of them menus with exactly one
`Play <land>` row.** At 27.5 s per decision that is ~1 hour of inference for a forced choice.

**MED — #7: `INCOMING THIS COMBAT` is gated on the COMBAT PHASES, so the windows where the seat can
still act about the attack do not carry it.** 20 opponent-turn windows at my seats print a board
with attackers ready and no total: **Main 1 10, Upkeep 6, Draw 4**. The sharpest repro is the
deck125 `vs146` loss: `1788475540-ai_baka_deck125-0x55fae9e3c7c0-vs-ai_baka_deck146.jsonl` **seq
23**, their Draw step, seat at **3 life**, opponent line reading `3 are creatures, 3 of them able
to attack right now` with a Goblin (1/1), a Hive of the Eye Tyrant (3/3) [menace] and a Silverquill
Silencer (3/2) — **seven power against three life, and no number anywhere in the prompt**. The
`forecast` form already exists (`not declared yet - N of their creatures can attack, for up to M`)
and rendered 4 times, all at `Combat begins`. Ask: extend that same form to the opponent's Upkeep,
Draw and Main 1 whenever `ableAttackers > 0` — it is the last window in which an instant of yours
can change the answer, and it is the one that has no number.

**LOW — #8 (INFO): the per-ask-kind prompt census, for lane H's §D arm verification.**
1,469 prompt-bearing decisions at my seats, **20.29 MB, narration 67.6% of the mean prompt** (the
corpus baseline is 60.8%; my two decks are the long-game decks). By kind — n / mean chars / mean
narration chars / narration share / total MB:
`cast-or-ask` 1,176 / 14,240 / 9,667 / 67.9% / **16.75 MB** · `priority` 80 / 15,703 / 10,970 /
69.9% / 1.26 · **`land-drop` 132 / 9,402 / 6,194 / 65.9% / 1.24** · `discard` 48 / 16,301 / 10,992
/ 67.4% / 0.78 · `blockers` 9 / 12,032 / 6,991 / 58.1% / 0.11 · `reveal` 5 / 13,718 / 4,630 /
33.8% / 0.07 · `announce-X` 7 / 9,412 / 5,938 / 63.1% / 0.07 · `mulligan` 12 / 1,488 / 0 / 0% /
0.02. The mulligan ask is the existence proof that a kind-scoped window is already possible and
costs nothing.

**LOW — #9 (INFO): three different fit-clause wordings now exist for one fact.** The X pricing
clause renders as `{X pricing: max affordable X=N (M mana total); ...}` on 401 cast rows, as
`{X pricing: X=N - you gain N life...}` + `{leaves K of your M floating mana unspent}` on 28
announce rows, and as `{X pricing: your mana affords only X=0 right now; ...}` on 7 cast rows with
no `leaves`/`spends` clause at all. All three are true; a reviewer's predicate for "carries a fit
clause" has to know all three, and the third is the one a strict grep misses. Not a defect — a
note for whoever writes the next census.

---

# Lategame specimen — decision-value read (this seat owns it)

`wave57/lategame-specimen.txt` = `1788475511-ai_baka_deck125-0x5582d4862110-vs-ai_baka_deck123.jsonl`
**seq 176**, turn 45, `ask`, **15,319 chars**, 2 rows. Split measured on the file itself:
**narration 11,902 (77.7%) | current situation 3,416 | question + menu + protocol 1** (the
question and its two rows sit inside the situation block for this ask kind).

**The decision.** `Land drop: play Seachrome Coast now?` — `1. Play Seachrome Coast |
2. Hold Seachrome Coast - do not play it now`. Board: the seat at **30 life**, opponent at **5**,
opponent battlefield **0 creatures** and 14 lands plus an Intruder Alarm, the seat with **16
untapped sources**, a Staff of Nin already ticking, and a hand of 9 cards whose most expensive card
is a Fall of the Gavel at 5 mana.

**Decision value: zero, and the prompt spends 78% of itself on the part that cannot reach it.**
- Row 2 is forbidden by the guide's rule #0, which is nine words long. Row 1 is the answer.
- The 17th land is dead on arrival: every card in hand is already payable several times over off
  16 sources making {W} 10 / {U} 14, and there is nothing in the deck above 6 mana but an Emrakul
  the seat is 9 lands short of.
- The opponent is at 5 with no creature and the Staff wins on its own.
- The information that could change the answer is the `Land drop:` line, the colour counts and the
  hand's one land — **~400 of the 3,416 situation chars, and 0 of the 11,902 narration chars.**
- What it was given instead is a 45-turn log, 345 event lines, including twenty-two
  `Opponent played <land>` lines and every Staff ping since turn 13.

The sharpest form of the ask is unchanged from wave 56 and this corpus prices it harder: **the
narration budget should be a function of the ask KIND**, lane H's `kind:N` arm already exists, and
the land-drop ask is the cheapest, most forced and third-largest kind at my seats — **132 asks,
132 forced answers, 1.24 MB, 65.9% narration, 75 of them two-row menus.** A `Land drop:` ask needs
the last two turns, not forty-five. The general form is in `wave57/deck125/general-suggestions.md`.

---

# Rotation evidence

- **deck125 — KEEP IN ROTATION.** 2-4 → 3-3 and the deck is not the ceiling. Every branch with a
  window held or improved: land drop 83/83, sweeper at a dead header 0/45, low-X Revelation 2/104
  (both takes correct), counters at a wall or a power-0 body 0/39, cleanup land rule 0 breaks for
  the fourth corpus, Path 0 bad targets, X menus 7/7 in range, D1 risk 0. **The Staff-ping edit
  flipped its own game** (5/5 at the punisher, 54/54 at the face; the `vs162` loss became a
  30/8 win). What is left is three things, and all three are the guide arguing with itself: an X
  rule whose premise is false at the window it fires in, a low-life Revelation floor stated three
  times seventy lines apart, and a Lightmine floor that forbade the play that survives. The Emrakul
  stop is the fourth and it is now a pure obedience problem — the render says everything.
- **deck126 — KEEP IN ROTATION, and KEEP IT TEXT-FREE ON THE HOLD ROW.** 3-3 held. Its ceilings got
  bigger and cleaner: zero-Tribute **0 of 445**, all-dead-menu spends **0 of 405**, land drop
  49/49, blockers **9 of 9 fully assigned** with 0 `BLOCKS: none` (the two wave-56 blocker edits
  landed), D112 1/1. Its remaining weakness is not the combo and no longer the blocks — it is Rule
  #7's ORDERING under pressure: three of its four entry-1 misses are one game, and each miss took a
  cheaper card off a lower entry while the win condition sat at row 1. The control is now a settled
  result at 26×; a fourth corpus would only confirm it, and the cost of keeping it is nil.

---

# Falsifiable predictions for wave 58

deck125:
- **W121** — ANNOUNCE_X menus answered at option 1: **N/N** (was 4 of 7; the 3 misses were menus
  whose largest X exceeded 3). X announced below the largest offered while the row prints
  `{leaves K of your M floating mana unspent}` with K >= 1 and no library floor and no DRAW PRICE:
  **0** (was 3 of 3).
- **W122** — a `Cast Sphinx's Revelation` row with `max affordable X` >= 1, at my_life <= 6, with
  no `DRAW PUNISHERS on the battlefield: theirs` paragraph, DECLINED: **0** (was 17 of 22).
  Declined WITH that paragraph: **N/N** (was 12 of 12, and correct).
- **W123** — `Cast Emrakul` taken while the row carries `{their converter:` or the prompt carries
  `LIFE-TO-DAMAGE CONVERTER on the battlefield: theirs`: **0** (was 1 of 1, and it was the loss;
  4 of 6 across five corpora). Emrakul taken with no converter line: **N/N**.
- **W124** — a `Cast Lightmine Field` row reading `deals N to each` with N >= 1, declined while the
  opponent's able-attacker power total is at or above my_life: **0** (was 2 of 2, and they were the
  loss). Lightmine cast below 3 attackers with the seat's life comfortably above that total: **0**
  (was 0 of 22).
- **W125** — cleanup sends of a `{right now:}` row while a `{dead right now:}` or `{spare:}` row is
  on the same numbered list: **0** (was 8 of 85). Counterspell sent past an unsent land at 6+
  lands: **0** (fourth corpus).
- **W126** — unchanged ceilings, each **N/N or 0**: land drop (83/83), sweeper at a dead header
  (0/45), low-X Revelation outside the low-life floor (0), Path at an empty-`{target text:}`
  power-0/1 candidate (0), Elixir activation below the gate, counters at a `[defender]` or
  power-0 stack line (0/39), Staff pings at a printed punisher (5/5) and at the face with none
  (54/54).

deck126:
- **D124** — a window printing a `Cast Sanguine Bond` or `Cast Exquisite Blood` row with no
  `[second copy:` tag, for a name absent from the battlefield line and with no
  `[NAMED BY THEIR ... this KILLS you]` price, answered with anything else: **0** (was 4 of 11).
- **D125** — a reply carrying two different `CHOICE:` lines: **0** (was 1 of 588, and it was the
  seat's last live window).
- **D126** — a `[RE-ASK]` answered with the SAME card name it just refused: **0** (was 1 of 3).
  Re-asks recovered to a printed row: **N/N** (was 2 of 3).
- **D127** — a live Tribute row declined at my_life <= 12 while the combat header prints
  `none of your creatures can block` for the majority of the incoming damage: **0** (was 4 of 6).
- **D128** — takes of a dead Idyllic Tutor (`every enchantment left in your library is a copy of
  one you already control or hold`): **0** (was 1 of 10). `[second copy:` takes: **0** (was 2 of
  23). Zero-Tribute takes: **0** (0 of 445, fourth corpus).
- **D129** — blockers windows with any available blocker left out of the BLOCKS line: **0**
  (was 0 of 9). `BLOCKS: none`: **0** (was 0 of 9).
- **HOLD CONTROL (skill #233 / #299 sunset)** — deck126 stays text-free for a fourth corpus.
  Predicted deck125 opponent-turn HOLD take rate **>= 15%** and deck126's **<= 3%**; the ratio has
  now read 7×, 7× and 26× across three corpora and I predict it stays above 5×.

Both seats (engine-side; no guide change can move these):
- **W127/D130** — discard rows whose verdict prints all zeroes and is tagged `{right now:}` rather
  than `{dead right now:}`: **0** (was 5 of 5 Final Judgments). Menus holding an all-zero Final
  Judgment that carry the `NO LIVE CAST ROW ON THIS MENU` token when every other cast row is dead:
  **N/N** (was 0).
- **W128/D131** — a printed `best case` / `one legal assignment` number that the board cannot
  reach: **0** (was 1 of 8). A reply whose prose disputes an engine-printed number: **0** (was 1).
- **W129/D132** — opponent-turn windows before combat (Upkeep, Draw, Main 1) with able attackers
  and no incoming forecast: **0** (was 20).
- **W130/D133** — stale async drops on the land-drop arm at my seats: **0** (was 38 of 85).
- **W131/D134** — `{their converter:}` tags rendered on a board the engine has already identified
  as a LOOP that carry a single-iteration total instead of 0: **0** (was 1 of 1).
- **W132/D135** — HOLD takes whose `chosen_text` lacks the row's skip-note tail: **0** (was 0 of
  122 — D4 held; predicted to stay held).

# What I did NOT check

- I did not run wagic, did not edit `bin/Res`, `src/` or `JGE/`, did not run git, and did not
  deploy. All guide work is under `wave57/`.
- I DID read `projects/mtg/src/AIPlayerGPT.cpp` read-only at three places (`rowSaysNoOp` ~19862,
  `incomingCombatLine` / `assignableRemainderDamage` ~14840–15070, and the D24 call site ~15595) to
  root-cause HIGH #1 and HIGH #2. Every other emitter string in this file is quoted from a rendered
  `prompt` and I did NOT confirm which function emits it, so the remaining lane attributions come
  from the carry-list, not from code.
- **Scryfall: nothing cross-checked this wave.** No primitive/Scryfall disagreement is reported
  either way because none was looked for. The one place it would matter is Final Judgment's Oracle
  verb ("Exile all creatures"), which I took from the engine's own `{card text:}` render.
- I grepped my 11 stderr files for the drop/rescue/heuristic/crash/abandon strings and read
  `game-126v130-1788475528.stderr` around its 29 drops. I did NOT read any stderr in full.
- I did not measure lane M's cache hits, lane P's purge, lane Q's autotap search, lane I's RSS, or
  lane G at all — none is observable from a translog and I did not instrument.
- Colour-pip auditing: I read `Paid ...` receipts only where a finding turned on them.
- I audited deck126's nine blocker replies against their A/B lines. Neither seat's OPPONENT's
  blocking was audited, and deck126 was offered no attackers window at all.
- The 385 `mana_only_windows_skipped` and 534 `hold_windows_skipped` are read off the `gameend`
  records; I did not confirm that none of them reached the model.
- D21, D30, D2's MDFC note, D14/D26/D27 and lane V are UNTESTED here rather than PASS or FAIL —
  neither deck runs a cycling card, an MDFC, or a cross-card duplicate-verdict pair.
- I did not open every one of the 1,497 records. Counted claims come from scripted passes over all
  12 seat logs; every narrative claim carries its file and seq, and I opened those individually.
