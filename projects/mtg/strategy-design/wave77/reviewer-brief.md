# Wave-77 step-3 brief (lean; REJECT is the expected verdict for anything not paid for by a decision the corpus shows going wrong. Amendment 332 stands: guides are decision-time instruction only, hard ceiling 20,000 bytes, no citations/history in a guide — evidence goes in YOUR review.md. Amendments 333 (heading = scope declaration) and 334 (no concessive clause for a rule's hard case) are drafted in wave74/strategy-writing-skill-v2-amendments.md, not yet folded in by the owner — apply them anyway.)

Eighth reasoning-on corpus (invariant 000). Wave 77 step one (wave76/known-bugs.md §R → wave77/lane-{CR,CS,CT}.md + wave77/codex-review.md → lane-CU.md, ten findings all CONFIRMED and fixed): CR window/hold — the land-drop window has its own `"land"` hold-check seam (all 22 wave-76 false brackets were cast/land pairs; the casting builder leaked `mNextAskPromptNote` onto the next window — R2a), the first asked window at a seam says so instead of comparing (R2b), R2c REFUTED (the counts were real target-roster changes) but CU F6 then took preview P/T out of the hold key, `hold_check_ref_seq` on every record, the own-loop annotation + `own_loop_windows_asked` (R1; CU F4 made it a three-way verdict IDLE/RESOLVING/THREATENED off the live stack with an `[own loop verdict:]` MARKER ROW so a hold re-opens), Q13's comparison on the ask-cache board key (R8; 104/109 boards had genuinely moved), `plan_names_uncastable_zone_card` restricted to cast-intent plans (R9), same-arm force-close defer + harvest keyed on the outdir manifest (R11; CU F10 = exact seat-log identity via a `WAGIC_GPT_TRANSLOG_FILE` announcement), and CU F5: a wall-miss record no longer consumes the hold-check window sequence; CS combat/sacrifice — one EVENT-naming clause on every life-gain-on-sacrifice row and BOTH edict branches, own converter too, `edictFloorScan` range (R3), the blockers AT BEST figure folds the metered "may" gain (R4; CU F7 gates every metered gain on the lifegain-prohibition predicate), the crack-back cover reaches targeted-removal rows, X sub-menu rows and the attackers stay-home line (R6; CU F2 single-target vs sweep, CU F3 the regeneration/return guard on both new walks, CU F8 rosters folded), legality-filtered blocker counts (R7), plan echo + neutral example on ANNOUNCE_X (R10); CT — creature battlefield entries carry a capped `{effect:}` gloss from `text=` (R5; 224/450 corpus entries glossed live; CU F9 snippet ceiling enforced, bound 38,657 B), `{kills whichever you target:}`/`{removes:}` rosters folded (R11-row); CU F1 — the ask key AND async-slot key now take the balanced-group strip (a wave-77 pin had enshrined cover-number drift, the wave-74 CG shape; the `crackBackKillRowTag` sibling fixed too). Records carry `thinking`, `reasoning`, `reasoning_chars`, `off_protocol_bytes`, `plan_line_missing`, `plan_steps_done`, `plan_names_uncastable_zone_card`, `hold_check_ref_seq`, `http_error_body`, `reasoning_ngram_repeat`; gameend carries the counters below. Wave-76 guide edits for all seven decks are the LIVE guides (installed 2026-09-11 at 3858f4e67; each deck's before->after list is in wave76/deck<N>/review.md).

Repo /home/magi/Projects/wagicGPT, game dir projects/mtg. Use python for translogs; use /usr/bin/grep (bare grep drops matches). DO NOT edit anything under bin/Res, src/, or run git or wagic; write only under projects/mtg/strategy-design/wave77/. Never write mechanisms as the owner's positions. Report what you did NOT check.

CORPUS (wave 77, SINGLE binary archives/wagic-42c41fc66-w77step1 = source 8e394a077; `--thinking on`, WAGIC_GPT_TIMEOUT=900, no operator token ceiling). INCIDENT: the first run (matchups-20260911-010157) hit the Spark pilot's silent engine wedge (#3 in four days) at 03:01 CDT, 2 h in; PILOT STALL fired at 03:16 with 1 game finished (126v162, epoch 1789106557); container restarted; the 20 stalled matchups RERUN as 20 concurrent two-deck pools (epochs 1789114832-1789114927), all finished naturally, and the new manifest harvest left every pool dir with exactly its own 2 seat logs. Corpus under review: /home/magi/.Wagic/ai/gpt/selfplay-runs/matchups-20260911-010157-final (42 seat logs all with gameend, 0 timeout-tainted, merged results.tsv, askreplay/ 29 files). 21 games, all completed naturally, 0 HUNG/crashed; 2,740 decisions (wave 76: 2,188; 2.02 ask-decisions/turn vs 1.73) = ask 1,999 + priority 567 + attackers 82 + discard 51 + reveal 21 + blockers 18 + recovery 5 + bottom 2 + defer 1, 100% with native reasoning (median 5,817 chars, max 35,886), **6 heuristic fallbacks** (5 `unparsed_reply` + 1 `noop_row_zero_reask`, all ask seam — wave 75 and 76: 0; engine seat (a) quotes every one) + 1 `repeat_count_reask` (a model re-ask) + 1 `defer`/`menu_not_askable`, 0 reply_truncated, 0 action_before_plan; CORPUS-WIDE off_protocol_bytes>0 120 (4.4%), plan_line_missing 141 (5.1%), latency p50 82.5 s (ask seam p95 175.6 s); gameend sums: `ask_replays_reserved` 1,334 / `identical_ask_answers_reserved` 1,073, `hold_released_turn` 132, `hold_windows_skipped` 2,128 (cast 1,433 / priority 695), `stop_reached_windows_skipped` 7, `chain_windows_only_selfharm` 32 / `chain_selfharm_rows` 479 / `chain_acting_rows` 1,374 (+ `_cast` 284 / 593 / 1,490), `crossphase_identical_reputs` 119 / `crossphase_board_unchanged` 2 (NEW), `own_loop_windows_asked` 1 (NEW), `force_close_arms_refused` 0 / `_deferred` 0 / `_same_arm_deferred` 0 / `_defer_bound_hits` 0, `forced_close_unrecorded` 10 (wave 76: 5), `phase2_answer_recovered` 15 / `_missing` 1, `main_phase_windows_skipped` 89, `mana_only_windows_skipped` 69, `own_turn_windows_skipped` 596, `reserve_decline_windows_noted` 28 / skipped 4, `plan_names_stranded_card` 4, `plan_names_uncastable_zone_card` 52 (wave 76: 102), `protocol_deviation_replies` 146, `async_drops` 53, `repeat_annotated_takes` 2, `identical_option_asks_resolved` 8, `blocker_forecast_gang` 4 / multi 178 / collapsed 2 / rows 218; wins deck152 6/6, deck146 5/6, deck162 3/6, deck125 3/6, deck123 2/6, deck126 1/6, deck130 1/6 (wave 76: 152 5/6, 130 4/6, 162 3/6, 126 3/6, 146 2/6, 123 2/6, 125 2/6 — deck130 4→1, deck146 2→5; hands are real, do not read variance as guide effect).

## Per-deck agent (one per deck: 146, 152, 125, 126, 162, 123, 130)
Purpose (owner's words): read the transcripts of your deck, compare the LLM's actions to the
instruction set (the guide projects/mtg/bin/Res/ai/baka/deck<N>_strategy.txt + the general guide
wave62/general-strategy.md (wave-59 edition + R331; NOTE from the wave-63..67 syntheses: this file is NOT loaded at runtime (only deck<N>_strategy.txt is opened, AIPlayerGPT.cpp:15332) — the deck guide is the live surface) + the reply protocol in the prompt), find issues in the game, the
interface, and the guide, and surface anything else worth the core loop's attention.
Method: for each of your deck's 6 games (your seat's translog; the opponent's for context),
trace the decisions that decided the game. Classify each misplay PERCEPTION (the model misread a
true surface, or the surface was false/missing -> ENGINE/RENDER item, cite file+seq) vs STRATEGY
(read correctly, decided against the guide or against good play -> guide item). Note fallbacks,
re-asks, and any decision where the prompt's information was insufficient or wasteful (what a
better interface would have shown). Hands are real (mulligans, bottoming): do not read variance
as guide effect.
Output: ONE file, wave77/deck<N>/review.md: (1) game-by-game outcome + the deciding decisions with
seq citations; (2) engine/interface/card items ranked HIGH/MED/LOW with a concrete repro (file,
seq, the rendered line) — this is the primary deliverable; (3) guide verdict: KEEP as is (a
success verdict, state why) or EDIT — if EDIT, also write wave77/deck<N>/strategy.txt (the full
revised guide, start from the live one) and list each edit before->after with the seq that
paid for it IN review.md — the guide itself carries NO citation, count, or history (Amendment 332); hard ceiling 20,000 bytes, and an EDIT that grows the guide must say which rule earned its bytes; (4) OPTIONAL proposals, only when
your evidence supports them: general-guide changes (wave77/deck<N>/general-proposals.md) and
strategy-writing-skill changes (wave77/deck<N>/skill-proposals.md; current skill edition =
wave68/strategy-writing-skill-v2.md (the ~40 KB distillation, owner-installed 2026-09-06 after six Astra adversarial rounds; read it in full — it is short; the 1.3 MB wave68/strategy-writing-skill.md is lineage only) — read it before proposing; a proposal that
restates an existing amendment is noise). No prediction tables, no rotation evidence, no
docket bookkeeping. Final message: <= 10 lines.

## Engine-narration seat (one agent)
You read the whole corpus for the engine's sake, not the guides'. Deliverable wave77/engine-seat.md: (1) corpus census as last wave (games, decisions, decisions/turn vs wave 76's ~2.1, fallbacks by kind with genuineness, `async_drops`, `transport`, `deadline_pct`, reply shape, `reasoning_chars`, `reply_truncated`, `askreplay/`, `plan_steps_done`); (2) REGRESSION WATCH on the wave-77 changes — the section that matters most (SINGLE binary; 20 of 21 games are RERUNS after pilot wedge #3 — the first-run game is 126v162): (a) FALLBACKS ARE BACK — 5 `unparsed_reply` + 1 `noop_row_zero_reask` on the ask seam (two corpora at 0 before this): quote every reply verbatim, classify each (a reply shape the parser SHOULD read under the ruling = HIGH; a genuine non-answer = which pin), and say whether the heuristic's play changed the game; `phase2_answer_missing` 1 (quote the record; was the CU F5 wall-miss/seq change involved?); the new record kinds `defer` (1, `menu_not_askable`) and `recovery` (5): what are they, did any lose a decision; (b) the loop family — asks per (turn, phase) per seat; `own_loop_windows_asked` 1 (R1: quote the window — did the `[own loop verdict:]` marker row print, which verdict IDLE/RESOLVING/THREATENED, did the seat hold, and was the verdict TRUE against the stack), `hold_windows_skipped` 2,128 (wave 76: 1,280 — what grew, cast 1,433 / priority 695); (c) `[hold check: ...]` after R2 + CU F5/F6: for every record-adjacent pair at one seam INCLUDING the new `"land"` seam, is the claim true against the previous RECORD (wave 76: 22 false, all cast/land); `hold_check_ref_seq` is now on the record — use it, and report any record whose ref_seq is not the previous asked window at that seam; "first window asked" sentences: count, and any that follow an asked predecessor (CU F5's timeout case); (d) `crossphase_identical_reputs` 119 / `crossphase_board_unchanged` 2 (R8: the positive clause printed twice — quote both; are the 117 others genuinely moved boards); (e) `forced_close_unrecorded` 10 with `force_close_arms_refused` 0 / `_deferred` 0 / `_same_arm_deferred` 0 / `_defer_bound_hits` 0 — the bound STILL never fires; name each of the 10 (game, arm, what superseded it) and reconcile recovered 15 + missing 1 + unrecorded 10 against the stderr close count; (f) R3 — every life-gain-on-sacrifice row and edict branch: the event-naming clause present, the numbers true, the OWN-converter case, the prohibition case; any reasoning that still reads it as a cost; (g) R4 — blockers headers: AT BEST folded figure true; the prohibition gate (CU F7); (h) R5 — the creature `{effect:}` gloss: sample 30 entries against the card's `text=`; any gloss cut mid-clause into a false statement; max prompt bytes (wave 76: 30,375; CT's bound 38,657) and the count over 20 KB; (i) R6/R7 — `{crack-back cover:}` on targeted-removal rows / X sub-menu rows / the attackers stay-home line: arithmetic true, regenerating/returning bodies NOT subtracted (CU F3), single-target vs sweep (CU F2), rosters folded (CU F8); the aggregate blocker count legality-filtered; (j) R10 — every ANNOUNCE_X window carries the plan echo and the neutral example; `identical_option_asks_resolved` 8; (k) R9 — `plan_names_uncastable_zone_card` 52 (wave 76: 102; cast-intent window): sample 20 real vs false; `plan_names_stranded_card` 4; (l) R11 — the harvest by manifest: every pool dir held exactly 2 seat logs (verified at consolidation — say so); the same-arm defer never fired; (m) `async_drops` 53 (wave 76: 37) — all re-asked? `identical_ask_answers_reserved` 1,073 / `ask_replays_reserved` 1,334; `protocol_deviation_replies` 146 (5.3%, wave 76: 4.2%) = `plan_line_missing` 141 + 5? — classify; `main_phase_windows_skipped` 89 under the record's turn; (n) CU F1 — the ask/async keys under the balanced strip: any window re-asked with rows differing only inside `{...}`/`[...]` (the CG shape) — count byte-identical-after-strip re-asks; (o) the harness — PILOT STALL fired once, true positive (engine froze 03:01 CDT, tripwire 03:16); (3) adjudicate EVERY prediction in wave77/lane-{CR,CS,CT,CU}.md (CU = the ten Astra findings) as PASS / FAIL / UNTESTED with counts and file+seq (UNTESTED is a real verdict), plus the carried UNTESTED docket (BS F2 Tribute second edict, BS F3/land latch `declined_face_latches` 0 for the 5th wave, BS F5 blockers ceiling, BS F6 distinct-index re-ask, CF F1 pending-deadlock, F2/F5-nonlifelink/F8 haste/F9, the reasoning-body halves of N14a/N14d/O20; {X}-announcement payment loss — count `dropped after its X was announced`); (4) engine/render/narration defects ranked HIGH/MED/LOW with repro — especially any new render clause stating a false fact (the loop-scope sentence, `[cost:]` on reveal rows, the ghostform gloss, the cannot-block subject); (5) the owner's lategame specimen (standing rule, alert-only): ONE late-turn decision (turn >= 25), full `prompt` verbatim to wave77/lategame-specimen.txt with a 3-line header. Final message: <= 15 lines, HIGH items first.

## Synthesis (runs only if any deck agent wrote general-proposals.md or skill-proposals.md)
Read all proposals + engine-seat.md. Decide each proposal: ADOPT (edit wave77/general-strategy.md,
a copy of wave62's (the R331 edition; waves 63-67 wrote no edition; wave 68 installed wave68/strategy-writing-skill-v2.md as the current edition), or append a numbered amendment to wave77/strategy-writing-skill.md, a
byte-verbatim copy of wave59's with amendments appended — verify the prefix with cmp) or
REJECT with one line of reasons. Write wave77/synthesis-notes.md. If a deck agent's guide edit
contradicts an adopted general change, note it; do not edit deck guides yourself.

METHOD NOTE (from the wave-59 engine seat): the translog `phase` field is not the rendered phase — count phase-gated facts from the prompt text.
