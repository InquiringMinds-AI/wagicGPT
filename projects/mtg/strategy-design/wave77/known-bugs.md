# Wave-77 known bugs (corpus matchups-20260911-010157-final: 21 games = 1 first-run + 20 reruns after pilot WEDGE #3, SINGLE binary archives/wagic-42c41fc66-w77step1 = 8e394a077; sources wave77/engine-seat.md + wave77/deck<N>/review.md)

Invariant 000 held: 2,746/2,746 decisions with native reasoning, 21 natural finishes, 0 truncations, 0 hung. BUT 5 HEURISTIC ANSWERS (all `unparsed_reply`, all the deck125 seat — S1) after two corpora at zero, plus 2 model re-asks (`noop_row_zero_reask` true positive self-corrected; `repeat_count_reask` — S2). PILOT STALL fired once, true positive (15 min); the CU F10 manifest harvest verified two ways (42 announcements == harvested set; 20/20 pools held exactly 2 logs). Wave-77 items PAID: hold-check 2,174/2,174 referents correct, 0 false claims (wave 76: 22) — R2a/R2b/R2c CLOSED, land seam clean, 18 first-window sentences all genuine; R1 storm FIXED (max asks per phase 4 vs 35; the one own-loop window's RESOLVING verdict was TRUE); R3 closed with the model's own words ("takes 4 off YOU, not fatal — fine"); R4 6/6, R5 gloss true on 47+26 samples (Blastminer countered on sight — the card that lost wave 76), R6/R7/R8 (56 of 57 prints), R9 (102 → 52; deck146 11 → 0), R10, R11, F2/F6/F8/F10 PASS; forced-close identity 15+1+10 = 26 = stderr. Seven wave-76 guide edits paid (deck146 COUNTER TEST fired-and-failed once → moved; wave-74 converter edit tested for the first time, passes). Six wave-77 guide EDITs installed (031db297b), deck152 KEEP at 6/6. Wins 152 6/6, 146 5/6, 162 3/6, 125 3/6, 123 2/6, 126 1/6, 130 1/6.

## S — routed to wave 78 (HIGH first)
- S1 HIGH, OWNER RULING NEEDED (measure-only until ruled): FIVE `unparsed_reply` HEURISTIC ANSWERS, one shape — a PLAN line then `<n> (<short name>)` with the `CHOICE:` label absent, in-range number, byte-exact short name, `coded_answers: 0` (deck125 seat: `125v126` seqs 140/201 cost a Path to Exile the model was explicitly HOLDING — the heuristic cast it; `125v162` seq 49 `PLAN: Cast nothing right now.\n4 (Cast nothing right now)`; all five quoted in engine-seat.md §2(a)). The standing ruling says no parser reads outside the two LABELLED parts and no tolerance licenses prose — this line is not prose, it is the answer line minus its label; whether it may be read is the owner's call. SHIP NOW (no parser change): the deviation class on these records is `answer_label_absent` (the shipped `prose_outside_two_lines` is FALSE on all 5), the `recovery` record links to the ask it recovered (`recovered_seq`), and the measure `answer_label_absent_heuristic_played` counts the ones the heuristic then played. Guide side: deck125's revised guide names the label in its templates (seat edit). (engine-seat HIGH-1/HIGH-2, deck125 B-2, deck162 §9)
- S2 HIGH — THE REPEAT ROW'S INSTRUCTION CONTRADICTS THE WINDOW'S CLOSING SENTENCE: the row demands `CHOICE: 2 (… x<N>)` while the closing boilerplate says "the action and card name only" — `123v126` deck123 seq 38, the corpus's only `repeat_count_reask`: the model looped on the contradiction for 339 s (`reasoning_ngram_repeat` 0.626, budget hit). Fix: one instruction — the closing sentence carves out repeat rows (`… or, on a row marked x<N>, the count too`), the row's bracket quotes the exact form once; pin both surfaces agree on every menu that carries a repeat row. (deck123 HIGH-1)
- S3 HIGH — A STOP-REACHED ROW IS RE-OFFERED 12 TIMES IN ONE TURN: `126` deck123 seqs 41-52 — the row prints `{if you pass here, this option is not offered again until the board changes}` AND `M=31, your stated stop=29 … ALREADY AT OR PAST your own stop`, yet it is put again and again (~16 min of decode for zero decisions); `stop_reached_windows_skipped` covered 5 of 17; all 7 corpus-wide stop-skips are this deck. The ruling allows collapsing a window whose answer is already given (a reached stop) when no legal option is lost: the collapse must cover the re-put when the ONLY new-action row is the stop-reached repeat row and the ask-cache board key is unchanged; a window with any other new row stays. Pin the seq-41→52 shape = 1 ask; measure `stop_reached_reputs_collapsed`. (deck123 HIGH-2)
- S4 HIGH — NINE BYTE-IDENTICAL DRAW-STEP PRIORITY WINDOWS WHILE A 7-OBJECT OPPONENT TRIGGER STACK DRAINED: `152v162` deck152 seqs 55-63 (~15 min); Katilda's unused activation sat above the hold row so the guide correctly forbade holding. Same family as R1: nothing collapsed — annotation + hold guidance: when the stack holds N of THEIR triggers and the seat's rows are byte-identical to the last asked window, print `[their stack is draining N triggers - each link will put this same list to you; HOLD (<hold row text>) covers every link, the activation above stays available after]` and verify the hold latch holds across the drain (fixture: 7-link drain after a hold = 0 asks); measure `stack_drain_windows_asked`. Annotation only; key-stability pin set. (deck152 HIGH-2)
- S5 HIGH — COUNTERSPELL ROWS OFFER A SPELL THE SEAT'S OWN COUNTER ALREADY TARGETS: `125v146` deck125 seq 125 — both counterspell rows print `can target on the stack: Silverquill Silencer` while the seat's own Essence Scatter sits above it on the stack aimed at that spell; the seat burned its second Scatter at 8 life, took 7, lost. Fix: a row whose target is already targeted by a pending counter of the seat's carries `[your <Essence Scatter> already targets this - a second counter is redundant unless theirs resolves first]`; annotation, key-stability pin set. (deck125 HIGH B-1)
- S6 HIGH — THE `[own loop verdict:]` MARKER ROW NEVER RENDERS (CU F4 pushed it into the hold keys only): 0 occurrences in 42 logs; at the one own-loop window (`126v130` deck126 seq 43) the RESOLVING verdict was true but invisible and the seat took a row instead of the hold. Fix: render the verdict as a visible prompt line (outside the ask key, as the annotation family is) while keeping the key-side marker that re-opens holds; pin that the line prints on the seq-43 shape and that a THREATENED verdict names the spell and component. (deck126 HIGH-1, engine-seat §2(b))
- S7 MED — `hold_check_ref_seq` IS A WINDOW ORDINAL, NOT A RECORD `seq` — four deck seats + the engine seat could not join it (~80% match to the record index, off by 1-3; 246/294 land at the wrong seam if read as a seq); 18 records carry it with no bracket, 2 (`146v126` seqs 46/55) staged, committed and rendered nothing. Fix: emit the referent's RECORD seq (or rename to `hold_check_ref_window` and add `hold_check_ref_seq` = the record); fix the two staged-no-render cases; pin. (deck162 HIGH-1, deck126 HIGH-2, deck130 HIGH-2, deck146 MED, deck123 MED-4, engine-seat MED)
- S8 MED — THE `{effect:}` GLOSS CUTS MID-CLAUSE INTO A FALSE STATEMENT: Howling Mine renders `…if Howling Mine is untapped, that player...` 55 times; Brutal Cathar loses its "until this leaves the battlefield" return clause (`123` seq); Lightning Greaves ×35. Fix: cut at clause boundaries (sentence end / ` -- `), preferring whole clauses under the cap, and mark an omitted clause as `(…more)` never a dangling verb; pin the three cards. (engine-seat MED, deck123 MED-5, deck162 §5)
- S9 MED — HOLD-CHECK RESIDUALS: (a) the count over-counts when a second copy RENUMBERS an existing row's handle (`Goblin` → `Goblin #1` when `#2` arrives): `152` deck130 seq 6→10 says 8 new, 5 appeared; `126` 12→18, `123` 62→70, `125` 84→88 — key on the instance, not the printed ordinal; (b) rows that DISAPPEARED are never reported (`125` deck152 seqs 20/35) — the bracket says `N gone` too; pins. (deck130 HIGH-1, deck152 MED)
- S10 MED — THE `defer` RECORD IS WRITTEN INTO THE WRONG SEAT'S LOG AND CARRIES NO OPTIONS: the opponent's Starstorm X-menu landed in the deck152 seat log (`152v130` seq 21; deck130 re-announces at seqs 51-53) with an empty `options_text`, so the deferred menu is unrecoverable — and it is the corpus's only X-announcement payment loss. Fix: resolve the ACTING player for the translog file at the defer site (CU F10's announcement gives the file); record `options_text`; pin. (deck152 HIGH-1, engine-seat MED)
- S11 MED — `forced_close_unrecorded` 5 → 10 WHILE EVERY ARM-BOUND COUNTER IS 0 FOR THE FOURTH WAVE: the identity holds (15 + 1 + 10 = 26 = stderr `unclosed <think>` lines) but the 10 are not attributable; `phase2_answer_missing` 1 (quote it). Fix: a per-event `forced_close` record (arm, seq, outcome) so the next seat can name each; and find WHY the R11a/CU F2 bound never fires — a fixture that arms two closes on one arm with an empty park must show `force_close_same_arm_deferred` 1. (engine-seat MED, deck126 MED-5)
- S12 MED — `Opponent life trend:` NETS THE SEAT'S OWN DAMAGE AGAINST THEIR GAIN: a deck gaining 7/turn prints `-5`; every "are they gaining" guide rule keys on a flickering figure (deck130). Fix: print gain and loss separately (`+7 gained / -12 taken per turn`); pin. (deck130 MED-1)
- S13 MED — PROMPT BYTES UP AGAIN: max 36,211 B, deck125 42% of prompts over 20 KB (was 21%), deck130 118/431 over 20 KB (max 17,199 → 30,125) — repeated `{card text:}` per row on shared-source target menus. Fix: a target menu whose rows share one source prints the card text ONCE above the rows; measure bytes before/after on the deck130 max prompt; the gloss stays. (deck130 MED-2, deck125 B-5, deck162 §5)
- S14 MED — pricing/wording, one lane: `[X pricing]`'s "largest X that still leaves it payable" names the cheapest CARD, never the cheapest COUNTERSPELL — the input the tap-out gate needs (`162` deck125 seq 180: X=9 tapped out before their four-permanent turn); `[second copy:` prices a waste with `DOUBLING` / `each line it repeats happens again` and the model read it as an upside (`125` deck126 seq 51); loyalty rows print `[cost: Counters]` with no `spends 3 of 5, she is at 2` figure (deck146). Pins; key-stability where a number enters.
- S15 LOW — a 21-row Adversary menu whose rows 2-20 are annotated no-ops (`123` seq 7) — fold no-op rows display-only with the option set intact; the Teferi +1 chain costs 17 calls/game (report; a chain, not a loop); `crossphase_board_unchanged` 2 of 119 both on deck125 (R8 works; the clause is rare because boards move — no action).
- S16 REPORT-ONLY (owner-ruled territory): reasoning tail / protocol re-quoting unchanged; `plan_line_missing` 141 corpus-wide (5.1%), the dropped label; `protocol_deviation_replies` 146.

Docket: still UNTESTED, carried: CU F3 (regen under the new cover walks), F5 (0 wall-miss events), F7 (no lifegain prohibition on any board), CS's `AT BEST once the` pin, BS F2/F5/F6, CF F1, the reasoning-body halves. `declined_face_latches` 0 for the 5th wave — RETIRE the counter next wave if still 0. Census rule: the kind-sum must include `recovery` and `defer` (2,746, not `protocol_replies` 2,740).
