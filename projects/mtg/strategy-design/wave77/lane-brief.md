# Wave-77 step-one lane brief (base = master after wave76 known-bugs; evidence = wave76/known-bugs.md §R + wave76/engine-seat.md + wave76/deck<N>/review.md)

RULINGS THIS WAVE TOUCHES: 000 — reasoning only in the native channel; reply = PLAN then action, nothing else; no instruction licenses prose; no parser reads outside the two labelled parts (a present `CHOICE:` label on the PLAN's physical line is inside — P9); caps never bound reasoning; the golden protocol TEXT (reply-protocol.txt / kReplyProtocol) and the thinking regime are owner-ruled — render brackets and row text may change; R12 is REPORT-ONLY. 00 — full games; a game answered by heuristics is not a corpus game. 3 — suite THREADS=1 0 failed (known flakes: lifeline, merrow_reejerey, intrepid_adversary_repeated_payment), PARSETEST 0 failed. Never remove, cap, or auto-answer a legal option (R1's loop guidance is an ANNOTATION plus the model's own hold row — nothing is collapsed or auto-answered; R9 is MEASURE ONLY). No VPK. No AI attribution. NO CORPUS from a lane; no lane sends requests to the pilot this wave.

Standing mechanics: as wave71/lane-brief.md (worktree-only; `make -f Makefile.sdl -j8`; both link guards OK; binary freshness; every wagic run under `systemd-run --user --scope -q -p MemoryMax=4G -p MemorySwapMax=0 --`; suite THREADS=1 as a detached unit `w77-<LANE>-suite` waited with a FOREGROUND until-loop (no Monitor); count both failure strings; PARSETEST from bin; Edit tool for UTF-8 sources, byte-splice for MTGRules.cpp/GameObserver.cpp/primitives; U+FFFD 0; RED-on-base evidence + a falsifiable prediction per fix; commit with explicit paths, `git add -f` under bin/Res; write wave77/lane-<LANE>.md; touch `~/.gatelogs/W77_<LANE>_DONE`; weakest evidence). LESSON OF WAVE 74 stands: any NEW annotation carrying a board-derived number must be proven OUTSIDE every key (ask, async slot, hold latch, hold-check, option-set) with a PARSETEST pin that two windows differing only in that number yield identical keys, and the held set in a key pin is built the way the LIVE seam builds it (holdActionKeyRow via mLastMenuRows). LESSON OF WAVE 76: (1) a bracket that prints a NUMBER or a COUNT must be pinned on the cases where the count is zero, one, and a reorder — Q1 shipped with the referent fixed and the count still wrong on reorders and disappearances; (2) a fix-lane finding whose fix is "compare more state" (CQ F4) can make a clause fail-closed into silence — pin that the positive clause still prints on the shape it was built for; (3) a clause that carries a number about LIFE must name the EVENT that moves it, or the model reads it as a statement about the row's mana cost (R3). LESSON OF WAVE 75: (1) an instruction's REFERENT must be something the reader can see — the hold-check bracket spoke of "the last window" meaning the last one the engine BUILT, while 3,402 built windows never reached the model; any bracket that refers to history refers to ASKED history; (2) a helper that hardcodes its input (the wave-74 stats script ignored its argument) reports the wrong corpus with full confidence — new tools take the run dir as an argument and echo it in their first output line; (3) a prediction's negative clause ("non-zero means lost") needs its own pin, not just the positive.

## Lane CR — the window/hold and meter family (R2, R1, R8, R9, R11)
R2 first (the HIGH, three residuals — (a) land seam, (b) streak-1 wording, (c) the count; pins for zero/one/reorder/disappear; `hold_check_ref_seq` on the record). R1 the own-loop guidance bracket + the hold path verified by a 5-link fixture (0 asks after a hold) + `own_loop_windows_asked` — annotation only, key-stability pin set, nothing collapsed. R8 the ask-cache board key in the Q13 comparison, with a pin that the positive clause prints on the 152v125 shape. R9 the cast-intent restriction + no-PLAN skip (measure only). R11 same-arm re-arm bound; harness harvest keyed on the outdir manifest (+ --selftest case). Weakest-evidence names what the translog cannot show.

## Lane CS — combat and sacrifice truth (R3, R4, R6, R7, R10)
R3 first (the HIGH): one clause shape on every life-gain-on-sacrifice row and on BOTH branches of a modal edict, naming the event and the converter; the seat's own converter too. R4 the at-best figure folds in metered lifegain. R6 the cover family reaches targeted-removal rows, X sub-menu rows, and the attackers menu's stay-home line. R7 legal-blocker counting in the aggregate and the cover's body count. R10 plan echo on ANNOUNCE_X + neutral example. Every clause with a number: key-stability pin set.

## Lane CT — the battlefield gloss and row size (R5, R11-row)
R5 first (the HIGH): creature entries on the battlefield line carry a capped `{effect:}` gloss of activated/triggered text (keywords already print) — static card text, no key pin needed, but measure prompt bytes before/after on the corpus's 30 KB game (`125v123`) and on the seq-7 deck125 board; the gloss must not push any prompt past 40 KB. R11-row: cap the `kills whichever you target:` roster like Q2's. Verify the gloss source against the engine's `auto=`/`text=` (the text= line is what the model should read; primitives can disagree with the scan).

## Lane CU — fix lane for the Astra review (wave77/codex-review.md) — seeded after step-one gate 1 — base = master 90643013b (gate 1: 1291/0, 76 AI/0, PARSETEST 6314/0)
Ten findings, each CONFIRM (fixture/pin RED on base reproducing Astra's trigger THROUGH THE LIVE CALLER PATH, then fix) or REFUTE (a pin proving the trigger cannot occur, with the reason). Astra's recurring charge: pins that validate helper assumptions or enshrine the wrong behaviour — every pin here exercises the live path. Key-stability pin sets with the held set built as the live seam builds it, and the ASK key + async-slot key included (not only hold/option-set).
- F1 HIGH R6 (AIPlayerGPT.cpp:~89644, lane CS): the R6 "key-stability" pin asserts the cover NUMBERS survive `stripDeclineReaskTags` into `keyTailStr` → the ask key and async slot DRIFT with the cover figure (8/9 → 9/4 = a different ask key = a re-ask of an answered window — the wave-74 CG shape); an older sibling does the same and the pin cites it as precedent. CT's roster pin checks only hold/option-set keys while claiming ask-key coverage. Fix: every board-derived clause on a row is stripped from `keyTailStr` before the ask key (extend the tail stripper to the whole annotation family — the same balanced-group rule the option-set stripper now uses); rewrite both pins to require EQUAL ask + async-slot keys across changing numbers; audit the "older sibling" and fix it too; re-derive, never adjust, any pin that assumed the drift.
- F2 HIGH R6/R10 (AIPlayerGPT.cpp:~45521, lane CS): the ANNOUNCE_X per-X cover walk sums every killable target without checking `cbxSv.sweep` — a single-target `damage:X` at X=3 vs two 3/3 attackers claims six removed; the parent-row path requires `sv.sweep`. Fix: the per-X helper takes the sweep/targeted classification; targeted = ONE victim (the best the seat could pick, named as such); pin single-target vs sweep at the same X.
- F3 HIGH R6 (AIPlayerGPT.cpp:~43323 and ~45531, lane CS): the targeted-removal cover and the per-X cover subtract bodies without `w76CrackVictimTrulyRemoved` (regeneration, ghostform, persist, undying) — CQ F5's bug re-introduced on two new paths. Fix: both paths call the guard; pin a regenerating attacker under Hammer and under an X sweeper = not subtracted.
- F4 HIGH R1 (AIPlayerGPT.cpp:~30503, lane CR): `w77OwnLoopResolving` = loop pieces assembled + ANY unresolved stack object; the opponent's removal targeting Bond with no gain/loss trigger pending prints "your loop is resolving on its own - this row is not needed" on the seat's counterspell row — a hold there loses the loop. Fix: the predicate requires a PENDING loop trigger (a life-gain/loss trigger of the loop's own cards on the stack) and NO stack object of the opponent's that targets a loop component; when the opponent's stack object targets a component, print the opposite (`their <spell> targets your <component> - the loop stops if it resolves`); pin both through the live seam (the 5-link fixture must derive the predicate from the board, not pass `true`).
- F5 HIGH R2 (AIPlayerGPT.cpp:~30454, lane CR): a timed-out asked window loses its history commit — `flushWallMissRecord` consumes `mWindowSeq` N, the ask record then consumes N+1 without restaging, `pendingSeq != windowSeq` rejects the commit, and the next window says "first window I have asked you"; the wall-miss record also consumes the hold-reference stamp. This is the silent-drop suspect CR named and did not change. Fix: the staged menu commits on the ASK record regardless of intervening telemetry records (key the commit on the ask, or exclude telemetry kinds from `mWindowSeq`); pin the timeout sequence through the live poll path.
- F6 MED R2c (AIPlayerGPT.cpp:~88754, lane CR): the refutation pins reorder simplified names, not the live roster's board-derived preview facts — `Goblin (creature 1/1)` → `(creature 2/2)` in `targetPreviewFacts` makes an unchanged action "new" and reopens a hold, because `holdActionKeyRow` keeps creature P/T parentheses (only land parentheses are stripped). Under the owner's ruling the action identity is the TARGET INSTANCE, not its preview. Fix: strip preview facts (P/T, counters, status) from the target roster in the action key — keep the instance handle/name; pin the 1/1 → 2/2 case = 0 new; re-examine whether R2c's "real changes" were preview drift (if so R2c is CONFIRMED after all — say which).
- F7 HIGH R4 (AIPlayerGPT.cpp:~28315, lane CS): the optional blocking-gain accumulator meters a trigger's gain without the lifegain prohibition check (Perimeter Captain vs Erebos: prints +1 and withdraws "no block saves you"; true −1); the edict target gloss suppresses the converter damage under a prohibition but still prints the prohibited gain. Fix: one prohibition predicate (`assertcangainlife`'s) gates every metered gain — blocking, edict, converter; pins for both surfaces.
- F8 MED R6 (AIPlayerGPT.cpp:~26526, lane CS): the targeted-cover annotation enumerates every victim (`name takes N off that total, leaving M`) — 100 one-toughness attackers = 100 entries, bypassing CT's roster fold and invalidating the prompt bound. Fix: route through `joinVictimRoster` (count + shapes + K names + `and N more`) with the total stated once; pin the 100-body case under a byte bound.
- F9 MED R5 (AIPlayerGPT.cpp:~89161, lane CT): the 37,121 B bound assumes `boardEffectSnippet` ≤ 2× the tier; three 55-B clauses at the 55-B tier emit head + omitted-marker + tail = 163 B (319 with wrappers) > the asserted 266 B per name. Fix: make the snippet's byte ceiling a hard cap in code (truncate to the tier including markers) and re-derive the bound from the capped value; pin the three-clause case at the cap.
- F10 MED R11 (selfplay-harness.sh:~140, lane CR): harvest membership = deck pair + a 15 s tolerance (not 6) — accepts foreign logs at +5 s for both `125v123` and `125v162`, rejects a legitimate seat at +16 s; same-pair concurrent pools indistinguishable. Fix: match a seat log to a game by the game's OWN seat-log names — the harness knows each game's two log basenames at spawn (wagic prints them / the epoch is the game's own start) — record them in the outdir manifest at spawn and harvest exactly those; keep a tolerance only as a fallback with a warning; `--selftest` cases for the three shapes Astra executed.
Order: F1, F5, F4, F3, F2, F7 (key ruling / lost history / false loop verdict / survival), then F6, F8, F9, F10. Gate: suite THREADS=1 + PARSETEST + harness --selftest + instruction guard; lane report wave77/lane-CU.md with per-finding CONFIRM/REFUTE, RED evidence, fix, GREEN, prediction, weakest evidence.
