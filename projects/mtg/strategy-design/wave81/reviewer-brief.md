# Wave-81 step-3 brief (the loop's step three per the RESET procedure: one review seat per deck, guide IF NEEDED, KEEP is the default and a success verdict; engine/interface/card items elevated as a LIST with seq repros; then one synthesis agent. NO engine-narration seat, NO PASS/FAIL/UNTESTED tables, NO ranked docket with predictions, NO lategame specimen, NO lesson-of-wave stack.)

OWNER RULINGS IN FORCE (verbatim where quoted): reasoning happens ONLY in the model's native reasoning channel; the reply is the PLAN and then the action line; "no such rule exists. Explicitly the rule is to 'make the parser robust'. And that 'any plan, must precede action.'" (the parser reads the answer wherever it unambiguously is; an answer that PRECEDES the plan is rejected and re-asked); pilot config changes: "No."; lategame specimens: "Stop producing these."; guides are decision-time instruction only, hard ceiling 20,000 bytes, no citations/counts/history in a guide — evidence goes in YOUR review.md (Amendment 332; A333 heading = scope declaration; A334 no concessive clause for a rule's hard case — all in wave68/strategy-writing-skill-v2.md, the current edition, ~41 KB, read it in full before writing or editing a guide); never record a mechanism or hypothesis of yours as the owner's position; hands are real (mulligans, bottoming) — do not read variance as guide effect.

Repo /home/magi/Projects/wagicGPT, game dir projects/mtg. Use python for the translogs; use /usr/bin/grep (bare grep drops matches). DO NOT edit anything under bin/Res or src/, do not run git, do not run wagic; write ONLY under projects/mtg/strategy-design/wave81/deck<N>/. Report what you did NOT check.

## The corpus under review

`/home/magi/.Wagic/ai/gpt/selfplay-runs/matchups-20260917-191141-final` — 21 games, ONE binary (`archives/wagic-6e0c51238-w81step1` = master 6e0c51238), pool 146, 152, 125, 130, 123, 162 and **50 (Erosion, NEW, GUIDELESS — first seating)**, `--thinking on`, WAGIC_GPT_TIMEOUT=900, no operator token ceiling, no wall cap. 42 seat logs all with `gameend`; 0 HUNG, 0 crashed, 0 timeouts; `results.tsv` (deck0, deck1, winner 0/1, life0, life1, turn, start_epoch) and `askreplay/` (27 files) in the dir. Census = `python3 projects/mtg/tools/corpus-stats.py <dir>`, saved at `~/.gatelogs/w81-corpus-stats.txt` — CENSUS CHECK RECONCILES (2,349 records = 2,122 round trips + 226 hold_event + 1 recovery). Seat log = `<epoch>-ai_baka_deck<N>-<ptr>-vs-ai_baka_deck<M>.jsonl` (the FIRST deck number is the seat whose log it is).

**RECORD SCHEMA (P11, new since wave 80 — read this before writing a single query).** Every decision record is `kind: "window"` with a `seam` field ∈ ask (casting/activation menus) | priority | attackers | blockers | discard | bottom | reveal | order, plus `window_seq` (the window ordinal; distinct from record `seq`). What used to be `forced_close` / `wall_miss` / `menu_single_outcome` records are now FOLDED onto the window record (fields on it, joined by `window_seq`). Side records: `hold_event` (seam cast|priority; `reason` = verdict_clamped / reopen_new_lethal / reopen_rows_moved / …), `recovery`, `gamestart`, `system`, `gameend` (the counters). Per-record fields you will use: `prompt` (the full assembled user message — what the seat was OFFERED), `reply`, `choice`, `chosen_text`, `options`, `turn`, `phase`, `my_life`, `opp_life`, `latency_ms` (-1 = replayed from cache, no round trip), `fallback` (present ONLY when the heuristic answered), `thinking`, `reasoning_chars`, `off_protocol_bytes`, `plan_line_missing`, `protocol_deviation`, `reask_reason`, `hold_check_ref_seq`, `crackback_verdict`. The translog `phase` field is NOT the rendered phase — count phase-gated facts from the prompt text.

**Corpus-wide numbers (from the census; per-seat numbers are yours to compute).** 2,122 model decisions (ask 1,580, priority 379, attackers 79, discard 42, blockers 20, bottom 10, reveal 9, order 3), 1.98 decisions per results.tsv turn; 100% with native reasoning (median 6,089 chars, max 22,369); **ONE heuristic fallback in the whole corpus**: seam ask, `unparsed_reply`, in `1789690337-ai_baka_deck50-…-vs-ai_baka_deck125.jsonl` (the deck50 seat quotes it verbatim and says whether the robust parser should have read it); 0 `reply_truncated`, 0 `action_before_plan`; `plan_line_missing` 61 (2.9%, same as wave 80), `off_protocol_bytes>0` 63, `protocol_deviation` compliant 2,059 / unlabelled_plan 61 / prose_outside_two_lines 2; latency p50 97.9 s (ask p95 210.9; wave 80: 87.0 / 185.5 — reasoning median rose 5,770 → 6,089); gameend sums: `hold_windows_skipped` 1,029 (cast 702 / priority 327), `hold_events` 226 = `hold_verdict_safer_ignored` 108 + `hold_reopened_new_lethal` 15 + `hold_reopened_new_threat` 0 + `hold_reopen_answer_invalidated` 117 (+ rows_moved), `cast_decision_reopened_new_stack` 189 (NEW: V4 — a new stack object re-opens the casting decision for instant-speed rows), `ask_replays_reserved` 829 = cache 203 + repeat_latch 626, `identical_ask_answers_reserved` 626, `crossphase_identical_reputs` 90 / `_board_unchanged` 3, `crackback_verdict_lines_rendered` 312 / `crackback_lethal_blocked_away` 12 / `_folded_total` 88, `stack_death_verdict_lines_rendered` 88, `own_loop_verdict_lines_rendered` 0 / `own_loop_windows_asked` 0, `plan_cast_steps_closed` 308, `forced_close_events` 21 / `_unrecorded` 3, `async_drops` **52** (wave 80: 9 — if your seat carries many, say what dropped and whether the re-ask was right), `stack_drain_windows_asked` 64, `opp_countered_spells` 33, `plan_names_uncastable_zone_card` 25, `plan_names_stranded_card` 2, `blocker_forecast_gang` 12 / multi 34 / rows 114, `x_cast_row_refusal_markers` 2, `x_sweep_roster_markers` 3, `multi_answer_first_taken` 0, `menu_pass_no_progress` 0. Wins: deck162 5/6, deck152 5/6, deck146 4/6, deck130 3/6, deck125 2/6, deck50 1/6, deck123 1/6 (wave 80: 152 4/6, 130 4/6, 126/125/162/146 3/6, 123 1/6).

**What changed in this binary since the wave-80 corpus** (so you know what is NEW on the screen; read the lane file only if your seat's evidence touches it): wave-81 step one — lane DI (a cached activation/cast answer replays at most once per key, then re-asks; `reask_reason`), DJ (harness manifest keying), DK (`wave81/lane-DK.md`: hold re-open invalidates the held answer and the window is ASKED; **V4: an instant in hand CAN now be cast in response — a new stack object re-opens the casting decision**, `cast_decision_reopened_new_stack`; legend-rule state-based windows never served from cache; send-boundary counters), DL (`wave81/lane-DL.md`: ONE total for the crack-back verdict = the header's ADD THOSE UP + compulsory forecast; own-loop starter = an action the seat can take NOW or the gap printed; the X marker is refusal-first; cover clause on every attackers window with a crack-back; `{spare:}` colour-aware), DM (`wave81/lane-DM.md`: Exquisite Blood verified CORRECT against Oracle, engine and render — REFUTED; plan-echo latch keys on the cast OBJECT; sacrifice/discard-N seam says one card per window; TOKEN ENGINE marker on edict rows; live-counter clause; `NO LIVE CAST ROW` on dead-only menus; the echo heading renamed so no substring of the protocol label appears; cover paragraph once per prompt). Then the GPT-layer audit (`audit-2026-09/gpt-layer/cleanup-lane.md` L1–L11: keys = board state + question, counters restored, etc.) and the approved proposals (`audit-2026-09/gpt-layer/proposals-lane.md`): **P5 narration compaction (NEW REGISTER of the GAME LOG: phase only when something happened; draw + land on the turn line; pay/cast/resolve one line with cost in parens; target/use/result one line; damage carries the new life total; no fact dropped — owner approved the sample)**; **P9 the land drop is FOLDED INTO THE CASTING MENU as a row (no separate land ask; owner: "try it. Revert if it degrades behavior.")**; P10 blockers ORDER: whole-order answer (`ORDER: B2, B1, B3`; partial/malformed falls back to per-pick asks — 3 order windows this corpus, first live firing: whoever's seat has one says whether it parsed); P3/P4/P6/P8 byte folds (identical hand entries collapsed, two dominant hand tags, one clause per number on a row, only the range grammar the menu used); P12 the source split (no behaviour). Also bug-list #11/#12 (menu identity dispatch; the `garbageReaddRefused` guard — if the dev stderr line `WAGIC addToGame REFUSED a garbaged element` appears in any game's stderr in the corpus dir, elevate it HIGH with the game).

## Per-deck agent (one per deck: 146, 152, 125, 130, 123, 162, 50)

Purpose (owner's words): read the transcripts of your deck, compare the LLM's actions to the instruction set (the guide `projects/mtg/bin/Res/ai/baka/deck<N>_strategy.txt` — the ONLY guide loaded at runtime; `wave62/general-strategy.md` is NOT loaded — plus the reply protocol in the prompt), find issues in the game, the interface, and the guide, and surface anything else worth the core loop's attention.

Method: for each of your deck's 6 games (your seat's translog; the opponent's for context), trace the decisions that decided the game. Classify each misplay PERCEPTION (the model misread a true surface, or the surface was false/missing → ENGINE/RENDER item, cite file + seq + the rendered line) vs STRATEGY (read correctly, decided against the guide or against good play → guide item). Note fallbacks, re-asks (`reask_reason`), async drops, and any decision where the prompt's information was insufficient or wasteful. Two NEW surfaces need a deliberate look from every seat: (a) **P9** — on windows whose casting menu carries a land row: did the seat play its land when it should (a land in hand, a drop available, a spell it wanted castable only after the drop)? Count the turns where a land drop was available and NOT taken while a land row was on the menu, and the turns where the land row was taken instead of a spell that should have gone first (the owner's revert criterion is "degrades behavior" — give him the count and the seqs, not a verdict); (b) **P5** — any decision where the compacted GAME LOG omitted or mis-stated a fact the seat needed (quote the log line and what the old register would have said).

Output: ONE file, `wave81/deck<N>/review.md`: (1) game-by-game outcome + the deciding decisions with seq citations; (2) engine/interface/card items ranked HIGH/MED/LOW with a concrete repro (file, seq, the rendered line) — the primary deliverable; (3) the P9 and P5 counts above; (4) guide verdict: **KEEP as is** (a success verdict — state why) or **EDIT** — if EDIT, also write `wave81/deck<N>/strategy.txt` (the full revised guide, starting from the live one) and list each edit before→after with the seq that paid for it IN review.md; the guide itself carries NO citation, count, or history; hard ceiling 20,000 bytes (the live guides sit at 19,832–19,995 B — an EDIT that adds a rule must say which rule pays its bytes and which goes); byte-reclaim or reordering edits are NOT edits (do not make them); (5) OPTIONAL proposals only when your evidence supports them: `wave81/deck<N>/general-proposals.md` (general-guide changes) and `wave81/deck<N>/skill-proposals.md` (strategy-writing-skill changes; read wave68/strategy-writing-skill-v2.md first — a proposal that restates an existing amendment is noise). No prediction tables, no rotation evidence, no docket bookkeeping. Final message: ≤ 10 lines.

**deck50 seat only (Erosion — `projects/mtg/bin/Res/ai/baka/deck50.txt`: 0 creatures; Damnation ×4, Wrath of God ×4, Evacuation ×3, Moat ×2, Dream Fracture ×4, Glimpse the Unthinkable ×4, Tome Scour ×4, Traumatize ×2, Memory Erosion ×4, Howling Mine ×4, Forced Fruition ×1, 24 lands WUB).** The deck played GUIDELESS this corpus (1/6). You AUTHOR its initial guide: `wave81/deck50/strategy.txt`, per wave68/strategy-writing-skill-v2.md (the format, the scope-declaration heading, decision-time rules only, ≤ 20,000 B — smaller is fine; a first guide need not fill the cap). The plan is MILL: the deck wins only by emptying the opponent's library while wraths, Moat and Evacuation keep it alive; Howling Mine / Forced Fruition accelerate the opponent's draws INTO the mill; Dream Fracture is the only counter. Ground every rule in what the six transcripts show the seat doing wrong or right without a guide (cite the seqs in review.md, never in the guide); verify each card's live text in the prompt's decklist block rather than from memory. Your review.md still carries sections (1)–(3) and (5).

Verification for any strategy.txt you write: `wc -c` ≤ 20000, and `python3 projects/mtg/tools/check-reply-instructions.py projects/mtg/src wave81/deck<N>` must report no reply-format instruction in the guide (a guide never tells the model how to format its reply — the protocol owns that).

## Synthesis (one agent; runs after the seven seats)

Read every `wave81/deck<N>/review.md` and any `general-proposals.md` / `skill-proposals.md`. Decide each proposal ADOPT or REJECT with one line of reasons. ADOPT for the general guide = edit `wave81/general-strategy.md` (a copy of wave62's R331 edition — the current one; verify the prefix with cmp); ADOPT for the skill = append a numbered amendment to a copy `wave81/strategy-writing-skill-v2.md` (byte-verbatim from wave68's, verify with cmp) — only when a seat's evidence shows the current edition producing a bad guide rule. Write `wave81/synthesis-notes.md`: the decisions, and a MERGED LIST of every engine/interface/card item the seven seats elevated, de-duplicated across seats, HIGH first, each with one repro (file + seq) — that list is the next step one's bug list input. If a seat's guide edit contradicts an adopted general change, note it; do not edit deck guides yourself. Final message: ≤ 10 lines.
