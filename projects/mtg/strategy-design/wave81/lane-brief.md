# Wave-81 step-one lane brief (base = master after wave80 known-bugs; evidence = wave80/known-bugs.md §U + wave80/engine-seat.md + wave80/deck<N>/review.md)

RULINGS THIS WAVE TOUCHES: 000 — reasoning only in the native channel; reply = PLAN then action, nothing else; no instruction licenses prose; the PARSER IS ROBUST (owner 2026-09-12: "no such rule exists. Explicitly the rule is to 'make the parser robust'. And that 'any plan, must precede action.'") — it reads the answer wherever it unambiguously is and rejects only an answer that precedes the plan or is ambiguous; parser robustness is loop-owned; caps never bound reasoning; the golden protocol TEXT (reply-protocol.txt / kReplyProtocol) and the thinking regime are owner-ruled — render brackets and row text may change; V16 changes RENDER text only (the echo heading), never the protocol label; V6 is an ENGINE rules fix verified against Oracle before any edit; the parser is robust by ruling (DD merged; first live refusal `126v162` seq 62 read correctly). 00 — full games; a game answered by heuristics is not a corpus game. 3 — suite THREADS=1 0 failed (known flakes: lifeline, merrow_reejerey, intrepid_adversary_repeated_payment), PARSETEST 0 failed. Never remove, cap, or auto-answer a legal option (R1's loop guidance is an ANNOTATION plus the model's own hold row — nothing is collapsed or auto-answered; R9 is MEASURE ONLY). No VPK. No AI attribution. NO CORPUS from a lane; no lane sends requests to the pilot this wave.

Standing mechanics: as wave71/lane-brief.md (worktree-only; `make -f Makefile.sdl -j8`; both link guards OK; binary freshness; every wagic run under `systemd-run --user --scope -q -p MemoryMax=4G -p MemorySwapMax=0 --`; suite THREADS=1 as a detached unit `w81-<LANE>-suite` waited with a FOREGROUND until-loop (no Monitor); count both failure strings; PARSETEST from bin; Edit tool for UTF-8 sources, byte-splice for MTGRules.cpp/GameObserver.cpp/primitives; U+FFFD 0; RED-on-base evidence + a falsifiable prediction per fix; commit with explicit paths, `git add -f` under bin/Res; write wave81/lane-<LANE>.md; touch `~/.gatelogs/W81_<LANE>_DONE`; weakest evidence). LESSON OF WAVE 80: (1) a cached answer is a MODEL decision made ONCE — any path that re-executes it on a changed board without re-asking is a loop waiting for an untap effect (Thraben Doomsayer + Intruder Alarm: one token, 3,674 replays, the game hung 3,641 s; and the same family sat unhung at deck126 in runs of 39 and 17) — every replay path carries a bound AND a board-change exit, and a re-open that the same latch immediately re-clamps is not a re-open (V3); (2) a helper's textual merge resolve is not a merge — nine ctor initializers were dropped by the auto-resolve and every gate stayed green; verify SYMBOL SETS across the merged branches (check-merge-inits.py after every merge-lane.sh); (3) every mid-run harness check keys on the run's MANIFEST — a shared directory scanned by time will read a neighbour's logs (the false regime gate killed a correct rerun); (4) a verdict must use the SAME total the screen publishes — the crack-back verdict took the raw combat sum while the header's own ADD THOSE UP printed a larger one, 11 self-contradicting windows (V5). LESSON OF WAVE 79: (1) a counter with no per-record trace cannot be adjudicated — `hold_verdict_safer_ignored` 59 / `hold_reopened_new_threat` 0 had to be called UNTESTED by default, the exact outcome the wave-78 lesson exists to prevent; every new counter ships with a record field naming the event it counted, and every render counter increments AT THE SEND; (2) a verdict that lives only in a key is invisible — `[crack-back verdict:` / `[stack death verdict:` rendered 0 times in 2,203 prompts (wave 77's lesson, re-confirmed); (3) the model follows what is ON THE SCREEN: the casting menu's format example built from row 1 was picked in 62/139 windows, and a plan echo with its cast clause trimmed made the seat decline its own announced X — fix the emitter the seat reads, not the model. LESSON OF WAVE 78: (1) a pin can pass while the live path renders NOTHING (S6: pinned rendered bytes, 0 renders in 42 logs) and a fix can be right at the helper while the caller's gate never admits the real case (S3: `planTurn == nowTurn`, and 40/44 windows lacked the repeat row the gate demanded) — every fix ships with a CORPUS-DERIVED fixture: the actual prompt/record pair that motivated the item, driven through the LIVE caller, plus a predicted NON-ZERO counter value in the next census; a counter that reads 0 after the fix is a FAIL, not "untested"; (2) a key-stability claim is proven only on the corpus's own re-asked pairs — the CG shape survived four waves of green per-row pins because no pin ever took a real re-asked prompt pair as its input. LESSON OF WAVE 74 stands: any NEW annotation carrying a board-derived number must be proven OUTSIDE every key (ask, async slot, hold latch, hold-check, option-set) with a PARSETEST pin that two windows differing only in that number yield identical keys, and the held set in a key pin is built the way the LIVE seam builds it (holdActionKeyRow via mLastMenuRows). LESSON OF WAVE 77: (1) a verdict the engine computes but pushes only into a KEY is invisible to the model — every verdict that should change a decision must be RENDERED, and its pin must assert the rendered bytes, not the key (S6); (2) a field named `_seq` that is not a record seq wastes eight seats — name telemetry by what it is and pin the join (S7); (3) the census must sum every record kind, including the new ones a lane adds. LESSON OF WAVE 76: (1) a bracket that prints a NUMBER or a COUNT must be pinned on the cases where the count is zero, one, and a reorder — Q1 shipped with the referent fixed and the count still wrong on reorders and disappearances; (2) a fix-lane finding whose fix is "compare more state" (CQ F4) can make a clause fail-closed into silence — pin that the positive clause still prints on the shape it was built for; (3) a clause that carries a number about LIFE must name the EVENT that moves it, or the model reads it as a statement about the row's mana cost (R3). LESSON OF WAVE 75: (1) an instruction's REFERENT must be something the reader can see — the hold-check bracket spoke of "the last window" meaning the last one the engine BUILT, while 3,402 built windows never reached the model; any bracket that refers to history refers to ASKED history; (2) a helper that hardcodes its input (the wave-74 stats script ignored its argument) reports the wrong corpus with full confidence — new tools take the run dir as an argument and echo it in their first output line; (3) a prediction's negative clause ("non-zero means lost") needs its own pin, not just the positive.

## Lane DK — holds, response windows and the send boundary (V1, V3, V4, V9, V15)
V1 adjudicate DI against the step-one binary's cached-replay runs: fixture from `126v125` seq 96 (run of 39) and `126v162` seq 33 (run of 17) through the live `chooseOrderedAction`; pin that a legend-rule / state-based-action window (`146v162` seq 27) is never served from the ask cache; predict `cached_replay_reasked` > 0 with `reask_reason: cached_replay_bound` on the next corpus. V3 a `reopen_new_lethal` / new-threat re-open INVALIDATES the retained hold answer for that key before the hold check runs, so the re-opened window is ASKED; fixtures `125v162` seqs 18-24 and `125v126` records 92/93 (Exquisite Blood on the stack) through the live latch; predict every `hold_event` re-open is followed by an ask record on the same window and `hold_reopened_new_threat` > 0. V4 the casting decision is per (phase, stack state): a NEW stack object re-opens it for instant-speed rows; key-stable (the stack object's identity, not its text, enters the key — LESSON OF WAVE 74 pin); fixture `152v125` seq 101 (Fateful Absence vs Essence Scatter, 6 mana up); predict `[no cast row now: you already answered this phase's Casting decision` on non-empty-stack windows → 0 and `unparsed_reply` 0. V9 `sent` is set by the transport call, the priority seam stamps faces like the ask seam; pin counter = rendered = stamped on a fixture that mixes a cached decline, a suppressed hold window and a real send. V15 per-record trace for the six untraced counters; `hold_event` carries the record seq of its window (name the field `window_record_seq`, pin the join).
## Lane DL — verdict truth (V5, V7, V8, V10, V13-spare, V17-gap)
V5 one total: the crack-back verdict and the bracket next to the rows use the figure the CRACK-BACK line publishes after `ADD THOSE UP` PLUS the compulsory draw forecast (extend DH F9's fold from the blockers verdict to this one); fixtures `152v130` seq 17, `146v162` seqs 21/53, deck123 `162` seqs 97/137; predict 0 windows where a `you survive` verdict shares a screen with `that would KILL you`. V7 the own-loop starter is an action THIS seat can take now or a trigger this seat causes; opponent-dependent triggers (block, attack-into) never qualify; a loop half is never a starter; no starter → the gap line (DH F6's branch, never printed — make it print on deck126's `126v130` seq 39 board); pin both deck126 boards; predict `It starts from Perimeter Captain` 0 and the gap line > 0. V8 the cast-row `best X` marker takes the X-menu's refusal-first wording on a refused cast (fixture `162v130` seq 75); the X-pricing sweep marker carries U7's roster (fixture `130v152` seq 49); pins. V10 no annotation renders INSIDE an offered attacker's row (`146v162` seq 52 — the `[held back …]` tag moves to its own line); the stay-home cover clause prints on every attackers window that prints a crack-back (`152v146` seq 19); pins; predict cover-clause count = crack-back-header count on attackers prompts. V13 the cleanup `{spare:}` verdict is colour-aware (`162v130` seq 47). V17 the gap branch pinned.
## Lane DM — engine rules, echo, intel and bytes (V2-residual, V6, V11, V12, V13, V14, V16, V17)
V6 FIRST verify Exquisite Blood's Oracle on Scryfall and read the `lifelostfoeof` trigger's damage path in the engine; suite fixture: Exquisite Blood out, opponent takes combat damage → controller gains that much (and Sanguine Bond chains once per event, no infinite loop in the fixture — bound the test); fix the ENGINE, not the card, unless the script itself is wrong; report both. V11 the plan-echo cast latch keys on the cast OBJECT (`152v125` 15→16 two-sentence shape; `152v126` 19→20 target window); pins. V12 the sacrifice/discard-N seam says one card per window and N windows; `multi_answer_first_taken` in the gameend census; fixture `152` seq 217. V13 `TOKEN ENGINE` marker on the edict cast row and the forced player-target ask (`125v123` seqs 238/239); a `they have countered N times this game` clause when the opponent's graveyard holds counterspells (`125v123` seq 143 — intel, one line); `NO LIVE CAST ROW` on menus whose only cast rows are dead (`126v130` seq 15). V14 the crack-back cover paragraph once per prompt with per-row deltas (`162v146` seq 30); narration trim budget a function of total prompt size; before/after with prompt-byte-census.py — V17 fix its line-297 crash first, and the `x<N>` exemplar goes neutral, and cycling rows say they discard. V16 rename the echo heading so no substring of the protocol's `PLAN:` label appears in it (render text only; the golden protocol is untouched — verify with `git diff --stat` on reply-protocol.txt = empty); pin. V2 residual: runmanifest.py's degraded (manifest-less) path prints `MANIFEST ABSENT - scanning by time` on its first line.
## Lane DN — fix lane for the Astra review (wave81/codex-review.md) — seeded after step-one gate 1
Shape as wave80's lane DH: per-finding CONFIRM/REFUTE with RED evidence, fix, GREEN, a NON-ZERO prediction; gate = suite THREADS=1 + PARSETEST + harness --selftest + corpus-stats --selftest + instruction guard + check-ctor-init + check-merge-inits; report wave81/lane-DN.md; touch `~/.gatelogs/W81_DN_DONE`.
