# general-suggestions.md (core-prompt proposals) — deck102 wave 22

PASS — no core-prompt change proposed this wave.

The concrete items this corpus surfaced route to other layers, not the core prompt:
- R-DFC-FLIP (DFC "Flip Side" toggle offered as a repeatable priority pseudo-action) -> ENGINE / option
  generation -> notes.md.
- R-POX-DISCARD-TARGET (a player's own forced discard rendered as a confusing "TARGET CHOICE") ->
  REPRESENTATION -> notes.md.
- painland crutch demotion + Tergrid DFC gap -> GUIDE -> strategy.txt.
- s42 stale_echo NEW shape -> harness echo family -> deck140 owner + notes.md.

One WATCH for synthesis (not yet a core-prompt ask): the behind/confused giant-reply tax now shows as
VERBATIM-REPEATED reasoning loops that truncate (s29 repeats an identical mana-arithmetic block 6x; s21
repeats an identical flip-flop dozens of times). Answer-first contains the parse-failure risk (0 unparsed),
so this is not urgent, but a harness-side "detect a verbatim-repeated reasoning block and cut the generation"
guard would reclaim the wasted decision AND remove the window in which a looping body disavows its own
committed CHOICE (the 2 retracted_choice fires). Routing this to notes as a harness candidate rather than a
core-prompt edit, since the core prompt already gets a leading coded line out of the model; the failure is in
the tail, not the head.
