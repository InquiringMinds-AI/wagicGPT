# deck27 general-suggestions.md — core-prompt proposals (wave-22)

Layer-routing enforced: single-seat items CANNOT add or cut a core line; engine/representation and
parser items live in the notes.md ledger, never in core prompt text. **This seat proposes NO core-prompt
change.**

## Core prompt: PASS (no change proposed)

Nothing this seat found is both (a) 2+-seat convergent AND (b) properly core. Detail:

1. **Answer-first (0 fallbacks) validated again — but it is HARNESS/PARSER, not core prose.** 135/135
   clean coded first lines, two 15.7k/9.7k giants parsed clean. The NEW finding this corpus is a
   DOWNSIDE at the same layer (commit-lock froze a snap misplay at vs109 s15) — still parser/harness,
   still not core. Recorded as cross-seat data for synthesis (every reviewer should be seeing the
   0-fallback effect; synthesis should also poll for harmful commit-locks). No core text follows.

2. **Wither annotation round 2 — ENGINE, closed.** Validated, zero false positives, incl. a pro-green
   "neither dies" interaction and big-trample+shrink math (notes.md #1). Not core.

3. **Multi-attacker blocker allocation (vs109 s15 misplay) — GUIDE, deck-specific.** The fix is a
   free-kills-first ALLOCATION rule; it belongs in the per-deck blocking section (deck27 is a real-
   blocker deck), NOT core. Applied to strategy.txt this wave. If synthesis sees the SAME allocation
   error across multiple real-blocker decks, THEN a core blocking-priority line could be warranted —
   flagging as a cross-seat watch, not proposing core text on N=1.

4. **Late-stall durdle revision — GUIDE off-case, unexercised (3rd corpus).** Owns the guide layer;
   trigger did not occur. Not core.

5. **No-answer-to-lifegain (vs140) — DECK CONSTRUCTION, not prompt.** Routed to notes.md #5 for the
   roster owner. Not core, not guide-fixable.

## Confirmations for the synthesis agent (no text change)

- **Trust-the-rendered-number held again with ZERO mana fallbacks** on the hybrid {U/B} base — Nth data
  point. Reinforces the existing rule; no new prose.
- **The evasion/protection win-path is deck-specific** (drove the sole win, vs44) — correctly kept in
  the per-deck guide, out of core.
- **CAUTION for synthesis:** do NOT read deck27's 1/6 as evidence any wave-22 batch item hurt — the
  guide is frozen, tax is 0, and deck27 had ZERO exposure to the fetch/N9/echo/transform/painland
  changes. The swing is exogenous (pool difficulty + variance). Symmetric to wave-21's caution about
  not reading its 5/6 as a batch success.

## Engine/representation/parser items produced (routed to notes.md, NOT core)
- Wither round-2 validation (closed).
- Answer-first commit-lock 1st harmful instance (parser/guide seam) — cross-seat synthesis watch.
- Ahead-loop no-recurrence (decode watch, unchanged).
- deck27-has-no-lifegain-answer (construction, roster owner).
