# Skill proposals — deck44 wave 22

Two small proposals; both reinforce existing rungs rather than open new ones. The skill is mature and my
review mostly APPLIED it (the crutch-lifecycle demotion) cleanly.

## P1 (ADOPT — reinforcement) the OPTION-LINE-ANNOTATION demotion terminus, confirmed at a 2nd seat + a DELETE refinement
Wave-21 Method-headline-3 wrote the crutch-lifecycle DEMOTE rung and gave the option-line-annotation
terminus a deck14 witness (bounce-on-stack: 26 windows, 25 declines). deck44 is a SECOND, independent
seat confirming the SAME terminus shape for a different annotation:
- Annotation: GFTT friendly-only `(warning: this would harm your own side ...)`.
- Confirmation instrument (matches the rung's "(i) misplay-count -> 0 AND (ii) DECLINE-RATE on the
  annotated cast"): self-GFTT misplays = 0; friendly-only GFTT chosen 0/28 windows.
- Demotion applied: walkthrough 5 lines -> 3 (keep anchor + marker string).

Add to the rung a small refinement earned here: **when the crutch is a SITUATIONS-section bullet (a
"situation that decided a game") and the underlying case is now engine-annotated AND never actually
decided a game in the corpus, DELETE it outright rather than demote it** — a SITUATIONS entry has to earn
its slot by having decided a game; an engine-carried, never-deciding case fails that test twice over. (I
deleted deck44's self-GFTT SITUATIONS bullet and demoted only the reference passage in the mechanics
section.) This distinguishes the two homes: mechanics-section crutch -> DEMOTE to anchor; SITUATIONS
crutch for an engine-carried non-deciding case -> DELETE.

## P2 (CONSIDER — wording) state the PRINCIPLE, not just the triggering CASE, for a "let it resolve" rule
Observed: deck44's guide said "A Cruel Edict when you have no creature does NOTHING - let it resolve." The
model applied that line while it DID have a creature and visibly muddled ("Wait, I *do* have a creature"),
because it treats a guide line as a rule to pattern-match, not a principle to apply. It reached the right
answer but spent reasoning fighting the incomplete rule. The Step-5 fix: where a "let it resolve" rule has
a non-trivial complement (here: you DO have a creature), state the PRINCIPLE that covers both branches
(the principle is "don't Arcane Denial into their 2 cards to save a cheap body") rather than only the
zero-case. Not a new rung — a concrete instance of the existing positive-framing / word-for-the-executor
guidance, worth a one-line example in Step 5 if the same "case stated, complement muddled" shape shows at
another seat (single-seat so far -> CONSIDER, promote on a 2nd witness).
