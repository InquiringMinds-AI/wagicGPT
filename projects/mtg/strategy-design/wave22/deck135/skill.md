# Deck-135 wave-22 — strategy-writing skill proposals

## PASS (considered, no change proposed) — with one crutch-pacing observation logged for synthesis.

Nothing this corpus argues for a change to `wave21/strategy-writing-skill.md`. Reasoning:

- The deck135 guide is FROZEN and took zero decision-level leak; there is no guide-authoring failure
  to distill into a skill rung.
- The one skill-adjacent situation is the CRUTCH-LIFECYCLE rung. The frozen guide carries extensive
  fetch re-crack language ("a line reappearing does NOT mean you cracked it... if you still have no
  extra land in play, you have NOT cracked yet") that was written for the now-FIXED ENGINE-F1 fetch-
  fizzle / item-8 re-arm era. Per the skill's crutch-lifecycle rung, that is a RETIREMENT CANDIDATE.
  I deliberately did NOT retire it this wave, and the reason generalizes into a small pacing note:

  **Crutch-retirement pacing:** do not retire a crutch on the SINGLE corpus that first validates the
  engine fix it guarded against. The binary here (`/tmp/wagic-1d8ed8194`) was live-UNVALIDATED before
  this run; 10/10 clean fetches is strong but one-corpus-old, and the crutch is the safety net if the
  fix regresses. Also, the fetch re-crack language does double duty — half of it ("an uncracked fetch
  re-lists every window; crack it for the color") is STILL a true, load-bearing engine fact
  independent of the fizzle. So retire only the fizzle-specific half, and only after a SECOND
  confirming corpus. This is a refinement of the existing crutch-lifecycle rung (retire when the
  defect is fixed) with a pacing qualifier (wait for n=2 confirmation; separate the still-true half
  from the fizzle-specific half), not a new rung.

If synthesis wants this crutch-pacing qualifier promoted into the skill text, it is a one-sentence
addition to the crutch-lifecycle rung. I am flagging it rather than editing the 433KB skill, since a
single seat's observation is thin evidence for a skill-level rule and the existing rung already
covers the main case.
