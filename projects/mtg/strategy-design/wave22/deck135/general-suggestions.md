# Deck-135 wave-22 — core-prompt / general suggestions

## PASS (considered, no core-prompt change proposed).

All actionable findings from this seat route to the ENGINE/HARNESS layer (notes.md), not the shared
core prompt. Layer-routing check:

- ENGINE-F1 fetch fix, ENGINE-R1 partition, ENGINE-R6 transform annotation, ENGINE-R4 activation,
  the R-cosmetic transform-name bleed -> all ENGINE layer -> notes.md.
- HARNESS-ECHO-Transform (the transform dual-name echo mismatch that stale_echoed a correct pick)
  -> HARNESS/parser layer -> notes.md.
- The frozen guide took no decision leak -> no guide change (strategy.txt is a byte-copy).

One cross-cutting OBSERVATION worth surfacing to synthesis (not a core-prompt edit request): the
shipped (d) "[available NOW...]" transform annotation was a decisive success — it fixed a misplay
class (the manual-graveyard-recount decline) that had persisted for multiple waves, and it did so by
ANNOTATING THE OFFER rather than adding core-prompt instruction text. This re-confirms the wave-21
thesis and argues for the ENGINE representation layer (not the core prompt) as the right home for
"this threshold/condition is already satisfied" signals on any condition-gated option (transforms,
level-ups, monstrosity, threshold/delirium abilities, etc.). That is an ENGINE-representation
recommendation (route: engine devs), which is why it does not become a core-prompt suggestion here.

No core-prompt proposal this cycle.
