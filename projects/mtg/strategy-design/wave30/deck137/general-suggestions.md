# deck137 -> general-strategy (core prompt) proposals -- wave 30

## PASS. No core-prompt change proposed. (25th consecutive core PASS from this seat.)

Nothing this corpus routes to the SHARED core prompt. Rationale by layer:

- The open defect (offer-then-can't/won't-complete convoke: March X-spell DEFERS or SILENTLY
  no-ops, Loxodon fixed-cost SILENTLY no-ops) is ENGINE, not prompt -- routed to notes.md
  (N-137-MARCH-XCONVOKE and N-137-LOXCONVOKE-SILENT-NOOP both STILL OPEN; new sub-finding that the
  fallback metric undercounts). A core-prompt line cannot make an unresolvable convoke payment
  complete.
- The behavior levers (X>=2-3 March affordability threshold; one-attempt-per-card anti-re-pick)
  are DECK-SPECIFIC to March/convoke and live in the deck137 guide, not the core prompt. Both
  worked this wave (model computed X=3 correctly; churn tamed to <=2 picks). Generalizing them
  would add noise for every non-convoke, non-X seat.
- Comprehension surfaces the core prompt owns (instance handles #N, land-identity tags, adventure
  bodies, shockland ETB "pay 2 life" menus, blockers legality, the X-announce step, the tapped-tag
  reword) all rendered clean and bound correctly: 0 unparsed, 0 empty, 0 stale_echo, 0 retracted,
  0 illegal gang-piles across all 6 logs. The tapped-tag reword rendered 131x and was read
  correctly every time. The X-announce seam parsed cleanly when it fired (vs152 `X = 3`).
- g3 (loot/discard framing) is N/A at this seat -- deck137 has no loot/discard effect; no such ask
  fired, so there is nothing to confirm the new framing against here.

The core prompt is doing its job at this seat: every decision surface the model needed was
present, legal, and correctly disambiguated. The residual failures are one (partially-fixed)
engine defect and zero prompt defects. Core: PASS.
