# deck122 -> general-strategy (core prompt) suggestions (wave 30)

PASS (25th consecutive expected).

Nothing from the deck122 seat routes to the symmetric core prompt. The corpus was clean from
the core-prompt's seat: ZERO fallbacks across all 6 deck122 seats, no parser contradictions,
no unparsed/retracted/deferred records, blocker/attacker bundles parsed correctly (vs139
seq19: 5-attacker bundle accepted). The core prompt's protocol, narration, and choice-
formatting handled a lifegain engine, a planeswalker (+1 loyalty chain), may-triggers,
conditional-P/T renders `(printed X/Y)`, and a 26-turn 42-record grind without a single seam
failure. No core change indicated.

Every deck122 finding is deck-specific (life-as-engine, Felidar 40-line, Serra/Divinity
thresholds, Sorin +1 chain -> strategy.txt, now all VALIDATED and frozen), a skill-layer
pattern (alt-win-threshold rung, resource reframe, adjudication-metric caveat -> skill.md),
or an engine/probe item (Felidar alt-win live firing -> notes.md N-122a).

Layer-routing note for synthesis (unchanged from wave 29, reconfirmed): the two headline
deck122 teaches (life-threshold play, alt-win pursuit) are STRATEGY teaches for the deck
guide, NOT the symmetric core -- generalizing "value your life / pursue alt-wins" into the
core would be noise for the aggro/control decks that do not care. Do not promote.
