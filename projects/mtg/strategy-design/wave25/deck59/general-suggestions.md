# deck59 — general-prompt (core) suggestions: PASS (20th consecutive)

No core-prompt change proposed. This first-guided validation confirms deck59's two levers
are DECK-SPECIFIC and correctly live in the guide, not the core:
  1. The W/B-off-Plains+Swamp mana simplification + pip table — a per-deck FACT. The core's
     generic hybrid handling ("a hybrid pip can be paid with either color; if a Cast option
     is listed it is payable; trust the Mana-available line") was necessary but not
     sufficient guideless (3/6 with the veto/mulligan-anxiety live); the per-deck fact
     table is what collapsed the tax. Correct layer split — the fact is deck-specific.
  2. The persist-makes-your-sweeper-one-sided GO-gate — a per-deck engine fact, and the
     exact INVERSE of the deck102 symmetric-card STOP-gate that is also kept in-guide
     pending a second seat. Still single-seat; still belongs in the guide.

The rule-3 sixth face (symmetric-effect-asymmetric-in-your-favor via recursion) remains
flagged for eventual CORE promotion ONLY if a second recursion/undying deck reproduces the
UNDER-fire (see skill.md). Single-seat now, so it stays in the guide. Core PASS — the
validation adds no core-lane signal; both levers stayed correctly below core.
