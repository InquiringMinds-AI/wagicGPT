# Skill note (wave 25, deck59) — FIRST-GUIDED VALIDATION of the guideless-Step-0 method: a 3/6 -> 6/6 jump, both new-face levers confirmed at the seat

Not a PASS. A first-guided corpus that jumps 3/6 -> 6/6 is skill-METHOD evidence, and the
lever that carried it validates a Step-0-bis authoring claim that had only been asserted.

## What made this guide land (three confirmations, one per method claim)

**1. The rule-3 SIXTH face (symmetric effect ASYMMETRIC-in-your-favor via recursion) —
its GO-gate + recursion-WHY authoring template is VALIDATED (still single-seat).** The
wave-24 skill added this face with a specific prediction: a scoped go-gate keyed to a
board-readable trigger MUST carry the recursion WHY or "the weak executor keeps
pattern-matching board-wipe = slow / kills my stuff = bad." Wave-25 confirmed the
prediction directly — at the exact wave-24 loss spot (vs27 s14, myL2 facing lethal) the
pilot cast the sweeper and its reasoning REPRODUCED the WHY nearly verbatim ("my persist
creatures return... trades my single card for their entire board... the only move that
prevents immediate death"), and it correctly HELD the sweeper while ahead (vs109, life flat
at 13, the wave-24 LOSS matchup now a dominant win). The go-gate fired AND the closed
exception held. **Keep the face tagged single-seat for PROMOTION** (promotion still needs a
SECOND recursion/undying/"you-gain-what-they-lose" deck reproducing the UNDER-fire) — but
record that its authoring template is now VALIDATION-CONFIRMED, not merely proposed: a
go-gate keyed to a board-readable trigger + the mechanism WHY countermands the core's
"don't overcommit into a sweeper" reflex for a self-recovering board.

**2. The belief-that-VETOES corrective FACT collapses BOTH the cast-tax AND the
mulligan-anxiety in one corpus (third seat, now with a full-cost witness).** The wave-24
skill flagged that deck59 gave the veto-face a NEW mulligan-quality cost. Wave-25 shows the
single corrective FACT (rule #1 mana simplification + pip table + "if it's listed it's
payable, trust the annotation") killed BOTH: zero false vetoes, zero color-anxiety
mulligans, ask-giants >4k 33->8. Durable lesson for authoring a mono-off-color hybrid deck:
one mana-simplification FACT is sufficient for the whole face (cast, land-play, AND
pregame keep) — do not write separate mulligan prose; the same fact covers the keep.

**3. Crutch-lifecycle matched-pair completed at a combat seam (annotate-to-teach -> ship
the FACT -> read at the decision).** The guide narrated the persist-return VALUE in rules
#2/#3 (the crutch); the b6 batch shipped the return narration + trade-annotation return
clauses (the FACT); wave-25 confirms the model now reads the value AT THE BLOCK (11/12
block decisions cite the return, blocks made because it was visible). This is a clean
reference witness that a guide-narrated synergy and a representation FACT are complementary
— the guide teaches the pattern, the FACT makes it legible at the seam where the decision
is made. Do NOT demote the guide's rule-#2/#3 recursion prose because the FACT shipped: the
FACT makes the return VISIBLE, but rule #2's sweeper GO-gate is a BELIEF-correction (the
model still pattern-matches "wipe = bad" without it) — a belief-correcting crutch needs the
stronger 2-corpora bar before demotion, and this is only corpus 1.

## Method reflection: a perfect record does NOT mean zero residual tax
The 6/6 hid nothing on the LEVERS (both fire correctly), but it did NOT eliminate the
spiral — it RELOCATED it (exactly the wave-24 headline-5 corollary about flooring one seam
moving the spiral to the next). The mana spiral collapsed; the residual >6k giants moved to
Restless Apparition pump-timing (a REPRESENTATION ambiguity, engine-lane) and tight-game
combat math. Reviewer discipline for a first-guided WIN corpus: audit reasoning-LENGTH by
decision TYPE, not just the win column — a validated lever can co-exist with a relocated
tax that is now someone else's lane (here, the engine's).
