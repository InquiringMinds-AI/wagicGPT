# deck137 general-suggestions.md (core prompt) — wave 25

## PASS (would be the 20th consecutive core PASS)

No core-prompt edit is justified from this seat. Every candidate routes lower or is a positive
confirmation of core behavior already shipped:

1. **b2 NATURAL-STOP precedence — CONFIRMED WORKING, no change.** Four first-vs-final disagreements at
   this seat (deck27 s19/s40, deck59 s46, deck131 s37) all resolved correctly: the model revised to a
   better final answer and core took the final coded line. This is the wave-25 b2 mechanism doing
   exactly what it should. Note to synthesis: b2 validated positively at deck137; nothing to add.

2. **The "resolved adventure body misread" (deck27 s21)** — the model treated a battlefield
   `(1/1) [adventure]` creature as "not a real 1/1" and declined a legal Lovestruck swing. This is a
   MODEL BELIEF against a CORRECT render (the (1/1) is shown), addressed at the DECK layer this wave
   (guide paragraph). It is SINGLE-SEAT and deck-specific (Lovestruck/adventure). WATCH for synthesis:
   if a SECOND deck shows a resolved-body tag ([adventure], [from exile], token) read as "not a real
   creature," THEN the fix is representation-layer (drop/annotate the tag on resolved bodies), not
   core — flag it, do not act on one seat.

3. **The generic-seam over-deliberation spiral** (5 replies 12.8k–15k ch on land-drops/priority/casts)
   is corpus-known ("long deliberations persist but nothing breaks") and harmless (b2 protects
   correctness). A general "commit briefly, don't re-derive rules" directive WOULD be core if ever
   wanted — but it is not justified on harmless latency alone, and the skill's PROPOSAL 1 argues the
   right response is to STOP flooring, not to add a core clause. No core action; noted as the standing
   latency phenomenon.

Core's existing act-floor, interface contract, attack-floor, blocking, NATURAL-STOP precedence, and
TRUST-THE-RENDERED-NUMBER language are all present and correct for this deck. The deck's residual
leaks (tempo/board-development, late stabilization, the relocated generic-seam spiral) are per-deck,
representation, or latency — addressed in strategy.txt and notes.md, none in core.

Convoke "trust the offered alternative-cost option, the engine taps for you" remains SINGLE-SEAT
(deck137 is still the only convoke deck) — not a core completion yet.
