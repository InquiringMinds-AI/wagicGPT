# deck131 wave-25 — general-suggestions.md

PASS.

No core / general-prompt (shared decision-prompt) change PROPOSED from this seat. The seat's findings route
cleanly by layer without touching the shared prompt:
- The tapped-read misread recurrence (vs137 s26, I-1) is a MODEL-COMPREHENSION frailty against a CORRECT,
  unambiguous representation ([tapped] present on the tapped creatures, absent on the untapped Trostani +
  Soldiers) — not a representation defect the core prompt could fix. Non-decisive (adjudication-turn,
  EV-robust swing). Now 3 corpora but STILL single-SEAT (deck131 only); below the bar to touch the shared
  prompt. Mitigation stays in the per-deck guide (guard shipped wave-23, held 8/9) and a notes WATCH.
- The two guide-fixable findings (bottom-step miscalibration I-2; never-block lethal-only-blocker exception
  I-3) are PER-DECK GUIDE rules-correctness fixes, applied to strategy.txt — not shared-prompt changes.
- The rules-fix batch (a1 upkeep priority, a2 no untap casting) validated CLEAN at this seat with no core
  representation issue (deck131 has no upkeep-trigger cards; no untap-window casting).

The general prompt's battlefield, mulligan, and bottom representations are CORRECT and complete here; the
residual failures are model comprehension / edge-case guide-rule gaps, not defects the core prompt could
address.

**BELOW-BAR CANDIDATE flagged for the synthesis agent (NOT a proposal, does not break this PASS):**
affirmative untapped-blocker tagging. The tapped-read misread class (I-1) is the model failing to enumerate
untapped blockers from the ABSENCE of a [tapped] tag ("attackers are tapped, so nothing can block" —
ignoring freshly-deployed untapped creatures/tokens). A candidate core-prompt mitigation would tag untapped
opponent creatures AFFIRMATIVELY (e.g. append "[untapped - can block]" to any opponent creature lacking
[tapped]), converting a silent absence into a present token the model must read — a change that would help
EVERY seat's combat reads. It is BELOW BAR now because the evidence is single-SEAT (deck131 only) and the
current representation is correct-not-defective. Surface it to the synthesis agent ONLY to weigh IF another
reviewer independently reports a combat tapped-read misplay this wave (cross-seat convergence would promote
it from candidate to proposal). Absent that convergence, this stays a WATCH; the seat's answer is PASS.

This is the 20th consecutive general-prompt PASS.
