# general-suggestions.md (core-prompt proposals) — deck102 wave 25

PASS — no core-prompt change proposed this wave (20th consecutive PASS from this seat's routing view).

The wave-24 WATCH I raised (the reply-protocol EXAMPLE seeding an OUT-OF-RANGE coded index) is now
RESOLVED by the b1 de-fang shipped in this corpus ("CHOICE: 3 (Cast Example Card)"). Evidence: 0
fallbacks at this seat, 0 corpus-wide, and the s5-class out-of-range coded line did not reproduce. The
core-prompt example fix I flagged as the escalation path is effectively already in place; the watch can
be CLOSED. No residual core-prompt item at this seat.

All other items this corpus route BELOW/BESIDE core, not to it:
- pregame over-mulligan (vs109, turn-1-chase) -> GUIDE ambiguity ("early" undefined) -> strategy.txt
  edit applied + skill.md rung (NOT core-prompt; the model had the hand correctly, the guide term was
  fuzzy).
- steal-window validation debt (3rd wave, 0 genuine windows) -> ENGINE/harness deck199 PROBE -> notes.md.
- R-DFC-BACKFACE-RESOLVES-FRONT, un-re-exercised 3rd wave -> ENGINE -> notes.md (bundle with the probe).
- pw-redirect removal (a4): 0 redirect records corpus-wide -> VALIDATED, nothing to route.
- Tergrid cast rules-correct + flip-thrash 0/6 + Smallpox discipline round 4 -> VALIDATED -> notes.md.
