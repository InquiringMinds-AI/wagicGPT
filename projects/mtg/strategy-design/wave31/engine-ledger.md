# Wave-31 consolidated engine ledger -- the wave-32 step-1 docket

Every item below comes from a seat's `notes.md` with the seat's own repro. Corpus
`~/.Wagic/ai/gpt/selfplay-runs/matchups-20260727-025026`, binary `cf7cb2e53`, unless stated.
Ranked by (game-cost observed) x (mechanism located) x (repro quality). Severity and repro quality
are separate fields on purpose (wave-31 HL4).

Two cross-cutting directives for the fix agent before the list:

- **PATH-SCOPED FIXES ARE THIS WAVE'S DOMINANT DEFECT SHAPE (wave-31 HL5).** Three items below
  (#2 N-122c, #4 N-137b, #17 N-152e) exist only because an earlier fix landed on one path and not
  its sibling. Before closing anything here: enumerate every path that emits the surface and quote
  the post-fix string from EACH; for an engine fix, name the production call sites. Grep for the
  NEW string *and* the OLD string.
- **THE DIVERGENCE-C FAMILY HAS THREE TRIGGERS AND ONE EXIT (#10). Fix the EXIT (loud re-ask), not
  the triggers.** Per-trigger patches will chase this indefinitely; a third trigger already exists
  that nobody predicted, at a seat with no convoke in the deck.

---

## Tier 1 -- cost a game this corpus, mechanism located

| # | id | sev | one-line mechanism | file:line | repro | validated next by |
|---|---|---|---|---|---|---|
| 1 | **N-152c** | **FIRST-ORDER** | strict `potentialMana` sets `used[card]=true` after a card's FIRST mana ability, so a two-ability dual land contributes only its FIRST colour and multi-pip spells are silently NOT OFFERED on the cast path (the permissive fix landed on `hasInstantResponse` ONLY) | `ManaEngine.cpp:163-199`; strict callers `LegalActions.cpp:348` (`castableForDisplay`), `DecisionContract.cpp:434` + **:461 staleness gate**, `AIPlayerBaka.cpp:2754`; the existing permissive precedent + its comment at `LegalActions.cpp:296-301` | deck152 vs146 **seq26 t15 (my_life 1)** and **seq21 t13**: Sigarda `{1}{G}{W}{W}` in hand, payable off Hengegate {W} + Overgrown Farmland {W} + Barkchannel {G} + Branchloft {1}, not offered. **Positive control** deck137 seq28 t13: offered off only FOUR sources, white from Deserted Beach (`{T}:add{W}` first) | **deck152** (Step-0 seat, stays). Fixture: Overgrown Farmland x2 + Barkchannel + Branchloft + Hengegate + Sigarda -> must be OFFERED and must RESOLVE; mirror negative: three Forests + Sigarda -> not offered. The build and the `:461` staleness gate MUST use the same potential |
| 2 | **N-122c** | **HIGH, CROSS-SEAT** | the wave-30 g4 tapped-tag reword was not applied to the DECLARED-ATTACKER path, so every attacker prints a self-contradiction: `[tapped - cannot attack or block this turn] [attacking]`. **331 occurrences across the 42 corpus logs**; models at **4 separate seats** verbalized the paradox | render path emitting the creature tag (same emitter as the g4 change) | deck122 vs158 **seq39 t13**, kind=blockers, life 8v7: `Orc army (19/19) ... [tapped - cannot attack or block this turn] [attacking]`; reply: *"this is a contradiction in the log description ... I will assume it does NOT attack"*; correct head line `BLOCKS: B1:A2, B2:A3, B3:A1, B4:A1` self-corrected into dropping both chumps; **game lost at -16** (`stderr:1100` shows only 2 Defenser Toggles applied) | any seat with a combat log (all of them). **CLOSES ONLY ON BOTH** (method HL3): a before/after render quote on an attacking token AND a block decision that correctly chumps a large attacker. Fix direction: `[tapped - attacking]` / `[tapped - blocking]` for combat participants; reserve the long clause for tapped non-combatants |
| 3 | **N-146h** | **HIGH** | `Cast Agadeem's Awakening` (MDFC FRONT face, `{X}` cost) silently no-ops: mana is floated, the face-chooser answer is consumed, and the entire turn narrates one line. Hypothesis: no ANNOUNCE_X step is ever reached, X defaults to 0, and the only live branch `this(X=0)` targets `creature[manacost=0]|myGraveyard` -- no legal target -> the cast aborts before the stack with the card still in hand | primitive in `Res/sets/primitives/*.txt` (`name=Agadeem's Awakening`, `mana={B}{B}{B}{X}`); AI cast path / ANNOUNCE_X gating for `{X}` on an MDFC front face | deck146 vs158 **seq30/31 t14**, identically **seq32/33 t16**, offered again seq34 t18. Three main phases and ~21 mana consumed with the opponent on 5 life while a castable Silverquill Silencer sat in hand; **deck146 lost at t19** | **deck146** (holds). Checks in order: (a) does the AI cast path reach ANNOUNCE_X for `{X}` on an MDFC front face -- compare against a non-MDFC `{X}` spell to isolate MDFC-ness vs `{X}`; (b) if X=0 is correct MTG-wise then the option must not be OFFERABLE, and the mana must not be committed before the legality check; (c) whatever the resolution, **it must not be silent** |
| 4 | **N-137b** | **HIGH** | divergence A's convoke creature-credit was added to `ManaEngine::maxAnnounceableX`, which **neither MTGRules announce path calls** -- both derive the X option count from the floated pool, and a convoke cast is an ALTERNATIVE-cost `{0}`+Convoke shell by construction, so the credit is unreachable on the convoke route | credit at `ManaEngine.cpp:640` (refs: `AllAbilities.cpp:8209`, `AIPlayerBaka.cpp:2644`, `include/ManaEngine.h:103`); the two hand-rolled paths at **`MTGRules.cpp:433`** (normal) and **`MTGRules.cpp:1057`** (`MTGAlternativeCostRule::reactToClick`); shell construction at `CardPrimitive.cpp:231-252` | corpus-wide counts: `Announce the value of X` = **28 at deck158** (normal X cast -- reachable path works), **0 at deck137** across six games despite 5 March picks; `You can afford X up to N` = 32 / 0 | **the deck198 convoke PROBE** (deck137 findings §8). Wave-30's mistake must not repeat: "A is fixed" is provable only by an `ANNOUNCE_X` record at a CONVOKE-route pick plus `Soldier (1/1): created -> battlefield` xN. **A fixture on the normal-cast path does not test this.** Fix direction: route both MTGRules announce blocks through `maxAnnounceableX`, guarding the alternative path on the alternative cost object |
| 5 | **N-137a** | **HIGH** | convoke's colour scan `break`s on the FIRST index because BOTH branches set `next = true`, so only GREEN (index 1) is ever examined and colours 2..5 are dead -- a `{X}{G}{W}{W}` convoke spell is offerable only when the LANDS alone can make both `{W}`. CR 702.51 violation | **`ExtraCost.cpp:1241-1267`** (`convokeReduceByCreatures`, the OFFER probe used by `Convoke::offerable`) and **`ExtraCost.cpp:1178-1231`** (`Convoke::getReduction`, the real payment, both passes); colour constants `include/MTGDefinitions.h:100-105` | deck137 vs146 **s20 (t8)** and **s24 (t10)**: 3-4 untapped Forests + two untapped WHITE Human tokens + 2x March in hand -> **no March line offered at all**. Contrast: `{4}{W}` Venerated Loxodon (one coloured pip) convokes fine (vs158 s21, vs122 s30) | **the deck198 convoke PROBE** -- its 12x-Forest mana base is the F4 discriminator (pre-fix March must NEVER be offered on an all-Forest board with white convokers; post-fix it must be). Fix BOTH functions together (the probe's own comment mandates agreement) |
| 6 | **N-158g** | **HIGH** | `Mana available: {r}{b} from 5 untapped sources` leads with the COLOUR SET, which the pilot binds to the POOL SIZE. Not an instruction gap -- the core says it in three places and loses at distance -> REPRESENTATION per the standing routing rule | the `Mana available:` render | deck158 vs148 **seq19 t9, my_life 1 -- GAME-LOSING**: offered `Cast Snarling Warg {3}{b}` declined -- *"I have {R}{B} available, but Snarling Warg costs {3}{B} (four mana total). I cannot afford to cast it"*; died at -4 the next turn to damage the Warg could have blocked. Same shape vs148 seq17, vs146 seq31, vs152 seq11 (plus a second false belief: off-colour mana cannot pay generic). **8 of the seat's 10 largest replies are mana arithmetic**, three on LAND DROPS (13,319 ch / 208 s) | **deck158** (holds). Fix is WORDING, count-first: `Mana available: 5 (five untapped sources; colours you can make: {r}{b})`, or minimally `5 untapped sources, producing {r}{b}`. The guide stopgap (deck158 Rule #1) DEMOTES the corpus after this lands; verify by grepping the seat's replies for `"I only have {"` / `"cannot afford"` against offered casts -- target zero |
| 7 | **N-139k** | **HIGH** | `[summoning sick - cannot attack this turn]` states only the attack restriction and is read as "cannot block" -- the neighbouring `[tapped - cannot attack or block this turn]` spells out BOTH, so the shorter tag reads as the same class of statement | creature tag render (same emitter family as N-122c) | deck139 vs158 **seq26 t10** (life 7 v 6): *"The opponent has no untapped creatures to block ... my attackers will deal 6 damage ... winning the game immediately"*, then declined a castable Migratory Greathorn on that belief. Sauron was untapped, blocked at seq28, opponent lived at 3 and won (`winner=1 life0=0 life1=3 turn=11`) | **deck139** (holds). One clause: `[summoning sick - cannot attack this turn, but CAN block]` -- same shape as the g4 split that validated at 131 reads with no misread |

## Tier 2 -- wrong commits, silent aborts, and rules divergences with exact repros

| # | id | sev | one-line mechanism | file:line | repro | validated next by |
|---|---|---|---|---|---|---|
| 8 | **N-137c** | **HIGH** | the alternative-cost CONVOKE branch consumes the mode-menu answer, floats the whole land base, then abandons the cast: **no ANNOUNCE_X, no `hand -> stack`, no token, no `defer` record**, card still in hand next turn | inside the alternative(convoke) branch downstream of the menu answer; instrument `MTGAlternativeCostRule::reactToClick` around the announce block and the `isExtraPaymentSet()` / `setExtraCostsAction` return at `MTGRules.cpp:1150-1160` | deck137 vs139 **s22-24 (t8), s29-30 (t10), s36-38 (t12), s41-42 (t14)** -- four repros, pool 4-8 available. **Zero Soldier tokens from March across six games.** **SAME-SEAM CONTROL: vs148 s53-54 (t16)** -- byte-identical menu for Venerated Loxodon answered `Cast Card Normally` narrates `hand -> stack` / `stack -> battlefield`, so the menu and the normal branch are SOUND | **the deck198 convoke PROBE.** Treat as a SEPARATE defect requiring its own instrumented repro -- N-137b is a plausible but UNPROVEN contributor (at vs139 t14 the pool alone was 8, so the pool-only option count at `MTGRules.cpp:1057` should still have armed a menu). **Do not touch** `AIPlayerGPT::mStuckCastLines` (`AIPlayerGPT.cpp:3845-3852`, consumed `:4086`) -- the 2-per-turn re-pick cap is working and killed wave-29's March spiral |
| 9 | **N-148b** | **HIGH** | the reverse `[attached to: X]` render omits `instanceHandle(host)` -- the one surface where the handle was never applied, and exactly where a token deck collides | **`src/AIPlayerGPT.cpp:693`** (`out << " [attached to: " << host->getDisplayName() << "]"`); handle helper `instanceHandle()` at `:414` | deck148 vs152 **seq30/31 (t18)**, the deciding turn of a game lost by 2: three identical `Kor ally #1/#2/#3 (1/1)` and `Cranial Plating [attached to: Kor ally]` -- **no `#2`**. The model mis-attributed the buff (*"The 7/1 Kor ally (currently equipped with Captain's Claws) ... Cranial Plating grants it +6/+0, making it a 13/1. This is lethal"*), re-equipped the same Plating, and swung 9 instead of 15; opponent survived at 2 and killed it back. Final -2 to 2 | a **token+equipment PROBE deck** (the surface no roster deck loads now that 148 rotates). Fix is one expression: append `instanceHandle(host)`. **Second live instance of the wave-26 HL5 clause (e)** (identical names + missing handle -> wrong commit; wave-26's was two Lovestruck Beasts at a blockers B-line), which argues the handle should be an INVARIANT at every surface that names a permanent |
| 10 | **DIVERGENCE-C FAMILY** (N-137d + N-158i + the plain-cast case) | **HIGH (mechanism), family** | `AIPlayerBaka::FindCardToPlay(pMana, "*")` returns NULL with `aiForcedCandidate` set, so a cast `offerable()` approved is rejected -> **silent defer, silent no-op, window burned, model never asked**. THREE trigger shapes, ONE exit | emission at **`AIPlayerGPT.cpp:4135`**, reached from the validation at **`AIPlayerGPT.cpp:4117-4123`** (brief cites ~`:3991` for the FindCardToPlay validation) | (a) convoke reject (original); (b) **PLAIN cast** -- deck137 vs152 **s34 t12** (`kind=defer, choice=-1, options=0, prompt="", reply=""`), the s33 pick was the plain `Cast March {g}{w}{w}{x}`, convoke correctly absent; (c) **X-slack == 0** -- deck158 vs146 **s25 t13** at a seat with NO convoke, three Mountains vs `{X}{R}{R}{R}`, with a same-game counterfactual at **s27/28** (6 sources, slack 3) that announces X and resolves | **deck158** (holds; cleanest repro -- severity LOW because X=0 makes a 0/0 that dies to SBAs, repro quality HIGH) + deck152. **Fix the EXIT: LOUD RE-ASK.** deck137's sharpening rules out the other half -- at s34 the reject was substantively RIGHT (X could only be 0 and March's script is `auto=this(X=0) donothing`), so "accept on `offerable()`'s payability" would let guaranteed-blank casts through. Re-word the ledger item away from "convoke casts" |
| 11 | **N-148c** | **HIGH** | Stone Haven Pilgrim's `1/1 ueot && lifelink ueot` fires its first clause and NOT its second -- granted lifelink never produces life. The guide is faithful to Oracle AND to the engine's own `text=`/`auto=`, so this must NOT be stopgapped in prose | primitive `borderline.txt` (`auto=@combat(attacking) source(this) restriction{...}:1/1 ueot && lifelink ueot`); the construct appears **21x in borderline.txt / 29x in mtg.txt** for `lifelink ueot`, incl. activated abilities | deck148 vs158 narration **turn 5** (Pilgrim deals 4 = 2+1+1, confirming the pump; life 19 -> next change is -3, no +4) and **turn 7** (deals 5, no +5); deck148 vs146 **turn 12** (deals 4 to a Goblin, life 13 -> 12, no +4). Opponent lifegain lines DO render in these same logs (`Opponent's life +3 (now 15)`), so the renderer is not the gap | a `[AI]` fixture: Stone Haven Pilgrim + any artifact = a two-line repro. **Fix the CLASS, not the card** -- determine whether it is (a) the `&&` chain dropping the second clause when the first is a P/T pump with explicit `ueot`, (b) granted lifelink not consulted by the combat-damage life-gain path, or (c) trigger-vs-damage ordering |
| 12 | **N-122d** | **MED-HIGH** | `truncated_abandoned` at the blockers seam DISCARDS an already-compliant answer-first line and defaults to **no blocks** -- the worst possible default at that seam (it maximizes incoming damage and it is the adjudication tiebreaker) | the truncation path; `game-137v122-...stderr:1339` `AIPlayerGPT: truncated-abandoned block commit -> safe no-blocks default` | deck122 vs137 **seq22 t10** (head line `BLOCKS: B1:A3, B2:A4`, tail re-derives the same pick; harness took no blockers; 19 -> 14 life, 3 Blood Artist drains forgone) and vs158 **seq26 t9** (head `BLOCKS: B3:A2, B1:A1, B2:A3`; harness took no blockers; 13 damage instead of the model's -4). Both are 2 of the corpus's 6 fallbacks. NOT a hard character cap -- 13,283 and 13,689-char replies at the same seat parsed fine | any seat with long blocker deliberations (deck139/152 both produce them). Two separable fixes: (1) **honour the head `BLOCKS:` line on truncation when it parses to a legal assignment** (guard: if the truncated tail contains a LATER complete assignment the existing line-anchored self-correction rules apply -- head and tail AGREED in both witnesses); (2) prefer the Baka heuristic's own block assignment over a blanket decline |
| 13 | **N-139i** | **MED** | London bottoming has three defects in one seam: the prompt MIS-LABELS the mulligan count (it prints the remaining-to-bottom number wearing a mulligan label), it demands N card numbers while the engine consumes exactly ONE per ask, and **it stops early -- 7 bottoms required, 4 fired, the seat kept a 3-card hand instead of 0 (CR 103.5 violation)** | the pregame bottoming loop + its prompt text | `game-139v122-1785150987.stderr:144-152` (`KEEPS after 7 mulligan(s); will bottom 7 card(s)` then `(1 of 7)`, `(2 of 6)`, `(3 of 5)`, `(4 of 4)`) with translog seq8-11; the model reasons against the contradiction out loud at seq11 (*"the prompt states I 'kept after 4 mulligans,' meaning I am on a 3-card hand"*) and that ask cost **200,461 ms / 12,497 chars** | **deck139** (holds). Fix: state the true mulligan count and the true per-ask requirement (either ask once for exactly N names, or ask N times for ONE name and SAY so -- `put ONE card on the bottom; 3 more after this`), then hold the loop to the full N |
| 14 | **N-139j** | **MED-HIGH** | the attackers menu has no power annotation while the blockers menu does, so 0-power creatures are declared as attackers | attacker option render (mirror of the shipped blocker annotation) | deck139 vs137 **seq16 (T5), seq18 (T7), seq19 (T9), seq21 (T11)** -- four declarations sending 0/3 Arboreal Grazers (`power=0` per `primitives/borderline.txt`); seq21 reply: *"Attacking with all three 0/3 Grazers deals 3 damage"*; seq19 (208 s) reaches the right answer only AFTER the answer-first line committed | **deck139** (holds). Deck-agnostic fix mirroring the validated blocker text: append `[deals 0 - this attack deals no damage to the opponent]` when effective power is 0 |
| 15 | **N-158f** | **MED** | Assault on Osgiliath's `emblem transforms((...double strike...haste)) ueot` appears not to reach the amassed Orc Army -- an unconditional line that should fire on both faces | `borderline.txt:5890` | **HASTE** -- deck158 vs146 seq28/29, game Turn 16: X=3 announced and paid, Army created with three counters, then `=== Turn 17 - opponent's turn ===` with **no attackers window** (the fresh 3/3 was summoning-sick), at 5 life vs 11 with the pilot's plan naming the swing. **DOUBLE STRIKE** -- vs152 game Turn 12: 7/7 Army blocked by a 3-toughness Elite Spellbinder logs a single `Damage: 7` plus `Damage: 3` back | **deck158** (holds). Axis to check: whether `lord(*[orc;goblin]|mybattlefield)` matches the `Orc Army` token at emblem-evaluation time (created as `Creature Orc Army` then `transforms((Orc,...))` by the amass macro), and whether an emblem created in the same resolution as the token applies to it. Deliberately NOT written into the guide |

## Tier 3 -- representation defects that mislead but have not yet cost a game

| # | id | sev | one-line mechanism | file:line | repro | validated next by |
|---|---|---|---|---|---|---|
| 16 | **N-152d** | **MED** | the `(printed X/Y)` delta annotation reads the FRONT face of a TRANSFORMED DFC, so an unbuffed transformed werewolf is presented as a pumped small creature | printed-P/T source (should be the currently-displayed face's model, as name/keywords/abilities already are) | deck152 seat: deck146 seq23/26 t15, deck137 seq27 t13, deck139 seq16 t9 + seq26 t11 (`Moonrage Brute (3/3) (printed 2/2)`; ground truth `power=3 toughness=3` in `borderline.txt`, no counters, no anthem) and deck148 seq45/49 t19 (`Tovolar's Packleader (7/7) (printed 6/6)`, true 7/7). The annotation is CORRECT on genuine buffs in the same prompts, so this is face selection | **deck152** (holds). Suppress the tag when the delta is zero after the correction; **PARSETEST should gain a case**: transformed DFC with no counters -> no `(printed ...)` tail |
| 17 | **N-152e** | **LOW (cosmetic residual of h4)** | the OLD misleading Flip-Side text survives on TRANSFORM DFCs (h4 covered MDFC lands only): it promises the back face "appears there as an alternative-cost cast" -- false for this card class -- and renders the back face as the literal placeholder `"backside"` where the MDFC path resolves the real name | the Flip-Side option render, transform-DFC branch | deck152 seat: deck148 seq36 t15 (Tovolar's Huntmaster, verbatim) and deck139 seq28 t11 (Brutal Cathar) | **deck152** (holds). The same one-line rewrite that fixed the land case, plus resolving the back-face name. **Third instance of the wave's path-scoped shape** |
| 18 | **N-158c** | **MED** | Foray of Orcs renders `{right now: damage 0}` in 100% of cases -- the magnitude is built before the amass, off a source with power 0, while the true value is the post-amass Army's power | `borderline.txt:41425-41427` (amass 2, *then* `damage:power target(creature|opponentbattlefield)`) | deck158 vs152 seq17 (rendered 0, resolved 2) and vs137 seq19 (rendered 0 with a 7/7 Army out, true 9). The pilot reasoned past it both times at 13.1k and 9.8k chars and argued with the annotation explicitly | **deck158** (holds). Evaluate `damage:power` against the post-amass Army, or **suppress the `{right now: ...}` clause when the magnitude cannot be evaluated -- a printed 0 is worse than no annotation.** Guide stopgap removal condition: the option line shows the post-amass number |
| 19 | **N-158h** | **MED** | Feed the Swarm renders a CONSTANT `{right now: life -2}` regardless of target; the true self-cost is the DESTROYED permanent's mana value, which the evaluator cannot know at option-build time | `borderline.txt:38779` (`newability[life:-manacost opponent]`) | ~44/44 renders print -2. Observed actuals: Nadaar `{2}{W}` -> **-3** (vs146 seq20/21, life 8 -> 5); Stone Haven Pilgrim `{1}{W}` -> -2; Serra Ascendant `{W}` -> **-1**; Kor ally token MV 0 -> **0**. The deck finishes its wins at 3-15 life, so a 1-life error is live | **deck158** (holds). Either evaluate per legal target and fold into the existing `- legal targets right now: A, B, C` list (`... Nadaar, Selfless Paladin (costs you 3 life)`), or drop the clause for target-dependent self-costs |
| 20 | **N-146i** | **MED** | `Cast <card>` option lines carry NO card text (**0 of 163** at deck146) while activated-ability and loyalty options carry the full text -- the model gets rules text for abilities it activates but not for spells it casts | the Cast-option render | deck146: `2. Cast Acererak the Archlich {2}{b} (5/5)` against the primitive's `auto=if type(Tomb of Annihilation[dungeoncompleted]|myzones)~equalto~0 then name(Return to hand) moveTo(hand)`. **59 reply passages across 3 games** planning to attack or block with a creature that never stays; at vs148 seq18 t8 it declined an offered Vanishing Verse on the opponent's Kor lord to cast him and the board swung 12 the next turn | **deck146** (holds). Cheapest first: (1) narrow -- annotate Cast options for permanents whose ETB immediately moves them out of play (`{on resolve: returns to your hand - you do not keep the body}`), the same shape as the existing ETB pay-or-tap and PT-pump annotations; (2) broad -- put `{card text: ...}` on Cast lines as it already is on ability lines, closing the class. Kin to the wave-26 R-ADV-FACE family. The deck146 guide's corrective teach DEMOTES once this lands |
| 21 | **N-148a** | **LOW (but it is N-148b's precondition)** | EQUIPMENT never appears in the host creature's FORWARD attachment list -- `describeAttachments()` finds attachments by `att->auraParent == host`, and the engine keeps an Equipment's host in `target`, not `auraParent` | **`src/AIPlayerGPT.cpp:376`** (scan at `:388`); the forward-render block at `:686-693` documents the very fact that defeats it | measured across all six deck148 seat logs: **112 reverse `[attached to: X]` renders and 0 forward `{attached:` renders**. Cleanest at vs122 seq26 t12 (three artifact lines to reverse-map onto one Kor Duelist) | the token+equipment PROBE deck. Fix: extend `describeAttachments` to also match `att->target == host` for `TYPE_EQUIPMENT`/`"fortification"`, mirroring the type test the reverse block already performs |
| 22 | **N-148d** | **LOW (single witness, cheap fix, strong precedent)** | a creature in HAND carries no explicit creature marker -- `typeTag()` returns `""` for creatures by design and creature-ness is signalled only by the `(P/T)` parenthetical, while every other type gets `[artifact]` / `[enchantment]` / `[instant]` / `(land: taps for {W})` | **`src/AIPlayerGPT.cpp:293`**; precedent in the source's own `landTag` comment at ~`:313` (deck93 wave-27, "mulliganed the identical 3-Swamp opening hand 5 of 6 games claiming 'zero lands'") | deck148 vs146 **seq1 (t0, mulligan)**: hand contains `Kor Duelist {w} (1/1)` + two Plains; reply *"This hand has zero creatures... the lack of a body makes this a dead hand"* -> mulligan, against the guide's own explicit KEEP rule. Counter-evidence for calibration: the same corpus read creature-presence correctly twice at vs137 seq1/seq2 | any aggro seat. Give creatures an explicit marker on hand/choice lines (`(1/1 creature)` or a `[creature]` tag), keeping the P/T |
| 23 | **N-158a / N-158b / N-158d** | **LATENT, low** | (a) amass cast options carry no magnitude and no resulting Army size, so the pilot re-derives it every time (the seat's top-2 tax spikes); (b) `- Counter added to Orc army` narration has no count and no source, hiding the Mauhur bonus (four lines = +8 in one game, two lines = +2 in another); (c) Foray's damage TARGET CHOICE header names the ARMY as the ability source, inviting self-target | amass render + the same magnitude evaluator as N-158c | (a) deck158 vs137 seq18/19; (b) vs122 game Turn 12 vs vs137 game Turn 12 (arithmetic proof of the ambiguity); (c) vs152 seq18 | **deck158** (holds). Group (a) with N-158c -- same evaluator. Suggested: `{right now: Army 7/7 -> 9/9}` on amass casts; `- Orc army gets 2x +1/+1 counters (now 9/9) [Foray of Orcs]`; `TARGET CHOICE for Foray of Orcs` when the ability was transformed from a spell |
| 24 | **N-158e** | **LOW-MED (design call)** | flash creature casts are absent from priority-window legal-action lists, which makes every flash creature a strictly-main-phase card | priority-window legal-action build | deck158 vs152 **seq27 t13, own Upkeep**: hand holds `Orcish Bowmasters {1}{b}` (`abilities=flash`, `borderline.txt:80464`), `{r}{b} from 4 untapped sources`, and the entire legal-action list is `1. Draw 1 with Clue`. Related datum: **6 priority windows in 6 games, only 1 on the opponent's turn** | **deck158** (holds). Also check whether the h5 display-toggle auto-pass is over-suppressing response windows for a seat with a flash creature and instant-speed mana up |
| 25 | **N-122b** | **MED, mechanism UNCONFIRMED** | no casting-decision ask offered while payable spells sat in hand, two turns running -- reported as OBSERVED with a repro, not diagnosed | candidate: `AIPlayerGPT::FindCardToPlay` candidate validation rejecting payable casts before the ask is built (same family as #10, but manifesting as a MISSING ASK rather than a defer) | deck122 vs148 **t11 and t13**: stderr's only deck122 event at t11 is `declared attack (0 of 1)`; seq16 shows `{b}{w} from 3 untapped sources` with `Blood Artist {1}{b}` and two `Vampire Nighthawk {1}{b}{b}` in hand, all payable off Chapel/Scrubland/Plains. The same seat WAS offered Blood Artist in other games (vs158 seq19, seq28), so the gap is state-specific. Part of the vs148 loss is engine-lane | investigate alongside #1 (N-152c) and #10 -- all three are "a payable cast never reaches the model". Also on the same path: `AIPlayerBaka: Card was a land and ai cant play any more lands this turn.` fires immediately after every GPT land pick (probably benign, worth checking while in there) |

## Tier 4 -- low priority / housekeeping (do not spend a wave-32 slot)

| # | id | one-line | pointer |
|---|---|---|---|
| 26 | **N-137e** | Flower // Flourish's `revealzone(mylibrary)` SEARCH reveal is still whole-library (54/50/48/47 options) -- but the ELIGIBILITY header + per-option `[eligible]` tags work: all 12 reveals picked a legal card and the fetch ARRIVED. Cost is prompt size, not correctness | `borderline.txt:41113`; deck137 vs158 s6, vs139 s10, vs148 s17/s19. Fold into the N-136a family only if a seat shows a misread |
| 27 | **N-139l** | merged-pile cost pip render drift: after a merge the pile line shows a single pip where the pre-merge line showed the full cost (`Gemrazer {3}{g}` -> `{g}`). No decision affected -- a permanent's printed cost is not a live input -- but the number shown is wrong | deck139 vs146 seq23 vs seq25; same at vs148 seq13+, vs158 seq16+, vs152 seq11+ |
| 28 | **N-137f** | `in GetCost Seems ManaCost was not properly initialized` fires **exactly 24 times in every one of the six deck137 stderr files**, always at deck LOAD (lines 97-120 of ~1500), invariant to whether convoke or mutate ever fired. The N-139h commit message cites this trace as evidence the OOB precondition is live in the mutate/convoke paths -- **at this seat it is not a gameplay signal.** Re-check whether the 24 are the same 24 card definitions in every game before treating it as evidence | deck137/notes.md |
| 29 | cosmetic (deck146) | an absent AI stats file logs at `FATAL:` severity while being fully recoverable (`game-146v158-...stderr:80: FATAL: AIStats.cpp:load : can't load ai/baka/stats/ai_baka_deck158.stats`) -- it trips crash greps in every corpus that rotates in a new deck. Downgrade to a warning | deck146/notes.md |
| 30 | cosmetic (deck122) | Clue token annotation garbled and mis-sourced: `1. Draw 1 with Clue [cost: {2}, Sacrifice] {card text: "() source: Serra Ascendant"}` -- empty parenthesised text, attributed to a creature that had already left the battlefield. The model used the option correctly anyway | deck122 vs152 seq27/seq28 (t11/t12) |

---

## Probe obligations (three, all carried out of this wave)

| probe | owner-lane | spec | PASS condition |
|---|---|---|---|
| **N-122a -- Felidar Sovereign alt-win** | between-wave engine task (deck122 rotates OUT; this does NOT hold a seat) | stacked deck199-style probe: Felidar Sovereign + lifegainers, **no other win route**. Ability is well-formed: `auto=@each my upkeep:this(controllerlife > 39) wingame` (`bin/Res/sets/primitives/mtg.txt`) | the win-check FIRES at an upkeep with controller life > 39. Two corpora of random draws have now failed to co-present card + threshold (wave-31: never on the battlefield; 40+ reached without it, finishing at 63 with five own upkeeps passed at 40+) |
| **deck137 convoke probe (deck198)** | wave-32 step-1, MANDATORY hand-off | full spec at `wave31/deck137/findings.md` §8: `deck198.txt` = 4x March of the Multitudes, 4x Venerated Loxodon, 4x Emmara, **12x Forest**, 4x Plains, 4x Edgewall Innkeeper, 4x Lovestruck Beast (white Human tokens), 4x Flower // Flourish; `deck198_strategy.txt` = a copy of `deck137_strategy.txt`; pinned one-shot GPT games with `timeout 3000`; **DELETE both probe decks afterwards** and verify the highest `deckN` is back to the real roster | all ARRIVAL-TRACED: (a) an `ANNOUNCE_X` record on a CONVOKE-route pick + `March: hand -> stack` + `Soldier (1/1): created -> battlefield` xN with N == announced X; (b) on an all-Forest board with >= 2 untapped white Human tokens the convoke line IS offered and the pool balances after payment; (c) `Cast Card Normally` still resolves (negative control, works today at vs148 s53-54); (d) offered-convoke picks producing no downstream game-state effect == 0 |
| **N-139h -- Pollywog discounted mutate** | wave-32, paired with the deck139 HOLD | scripted board: **Pollywog Symbiote on the battlefield** (`auto=lord(creature[mutate]|mycastingzone) altercost(colorless,-1)`) + one non-Human host + **Nethroi, Apex of Death** in hand with `{4}{GW}{B}{B}` payable; assert the discounted mutate is offered, chosen, PAID, and that the `[mutated pile]` line appears. Repeat for **Snapdax `{2}{BR}{W}{W}`** (the multi-symbol/hybrid shape the wave-30 report fingered) | the discounted mutate resolves with no crash **and the paid cost is correct**. **Run a RELEASE build too:** with assertions off the same underflow computes a wrong cost silently instead of aborting, so a debug-only PASS is not a validation. Why a probe and not a corpus: this wave Pollywog reached the battlefield in 1 of 6 games, its single discounted offer was DECLINED, and it died before the mutate that was taken -- the corpus cannot be relied on to produce the state |

## Carried, not exercised this corpus

N-139g (under-card statics -- Pollywog never survived under a pile; stays answered-positive from
wave-30, not re-tested) · N-152a MDFC back-face LAND placement (engine-R; annotation half CLOSED by
h4, and the blast radius is **smaller** than assumed -- Teferi is NOT blocked, only the Pathway
back-face colours are unreachable; removal condition unchanged: both faces offered in the play-land
menu) · Rankle mode-bleed watch · L-18-1 planning tax (recurs as pure LENGTH with zero correctness
cost -- keep as a latency WATCH, add no per-seat floor) · N-134b/d · L2 cross-zone dup-name · ETB
header card-name · N8 · Azcanta · undying · commit-lock (second witness this wave, deck139 vs137
seq19) · prose-reversal · untapped-tagging · I-4.

## Closed this wave (do not re-open)

**h1/N-139h** crash gate -- zero crash/abort/assert/core-dump signatures in all 21 stderr files
(confirmed independently at deck122, deck137, deck139, deck146, deck148, deck152, deck158). No
regression; positive revalidation is the separate probe above. · **h3/N-136a** reveal chooser
(3 empty-graveyard reveals = NO model call). · **h4** MDFC land annotation (48 truthful renders;
old text gone from all LAND lines -- but see N-152e for the transform-DFC sibling). · **h5/N-152b**
display-toggle-only auto-pass (deck152 priority asks 148 -> 22, toggle-only windows 121 -> 0;
deck146 24 -> 18; windows containing a Flip Side ALONGSIDE a real action correctly still ask).
· **h6/N-146g** planeswalker magnitude skip (deck146 14/14 bogus -> 0; Kaya's 9-option loyalty menu
clean on its first live render).
