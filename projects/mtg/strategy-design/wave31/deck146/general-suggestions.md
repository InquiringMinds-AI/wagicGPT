# deck146 -- wave-31 core (general prompt) suggestions

**PASS.**

The core prompt was not the responsible layer for anything found at this seat this corpus.

- The one fallback (vs122 seq18, `unparsed_reply`) is an off-menu invention -- index 4 into a legal
  set of {0,1}, naming a card that was neither offered nor affordable. The core already carries the
  lists-are-complete rule, and the parser rejected the reply correctly. Adding a louder or a third
  restatement of a rule the model demonstrably knows is the sanctioned-against move; there is no
  core TEXT defect here. Routed to skill.md as an observation and to findings.md (e) as the
  decomposition.
- The two substantive findings route elsewhere by construction: Acererak's false-persistence belief
  is a guide fact plus a render omission (strategy.txt + notes.md N-146i), and the Agadeem's
  Awakening silent no-op is an engine defect (notes.md N-146h).
- Everything the core is responsible for behaved: answer-first CHOICE lines parsed on 219 of 220
  calls, PLAN carry-forward rendered correctly in every prompt inspected, the CURRENT SITUATION
  board snapshot was accurate wherever cross-checked against narration, and the dungeon-completion
  status line gated correctly on N>0.

This is the **26th consecutive PASS** by this seat's count.
