# Skill feedback — deck109 wave-24

**PASS.** No skill change proposed.

The wave-23 skill refinement (the crutch-lifecycle CLOSED-criteria rung: "one clean corpus is
insufficient evidence of redundancy for a COST/COMPREHENSION crutch — the model can ignore a
correctly-rendered cost from prior belief, and the miss is intermittent; require a 2-corpora
validation clock") was APPLIED this wave, not revised. This corpus is the first tick of that clock
for the restored Pillar cost line, and the rung worked exactly as intended: it correctly told the
reviewer to treat a single clean corpus (even one with the trigger present in the regressing
matchup) as necessary-but-not-sufficient and to keep the seat one more cycle rather than declare
convergence. The rung needs no sharpening — its prediction (intermittent, prior-belief-driven,
needs multi-corpus confirmation) matched the observed behavior and gated the rotation decision
correctly.

One OBSERVATION for the skill author (not a proposed change, flagged for synthesis judgment): the
new pregame mulligan phase introduces a decision surface (marginal aggressive-hand keep/mull, and
the bottom-N-at-keep translog record) whose validation depends on DRAW VARIANCE — a seat can go a
full 6-game corpus without a single mulligan or marginal hand (deck109 did this wave: all 6 hands
were trivial 3-land keeps, 0 mulligans, 0 bottom events). If the skill wants mulligan judgment
validated per seat, it may need an explicit "surface-unexercised" carry-forward flag so an
untested mulligan/bottom surface is not silently read as "confirmed clean" across the pool. Left
as an observation; no rung edit proposed on one seat's evidence.
