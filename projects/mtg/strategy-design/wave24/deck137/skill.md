# deck137 skill proposal (wave 24)

## PROPOSAL 1 — CRUTCH-LIFECYCLE worked example: the R-ADV-FACE crutch DEMOTED after the FACT shipped.

The wave-23 skill added the alternative-cost dual-option card as a fifth rule-3 idiosyncrasy face
(the alt-cost option carrying the OTHER face's stats), with the authoring move "capability table +
corrective fact, pair with a representation-ledger item so the guide block demotes once the option
carries the effect." **This wave that loop CLOSED cleanly and is worth recording as the reference
example of crutch-lifecycle discipline:**
- The engine fix shipped (adventure-cost options now render `{adventure spell: <effect>}`, no wrong
  (P/T)). Live acceptance PASSED conclusively — the Usher self-bounce misfire recurred 0x; the model
  now reads the effect and casts adventure spells correctly (deck59 s35, deck133 s19).
- Per the removal condition, deck137's guide DEMOTED the interim crutch this wave: dropped the
  "(P/T) is the creature you get LATER" TRAP paragraph and the "the option line does not tell you the
  effect — memorize this" framing, keeping only the pure STRATEGY (when to use each face).
- **Skill takeaway (already doctrine, now with a clean witness): the interim guide crutch and the
  durable engine FACT are a matched pair — annotate to teach, ship the FACT, then DEMOTE the crutch
  once the FACT surfaces.** No new rung needed; the fifth-face WATCH tag from wave-23 can be RETIRED
  as "resolved at the representation layer" rather than promoted — a single-seat misrender that the
  option-annotation fix eliminated is not a durable model idiosyncrasy, it was a representation bug.
  Recommend synthesis mark the wave-23 "fifth idiosyncrasy face" WATCH as CLOSED-by-FACT, not
  promoted (contrast the genuine model-belief faces, which persist because no FACT can fully fix a
  belief).

## PROPOSAL 2 (single-seat WATCH) — the SPIRAL relocates to the next-unfloored seam.

deck137 gives a clean causal witness for a general skill phenomenon: when a guide/representation
fix eliminates a decode spiral at one seam, the model's WORST spiral does not vanish — it
RELOCATES to the next seam that lacks a commit-rule. Wave-23 deck137's max spiral was the ATTACKERS
seam (15,901 chars, "ATTACK: none" passivity); the wave-23 guide added an attack floor + commit
rule; this wave the attackers seam is spiral-FREE (0 none-spirals) and the new max spiral is a
BLOCKERS lethal-defense decision (13,642 chars, deck59 s39 — opened with an illegal one-blocker-on-
four-attackers assignment, re-derived "at most one attacker per blocker" mid-reply).
- **Authoring implication for the skill: a per-seat commit-rule/floor is not a one-time fix — audit
  ALL high-stakes decision seams (attackers, blockers, targets, X) for a floor, because flooring one
  pushes the spiral to the unfloored neighbor.** deck137's guide adds a blocking-under-lethal commit
  rule this wave (the blockers analogue of its attack floor). Tag single-seat; if a SECOND deck's
  worst spiral is a lethal-block after its attackers seam was floored, promote "give every
  high-stakes seam a commit rule, not just the one that spiked last wave" to a named skill rung.
- The 1.05 repetition_penalty again did NOT break this structured spiral (bigger than any wave-23
  spiral), reconfirming: a GUIDE/representation commit-rule cures spirals the sampling penalty
  cannot. Pilot evidence is consistent across two corpora at this seat.
