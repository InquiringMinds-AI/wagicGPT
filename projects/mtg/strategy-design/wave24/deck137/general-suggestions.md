# deck137 general-suggestions.md (core prompt) — wave 24

## PASS (would be the 19th consecutive core PASS)

No core-prompt edit is justified from this seat. Both candidate items are single-seat and route
lower:

1. The R-ADV-FACE fix was a REPRESENTATION change (engine option annotation), now validated and its
   guide crutch demoted — never core. Nothing to add.

2. The BLOCKERS lethal-defense spiral (deck59 s39, 13.6k chars: model opened with an illegal
   one-blocker-onto-four-attackers assignment despite the prompt's explicit "assign each blocker to
   AT MOST ONE attacker" line) is a candidate general phenomenon, but it is SINGLE-SEAT this corpus
   and the constraint is ALREADY in the blockers prompt — so this is a comprehension/spiral failure
   under lethal pressure, not a missing core fact. Addressed at the deck layer (a blocking-under-
   lethal commit rule in strategy.txt, the blockers analogue of the attack floor). WATCH: if a
   SECOND deck opens an illegal multi-attacker-per-blocker assignment under lethal pressure, the
   right move is likely a core/representation touch (a one-line reinforcement or a prompt-side
   ordering change), NOT more per-deck guides — flag to synthesis, do not act on one seat.

Core's existing act-floor, interface contract, attack-floor and TRUST-THE-RENDERED-NUMBER language
are all present and correct for this deck. The deck's residual leaks (tempo/board-development,
late stabilization, the lethal-block spiral) are per-deck or representation, addressed in
strategy.txt and notes.md.

Note for synthesis: the convoke "trust the offered alternative-cost option, the engine taps for you"
line proposed as an eventual core completion in wave-23 remains SINGLE-SEAT (deck137 is still the
only convoke deck). Not yet.
