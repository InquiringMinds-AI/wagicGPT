# deck133 wave-24 — skill proposal

**PASS — with one small reinforcement offered for synthesis to weigh (not a new method).**

deck133's wave-23 proposal (the FACT-surface vs JUDGMENT-surface split under the crutch-lifecycle)
was offered last cycle and is not re-litigated here. This corpus surfaced no NEW methodological
gap in the strategy-writing skill: the guide is byte-frozen, the two wave-23 adds are
validated/insurance, and every finding routes cleanly to an existing layer (engine acceptance,
harness/parser, construction) — which is itself evidence the current method's layer-routing and
crutch-lifecycle are holding.

**One reinforcement (evidence for the skill's ACCEPTANCE-TEST discipline, not a new rung).**
This corpus is a clean demonstration of the skill's "verify with mechanical evidence, cite exact
windows" posture paying off in the OPPOSITE direction on two engine/harness items at the same
seat in the same wave:
- The **Yawgmoth SacrificeCost fix PASSED** decisively because the acceptance test was framed
  mechanically (count offers vs controlled-creature count) AND included the discriminating
  negative case (the proliferate mode legitimately offered at creatures:1) — which is what
  proved the fix is SURGICAL rather than a blanket suppression. The lesson worth keeping visible
  in the skill: an acceptance test is only trustworthy if it also names the case that SHOULD
  still fire, not just the case that should stop.
- The **retracted_choice hotfix acceptance produced a residual FP** whose exact trigger (the
  model quoting the prompt's own `e.g.` example verbatim) was only findable by reading all
  `CHOICE:` occurrences in context, not by trusting the fallback label. The lesson: a fallback's
  CLASSIFICATION is a hypothesis; the reviewer confirms it against the raw reply, and "the
  hotfix fired" is not the same as "the hotfix was right."

Both are already implicit in the skill's evidence discipline; offered as corroboration that the
method is working, and as a nudge that acceptance-test framing should routinely include the
negative/should-still-fire case. **No skill edit proposed.**
