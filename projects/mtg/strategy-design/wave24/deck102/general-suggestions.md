# general-suggestions.md (core-prompt proposals) — deck102 wave 24

PASS — no core-prompt change proposed this wave (19th consecutive PASS from this seat's routing
view). But raise ONE precise WATCH for the synthesis agent, because the seat's one fallback touches
core-appended text:

WATCH — the reply-protocol EXAMPLE can seed an OUT-OF-RANGE coded index. deck102 vs133 s5 emitted
`CHOICE: 2 (Cast Commander's Sphere)` as its only coded line when only options 0 (pass) and 1 (Flip
Side) were legal -> unparsed_reply. The index 2 was pattern-filled from the protocol's literal
example `e.g. "CHOICE: 2 (Cast Fatal Push)"`, compounded by a stale PLAN naming an unaffordable card.
Harm was ZERO (heuristic passed, the correct action) and the shape is ISOLATED (1/205 this seat, 1
across the corpus at this classification), so it does NOT meet the bar to break the 18-PASS streak on
its own. But if a 2nd seat produces an out-of-range coded line traceable to the example's index, the
fix is a small core-prompt edit — make the example index-agnostic (e.g. `CHOICE: <the number>
(<action name>)`) and/or add "choose only a listed number; if you can afford nothing, CHOICE: 0
(pass)" — so a priority window that offers only a display-toggle cannot pattern-match into a
phantom cast. Escalate then; PASS now.

Other items this corpus route BELOW/BESIDE core, not to it:
- s5 out-of-range hallucination + stale-plan seeding -> HARNESS/plan-hygiene + core-example WATCH
  above -> notes.md.
- vs59 pregame mulligan hand-misread ("zero Swamps" on a 2-Swamp hand) -> MODEL attention -> notes.md
  (representation was correct).
- R-DFC-BACKFACE-RESOLVES-FRONT -> ENGINE, un-re-exercised this corpus -> notes.md.
- Tergrid cast rules-correct + flip-thrash 0/6 + Smallpox discipline -> VALIDATED -> notes.md.
- 102vs133 timeout = latency-bound slow grind (18 priority windows on a stalled board) -> HARNESS
  throughput -> notes.md.
