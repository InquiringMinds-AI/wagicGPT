# deck59 — general-prompt (core) suggestions: PASS

No core-prompt change proposed. The core already carries everything deck59 needs at the
core layer: the hybrid-mana correction ("a hybrid pip can be paid with either color; if a
Cast option is listed it is payable; trust the Mana-available line"), the act/deploy floor,
the beatdown/race re-check, and the don't-overcommit-into-a-sweeper prior. deck59's two
real levers are DECK-SPECIFIC and belong in its guide, not the core:
  1. The W/B-off-Plains+Swamp mana simplification + pip table (a per-deck fact).
  2. The persist-makes-your-sweeper-one-sided synergy (a per-deck engine fact, and the
     scoped go-gate is the exact inverse of the deck102 symmetric-card gate that was
     deliberately kept in-guide pending a second seat).
The persist/symmetric-sweeper face is flagged for eventual core promotion ONLY if a second
recursion deck reproduces the UNDER-fire (see skill.md) — single-seat now, so it stays in
the guide. Core PASS (this continues the core's long PASS streak).
