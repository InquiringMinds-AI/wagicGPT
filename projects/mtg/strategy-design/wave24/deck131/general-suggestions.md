# deck131 wave-24 — general-suggestions.md

PASS.

No core / general-prompt (shared decision-prompt) change proposed from this seat. The seat's findings
route cleanly by layer without touching the shared prompt:
- The tapped-state misread recurrence (vs59 s23) is a MODEL-COMPREHENSION frailty against a CORRECT,
  unambiguous representation ([tapped] present on one creature, absent on the other) — not a
  representation defect the core prompt could fix. Single-seat, now 2 matchups at deck131 only; below the
  bar to touch the shared prompt on N=1 seat. Mitigation stays in the per-deck guide (already shipped
  wave-23) and a notes WATCH.
- The answer-first CHOICE-line-vs-conclusion contradiction (vs27 s30) is a HARNESS extraction issue
  (first-line locking), routed to notes.md I-2 and skill.md rung 3 — not a general-prompt representation
  change.
- The pregame mulligan phase validated cleanly (6/6 keep/mull correct, mulligan ask now renders on every
  game, correct bottom N); its one gap (bottom-card priority) is a per-deck guide addition, not a core one.

The general prompt's battlefield and mulligan representations are CORRECT and complete here; the residual
failures are model comprehension / harness extraction of correct representations, not defects the core
prompt could address.

This is the 19th consecutive general-prompt PASS.
