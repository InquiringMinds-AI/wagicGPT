# Skill deltas — deck135 wave-16

Current skill: `strategy-design/wave15/strategy-writing-skill.md`. Proposed deltas from this seat:

## NONE (no change to the skill).

The wave-16 deck135 review exercised the skill's machinery cleanly and found nothing it mis-handles.
The three hardest judgment calls this cycle all resolved correctly using existing skill principles:

1. **Exercise-check discipline held.** The brief's "an owned engine fix that got LITTLE exercise is
   not-exercised != not-fixed" framing (skill's decision-as-unit doctrine) kept the review honest: the
   69793930a tapped-out free-cast fix saw NO clean firing at this seat, so it was reported UNVALIDATED
   rather than either declared fixed (no evidence) or declared broken (the one free alt-cast that fired
   had mana up, not a counterexample). The skill's separation of "the engine capability exists" from
   "this window exercised it" did the work without temptation to overclaim.

2. **Layer-routing routed a NEW engine mechanism to notes, not the guide.** The hardcast-Force-offered-
   while-unpayable fizzle (deck110 x3) and the model's card-color hallucination (Into the North "blue")
   sit cleanly in the engine ledger + model watch. The FROZEN guide already forbids the *decisions*
   (never Force face burn; sanctioned-target list), so the skill's "would a new line prevent a class the
   guide does not already state?" test answered NO and held the guide byte-identical.

3. **The stale_echo verdict used the skill's "verify the menu actually shown" reflex.** Both stale_echos
   (deck133 s33, deck44 s18) were adjudicated by reading the ENGINE's offered options, not the model's
   echo — the skill's ground-truth-first habit — and both were confirmed correct catches (echo names an
   unoffered land type; not a superstring). No wrongful downgrade; the superstring fix caused no false
   negatives.

One OBSERVATION for the synthesis skill-owner (not a delta I assert): the wave-15 deck135 skill note
floated "name and date the first live confirmation of a long-open engine item." This cycle showed the
DUAL is equally valuable: an owned fix can SHIP and still get zero clean exercise, and the review must
say so loudly (UNVALIDATED with the reason: the triggering window shape did not occur). If the skill
does not carry a "when an owned fix ships but its trigger window did not occur, report NOT-EXERCISED
explicitly and carry the validation TODO forward — do not let a shipped fix read as a validated one"
instruction, the synthesis may weigh it. Cross-seat call, not a single-seat delta.
