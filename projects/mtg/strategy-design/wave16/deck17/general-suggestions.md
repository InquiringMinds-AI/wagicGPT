# general-suggestions.md - deck17 (wave-16)

Cross-deck / core-prompt / representation candidates from this seat. None is a core-line change
on single-seat evidence (doctrine); listed for synthesis to cross-reference.

## REPRESENTATION (promote candidate, now 2nd-order): STALE WITHIN-TURN MANA tracking.
The wave-15 hybrid-affordability annotation SHIPPED and WORKED - the hybrid off-color belief is
dead at this seat. But a successor tax appeared: on a second cast in the same main phase the
model recounts mana from its total Island count, ignores the explicit `Mana available: N from K
untapped sources` line, and distrusts the (correct) Cast list - burning 6000-9600-char replies
(vs62 s13, vs110 s5). The prompt is NOT wrong; the model overrides a correct line. Two cheap
representation levers worth a synthesis look: (1) tag the Cast list itself, e.g. header "Cast
list (already accounts for mana spent this turn)"; or (2) narrate consumed same-turn casts as a
mana delta ("you have cast Scion this turn; 1 mana remains"). This is the affordability family's
next member after the hybrid annotation - route: representation, NOT core prose. Promotion trigger
= a 2nd seat showing the "why isn't X listed?" oscillation (deck44/deck21 hybrid seats likely).

## STEAL-EARLY as a possible cross-deck core nudge (watch, not a proposal).
deck17 held its steal auras past the ideal window in the mirror (Class 4). This is guide-layer
here (deck-specific "steal the anthem"), but the general shape - "the model hoards its best
removal/answer and casts a durdle spell instead" - may recur on other control/interaction decks.
Flag for synthesis to check whether a core line about not hoarding reactive answers has 2+ seats.
Not originated as core from deck17 alone.

## Latency/tax remains the adjudication driver, and it is now smaller but not gone.
The hybrid tax collapsed materially (most hybrid casts now rl<900 vs wave-15's thousand-token
oscillations), which is the guide+annotation firing. Residual big replies are the stale-mana
class above plus carried degenerate-repetition (notes.md). Reply length at cast seams remains the
right guide-fired signal to track.

## Core PASS vote (this seat).
No 2+-seat core candidate originates here. Core's land/cast/steal-decline/block-when-favorable
rules all fired correctly. deck17's needs are deck-specific (deployment-excludes-walls, steal
priority, race branch) - guide-layer. Core = PASS from deck17.
