# deck21 seat - general (cross-deck) suggestions (wave-16)

Non-deck-specific observations; route to the harness/engine/core-prompt owner, layer-routed.
Doctrine: single-seat items cannot add/cut a core line - these are FLAGS with cross-seat
triggers, not core edits.

1. HYBRID-PIP ANNOTATION IS WORKING - CANDIDATE TO STOP RE-TEACHING PER GUIDE. deck21 is a
   top-3 seat for the "[hybrid: each {r/g} pays with R or ...]" annotation. Effect measured
   before/after: wave-15 (no annotation) had a hybrid MISREAD (vs133 s3, declined Scuzzback
   for "lacking green mana"); wave-16 with the annotation cast 7 hybrid cards (Scuzzback x4,
   Boggart, Boartusk x2) with ZERO declines. The misread class is gone with the annotation
   live. Cross-seat trigger for the synthesis owner: if deck17/deck44 (the other high-hybrid
   seats) also report 0 hybrid declines this wave, the per-guide HYBRID paragraph is now
   redundant belt-and-suspenders and future guides can shrink it to one line (deck21's guide
   already trimmed it this wave). Engine annotation earned its keep.

2. CORE "hold instant-speed interaction" LINE - the countermand is now VALIDATED, escalate the
   core-conditioning question. w15 flagged this (deck21 + deck109 both override it). w16
   confirms the override WORKS: deck21 held zero burn as interaction across 6 games (the w15
   game-loser is dead). This strengthens the case that the CORE line should be conditioned
   ("...unless your instants are burn you are using as a clock") rather than countermanded per
   burn-deck. Still needs the 2nd-independent-deck cross-seat trigger before touching core;
   deck21 now supplies a clean validated data point FOR the conditioning.

3. REASONING-TASK LOOP GUARD (carry-over, reduced but not gone). w15's tax epicenter is much
   quieter with the guide, but format-loops persist: vs62 s8 and vs44 s8/s20 burned full
   generations looping on the CHOICE-line FORMAT ("does CHOICE need a target?") before routing
   to heuristic. These are not deck-knowledge gaps the guide can close - they are protocol
   confusion. A cheap n-gram/line-repetition early-stop (proposed w15 #1) would recover the
   latency AND could let a truncated reply still carry a CHOICE. Archetype-independent.

4. MANA-FLOOD has no floor at this seat and decided a game. vs133 was lost 2-2 with an EMPTY
   board T5-T13 (deck21 drew only Mountains; Goblin Gang Leader stranded under its mana cost).
   This is pure construction/draw variance, not a play error - but it is a reminder for the
   synthesis/roster owner that deck21's 20-land, top-heavy-anthem build floods hard, and a
   couple of its close losses are curve variance, not guide failures. If deck21 keeps
   under-performing, the lever is deck construction (land count / curve), not the guide - route
   to notes.md's construction item, not a prompt change.
