# General-strategy (core-prompt) suggestions — deck44 wave 16 (layer-routed)

Core prompt = `bin/Res/ai/gpt/system_prompt.txt`. Per doctrine a SINGLE seat cannot add/cut a core
line; these are convergence candidates for synthesis only.

## G1 (NO core change) — the seat's issues are not core-prompt issues
deck44's pilot decisions were clean; the one misplay (self-GFTT, F2) and the two losses (color-screw,
reactive draw) are engine/representation/construction, none fixable in the shared core prompt.
**No core edit proposed from this seat.** Core has held PASS 10+ consecutive waves; this cycle adds a
positive control (4/6, clean decisions) — not a change signal.

## G2 (cross-seat WATCH, route to synthesis — do NOT edit core) targeting rules-confusion: "remove a creature" vs "counter a spell on the stack"
At vs135 s21 the model tried to Go-for-the-Throat a creature that was still ON THE STACK, believing
instant-speed removal could answer a spell before it resolves. The core already distinguishes counters
(answer the stack) from removal (answer the battlefield), yet the confusion produced a self-target.
This is the same *family* as the corpus-wide damage-vs-toughness and summoning-sickness model gaps
(wave-15 cross-seat model watches) — a rules-model deficiency, not a prompt-text gap. **Route: model
watch / representation (the engine-side own-target suppression in the ledger neutralizes the
consequence regardless of whether the model ever learns the rule).** Do not add core prose; the fact
is present and would just be re-disobeyed.

## G3 (convergence flag, not a proposal) hybrid annotation is a keep
Convergent with the other high-hybrid seats (17/21): the `[hybrid: ...]` option-line annotation is
net-positive and should stay. deck44 evidence: 6/6 correct, 0 misreads, one black-screw rescue (F4).
Representation-layer, already shipped — flagged only so synthesis can tally cross-seat support.
