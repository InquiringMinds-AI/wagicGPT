# Skill proposals — deck44 wave 16 (for the synthesis agent)

CURRENT skill = `strategy-design/wave15/strategy-writing-skill.md`. Proposals below are for the
strategy-WRITING skill (how guides are authored), not the deck44 guide itself (FROZEN).

## S1 (REINFORCE — proven again this cycle) "present-and-disobeyed" ceilings are representation/engine fixes, NOT more prose
The deck44 guide already forbids the self-GFTT-into-your-own-creature line in TWO places (lines
117-120 and 164-166, explicit and correct). The model READ the `only legal targets are YOUR OWN`
annotation at vs135 s21 and cast anyway (F2). Adding a third prose warning would not help. The
skill should keep (and this cycle re-confirms) the wave-15 principle: **when an instruction is
present in both the core and the annotation and is still disobeyed, escalate to a STRUCTURAL fix
(suppress the option / make the misplay illegal), do not add guide text.** A guide reviewer's
correct move on such a finding is to ROUTE IT to the engine ledger and FREEZE, which is what this
seat did. Worth an explicit skill bullet if not already crisp: "a recurring misplay the guide
already addresses is evidence FOR an engine item, not against the guide."

## S2 (METHOD — the wave-15 correction, generalized) verify a corpus-wide claim across seats BEFORE building method on it
The wave-15 deck44 reviewer inferred "combat asks removed corpus-wide" from a single seat's phase
histogram and built a block-prose-trim recommendation on it; synthesis refuted it with per-seat
counts (deck44 alone had 29/12). The skill should carry a hard rule (may already be implied by the
composition-seam doctrine): **a finding asserted as CORPUS-WIDE must be checked against >=2 other
seats' logs before it is written as a finding, and NEVER used to justify a core/method change from a
single seat.** Cheap counter-check: `kind=attackers`/`kind=blockers` enumeration across all seat logs
takes one script. This cycle's corrected read (30/14 at deck44) is the concrete cost of skipping it.

## S3 (KEEP — validated) the FREEZE discipline is working
deck44's guide is now frozen 12 consecutive waves and posted 4/6 this cycle with clean pilot
decisions. The skill's freeze-when-not-warranted posture is correct; the seat's failures this cycle
were construction/variance/engine, none guide-authorable. No change requested — logged as a positive
control that the frozen-guide + route-to-ledger loop converges.

## S4 (no change requested) hybrid-annotation authoring
The hybrid-pip annotation (an ENGINE/representation feature, not guide prose) did its job at this seat
(F4: 6/6 correct, 0 misreads, plus one black-screw rescue). No guide-authoring change follows — the
deck44 guide's existing Oona's Gatewarden line ("casts off a single Island OR Swamp") already teaches
the fact; the annotation reinforces it at the option line. Flag for synthesis only as convergent
evidence that the annotation is net-positive and safe to keep.
