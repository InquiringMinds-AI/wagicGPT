# deck62 general-suggestions (core-prompt proposals, layer-routed)

Per doctrine: a single seat cannot add or cut a core line. Everything below is routed to a layer
BELOW the core, or explicitly held as a single-seat candidate needing cross-seat corroboration.
Core status from this seat: **PASS** — no core-wording candidate. The deck's failures were
missing DECK KNOWLEDGE (handled by its new guide) and reasoning-tax on card mechanics (handled by
the guide's capability table), not gaps in the universal priors.

## G1 — HELD single-seat candidate (do NOT promote): a "your cards do only what their listed text
says; do not invent effects" anti-hallucination line.
qwen fabricated removal/stat effects for Fists of Ironwood and Web (vs110 s3-s5). This is the
representation-beats-instruction wall's cousin: the model invents card text. But the durable fix is
REPRESENTATION (the option line already carries the card's rules-text snippet) + the per-deck
capability table, NOT a louder core line — a generic "don't invent effects" line is unobservable
and untestable, and every deck's guide can pre-answer its own cards far more cheaply. Promotion
trigger: a SECOND engine/combo deck's reviewer finding qwen fabricating a card's effect in a way
the option-line rules-text snippet did NOT prevent — then the candidate is a REPRESENTATION item
(strengthen the option-line effect annotation), never a core sentence.

## G2 — reinforces an EXISTING core fact, no edit: the summoning-sickness / eligible-attacker-list
authority.
The core already states the attacker/blocker reply is the COMPLETE declaration and that lists are
complete/eligible. deck62 s20 (vs135) shows qwen re-deriving eligibility (counting summoning-sick
Saprolings) despite this. The core statement is present-and-under-applied, not missing — so this is
the per-deck guide's job (deck62's guide now says "attack from the list; it already excludes the
ineligible"). No core change. Flag only if a cross-seat pattern shows the eligible-list authority
losing corpus-wide.

## G3 — no action, confirms a core PASS: the attack-floor and act-floor lines fired correctly where
qwen was not drowning in tax.
When qwen was not tax-locked it deployed and attacked sensibly (vs21 win, vs44 grind). The core
act-floor / attack-floor priors are doing their job; deck62's passivity losses trace to combat-math
TAX (representation) plus missing deck knowledge, not to a weak core floor. This corroborates the
wave-15 core PASS from an engine-deck seat.

## Summary
No core edit proposed. G1 held single-seat with a representation-layer promotion path; G2/G3 confirm
existing core lines. The deck's needs are fully served by its new guide (capability table + engine
sequencing + aura-targeting + attack floor) and by the reasoning-tax reduction the guide provides.
