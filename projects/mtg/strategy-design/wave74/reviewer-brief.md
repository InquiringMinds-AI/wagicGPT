# Wave-74 step-3 brief (lean; REJECT is the expected verdict for anything not paid for by a decision the corpus shows going wrong. Amendment 332 stands: guides are decision-time instruction only, hard ceiling 20,000 bytes, no citations/history in a guide — evidence goes in YOUR review.md.)

Fifth reasoning-on corpus (invariant 000). Wave 74 step one (wave73/known-bugs.md §O → wave74/lane-{CC,CD,CE,CF,CG,CH}.md + wave74/codex-review.md): CC render truth — the ghostform gloss reads liveness from the GRANTED abilities (O1), the forced-sacrifice header keys on the chosen mode (O3), ATTACK TOTAL adds converter damage per converter rate (O5/F5), K excludes tapped no-untap creatures (O12), loyalty rows carry consequence clauses incl. battlefield-granted haste (O15/F8), `(keeping 7)` on the first mulligan window (O16), X menus ASCENDING so index = X (O19 — riskiest change, first live ANNOUNCE_X windows this corpus), hold bracket names the next seam (O20); CD window model — the O2 async-arm repair (forced-close/retry arms keep a decision pending; per-arm retry slots + park, F1/F2) and an entry trace at the attackers phase, both MDFC faces on the land-drop menu (O4), the hold re-open bracket normalises projected life (O6), `main_phase_windows_skipped` noted from every arm and excluding hold-suppressed phases (O10), `chain_windows_only_selfharm` + `chain_selfharm_rows` + `chain_acting_rows` instruments (O13, measure only), X-abandonment trace (O23), harness PILOT STALL tripwire (O26/F3/F4); CE stop/plan + pricing — the `stop=;M=` clause is STATE not a step and rides every carry (O7/F9), stop clause on every Create row (O7b), stop-vs-life reconciliation (O7c), keep-X on the X cast row (O8/F6), own-clock tag on decline/pass rows with a recurrence + lethal-incoming gate (O9/F7), the declined-count decline row TAGGED `this same question will be asked again this turn` (O11), mana-bill fold (O14), placeholder-free repeat-row name (O17), range collapse of identical-effect target rows with the option set intact (O18), `plan_names_stranded_card` instrument (O25); CG — the O11 count moved OUT of every ask/slot key (it had killed the identical-window replay: the first relaunch froze 11 games on 384-ask loops), harness WINDOW LOOP tripwire; CH — the hold latch keys on the row's ACTION identity with every render annotation stripped (life-dependent clauses had re-opened a hold at every link of a trigger chain: 41 asks in one upkeep), the tripwire requires an unchanged board. Records carry `thinking`, `reasoning`, `reasoning_chars`, `off_protocol_bytes`, `plan_line_missing`, `plan_steps_done`; gameend carries the counters above. Wave-73 guide edits for all seven decks are the LIVE guides (deck123's also carries CE's O17 rename).

Repo /home/magi/Projects/wagicGPT, game dir projects/mtg. Use python for translogs; use /usr/bin/grep (bare grep drops matches). DO NOT edit anything under bin/Res, src/, or run git or wagic; write only under projects/mtg/strategy-design/wave74/. Never write mechanisms as the owner's positions. Report what you did NOT check.

CORPUS (wave 74, MIXED BINARY — say so wherever it matters: 18 games from matchups-20260909-104713 on archives/wagic-60e9fe2ad-w74step1b (source 1d1d3744d, through CG) + 3 games (152v125, 125v126, 125v123) rerun on archives/wagic-76c5fab24-w74step1c (source aca5e06f8, + CH: hold key = action identity) after the WINDOW LOOP tripwire false-fired on a finite Sanguine Bond chain — the engine seat's HOLD regression watch must split by binary (rerun games have start_epoch ≥ 1788998000); `--thinking on`, WAGIC_GPT_TIMEOUT=900, no operator token ceiling): /home/magi/.Wagic/ai/gpt/selfplay-runs/matchups-20260909-104713-final (21 games, all completed naturally, 0 HUNG/crashed; 2,396 decisions (wave 73: 2,338) of which 1,866 ask-seam (wave 73: 1,757), 100% with native reasoning (median 6,004 chars), 4 fallbacks (1 http_error, 2 unparsed_reply — quote them, 1 noop_row_zero_reask — genuine?), 1 reply_truncated (quote it), 0 action_before_plan, 86 off_protocol_bytes>0 (4.6%, up from 2.8% — say what shape), 93 plan_line_missing (5.0%, up from 2.9%), latency p50 81.8 s / p95 183.1 s, decisions/turn 2.04 ask-seam; gameend sums: `ask_replays_reserved` 584 / `identical_ask_answers_reserved` 1 (wave 73: 1,262 / 999 — the CG fix restored replays but `identical_ask_answers_reserved` is still ~0: explain which replay path each counter names and whether 1 is a regression), `hold_released_turn` 70 (trace every one, split by binary), `hold_windows_skipped` 1,262 (cast 1,068 / priority 194), `stop_reached_windows_skipped` 30 (wave 73: 366 — deck123 stops still parse? the CE carry keeps the clause now), `sibling_window_asks_skipped` 0 and `chain_windows_collapsed` 0 (both again — read the code, and note CH's finding that the chain instruments run at the PRIORITY seam only while the chain windows were at the CAST seam), `chain_windows_only_selfharm` 6 / `chain_selfharm_rows` 560 / `chain_acting_rows` 1,123 (O13: what share of acting windows offered only do-nothing rows — CH measured 17% by hand), `main_phase_windows_skipped` 72 (wave 73: 375 — O10 rewired; is 72 the honest figure?), `reserve_decline_windows_noted` 11 / skipped 1, `plan_names_stranded_card` 3 (quote all three), `async_drops` 84 (wave 73: 43 — doubled; any decision lost?), `phase2_answer_recovered` 10 / `phase2_answer_missing` 2 (quote the 2), `repeat_annotated_takes` 2; askreplay/ sidecar 26 files; wins deck152 5/6, deck146 5/6, deck162 4/6, deck123 3/6, deck126 2/6, deck130 1/6, deck125 1/6). 

## Per-deck agent (one per deck: 146, 152, 125, 126, 162, 123, 130)
Purpose (owner's words): read the transcripts of your deck, compare the LLM's actions to the
instruction set (the guide projects/mtg/bin/Res/ai/baka/deck<N>_strategy.txt + the general guide
wave62/general-strategy.md (wave-59 edition + R331; NOTE from the wave-63..67 syntheses: this file is NOT loaded at runtime (only deck<N>_strategy.txt is opened, AIPlayerGPT.cpp:15332) — the deck guide is the live surface) + the reply protocol in the prompt), find issues in the game, the
interface, and the guide, and surface anything else worth the core loop's attention.
Method: for each of your deck's 6 games (your seat's translog; the opponent's for context),
trace the decisions that decided the game. Classify each misplay PERCEPTION (the model misread a
true surface, or the surface was false/missing -> ENGINE/RENDER item, cite file+seq) vs STRATEGY
(read correctly, decided against the guide or against good play -> guide item). Note fallbacks,
re-asks, and any decision where the prompt's information was insufficient or wasteful (what a
better interface would have shown). Hands are real (mulligans, bottoming): do not read variance
as guide effect.
Output: ONE file, wave74/deck<N>/review.md: (1) game-by-game outcome + the deciding decisions with
seq citations; (2) engine/interface/card items ranked HIGH/MED/LOW with a concrete repro (file,
seq, the rendered line) — this is the primary deliverable; (3) guide verdict: KEEP as is (a
success verdict, state why) or EDIT — if EDIT, also write wave74/deck<N>/strategy.txt (the full
revised guide, start from the live one) and list each edit before->after with the seq that
paid for it IN review.md — the guide itself carries NO citation, count, or history (Amendment 332); hard ceiling 20,000 bytes, and an EDIT that grows the guide must say which rule earned its bytes; (4) OPTIONAL proposals, only when
your evidence supports them: general-guide changes (wave74/deck<N>/general-proposals.md) and
strategy-writing-skill changes (wave74/deck<N>/skill-proposals.md; current skill edition =
wave68/strategy-writing-skill-v2.md (the ~40 KB distillation, owner-installed 2026-09-06 after six Astra adversarial rounds; read it in full — it is short; the 1.3 MB wave68/strategy-writing-skill.md is lineage only) — read it before proposing; a proposal that
restates an existing amendment is noise). No prediction tables, no rotation evidence, no
docket bookkeeping. Final message: <= 10 lines.

## Engine-narration seat (one agent)
You read the whole corpus for the engine's sake, not the guides'. Deliverable wave74/engine-seat.md: (1) corpus census as last wave (games, decisions, decisions/turn vs wave 73's ~2.1, fallbacks by kind with genuineness, `async_drops`, `transport`, `deadline_pct`, reply shape, `reasoning_chars`, `reply_truncated`, `askreplay/`, `plan_steps_done`); (2) REGRESSION WATCH on the wave-74 changes — the section that matters most, SPLIT BY BINARY where holds are concerned: (a) the CG/CH loop family — for every seat, asks per (turn, phase); any phase with > 10 asks: is it a genuine chain (board moves each link) or a re-put window (board unchanged)? For chains: was a HOLD taken, and did it hold across the links on the CH binary (the 3 rerun games) versus re-open on the CG binary (the 18)? quote `declined this exact list N times` maxima per seat (wave 73: ≤ 3); (b) EVERY `hold_released_turn` (70) — no hold outlives the holder's untap; (c) O2 — for every own turn with `able to attack right now` > 0 on the board line: an attackers record exists, or the new entry trace in the stderr names why not (list every miss = HIGH; the stderr has the trace line); also any turn where the seat never passed (a pending deadlock: many ticks, no decision, no pass — the F1 hazard); (d) O19 — every ANNOUNCE_X window: the echoed index equals the X the engine applied (`echo_index_conflict` notes = FAIL); (e) O4 — every land-drop menu with an MDFC in hand lists both faces; no double-decided drop; (f) O11 — takes of the tagged decline row vs the `[you declined this exact list N times]` clause: did the tag move the 40% (wave 73)?; (g) O7 — every carried PLAN on deck123 keeps its `stop=;M=` clause; `stop_reached_windows_skipped` 30 vs 366: fewer collapses because fewer loops, or because the stop no longer parses?; (h) O10 — `main_phase_windows_skipped` 72: cross-reference each counted phase against the seat's Casting decisions as last wave did (233 of 375 were false then); (i) new render clauses stating a FALSE fact: the O9 own-clock tag (rate and turns-to-zero vs the actual clock), the O8 keep-X line, the O7c stop-vs-life note, the O15 loyalty consequences, the O5 converter arithmetic, the O1 ghostform gloss (56/56 false last wave — now?), the O16 `(keeping 7)`; (j) `async_drops` 84 — trace a sample of 10: was the dropped answer re-asked, and did any decision fall to the heuristic; (3) adjudicate EVERY prediction in wave74/lane-{CC,CD,CE,CF,CG,CH}.md as PASS / FAIL / UNTESTED with counts and file+seq (UNTESTED is a real verdict), plus the carried docket ({X}-announcement payment loss — count `dropped after its X was announced` in the stderr files; BS F1/F2/F3/F5/F6; the land latch; N16's `main_phase_windows_skipped`); (4) engine/render/narration defects ranked HIGH/MED/LOW with repro — especially any new render clause stating a false fact (the loop-scope sentence, `[cost:]` on reveal rows, the ghostform gloss, the cannot-block subject); (5) the owner's lategame specimen (standing rule, alert-only): ONE late-turn decision (turn >= 25), full `prompt` verbatim to wave74/lategame-specimen.txt with a 3-line header. Final message: <= 15 lines, HIGH items first.

## Synthesis (runs only if any deck agent wrote general-proposals.md or skill-proposals.md)
Read all proposals + engine-seat.md. Decide each proposal: ADOPT (edit wave74/general-strategy.md,
a copy of wave62's (the R331 edition; waves 63-67 wrote no edition; wave 68 installed wave68/strategy-writing-skill-v2.md as the current edition), or append a numbered amendment to wave74/strategy-writing-skill.md, a
byte-verbatim copy of wave59's with amendments appended — verify the prefix with cmp) or
REJECT with one line of reasons. Write wave74/synthesis-notes.md. If a deck agent's guide edit
contradicts an adopted general change, note it; do not edit deck guides yourself.

METHOD NOTE (from the wave-59 engine seat): the translog `phase` field is not the rendered phase — count phase-gated facts from the prompt text.
