# Wave-74 step-one lane brief (base = master after wave73 known-bugs; evidence = wave73/known-bugs.md §O + wave73/engine-seat.md + wave73/deck<N>/review.md)

RULINGS THIS WAVE TOUCHES: 000 — reasoning only in the native channel; reply = PLAN then action, nothing else; no instruction licenses prose; no parser reads outside the two lines (a LABELLED number on the PLAN line is inside the line); caps never bound reasoning; the golden protocol TEXT (reply-protocol.txt / kReplyProtocol) and the thinking regime are owner-ruled — render brackets and row text are NOT the protocol and may change. 00 — full games; a game answered by heuristics is not a corpus game. 3 — suite THREADS=1 0 failed (known flakes: lifeline, merrow_reejerey, intrepid_adversary_repeated_payment), PARSETEST 0 failed. Never remove, cap, or auto-answer a legal option; collapsing windows with NO legal response other than pass/hold loses nothing — O13 is MEASURE ONLY for exactly this reason. No VPK. No AI attribution.

Standing mechanics: as wave71/lane-brief.md (worktree-only; `make -f Makefile.sdl -j8`; both link guards OK; binary freshness; every wagic run under `systemd-run --user --scope -q -p MemoryMax=4G -p MemorySwapMax=0 --`; suite THREADS=1 as a detached unit `w74-<LANE>-suite` waited with a FOREGROUND until-loop (no Monitor); count both failure strings; PARSETEST from bin; Edit tool for UTF-8 sources, byte-splice for MTGRules.cpp/GameObserver.cpp/primitives; U+FFFD 0; RED-on-base evidence + a falsifiable prediction per fix; commit with explicit paths, `git add -f` under bin/Res; write wave74/lane-<LANE>.md; touch `~/.gatelogs/W74_<LANE>_DONE`; weakest evidence; no corpora).

## Lane CC — render truth (O1, O3, O5, O12, O15, O16, O19, O20, O21)
O1 first (a false fact in the dangerous direction on 56/56 renders): judge ghostform liveness from the creature's GRANTED abilities (the `@movedto` trigger present = live), fixtures for both states, RED on base for the live state. O3: the forced-sacrifice header keys on the CHOSEN mode's effect on the target, not on the card having a sacrifice mode; PARSETEST contrast pin from `146v125` seq 60 vs `146v126` seq 27. O5: ATTACK TOTAL under the seat's own converter — add lifelink damage once more or drop the categorical verdict; pin. O12: K excludes tapped + no-untap creatures. O15: loyalty rows get consequence clauses (summoning-sick token; cumulative emblem; the `{right now: …}` pricing the Staff rows already have). O16: `(keeping 7)` on the first mulligan window in the later windows' spelling. O19: X menus ascending. O20/O21: the hold bracket says when another seam asks again this phase; hold text length; the worked example's forbidden annotation copy; the second-copy gloss tone.

## Lane CD — the window model (O2, O4, O6, O10, O13, O23, O26)
O2 first (a legal attack silently never asked, three times, one of them a lost game): entry trace at MTG_PHASE_COMBATATTACKERS, find the caller that never reaches `pendingCombatDecision`, fixture RED on base if the shape reproduces (deck152 HIGH-1 lists the three turns and the excluded look-alikes). O4: both faces on the `Land drop:` menu (never drop a face). O6: the hold key normalises projected life totals (`you/they would be at N`, `takes N damage - they would be at N`) and counts only rows absent from the held set; pin `130v162` seqs 36→37. O10: `noteMainPhaseCastingOffered` from every arm that issues a Casting decision; exclude hold-suppressed phases. O13: instrument `chain_windows_only_selfharm`, NO collapse. O23: find the path the `130v162` seq 52→53 abandonment took; make `dropped after its X was announced` fire there (or name the instrument that should). O26: harness PILOT STALL tripwire (newest K records all timeout fallbacks → banner + stop) and per-game timeout counts in the results banner, both under `--selftest`.

## Lane CE — the stop/plan family + cast pricing (O7, O7b, O7c, O8, O9, O11, O14, O17, O18, O25)
O7 first: the `stop=`/`M=` clause is state, not a step — `planStepEnds` must not open a step on it, and the carry re-prepends the persisted `mStatedStop`/count; pin `123v130` seq 141→142 and `123v125` seq 251→252 as carries that keep their counts. O7b: the stop clause on every Create row of the seam. O7c: `your stated stop was set when their life was N; it is now L`. O8: the X cast row prints the largest X that keeps the cheapest castable hand card payable, in the `<card> needs N` grammar. O9: decline/hold/pass rows price the seat's own repeatable damage clock. O11: the declined-count menus tag the plain decline row `(this same question will be asked again this turn)` — never remove it. O14: fold the mana-bill brackets. O17: repeat row short name without a placeholder. O18: identical-effect target rows over a threshold collapse to one range row whose answer names the target — the option SET must still reach every target. O25: instrument `plan_names_stranded_card`.

## Lane CF — fix lane for the Astra review (wave74/codex-review.md; base = master 92843101e after gate 1: 1285/0, 76 AI/0, PARSETEST 5708/0)
Nine findings, each CONFIRM (fixture/pin RED on base reproducing Astra's trigger, then fix) or REFUTE (a pin proving the trigger cannot occur, with the reason). F1 HIGH O2: the retry-arming branches still overwrite the single shared retry record regardless of owner (land forced-close outstanding, casting arms another) — per-arm retry storage, not a boolean flag; pin the interleaving Astra names. F2 MED O2: the decode-garbage retry branch (AIPlayerGPT.cpp:~16860) never sets mRetryArmLand — set it; pin a land-drop decode-garbage retry launching. F3 HIGH O26: the harness stall predicate ("last three per unfinished log") both kills a slow healthy run (21 live logs, six with one initial timeout, fifteen still on gamestart → STALL 6) and misses a stalled tail (one seat with 20 consecutive timeouts → OK 3; an old success vetoes six newer failures → OK 6) — redefine as the newest K decisions ACROSS seats by record time with every active seat represented, selftest all three of Astra's cases. F4 MED O26: the results banner claims "every decision was answered by the model" while counting only exact `timeout` — count every fallback class the tripwire recognises (wall_miss_no_retry, empty_reply, http_error) and say which; selftest the agreement. F5 HIGH O5: lifeToDamageConverterScript accepts fixed-one-life triggers (Cliffhaven Vampire `:life:-1 opponent`) as damage-proportional converters, and two Sanguine Bonds collapse to one — compute the converted amount from the battlefield (per-converter rate × count), pin Cliffhaven (4 lifelink vs 8 → 3, not 0) and double Bond. F6 MED O8: `maxX - keepNeed` mixes a colour-limited X budget with generic cost (Drain Life + Staff, 4 Swamps + 7 colourless → says no X preserves Staff; X=3 does) — compute keep-X from total payable mana after the fixed cost, with the colour limit applied to X only; pin the trigger. F7 MED O9: the own-clock tag scans scripts without checking the ability can function or recur (a tapped DOESNOTUNTAP pinger promises a two-turn clock) — require an untapped-or-untapping source with a usable activation (or a triggered upkeep source) and ADD the lethal-incoming gate the lane skipped; pin both. F8 MED O15: the token-cannot-attack clause reads haste from the planeswalker's own text only (Fervor on the battlefield) — read granted haste from the battlefield lords/statics or drop the categorical wording to "unless it has haste"; pin Fervor. F9 MED O7: the state-prefix scanner treats a thousands separator as a delimiter (`M=1,000; cast Staff` carries `M=1,cast Staff`) — accept `\d{1,3}(,\d{3})*` inside the state clause; pin the carried number, not only the step count. Also: O19's permutation pins do not exercise the live menu → parser → application path — add one PARSETEST case that runs an ANNOUNCE_X menu through render + answer + apply in the new ascending order. Suite THREADS=1 + PARSETEST, both 0 failed; write wave74/lane-CF.md; touch ~/.gatelogs/W74_CF_DONE.

## Lane CG — CORPUS-KILLING REGRESSION: the ask-cache key is unstable (O11) — base = master cfcc37a8f (gate 2: 1285/0, 76 AI/0, PARSETEST 5748/0)
The wave-74 corpus (matchups-20260909-015553, binary archives/wagic-4433a1199-w74step1) was STOPPED at 8h under invariant 00: 11 of 21 games frozen at turns 3-15. Shape (game 126v123, deck123 seat 1788936956-ai_baka_deck123-…jsonl): 384 of 386 asks are the SAME casting decision at turn 4 Main phase 1, each a full model call (~60-100 s), each answered `Cast nothing right now`; consecutive prompts differ ONLY in the carried PLAN and in the declined count — `{… you have already declined this exact list 297 times this turn}` → `298` — which the O11 tag (`declineRowReaskTag`, AIPlayerGPT.cpp:~28328) writes INTO the decline row's text. The ask key is `boardKey + tailStr` (AIPlayerGPT.cpp:~36984) and tailStr is the rendered option list, so the key changes every window → `mAskCache` never hits → the identical-window replay that absorbed these re-asks is dead corpus-wide: gameend `ask_replays_reserved` 0 and `identical_ask_answers_reserved` 0 on EVERY wave-74 seat (a comparable wave-73 seat: 14 / 5), and 12 seats show 100-382 declines of one list in one turn (wave 73: ≤3). The wave-73 render kept the count OUTSIDE the key (`[you declined this exact list N times…]` appended to userTail AFTER the key at ~36988). Fix: the O11 clause is appended to the user-facing tail after the key, never to the row text that feeds the key (or the key is computed from rows with the volatile clause stripped); PARSETEST pin: two windows differing only in the count yield one askKey; suite fixture RED on base: a seat holding one unplayable land and no castable spell in Main 1 — the casting decision reaches the model at most twice per identical state and the game reaches combat (count `Casting decision (Main phase 1` per turn in the fixture's trace, or assert `identical_ask_answers_reserved` > 0). Also verify, with a pin, that after `Cast nothing` with the land arm parked (CD O2's per-arm slots) `decisionPending()` is false and Act passes — the second candidate cause; state which of the two is load-bearing. Keep O11's intent (the tag) — only its placement moves. Harness (tools/selfplay-harness.sh): add a REPEAT-WINDOW tripwire beside PILOT STALL — a live seat whose newest prompt carries `declined this exact list N times` with N ≥ 40 prints `== WINDOW LOOP` and stops the run (selftest on a crafted log; this loop ran 8h unseen). Suite THREADS=1 + PARSETEST 0 failed; write wave74/lane-CG.md; touch ~/.gatelogs/W74_CG_DONE.
