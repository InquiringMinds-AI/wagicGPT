# Wave-74 known bugs (corpus matchups-20260909-104713-final: 21 games, MIXED binary — 18 on CG wagic-60e9fe2ad, 3 rerun on CH wagic-76c5fab24; sources wave74/engine-seat.md + wave74/deck<N>/review.md)

Invariant 000 held: 2,396/2,396 decisions with native reasoning, 4 fallbacks (2 of them the phase-2 HTTP 400 — P2), 1 truncation (P21), 0 hung. The wave itself: TWO corpus-killing regressions found and fixed before this corpus (CG: the O11 count inside the ask key; CH: life-dependent annotations re-opening a hold per chain link), so the loop grew a WINDOW LOOP tripwire, a PILOT STALL tripwire, and the early-check census. Wave-74 items PAID: O1 ghostform gloss 81/81 TRUE (was 56/56 false); O2 attackers window 33/34 asked on deck152 with the miss traced (wave 73's HIGH does not recur); O4 both faces annotated 44/44 (but see P18); O5 converter arithmetic 2/2, one won a game; O7 20/20 carries keep `stop=;M=` (was 0/50) — wave-73 deck123 HIGH-1 CLOSED, O7b 46/46, O7c 6/6 TRUE and it won a game; O16 42/42; O19 33/33 ANNOUNCE_X ascending, index = X, 0 conflicts; O23 discharged (5 fires = 5 declines, 53 mana lost, all deck125 Revelations); O25 3/3; O18 104 KB → 34 KB; CH hold held across an 84-link Bond chain with ZERO asks (deck126) and 0 counter-only re-opens on the CH games vs 12 on CG. `hold_released_turn` 70/70 at minimum span; `async_drops` 84/84 re-asked, 0 lost. All seven wave-74 guide EDITs installed (cce04d4af). Wins 152 5/6, 146 5/6, 162 4/6, 123 3/6, 126 2/6, 130 1/6, 125 1/6.

## P — routed to wave 75 (HIGH first)
- P1 HIGH — THE O11 TAG IS A NET NEGATIVE AND THE HOLD-CHECK BRACKET STILL KEYS ON ANNOTATIONS. (a) The tagged plain decline row was taken 284/561 = 50.6% (wave 73: 40%; CE predicted < 30%) — it damps with N (56% at N=1 → 26% at N≥9) but the headline rose; its rising count is the byte that killed run 1 (CG) and re-opened holds on the CG binary (engine-seat HIGH-1: deck125 t27 Draw 35 asks, 18 holds taken, life 26 → 2, every re-open an annotation; deck123 vs146 seqs 39-51 thirteen asks with empty `events`; deck162 vs125 seqs 66-76 eleven asks). REVERT the in-row tag (the separate `[you declined this exact list N times…]` line, outside every key, stays); never remove the decline row itself (a legal option). (b) `[hold check: N rows above are new …]` (O6's `holdReopenNote`, on the RENDER key by CH's deliberate choice) calls a row new when only `{leaves N of your M untapped mana sources}` moved (deck146 vs125 seqs 65-75 and 90-98: the same dead 3-row menu re-put 8 times, verdict oscillating) and contradicts the CH latch on 351 CH-binary renders (engine-seat MED-4). Fix: the bracket judges by the same ACTION key as the latch (a row that changes only in its annotations is the same row — say so in the bracket); pin the seq-65/66 pair. (deck125 A-5, deck146 HIGH 1, deck162 HIGH, deck123 HIGH-1, engine-seat HIGH-1/MED-4/§2f)
- P2 HIGH — the phase-2 forced-close prefill is rejected by the pilot's chat template (HTTP 400 `continue_final_message is set but the final message does not appear in the chat after applying the chat template`) on 2 of 12 forced closes, and one of the two was an ATTACK DECLARATION served by the heuristic (`126v152` deck126 seq 48, t20, three lifelink Vampires + Sanguine Bond vs 9 life — a lethal swing lost; `123v152` deck123 seq 64 at 448 s, `max_tokens_reasoning: 0`); the 400 body is NOT logged anywhere. Plus 5 of 17 `unclosed <think>` forced closes leave NO decision record at all (engine-seat HIGH-3 table: 125v130, 126v123, 152v130 one each, 126v130 and 152v126 one each short) — CD's "third path". Fix: (a) log the 400 body to stderr and the record; (b) diagnose the prefill shape against the LIVE pilot with a single probe request (allowed for this item only: one `curl` per shape, `--thinking on`, tiny prompt) — the template appears to strip the reasoning from the final assistant message so the continuation no longer matches; send the phase-2 continuation in a shape the template keeps (e.g. the answer prefix as the final message content with `reasoning_content` carried separately, or `add_generation_prompt` variants) and pin the request JSON; (c) every forced close writes a record (the recovery record if nothing else) — count `forced_close_unrecorded` and make it 0 on a stub run. (engine-seat HIGH-2/HIGH-3, deck126 MED-4, deck123 MED-3)
- P3 HIGH — Exquisite Blood `{4}{b}` tagged `[cannot pay now: needs 5 mana, you have 4 untapped sources]` and given NO cast row while Sanguine Bond `{3}{b}{b}` (strictly harder) is offered and resolves off the same four sources (`126v162` deck126 seq 22, t15; also seqs 17/21/26 — 16 of the seat's `cannot pay` tags): two Overgrown Battlements add {G} per defender (each {G}{G}), Chromatic Lantern any colour — the payability oracle under-counts board-dependent mana producers while the cast planner counts them. It decided that game (the win-condition half never cast). Fix in the shared oracle/`potentialManaPermissive` path, fixture RED on base (Battlement + one other defender + Lantern; Blood must be castable and listed). (deck126 HIGH-1)
- P4 HIGH — Sorin, Lord of Innistrad's `-6` charged THREE loyalty, destroyed nothing, reanimated nothing, and a second loyalty ability (`-2`) was offered and taken the same turn (`126v152` deck126 seqs 38 → 39 → 40, t18; script planeswalkers.txt:3059 is faithful to Oracle). Two engine defects: the loyalty cost parse of `C(0/0,-6,Loyalty)` (a two-digit/negative clamp?) and the once-per-turn loyalty latch not set when the ability fizzles or resolves with an `upto:3` target list. Fixtures RED on base for both; also the `-6` row is the only loyalty row with no `{right now:}` clause (O15 gap). (deck126 HIGH-2)
- P5 HIGH — under a live `LOOP RUNNING`/`LOOP COMPLETE` paragraph, a row that costs the seat life still prints the arithmetic figure `{right now: takes 1 damage - you would be at 41}` when the chain makes it 0 (`125v126` deck125 seq 267, t53 — the decision that lost a 42-7 game). Fix: while a loop paragraph is live, every row whose `{right now:}` moves the seat's life DOWN or the opponent's UP prints `you would be at 0 - this row feeds their chain` (the `[NAMED BY THEIR …]` override pattern). (deck125 A-1)
- P6 HIGH — `CRACK-BACK NEXT TURN` is phase-gated to the seat's main phases: 40/40 renders in Main 1/2, 0 of 173 own-turn Upkeep windows, 0 of 38 own-turn Draw windows — a draw-go deck commits its mana in the upkeep (`125v123` deck125 seqs 68-73 t23 Upkeep, 40 Humans on their board, no line; seq 79 Main 1 prints `would KILL you`). Fix: render in every own-turn window. (deck125 A-2)
- P7 HIGH — the `{crack-back cover: … adds 1 body … you would be at 3, which you SURVIVE … nothing uncounted here overturns it}` gloss counts a LEGEND-RULE DUPLICATE as a body on a row that prints the legend rule two brackets earlier (`130v162` deck130 seqs 44/45, second Rorix). Fix: subtract any body the row's own text says leaves on resolution (legend rule, sacrifice-on-ETB) before computing cover; pin. (deck130 HIGH)
- P8 HIGH — after a spell is COUNTERED, Main phase 1 did not re-ask the Casting decision with Rorix `[castable now]` and 6 sources open, and `main_phase_windows_skipped` read 0 (`130v125` deck130 seq 61 → 62, t20); NOT uniform (seq 24 → 26 t12 a countered Stone Rain WAS followed by a re-ask; seq 64 → 65 a resolved cast was). Find the arm that swallows the post-counter window; fixture RED on base if the shape reproduces (own main phase, cast countered, another castable card in hand → a second Casting decision). (deck130 HIGH)
- P9 MED — the reply parser rejects a well-formed answer whose `CHOICE: 1 (Play Sunpetal Grove)` shares the PLAN's physical line (`125v126` deck126 seq 154, `unparsed_reply`, fell to the heuristic). The protocol TEXT stays (owner-ruled); the parser recognising a present, unique, anchored `CHOICE:` label after the PLAN on the same line is reading INSIDE the two labelled parts, not prose tolerance — pin it, and pin MUST-NOT-MATCH a second `CHOICE:` or any prose after the answer. (deck126 MED-3, engine-seat MED-1)
- P10 MED — the O9 own-clock tag promises `reaches 0 in 18 more turns` to a seat at ≤5 life with `4 damage ALREADY ON THE STACK … KILLS you` in the same prompt (`125v162` deck125 seqs 169-175; 7 renders at ≤5 life; 112/325 in games then lost): F7's gate reads `mIncomingCombatTurn` only. Fix: gate on ANY lethal the prompt already states (stack, combat, loop). (engine-seat MED-2)
- P11 MED — the O8 keep-X clause names the row's OWN card on 27/238 renders (`Sphinx's Revelation … in your hand needs 3: no X on this row leaves it payable` — the keep candidate is a second copy): say `another copy of`, or choose the keep candidate among OTHER cards. (engine-seat MED-3)
- P12 MED (instrument) — the O13 chain instruments run at the PRIORITY seam only: on the CH binary 113 windows offered nothing but `this does nothing` rows while `chain_selfharm_rows` read 2; the honest corpus figure is 174/2,396 = 7% (deck126 85, deck123 58, deck146 31). Run the census at the CASTING seam too; measure only. (engine-seat MED-5, CH)
- P13 MED — `main_phase_windows_skipped` is still 39% false (28 of 72 counted phases DID put a Casting decision; deck152 16, deck146 9): a third arm issues a casting ask without `noteMainPhaseCastingOffered()`. Find it. (engine-seat MED-6, deck152 MED-3)
- P14 MED — the repeat row's printed short name is still un-answerable by itself: `CHOICE: 3 (Create vampire with Bloodline Keeper, repeated then stop)` → `repeat_count_missing` (`123v125` deck123 seq 58; O17 removed the `N` but the name still carries no count). Give the row a short name the footer's copy-instruction can satisfy, or make the bare copy resolve to the batch with a stated count. (engine-seat MED-7)
- P15 MED — 14 of 28 X menus still carry the largest-X-first boilerplate (`… largest X first: its first number is X = 12, the next X = 11 …`) after O19 made them ascending (`125v162` deck125 seq 148). Delete or rewrite ascending. (deck125 A-3)
- P16 MED — O18 not applied at the Equip seam: 39 identical `Equip with Lightning Greaves targeting Human #N` rows in an 81-row menu (`123v125` deck123 seq 186; also 222, 242). Collapse identical-effect target rows there too; the option set must still reach every target. (deck123 MED-2)
- P17 MED — `plan_names_stranded_card` does not fire when the plan names a card in NO castable zone (`125v152` deck125 seqs 28-29: plan says cast Revelation, hand has none, Revelation in the graveyard): widen to "named card in no zone you can cast from"; measure only. (deck125 A-4)
- P18 MED — the land drop is still decided at TWO windows: the first offers `Play Brightclimb Pathway {TWO FACES, ONE CARD: … the next window asks which face}`, the faces come at the second (9 repro pairs on deck146 alone; 18 of 322 decisions). O4's annotation works (the seat switched faces twice). Fix: the first window lists each face as its own row and the second window is not asked. (deck146 MED 3)
- P19 MED — the HOLD row is ~1,100 bytes (`125` seq 72: 1,074 B of a 3-row menu; wave 73: ~450 B) and holds were taken in 11/322 decisions on deck146: it costs ~1 KB on ~290 windows for 11 uses. CC refused a blind cut. Decide WITH EVIDENCE: for each sentence of the hold row, count how often the corpus reasoning bodies quote it (the `reasoning` field); keep the quoted ones, move the rest to a once-per-prompt paragraph. (deck146 MED 4)
- P20 MED (instrument) — `reasoning_degenerate` (exact 40-char window ratio) reads clean on every record (median 0.0034) while 319 of 322 deck146 traces re-quote the protocol block and loop self-verification boilerplate with small edits. Replace or supplement with a near-duplicate measure (token 8-gram repetition rate); measure only. (deck146 HIGH 2)
- P21 MED — the corpus's one `reply_truncated` cut at exactly 64 characters (`130v146` deck130 seq 16: `PLAN: Hold priority, then activate Talisman for {R}, cast Siege`), `reasoning_chars` normal, retry recovered: a short transport read returned a partial body as complete? Find the 64. (deck130 MED)
- P22 LOW — the pathway MDFC card scripts log `ABILITYFACTORY Error parsing: {0}:doubleside(grimclimb pathway)` and six `Parser returned NULL {t}:add{g}` / `{t}:add{w}` (borderline.txt:14068, :13564, :19785, :52390); O4 works through `autohand=`, so the harm is unknown — fix the script grammar or the parser, pin. (deck123 LOW-4)
- P23 LOW — residue: 2 `echo_index_conflict` outside the X family (`146` seq 49, `126` seq 66, both name-rescued); the X ladder's draw-punisher economics print AFTER the cast is committed (`125v162` seqs 147 → 148 — the decline row was the only way out); the hold row taken on menus printing a live activated row (deck126 LOW-4, third wave); `off_protocol_bytes` and `plan_line_missing` are ONE event (the unlabelled plan sentence; 12/16 on deck162) — report them as one; 31 replies carry an answer and no plan (owner-ruled report-only).
- P24 LOW (loop tooling) — `~/.gatelogs/corpus-early-check.py` keys on file mtime vs the launch script and is confounded by any other run writing the shared logs dir (CG's probe logs showed as 72 seats); the harness PILOT STALL K-pool can fill from ONE seat (a lone seat's silent tail would stop a corpus whose other 20 seats answer) — bound per-seat contribution to ceil(K/live).

Carried, UNTESTED again (never build on): BS F2 Tribute second edict (4th wave); BS F3 / land latch (`declined_face_latches` 0, 4th wave); BS F5 blockers ceiling; BS F6 distinct-index re-ask; CF F1 pending-deadlock direct measurement; F2/F5-nonlifelink/F8 haste/F9; reasoning-body halves of N14a/N14d/O20.
