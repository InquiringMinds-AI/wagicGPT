# Deck110 wave-9 — suggestions for the general prompt / core (NOT deck-specific)

deck110 proposes NO core prompt-body edit this wave. All deck110 signal routes to REPRESENTATION
(engine ledger) or is frozen. Rationale + one cross-deck-relevant representation item below.

## No core edit — the wall this wave is representation, not instruction.
The one live deck110 defect (artifact-count fold-in) is a surfaced-value re-derivation error. Per
the standing "representation beats instruction, do not escalate wording at a read-then-disobey
wall" doctrine, it is NOT a core-sentence candidate — a core line about trusting displayed counts
would be a louder copy of a deck guide line the pilot already partially ignores. Single-seat,
representation-owned. FAILS the convergence gate for core.

## CROSS-DECK REPRESENTATION ITEM (engine ledger, not core prompt) — per-permanent tag completeness.
The per-permanent battlefield line tags SOME permanents `[artifact]` but not all: artifact LANDS
(Darksteel Citadel) and artifact CREATURES (Etched Champion, Signal Pest, Memnite, Vault Skirge,
Ornithopter) render WITHOUT the tag, while the summary `Artifacts in play: you N` counts them
correctly. Any deck whose decisions turn on a permanent SUBTYPE the pilot must re-derive (artifact
count for affinity/metalcraft here; could recur for enchantment/land-subtype triggers elsewhere)
is exposed to the same both-directions mis-tag. Recommend the engine tag EVERY permanent with its
game-relevant type consistently, so the per-item surface AGREES with any summary line. This is the
generalized form of the wave-8 "surface the computed value" fix: surface it AND make the components
un-mis-taggable. Owner: representation/engine, not the prompt. Low urgency (no proven loss yet); a
good pause-window backlog item.

## Confirmations relevant to core (no action)
- Post-A/B core (hand->stack anchor removed): 0 engine no-ops, 0 desyncs, 0 defer, 0 unparsed
  across 200 deck110 decisions — corroborates the anchor removal caused no regression at this seat.
- cast-nothing 10% (down from wave-8's 17%), concentrated in a grind matchup — the threat-before-
  value / deploy-floor core lines continue to behave; no durdle regression.
