# Skill deltas from deck110 — wave 9 (FINAL wave before pause)

Contributions to the wave-9 synthesis from the deck110 (Etched Affinity) seat. deck110 shipped
NO guide edit this wave (frozen); the method contribution is a sharpened terminus for a
representation fix that a guide READ instruction could NOT fully carry.

## HEADLINE — a guide READ instruction added over a surfaced-and-accurate value only PARTIALLY
## kills a re-derivation false belief; the terminus is TAG-COMPLETENESS at the representation
## layer, not more guide wording.

Wave-8's terminus for a surfaced value was "teach the pilot to READ the line, demote/simplify the
hand-count workaround." deck110 tested that terminus for the first time and it landed only
PARTWAY. The pilot now matches `Artifacts in play: you N` on most count-stating decisions, but the
specific false belief the READ was meant to kill — *Glimmervoid and basic lands are artifacts* —
SURVIVED the instruction (vs44 s7 "Play Glimmervoid to increase artifact count to 3"; vs109 s28
folds Glimmervoid; vs140 s65 counts Mountains + Islands as artifacts), all while the correct,
Glimmervoid-clean N sat one line above.

The generalizable method point: **a READ instruction competes against the executor's habit of
RE-DERIVING; when re-derivation is available and the executor distrusts summaries, the instruction
does not win by repetition.** The durable fix is to make re-derivation IMPOSSIBLE-to-get-wrong at
the representation layer — here, tag EVERY artifact on the per-permanent battlefield line
(`[artifact]` currently appears on Cranial Plating/Elixir but NOT on Darksteel Citadel or artifact
CREATURES), and leave Glimmervoid/basics untagged, so a re-derived count AGREES with the summary N
in both directions. An INCOMPLETE tag set actively FEEDS the miscount (the pilot under-counts
untagged artifact lands/creatures and over-adds Glimmervoid). This extends the wave-8 "prefer an
engine-SURFACED number, verify its offset" rung: the offset can be zero on the SUMMARY yet the
per-item TAGS still license a wrong re-derivation — verify BOTH surfaces agree, not just the summary.

## SECONDARY — score a two-wave representation loop by whether the FALSE BELIEF died, not whether
## the number is read more often, and STOP escalating the guide when the instruction hits the wall.

deck110's fold-in is a read-then-disobey wall: surfaced + accurate + guide-pointed-at + corrective
fact stated, and still bypassed. Per the standing representation-beats-instruction rule, this is a
FREEZE-the-guide signal, not a rewrite signal — the wave-8 wording is retained verbatim because it
helped (matches rose) and because further wording would be the escalate-at-a-wall anti-pattern.
The residual is routed to representation (engine ledger), and — because this is the PAUSE wave —
captured self-contained in notes.md (E3) with the exact tag-completeness lever and its citations.

## CONFIRMATIONS (no method change; continuity)
- The confirmed-fired-representation DEMOTION terminus (deck110's re-attach marker, the original
  exemplar) held a THIRD running wave: 47 marked offers / 0 taken, 0 no-op equips. A closed loop
  stays closed; freeze verbatim, no re-verification burden beyond a grep.
- Turnaround/obeyed-but-losing freeze-mode held for a fourth wave: same guide, per-game win/loss
  flips are pure draw variance (vs44 L->W, vs109 W->L) — do not chase them with guide edits.
