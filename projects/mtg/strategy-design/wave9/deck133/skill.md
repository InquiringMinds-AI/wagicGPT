# deck133 wave-9 proposals for the strategy-writing skill

Mode this wave: OBEYED-BUT-LOSING / TURNAROUND (record 3W/3L real games; the 135 wedge is now a
COMPLETED win — livelock fixed). ZERO guide edits (frozen). Proposals below are METHOD data points,
each flagged single-seat vs confirming-existing. This is the FINAL wave before the project pause.

## 1. [CONFIRM — terminus held into a SECOND corpus, on a MULTI-cast sample] A confirmed-fired-representation DELETION stays deleted, and the low-density card that motivated the "keep it, never-drawn != invalid" stance finally produced its multi-cast validation.
The wave-8 Gray Merchant "+2" deletion rested on a SINGLE cast + the brief probe. Wave-9 supplied THREE
GM casts (drains 6/8/10, all shown == resolved), all off the shipped bare-N display — including all
three GM roles (stabilizer, tempo drain, combined-lethal finisher). Two method points reinforced, no
change: (a) a shipped-and-verified representation deletion does NOT regress a wave later — do not
re-audit it at full prominence once the deletion is confirmed twice; (b) the wave-8 "keep a low-density
keyed trigger even after waves of no data" stance is vindicated a second way — GM reached the list in
2/6 games last wave and 4/6 this wave, and the multi-cast sample only arrived now. Under-powered
evidence for MANY waves is the NORM for low-density cards; cutting on absence would have been wrong.

## 2. [CONFIRM — cross-seat representation completion] A broadened engine rider is verified from the CONSUMING seat, which is the OPPONENT, not the deck whose card it is.
The Obliterator sac-punisher rider is deck133's wall mechanic, but it is CONSUMED by the DEFENDER
deciding whether to block — i.e. in the OPPONENT's seat log, never deck133's. The wave-8 verdict
("punisher rider didn't fire for Obliterator / no block window") could only be closed by reading the
opponent seats of games where deck133 ATTACKED with Obliterator (OPP135 seq38, OPP131 seq56 — rider
rendered, both defenders declined the block). Method note (extends the composition-seam rule across
seats): to verify a representation that renders for the OPPONENT of the reviewed deck, grep the
opponent's seat file at the seam the reviewed deck's card creates — the fix and its consumer live in
different files AND different seats. A reviewer looking only at deck133's own log would have reported
the rider as still unverified.

## 3. [CONFIRM + SHARPEN — the read-then-disobey wall has a HALLUCINATED-EMPTY-HAND shape] When the deciding fact is surfaced AND the pilot FABRICATES a different fact over it, the fix is a HARDER representation on the OPTION LINE, never more guide prose — and a maximally-prominent rule (stated twice) that is still bypassed is the proof.
deck133 vs131 seq58: `Opponent hand size: 0` was surfaced; the guide forbids Thoughtseize-into-empty-hand
in TWO places; the pilot cast it anyway, its reply inventing "the opponent may have a removal spell or a
counterspell." This is distinct from the plain read-then-disobey wall (there the pilot reads the fact
and acts against it); here the pilot OVERWRITES the surfaced fact with a fabricated one (a card in a
0-card hand). Same routing conclusion, sharper trigger: do not add a third copy of the rule (the two it
has already lost); ship an OPTION-LINE annotation that puts the corrective fact WHERE the action is
chosen (`Cast Thoughtseize {b} - opponent's hand is EMPTY; this only costs you 2 life`), exactly the
form of the shipped own-targets annotation. The authoring test: if a fact is BOTH surfaced in CURRENT
SITUATION AND stated in the guide, and the pilot still fabricates over it, the only rung left is
annotating the OPTION itself — the last place before the number is emitted. Single-seat but a clean
exemplar of the own-targets-annotation family; keep as a watch under the existing representation method.

## 4. [CONFIRM — freeze mode sizing held, tighter than wave-8] Freeze-check on a 3W/3L wave produced a ZERO-edit diff.
Wave-8's mixed control-arm wave yielded a one-edit diff (the engine-driven GM correction, since shipped).
Wave-9, a different mixed 3W/3L round-robin, yields a ZERO-edit diff: every prior fix is shipped or
obeyed, the losses are construction-bound (no model-fixable #1-slot lever), and the one live leak
(Thoughtseize) routes to representation, not the guide. Positive data point: as a wave loop reaches its
terminus, the correct guide diff trends to zero — a frozen guide that stands alone is the SUCCESS
signal for a closing wave, not an under-effort one. The "size the residual lever's ceiling" gate
correctly declined every construction-bound loss.

## 5. [METHOD — lift stale exclusions when the engine fix ships] A matchup EXCLUDED as an engine wedge in a prior wave must be RE-INCLUDED once the fix ships — do not carry the exclusion forward by habit.
135v133 was excluded in wave-8 (turn-2 livelock). Wave-9's binary shipped the cast-seam livelock breaker,
the game completed (T11 win), and it re-entered the play-quality set — where it supplied the corpus's
cleanest combined-lethal GM line. Method note: prior-wave exclusions are DATED to a binary; when the
brief confirms the fix shipped, verify the matchup completes at the seat and lift the exclusion, rather
than inheriting "excluded" from last wave's findings.
