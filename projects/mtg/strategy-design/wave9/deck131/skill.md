# deck131 seat — wave-9 method contributions (for the wave-9 synthesis)

Mode this wave: OBEYED-BUT-LOSING for the FIFTH straight wave (0/6). Unlike waves 5-8 there is
NO new guide diagnosis — the wave-8 revision is validated on every axis it targeted and the guide
FREEZES. The value this wave is a clean CLOSING result plus one cross-cutting promotion. Proposed
additions/refinements:

## 1. [PROMOTE — the strongest general lesson this wave] A guide fix can SUCCEED at the reasoning
level and the SAME bad outcome still appear via the REPLY PROTOCOL — and the two are distinguished by
reading the PLAN, not the choice. Wave-8 fixed deck131's mulligan by making the red-source gate
STRICTLY ORDERED and un-overridable (killing a false-KEEP where the pilot's REASONING got hijacked by
a lone "Young Pyromancer ... ALWAYS a keep" token). Wave-9, the SAME 0-Mountain/no-draw-spell hand
appeared (vs44 seq1) and the pilot's PLAN reasoned CORRECTLY to Mulligan — it explicitly named and
REJECTED the Young Pyromancer bait ("without a red source ... I cannot cast my win condition. I will
shuffle this hand back"). The de-hijacking WORKED. BUT `chosen_text`=Keep: the head token `1` (=Keep)
was emitted BEFORE the PLAN and never revised — REPLY-PROTOCOL INTENT-COLLAPSE. METHOD: when a
guardrail/gate rewrite targeted a bad OUTCOME, do NOT score the fix by the outcome alone — score it by
whether the REASONING now reaches the right verdict. A fix that corrects the reasoning while the
outcome stays wrong is VALIDATED (freeze it) and the residual belongs to the reply-protocol layer, not
the guide. The verification metric SPLITS: report reasoning-correct and outcome-correct separately
(no-red mulligans: reasoning 1/1, outcome 0/1). This is the mirror of confirm-the-fix-FIRED: here the
fix fired in the reasoning; the leak moved to a lower layer.

## 2. [PROMOTE — the reply-protocol intent-collapse is now MULTI-SEAT; escalate from WATCH to A/B]
Wave-8 logged intent-collapse as a SINGLE-instance watch (deck135 s48: head `4`=Cast nothing, PLAN
concludes cast Icehide Golem, self-corrected next phase). Wave-9 adds a SECOND, cleaner seat on a
DIFFERENT deck (deck131 vs44 seq1: head `1`=Keep, PLAN concludes Mulligan, NOT self-corrected — a
one-shot mulligan window, so the stale head is FINAL and costs the game). Two seats, two decks, two
waves, identical mechanism (head committed before reasoning; reasoning reverses; head never revised).
This clears the single-seat bar the wave-8 synthesis set for the reply-protocol A/B. RECOMMEND: run
the A/B now (accept the LAST bare number emitted / move the CHOICE line AFTER the PLAN), judged by
decision-quality on symmetric core, not win-rate. Distinct from PLAN/CHOICE MISMATCH (there the head
matches a DIFFERENT genuinely-intended action); here head and reasoning target the SAME window and the
head is simply stale-first. Route to the reply-protocol layer; never re-word any guide over it.

## 3. [CONFIRM] The land-menu fix (unofferable-option → enumerated) is CLOSED across two corpora.
Mountain-first 9/9 this wave (12/12 wave-8), the pilot picks by NAME regardless of list position. The
interim stopgap stays dropped. A representation fix confirmed-and-obeyed on a SECOND corpus is fully
retired — no further verification needed; treat as permanent unless the co-offer count regresses to 0
(which would mean the enumeration itself regressed).

## 4. [CONFIRM] OBEYED-BUT-LOSING + precondition-counting held for a FIFTH wave as the right posture,
and a PAUSE-clean guide is a legitimate wave outcome. n=6 win-rate is noise; the stable signal is
per-seam obedience (attack participation 100%, mulligan both directions, Guttersnipe-out cast rate 0
leaks, block quality, NEVER-block-Guttersnipe held) and the representation ledger. Every loss lacked a
precondition the guide cannot create (a threat that sticks, a clock the 2-damage engine can beat vs
lifegain). Two losses this wave were CLOSE (opp 9, opp 5) — the residual is threat density (6/60), a
construction lever, not a guide lever. A loud rewrite off 0/6 would churn frozen, obeyed lines; the
correct wave product is a FREEZE with the residual routed to construction + reply-protocol. This is
the terminus a multi-wave OBEYED-BUT-LOSING seat reaches: the guide is done; further wins come from
the decklist and the harness, not the prose.

## 5. [CONFIRM] A precondition-rare defensive clause (the wave-8 board-wipe counter clause) can go a
whole corpus with NO test window and that is not a failure — freeze it, note "untested", do not churn
or delete. vs140 (the Pyroclasm deck) never resolved a sweeper into a live Guttersnipe with a counter
up this wave; the counter seam instead played WELL on the threats it did face (Dissipate/Counterspell/
Essence Scatter on Staff of Nin / Venser's Journal / Blightsteel Colossus). Absence of a test window
≠ evidence against the clause. Same for the single-seat `damage>=toughness=dead` watch: no recurrence
because the precondition didn't recur — it stays single-seat, not promoted, not dropped.
