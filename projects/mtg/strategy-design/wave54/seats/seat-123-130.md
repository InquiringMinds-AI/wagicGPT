# Wave-54 seat: deck123 (2/6) + deck130 (2/6)

ELEVENTH fair-hand corpus, `~/.Wagic/ai/gpt/selfplay-runs/matchups-20260903-034355/`, binary
`caf023dbc` (wave-54 step-1 lanes A-F + audit lanes G-N + wave-53 lane AA). Model
`qwen36-35b-a3b` on every gameend record at both seats. Guides = the wave-53 reviewer revisions
(`baf36b936`) plus the boundary pass (`a0067bae5`). Per the carry's binding rule nothing below
attributes a win-rate, a game length or a curve to a guide edit: hands are real and this corpus's
games are much shorter than wave 53's at every seat but one.

**Files (short -> full).**
- 123v125 `1788425038-ai_baka_deck123-0x559abc0a67c0-vs-ai_baka_deck125.jsonl`
- 123v126 `1788425079-ai_baka_deck123-0x55ea52d6d990-vs-ai_baka_deck126.jsonl`
- 123v130 `1788425067-ai_baka_deck123-0x55f21b7f16b0-vs-ai_baka_deck130.jsonl`
- 123v146 `1788425057-ai_baka_deck123-0x562dbd9c9f50-vs-ai_baka_deck146.jsonl`
- 123v152 `1788425063-ai_baka_deck123-0x5599da8361c0-vs-ai_baka_deck152.jsonl`
- 123v162 `1788425060-ai_baka_deck123-0x55b799669da0-vs-ai_baka_deck162.jsonl`
- 130v123 `1788425067-ai_baka_deck130-0x55f21cb0dbb0-vs-ai_baka_deck123.jsonl`
- 130v125 `1788425054-ai_baka_deck130-0x562a87d286e0-vs-ai_baka_deck125.jsonl`
- 130v126 `1788425069-ai_baka_deck130-0x560ee086a010-vs-ai_baka_deck126.jsonl`
- 130v146 `1788425059-ai_baka_deck130-0x55a765e11f40-vs-ai_baka_deck146.jsonl`
- 130v152 `1788425065-ai_baka_deck130-0x55c0c8922820-vs-ai_baka_deck152.jsonl`
- 130v162 `1788425044-ai_baka_deck130-0x5647da3006c0-vs-ai_baka_deck162.jsonl`
Opposing seat logs read for the other side of the narration; eleven `game-*.stderr` files read for
the drop/release counts in the ENGINE list only.

**Records.** deck123: **560 records = 558 decisions + 6 gamestart + 6 gameend + 8 `recovery`;
16 fallbacks.** Kinds: ask 470 / priority 63 / attackers 8 / blockers 4 / discard 8 / reveal 2 /
bottom 2. **2 wins** (v146 t10 12/-3, v162 t12 13/-32), **4 losses** (v152 t9 0/29, v126 t19 0/44,
v130 t24 0/20, v125 t64 0/49).
deck130: **340 records = 338 decisions + 6 gamestart + 6 gameend; 1 fallback.** Kinds: ask 222 /
priority 96 / attackers 18 / blockers 1 / discard 1. **2 wins** (v125 t20 20/-5, v123 t24 20/0),
**4 losses** (v152 t11 -3/29, v162 t11 0/12, v126 t18 0/44, v146 t33 -2/14).
Combined **896 decisions, 17 fallbacks = 1.90 %** (wave 53 at these seats: 2/1,021 = 0.20 %) —
and **17 of the corpus's 23 fallbacks are mine**. 0 empty replies, 0 timeouts, 0 `choice: -1`
without a fallback. Latency p50 **8.6 s** (wave 53: 15.8 / 16.3), p90 46.8 s, >60 s 56 of 896,
max 1,488,661 ms (`123v125` s1, the opening mulligan under 21-game launch contention — the
corpus's one `long_reply`). Max prompt at my seats **23,798** (`123v125` s309, turn 63).
`123v125` at 312 records is the corpus's largest seat log.

**Opponent-turn windows, reported separately (carry obligation).**
| seat | opp-turn casting asks | casts | plain declines | HOLD taken | own-turn asks | own-turn casts | `hold_windows_skipped` |
|---|---|---|---|---|---|---|---|
| deck123 | **268** | **2** | 234 | **32** | 146 | 38 | 31 / 9 / 0 / 0 / 0 / 0 |
| deck130 | **9** | **0** | 7 | **2** | 142 | 55 | 16 / 0 / 0 / 0 / 0 / 0 |
HOLD row: **286 renders, 34 takes (11.9 %)** — wave 53 at these seats was 340 / 4 (1.2 %). My
seats hold **34 of the corpus's 43 takes** and **4 of its 7 non-zero `hold_windows_skipped`
gameends** (sum 56 of 72). Identical-declined runs of 3 or more: deck123 **244 of 558 (43.7 %)**,
deck130 **12 of 338 (3.6 %)**. Declined-note `N >= 3` windows: deck123 165, HOLD taken in **20**
(12.1 %), a cast in 1; deck130 6, HOLD taken 0.

**Mulligans / hands (new-baseline record).** deck123 **8 looks, 2 mulligans, 6 keeps, 2 bottoms**
— both mulligans at 1 land under `would not cover any spell in it`, both keeps at (keeping 6),
land counts on the keeps 3,3,3,3,4,3. deck130 **6 looks, 6 keeps, 0 ships, 0 bottoms**, land
counts 1,2,2,3,3,2. **No mull-to-zero, no repeated mulligan, no floor break at either seat;
nothing to raise to the owner.** Both keep sets agree with their guide's header rule at every
look (14/14). Two notes: (1) `123v146` s2 is the FIRST render at deck123 of the `would not cover
any spell in it (you have 3 lands ...)` branch — kept, and won on turn 10; (2) `130v123` s1 kept a
**one-land seven** whose coverage line named two cards, which that guide licenses explicitly, and
won 20 to 0. deck130's `would not cover` branch: **0 renders, fourth corpus** -> #132 stays
UNTESTABLE-AT-THIS-SEAT (skill #200). First corpus with real `bottom` records at my seats: 2, both
correct, both deck123.

---

# deck123 - Intruders of Thraben. 2/6.

## L1 - v125 (dead turn 64, 0 to 49): 303 decisions, five cards spent off rows that said they did nothing
The corpus's longest and largest seat log, and the only game at either seat that resembles wave
53's shape. Counters answered every threat; the seat never held a creature past turn 14. Three
separate findings live in it.
- **Five dead casts, each with the verdict in the pilot's own prose.** s147 and s192 and s269
  `Cast Intruder Alarm {2}{u} [second copy: you already control Intruder Alarm; both stay on the
  battlefield - no legend rule, but its effect is already on the battlefield and a second copy
  changes nothing]`, whose replies read "second copy is dead", **"cast second Intruder Alarm
  (dead, but only way to proceed)"** and "legal but redundant". s176 `Cast Damnation {2}{b}{b}
  {right now: destroys 0 of their creatures (0 without a restriction against attacking), 0 of
  yours}` under **`PLAN: Damnation: N 0, K 0, M 0; cast.`** - the first time in four corpora the
  guide's mandated three-number line was written, and it was written above the cast it forbids.
  s211 `Cast Tribute to Hunger {2}{b} {right now: they control 0 creatures - at 0 this does
  nothing}`. On four of the five, every other cast row on the menu was also annotated dead.
  A54-1, A54-2, A54-6, A54-TRIM1; G1 for the render half.
- **The `named_row_reask` cluster: seven of the eight, all one shape.** s37/s100/s150
  `CHOICE: 1 (Cast Bloodline Keeper)` (menu row 1 is Cast Damnation; the Keeper is not in hand),
  s56/s105 `CHOICE: 7 (Cast Vision Skeins)` on five- and six-row menus, s58/s107
  `CHOICE: 7 (Cast Bloodline Keeper)`. Those numbers are this guide's OWN cast-list entries
  (entry 1 = Bloodline Keeper, entry 7 = Vision Skeins), answered as if they were rows. A54-7.
- **The window economy.** 190 opponent-turn casting asks in this one game, 165 of them carrying
  the declined-note; the HOLD row taken 17 times and `hold_windows_skipped` **31** - the highest
  in the corpus. But every take was LATE: turn 41 declined ten identical menus and held on the
  *Cleanup* ask (s181-s191), turn 55 declined eleven and held at End (s241-s252), turns 43 and 57
  declined twelve and thirteen and never held at all (s198-s209, s256-s268). A54-0.
- s61 fetched a **Scrubland** (a dual) - the fetch rung has largely recovered, see the denominators.
**CLASSIFICATION: matchup ceiling (counters) + STRATEGY x3 (dead casts, hold timing) +
one guide-authoring defect that the parser absorbed (A54-7).**

## L2 - v126 (dead turn 19, 0 to 44): an unwinnable board, and 29 asks inside the loop that killed it
deck126 assembled **Sanguine Bond + Exquisite Blood**. Primitives: Sanguine Bond
`auto=@lifeof(player) from(*[-lifefaker]|*):life:-thatmuch opponent` (mtg.txt:99004), Exquisite
Blood `auto=@lifelostfoeof(player):life:thatmuch controller` (mtg.txt:37966) - a closed loop, and
nothing in deck123's sixty removes an enchantment.
- **s88 is NOT a misplay, and I nearly filed it as one.** 58 attackers, 6 untapped blockers,
  opponent on 28 life, `ATTACK: none`. The guide's Branch B arithmetic (A >= 3B+3 = 21) and its
  lethal override both said attack. The render said `(their life LOOP is in play: any life they
  gain or you lose in this combat chains without limit - fatal to you, not a trade)` and `Both
  halves of their life LOOP are in play`. **The render is true**: Pride Guardian gains 3 *on
  block* (mtg.txt:88356) and Perimeter Captain 2 (85398), blocking triggers resolve before combat
  damage, and one point of their gain ends the game at the pilot's zero at any width. The pilot
  obeyed the render and was right; the GUIDE was wrong. A54-5.
- **s112-s140: 29 consecutive `ask` records inside the drain**, life ticking 16/28 -> 0/44 one
  point at a time, every menu a two-row `Cast Spark Spray` / `Cast nothing right now`. The HOLD
  row was taken six times inside the loop (s117, s119, s120, s127, s129, s136) and did not stop a
  single re-ask, because each iteration moves a life total and "any change re-opens this window".
  E1 (HIGH) below.
- Five `plan_choice_conflict` fallbacks (s29, s58, s62, s90, s95), all on the repeat row, all
  triggered by the PLAN shape this guide mandates. A54-3, G2.
**CLASSIFICATION: matchup (an assembled two-card loop) + a reply-contract defect + the corpus's
largest single block of dead LLM windows.**

## L3 - v130 (dead turn 24, 0 to 20) and L4 - v152 (dead turn 9, 0 to 29)
v130: Ponza stripped the mana. Thraben Doomsayer died to a turn-6 Pyrite Spellbomb, three lands
were destroyed, and from turn 17 the seat made no play at all - 42 decisions in 24 turns, three
Cleanup discards (s37 Damnation **with a duplicate Damnation on the list - correct**, s41 a
duplicate Tragic Slip, s42 Idyllic Tutor at 2 life with nothing castable). One break: **s29
`Cast Lightning Greaves {2}` on a battlefield line reading "of which 0 are creatures"** (A54-R6),
and the eighth `named_row_reask` (s26, `CHOICE: 5 (Cast Bloodline Keeper)`).
v152: dead on turn 9 with nine decisions - Doomsayer t6, Keeper t8, both answered on curve, life
20 -> 17 -> 10 -> 0. Nothing to fix; the deck did not get a turn.
**CLASSIFICATION: matchup ceiling in both; one Greaves break.**

## The wins
- **v146 (t10, 12 to -3).** Mulliganed a one-land seven (s1), kept a three-land six under
  `would not cover any spell in it (you have 3 lands ...)` (s2), bottomed Vision Skeins (s3);
  Doomsayer t6, Alarm t8, **s12-s14 three token activations in main phase 1**, attack t8,
  Marsh Flats -> Plains t10, Greaves onto the Doomsayer at 23 creatures, **s24-s28 five more
  activations, then `ATTACK:` sixteen Humans** for -3.
- **v162 (t12, 13 to -32).** s9 fetched Underground Sea for the {U}, Alarm t6, Keeper t8,
  **s14-s17 four Vampire activations in its OWN upkeep**, s20 Tribute to Hunger on their only
  creature (Master of the Feast, +5 life), Greaves on the Keeper, s25 a lone-Keeper attack, and
  s35 `ATTACK:` the Keeper plus twelve Vampires for -32.
Both wins were the loop firing at the right stop, and both were over before the draw-punisher and
counter shells could act.

## Rungs, with denominators (deck123)
- **RULE -1 / mulligan**: 8 looks, 2 ships, 6 keeps, 2 bottoms. 8/8 agree with the header; both
  ships are `1 land + would not cover`; both bottoms correct (a duplicate Tragic Slip; a Vision
  Skeins over Damnation and two Doomsayers).
- **RULE 0 land drops**: 21 land-drop asks, a land played at 21. **PASS 21/21.**
- **RULE 1 repeat row**: repeat rows offered in 8 windows across v126; takes x2, x2, x17, x13.
  The x17 and x13 (s58/s59) were at M = 45 against a written stop of 34 - **2 takes past the
  stop**, in a game already lost to the loop. All four takes carried `L=..., C=..., stop=...`.
- **RULE 2 Marsh Flats**: 6 windows offered, **5 takes**, 4 of them a DUAL (Underground Sea x3,
  Scrubland) and **1 a basic** (`123v146` s20, Plains, against a deck with no land destruction) -
  wave 53 was 3 basics of 8. `{this land makes no mana - crack it for a land ...}` 21 renders.
- **RULE 3 Damnation**: **1 cast, and it was the dead one** (`123v125` s176, `destroys 0 ... 0 of
  yours`) - wave-53 P5 **FAILS**. `Damnation: N, K, M` written **1 of 1** (wave 53: 0 of 6) - the
  line finally shipped and did not change the decision.
- **RULE 4 Tragic Slip**: 12 `{kills ...}` cast rows offered; **1 cast**, `123v126` s44, on a
  `{kills: <name>}` row - **correct**. 0 takes on a `{kills 0 of the N ...}` row (wave 53: 1).
  **P2 PASSES.**
- **RULE 5 edicts**: N=1 windows with the mana 2, cast 2 (v162 s20, and the v125 late window at
  s310 `they control 1 creature`) - both correct. **N = 0 casts: 1** (`123v125` s211) against
  0 of 238 last corpus. N >= 2 casts: **0** (wave 53: 3) - **P9a PASSES**.
- **Cast list**: `[second copy:` rows offered 20 (Intruder Alarm), **taken 3** - **P8 FAILS**
  (wave 53: 0 of 8). Dead Idyllic Tutor casts 0 of 2 Tutors. Lightning Greaves at
  `of which 0 are creatures`: **1** (wave 53: 0) - **P6 FAILS**.
- **Cleanup discard**: 8 asks, 0 breaks - every send was a duplicate, a spare, or a hand where
  nothing was castable. **Blockers**: 4 windows, 0 breaks (v146 s8 `no blockers` into a board it
  outraced, s17 `Human blocks Goblin`, v162 s13, v126 s106 `Thraben Doomsayer blocks Vampire`).
  **Attackers**: 8 windows, 2 lethal alpha strikes, 0 attacks into a Captain/Guardian line.

---

# deck130 - Budde's Ponza. 2/6.

## L1 - v152 (dead turn 11, -3 to 29): the X menu with no marker, and the board it swept was its own
**The decisive misplay at either seat this corpus, and it is a render gap with a guide gap
behind it.**
- **s15 (turn 10, 9 life against 25).** `Cast Starstorm {r}{r}{x} {X pricing: max affordable X=4
  (6 mana total); smallest X that kills an OPPONENT creature: X=2 ...}` - taken, with
  `PLAN: Cast Starstorm at X=2 to kill Intrepid Adversary and Elite Spellbinder`.
- **s17, the X menu**, largest-first per the owner ruling:
  `1. X = 4 {kills THEIRS: Intrepid Adversary, Elite Spellbinder, Sigarda, Champion of Light;
  YOURS: Siege-Gang Commander, Goblin x3}` / `2. X = 3 {kills THEIRS: Intrepid Adversary, Elite
  Spellbinder; YOURS: ...}` / `3. X = 2 {same kills as X=3, for 1 less mana}` /
  **`4. X = 1 {kills THEIRS: none; YOURS: Goblin x3}`** / `5. X = 0`. **Answered 4**, under
  `PLAN: Kill Sigarda and Elite Spellbinder with Starstorm X=1` - a sentence the row it named
  denies in its own words. It killed its own three Goblins and nothing of theirs.
- **s18**, one attacker row left: `A1. Siege-Gang Commander (2/2) ... [their untapped blockers:
  Sigarda, Champion of Light (4/4) (your attacker dies, their blocker lives)]` - sent, and dead
  three life later.
- **Why the guide did not stop it**: STEP 1's no-marker fallback is "the row whose YOURS reads
  none", and no row on this menu reads none. **And the marker itself,
  `[<- most kills at any affordable X that costs you nothing]`, rendered ZERO times in the entire
  42-log corpus** (grepped across every `.jsonl`). D54-1, D54-4; **G6 (HIGH)** for the render.
**CLASSIFICATION: STRATEGY (a rule with no branch for the board it met) + PERCEPTION (a marker the
guide is built on that never prints).**

## L2 - v126 (dead turn 18, 0 to 44): 40 asks inside the same loop that killed deck123
The mirror of deck123's L2, from the other side of the pool. From s67 to s106, forty consecutive
`ask` records on the pilot's own Main phase 1, life 20/25 -> 0/44 one point at a time, the stack
line reading `1 (top): ability: Sanguine Bond's Life Loss [from their Sanguine Bond]`, the menu a
two-row `Cast Spark Spray` / `Cast nothing right now`. `identical_option_asks_resolved` on this
gameend: **0** - the de-dup never fired, because each row's own
`{... and 1 to the opponent at life 26 leaves them at 25}` moves every iteration. 40 model calls
for a game that was already over. E1 (HIGH).
One real rung note before the loop: **s58 took the TOP of a collapsed X run** (`X = 5` with
`X = 4 {X pricing: same kills as X=5, for 1 less mana}` directly below) - D54-2, third corpus.
**CLASSIFICATION: matchup + the engine item.**

## L3 - v146 (dead turn 33, -2 to 14) and L4 - v162 (dead turn 11, 0 to 12)
v146: a 33-turn topdeck war. Land destruction on curve to turn 12, then twenty turns at 17 life
with a 1/1 Dwarven Blastminer as the entire clock. **The three late land-destruction casts that
look like rung breaks are not**: `Your hand (1 card)` at s44, s47 and s60, and in each the card was
the only legal cast (S6 in the skill file). The one real break is **s52, a Pyrite Spellbomb
sacrificed at the opponent's face at 17 against 15 with their battlefield line showing 0
creatures** - D54-3. s36 correctly declined a Starstorm reading `even at X=6 NOTHING the OPPONENT
controls dies` and cycled it instead.
v162: Ob Nixilis + Underworld Dreams. 25 decisions, Rorix t10 for 9 damage, and **0 draws or
cycles taken under the `DRAW PUNISHERS on the battlefield` line across all 12 renders** - fourth
corpus at zero. s25 aimed the Spellbomb at Ob Nixilis, per the punisher bullet.
**CLASSIFICATION: matchup ceiling in both; one face-damage break.**

## The wins
- **v123 (t24, 20 to 0).** Spellbomb t2, Blastminer t4 and t6, a Spellbomb at the Doomsayer in
  their upkeep (s7, `takes 2 damage - DIES`), Stone Rain on the Tundra and the Scrubland (s15,
  s23), Lay Waste on an Underground Sea (s55), Blastminer activations on two more, Siege-Gang t16,
  and then **s72-s77 four Goblin sacrifices in one turn, 10 -> 2**, finished at s87. Held deck123
  to two lands and never lost a life point.
- **v125 (t20, 20 to -5).** Molten Rain t8, Blastminer on a Glacial Fortress t10, Molten Rain t16,
  Lay Waste t18, a second Talisman taken correctly (Talismans stack, mtg.txt:117529), and six
  attackers windows against a deck that had three attack windows in fifty turns last corpus.

## Rungs, with denominators (deck130)
- **#0 draw punishers**: 12 engine renders (2 of the "12" my grep found are the pilot's own PLAN
  text echoed back - the engine line rendered 10), **0 draws/cycles under it** - PASS, fourth
  corpus.
- **#1 cycling**: 13 cycles taken, **0 wrong** - every one either after the land drop, at a board
  with no opposing creature, or on a Starstorm the row itself priced as killing nothing
  (`130v146` s36/s37).
- **#2 damage order**: face casts **6**; above the 6-life floor **1** (`130v146` s52 at opponent
  15) - wave 53: 4. The `130v123` sequence (s72 at 10, s73 at 8, s76 at 6, s77 at 4, s87 at 2) is
  the licensed lethal chain and closed the game; not counted. **Lethal declined: 0** (wave 53: 3)
  - **P9 PASSES.** Takes at a `SURVIVES` row: 0 (wave 53: 1). Planeswalker windows 1, taken.
- **X menu**: **2 menus reached**; the marker row rendered **0 times** (wave 53: 2 renders, 2
  takes) -> the wave-53 P15 prediction is **UNTESTED, not PASS**. Of the two menus, **1 was
  answered on the worst live row** (`130v152` s17) and **1 took the top of a collapsed run**
  (`130v126` s58). 0 rows naming Rorix or the Commander in YOURS were taken - the s17 row named
  the Commander at X=4 and X=3, which is the rule that steered it downward into X=1.
- **Land plan**: **22 land-destruction casts**; against a line printing 5+ lands **2**
  (`130v125` s22 at 6, s30 at 5) - wave 53: 6 of 26, **P10 PASSES on the second clause**. Below
  the 8-life floor: 1 (`130v146` s60 at 6 life, sole card in hand). 0 casts at an opponent within
  the damage in hand - **P10 PASSES on the first clause** (wave 53: 2).
- **Blastminer**: 5 activations, 0 self-targets, 0 basics targeted; sent into `(neither dies)`
  tags and held back where his {2}{R} had a job. **Land drops**: 26 asks, a land played at 26.
- **Talisman life**: `{paying this costs you N life ... you would be at K}` 12 renders, 1 take;
  casts paid by a coloured pip at life <= 3: **0** (PASS, fourth corpus). `[second copy:` Talisman
  rows taken 4 - correct, Talismans stack (three separate `{T}:Add` lines, mtg.txt:117529).
- **Cleanup discard**: 1 ask (`130v162` s24), sent a spare Mountain from a hand of three - correct.
  **Blockers**: 1 window. **Attackers**: 18 windows, 1 break (`130v152` s18), 0 attacks into a
  Captain/Guardian line (`130v126` s19/s33 answered `no attackers` with Perimeter Captain up -
  correct).
- **Race check**: 3 lone-Rorix windows, sent 3/3, no flying blocker on any of them - **no race
  arose; UNTESTED, sixth corpus.**

---

# ROTATION EVIDENCE (both seats, all six opponents)
| matchup | deck123 result | what decided it | deck130 result | what decided it |
|---|---|---|---|---|
| 123 vs 130 | L t24 0/20 | mana stripped; no play after t17 | **W** t24 20/0 | LD on curve + a four-Goblin burn turn |
| vs 125 | L t64 0/49 | counters; five cards spent off dead rows | **W** t20 20/-5 | Molten Rain / Lay Waste on curve, six attack windows |
| vs 126 | L t19 0/44 | Sanguine Bond + Exquisite Blood, unwinnable from t15 | L t18 0/44 | the same loop, from the other side |
| vs 146 | **W** t10 12/-3 | mull to six, Doomsayer + Alarm, 16 Humans | L t33 -2/14 | a 1/1 clock for twenty turns off a one-card hand |
| vs 152 | L t9 0/29 | dead on turn 9; the deck never got a turn | L t11 -3/29 | **the X=1 that swept its own board** |
| vs 162 | **W** t12 13/-32 | Keeper upkeep chain, Tribute, 13 attackers | L t11 0/12 | Ob Nixilis + Underworld Dreams outraced Rorix |
Both decks are 2-4 for the second corpus running. The two decks share ONE opponent they both lose
to for the same structural reason (deck126's assembled life loop, which neither sixty can remove);
otherwise deck123 beats what it can chain against before turn 12, and deck130 beats what it can
strip before turn 10. Games are far shorter than wave 53 at eleven of twelve seats - the brief's
instruction not to read that as a model change applies here.

---

# PREDICTION ADJUDICATION

## Set A - the wave-53 guide edits, at these seats
| edit / prediction | verdict | evidence |
|---|---|---|
| **P1** A53-1: 0 basics fetched while a dual is listed and the opponent has no LD | **FAIL 1 of 5** (wave 53: 3 of 8) - and 0 fetches with {U} missing from the mana line | `123v146` s20 (Plains) |
| **P2** A53-2: 0 Tragic Slip takes on a `{kills 0 of the N ...}` row; `{kills: <name>}` takes N/N | **PASS 0, and 1/1** | `123v126` s44 |
| **P3** A53-3: 0 replies whose PLAN asserts a fact the same window's `{right now: ...}` denies | **FAIL 2** - `PLAN: Damnation: N 0, K 0, M 0; cast.` and `Kill Sigarda ... with Starstorm X=1` | `123v125` s176; `130v152` s17 |
| **P4** A53-4 (HOLD): >= 1 take per game with a repeated dead menu; runs of 3+ below 25 % | **SPLIT.** Takes: 32 of 275, and non-zero in every game that had a repeated dead menu (v125 17, v126 14, v130 1) - **PASS**. Runs of 3+: **43.7 %** at deck123 - **FAIL**, and worse than wave 53's 39.7 %. The lever is the row/engine (E1, E2), as P4 itself said | `123v125` s181-s191, s198-s209 |
| **P5** A53-5: 0 Damnations cast on a `destroys 0` row in any phase | **FAIL 1 of 1 Damnation cast** | `123v125` s176 |
| **P6** A53-6: on a non-lethal blockers window while behind on life, a token blocks the largest attacker | **UNTESTED** - 4 blockers windows, none printed the non-lethal hint while behind | - |
| **P7** A53 (third corpus): `Damnation: N, K, M` written N/N | **PASS 1/1 on the text, and the rung is DEAD anyway** - the line shipped and the dead cast happened on the same line. Wave 53 said this is the point the ask becomes a render item: G1 | `123v125` s176 |
| **P8** 0 `[second copy:` takes on Intruder Alarm | **FAIL 3 of 20 rows** (wave 53: 0 of 8) | `123v125` s147, s192, s269 |
| **P9a** A53-9: 0 edicts on a `they control N creatures - they choose which one` row | **PASS 0** (wave 53: 3) | - |
| **P9** D53-1/2: 0 windows declining a row that is lethal to a player | **PASS 0** (wave 53: 3 in one game) - the prediction the last seat file cared about most | - |
| **P10** D53-3: 0 LD casts with the opponent inside the damage in hand; 5+-land casts below 4 of 26 | **PASS on both** - 0, and 2 of 22 | `130v125` s22, s30 |
| **P11** D53-4: HOLD takes >= 1 in every deck130 game with an opponent-turn dead-menu run | **UNTESTED** - deck130 had **9** opponent-turn casting asks all corpus (wave 53: 72); no such run arose. 2 takes, `hold_windows_skipped` 16 in `130v123` | - |
| **P12** face damage at opponent life >= 7 outside a lethal chain: 0 | **FAIL 1** (wave 53: 4, wave 52: 6) - trend is real, the rung is nearly closed | `130v146` s52 |
| **P13** (engine-conditional) lethal tail on cast rows -> `kills 0` face rows taken N/N | **UNTESTED** - the tail ships (`- and 1 to the opponent at life 20 leaves them at 19`, `130v130`-family rows) but no window put a lethal face row in front of this seat | - |
| **P14** 0 cycles under DRAW PUNISHERS, fourth corpus | **PASS 0 of 12** | - |
| **P15** the X-menu marker row taken N/N | **UNTESTED, and the reason is the finding** - **0 marker renders in the whole corpus**. Scored PASS twice on a surface that has stopped printing (skill S5) | `130v152` s17, `130v126` s58 |
| **boundary B8 part 1** (deck130 `would not cover` keep at 2+ lands) | **UNTESTED, fourth corpus** - 0 renders, 0 ships, 0 bottoms. #132 stands | - |
| **boundary B8 part 2** (deck123's completed hands) | **PARTIALLY TESTED, and it PASSES** - the `would not cover` branch rendered at deck123 for the first time at 1 land (shipped, twice) and at 3 lands (kept, and won) | `123v126` s1/s2, `123v146` s1/s2 |

## Set B - the wave-54 engine lanes, by the emitter's actual string at these seats
| lane | verdict |
|---|---|
| **A** (D2 a-d, D12) | **PASS on the shape, SPLIT on the economy.** Row text is exactly `Pass priority, and do not ask me again this turn unless the board changes (any change re-opens this window; you give up no cast)`: **286 renders, 0 of the old string, 0 on the pilot's own turn**. HOLD takes **34 (11.9 %)** vs 4 (1.2 %). `plan_choice_conflict` on a HOLD take: **0**. `hold_windows_skipped > 0` on gameends where the row was taken: **3 of 4** - the miss is `123v130` s17, a Cleanup-step take with no window left in the turn to skip, which I read as vacuous rather than a defect. Takes in declined-note `N >= 3` windows: **20 of 165 = 12.1 %**, target was >= 20 % - **FAIL**. Identical-declined runs at deck123 **43.7 %**, target < 15 % - **FAIL at this seat** (the corpus figure of 12.2 % is carried by the short games). Opponent-turn casts: **2** at my seats (wave 53: 5 in 340 offers). Served PLAN blocks longer than one paragraph: **0** (the wave-53 H3 unbounded-capture defect is **CLOSED**); stale-plan decisions with no `"<card>" is no longer on your menu` clause: **0** (the clause renders, e.g. `130v123` s6). `pass_hold_ambiguous` **4 at my seats, 4 FALSE POSITIVES** - every one is `CHOICE: 0 (pass)` executing row 0 correctly (`123v126` s72, s74, s101, s110), and after the reword `(pass)` heads both rows. Confirms the brief's read; docket it. |
| **B** (D13/D14/D15/D9) | **PASS, and it saved eight windows from a wrong cast.** `latched_row_mismatch` fired **once at my seats** (`123v125` s147): the first coded line `CHOICE: 3 (Cast Intruder Alarm)` executed row 3, and a second coded line `CHOICE: 2 (Cast Bloodline Keeper)` differed in BOTH index and parenthetical - the stamp's exact predicate, and **not** a legitimate name-over-index remap. **PASS.** The name-over-index machinery itself is the lane's real result: 8 replies naming a card that was not on the menu (A54-7's table) produced **0 wrong casts** - three `stale_echo_in_range` refusals of an in-range index whose name was stale, five `named_row_not_offered`, five recovered on the re-ask and three exhausted to `choice: -1` + fallback. Replies naming a card that occurs only inside an annotation, executing a row: **0**. `plan_contradicts_noop_row`: **0 renders** at my seats. Answered replies at >= 95 % of the deadline without `long_reply`: **0**. |
| **C** (D4/D10/D11/D18/D5) | **MIXED - see the three-number audit.** D10's `[DRAW PRICE:` tail: **8 rows, 8 with `- you would be at K`** (wave 53: 0 of 58) - **PASS N/N**; 0 casts from a DRAW PRICE row; `this KILLS you` 0 renders (no window reached damage >= life). D4/D11 `{kills: <name>}` on unconditional removal: **62 rows** (deck123 13, deck130 49), 12 takes, 0 on an indestructible target - **PASS**, with a new sibling defect (G8: the single-target land list names the pilot's own Mountains under the word "kills" and marks no ownership, `130v146` s23 vs its own target menu at s24). Rows enumerating a player whose `{kills ...}` omits it: **0** at my seats (2 renders of `(player, life N)` inside a summary, both correct). D18 fit clause `no other row on this menu needs more than N`: **177 renders, 23 takes**, 0 priced multi-row menus without it - **PASS**. D5 modal `{modes live right now: ...}`: **0 renders - UNTESTED** (no modal card at either seat). |
| **D** (D3/D6/D8/D25) | **UNTESTED, almost entirely.** `CHOOSE A CARD TO PUT ONTO THE BATTLEFIELD` / `Pick the card you MOST NEED` / `your own effect` / `FORCED SACRIFICE`: **0 renders each** - neither deck met an annihilator, a forced-sacrifice mega-menu or a battlefield-destination search this corpus (wave 53's M5 specimen was a deck130 game; no Emrakul arrived). D8(b) de-dup: `identical_option_asks_resolved` **1 across my twelve gameends** (`130v125`), and **0 on the two gameends that needed it most** - the 40-ask and 29-ask drain loops (E1). Renders repeating one ordinal-free row more than twice: the drain loops do exactly that, 40 and 29 times, and are not caught because a life number inside the annotation moves. |
| **E** (D21/D17/D16/D20/D19) | **PASS on every string I could reach.** D21: `while ahead on LIFE` **0 renders** - the corpus's only render FALSEHOOD is gone from my seats. D17: **26 of 26** attackers prompts carry `They have N untapped creature(s) able to block; declaring more than N attackers leaves at least (your attackers - N) of them unblocked` (I first miscounted 3 misses; the singular form drops the "s"). D19: `ability: X's X` name-twice stack lines **0** across 142 prompts carrying an `ability:` stack line; all use the possessive-and-source form, e.g. `ability: Sanguine Bond's Life Loss [from their Sanguine Bond]`. D16: `would not cover any spell` renders **4**, all with the cause clause (`no spell in it is castable off {W}{U} alone`, `... off colourless mana alone`, `... off {W}{B} alone`) - **PASS 4/4**. D20 colourless fetch rows: **0** except lands - the fetch rows still print no colour at all (G4), which is the older M2 item rather than a D20 break. |
| **F** (D7 a-c, D38) | **PASS on what arose.** Records with `choice: -1` and no `fallback`: **0** (8 such records at deck123, all carrying `named_row_reask` or `named_row_not_offered`). 0 hangs, 0 timeouts, the no-progress supervisor never fired at my games. Reveal force-close, `assertmtgid`, the transformed-card `getCard` fix: **UNTESTED** - 2 reveal asks at my seats, both resolved normally. |
| **AA** (Vita softlock / AI-window stall floor) | **FIRED, REPEATEDLY, ON GPT SEATS - see E1.** The corpus prediction was "0 such declines on GPT seats". My eleven stderr files carry **33, 30, 44, 64 ...** `interrupt window held by Player N ... for 12002 ticks with no progress - releasing so the game advances` lines. The threshold reached is the tick floor, not the 1,200 s wall, so this is not the strict FAIL the carry defines - but it is not 0 either, and it is the mechanism behind the corpus's `dropping stale async answer` spike. |
| **G-N (audit lanes)** | **No behavioural drift I can attribute, with one exception (E1) and one caveat.** G/H/I/J: 0 crashes, 0 dead-token signatures, no verdict drift I can see from the logs (I did not read RSS or ASAN). K: not exercised - 0 Baka fallback blocks at my seats. L: prompt shapes are wave-53-identical (`- Paid` receipts render, `{leaves N of your M untapped mana sources untapped}` on 177 rows, 0 `http_error` records, longest non-log segment well under the trim). M: the ask cache is visible in stderr as one `-> chose N of M` followed by up to five `chose to cast nothing` replays, and **that replay is exactly where the stalled window appears** - see E1 for the flag I would flip first. N: Vita only, not observable here. |

## THREE-NUMBER LITERAL AUDIT (#188) - counted from `options_text` (renders) and `chosen_text` (takes) for row literals, and from `prompt` for header/hint literals; never from the guide text inside the prompt
| literal (lanes A-E) | old-string renders | new-string renders | takes under the new string |
|---|---|---|---|
| HOLD row: `Hold priority - do not ask me again this turn unless the board changes` -> `Pass priority, and do not ask me again this turn unless the board changes (any change re-opens this window; you give up no cast)` | **0** | **286** (deck123 275, deck130 11); 0 on the pilot's own turn | **34** (deck123 32, deck130 2) |
| `[DRAW PRICE: ... ]` + `- you would be at K` | 7 rows carried the tag last shape | **8 rows, 8 with the tail**, 0 with `this KILLS you` | **0 of 8** |
| `{kills: <name>}` on unconditional removal | 0 verdict-free removal rows | **62** (deck123 13, deck130 49) | **12** (deck130 11, deck123 1) |
| `{kills N of the M legal targets ...}` | 0 | **12** (deck123) | **0** |
| `[second copy: ... but its effect is already on the battlefield and a second copy changes nothing]` | 0 of the wave-53 short form on Intruder Alarm | **20** Alarm rows in the new form; Talisman rows keep the plain `no legend rule` form (24) | **3 Alarm (all wrong), 4 Talisman (all correct - it stacks)** |
| fit clause `{leaves N sources - no other row on this menu needs more than N}` | n/a | **177** (deck123 167, deck130 10) | **23** |
| `They have N untapped creature(s) able to block ...` | n/a | **26 of 26** attackers prompts | n/a (header) |
| `while ahead on LIFE` | **0** | **0** | - (**the falsehood is gone**) |
| `ability: <source>'s <effect> ... [from your/their <source>]` | **0** bare, **0** name-twice | **142** prompts | n/a |
| `would not cover any spell in it (you have N land(s); no spell in it is castable off ... alone)` | n/a | **4**, all with the cause clause | 2 ships, 1 keep-and-win, 1 keep |
| `{modes live right now: ...; Dead: ...}` | 0 | **0** | - (**UNTESTED**, no modal card) |
| `CHOOSE A CARD TO PUT ONTO THE BATTLEFIELD ... Pick the card you MOST NEED` | 0 | **0** | - (**UNTESTED**) |
| `FORCED SACRIFICE` / `your own effect` | 0 | **0** / **0** | - (**UNTESTED**, no annihilator) |
| `[<- most kills at any affordable X that costs you nothing]` | n/a | **0 in the entire 42-log corpus** | - (**UNTESTED**, and that is the finding: G6) |
| `[you declined this exact list N times already this turn]` | n/a | **287** prompts (deck123 240, deck130 47) | 1 decline -> cast flip after it (`123v125` s310, and it was correct) |

---

# ENGINE / RENDER ITEMS (ranked)

**E1 (HIGH, engine window loop). An unbounded mandatory life-loss loop opens one LLM window per
iteration, and the de-dup guard cannot see it.**
Repro: **`130v126` seq 67-106 (40 consecutive asks)** and **`123v126` seq 112-140 (29
consecutive asks)** - the two sides of the same game-ending Sanguine Bond + Exquisite Blood loop.
Every ask is the pilot's own phase, the stack line reads `1 (top): ability: Sanguine Bond's Life
Loss [from their Sanguine Bond]`, the menu is two rows, and life moves one point per record until
0. `identical_option_asks_resolved` on both gameends: **0**. The reason the D8(b) guard misses it
is inside the row: `Cast Spark Spray ... {no creature target - and 1 to the opponent at life 26
leaves them at 25}` - lane C's own pricing annotation makes every iteration a byte-different
string. **69 dead model calls at my seats, 7.7 % of my entire decision count.**
**Ask:** make the de-dup key the option SET (row names + costs + ordinals), not the rendered
string, so numeric annotations that only track a moving life total do not defeat it; and cap
re-opens within one phase when the only board change is a life total. Same key fixes the HOLD
row's contract in a drain (the row was taken six times inside `123v126`'s loop and stopped
nothing, because "any change re-opens this window" is literally satisfied by the life tick).

**E2 (HIGH, engine window lifecycle). The 488 `dropping stale async answer` lines are the AA stall
floor releasing an interrupt window the GPT seat never closed - one drop per release, and the
brief's "each drop is a wasted model call" is REFUTED.**
Mechanism, pinned from stderr + the seat logs. The stderr pattern is invariant:
```
AIPlayerGPT: Casting decision (Main phase 1, YOUR turn): ... -> chose 4 of 4
AIPlayerGPT: chose to cast nothing          <- x5, the cached answer replayed
Action added to stack: NextGamePhase.  (Current phase is: Main phase 1)
AIPlayerGPT: dropping stale async answer
ActionStack: interrupt window held by Player 2 on 'NextGamePhase. (Current phase is: Main phase 1)' for 12002 ticks with no progress - releasing
wagic: ... for 12002 ticks (turn 10 phase 4) - releasing so the game advances
```
(`game-123v130-1788425064.stderr`, the block at turn 10 phases 4/5/6/9/10/11/12.)
**Drops equal releases, game by game**: 126v123 **64 / 64**, 123v130 37 / 33, 126v130 32 / 30,
125v123 45 / 44, 146v130 2 / 2, **152v130 0 / 0**. And the drops do not buy model calls: 123v130
has 37 drops in a seat log of 41 decisions across 24 turns. What the seat is paying is a
**12,000-tick spin at every phase in which the seat's answer was served from cache rather than
by closing the window** - the `chose to cast nothing` replay count tracks the drop count exactly
(126v123: 172 replays / 64 drops; 152v130: 0 / 0).
**Which flag to flip first: `WAGIC_GPT_AUDIT_M_OFF=1` (lane M, ask cache / situation prefill).**
The replay is lane M's cache serving an answer without the window being marked progressed. If the
drops persist with M off, flip lane A's HOLD keys next (`123v125` and `123v126` are the two games
with both the most HOLD takes and the most drops); lane AA's floor is the RESCUER here, not the
cause - it is the only reason these games finish - so it should be flipped last and only to
confirm that the spin is what the floor is absorbing.

**E3 (HIGH, render). The X menu's marker never printed, and the unmarked menu lost a game.**
Repro: `130v152` s17 (full menu quoted in deck130 L1 above); `130v126` s58 for the collapsed-run
half. `[<- most kills at any affordable X that costs you nothing]`: **0 renders in all 42 logs**.
Asks in `wave54/deck130/general-suggestions.md` G6 and G7: always mark exactly one row, and put
the mark on the BOTTOM row of a collapsed run.

**E4 (MED, parser). `plan_choice_conflict` fires on the PLAN template the pool's guides mandate,
and four of seven re-asks returned the identical reply.**
Repro: `123v126` s29 -> s30 (`CHOICE: 2 (... x2)` + `PLAN: ... (stop reached)`, re-asked,
identical, executed), s58 -> s59, `123v146` s23 -> s24, s26 -> s27. One true positive: s90 -> s91,
where the re-ask corrected a take into the pass the plan wanted. Ask in `deck123/general-
suggestions.md` G2: fire only when the prose NAMES a different row. Guide half shipped as A54-3.

**E5 (MED, render). `pass_hold_ambiguous` is a false positive at 4 of 4 firings here.**
Repro: `123v126` s72 - menu is `1. Create human with Thraben Doomsayer` / `2. Pass priority, and
do not ask me again this turn ...`, reply `CHOICE: 0 (pass)`, executed row 0, correct. Also s74,
s101, s110. After lane A's reword `(pass)` heads both the implicit pass row and the HOLD row, so
the stamp cannot discriminate. Confirms the brief's corpus-wide read (16/16).

**E6 (MED, render). A single-target `{kills:}` list names the caster's own permanents with no
ownership mark.** Repro: `130v146` s23, `Cast Stone Rain {2}{r} {kills: Plains #1, Plains #2,
Mountain #1, Mountain #2, Mountain #3, Mountain #4, Mountain #5}` - five of the seven are the
pilot's own Mountains. The target menu one screen later (s24) marks every row
`[your battlefield]` / `[opponent's battlefield]`. Ask: G8.

**E7 (MED, render). Fetch rows name a land and never its colours.** Carried unchanged from
wave-53 M2. Repro: `123v130` s8, `123v146` s20 - four and five `Put a card onto the battlefield
with Marsh Flats targeting <land>` rows, `{this land makes no mana - crack it for a land: your
untapped mana sources go from N to N+1}` on each, no colour anywhere. 21 renders at this seat.
Behaviour improved without it (4 of 5 fetches took a dual), so this is now correctness, not
bleeding. Ask: G4.

**E8 (LOW, render). Library-reveal menus still do not collapse duplicates.** `123v125` s9 lists
**50** rows with `Intruder Alarm` x4, `Damnation` x4, `Thraben Doomsayer` x4 as separate identical
strings; `123v126` s23 lists 45 the same way. The battlefield line already collapses with `xN`
handles. Ask: G3.

**E9 (LOW, render). Ability rows that damage a player print no life arithmetic.** `130v162` s25,
`Deal 2 damage with Pyrite Spellbomb targeting Ob Nixilis` on a screen whose player row reads
`the opponent (player, life 12)`. Cast rows have carried the tail since lane C
(`130v126` s66: `- and 1 to the opponent at life 24 leaves them at 23`); ability rows have not.
Ask: G9.

**Closed from wave 53:** H2 (the cast row's kill summary excluding player targets) - **CLOSED**:
`- and N to the opponent at life A leaves them at B` now renders on damage rows, and lethal
declines went 3 -> 0. H3 (the PLAN capture swallowing the reply tail) - **CLOSED**: 0 served PLAN
blocks longer than one paragraph across 896 decisions. M3 (the DRAW PRICE tail) - **CLOSED**,
8 of 8. M4 (`while ahead on LIFE` at `my_life <= opp_life`) - **CLOSED**, 0 renders. H1's false
negative half (`answer_replaced` catching a real prose reversal) - **still working**:
`123v125` s39, `CHOICE: 1 (Cast Damnation)` whose prose reversed itself, replaced and executed as
`Cast nothing right now`.

**Card facts checked** against `projects/mtg/bin/Res/sets/primitives/mtg.txt` with `/usr/bin/grep`:
Sanguine Bond (99004, `@lifeof(player) ... life:-thatmuch opponent`, Enchantment), Exquisite Blood
(37966, `@lifelostfoeof(player):life:thatmuch controller`, Enchantment), Perimeter Captain (85398,
0/4 defender, `@combat(blocking) ... may life:2 controller`), Pride Guardian (88356, 0/3 defender,
`life:3 controller` on its own block), Thraben Doomsayer (120583), Intruder Alarm (58850, `lord
(creature) doesnotuntap` + two `@movedTo ... untap all`), Damnation, Tragic Slip, Tribute to
Hunger, Devour Flesh, Idyllic Tutor, Vision Skeins, Lightning Greaves, Bloodline Keeper,
Marsh Flats / Tundra / Underground Sea / Scrubland (as wave 53), Starstorm (112835,
`damage:X all(creature)` - creatures only), Siege-Gang Commander (105071), Spark Spray (110142),
Lay Waste (66043, `target=land auto=destroy`), Stone Rain (113726), Molten Rain (75759,
`teach(land[-basic]) damage:2 targetcontroller` + `destroy`), Hammer of Bogardan (52046),
Pyrite Spellbomb (90168), Dwarven Blastminer (33251), Talisman of Impulse (117529).
**No primitive/Oracle divergence found; no Scryfall fetch was needed** - every fact I relied on is
a P/T, a subtype, a trigger condition or a script line, and the render agreed with the primitive
in every case. The one place it mattered most was the Sanguine Bond + Exquisite Blood timing: the
primitives and the trigger order say the render is TRUE, which turned a candidate HIGH render
falsehood into a guide edit (A54-5).

---

# LATEGAME SPECIMEN CANDIDATE (invariant 0)
1. **`123v125` seq 302-310** (turn 63, the corpus's deepest turn at my seats; prompts 22.0-23.8 KB,
   the largest here). Nine consecutive records across Upkeep x5, Main phase 1 x2, Attackers and
   Combat ends, on a 44-permanent game log, exercising at once: the repeated-Upkeep window shape
   (five asks in one phase step), the HOLD row taken mid-run at s306 with
   `[you declined this exact list 4 times already this turn]` on the prompt, the declined-note
   counter resetting across the phase boundary, and finally **s310, a live
   `Cast Tribute to Hunger {2}{b} {right now: they control 1 creature - ...}` taken on the
   opponent's Combat ends step after 300 decisions** - the correct edict, at the end of a game
   already lost. It is a better late specimen than the corpus baseline (`125v123` s188) for the
   window-economy lanes specifically, because it contains a HOLD take, a same-phase repeat run and
   a correct opponent-turn cast in nine records.
2. Alternate: **`130v126` seq 100-106** - seven records of the drain loop at 3/41 down to 1/44,
   two-row menus, the Sanguine Bond stack line, and the annotation that defeats the de-dup guard,
   in a form small enough to diff.

---

# FALSIFIABLE PREDICTIONS FOR WAVE 55

**deck123**
- **P1.** With A54-7 (cast list relabelled CL0-CL8): `named_row_reask` + `named_row_not_offered`
  at this seat 8 -> 0, and replies naming Bloodline Keeper or Vision Skeins off a menu that does
  not list them 8 -> 0. If this does not move, the guide is not the source and the engine ask is
  to echo the menu's own numbering in the reply-format line.
- **P2.** With A54-1: casts off a menu where EVERY cast row carries a dead verdict, 5 -> 0. With
  G1 shipped as well, 0 with a margin; without G1, I expect 1-2 and the rung then belongs to the
  render (wave-53 P7's exit test, made two-sided in skill S3).
- **P3.** With A54-2: `[second copy:` takes on Intruder Alarm 3 -> 0 (20 rows offered).
- **P4.** With A54-3 and G2: `plan_choice_conflict` firings whose re-ask returns the identical
  reply, 4 -> 0. Total `plan_choice_conflict` at this seat 5 -> at most 1 (the s90 shape, which
  should keep firing and keep correcting).
- **P5.** With A54-0: the declined-note N at the FIRST HOLD take of a turn falls below 3 in at
  least half the turns that offer the row (this corpus: 0 of the four multi-window turns in
  `123v125` held before N = 4). Identical-declined runs of 3+ at this seat below 30 % (43.7 %) -
  but only if E1 ships; without it, expect no movement and read that as confirmation that the
  remaining runs are engine-generated, not decided.
- **P6.** With A54-5 (Branch B2): 0 attacks declared while BOTH Sanguine Bond and Exquisite Blood
  are on the opponent's line and any untapped creature they control can gain them life
  (this corpus: 0 already - the prediction is that it stays 0 for the RIGHT reason, and that the
  guide no longer prints an arithmetic that says otherwise).
- **P7.** With A54-6: edict casts on a `they control 0 creatures - at 0 this does nothing` row,
  1 -> 0. With A54-R6: Greaves casts at `of which 0 are creatures`, 1 -> 0.
- **P8 (engine-conditional, E1).** If the de-dup key becomes the option set: decisions inside a
  mandatory life-loss loop at this seat, 29 -> under 5 per loop.

**deck130**
- **P9.** With D54-1: takes of an X row reading `kills THEIRS: none` while another affordable row
  names an opponent creature, 1 -> 0. **This is the prediction I care about most at this seat** -
  it is the game deck130 lost.
- **P10.** With D54-2: takes of the TOP row of a collapsed `same kills as X=N` run, 1 -> 0
  (three corpora at non-zero; if it breaks a fourth time the rung is exhausted and G7 is the
  remaining lever).
- **P11 (engine-conditional, E3/G6).** If the X menu always marks one row: marker renders on
  X menus, 0 of 2 -> N/N, and the marked row taken N/N. Until then P15's "taken 2/2" from wave 53
  must be re-read as 0 renders, not a pass.
- **P12.** With D54-3: face damage at `Opponent life:` 7 or more outside a lethal chain, 1 -> 0.
  Four corpora of 6, 4, 1 - if this reaches 0 the rung can be compressed to one sentence in
  wave 56.
- **P13.** With D54-4: attackers answers sending a lone body whose own tag reads
  `(your attacker dies, their blocker lives)`, 1 -> 0.
- **P14.** `cycling` / draw takes while the `DRAW PUNISHERS` line is on screen: 0, **fifth
  corpus** (0 of 12). If this holds again the rung can be compressed to the line plus its two
  thresholds.
- **P15.** Land destruction against a line printing 5+ lands stays below 3 of 22 (this corpus: 2;
  wave 53: 6 of 26), and 0 casts at an opponent already inside the damage in hand (this corpus:
  0). Both are close to closed; report them with the hand-line denominator (skill S6).

---

# WHAT I DID NOT CHECK
- **The other five seat files, the corpus-wide totals, and the ten games neither of my decks is
  in.** Every count here is over my own twelve seat logs and the eleven stderr files that belong
  to them. The brief's corpus figures (2,212 decisions, 23 fallbacks, 426 HOLD renders / 43 takes,
  15,170 receipts, 488 stale drops, max prompt 32,215) are quoted, not recomputed; my 896
  decisions, 17 fallbacks, 286 HOLD renders / 34 takes and 281 stale drops (over my eleven games) are this seat pair's
  share. Where I say "0 renders in the entire 42-log corpus" (the X-menu marker) I did grep all 42
  `.jsonl` files for that one string and nothing else.
- **The engine source.** Nothing under `src/`, `JGE/` or `bin/Res` was read or written except
  `Res/sets/primitives/mtg.txt` and the two guides I own. Every E-item is stated from emitted
  strings, JSONL fields, stderr lines and the lane notes; where I say a clause "does not print" I
  mean it appears in no row or prompt at my seats, not that I read the emitter. In particular
  E2 names the flag I would flip first from the log pattern; I did not run wagic and cannot A/B it.
- **stderr beyond the drop/release/replay counts.** I counted `dropping stale async answer`,
  `releasing so the game advances`, `chose to cast nothing`, `-> chose N of M` and read two
  ~60-line windows around drops in two files. No invariant line, no memory counter, no ASAN
  output, no `frames`/`avg_swp` line was examined - so the G-N audit-lane verdicts above are
  "no drift I can see in the logs", not a clean bill.
- **RSS, render caches, Vita parity** (lanes I, J, N) - not observable from seat logs at all.
- **Scryfall.** No web fetch. Every card fact I relied on is a P/T, a subtype, a trigger condition
  or a script line, and the primitive was sufficient.
- **The suite, PARSETEST, and any build.** No wagic run, no build, no deploy, no git.
- **Lanes K (Baka `selectAbility`) and the owner batches** - 0 Baka fallback blocks and 0
  heuristic handoffs at my seats; the corpus's one `giving this decision to the heuristic` is not
  in my twelve logs. Reported UNTESTED, not inferred.
