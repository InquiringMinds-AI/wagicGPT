# Wave-54 seat: deck125 (1/6) + deck126 (4/6)

Corpus `matchups-20260903-034355` - ELEVENTH fair-hand corpus, first on wave-54 step-1 (engine
lanes A-F, audit lanes G-N, wave-53 lane AA). Binary master **caf023dbc**, model qwen36-35b-a3b on
Spark :8084. Guides = wave-53 reviewer revisions (baf36b936) + boundary pass (a0067bae5).
21/21 natural; D1 discharged.

**Volume.** deck125 **405** decisions over 6 seat logs, deck126 **281** over 6 - **686 of the
corpus's 2,212 (31%)**. Wave 53 was deck125 1,075 / deck126 267. deck125's decision count fell
**62%** while its turn count fell only 35% (261 -> 169). That collapse is not a model change and is
not the audit lanes: it is HIGH #1 below, and it hits the draw-go seat hardest because the windows
it lost are opponent-turn priority windows. Largest prompt on my seats **27,183** (`126v146` seq 55,
t30, `reveal`); longest seat log `125v123` (189 decisions, 63 turns).

**Results.** deck125 **1W-5L** (won vs123 t63 49/0; lost vs152 t18 -11/29, vs162 t19 0/22, vs146
t18 -6/22, vs126 t28 0/41, vs130 t19 -5/20). deck126 **4W-2L** (won vs162 t14 38/0, vs125 t27 41/0,
vs130 t17 44/0, vs123 t18 44/0; lost vs152 t14 -10/20, vs146 t32 -5/5). Wave 53 was 4-1 / 2-4.
Real hands, n=6 per deck, and HIGH #1 changed the number of windows each seat got: **the swing is
not attributed to guide edits in either direction.**

**Health (12 seat logs).** Fallbacks **2 / 686 (0.29%)** - `plan_choice_conflict` at `126v125`
seq 19 and `named_row_reask` at `126v146` seq 37, the latter followed by exactly one
`kind: "recovery"` record (seq 38) carrying `named_row_reask_recovered`; the former is the corpus's
only `plan_choice_conflict` on my seats. Parse notes: `pass_hold_ambiguous` **4** (`125v162` 2,
`125v146` 2 - every one a plain `CHOICE: 0 (pass)` executing row 0 correctly, i.e. the corpus's
false-positive class, confirmed at this seat), `decision_reversed_in_prose` 2 (both `126v125`, one
with `plan_choice_conflict_exhausted`), `multi_answer_first_taken` 0, `echo_index_conflict` **0**
(wave-53 MED #5 did not recur), `stale_echo_in_range` 1, `multiblock_first_wins` 1,
`blocks_last_line_taken` 1, `latched_row_mismatch` **0**. `- Paid` receipts read off `events`:
**122 / 0 no-source**, 3 `from mana already floating`. `commit_retracted` / `answer_replaced` /
`empty_reply` / `timeout` **0**. Latency: deck125 p50 2.3-24.7 s, max 172.7 s; deck126 p50
7.0-42.1 s, max **495.7 s** (`126v152` seq 1, the mulligan) - no reply reached 95% of the 900 s
wall, so lane B's `long_reply` stamp is UNTESTED-AT-THIS-SEAT. Wave-53 LOW #8 (600-869 s replies)
did not recur.

**Card facts.** Every fact below is read off `projects/mtg/bin/Res/sets/primitives/*.txt` with
`/usr/bin/grep`. Howling Mine `mtg.txt:55935` `auto=@each my draw sourcenottap:draw:1 controller` +
`auto=@each opponent draw sourcenottap:draw:1 opponent`, `type=Artifact`. Teferi's Puzzle Box
`mtg.txt:118332` `auto=@each my draw:name(recycle draw) count(type:*:myhand) && bottomoflibrary
all(*|myhand) && draw:countedamount controller` (+ the opponent mirror), `type=Artifact`.
Underworld Dreams `mtg.txt:125822` `auto=@drawfoeof(player):damage:1 opponent`. Ob Nixilis, the
Hate-Twisted `planeswalkers.txt:2684` `auto=@drawfoeof(player):damage:1 opponent`. Fate Unraveler
`mtg.txt:39182` same trigger, 3/4 Enchantment Creature. Forced Fruition `mtg.txt:42754` - **not in
any prompt this corpus**. Dream Fracture `mtg.txt:31833` `target=*|stack` / `auto=fizzle` /
`auto=draw:1 controller` / `auto=draw:1 targetController`. **Silverquill Silencer
`borderline.txt:102764`**: `auto=chooseanameopp transforms((,newability[@movedto(*[chosenname]|
opponentstack):life:-3 opponent && draw:1 controller])) forever chooseend nonland`,
`text=As Silverquill Silencer enters, choose a nonland card name. -- Whenever an opponent casts a
spell with the chosen name, they lose 3 life and you draw a card.` (this is deck126's new finding,
STRATEGY #2). Sanguine Bond `mtg.txt:99004`, Exquisite Blood `mtg.txt:37966`, Perimeter Captain
`mtg.txt:85398`, Pride Guardian `mtg.txt:88356`, Overgrown Battlement `mtg.txt:83596` (no life
clause) re-read unchanged from wave 53. **Scryfall not consulted; no primitive/Scryfall
disagreement is reported either way.**

## NEW-BASELINE record (mulligans)

| seat | offers | mulls taken | kept-hand shapes (the engine's own counts) | bottoming |
|---|---|---|---|---|
| deck125 | 6 | **0** | 4L/3S x2, 5L/2S x2, 2L/5S (`vs146`, coverage names Essence Scatter), **2L/5S with `would not cover any spell in it` and the cause clause `2 lands are not enough for your cheapest spell at mana value 3`** (`vs126` seq 1, kept, LOST t28) | none |
| deck126 | 6 | **1** | mulled a **1L/6S** seven whose coverage read `would not cover any spell in it (you have 1 land; no spell in it is castable off {B}{G} alone)`; kept the 3L/4S replacement; other keeps 3L/4S x2, 4L/3S, 2L/5S, and one whose count line did not render | **1 `bottom` ask** (`126v162` seq 3, bottomed Chromatic Lantern from a 3L/4S seven holding 2 Idyllic Tutors) |

Twelve offers, **one mulligan, one bottom ask, no chain, no mull-to-zero, no floor text.** The
deck126 mulligan is the corpus's cleanest single decision at my seats: the guide's odds-form
one-land branch fired on exactly the hand it was written for, the reply names the cause the render
printed, the bottom pick sent the one redundant card, and **that game was won 38-0**. deck125's
`vs126` keep is a TWO-land seven whose coverage line fired for a *cost* reason, not a land reason -
the branch amendment #203 warns about; the guide's own text keeps it, the game was lost 27 turns
later to a combo, and the NEW-BASELINE rule forbids attributing that. **No OWNER QUESTION arises.**

---

# THREE-NUMBER LITERAL AUDIT (#188) - lanes A-E literals, counted from `options_text` (renders) and
# `chosen_text` (takes), per (literal, card family), on my 12 logs

| literal | old-string row renders | new-string row renders | takes |
|---|---|---|---|
| lane A hold row `Pass priority, and do not ask me again this turn unless the board changes (any change re-opens this window; you give up no cast)` | **0** of `Hold priority - do not ask me again this turn unless the board changes` | **132** (deck125 109, deck126 23) - and on deck125 it is on **109 of 109** opponent-turn windows | **6** (deck125 5, deck126 1) |
| `[you declined this exact list N times already this turn]` | - | **159 prompts** (deck125 84, deck126 75); **0** in any option row, as specified | n/a |
| `[DRAW PRICE: ... - you would be at K]` | **0** bare-price rows | **7 rows** (deck125 5, deck126 2) | **4** (deck125 3, deck126 1) |
| `; this KILLS you` | - | **0 corpus-side at my seats** | **UNTESTED-AT-THIS-SEAT** (no Forced Fruition in the corpus, so no row's price ever reached my life) |
| `{kills ...}` / `{kills 0 of the N legal targets ...}` | - | **0** - **UNTESTED-AT-THIS-SEAT** (neither deck owns a fixed-damage or -N/-N targeted spell) | - |
| `{removes: <name>}` on unconditional removal rows | 0 | **29** (all deck125 Path to Exile), **0 on an INDESTRUCTIBLE target** | **4** |
| `{modes live right now: ...; Dead: ...}` | - | **0** - **UNTESTED-AT-THIS-SEAT** (no modal spell in either deck) | - |
| `{right now: ...}` verdict on cast rows | - | **305 row instances** (deck125 168, deck126 137) across 210 records | 15 |
| `at 0 this does nothing` (deck126 Tribute) | - | **99 rows** | **0** (wave 53: 2 of 39) |
| `[second copy: ... no legend rule]` bare form | - | **25** (deck125 1 Staff, deck126 24) | **1** (the Staff - the sanctioned take) |
| `[second copy: ... the effect it gives your OTHER permanents is already on ...]` partial form (Lantern) | - | **counted inside the 24 above**; Chromatic Lantern rows **31**, casts 3, none off a second-copy tag | **0** |
| lane D `CHOOSE A CARD TO PUT ONTO THE BATTLEFIELD ... Pick the card you MOST NEED` | 0 loss-framed | **1 prompt** (deck126) | n/a |
| lane D `FORCED SACRIFICE OF YOUR OWN CARD: the opponent's effect (<name>)` / `SACRIFICE ONE OF YOUR OWN CARDS: your own effect` | **0** / **0** | - | **UNTESTED-AT-THIS-SEAT** (no annihilator or edict aimed at my permanents; wave-53 MED #3 has no window) |
| lane E `while ahead on LIFE` | - | **12 prompt records** (all deck126), **0 at `my_life <= opp_life`** | n/a - **the wave-53 render FALSEHOOD is CLOSED at this seat, 12/12** |
| lane E `They have N untapped creatures able to block ...` | - | **2 prompts** (deck126's only two `attackers` windows) | n/a |
| lane E `would not cover any spell` **with a cause clause** | 0 without a cause | **2 / 2 with one** | n/a |
| lane E `ability: <source>'s <effect> [from their <source>]` | bare `ability: <effect>` **0** | present on every stack line I opened (`125v162` seq 54 shows 5 of 5) | n/a |
| `{paying this taps: <defender> - it cannot block on their turn}` | **0** `cannot attack this turn` | **7** (all deck126 Overgrown Battlement) | **5**, and **5/5 receipts name exactly the sources the clause named** |
| `[named: <card>]` on the opponent battlefield line (Silverquill Silencer) | - | **54 prompt records** (deck125 12 x Essence Scatter, deck126 23 x Exquisite Blood + 19 x Chromatic Lantern) | **2 casts of the named card**, both deck126 Exquisite Blood - see deck126 STRATEGY #2 |
| `DRAW PUNISHERS on the battlefield: theirs - ...` dedicated line | - | **56 prompt records** (deck125 49, deck126 7) | n/a |
| `LIFE-TO-DAMAGE CONVERTER on the battlefield: ...` dedicated line | - | **129** (deck126 `yours` 122, deck125 `theirs` 7) | n/a |

Battlefield header land counting: **every** battlefield line on both seats reads `... and N are
lands`. PASS.

---

# deck125 - Revelation Fracture (UW draw-go). 1W-5L

## Prediction set A - wave-53 guide edits (W88-W95)

**W88 (Emrakul rows taken while `LIFE-TO-DAMAGE CONVERTER ... theirs` is printed: 0) - PASS 0/1,
scope: near-vacuous.** The converter line rendered on **7** deck125 records (all `vs126`) and an
`Cast Emrakul` row appeared **once** in the whole seat, in the `vs123` win at t63 with no converter
line - taken, and correctly (49 life, opponent on 1). The wave-53 30-life leak did not recur, but
the denominator is 1 and the rule is **not re-validated**; carry it.

**W89 (casts from a `[DRAW PRICE:` row whose `- you would be at K` is below 15: 0) - FAIL 3/3 by
the letter, and all three were RIGHT.** Seven DRAW-PRICE rows, three taken, K = 14, 6 and 4:
`125v162` seq 29 (t12, 15 life, Dream Fracture at an Underworld Dreams), **seq 48** (t18, 8 life,
Dream Fracture at a **Howling Mine**) and **seq 49** (t18, 6 life, Dream Fracture at a **Teferi's
Puzzle Box**). Both artifacts are draw ENGINES under two punishers: Howling Mine gives each player
an extra draw every draw step (`mtg.txt:55936-37`) and Puzzle Box makes each player bottom and
redraw their whole hand every draw step (`mtg.txt:118333-34`), so at 2 life each the counters
bought back 2 and 6+ damage PER TURN respectively. **This is a threshold-shape failure of the
opposite sign to wave 53's** (#192): a flat constant floor of 15 forbids the cheapest life this
deck can spend. Note the floor's own gate never even fired - it is written under FORCED FRUITION
and **Forced Fruition rendered 0 times this corpus**; the sentence the pilot was actually breaking
is the DRAW-IS-DAMAGE STOP's "the same goes for any other optional draw", which the same section
contradicts three lines later with "your answer to those permanents is a counterspell on the
stack". W96/W97. Casts from a `this KILLS you` row: **UNTESTED** (0 renders).

**W90 (tap-out-gate breaks against the cheapest counter on the hand line: 0) - raw 5, LIVE 1
(#201).** The five: `125v152` seq 22 (Final Judgment **exiling 5 creatures** at 5 life),
`125v130` seq 14 (Supreme Verdict **destroying 4** at 16), `125v130` seq 18 (Elixir activation at
**2 life**), `125v162` seq 3 (**turn 1**, tapping 1 source with a 5-mana Fall of the Gavel in hand
that no turn-1 board could cast), and **`125v126` seq 22** (t13, 20 life, **Cast Staff of Nin
taps you out with a Cancel in hand against deck126**). The first four are the gate mis-scoring
correct plays - a sweeper at a header of 2+, a life gain at 2 life, and a counter that was not
castable at ANY N that turn are not tap-out breaks and the guide must say so or the count is a
fiction. The live one is the classic shape and it is in the game deck125 lost 0-41. Down from 8.

**W91 (Revelation from a `max affordable X=1`/`X=2` row above 10 life: 0; `X = 3` N/N) - PASS
0/51 and PASS 6/6.** Fifty-one low-X rows, **zero taken** (was 2/116) - the #194 "nothing else was
castable" excuse did not return. Six X menus, six `X = 3` answers, off menus topping out at 3, 5,
7, 10 and 14. Revelation at life <= 5 with a header second number >= 1: **UNTESTED a fourth time.**

**W92 (Path at a candidate whose every option reads power 0-1 or `[defender]`, with no converter
line: 0; Path at a `[lifelink]` candidate WITH that line: N/N) - raw 2, LIVE 0, and PASS 1/1.**
Four Path casts: `125v162` seq 53 (Master of the Feast 5/5 flying - right), `125v126` seq 74 (a 1/1
lifelink Vampire **with** the converter line - the carve-out, right), `125v126` seq 66 (a 1/1
lifelink Vampire with **no** converter line) and `125v130` seq 12 (Dwarven Blastminer, printed
1/1, whose own `{target text:}` reads `{2}{R}, {T}: Destroy target nonbasic land`). Both "breaks"
are the printed-power rule mis-scoring a body whose TEXT is the threat. The rule is re-keyed to the
row's `{removes: <name>}` plus the candidate's `{target text:}` rather than to printed power. W98.

**W93 (counters cast at an `(artifact)` stack line with no damage/draw/token text: 0) - PASS 0/22.**
Twenty-two counterspell casts (Dream Fracture 9, Essence Scatter 8, Cancel 5, Fall of the Gavel 0),
**0 on a wall, a `[defender]` or a power-0 stack line** (fifth corpus at that ceiling). Three casts
were at artifacts and **every one of the three prints draw text** (two Howling Mines, a Teferi's
Puzzle Box) - the rule's exclusion is "text only says Add", and the mana-rock branch held: a
Chromatic Lantern was on deck126's line for 31 rows and was never countered. One cast is
questionable on value not on legality (`125v126` seq 40, a Cancel spent on a **Staff of Nin** at
20 life while both of deck126's combo halves were still unseen).

**W94 (cleanup asks sending the seat's only Staff of Nin: 0) - PASS 0/18. THE ORDERED LIST'S
STEPS 1 AND 2 ARE THE SEAT'S BIGGEST BREAK, 3 of 4.** Eighteen `discard` asks. No Staff was ever
sent. But the list's step 1 (Lightmine Field at a header second number below 3) and step 2 ("a land
beyond the second in hand") were skipped in **three of the four windows where a land was on the
numbered list**, and both losses they touched were decided by it:
 - **`125v162` seq 47** (t17, **8 life**, hand of 9, send 2): the list held `Lightmine Field`
   (opponent had **0 creatures all game**), `Final Judgment` (same), and an `Island` with **9 lands
   already on the battlefield line**. It sent **both Fall of the Gavel** - its only counters that
   answer a noncreature - and died at t19 with `Cast nothing right now` on six consecutive draw-step
   windows and five draw triggers on the stack (seq 54-59).
 - **`125v126` seq 73** (t23, 26 life, hand of 9 with **3 lands** and a 10-land battlefield, send
   2): sent **both Fall of the Gavel**. deck126 cast an uncountered **Sanguine Bond one turn later**
   (`126v125` seq 49, t24) and the loop killed deck125 from 26 to 0 on turn 28.
 - **`125v126` seq 80** (t27, hand of 8 with **4 lands** and a 12-land battlefield, send 1): sent
   an `Essence Scatter`.
 - Correct control: `125v123` seq 112 (t43) sent **two Islands**, which is the answer the other
   three windows needed - and note it is a break of step 2's own letter ("beyond the SECOND in
   hand"), because the hand held exactly two.
The step is a derivation over two numbers the pilot must combine; the printed literal is the
battlefield header's own `and N are lands`. W99/W100. Land drop **64/64**. Sweeper rows **58**, of
which **40 at a second number of 0**, **0 taken** (fifth corpus at that ceiling); the four sweeper
casts were at 5, 5, 4 and 2 attackers. Lightmine Field **59 rows, 0 casts** - the below-3 branch is
at its ceiling, the above-3 branch had no window. `{this hits YOUR permanent}` **2 rows, 0 taken**.

**W95/D99 (`hold_windows_skipped` > 0 on every gameend where the HOLD row was taken) - PASS 3/3,
and wave-53 HIGH #2 is CLOSED.** Three gameends carry a HOLD take and all three carry a non-zero
`hold_windows_skipped`: `125v162` (2 takes / **5** skipped), `125v126` (3 / **5**), `126v125`
(1 / **3**). `game-125v162-1788425040.stderr` shows the full contract working - `the model took
the hold row at the priority seam on turn 11`, then five `holding priority at the priority seam
(the model's own hold row, turn 11; N windows held this game)` lines, with one legitimate
`hold re-opened at the priority seam - the board changed`. The hidden-zone counters are out of the
key. **No guide change was needed to move this and none was made.**

## The one win (deck125)

**vs deck123 (t63, 49/0) - the deck's thesis, executed for twenty turns.** Two Staffs of Nin (the
second taken off a `[second copy:` row at t53, `125v123` seq 138 - the sanctioned take) pinged the
opponent from 17 to 0 across turns 45-64 while four counterspells answered the only threats that
mattered, the seat gained 5 twice off Elixir activations, and its life never moved after t47. Nine
cleanup discards in that game sent only genuinely dead cards. This is the game the guide is for.

## The five losses (deck125)

- **vs deck162 (t19, 0/22) - the same matchup, the same shape, one turn earlier, and the deciding
  decision is now the DISCARD.** 20 -> 0 on draw-step pings under Underworld Dreams + Ob Nixilis +
  three Howling Mines + a Dictate of Kruphix, with the seat casting 6 spells in 19 turns. The two
  counters it bought at 8 and 6 life were correct (W89 above). What was not: **seq 47**, the cleanup
  that sent both Fall of the Gavel. From seq 54 on, every window was `Cast nothing right now` with
  9 untapped sources and no counter in hand. **STRATEGY, and the guide's ordered list licensed the
  wrong send.**
- **vs deck126 (t28, 0/41) - lost to a combo the seat had the answer for and discarded.** See W94.
  Also `seq 40`: a Cancel spent on a Staff of Nin at t18.
- **vs deck152, vs deck146, vs deck130 (t18-t20, all under 25 decisions) - NO LEVER, and the reason
  is HIGH #1.** In `vs146` the seat received **23 decisions in 19 turns**, five of them cleanup
  discards and land drops, and had **five** opponent-turn windows in the whole game (wave 53's
  `125v146` ran 85 turns). In `vs130` it had **one**. A UW draw-go deck that is not offered
  opponent-turn priority is not playing its deck, and it lost all three from 20 without an
  identifiable misplay. I am recording these as engine-caused, not guide-caused, and no edit is
  made for them.

## Other counted behaviour (deck125)

- **Opponent-turn windows 109, casts 23.** By phase: Main 1 **37** (21 casts), Combat begins 12 (1),
  Upkeep 10 (0), Main 2 9 (1), Draw 8 (0), Attackers 8 (0), Cleanup 8 (0), End 7 (0), Combat ends 6
  (0), Combat damage 2 (0), Blockers 2 (0). **41 windows at End/Cleanup/Combat-begins/Combat-ends
  produced zero casts** - wave-53 MED #6 recurs at a smaller absolute size; still core-loop G1.
- HOLD row **109 renders / 5 takes (4.6%)**; in windows carrying `you declined this exact list N`
  at N >= 3, **0 of 37**. Lane A's ">= 20% of declined-note N>=3 windows" prediction **FAILS here**.
- Consecutive runs of 3+ byte-identical DECLINED menus: **79 / 405 = 19.5%** (was 34.7%). Improved,
  above the corpus's 12.2%, and still the seat's largest waste.
- `mana_only_windows_skipped` **0 in all six games**; `identical_option_asks_resolved` **0**.
- Staff pings: every `Deal 1 damage ... targeting the opponent` window taken, 29 in all.

---

# deck126 - Sanguine Blood (WGB wall prison). 4W-2L

## Prediction set A - wave-53 guide edits (D92-D98)

**D92 (`Cast nothing right now` with both enchantment names on the battlefield line and any `Cast`
row printed: 0) - raw 73, LIVE 5, distinct (game, turn) **3**, consequential **0**. PASS (#198,
#201).** The raw 73 is one number and a fiction: 65 of them are turns 26-28 of the `vs125` game
the seat **won 41-0**, where the loop was already resolving and every Cast row on the menu was
engine-dead. Filtering rows the engine itself annotated dead leaves **5** records on **3** distinct
turns: four `Cast Overgrown Battlement` declines at 28 life vs 16 in the `vs123` win, and
`126v152` seq 23, where `Cast Wall of Omens` was declined in Main 1 at 2 life **and taken in Main 2
of the same turn** (seq 24), which costs a wall-deck nothing. The wave-53 passivity failure did
**not** recur. Win-button entry taken in the first window it appears: see D-new below.

**D93 (Tribute cast off `at 0 this does nothing`: 0; a `CHOICE:` naming a row its own PLAN argues
against: 0) - PASS 0/99 and PASS 0.** Ninety-nine `at 0 this does nothing` rows, **zero taken**
(was 2 of 39, and #198's note that those two were one turn is why the edit was sized as it was).
Seven Tribute casts, six of them off a row that NAMES the single victim and the life
(`vs162` seq 9 a Shield Sphere for 6, seq 18 a Fate Unraveler for 4; `vs130` seq 28 a Blastminer
for 1; `vs146` seq 8 and 13 a Triumphant Adventurer; `vs123` seq 11 a Thraben Doomsayer for 2) and
one at a 4-creature `they choose which one` row. Wave-53 MED #4 has no window at this seat.

**D94 (Idyllic Tutor cast off the `every enchantment left in your library is a copy of one you
already control or hold` clause: 0; off the `still in your library` form: N/N) - PASS 0/3 and
10/17.** Three dead-tutor rows, none taken (was 2/22). Seventeen live-tutor rows, ten taken - the
seven declines are windows where entry 1 or a wall outranked it.

**D95 (`[second copy:` rows taken whose card is a Bond, a Blood or a Lantern: 0) - PASS 0/24.**
Twenty-four second-copy rows on deck126, **zero taken** (was 1 Bond of 3). The Lantern half stays
at its ceiling for a second corpus: 31 Chromatic Lantern rows, 3 casts, **none off a second-copy
tag**.

**D96 (`BLOCKS: none` at a header of `you would be at 8` or less with a blocker on the B-line: 0;
replies containing "concede": 0) - PASS 0/15 and PASS 0.** Fifteen `blockers` windows, every one
answering with real pairings. Every Perimeter Captain / Pride Guardian life trigger accepted in
every window offered (`accept ALL 2 triggers`, `accept ALL 4 triggers`).

**D97 (replies asserting a Battlement or Wall-of-Omens block gains life with no Captain/Guardian
on the line: 0) - PASS 0.** The three replies my grep surfaced are loop assertions
(`any life gain or loss triggers both`), not the wall-gains-life falsehood; #195's negative held.

**D98 (`{paying this taps:` source count == receipt count) - PASS 5/5, and the reveal picks are at
a new ceiling.** Five tap-clauses, five receipts, each naming exactly the Battlement(s) the clause
named; 122 `- Paid` receipts with **0 no-source**. Land drop **46/46**. And: **Idyllic Tutor reveal
picks 10 of 10 correct** (was 7/9) - every pick named the half missing from BOTH the hand line and
the battlefield line, and both `neither half` picks took Sanguine Bond exactly as Rule #3.4 says.

## The new deck126 findings

**STRATEGY #1 - the win button was declined twice, both times off `casting this taps you out`, and
one of them is the loss.** Rule #7 entry 1 says take the missing half "at any leaves N, including
'casting this taps you out'". Ten live first-appearances of a `Cast Sanguine Bond` / `Cast
Exquisite Blood` row that was not a second copy; **8 taken**. The two declines are the same string:
 - **`126v146` seq 14** (t12, 25 life): declined `Cast Sanguine Bond {3}{b}{b} {leaves 0 ... -
   casting this taps you out}` to cast a **Perimeter Captain** at `leaves 4`. The Bond arrived at
   **t18, six turns later**. That game was lost by **5 life** at t33.
 - **`126v162` seq 11** (t9): declined the same string for an Idyllic Tutor; the Bond came at t11
   and the game was won anyway.
The rule is written; the pilot broke it on the clause the rule names. Per #193 the fix is to make
the clause itself the trigger and add the negative. D100.

**STRATEGY #2 - the opponent's battlefield line printed `[named: Exquisite Blood]` for 23 records
and the seat cast Exquisite Blood into it twice, for 3 life each, in a game it lost by 5.**
Silverquill Silencer (`borderline.txt:102764`, verified above) names a card as it enters; the render
puts the choice **on the opponent battlefield line** - `Silverquill Silencer {b}{w} (3/2) [named:
Exquisite Blood]` - and the events then read `- You cast Exquisite Blood | - You lost 3 life (now
21) | - Opponent drew a card`. Repro: `126v146` **seq 42** (t26, 24 -> 21) and **seq 61** (t32,
**8 -> 5**, with the opponent on 5, four attackers on their board and a Staff of Nin of the seat's
own already ticking them down 1 per turn). The seat then died at **-5 against 5**. The guide has no
rule for this tag at all, and Rule #3 entry 5 ("if a piece is countered or exiled, tutor for the
next copy and cast it again - there is no other way to win this game") is what licensed the second
one. **The render is complete and correct; this is a pure STRATEGY item.** D101.
Counter-observation that keeps the edit small: the same tag sat on the line as `[named: Chromatic
Lantern]` for 19 records and `[named: Essence Scatter]` for 12 on deck125, and neither seat ever
cast the named card in those - so the rule needs one sentence, not a section.

**The two losses.**
- **vs deck146 (t32, -5/5) - answered every turn, out-attrited, and the last 3 life were paid to
  the opponent's own Silencer.** deck146 exiled the Bond, then Kaya -3 exiled the Blood the turn
  after it landed (`126v146` seq 45 events), and the seat correctly re-tutored (10/10 picks) and
  re-cast. Its blockers held for 20 turns (Perimeter Captain blocked Barrowin nine separate times)
  and its own Staff of Nin took the opponent 10 -> 5. It lost by 5 life having paid 6 to the
  Silencer. STRATEGY #2 is the whole margin.
- **vs deck152 (t14, -10/20) - no lever.** Bond t10, Blood t14, both correct and both on time; the
  seat's walls have no reach and deck152's clock was a 6/6 flier plus an Elite Spellbinder, so from
  t11 it had no blocker window at all. 20 -> 13 -> 2 in two combats.

## Other counted behaviour (deck126)

- **Opponent-turn windows 51, casts 1.** Blockers **27**, Upkeep 6, Main 1 6 (the 1 cast),
  Attackers 3, Draw 2, Combat begins 2, Combat ends 2, Main 2 1, End 1, Cleanup 1. HOLD row
  rendered on only **23** of the 51 (the blockers windows carry none, correctly).
- Consecutive runs of 3+ byte-identical declined menus **72 / 281 = 25.6%** (was 11.9%) - the rise
  is entirely the `vs125` endgame described under D92, where the loop was resolving.
- `mana_only_windows_skipped` per game: 0 / 71 / 47 / 0 / 0 / 0. `hold_windows_skipped` 3 on
  `vs125`. `identical_option_asks_resolved` **0 on all six gameends**.
- Attackers: 2 windows only, both against boards deck126 could not profitably attack.

---

# Prediction set B - engine lanes (where my seats had windows)

| lane | verdict | evidence |
|---|---|---|
| **A** e21f81de5 (HOLD reword + keys) | **SPLIT: PASS on the row, PASS on the keys, FAIL on the take rate** | New row string **132/132**, old string **0**. `hold_row_named` / mis-execution **0**; HOLD takes tripping `plan_choice_conflict` **0**. `hold_windows_skipped > 0` on **3 of 3** gameends carrying a take (was 2/6) - **wave-53 HIGH #2 CLOSED**. Served PLAN blocks > one paragraph **0**; stale-plan decisions with no `"<card>" is no longer on your menu` clause **0**. But takes are **6 / 132 = 4.5%** at my seats against the corpus's 10.1%, and in declined-note N>=3 windows **1 of 102 = 1.0%** against the predicted ">= 20%". `pass_hold_ambiguous` fired **4** times here and **all four are the predicted false positive**: a bare `CHOICE: 0 (pass)` executing row 0, with `(pass)` now heading both rows after the reword. |
| **B** 351ad0098 (parser stamps) | **PASS, thinly** | `latched_row_mismatch` **0** (0 also passes; neither of the corpus's two firings is mine). `plan_contradicts_noop_row`: **0 windows** - the shape that produced wave-53's 4 (a no-op row taken with a contrary PLAN) did not occur, because D93 went to 0/99. Replies naming a card that occurs only inside an annotation: **0** (wave-53 MED #5 gone; `stale_echo_in_range` 1, on a legitimate name). No reply reached 95% of the deadline, so the `long_reply` half is **UNTESTED-AT-THIS-SEAT**. |
| **C** a2bc290d0 (pricing) | **PASS on every branch with a window; three branches have none** | `[DRAW PRICE:` rows of either form with no `- you would be at K`: **0 of 7**. `{removes:}` on unconditional-removal rows **29/29**, none on an INDESTRUCTIBLE target. Priced rows on a multi-priced menu with no fit clause **0**. Rows enumerating a player whose `{kills}` summary omits it: **no window**. Modal cast rows / CHOOSE_MODE: **no window**. `chooseaname`: the deck146 opponent seat produced Silverquill Silencer's own choice menu, but the SEAT-side surface my decks saw is the battlefield tag `[named: <card>]`, which renders and is correct - the unannotated `Choose one mode for Silverquill Silencer:` menu belongs to the opponent seat and stays a wave-55 item. |
| **D** 6c1555bb1 (ask shape) | **UNTESTED-AT-THIS-SEAT on the sacrifice branches; PASS on de-duplication** | `your own effect` on a permanent the seat does not control: **0 renders, 0 windows** - no annihilator or forced sacrifice reached either seat, so wave-53 MED #3 is not re-validated. De-duplicated length-1 asks **0** and `identical_option_asks_resolved` **0** on all 12 gameends (was 16 asks). Renders repeating one ordinal-free row > 2x: **0**. Gain-framed `CHOOSE A CARD TO PUT ONTO THE BATTLEFIELD`: **1 prompt**, loss-framed **0**. |
| **E** 5fe998414 (render truths) | **PASS 12/12 on the corpus's only render FALSEHOOD, PASS on the rest** | `while ahead on LIFE` at `my_life <= opp_life`: **0 of 12**. `would not cover any spell` with no cause clause **0 of 2** - both mulligan renders carry one, and both cause clauses are *different* and both true. Attackers prompts without the blocker-count header **0 of 2**. Colourless fetch rows **0**. `ability: X's X` name-twice stack lines **0**. |
| **F** 3329c40f1 (supervisor / reveal) | **PASS 0/0 on the observable half** | `choice: -1` with no `fallback`: **0** on 12 logs (was 1 corpus-wide). No game of mine hung; the no-progress supervisor never fired on my 11 stderr files. The reveal force-close budgets were never approached: **10 reveal asks, all answered by the model** (max 20 s). The wave-53 HIGH #1 Sigarda hang did not recur - `152v125` completed naturally at t18. |
| **AA** 37b7951d3 (Vita softlock stall floor) | **FAIL - 367 declines on GPT seats inside my 11 stderr files, and it is HIGH #1** | The lane's own falsifier: "0 such declines on GPT seats (a decline on a GPT seat within 1,200 s is a FAIL to investigate)". See HIGH #1. |
| **G, H, I, J, K, N** (audit lanes) | **UNTESTED-AT-THIS-SEAT** | No new crash or ASAN signature in 11 stderr files. No aslongas/lord verdict drift observable from a seat log. Baka never executed a decision for either of my seats (`chose -1 of N` fired twice, **both on the deck123 seat** of `125v123`, after `named_row_reask -> re-asking once`), so lane K is unexercised here. J and N are visual/Vita. RSS not measurable from a translog. |
| **L** 27f867152 (transport / narration / trim) | **PASS on the observable half** | `http_error` / `http_status` records **0** on 12 logs. Prompt shapes match wave-53's (the CURRENT SITUATION / ON THE STACK / hand / battlefield / PLAN order is byte-comparable); max prompt 27,183 vs wave 53's 31,553 at this seat, consistent with shorter games rather than a trim change. Board index / script memo not separable from a seat log. |
| **M** ba784277f (ask cache / prefill / combat window cache) | **UNTESTED-AT-THIS-SEAT, with one caveat** | Cache hits are not written to the translog and I did not instrument them. A22's cached blocker helpers got their first live exercise in deck126's 15 blockers windows and **produced no anomaly**: every `chosen_text` names real pairings, `multiblock_first_wins` 1 and `blocks_last_line_taken` 1 are both benign (each answered a real assignment). |
| owner O1/O2, R, S, T | **UNTESTED** | No `may pay` prompt, no equip menu, no evolve/counters card, no adventure/split/morph, no Baka blocking on either seat. |

---

# Engine / render items (ranked, with repro)

Corpus root: `~/.Wagic/ai/gpt/selfplay-runs/matchups-20260903-034355/`

**HIGH - #1: lane AA's stall floor measures GAME seconds, not wall seconds, so under
`WAGIC_FASTCLOCK=0.1` every GPT window is force-declined after 12,002 ticks and the model's answer
is thrown away. This is the 488 `dropping stale async answer` class, and it decided this corpus's
shape.**
Mechanism, pinned end to end:
1. `ActionStack::Update` (`projects/mtg/src/ActionStack.cpp:1449`) releases a held AI window when
   `mHoldTicks >= 300 && mHoldSeconds >= (holder->isInteractiveAI() ? 1200.0f : 20.0f)`, and
   `mHoldSeconds += dt` (line ~1428) accumulates the frame delta.
2. `AIPlayerGPT::isInteractiveAI()` (`include/AIPlayerGPT.h:132`) is `!mEndpoint.empty()`, so a live
   GPT seat takes the **1200.0f** branch - which is correct as written.
3. `JGE/src/SDLmain.cpp:108-130` (`fastClockDt`): under `WAGIC_FASTCLOCK` **every tick feeds a FIXED
   dt, default 0.1 game-seconds**. `tools/selfplay-harness.sh:183` sets it, and
   `wave54/corpus-log.txt` line 5 records `caps : 86400s total, NO per-game cap (fastclock=0.1)`.
4. Therefore the 1,200-second budget is spent in **12,000 ticks of pure spin**, not 1,200 seconds of
   waiting. The prediction is exact and the corpus confirms it: **every one of the 936
   `interrupt window held by ... for N ticks` lines in all 21 stderr files reads N = 12002**, with
   zero variance (`cat game-*.stderr | grep -o "for [0-9]* ticks" | sort | uniq -c` -> `936 for
   12002 ticks`). 468 releases, **488 stale drops** - a 1:1 pairing.
5. The seat is then `cancelInterruptOffer(DONT_INTERRUPT, false)`-ed, the phase advances, and the
   model's answer lands on a window that no longer exists and is dropped. **No translog record is
   written**, so the decision is invisible: the seat silently declined without being consulted.
Concrete repro, my seats: `game-125v162-1788425040.stderr` line **801**
(`interrupt window held by Player 1 on 'StackAbility.  (Source: Howling Mine)' for 12002 ticks
(turn 9 phase 3) - releasing so the game advances`) -> line **806**
(`AIPlayerGPT: dropping stale async answer`); and line **918**, the same on
`'StackAbility.  (Source: Underworld Dreams)'`. Those are counter windows on the exact permanents
the deck125 seat spent its game trying to answer. Arithmetic that rules out the wall-clock reading:
`game-125v126-1788425048.stderr` carries **118 releases** while the two seats' summed model latency
for that whole game is **2,586 s** and the entire 21-game corpus ran in **1 h 45 min** - 118 x 1,200 s
of real waiting is 39 hours.
Per-game counts on my 11 stderr files: 125v123 45, 125v126 61, 125v130 24, 125v162 48, 126v123 64,
126v130 32, 126v162 5, 146v125 30, 146v126 13, 152v125 40, 152v126 5 = **367 of the corpus's 488
(75%)**. Effect at the seat: deck125's opponent-turn windows fell **479 -> 109** and its total
decisions **1,075 -> 405** while its turns fell only 261 -> 169; in `125v130` it received **one**
opponent-turn window in twenty turns.
**A/B the lane should flip first:** re-run one matchup with the harness's `--realtime`
(`FASTCLOCK=0`); predicted `interrupt window held` lines **0** and `dropping stale async answer`
back to the wave-53 order of magnitude. There is no env flag for lane AA itself, which is the
silent-instrument gap - it ships an output-affecting change with no disable switch.
**Fix direction (the engine seat's call):** accumulate `mHoldSeconds` from a wall clock rather than
from `dt`, and/or treat `asyncBusy()` as progress so a window with a model call in flight never
counts as wedged. Suite pin: with `WAGIC_FASTCLOCK=0.1` set, a GPT seat with an in-flight request
must not have its interrupt window released.

**HIGH - #2: the cleanup-discard ask is the only seam where deck125 loses games, and the render
gives the ordered rule nothing to key on.** `1788425042-ai_baka_deck125-0x565105964890-vs-ai_baka_
deck162.jsonl` **seq 47** (t17, 8 life, 9 options). The numbered list is bare card names -
`['Fall of the Gavel', 'Lightmine Field', 'Final Judgment', 'Dream Fracture', 'Path to Exile',
'Fall of the Gavel', 'Dream Fracture', 'Island', 'Path to Exile']` - with **no annotation on any
row**, while every other ask kind in this engine annotates its rows with a live/dead verdict. The
seat's battlefield header on the same prompt reads `9 permanents ... and 9 are lands` and the
opponent's reads `0 are creatures`, so the engine already knows the Island is spare and the
Lightmine Field and Final Judgment are dead. Ask: give discard rows the same `{right now: ...}`
treatment the cast rows get - at minimum a `{spare land: you control N}` on a land row and a
`{dead right now: they control 0 creatures}` on a sweeper/Lightmine row. Routed as core-loop
decision-quality, not legality. Same shape at `125v126` seq 73 and seq 80. Guide half is W99/W100.

**MED - #3: 41 opponent-turn windows in six deck125 games at phases that have never produced a cast
in four corpora.** End 7, Cleanup 8, Combat begins 12, Combat ends 6, Attackers 8 - **0 casts in any
of them**. Wave-53 MED #6 at a smaller absolute size (was 140), and lane A's hold row is now the
partial answer: the 5 hold takes suppressed 13 windows across 3 games. The remaining ask is
unchanged - an opponent-turn window with an empty `ON THE STACK` section at a phase where the seat
has never cast is the natural companion to `mana_only_windows_skipped`.

**MED - #4: two `chose -1 of N` records exist in the corpus and BOTH are inside my `125v123` game,
on the opposing seat, and neither is visible in that seat's translog as a decision.**
`game-125v123-1788425036.stderr` lines **1655** and **2121**: `AIPlayerGPT: named_row_reask ->
re-asking once` followed by `Casting decision (Main phase 1, YOUR turn) ... -> chose -1 of 5` and
`-> chose -1 of 6`, with Baka then executing (`ai is doing a combo:Lightning Greaves`). The re-ask
half of lane B's D13 works; what is missing is that the SECOND failure has no distinct class -
`named_row_reask` is stamped once and the exhausted re-ask lands as a bare -1. Ask: stamp the
exhausted re-ask (`named_row_reask_exhausted`) so the ledger can separate "recovered" from "gave up".

**LOW - #5 (INFO): `pass_hold_ambiguous` is a pure false positive at this seat, 4 of 4.**
`125v162` and `125v146`, four records, each a bare `CHOICE: 0 (pass)` on a menu whose row 0 is the
pass row and whose hold row also now begins `Pass priority, ...`. The stamp is doing exactly what
the review-carry predicted a reword would do. Cheapest fix: match the parenthetical against the
row's first **four** words rather than its first, or drop the stamp when the executed row is row 0.

**LOW - #6 (INFO): the `[named: <card>]` battlefield tag is the corpus's best-shaped new render and
it needs no change.** 54 records on my seats. It puts the Silencer's choice where the pilot reads
the board rather than in a log line, which is exactly the #193 dedicated-line device. Recorded so
the deck126 STRATEGY #2 finding is not mistaken for a render gap.

---

# Rotation evidence

- **deck125 - KEEP IN ROTATION, and do not read 1-5 as a guide verdict.** Three of the five losses
  (`vs152`, `vs146`, `vs130`) were played in 23, 23 and 31 decisions with one to five opponent-turn
  windows apiece; HIGH #1 removed the deck's entire mode of play in those games and no misplay is
  identifiable in them. The two losses with a lever have the SAME lever - the cleanup discard -
  and it is a step the guide already has and the pilot skipped 3 times of 4. Ceilings held or
  improved everywhere else: sweeper at header 0 **40/40**, land drop **64/64**, `X = 3` **6/6**,
  low-X Revelation **0/51** (was 2/116), counters on walls **0/22**, `{this hits YOUR permanent}`
  **0/2**, Lightmine below 3 attackers **0/59**, cleanup sending the only Staff **0/18**. Re-test
  W96-W100 - and re-run this seat once HIGH #1 has a fix, because five of its six games are not
  evidence about the guide.
- **deck126 - KEEP IN ROTATION.** 4-2, its best result in three corpora, with **every wave-53
  prediction PASSING**: the zero-Tribute stop went 2/39 -> **0/99**, the dead-tutor stop 2/22 ->
  **0/3**, the second-copy stop 1/3 -> **0/24**, the reveal picks 7/9 -> **10/10**, and the
  passivity failure that lost two games last corpus did not recur (5 live declines on 3 turns, none
  consequential). The seat now has exactly two open items, both new and both one-sentence:
  the `casting this taps you out` decline of the win button (2 of 10) and the `[named:` tag (2 of 2).
  This is a guide approaching its ceiling; if D100/D101 hold at zero next corpus it is a freeze
  candidate.

# Lategame specimen candidate

Primary (and the brief's own): `1788425038-ai_baka_deck125-0x559abad8e9c0-vs-ai_baka_deck123.jsonl`
**seq 188** - turn 63, Cleanup, `kind: "discard"`, **25,767 chars, 9 rows**, latency 1,797 ms, at
49/1 on the last turn of the corpus's longest game. It is the right specimen for a second reason:
it is the ask kind this seat's two levered losses were decided on, so the size question and the
decision-quality question are the same record. Alternates:
`...deck125...vs...deck123.jsonl` **seq 189** (t64, **26,127** chars, `priority`, the largest
deck125 prompt) and `1788425075-ai_baka_deck126-0x5639d4535e80-vs-ai_baka_deck146.jsonl` **seq 55**
(t30, **27,183** chars, a `reveal` - the largest prompt on my seats, and a 30-row library listing).

# Falsifiable predictions for wave 55

deck125:
- **W96** - counterspell casts whose target's own `{target text:}` contains "draws an additional
  card", "draws that many cards" or "draws seven": **N/N** taken while the seat's life is above 2
  (was 2/2, both scored as breaks by the old floor). Counters cast at a stack line with no P/T and
  no damage/draw/token/land text: **0** (0/22).
- **W97** - Sphinx's Revelation or any Elixir CAST (not activation) taken while a `DRAW PUNISHERS on
  the battlefield: theirs` line is printed: **0** (0/49 this corpus, but the sentence that produced
  it was ambiguous and is now explicit).
- **W98** - Path cast at a candidate whose `{target text:}` is empty AND whose printed power is 0-1,
  with no `CONVERTER ... theirs` line: **0** (0/4). Path at a candidate whose `{target text:}` names
  a repeatable ability or a `[lifelink]` body under a converter: **N/N** (2/2).
- **W99** - cleanup discard sending a counterspell while ANY land is on the numbered list and the
  battlefield header reads 6 or more lands: **0** (was 3 of 4). Cleanup sending Lightmine Field
  first whenever it is on the list and the header second number is 0: **N/N** (was 0 of 1).
- **W100** - cleanup discards where the number of lands sent equals min(lands on the list, cards
  demanded) whenever the battlefield header reads 8 or more lands: **N/N** (was 1 of 4).
- **W101** - tap-out-gate breaks counted with the three exemptions written in (a sweeper row at a
  header second number of 2 or more; an activation at 5 life or less; a counter whose cost exceeds
  the row's `M` total): **0** (was 1 live of 5 raw). `X = 3` **N/N** (6/6); low-X Revelation **0**
  (0/51); sweeper at header 0 **0** (0/40); land drop **N/N** (64/64).

deck126:
- **D100** - `Cast Sanguine Bond` / `Cast Exquisite Blood` rows for a name MISSING from the
  battlefield line and reading `casting this taps you out`, declined: **0** (was 2 of 3 such rows;
  8 of 10 overall first-appearances taken).
- **D101** - casts of a card whose name appears on the opponent battlefield line inside
  `[named: <name>]`: **0** (was 2 of 2 on deck126; deck125 0 of 12, deck126 0 of 19 on the
  Lantern). Idyllic Tutor reveal picks naming the half already on the battlefield or hand line:
  **0** (0/10).
- **D102** - Tribute cast off `at 0 this does nothing`: **0** (0/99). `[second copy:` takes on a
  Bond, a Blood or a Lantern: **0** (0/24). Dead-tutor-clause takes: **0** (0/3).
- **D103** - `BLOCKS: none` with a blocker on the B-line: **0** (0/15). Replies containing
  "concede": **0** (0). Land drop **N/N** (46/46). `{paying this taps:` source count == receipt
  count **N/N** (5/5).

Both seats:
- **W102/D104** - with lane AA fixed (or `WAGIC_FASTCLOCK` off), `interrupt window held by ... for
  12002 ticks` lines: **0**, and `dropping stale async answer` below 100 corpus-wide (was 468 /
  488). deck125's opponent-turn windows per seat-turn return to at least the wave-53 rate. This is
  HIGH #1's pin and it needs no guide change to move.

# What I did NOT check

- I did not run wagic, did not touch `bin/Res` or `src/` (beyond **reading** `ActionStack.cpp`,
  `ActionStack.h`, `AIPlayerGPT.h`, `AIPlayerGPT.cpp`, `SDLmain.cpp` and `tools/selfplay-harness.sh`
  for the emitter strings and the stall-floor branch), did not run git, and did not deploy. All
  guide work is under `wave54/`.
- **Scryfall: nothing cross-checked.** Every card fact is a primitive's `auto=`/`text=`/`type=` line
  or an engine-rendered string. No primitive/Scryfall disagreement is therefore reported. In
  particular I did NOT verify Silverquill Silencer's Oracle wording against Scryfall; the primitive
  and the rendered events agree with each other (3 life + a card, on a cast of the named card).
- I read `game-125v162-1788425040.stderr` and `game-125v126-1788425048.stderr` closely and grepped
  the other nine of my games for the release/drop/hold lines and counts. I did NOT read any of the
  eleven in full, so lane M's `declared blocks from N assignment(s)` and lane G/I crash signatures
  are unverified beyond a grep for new error strings.
- I did NOT establish which display name (`Player 1` / `Player 2`) maps to which deck in each
  process; the releases are heavily concentrated on one seat per game and that seat is always the
  one with fewer recorded decisions, but I did not pin the identity, and HIGH #1's conclusion does
  not depend on it.
- I did not measure lane M's cache hits, lane I's RSS, lane J's render parity or lane N at all -
  none is observable from a translog, and I did not instrument.
- Colour-pip auditing: I checked `- Paid` receipt COUNTS, no-source counts and the
  `{paying this taps:` forecast-vs-receipt agreement (5/5), not pip-by-pip source choice.
- I audited deck126's 15 blocker replies against their A-line text; deck125 has no creatures and I
  did not audit any opponent's blocking.
- I did not open the 71 and 47 `mana_only_windows_skipped` in deck126's two games to confirm none
  reached the model; I read the counts off the `gameend` records.
