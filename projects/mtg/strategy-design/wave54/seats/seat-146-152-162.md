# Wave-54 seat: deck146 (5-1) + deck152 (5-1) + deck162 (2-4)

Corpus `~/.Wagic/ai/gpt/selfplay-runs/matchups-20260903-034355/`, binary master **caf023dbc**
(engine lanes A-F + wave-53 AA + audit lanes G-N), 18 seat logs on my decks, **630 decision
records** (146: 289, 152: 223, 162: 118; excluding `gamestart`/`gameend`/`recovery`), **356.8 min
of inference**, mean 35.0 s/decision. **4 of the corpus's 23 fallbacks**, 1 of the 10 `recovery`
records, and **the corpus's only `6 consecutive stale drops - giving this decision to the
heuristic` handoff** (E-2 below). 21/21 natural. Eleventh fair-hand corpus; first carrying the
reworded HOLD row, parser stamps, cast-row pricing, modal live/dead clauses, gain-framed
searches, render truths and the reveal stall guard. Guides under test = the wave-53 reviewer
revisions (live copies byte-identical to `wave53/deckN/strategy.txt`). Card facts read off
`bin/Res/sets/primitives/*.txt` with `/usr/bin/grep`; translogs read with python.

**Records (corpus-results.tsv):**
- deck146 **5-1**: W vs125 (22/-6 T19), vs130 (14/-2 T33), vs152 (7/-11 T21), vs126 (5/-5 T33),
  vs162 (4/-1 T29); L vs123 (-3/12 T10).
- deck152 **5-1**: W vs162 (18/0 T9), vs130 (29/-3 T11), vs123 (29/0 T9), vs125 (29/-11 T19),
  vs126 (20/-10 T15); L vs146 (-11/7 T21).
- deck162 **2-4**: W vs125 (22/0 T19), vs130 (12/0 T11); L vs123 (-32/13 T12), vs126 (0/38 T15),
  vs146 (-1/4 T29), vs152 (0/18 T9).
No rate is attributed to any guide edit in either direction (new-baseline rule). Games are far
shorter than wave 53 (my seats: 630 decisions vs 1,145) and my decks' opponent-turn window volume
collapsed with them - several lane-A predictions are UNTESTED at my seats and adjudicated
corpus-wide by the emitter's string, as the brief directs.

**Files (short -> full, all prefixed `17884250`):**
146: v125 `47-...deck146-0x556a263e2740-vs-...deck125` | v123 `57-...0x562dbc6948a0...` |
v130 `59-...0x55a764adc7f0...` | v162 `73-...0x55cbe78957e0...` | **v126 `75-...0x5639d32006f0...`** |
v152 `77-...0x56025e771830...`
152: v125 `40-...0x5636e7f6e580...` | v162 `53-...0x5607b651b550...` | v123 `63-...0x5599d9500410...` |
v130 `65-...0x55c0c75ec4f0...` | v126 `71-...0x5628aa896460...` | **v146 `77-...0x56025faa6f70...`**
162: v125 `42-...0x565106c7de60...` | v130 `44-...0x5647d8fe86f0...` | v126 `48-...0x5588427d0ed0...` |
v152 `53-...0x5607b7852a30...` | v123 `60-...0x55b7983517a0...` | v146 `73-...0x55cbe8bccf30...`
The corpus's **largest prompt (32,215 chars) is mine**: `146v126` s86.

---

## THREE HEADLINES FOR THE CORE LOOP

**1. ENGINE (HIGH) - 96% of the corpus's 488 `dropping stale async answer` lines are lane AA's
stall floor releasing GPT-owned windows on a TICK budget, and its 1,200 s LLM-seat allowance is
not being honoured.** Every release in the corpus reads `for **12002** ticks with no progress` -
**468 of 468, one value, no variance**, which is what a lone tick gate looks like and not what a
`ticks AND seconds` conjunction looks like. Releases 468, stale drops 488, and the drop follows
the release immediately in every sample I read. The mechanism, from `game-162v130` stderr
lines 412-418 and 490-507: an interrupt window opens on a `StackAbility` or `NextGamePhase`, the
seat is asked and a model call is in flight, `ActionStack: interrupt window held by Player N ...
- releasing` fires, the stack resolves or the phase steps, the answer lands into a window that no
longer exists (`AIPlayerGPT: dropping stale async answer`), and the ask is re-issued. Wall-clock
proof it is not the 1,200 s budget: the **largest latency anywhere at my seats is 347 s**
(`152v125` s24) and the corpus's only four-figure latency is the 1,489 s opening mulligan at
`123v125` under 21-game launch contention - the releases happen far below the allowance.
Review-carry's lane-AA corpus prediction is **"0 such declines on GPT seats (a decline on a GPT
seat within 1,200 s is a FAIL to investigate)"**: **FAIL, 468 corpus-wide, 174 in my 13 games**
(`152v125` 39, `162v130` 37, `146v125` 30, `162v123` 19, `152v123` 15, `146v126` 14, ...).
Cost is 468 wasted model calls and 468 re-billed prompts. Twenty of the windows released are
`NextGamePhase (Main phase 1)` and six are named SPELL windows (`Underworld Dreams`,
`Triumphant Adventurer`, `Silverquill Silencer`) - those are response windows the seat lost.
**Flag to flip first:** lane AA's stall floor - and note it is the one output-affecting change in
this wave's set with **no disable flag in review-carry §C**, which is itself the silent-instrument
gap; the A/B needs one. Candidates named in the brief that this evidence RULES OUT: lane A hold
keys (the drops occur on non-priority seams and on the seat's own turn), lane M's ask cache and
lane L's board index (both would not print a release line at all).

**2. ENGINE (HIGH) - a reveal chooser livelocked, burned six model calls, and gave a real
decision to the heuristic.** `146v123` seq 15 (T9, Main 1, `kind: reveal`, `fallback:
stale_livelock`, `choice: -1`, `latency_ms: 0`), with `recovery` at s16. stderr lines 1335-1343:
six `dropping stale async answer` lines back to back with NO intervening release line, no phase
step and no image load between them, then `AIPlayerGPT: 6 consecutive stale drops - giving this
decision to the heuristic`, then `Action added to stack: StackAbility. (Source: Pelakka
Predation)`. This is a SECOND staleness path, distinct from headline 1: the window's generation
key advances on each re-ask, so every answer is stale on arrival and the loop is only broken by
the six-drop guard. The decision handed away was Pelakka Predation's choose-a-card on a
four-row reveal whose annotations were correct and useful (`[does NOT qualify - goes to "put
back"]` / `[eligible for "choose a card"]`) with **Damnation** on the list. Lane F's reveal work
shipped a stall guard (20,000 ticks AND 600 s); this reveal never stalled, it livelocked, so the
guard has no window on it. `146v123` also carries 6 drops with **0** releases (a benign
end-of-game burst) and `152v126` carries 5 with 1 - the residual ~20 drops corpus-wide.

**3. GUIDE - deck162 braked ten own-turn windows on cards that hand the opponent nothing, and the
engine had the discriminator on the row the whole time.** `162v126` s7/s8/s9/s10/s11/s12 (turns
4, 4, 6, 6, 8, 8) and `162v146` s7/s8/s10/s11 (turns 4, 4, 6, 6): the identical two-row menu
`1. Cast Liliana's Caress {1}{b}` (or `Cast Fog Bank {1}{u}`) / `2. Cast nothing right now`,
answered row 2 every time, plan sentence "cast a punisher first". Underworld Dreams costs
`{b}{b}{b}`; the seat had TWO lands and no third black source until turn 10. RULE 2's brake is
about cards that hand the opponent a card, and every one of those prints
`{feeds: the opponent draws N extra card(s) per turn; converters on your battlefield: K ...}` on
its row - Caress and Fog Bank print no such clause. The guide keyed the brake to a CATEGORY
("draw engines") and the pilot widened it. Re-keyed to `{feeds:` (162-A), which keeps the two
correct Forced Fruition brakes at K of 0 firing as its control. That game ended 0 to 38.

---

## FAIR-HAND BASELINE - every mulligan and bottom on my seats

| seat | look | engine count / coverage clause | decision | verdict |
|---|---|---|---|---|
| 146v123 | 7 | 3 lands, covers 4 | KEEP (129 s) | right |
| 146v125 | 7 | **2 lands** {B}2, `(you have 2 lands; no spell ... off {B} alone)` | KEEP (139 s) | **right - the floor beat the literal** |
| 146v126 | 7 | 3 lands, covers 4 | KEEP | right |
| 146v130 | 7 | **2 lands**, covers 2 | KEEP | right |
| 146v152 | 7 | 3 lands, covers 2 | KEEP | right |
| 146v162 | 7 | 3 lands, covers 3 | KEEP (208 s) | right |
| 152v123 | 7 | **1 land**, `(1 land is not enough for your cheapest spell at mana value 2)` | MULL (109 s) | right |
| 152v123 | (keeping 6) | 1 land, same clause | MULL | right |
| 152v123 | (keeping 5) | 4 lands, covers 2 | KEEP; bottomed Barkchannel + Tovolar's | right |
| 152v125 | 7 | 3 lands, covers 2 | KEEP | right |
| 152v126 | 7 | 4 lands, covers 3 | KEEP | right |
| 152v130 | 7 | 4 lands, covers 3 | KEEP | right |
| **152v146** | 7 | **3 lands** {W}3 {U}1, `(you have 3 lands; no spell in it is castable off {W}{U} alone)` | **MULL** | **WRONG** - three rungs above the floor; reply names a COLOUR (152-B) |
| 152v146 | (keeping 6) | 5 lands, covers 2 | KEEP; bottomed Teferi | right |
| 152v162 | 7 | 3 lands, covers 4 | KEEP | right |
| 162v123 | 7 | 2 lands, covers 3 | KEEP | right |
| 162v125 | 7 | 5 lands, covers 2 | KEEP | right |
| 162v126 | 7 | **1 land**, covers 2 Shield Spheres | MULL | right |
| 162v126 | (keeping 6) | 2 lands, covers 2 | KEEP; bottomed Master of the Feast | right |
| 162v130 | 7 | 3 lands, covers 2 | KEEP | right |
| 162v146 | 7 | **0 lands** | MULL | right |
| 162v146 | (keeping 6) | 4 lands, covers 2 | KEEP; bottomed Peer into the Abyss | right |
| 162v152 | 7 | **1 land**, `(1 land is not enough ... mana value 2)` | MULL | right |
| 162v152 | (keeping 6) | 4 lands, covers 1 | KEEP; bottomed an Island | right |

**deck146 mulligans at two or more lands: 0 of 6 - P1 PASS for a second corpus** (two of the six
keeps are 2-land hands under the coverage line). **deck162: 0 of 3 mulligans at 2+ lands.**
**deck152: 1 of 4** - the same shape for a fourth corpus, adjudicated in `deck152/edit-texts.md`
under 152-B. Chains never went past (keeping 5); **no mull-to-zero, and no repeated mulligan
against a guide floor** (`152v123`'s chain was 1-land at both looks), so **the new-baseline
rule's OWNER-QUESTION trigger did not fire this wave and none is raised.**
**Land drops: 0 "Play no land right now" answers at any of my seats.**
Bottom asks: 4 at my seats (`152v123` s4 two cards, `152v146` s3, `162v126` s3, `162v152` s3) -
all four sent the most expensive card(s) the keep could not cast; `152v123` sent a fourth land
from a five-land keep, which is the guide's own rung.

---

## deck146 - Orzhov dungeon midrange (5-1), per game

- **v125 (W 22/-6 T19)**: clean. Two cleanup discards; the Emeria's Call rung fired (s20 `PUT: 6`
  with Emeria's Call at row 6 - wave-53 P2 was 0 of 3, this is **1 of 1**).
- **v130 (W 14/-2 T33)**, **v152 (W 7/-11 T21)**, **v126 (W 5/-5 T33)**: clean. `v126` is my
  largest seat (98 records, 96 decisions, 50.2 min) and carries the corpus's largest prompt.
- **v162 (W 4/-1 T29)**: the DRAW PRICE game. Four windows (s46/s49/s53/s55, turns 25-29, life
  8/7/8/6) with a full cast menu declined under `[DRAW PRICE: casting this draws YOU 7 cards
  (their Forced Fruition), and their Underworld Dreams deals you 7 - you would be at 1]`, and
  the game won on attacks alone. See 146-A - a WHAT THIS BOUGHT, not a break.
- **v123 (L -3/12 T10)**: the one loss, and it has a window. s13, T9, 20 life, own line 2
  creatures, their line `23 creatures, 23 of them without a restriction against attacking` behind
  Intruder Alarm + Thraben Doomsayer. Menu: Acererak (5/5), Pelakka Predation, Soul Shatter
  (`{right now: 23 tied at MV 0 (their highest)}`), nothing. Answered **Pelakka Predation** - a
  hand attack against a board. Dead at -3 the next turn. 146-C. The game also carries E-2.

**Fractions (deck146).** Attack windows **41**, A-lines **105**, sent **104**, `ATTACK: none`
**2** (`v126` s63, `v162` s12 - both with 0 legal profitable lines; not the P2 shape). Blocks **5
windows**, 1 `BLOCKS: none`. Own-turn `Cast nothing` on a menu with a live non-dead row: **14**,
of which **13 are correct** (4 DRAW-PRICE closes, 4 Acererak-only menus where Acererak bounces
itself, 2 lethal-attack turns, 2 legend-rule duplicates, 1 tapped-out end step) - **P3's deletion
control PASSES**. Cleanup discards **7**: Emeria's-Call rung 1/1, general most-expensive-first
**5 of 7** (146-B). `[DRAW PRICE:` rows **26**, takes **1** (a 1-card row, not a KILLS row);
`this KILLS you` **9 rows, 0 casts**. `[legendary:` 2 rows / 0 takes. `{this hits YOUR
permanent}` 19 rows / 0 takes. Silverquill Command **cast twice corpus-wide, both at this deck**.
`- Paid` receipts 2,129 renders / **0 no-source**.

---

## deck152 - Bant human midrange (5-1), per game

- **v162 (W 18/0 T9)**, **v130 (W 29/-3 T11)**, **v123 (W 29/0 T9)**, **v125 (W 29/-11 T19)**:
  clean; `v123` is the mull-to-5 that won 29/0.
- **v126 (W 20/-10 T15)**: the 152-A game. Three attack windows (s28/s34/s44) with one name in
  `their untapped blockers` and 8 held A-lines carrying `(you kill it, your attacker lives)` -
  the wave-53 P4 shape, but every tag also carries `(blocking trigger: they gain 3, and their
  converter takes 3 off you)` under Sanguine Bond. The two unblockable fliers alone took them
  23 -> 13 -> 2 and the seat never dropped below 20. Adjudicated in edit-texts as a rule
  staleness, not a pilot break.
- **v146 (L -11/7 T21)**: the one loss, and it has a window. s58, T19, the corpus's only
  `all_assignments_illegal`: two menace 2/1 Spiders, two blockers, answer `BLOCKS: B1:A3, B2:A4`
  - one blocker on each, so neither is blocked and the whole line is void. **Pure strategy**: the
  render states the menace rule three times and the guide already carries the affordance sentence
  keyed to its exact literal. 152-D. The game also carries s63's `latched_row_mismatch` on the
  OPPOSING seat's log (E-3) and the mulligan break at s1.

**Fractions (deck152).** Attack windows **27**, A-lines **65**, sent **52**, `ATTACK: none`
**0** (P5 holds). Held A-lines carrying `(you kill it, your attacker lives)`: **8, all 8 under a
life-loop tag** (152-A). Blocks **6 windows**, `BLOCKS: none` **2**, repeated B-numbers **0**,
`multiblock_reask` **0**, `all_assignments_illegal` **1**. `{paying this taps:` **6 rows / 1
take**, the take a ONE-name clause (wave-53 P6's must-stay-0 half: **0**). `[legendary:` **8 rows
/ 0 casts**, four of them consecutive own-main `Cast nothing` answers whose only live row was a
duplicate Teferi. `{kills:` 2 rows / 1 take. Bottom asks 2. `- Paid` 1,008 / 0 no-source.

---

## deck162 - UB Draw-and-Die (2-4), per game

- **v125 (W 22/0 T19)**, **v130 (W 12/0 T11)**: the engine assembled on curve; every Howling Mine
  decision correct (table in `deck162/edit-texts.md`).
- **v126 (L 0/38 T15)**: **the window.** Six Caress declines T4-T8 (headline 3), first punisher
  on turn 10, opponent at 26 before the seat cast a spell that mattered. 162-A.
- **v146 (L -1/4 T29)**: **the window.** Four Fog Bank declines T4-T6 plus s15's explicit "keep
  mana open" on the seat's own main for a sorcery-speed punisher (162-A's second half). Forced
  Fruition held correctly at K of 0 twice (s24, s29). The seat did land its engine and took the
  opponent to 4; deck146 won it by refusing to cast under the tax the seat built (146-A).
- **v123 (L -32/13 T12)**: **no window.** Mine T3, Caress T5, Master T7, Underworld Dreams T9,
  Fate Unraveler T11 - the engine on schedule - against a board that went from 1 creature at T9
  to **24** at T11 behind Bloodline Keeper + Intruder Alarm. At s11 (17 life) the alternatives
  were a Fog Bank that stops one of 24 and a second Caress.
- **v152 (L 0/18 T9)**: **no window.** 8 decisions, mull to 6, first punisher turn 6 at three
  lands, dead turn 9 to a curve-out.

**Fractions (deck162).** `Cast Howling Mine` rows **16**: untagged (first Mine) 4, taken 3 (two
at K of 0); the one decline is a punisher collision on an unaffordable menu. Tagged
`[second copy:` at K of 0: 1 row, **0 takes** (brake fired). Tagged at K >= 1: 11 rows, 4 takes.
**Wave-53 P8 PASSES both halves; the first-Mine over-brake lane CLOSES** (11 windows -> 0).
Own-turn `Cast nothing` with a live row **16**, of which **10 are 162-A** and 6 are correct
(2 Forced Fruition at K of 0, 2 Master of the Feast at K of 0 under the carve-out, 2 tapped-out).
Attack windows 3, A-lines 3, all sent. Blocks 5 windows. Bottom asks 3. `[second copy:` **20 rows
/ 5 takes**, all at K >= 1. `- Paid` 308 / 0 no-source.

---

## THREE-NUMBER LITERAL AUDIT (#188) - every literal lanes A-E changed that renders at my seats

| literal | old-string renders | new-string renders | takes under the new string |
|---|---|---|---|
| HOLD row: `Hold priority - do not ask me again this turn...` -> `Pass priority, and do not ask me again this turn unless the board changes (any change re-opens this window; you give up no cast)` | **0** (no `Hold priority` row anywhere in the corpus) | **426 corpus / 8 mine** | **43 corpus (10.1%) / 3 mine** |
| `0. Pass priority (take no action this window)` | n/a | 140 | see E-1 |
| mulligan coverage: `... would not cover any spell in it.` (bare) -> `... in it (<cause>)` | **0** (was 10 in wave 53) | **11** - count family 4, colour family 7 | 4 ships, 3 correct (152-B is the fourth) |
| `[DRAW PRICE: casting this draws YOU N cards (their <card>), and their <punisher> deals you N - you would be at K]` | n/a | **20 corpus / 20 mine**; DRAW PRICE rows total 44 corpus / **43 with the `- you would be at` tail, 0 without** | **0 casts from any of the 20**; 5 corpus takes are all 1-card rows |
| `this KILLS you` | n/a | **9 (all mine)** | **0 casts** |
| `{modes live right now: ...; dead (no legal object right now): ...}` | **0** unannotated modal rows | **17 corpus rows; 11 of 11 Silverquill Command rows** (was 0 of 28) | **2 casts, both mine** (was 0) |
| `{kills:` / `{removes:` / `{kills 0 of the N legal targets` | n/a | 75 / 38 / 12 corpus; 2 / 9 / 0 mine | 13 / 11 / 0 corpus; 1 / 7 / 0 mine |
| attackers header `They have N untapped creatures able to block ...` | prompts without it **0** | **71 of my 71 attack windows** | n/a (header) |
| `while ahead on LIFE` | n/a | 18 corpus / 6 mine | **0 renders at a record where `my_life <= opp_life`** - lane E's only-render-FALSEHOOD is gone |
| `[second copy:` | n/a | 97 corpus / 20 mine | 12 corpus / 5 mine, **all at K >= 1** |
| `[legendary:` | n/a | 29 corpus / 14 mine | **1 corpus / 0 mine** |
| `[repeat:` | n/a | 56 corpus / **0 mine** | 17 corpus |
| `[you declined this exact list N times already this turn]` | n/a | **327 corpus / 4 mine** (N>=3 on 274) | n/a (annotation) |
| `- Paid` | n/a | **3,445 at my seats** | **0 no-source** |
| `FORCED SACRIFICE` / `your own effect` on a permanent not controlled | n/a | **0 windows at my seats** | UNTESTED |

---

## PREDICTION SET A - wave-53 guide edits on my decks

| id | deck | prediction | verdict | evidence |
|---|---|---|---|---|
| P1 (146-A) | 146 | own-turn `Cast nothing` on a menu holding a `Cast Silverquill Command` row while the log shows a MV-2 creature in the graveyard: 0/N | **PASS** | lane C's D5 gave the row `{modes live right now: ...}` 11/11; the card was **cast twice** (`146v126` s47, `146v152` s41) against 0 of 28 last wave. Wave-53 headline 2 is closed |
| P2 (146-C) | 146 | `discard` records whose PUT line omits Emeria's Call while it is on the list: 0/N | **PASS 0/1** | `146v125` s20, Emeria's Call at row 6, `PUT: 6`. Only one window; the general order broke 2 of 7 (146-B) |
| P3 (146-T control) | 146 | removing the show-the-work ritual costs nothing: own-turn `Cast nothing` with a live undead row where the cast order names a matching entry: 0/N | **PASS 0/14** | 13 of the 14 are engine-correct closes (DRAW PRICE, bounce-loop, lethal turn, legend rule); the 14th (`146v123` s13) is a different rule, 146-C |
| P4 (152-A) | 152 | attack windows under a ONE-name converter line holding an undeclared A-line tagged `(you kill it, your attacker lives)`: 0/N | **FAIL 3 windows / 8 A-lines by the letter; RULE STALENESS by the evidence** | `152v126` s28/s34/s44. Every held tag also carries `(blocking trigger: they gain 3, and their converter takes 3 off you)` - a clause that did not exist when the rule was written. Fliers-only took them 23->13->2; seat won 20/-10 at 20 life. Re-keyed to arithmetic (152-A), not restated |
| P5 (152-B) | 152 | mulligans at two or more lands: 0/N; replies naming a colour or source as the ship reason: 0/N | **FAIL 1/4; FAIL 1** | `152v146` s1: 3 lands, `(you have 3 lands; no spell in it is castable off {W}{U} alone)`, reply *"Mulligan for a hand with at least one green mana source"*. Fourth corpus, and the trigger literal changed this wave - 152-B |
| P6 (152-C) | 152 | `{paying this taps:` rows taken in Upkeep or Main 1 naming TWO or more creatures: 0/N; one-name rows on a level counter / activation / noncreature: 0/N | **PASS 0/6; PASS 0** | 6 rows, 1 take, a one-name body cast |
| P7 (162-A) | 162 | at life <= 10 with fewer creatures than their unrestricted attackers, noncreature rows taken: 0/N; two affordable creature rows and only one taken: 0/N | **UNTESTED; UNTESTED** | no deck162 cast ask this corpus met the life-and-count precondition - `162v123` was at 17 and `162v126` never got below 20 until the last turn |
| P8 (162-B) | 162 | untagged first-Mine rows declined: 0/N; tagged rows taken at K of 0: 0/N | **PASS (1 of 4, and it is a punisher collision); PASS 0/1** | full row table in `deck162/edit-texts.md`. The over-brake lane closes; 11 windows -> 0 |

## PREDICTION SET B - engine lanes where my seats had windows

| lane | prediction | verdict | evidence |
|---|---|---|---|
| **A** (D2, D12) | HOLD-shaped echoes execute the row or are stamped N/N; HOLD takes tripping `plan_choice_conflict` 0; `hold_windows_skipped > 0` on every gameend where the row was taken; HOLD takes in declined-note N>=3 windows >= 20%; identical-declined runs < 15%; opp-turn casts >= 90% of 58; served PLAN blocks > one paragraph 0 | **PASS; PASS; FAIL 7/8; FAIL 8.0%; PASS 12.2%; FAIL 26 (45%) absolute / flat as a rate; PASS** | Takes on 8 gameends, `hold_windows_skipped` non-zero on 7 - the counter-example is `123v130` (1 take, hws 0). declined-note N>=3 windows **274**, HOLD taken in **22** = 8.0% against >= 20%. Opponent-turn casting windows 403, casts 26 = 6.5% (wave 53: 58/1,052 = 5.5%) - the absolute target fails because the corpus is 36% smaller, the RATE is up. My seats saw only 8 HOLD renders / 3 takes, so most of this is adjudicated corpus-wide |
| **A** (`pass_hold_ambiguous`) | new stamp, no field evidence; WATCH | **NOT the pure false positive the brief records** | 16 stamps. **13** are `CHOICE: 0 (pass)` executing row 0 - false positives of the reword, as the brief says. **3 are not**: `146v126` **s54, s56, s57** are `CHOICE: 3 (Pass priority)` executing row **3**, the HOLD row. Both rows now begin "Pass priority", so the parenthetical alone cannot separate them and only the index did. See `deck146/general-suggestions.md` R-1 |
| **B** (D13/D14/D15/D9) | `latched_row_mismatch` on every record whose index AND parenthetical differ from the executed row; a stamp on a legitimate name-over-index remap is a FAIL | **FAIL at my seat - the one I own is a FALSE POSITIVE caused by a mis-recorded row** | `146v152` s63: reply `CHOICE: 3 (Cast Silverquill Command)`, row 3 IS `Cast Silverquill Command`, index and name AGREE - yet the record says `choice: 5`, `chosen_text: "Cast nothing right now"`, `answer_replaced: false`, `coded_answers: 1`. stderr lines 2044-2070 show the spell WAS cast, modes chosen (`chose 7 of 9`) and targeted (Triumphant Adventurer in the graveyard). The GAME was right and the TELEMETRY was wrong; the stamp then fired on its own bad fields. E-3 |
| **B** (parse notes) | `decision_reversed_in_prose`, `stale_echo_in_range`, `named_row_not_offered` | **PASS at my seats** | 1 `decision_reversed_in_prose` at `146v126`, 1 at `152v146`; 0 `named_row_reask`, 0 `named_row_not_offered`, 0 `stale_echo_in_range` |
| **C** (D4/D10/D11/D18/D5) | rows enumerating a player whose `{kills}` omits it 0; `[DRAW PRICE:` rows without `- you would be at K` 0; casts from a `KILLS you` row 0; unconditional-removal rows with `{kills:}`/`{removes:}` N/N; priced rows on a multi-priced menu without a fit clause 0; modal rows with the live/dead clause N/N | **PASS 0; PASS 0/44; PASS 0/9; PASS; PASS; PASS 17/17 and 11/11 Silverquill Command** | The D10 tail is what won `146v162`; the D5 clause is what closed my wave-53 headline 2. Both are the strongest positives of the wave |
| **D** (D3/D6/D8/D25) | loss-framed asks whose destination is the battlefield/hand 0; `your own effect` on a permanent not controlled 0; ordinal-free row repeated > 2x 0; de-duplicated-length-1 asks 0 with `identical_option_asks_resolved > 0` | **PASS 0/1; UNTESTED; PASS; PASS** | 1 `CHOOSE A CARD TO PUT ONTO THE BATTLEFIELD` header at my seats, correctly gain-framed. No forced-sacrifice window at any of my seats. `identical_option_asks_resolved` = 4 on `146v126`'s gameend and 1 elsewhere (5 corpus-wide); **0 byte-identical option lists at my seats** against 14 at one seat last wave - my wave-53 E-3 is closed |
| **E** (D21/D17/D16/D20/D19) | `while ahead on LIFE` at `my_life <= opp_life` 0; attackers prompts without the blocker-count header 0; `would not cover any spell` without a cause clause 0; colourless fetch rows 0; `ability: X's X` stack lines 0 | **PASS 0; PASS 0/71; PASS 0/11; UNTESTED; PASS 0** | The corpus's one render FALSEHOOD is gone. The cause clause passes its own falsifier - and creates 152-B's problem, which is a guide re-key and R-2, not a lane failure |
| **F** (D7, D38) | `choice: -1` with no fallback 0; supervisor `hang` verdicts 0; reveal force-close budgets never reached | **PASS 0; PASS 0; PASS - but see E-2** | 1 `choice: -1` at my seats (`146v123` s15) and it carries `fallback: stale_livelock` + a paired `recovery`. The reveal guard's budgets were never approached because the failure was a LIVELOCK, not a stall |
| **AA** (Vita stall floor) | 0 AI-window declines on GPT seats within 1,200 s | **FAIL - 468 corpus-wide, 174 in my games** | headline 1. Every release at exactly 12,002 ticks; max latency at my seats 347 s |
| **G / H / I / J / K / N** (audit lanes) | crash/ASAN signatures, verdict drift, RSS, visual parity, Baka parity, Vita textures | **UNTESTED at my seats** | 0 crashes, 0 hangs, 0 timeouts in my 13 games; no Baka-executed decision at my seats except E-2's handoff; no card in decks 146/152/162 is an aslongas/lord/Isleback shape I could score lane H on |
| **L / M** (GPT transport, ask cache) | prompt bytes identical to wave-53 shapes; `http_error` 0; cache hits per game with turn-boundary clears | **PASS on bytes and http_error; UNTESTED on cache** | 0 `http_error` / `http_status` records; max prompt 32,215 vs wave 53's 32,068 on comparable arcs. The cache-hit counter is not in the seat records, so I cannot score it - and note headline 1 means an unknown share of calls were re-issued, which would inflate any naive hit rate |

---

## ENGINE / RENDER ITEMS (ranked; repro = file, seq or stderr line)

**E-1 (HIGH, correctness + efficiency) - lane AA's stall floor releases GPT-owned interrupt
windows on a tick-only budget.** Repro: `game-162v130-1788425042.stderr` lines 412-418 (a
Howling Mine trigger window released at turn 5 phase 3, answer dropped) and 490-507 (four
releases and four drops across turn 6 phases 2-3); `game-152v125-1788425038.stderr` (39 releases,
40 drops in one game). Numbers: 468 releases, **all at exactly 12,002 ticks**, 488 stale drops,
174 of them in my 13 games; 20 released windows are `NextGamePhase (Main phase 1)` and 6 are
named spell windows. Max latency at my seats 347 s against a stated 1,200 s LLM-seat allowance.
Ask: enforce the wall-clock half of the `ticks AND seconds` conjunction on the GPT path, and give
lane AA a disable flag - it is the only output-affecting change in this wave's set without one.

**E-2 (HIGH, correctness) - a reveal chooser livelocks and the six-drop guard hands the decision
to Baka.** Repro `146v123` seq 15 (`kind: reveal`, `fallback: stale_livelock`, `choice: -1`,
`latency_ms: 0`, `recovery` at s16) with `game-146v123-1788425054.stderr` lines 1335-1343: six
consecutive `dropping stale async answer` with no release, no phase step and no other engine line
between them, then the heuristic handoff. Distinct from E-1 (no `releasing so the game advances`
line anywhere in that game). The decision lost was a four-row Pelakka Predation reveal with
Damnation eligible. Ask: pin whether the reveal chooser re-registers a new window generation per
re-ask; the guard is masking a loop, not bounding a slow answer.

**E-3 (MED, observability) - `choice` / `chosen_text` can record a row the engine did not
execute, and a wave-54 stamp then fires on the bad fields.** Repro `146v152` seq 63 vs
`game-146v152-1788425074.stderr` lines 2044-2070. Record: `choice: 5`,
`chosen_text: "Cast nothing right now (combat comes next this turn)"`, `latched_row_mismatch`.
Reply: `CHOICE: 3 (Cast Silverquill Command)`, and row 3 was `Cast Silverquill Command`. stderr:
`AIPlayerGPT: casting Silverquill Command (model's pick)` -> `Choose an option ... chose 7 of 9`
-> `targeting ... Triumphant Adventurer ... [your graveyard]` -> resolved. So this is **one of the
two `latched_row_mismatch` firings the brief asks to adjudicate, and it is a FALSE POSITIVE**:
the row was executed exactly as named, and the stamp fired because the recorded fields disagreed
with the reply. Everything downstream that reads `chosen_text` (this seat file's own take counts
included) is affected. The other firing is `123v125` s147, not my seat.

**E-4 (MED, render) - the mulligan coverage clause prints a colour verdict without its scope.**
Repro `152v146` seq 1: `Playing every land in this hand would not cover any spell in it (you have
3 lands; no spell in it is castable off {W}{U} alone)`. 11 renders corpus-wide, 7 of them this
colour family, and the one three-land ship of the corpus is under it. The clause is TRUE and the
trust doctrine says the pilot must believe it; the defect is that a turn-one statement is
rendered in the grammar of a hand-level verdict, directly above a Keep/Mulligan ask. Ask: append
the scope (`... off {W}{U} alone on turn one`) or split the two families explicitly. See
`deck146/general-suggestions.md` R-2.

**E-5 (MED, render) - the HOLD row's short name collides with row 0's, and three replies
resolved only by index.** Repro `146v126` seq 54, 56, 57 (`CHOICE: 3 (Pass priority)` ->
executed row 3, the HOLD row) against seq 55 (`CHOICE: 0 (pass)` -> row 0), all four stamped
`pass_hold_ambiguous`. This is the correction to the brief's corpus note. Ask: change the HOLD
row's leading words or key its name-match on the tail clause. R-1.

**E-6 (LOW, render) - the blockers ask has no count header for the menace constraint.** Repro
`152v146` seq 58. The menace rule is stated three times per blocker and the seat still spread two
blockers across two menace attackers; the whole answer was voided. Ask: a D17-style header,
`2 of the 4 attackers need TWO or more blockers each; you have 2 blockers.` R-5.

**E-7 (LOW, observability) - `all_assignments_illegal` names no offending pair.** Same repro.
Ask: carry the pairs and the reason in the record, as `plan_choice_conflict` now does. R-6.

**Narrated actions with no decision record on my seats:** 0. Every `choice: -1` has a paired
`recovery` (1/1 at my seats, 10 corpus-wide against 23 fallbacks - the excess are fallbacks that
executed a row, which is the designed behaviour).

---

## LATEGAME SPECIMEN CANDIDATE
`1788425073-ai_baka_deck146-0x55cbe78957e0-vs-ai_baka_deck162.jsonl`, **seq 46-58** (turns
25-29): four full cast menus declined under `[DRAW PRICE: ... you would be at 1]` at 8, 7, 8 and
6 life against Forced Fruition plus two Underworld Dreams; a 14-row cleanup discard at s47
(`PUT: 1, 4, 6, 9, 10, 12, 14`, seven cards including a land); and the game won 4/-1 on attacks
alone. It carries lane C's D10 tail, lane C's D5 modal clause, the D18 fit clause and a
seven-card discard in one arc, and it is the clearest evidence in the corpus that a render clause
changed a decision.
Second choice, for the ENGINE seat: `1788425075-ai_baka_deck146-0x5639d32006f0-vs-ai_baka_deck126.jsonl`
**seq 54-57 and 85-86** - the three `pass_hold_ambiguous` HOLD takes (E-5) and the
`plan_choice_conflict` on a 12-row Kaya loyalty menu with a clean re-ask, plus the corpus's
largest prompt (32,215 chars at s86).

---

## ROTATION EVIDENCE (open lanes, not the record - #167)
- **deck146 - NO EXIT, but the lanes are smaller than they were.** 5-1. Wave-53's two big lanes
  both closed from the ENGINE side: 146-A's unsatisfiable literal was replaced by D5's modal
  clause (11/11, 2 casts), and 146-C's cleanup rung fired 1/1. What is open is one first-statement
  entry (146-A, the spell tax), one superlative re-key (146-B), and one recognise->do (146-C)
  from the deck's only loss. Three cheap keys, all with a corpus behind them - one more wave.
- **deck152 - NO EXIT, and the mulligan lane is now a RENDER lane.** 5-1. P4 turned out to be
  rule staleness rather than a break; P6 and the block rules held; the only new guide finding
  (152-D) is a failure shape under a rule that already renders. The mulligan floor has broken on
  the same shape in four corpora and the trigger literal changed underneath it this wave - if
  152-B's family re-key holds AND E-4 lands, the lane is finishable in wave 55.
- **deck162 - NO EXIT, and the record is the worst in the pool (2-4), but the diagnosis is
  clean.** The lane that was open (first-Mine over-brake) CLOSED at 11 windows -> 0. The lane that
  opened is its exact mirror - a brake keyed to a category firing on cards outside it, 10 windows
  across the deck's two window-carrying losses - and it now has a row-clause key (`{feeds:`) with
  a built-in control (the two correct Forced Fruition brakes must stay). Two of the four losses
  are no-window matchup losses in which the deck feeds a token engine; those are a deck-list
  question, not a guide one, and I have not treated them as findings.

---

## FALSIFIABLE PREDICTIONS FOR WAVE 55

**P1 (146-A).** deck146 casts from any row carrying `[DRAW PRICE: casting this draws YOU N cards`
where the row's `- you would be at K` reads K <= 1: **0/N** (was 0 of 20 - must stay). Turns under
that tax in which the seat declares no attackers while it controls an untapped creature able to
attack: **0/N** (the scope half; was 0 of 4).

**P2 (146-B).** deck146 cleanup `discard` records whose PUT line sends a card whose printed mana
cost is LOWER than a card left on the numbered list: **0/N** (was 2 of 7).

**P3 (146-C).** deck146 own-turn casts of a discard spell (Pelakka Predation) in a window where
the opponent header's "N of them without a restriction against attacking" exceeds the seat's own
creature count by 3 or more, while a creature row or a removal row is on the same menu:
**0/N** (was 1 of 1, and it lost the game).

**P4 (152-A).** deck152 undeclared A-lines whose tag reads `(you kill it, your attacker lives)`
with **no** `(blocking trigger:` clause inside it: **0/N**. Undeclared A-lines whose tag DOES
carry a `(blocking trigger: they gain G, ...)` clause and whose printed power exceeds 2G:
**0/N** (was 8 held, all under a gain clause, all at or below 2G).

**P5 (152-B).** deck152 mulligans taken under a coverage clause of the COLOUR family
(`(you have N lands; no spell in it is castable off {X} alone)`) at any land count: **0/N**
(was 1 of 1 window). Mulligans under the COUNT family at ONE land: unchanged, still correct.
Replies naming a colour or a source as the ship reason: **0/N** (was 1).

**P6 (152-D).** deck152 `blockers` answers assigning exactly ONE B-number to an A-number whose
row carries `no 1-on-1 result exists`: **0/N** (was 2 of 2, in the corpus's only
`all_assignments_illegal`). `all_assignments_illegal` records at this seat: **0/N**.

**P7 (162-A).** deck162 own-turn `Cast nothing right now` answers on a menu whose only non-pass
row prints **no** `{feeds:` clause: **0/N** (was 10 across two games). Casts of a row that DOES
print `{feeds:` at `converters on your battlefield: 0` with a Mine already on the line:
**0/N** (was 0 - the control that must stay).

**P8 (162-B).** deck162 replies containing "keep mana open", "hold mana" or "save mana for" on a
record whose `phase` is the seat's own Main phase: **0/N** (was 3).

**P9 (E-1).** With the stall floor's wall-clock half enforced on GPT seats: `ActionStack:
interrupt window held by Player N ... releasing` lines on a GPT-vs-GPT game: **0/N** (was 468,
every one at exactly 12,002 ticks); `dropping stale async answer` **< 60 corpus-wide** (was 488,
wave 53: 63).

**P10 (E-3).** Records whose `chosen_text` names a row the stderr shows was not the one executed:
**0/N** (was 1 of 1 examined). `latched_row_mismatch` stamps on a record whose reply's index AND
parenthetical both match a real row: **0/N** (was 1 of the 2 firings).

---

## WHAT I DID NOT CHECK
- **Scryfall was not fetched.** Every card fact above is read from the render's own `{card text:`
  / `{effect:` fields and, where the mechanism mattered, from `bin/Res/sets/primitives/*.txt`:
  Howling Mine, Liliana's Caress, Fog Bank, Shield Sphere, Underworld Dreams, Fate Unraveler,
  Ob Nixilis the Hate-Twisted, Forced Fruition, Dictate of Kruphix, Master of the Feast,
  Silverquill Command, Pelakka Predation, Acererak, Nadaar, Barrowin, Kaya, Lolth, Emeria's Call,
  Sigarda, Katilda, Pride Guardian, Sanguine Bond. Where a claim rests on a rules interaction
  (menace, Sanguine Bond's converter clause) I used the engine's own printed parenthesis, which
  is the surface the pilot decides from.
- **E-1's fix is not pinned to a line of source.** I read the stderr and the seat logs and did
  NOT open `AIPlayer.cpp` / `ActionStack.cpp`; the tick-only conclusion rests on 468 identical
  12,002 values plus the latency ceiling, which is strong but is inference, not a code read. The
  ENGINE seat owns the mechanism. Same for E-2's generation-key hypothesis.
- **Lane M's cache-hit counter is not in the seat records**, so its prediction is scored UNTESTED
  rather than passed - and E-1 means any hit rate measured this corpus is contaminated by re-asks.
- **Lanes G/H/I/J/K/N have no window at my seats** - reported UNTESTED, not passed. In particular
  no deck of mine runs an aslongas/lord/Isleback shape for lane H, and no Baka decision executed
  at my seats except E-2's handoff.
- **`{right now:` at my seats is 18 renders / 7 takes**, all on `{removes:` rows; I did not audit
  the 975 corpus-wide renders of that literal outside my seats.
- **Opponent-seat logs** were read only where they narrate my seats' effects (deck162's own view
  of `146v162`, and `146v152`'s stderr for E-3). The six opposing guides were not scored.
- **I did not re-read the 32,215-char prompt at `146v126` s86 beyond its option block**, and I did
  not measure per-seat prompt-size growth against wave 53.
