# Core-prompt / render proposals from the deck162 seat (wave 54)

## R-8 (MED) - a two-row menu whose only spell is priced identically for five consecutive windows
is the cheapest place left to save model calls
`162v126` s7-s12 and `162v146` s7-s11: the identical two-row ask (`1. Cast <one card>` /
`2. Cast nothing right now`) re-issued at Main 1 and Main 2 of the same turn, then again next
turn, with an unchanged board and an unchanged answer. These are the seat's OWN main phases, so
the HOLD row does not render there and lane A's turn-scoped suppression cannot reach them.
Ask: extend the `[you declined this exact list N times already this turn]` annotation - which
currently appears on 4 of my 630 decisions - to the same-turn Main 1 / Main 2 pair, so the second
ask of a turn carries the count. It is an annotation, not a suppression: the choice stays open.
(The suppression version of this belongs to the core loop and to the owner's largest-first /
no-hard-caps rulings; I am asking only for the note.)

## R-9 (LOW) - the `{feeds:` clause is the corpus's best-designed row annotation; say so in the
render truths so it is not trimmed
`{feeds: the opponent draws N extra card(s) per turn; converters on your battlefield: K - <names>}`
and its K-of-0 long form (`nothing of yours punishes their draws or discards yet - the cards you
hand them are free until a converter is out`) carry an entire deck's central rule on the row that
needs it, and its ABSENCE on non-gift rows is now this deck's brake key. 20 `[second copy:` rows
and 5 takes at this seat, all at K >= 1. Nothing to change; recorded so a later lane does not
collapse the clause onto a header, where the absence signal would be lost.

## R-10 (LOW) - the K number counts Liliana's Caress unconditionally, and the guide has to
un-count it by hand
The row prints `converters on your battlefield: K - <names>` counting Caress, but a lone Caress
converts only DISCARDS, so the guide carries a hand-written qualifier ("K counts only while the
Opponent hand size line reads 7 or more"). Ask: have the row do that arithmetic, e.g.
`converters on your battlefield: 1 - Liliana's Caress (discards only; their hand is 5, so this
converts nothing yet)`. It is the same class of work D4/D5/D10 already do on other rows, and it
would delete a paragraph from the smallest guide in the pool. No window in this corpus turned on
it - filing it as an ask, not a defect.
