# Wave-43 synthesis (2026-08-24)

## The headline: owner narration fixes + wave-43 engine lanes VALIDATED
All three prediction sets adjudicated. Owner fixes: ALL PASS (rules-text tails 0,
damage double-entries 0, (now N) 577/577, mana-hint tails 0 — note true delta baseline
was 67, the 2036 was a cumulative-prompt measurement artifact). Engine lanes: menace
attack-side PASS / defender-side vacuous (probe owed); reveal attribution 38/38;
transforms 6/6 with Day/Night carve-outs holding; DFC hand-flips 0; silent-cast ratio
1.00; X pricing 109/109; counter target text 57/57 (R4 gap CLOSED and USED — counter-
on-sight 10/10); owner tags 946/946; observer parity converged; run collapse 0; two
lexicon FAILs root-caused (teferieffect stack path; dungeons live in SIDEBOARD).

## THE INSTRUMENT FINDING OF THE CAMPAIGN: RIGGED OPENING HANDS
Rules.cpp OptimizedHand stacks every AI-vs-AI hand (3 lands+1 creature+3 spells,
42/42) and pins agressivity+100. EVERY mulligan verdict in every wave is retro-scoped
VACUOUS; heuristic-fallback attacks ran biased in all corpora. Wave-44 step-1 ships
WAGIC_SELFPLAY_FAIRHAND=1 (opt-in env; default unchanged for corpus comparability
until the owner rules otherwise).

## Guide themes
- Printed-power floors are blind to ENGINE creatures (W6 — two deck125 losses).
- Resource-loop decks need library-state rules, not just life-state (W7/W8 — lost from
  98 life by decking with the Elixir untapped; X cap must price Staff upkeep draws).
- Team-attack rules need per-attacker stops and sole-attacker branches (S1/S2 — the
  R1 deletion's residue; J for deck123 which missed the wave-42 R3a class).
- Dead prose gets DELETED (S4: 1/66 lifetime arms; S6: 21 lines / 4 decisions) — the
  decision-value-per-token criterion now cuts both ways.
- A WRONG teach corrected by evidence (S3: Shield Sphere attrition is a KILL, not
  a waste).
- Render-vs-guide attribution matured: deck130 EDIT 2 falsified AT THE RENDER (X-pricing
  headline scope), fix routed to engine first, guide reworded to read the annotation.

## Deployments: 18 edits + 2 deletions via wave43/edit-texts.md (deploy lane).

## Rotation adjudication (recorded in wave39/rotation-selection.md)
NO EXIT (fifth consecutive): 146 has open S1/S2/S4 predictions; 162 under-sampled with
3-of-4 edits null; 152 confirmed misplay + queued fix; 123 construction case OPEN at
the owner; 125 three HIGH edits new; 130 render-coupled EDIT 6; 126 D9 new. Structural
nulls (mulligan floors, trample carve-outs) recorded as UNREACHABLE-IN-POOL, not
pending — new skill amendment.

## Owner items (routed)
1. deck123 CONSTRUCTION CASE (measurement-backed, seat-written): assembly 3/6,
   conversion 0/3 when assembled, ~15 inert slots vs this pool, mono-source-white game,
   unprotected 3-drop win-con. HIS Magic judgment; seats change no decklists.
2. DS+trample one-token call: automation legal-but-not-maximal (toughness vs life at
   GuiCombat.cpp:133); manual floor and hasLethalDamage already use life; flipping
   moves exactly two pinned asserts. HIS call under don't-touch-assignment.
3. {room effect:} rules text in history (19 lines) — deliberate keep? Rooms are opaque
   by name alone.
4. Specimen: deck125 vs 126 seq 119 (29KB; library-17-two-Staffs X window where the
   deciding number sat unremarked at the bottom).
