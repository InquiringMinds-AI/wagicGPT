# deck158 — general/core-prompt observations (wave-35)

Core bar: 28 consecutive PASSes; burden of proof is ON core. Nothing at this seat clears it this
wave — route is PASS. Two observations for the synthesis seat's cross-seat check only:

1. **Single-number misreads look deck-agnostic for the 35B tier.** All four arm-C failure events
   at this seat were P/T-order, damage-vs-toughness, mana-count, or the 0-life boundary — inside
   otherwise rule-quoting traces. If other FP8 seats show the same shape, the cheaper fix is
   RENDER verdicts on target lines (see this seat's notes.md P6) rather than a core arithmetic
   lesson; a core "(P/T): the second number is toughness" line is the fallback only if the class
   proves cross-seat AND the render lane is declined.
2. **The prompt's "every listed choice is legal AND PAYABLE" line lost once to a mana recount**
   (C-36 seq16-17). One event, one seat — not core-actionable; noted so a recurrence elsewhere
   can be pooled.
