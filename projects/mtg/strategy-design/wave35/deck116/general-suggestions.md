# deck116 — wave-35 core/general-prompt suggestions

**PASS** (consistent with the 28-consecutive-PASS bar). Nothing at this seat showed up
deck-agnostic enough to bill every deck for:

- The one-blocker-one-attacker override (vs105 seq25) is a constraint the core prompt ALREADY
  prints at the ask ("Assign each blocker to AT MOST ONE attacker" — quoted by the pilot and
  then dismissed). A core line already present and reasoned past routes to
  harness/representation (the re-ask, notes.md item 1), not to a second copy of the same
  sentence — repetition is not a mechanism.
- The printed-outcome-branch trust ("(you kill it, your blocker lives)" as computed result,
  deathtouch/first-strike included) would be a candidate core completion if a SECOND seat shows
  the same re-derivation residual; single-seat evidence cannot add core text. Recorded here as
  the cross-seat watch: FP8 re-derives combat outcomes beside the printed lines (fp8 review,
  "Residual risk"); synthesis should check whether other FP8 seats show it before considering
  a core completion of the existing outcome-line sentence.
