# deck105 — general-prompt suggestion (wave 35)

Core has 28 consecutive PASSes; bar unchanged; this is a flag, not a demand.

**The blockers-line suffix should yield to a deck guide that carries its own block calculus.**
The composed prompt's "NOT lethal: block only where the trade favors you; taking damage while
ahead is often correct" is generically sound and specifically wrong at this seat, whose guide
computes the block decision numerically. Two-arm evidence: the 4.75-bit pilot quoted the suffix
as license to override the deck rule (A vs36 seq13; leaned on it A vs139 seq12); the FP8
standing opponent refused it correctly but had to adjudicate the collision in-trace (C vs158
seq11: "This is just the system prompt's assessment..."). The deck guide now scopes it textually.
If a core/render change is ever taken: suppress or soften the advisory clause (keep the "NOT
lethal"/"LETHAL" verdict and the "you would be at N" number — the deck rule depends on those)
when the active deck guide declares its own blocking rule. Cheapest form: a per-deck flag in the
guide header the composer already reads. Not urgent — FP8 handles the collision — but it taxes
every block ask at any seat with a numeric calculus (deck152 flagged the same seam).
