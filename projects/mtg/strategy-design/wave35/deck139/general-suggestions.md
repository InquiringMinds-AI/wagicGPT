# deck139 wave-35 — core/general-prompt suggestions

**PASS** (routing toward the 29th consecutive PASS). Nothing found at this seat is core-shaped:

- G1/G2 and the poison-coverage failure are deck-specific teaches (mutate mechanics, this
  deck's host pool, the infect matchup) — all shipped in the revised guide.
- The shockland-ETB fallback cluster (notes.md N2) is a render/option-label item, not prompt
  doctrine.
- The FP8 pilot honored the reply protocol everywhere else (0 empty, 1 unparsed, 2 stale_echo
  in 195 decisions), and the stale_echo firings were the matcher working as designed against a
  label-anchoring gap — no protocol change proposed (owner ruling #2: do not propose
  reply-format changes).
