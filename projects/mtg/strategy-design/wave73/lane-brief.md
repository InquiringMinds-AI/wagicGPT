# Wave-73 step-one lane brief (base = master after wave72 known-bugs; evidence = wave72/known-bugs.md §N + wave72/engine-seat.md + wave72/deck<N>/review.md)

RULINGS THIS WAVE TOUCHES: 000 — reasoning only in the native channel; reply = PLAN then action, nothing else; no instruction licenses prose; no parser reads outside the two lines (reading a LABELLED number off the PLAN line is inside the line); caps never bound reasoning; the golden protocol TEXT (reply-protocol.txt / kReplyProtocol) and the thinking regime are owner-ruled — render brackets and row text are NOT the protocol and may change. 00 — full games. 3 — suite THREADS=1 0 failed (known flakes: lifeline, merrow_reejerey, intrepid_adversary_repeated_payment), PARSETEST 0 failed. Never remove, cap, or auto-answer a legal option; collapsing windows with NO legal response other than pass/hold loses nothing. No VPK. No AI attribution.

Standing mechanics: as wave71/lane-brief.md (worktree-only; `make -f Makefile.sdl -j8`; both link guards OK; binary freshness; every wagic run under `systemd-run --user --scope -q -p MemoryMax=4G -p MemorySwapMax=0 --`; suite THREADS=1 as a detached unit `w73-<LANE>-suite` waited with a FOREGROUND until-loop (no Monitor); count both failure strings; PARSETEST from bin; Edit tool for UTF-8 sources, byte-splice for MTGRules.cpp/GameObserver.cpp/primitives; U+FFFD 0; RED-on-base evidence + a falsifiable prediction per fix; commit with explicit paths, `git add -f` under bin/Res; write wave73/lane-<LANE>.md; touch `~/.gatelogs/W73_<LANE>_DONE`; weakest evidence; no corpora).

## Lane BY — the HOLD family + the window key (N1, N2, N5, N6, N9, N16)
N2 first (four seats lost turns or games to it): a hold EXPIRES at the holder's next untap (count `hold_released_turn`), the priority window ALWAYS prints an explicit pass row (fix the `baseIndex == 1` condition), the hold row's text says it lasts until your next turn, and the "cannot take it later" clause leads the brace (N9). N1: sibling key = board minus the answered-tag; state the covering rule and count every skip with both option sets. N5: one loop-scope wording shared by the LOOP COMPLETE header and the LOOP SCOPE clause (pin they agree byte-for-byte on the scope sentence); the `[LOOP RUNNING … HOLD answers all]` bracket only on rows in the chain. N6: generalise the running-chain collapse to any chain whose windows carry no legal response row other than pass/hold (count `chain_windows_collapsed`; fixture from 130v162 t10 if reproducible). N16: find why a main-1 casting window did not fire with two castable cards (152v146 t18) — fixture RED on base if it is a window-model gap.

## Lane BZ — the stop grammar + list truth (N3, N4, N7, N10, N11, N15)
N3: the repeat row's bracket states the literal `stop=N; M=N` form; `repeatPlanStopAndCurrent` reads ONLY the PLAN line and accepts the labelled forms listed in known-bugs N3 (pin each; MUST-NOT-MATCH a bare number and prose outside the PLAN line); RED = 123v162 seq 11 reply on base parses no stop. N4: rung menus sorted numerically (pin the 14-row menu). N7: mana cost on every reveal/search row. N10: worked-example cut at a token boundary with balanced quotes/parens (pin the 79 malformed shapes' class). N11: loyalty clause untruncated in the board frame. N15: the noop re-ask quotes what was matched (row number AND short name) and why.

## Lane CA — instruments + render residue (N8, N12, N13, N14, N17)
N8: `replay_run` one semantics (per window), both re-serve paths bounded and counted. N12: wire `reserve_decline_windows_noted`; the declined-count clause — MEASURE the ~25% mis-answers' cause on the wave-72 records (wave72/deck125/review.md A-4) before changing anything, then fix that cause. N13: the land-drop seam gets the attackers seam's plan-vs-board name check. N14: the four render items (animated-land death price, ghostform counter, MDFC back-face link, cannot-block-beside-blockers). N17: re-check the carried-plan heading — 2 replies still echo `YOUR PLAN:`; if the heading still contains that string in any form, change the RENDER (never the parser).
