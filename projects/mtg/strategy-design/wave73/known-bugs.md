# Wave-73 known bugs (corpus matchups-20260907-225637-final: 21 clean games = 14 original + 7 rerun after the pilot vLLM wedge; sources wave73/engine-seat.md + wave73/deck<N>/review.md)

Invariant 000 held: 2,338/2,338 decisions with native reasoning (the brief's 1,757 was ask-seam only), 0 fallbacks, 0 truncations, 0 hung; decisions/turn 2.52 (2.15 without the two deck-125 stall games). Wave-73 regression watch: every one of 101 `hold_released_turn` releases landed at the holder's untap (CB F2 PASS); `0. Pass priority` on 416/416 priority windows and hold-as-pass 12 → 5 (BY N2b PASS); all 366 `stop_reached_windows_skipped` are deck123's own creature-count stops (BZ N3 PASS; CB F3 UNTESTED — no non-count stop was ever stated); sibling 0 (F1's subset bar binds: 0 of 40 hold-opened pairs had an empty acting set) and chain 0 (0 of 416 priority windows had an empty acting row) — both safe-direction zeros; reveal rows 99/99 carry a cost; rung menus 0 mis-ordered; MDFC back-face link 29/29. All seven wave-73 guide EDITs installed (0f916bd69). Wave-72 guide edits PAID on 125 (own-turn hold violations 66% → 8%), 146 (Kaya exiled both loop halves), 152 (0 hold takes in 222 decisions), 162 (K-of-0 brake). Corpus incident: the pilot server froze at ~17h with the API still answering — see psp-work/NOTES.md 2026-09-08; O26 below is the harness half.

## O — routed to wave 74 (HIGH first)
- O1 HIGH — the ghostform gloss is FALSE on 56/56 renders (CB F5 inverted): `w73GhostformTriggerLive` scans the action layer, but Kaya's script grants the return trigger as a `transforms((,newability[@movedto(this|graveyard) …])) forever` ability ON THE CREATURE (planeswalkers.txt:1874), so the scan never finds it and prints `(a marker only - the ability that returned it is gone)` while the event line six lines up says `killing it is not removal`. Repro `1788839837-…deck126-…-vs-…deck146.jsonl` seq 16; deck146 seat seqs 13-53. Fix: judge liveness from the creature's granted abilities (the movedto trigger present = live); fixture for BOTH states (live → full gloss; abilities removed → marker), RED on base for the live state. (engine-seat HIGH-1)
- O2 HIGH — the declare-attackers window is silently skipped with legal attackers, UPSTREAM of `pendingCombatDecision` (the N-146s class, still live): `152v162` deck152 seat seq 9 (T5, Luminarch Aspirant untapped/unsick → next record is T7), `152v123` seq 21 (T15: Tracker 5/4 + Wolf vs 9 life and one blocker — the deciding decision of the seat's only loss) and T17; in all three the stderr has NO `[combattrace]` line of any reason for that seat/turn, so the tracer (GameObserver.cpp:409-425, fires inside the gate) cannot see it. Fix: an entry trace at MTG_PHASE_COMBATATTACKERS (turn, seat, hasLegalAttacker, whether chooseAttackers was called), then find the caller that never reaches the gate; a fixture RED on base if the shape reproduces (own turn, untapped unsick creature, prior-window trigger/cast). Excluded by the seat: turns where the seat tapped its creatures for mana (tracer prints `no-legal-attacker` correctly). (deck152 HIGH-1)
- O3 HIGH — Silverquill Command's `+3/+3 and draw` TARGET picker renders under the `FORCED SACRIFICE OF ONE OF YOUR OWN CREATURES … pick the creature you can best AFFORD TO LOSE` header — every sentence false, the instruction inverted; the model picked its worst body and the pump made a 7/7 by accident. `146v125` deck146 seat seq 60 (mode chosen at seq 59 had no sacrifice half). The correct `TARGET CHOICE for Silverquill Command - its "creature gains 3/3 and sacrifice creature" ability` header renders for the sibling mode pair at `146v126` seq 27. The header is keyed on the CARD having a sacrifice mode, not on the CHOSEN mode's effect on the target. Fix + PARSETEST contrast pin from the two seqs. (deck146 HIGH 1)
- O4 HIGH — the dedicated `Land drop:` menu hides an MDFC pathway's second face (`1. Play Brightclimb Pathway / 2. Play Plains / 3. Play no land`), the full two-face menu comes only at the NEXT window, so the drop is decided twice and the first answer anchors (`146v162` deck146 seq 3→4, lost the game; also deck152 seq 17→18, deck126 seq 23→24 and 31→32, deck130 seq 29→30). Fix: print both faces on the land-drop menu itself (or suppress the land-drop seam when the only land is two-faced) — never remove a face. (deck146 HIGH 2)
- O5 HIGH — the `ATTACK TOTAL … puts them at N. That is NOT lethal` verdict counts combat damage only and ignores the seat's OWN Sanguine Bond + a lifelink attacker: `126v130` deck126 seat seq 43 printed "at 3 … NOT lethal", the opponent finished at 1; with the −2 emblem the true figure was lethal. Fix: when the acting seat controls a life-to-damage converter, add lifelink attackers' damage once more before the figure and the verdict, or drop the categorical verdict and print the combat subtotal only. Pin. (deck126 HIGH-1)
- O6 HIGH — the hold-check identity key ignores the projected-life carve-out its own contract prints (`a row that differs only by a LIFE TOTAL it projects … is the SAME row`): `130v162` deck130 seat seqs 36→37, 39→40, 40→41, 41→42 — exactly one rendered row changed (`you would be at 12` → `11`) yet the bracket says `4 rows above are new` and re-opens the hold every window (11 windows ~90 s each on a chain the seat could not touch; last wave's HIGH with the mechanism now located). Two fixes: normalise `you/they would be at N` (and the `takes N damage - they would be at N` form) out of the key; count only rows genuinely absent from the held set. Pin with the seq-36/37 pair. (deck130 HIGH)
- O7 HIGH — the plan carry strips the `stop=<N>; M=<N>` clause the repeat row's own bracket demands: `planStepEnds` (GptPlanCaveat.h:920) opens a step at `; `, so the counts clause is step one and `planStepsAfter` (:972) drops it with the executed action (`123v130` deck123 seq 141 → 142 carried `"M=2; this window create 27 vampires…"`; `123v125` seq 251 → 252 lost both). 50 carries lost their counts; stop oscillated 56→46→51→106→51 inside one turn. Fix ENGINE-SIDE (the guide contract is right): a `stop=`/`M=` clause is state, not a step — never open a step on it, and/or re-prepend the persisted `mStatedStop` (AIPlayerGPT.cpp:20404) to the carry header. Pin the two carries. Sub-items: O7b MED the stop clause prints only on the batch row — the single `[cost: Tap]` Create row is an unpriced bypass past the seat's own stop (`123v162` seq 16, `123v125` seq 624): print the `{right now: M=…, your stated stop=…}` clause on every Create row of the seam; O7c MED the stop is never reconciled against `Opponent life trend` (`123v125` seq 686: stop 103 set at life 100, life now 108 — the 102-attacker swing left them at 6): add `your stated stop was set when their life was N; it is now L` to the repeat row when they differ. (deck123 HIGH-1, MED-2, MED-3)
- O8 HIGH — the Sphinx's Revelation (any X spell) cast row is the ONLY cast row with no `{spends K of your M …; <card> needs N}` clause (`{no {leaves…} count on this row: what it spends depends on the X you announce}`), and it is the row where the arithmetic is hardest: `125v126` deck125 seat seq 308 planned "X=14, then cast Staff of Nin" — X=14 spent all 17 sources, the Staff was discarded at seq 317; same shape at 211/215, 250/253, 294/297, 429/432. Fix: print on the X cast row the largest X that still leaves the cheapest castable card in hand payable, in the same `<card> needs N` grammar. (deck125 A-1)
- O9 HIGH — no row prices the clock the seat ALREADY OWNS: `125v126` seq 453, 131 life vs 2, Staff of Nin pinging every upkeep, LOOP COMPLETE + converter paragraphs both printed and quoted four times in the reasoning — Emrakul cast anyway ("I have no other win condition"), Tribute chain, dead. The pass/decline/hold rows print nothing about the seat's repeatable damage source. Fix: when the seat controls a repeatable damage source (Staff of Nin, a ping ability) and no lethal is incoming, tag the decline/hold/pass row `{your Staff of Nin #1-#N reach 0 in K of your upkeeps at this rate}`. (deck125 A-2)
- O10 MED — `main_phase_windows_skipped` 375: de-duplicated (every (turn,phase) once — Astra's per-tick over-count is fixed) but `noteMainPhaseCastingOffered` is wired only to the standard branch (AIPlayerBaka.cpp:4159); the instant-speed response arm (:3949) can itself issue a Casting decision, so 233 of 375 counted phases DID offer one (honest figure 142); and it double-counts phases suppressed by the seat's own hold latch (deck126: 58 alongside 640 holds; 0 where holds were 0). Fix: note the offer from EVERY arm that issues a Casting decision; exclude hold-suppressed phases (or report the two meters as one). Measured cost: 0 land drops and 0 `[castable now]` non-instants lost in the 375. (engine-seat HIGH-2, deck126 MED-3)
- O11 MED — the `[you declined this exact list N times already this turn]` override lost 40% of its 136 windows (waves 70/71/72/73: ≈20/26/24/40%) — three waves of guide capitals have not moved it. Fix (never remove the row): tag the plain decline row on those menus `(this same question will be asked again this turn)`; count takes-after-clause. Repro `125v123` seq 377, `125v146` seqs 44, 82. (deck125 A-3)
- O12 MED — `K of them able to attack right now` counts creatures the same line tags `[doesn't untap during its controller's untap step] [tapped …]` (103 Humans under two Intruder Alarms, `125v123` seq 399); the deck125 guide has carried a compensating sentence since wave 70 and this corpus falsified half of it. Fix: subtract creatures that are tapped AND carry a no-untap effect. (deck125 A-4)
- O13 MED (MEASURE, do not collapse) — the 7 chain windows behind O6 had 8 "legal" rows: three Siege-Gang sacrifices at the seat's OWN permanents / at itself, one at a `- SURVIVES (toughness 4)` target, one at a face 17 away, three cycling rows under `[DRAW PRICE]` — legally non-empty, decision-theoretically empty, so `chain_windows_collapsed` 0 is the predicate, not an unreached path. The legal-option boundary forbids collapsing them; the lane INSTRUMENTS: count windows whose acting rows are all self-target / SURVIVES / draw-price rows (`chain_windows_only_selfharm`), report, no behaviour change. (deck130 HIGH 2, engine-seat §2)
- O14 MED — the mana bill hides behind a decoy: `{leaves 0 of your 3 untapped mana sources untapped - casting this taps you out}` immediately before `{paying this taps: Intrepid Adversary, Katilda … they cannot attack this turn}`; the reasoning quoted the first twice, never the second, and planned the attack the row had ruled out (`152v130` deck152 seat seq 20). Fix: fold the two into one bracket that names the creature sources inside the count. (deck152 MED-2)
- O15 MED — loyalty menus carry no consequence annotation while every neighbouring seam prices its rows: `126v130` deck126 seat seq 42 — `+1` token summoning sick (unsaid; the attackers seam says it one window later), `−2` emblem cumulative with the one already live (unsaid), the deciding decision of that loss. Fix: `+1: the token cannot attack until your next turn`, `−2: your Vampire #1 becomes 3/1 this turn` style clauses on loyalty rows. (deck126 MED-2)
- O16 MED — the fresh-7 mulligan window prints `1. Keep this hand / 2. Mulligan (a keep after this one would keep 6 cards)` with NO `(keeping N)` — the spelling every deck guide's mulligan bullets key on, so those bullets are unreachable on the first window (deck130 seq 2, identical in all games). Fix: print `(keeping 7)` on the first window in the same spelling the later windows use. (deck130 MED)
- O17 MED — the repeat row's short name contains the placeholder `N` (`Create human with Thraben Doomsayer, repeated N times, then stop`); the answer footer says copy the short name, the row's bracket says copying it with the N is refused — 4 re-asks (`125v123` deck123 seqs 620, 628, 639; `126v123` seq 74). Row text is not the protocol: give the row a short name with no placeholder. (engine-seat MED-2)
- O18 MED — a row generator with no cardinality budget: `125v123` deck123 turn 67-68 produced priority windows of 411 and 409 options at 104,131 B / 102,927 B (seqs 658, 664; wave 72's largest prompt 37,917 B) — 102 equip targets one per row; 18 windows corpus-wide ≥60 options, 17 this seat. Fix: identical-effect target rows over a threshold collapse to one range row whose answer names the target (the display already prints `2-103.` ranges — make the option set match). Do not drop a distinct option. (engine-seat MED-1)
- O19 MED — the descending X menu is the corpus's only index/name conflict class two waves running (`1. X = 7 … 7. X = 1`; `CHOICE: 5 (X = 5)` names index 5 = `X = 3`; `125v126` seq 100, `125v123` seq 624 — both rescued by the name match). Fix: render X ascending so the index tracks the number (BZ N4 left this family alone). (engine-seat MED-3)
- O20 LOW — a hold on the upkeep CASTING ask does not cover the upkeep PRIORITY menu (15 of 21 upkeep holds were followed by a priority record in the same phase) — the contract as written, but the hold row's headline `do not ask me again` is false at the very next window: say in the bracket when a different seam will ask again this phase. (deck130 LOW)
- O21 LOW — the hold text runs ~450 bytes on every casting menu (deck146 MED 3); the worked example still copies annotations the same sentence forbids (deck146 MED 4); the `[second copy: …]` "no legend rule" gloss reads as encouragement, 55 renders (deck126 LOW-5); the hold row is still taken on menus that print a live activated row (deck126 LOW-4).
- O22 LOW (report only, protocol owner-ruled) — `plan_line_missing` 3.1% corpus-wide, 14.5% on the attackers seam, 100% the bare plan sentence with the `PLAN:` label dropped (every seat); one reply still opens `YOUR PLAN` (`146v130` deck130 seq 47).
- O23 LOW — an announced X WAS abandoned this corpus (`130v162` deck130 seat seq 52 → 53, 9 mana left floating per the decline row's own text) and `dropped after its X was announced` still printed 0 — the instrument does not cover that path (MTGRules.cpp:475 or a different route). Find the path the abandonment took and make the instrument fire; the carried payment-loss docket stays UNTESTED.
- O24 LOW — `async_drops` 32 → 43; `identical_ask_answers_reserved` 999 of 1,262 replays (engine-seat LOW-1/2). Report only unless a drop cost a decision.
- O25 MED (instrument) — deck162 seq 11: the guide's K-branch beat the `{reserve: … STRANDS Master of the Feast}` verdict and the PLAN then named the stranded card. A PLAN naming a card the chosen row's reserve clause strands is mechanically detectable: count `plan_names_stranded_card`; no behaviour change. (deck162 HIGH)
- O26 MED (harness) — a wedged pilot is invisible to the hang watchdog: seats keep ticking on 900-s timeout fallbacks, translog mtimes advance, and the 4-h check saw "22 active logs" and called it normal. `selfplay-harness.sh`: (a) the periodic sweep counts timeout-fallback records; when the newest K (say 6) records across all seats are all timeout fallbacks, print `== PILOT STALL` with the count and STOP the run (a game answered by heuristics is not a corpus game — invariant 00); (b) the harvest/results banner reports the timeout-fallback count per game. Self-test both in `--selftest`.
- O27 LOW (docket) — the pilot's `qwen3.6-enhanced.jinja` template rejected a phase-2 forced-close prefill with HTTP 400 `continue_final_message is set but the final message does not appear in the chat after applying the chat template` (1 of 6 forced closes in this corpus; 3 in the server log). Mechanism untested; phase2_answer_recovered 2 elsewhere.

Carried, UNTESTED again (never build on): abandoned-{X} payment loss (0 fires, but see O23); BS F2 Tribute second edict; BS F3 / the land latch (`declined_face_latches` 0 on 42 gameends); BS F5 blockers ceiling; BS F6 distinct-index re-ask; CB F1 (0 collapses, no Lotus-Petal board); CB F3 non-count stop guard (never stated); BZ N10 example balance; N14a/N14d reasoning-body halves.
