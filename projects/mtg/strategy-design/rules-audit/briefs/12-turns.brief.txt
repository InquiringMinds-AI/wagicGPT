READ-ONLY AUDIT. Make no changes to any file except writing the single report file named below.

Goal: audit the wagicGPT engine (repo root = current directory; C++ sources in projects/mtg/src and projects/mtg/include) against CR section 5xx (turn structure, phases, steps, combat), and write the report to projects/mtg/strategy-design/rules-audit/12-turns.md.

Rules source - read this file, never rules from memory: projects/mtg/strategy-design/rules-audit/data/sec-5-turns.txt (official Comprehensive Rules text, effective 2026-02-27).

Audit contract - read and follow exactly: projects/mtg/strategy-design/rules-audit/00-frame.md
It defines the verdicts (IMPLEMENTED / PARTIAL / INCORRECT / UNIMPLEMENTED / N/A), one verdict per rule id, code-evidence discipline (file:line for IMPLEMENTED/PARTIAL/INCORRECT), the N/A policy for excluded game types, and the known-defect cross-reference (strategy-design/lexicon-triage/engine-bugs.md).

Engine orientation: Turn machinery centers on PhaseRing / MTGGamePhase / GameObserver; combat in GuiCombat / MTGRules / AllAbilities. A pre-game mulligan phase (CR 103.5/103.6) was just added in src/PreGamePhase.cpp; section 103 belongs to another auditor, but 5xx interactions with it (the first-turn draw skip) are yours.

Report format: per-hundred-block markdown tables with columns rule, verdict, evidence/note; then a summary counting each verdict class; then a HIGHLIGHTS list (incorrect and notable-unimplemented findings, priority-ordered).

Done means this command prints MISSING: 0:
python3 projects/mtg/strategy-design/rules-audit/check-coverage.py projects/mtg/strategy-design/rules-audit/data/sec-5-turns.txt projects/mtg/strategy-design/rules-audit/12-turns.md
Run it before claiming done and include its output with your claim. Do not end while it reports any missing rule ids.
