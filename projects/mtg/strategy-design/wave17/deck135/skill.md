# Deck-135 wave-17 — skill deltas

**Verdict: NONE binding.** The wave-16 methodology held cleanly at this seat: win column treated
as context; unit of analysis was the DECISION with seq repros; layer-routing enforced (guide diff
0 bytes; every residual routed to engine/model/construction, never into prompt text); veteran HIGH
adopt bar respected (a plausible KEY-CARDS Azcanta line was considered and rejected with reasons).

One OBSERVATION for the synthesizer (not a skill-text change — flag only if it recurs at other
seats and wants codifying):

- **Multi-step selection menus where the meaningful choice is already made or hidden.** The Azcanta
  cluster (charge c) and deck110's promoted E6 Mox Opal "Cast Card Normally" item are the same
  underlying shape: the engine surfaces an INTERNAL mechanic (a target-ordering loop; a degenerate
  cast label) as a bare menu with no statement of the EFFECT, no ELIGIBILITY marks, and no running
  state — and the model, unable to parse it, either rambles to a timeout or picks an ineligible
  option that silently voids. When a seat's unparsed cluster concentrates on ONE card/ability,
  the productive move is to reconstruct the ENGINE's actual mechanic from the events (what did the
  choice do? what went where?), not to grade the model's confusion — the confusion is a symptom,
  the representation is the bug. This is already implicit in the current skill's "characterize the
  prompt shape and what representation change the model needed" charge language; no new text needed,
  but it is now confirmed as a repeating cross-seat pattern (2 seats, wave-17), which the skill's
  engine-item routing already handles.
