# Deck-110 wave-17 — skill deltas

**None.**

The wave-16 strategy-writing skill held cleanly at this seat. Every methodology it prescribes was
exercised and none broke:
- **Layer routing** worked: the two live items (E6 Mox label, E2 equip marker) both routed to the
  ENGINE ledger, not the guide — and the guide correctly stayed frozen. No temptation to fix a
  representation bug with guide prose.
- **Unit = the DECISION with a seq repro** was the right lens: win-rate (4/6) was pure context; the
  substance was decision-level (self-blast absence, the E6 sole-option repros, the ALREADY-passed
  equips).
- **Validated-by-absence** (E2, 3rd wave) and **canary-pass** (H1) are already first-class moves in
  the skill; both applied without friction.
- The **verification traps** the skill warns about (metalcraft instance-count, name→index reconcile)
  did not need re-litigation — prior waves closed them and nothing re-opened.

One small reinforcement the corpus supports (already implicit in the skill, not a new rule): when an
engine batch ships a targeted fix (here the PLAN-caveat + own-target suppression), the OWNING seat
should re-run the EXACT prior repro spot as the canary — the deck21 rematch (same opponent, same
Galvanic-Blast decision point) was the cleanest possible before/after. Worth keeping as the default
canary construction, but it does not change the skill text.
