# skill.md — deck17 (wave-17)

**Deltas to the strategy-writing skill: none.**

The wave-16 skill's doctrine held cleanly at this seat and needs no amendment. Two points
worth carrying forward as *confirmations* (not new deltas):

1. **Step 0-ter (taught-class died/persisted/mutated) is the right validation frame for a
   REVISED guide, not just a first guide.** It surfaced the one non-obvious result this wave: a
   class the prior wave called "died quiet" (Cloud Sprite block restriction) had not truly died —
   it MUTATED from a wrong-choice into a token-budget ramble. Without the explicit
   died/persisted/mutated pass this would have been mis-recorded as settled. Recommend the skill
   keep flagging "died quiet" verdicts as re-check candidates: a class that stops producing WRONG
   CHOICES may still be producing WASTED REASONING.

2. **A root-cause guide fix (walls-aren't-flyers) that addresses two losses can be validated by
   reversal of the causal matchup, not just by aggregate win-rate.** vs110 flipping loss->win
   under the new deployment + race rules is stronger evidence than 4/6->5/6. The skill already
   privileges the decision over the win-rate; this corpus is a clean worked example.
