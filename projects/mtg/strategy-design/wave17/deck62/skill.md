# deck62 skill deltas (wave 17)

The Step 0-ter doctrine (adopted wave-16) held cleanly on this seat with NO amendment needed. The
died/persisted/mutated-before-win-rate frame, the tax-collapse-as-primary-signal frame, the
"reconcile looks-disobeyed vs the eligible set" check (vs17 s20 no-attack read correctly as a
forced defensive hold, not durdle), the "first revision tightens" expectation, and the
"belief-dies-but-tax-relocates" rule (the TRUST-printed class: belief present, tax moved from
combat-unparse to Blanchwood-cast recount) ALL applied 1:1 to this engine/voltron seat. Step 0-ter
is validated on a third independent first-guided seat.

## One reusable observation (candidate for Step 0-ter, single-seat — TAG, do not core-adopt yet)

**A tax-collapse this large (17->3 fallbacks, 14->1 giant replies) is itself the strongest possible
"guide worked" signal, and it can co-exist with a MUTATED residual that LOOKS like regression.**
deck62's one attackers-unparse (vs135 s19) is superficially the same failure family as wave-16's
combat unparses — but it is a DIFFERENT mechanism (wave-16 = tax-drowning past a parseable line;
wave-17 = a short, confident reply that names creatures absent from the offered list). Reading the
fallback COUNT alone would miss that the tax died and a crisp list-vs-board-read misread took its
place. Lesson for first-guide validation: when the fallback rate collapses, re-CLASSIFY the
survivors by mechanism (reply length + what the model actually did), not by kind label — a surviving
"unparsed" after a good guide is usually a new, narrower bug, not the old one persisting.

## Convergence flag for synthesis
The **sub-menu-misread shape** (a target/choose-N sub-menu read as a different decision type) now
has evidence at TWO seats this wave: deck135 (multi-target "Choose target K of N", its charge) and
deck62 (single-target "Choose the target for Web", vs135 s18). Cross-seat -> representation-layer
item, not a per-deck guide fix. Flagged to notes N1 and here for the synthesis pass.
