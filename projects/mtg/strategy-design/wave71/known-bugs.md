# Wave-71 known bugs (corpus matchups-20260907-085638, 21/21 natural, 0 hung; sources wave71/engine-seat.md + wave71/deck<N>/review.md)

Regression watch on the wave-71 shared-path changes: CLEAN (decisions/turn 2.25→2.40, latency flat, 0 hangs, stderr max 1.23 GB→557 KB, no ping-pong beyond length-2 pairs, `menu_pass_no_progress` 0, `ask_replays_refused` 0). Fallbacks 9→4 (0.19%); `reply_truncated` 0 at 288/256; `YOUR PLAN:` 7→0 after the render fix (but guides quoting the heading doubled the bare-label class — the two quotes are removed in the installed guides).
Prediction ledger: BO 5/5 PASS; BP 1 PASS, 2 PASS-with-M1, 3 UNTESTED; BQ L3/L5/L12 PASS, L4 PASS on substance / FAIL as worded (M4); BR L7/L11/L15/L16/L17/L19 PASS, L8 FAIL (M3), L6/L13/L14/L18a UNTESTED; BS F9 PASS, F8 weak PASS, F7 FAIL (M3), F1/F2/F3/F5/F6 UNTESTED (no double edict, no Proper Burial family, no declaration above the cardinality floor, 0 face-menu declines in 31 pathway menus).
Wave-70 guide edits adjudicated by the seats: 146 PAID (the Hive played; vs125 loss → win), 162 half (declines STRANDS once, takes it next window), 123 edit 2 paid / 3 half / 4 no, 125 edit 5 paid / edit 4 backfired (fixed this wave), 152 ATTACK TOTAL 2/4, 126 untested by shape. All seven guides EDIT verdicts installed (≤19,992 B, A332 0, guard OK).

## M — routed to wave 72 (HIGH first)
- M1 HIGH — the harness harvest never copies the `askreplay/` sidecar: engine writes to `~/.Wagic/ai/gpt/logs/askreplay/` (725 records = `ask_replays_reserved` exactly), `tools/selfplay-harness.sh:~610` copies only `$LOGDIR/*.jsonl`. Copy the sidecar files for this run's seats into `$OUTDIR/askreplay/`.
- M2 HIGH — `noop_row_zero_reask` false fires, a NEW class BQ-L3 does not cover: a PERMANENT whose zero verdict is PROSPECTIVE (`Cast Lightmine Field {deals 0 until they have an attacker}` 125v152 seq 32, 125v123 seq 7; `Cast Lightning Greaves {you control 0 creatures - this equips nothing}` 123v152 seq 10 — the model's own PLAN was the set-up line and the re-ask made it abandon it). 3 of 4 corpus fallbacks; exposure 54 permanent rows of 759 zero-verdict cast rows + the same claim in the `NO LIVE CAST ROW` header on 292 windows. A prospective/conditional zero on a permanent is NOT a no-op. ALSO (deck125 A-1): the re-ask re-serves the identical prompt and got the identical wrong answer 2 of 3 — the re-ask must NAME the refused row and its zero verdict (one labelled-line re-ask, no prose read).
- M3 HIGH — BR-L8/BS-F7 FAIL on the first live shape: opponent's Teferi's Puzzle Box trigger on the stack in the seat's own draw step, no hand-replacement line (123v162 seq 43-45); `abilityEmptiesHandOf` returned false; the predicate has no fixture and no pin. Fixture with the Box on each side.
- M4 HIGH — BQ-L4 as worded: 0 of 376 `priority` windows carry a Cast row; the Cast row appears at a SIBLING `ask` (209/243 castable-now groups; repro deck130-vs-125 seq 24 vs 25). Decide the window model: either the priority window renders the cast row or the ask IS the window and the priority window is not issued when an ask follows (double-asking costs a model call). Pair with M5.
- M5 HIGH — the GPT seat gets almost no OPPONENT-TURN windows: deck130 4 opponent-turn windows in 353 decisions, all at their Main 1 — its only instant (Starstorm) is functionally a sorcery. Response windows on the opponent's turn (their combat, their end step) must reach the model when an instant is castable; `hasInstantResponse`/`extendInterruptOffer` path. SHARED-PATH change — fixture RED on base, watch the decisions/turn cost.
- M6 HIGH — the PRIORITY-seam hold row still says "you give up no cast" above live loyalty/activation rows (BR L17a fixed the casting seam only): deck126 4 of 4 priority holds taken over Sorin rows, each against the reply's own PLAN. Price the hold row on every seam where an activation is live.
- M7 HIGH — the name-choice list (Silverquill Silencer) offers TOKEN names (deck130 seq 30 `Goblin {visible now: 2 on their battlefield}` chosen → dead Silencer) and sorts `{visible now:}` rows FIRST although for a cast trigger a visible copy is evidence the name is SPENT; the deciding number (copies not yet in a public zone) is never rendered. Exclude token names; render deck-count-minus-public; don't sort visible-first.
- M8 HIGH — an OPPONENT permanent's `{effect: "…"}` prints in the card's own voice ("you gain that much life") — 152v126 seq 35 the model read Exquisite Blood as its own refund and alpha-struck into a lifegain trap (cost the game). Normalise perspective on non-self lines (the converter paragraph already does).
- M9 HIGH — `{reserve:}` STRANDS verdict ignores an unused land drop (provably false at 162v146 seq 10 — the model overrode it and was right, which trains override of true verdicts); the verdict buries its consequence behind the "last window" clause (152 seq 17 read it as permission) — lead with "take this row and you CANNOT cast X this turn"; a reserve decline is not carried across upkeep→draw (`reserve_decline_windows_skipped` 0 in every gameend).
- M10 HIGH — a stop-dead Create row (stop reached) is re-offered at every phase seam of the turn: 123v152 seq 28-38 eleven identical windows, 1,287 s, nothing advanced. Collapse: a repeat row whose stated stop is reached is not re-asked this turn (hold-eligible / auto-skipped like a mana-only window — it is not a legal-option removal, the answer is already given).
- M11 HIGH — a repeatable upkeep row (Hammer return, deck130) ran 25 consecutive turns for 3 damage against a `+15 per four turns` life trend printed on the same screen — no render prices a repeatable row against the trend. Add the arithmetic line (damage per turn vs their net life per turn → "never closes").
- M12 MED — legend-rule choice wears a false header (`TARGET CHOICE … its "put a card into the graveyard" ability`, 146v162 seq 19/26) — the model binned the attacker and kept the summoning-sick copy. Name the legend rule and which copy STAYS.
- M13 MED — `[castable now]` stamped on hand cards at windows where the casting decision was already answered (27 of 47, 146v125 seq 150).
- M14 MED — own-turn window budget: 135 of 413 deck146 decisions (32.7%) resolved to a no-op (one turn cost 34 decisions); the L4 branch should not ISSUE an ask when no instant-speed cast candidate exists (falls back to selectAbility silently) — count skipped.
- M15 MED — `plan_line_missing` 90 (4.2%): an unlabelled plan line → the carry silently goes stale. Render side only: what in the prompt lets the label drop (measure per seam — deck152 says 9 of 14 at seams whose instruction caps the ACTION label only); the protocol text is owner-ruled, report.
- M16 MED — Sorin's `-6` row text truncated into the `{card text}` tail (deck126; printed twice vs wide boards, taken 0).
- M17 MED — reveal/search rows carry no legendary/duplicate annotation while cast rows do; the reveal window never names the destination zone; the carried PLAN at reveal windows is the stale combat plan (wave-70 L21, not routed then).
- M18 MED — at a casting seam the crack-back header omits the "nothing you control can legally block" clause; the tap-you-out row is the only row with no crack-back arithmetic (wave-70 L22, not routed then).
- M19 MED — `LOOP HALF PENDING` salience inverted: printed 158× where the loop can never close, no header for a COMPLETED pair (wave-70 L20, not routed then).
- M20 MED — the L7 "animatable" tail steered a decision wrong (125v152 seq 64: Supreme Verdict cast to "remove the impending Lair of the Hydra threat") — say the animation COST and that it is not a creature now.
- M21 MED — declined-count windows 26% mis-answered (17/65, deck125 A-4), unchanged across two waves of guide capitals — render fix.
- M22 MED — `ask_replays` longest run 38 and growing monotonically with turn vs the refusal cap 64: the cap will trip on long games — scope the refusal to a WINDOW (same state+question), not the game.
- M23 LOW — Silverquill per-cast life tax invisible to the mana-only P-list (deck123); `HOLD PRIORITY:` row head label-shaped (BR L18c docket); dungeon room header (BR docket); Idyllic Tutor one-window lag (carried).
UNTESTED (fixtures only, keep watching): BS F1/F2/F3/F5/F6, BP land latch.
