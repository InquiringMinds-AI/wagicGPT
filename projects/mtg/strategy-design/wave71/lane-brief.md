# Wave-71 step-one lane brief (base df0fa9376; evidence = wave70/known-bugs.md + wave70/engine-seat.md + wave70/deck<N>/review.md)

RULINGS THIS WAVE TOUCHES (skill invariants): 000 — reasoning only in the native channel; the reply is PLAN then action, nothing else; no instruction string may license prose/reasoning/headings/corrections; no parser reads outside the two lines; caps never bound reasoning; thinking regime + harness flags + protocol TEXT are owner-ruled (report, don't change). 00-P — a procedure change replaces and sweeps. 00 — corpora complete full games. 3 — suite 0 failed (lifeline + merrow_reejerey are the two known single-thread-only failures; THREADS=1 is ground truth), PARSETEST 0 failed. Owner ruling on the plan exemplar's bare commas: "dont care" — do not touch it. No VPK/EBOOT/APK. No AI attribution in commits.

Standing mechanics for every lane: work only in your worktree; `make -f Makefile.sdl -j8` from projects/mtg (regenerate with `qmake6 wagic-SDL.pro CONFIG+=debug -o Makefile.sdl` only on a .pro change; both link-time guards — ctor-init and reply-instructions over src + bin/Res/ai/baka — must print OK); binary freshness (mtime) before every gate; EVERY wagic run under `systemd-run --user --scope -q -p MemoryMax=4G -p MemorySwapMax=0 --`; suite as a detached unit `systemd-run --user --unit=w71-<LANE>-suite -p MemoryMax=4G -p MemorySwapMax=0 --working-directory=<worktree>/projects/mtg/bin bash -c 'unset WAYLAND_DISPLAY DISPLAY; WAGIC_HEADLESS=1 WAGIC_TESTSUITE=1 WAGIC_TESTSUITE_THREADS=1 ./wagic > ~/.gatelogs/w71-<LANE>-suite.log 2>&1; touch ~/.gatelogs/W71_<LANE>_SUITE_DONE'` waited with a FOREGROUND until-loop (no Monitor tool; rm the done-file first). Count both `==Test Failed !==` and `==Test timed out`. PARSETEST: `WAGIC_GPT_PARSETEST=1 ./wagic` from bin. Edit AIPlayerGPT.cpp / AIPlayerBaka.cpp with Edit (UTF-8); MTGRules.cpp, GameObserver.cpp, primitives are CP1252/CRLF — byte-splice only; `git diff | /usr/bin/grep -c $'\357\277\275'` = 0. Every fix ships RED-on-base evidence (a PARSETEST case or a suite fixture that fails on df0fa9376) and a falsifiable prediction for the next corpus. Commit on your branch with explicit paths (`git add -f` under bin/Res), no attribution; write wave71/lane-<LANE>.md (per item: confirmed/refuted, repro, fix, RED evidence, prediction); touch `~/.gatelogs/W71_<LANE>_DONE`. Report weakest evidence. Do not launch corpora.

## Lane BO — the revert list + the meters (known-bugs §R, L9, L10, LOW-3)
R1-R9 verbatim: delete the code and the pins of every DEAD mechanism (count what you delete: functions, state members, record fields, PARSETEST cases — the PARSETEST count DROPS; say by how much); keep exactly the counters/notes the rows say KEEP. UNTESTED rows are untouched. R3's second half = the owed answer-ceiling re-fit from THIS corpus's bytes (engine-seat §1: max PLAN 422 B, p95 191 B; p99 PLAN+action < 300 B at every seam): fit each seam's ANSWER ceiling to p99.9 × 2 with a floor you justify, never touching the reasoning budget; pin the arithmetic. L9: `reasoning_budget_hit` from the API's finish/usage evidence, not a char estimate; `reasoning_forced_close` only when a phase-2 close happened; wire `phase2_answer_missing/recovered` to the path that runs (4 answer-locked retries happened, counters read 0). L10: `plan_line_missing` on the record + gameend census; `plan_steps_done` on every record (BN F10 was unmeasurable). Prediction: next corpus's fallback rate and off-protocol rate under the deleted code.

## Lane BP — the hang (known-bugs L1, L2; engine-seat §0 has files, seqs and the four code sites)
L1: (a) a declined MDFC/face menu on a land drop latches that card for the rest of that land-drop window (the seam's own `held the land drop` pattern), so Baka cannot re-propose it; (b) the W64-AK pass floor gets a NO-PROGRESS arm (same serialized state + same question N ticks running → pass, counted); (c) `mAskCache` / the repeat re-serve refuse after N identical replays in one window and write a record. A suite fixture reproducing the livelock (MDFC in hand, land drop available, the AI seat declining) must be RED on base (hang → the suite's timeout) and green after. L2: every re-served answer writes a compact translog record (`kind`, `seq`, `replayed_from`) so a hung seat log is never silent; the harness's no-progress watchdog also reads stderr growth (a seat writing MB/s of the same line is a hang, not thinking). Prediction: zero HUNG games next corpus; `identical_ask_answers_reserved` visible per window.

## Lane BQ — decision-seam defects (known-bugs L3, L4, L5, L12)
L3 `rowSaysNoOp`: a `{kills:}` / verdict clause on the row defeats a no-op read; "does not apply" inside card text is never a verdict — pin Tragic Slip's row positive and negative (deck123 review has the rendered rows). L4: why does a `[castable now]` instant get no cast row at priority-kind windows (Starstorm 23 of 23) while Casting-decision asks offer it — find the filter (oracle legal set vs seam), fix, fixture RED on base. L5: the "Choose an option for X" modal after `Cast X` re-offers cycling and the model takes it against its PLAN (7 cases, deck130 review seq 35→36): carry the committed intent into the modal (the cast already chosen is not a fresh choice) or do not ask when the cast was the answer. L12: the reveal seam's tail/label cue (a reveal answered `BLOCKS:` then `PUT:`). Pins for all four.

## Lane BR — card/render truth (known-bugs L6, L7, L8, L11, L13-L18, L19)
L6 Tribute to Hunger: FIRST retrieve the Oracle text (Scryfall), then the primitive (`mtg.txt:124073`), then the engine — fix at the lowest layer that is wrong (feedback memory: express Oracle faithfully in card script before touching the engine); fixture. L7 animating lands in the header/sweeper-K/Lightmine-N counts (say "+N animatable" or fold them) — deck125 review has the windows. L8 `{feeds:}`/`{reserve:}` fold pending hand-replacing triggers on the stack (Puzzle Box) or state that the hand changes. L11 the RENDER heading over the carried plan (`YOUR PLAN`) must not be label-shaped — the parser never accepts `YOUR PLAN:`; also find what in the prompt lets 92 replies drop the `PLAN:` label and fix the render, not the parser. L13 modal spell narrated as resolved before its mode ask. L14 manland "asked at its own window" only when payable; rung-ceiling explanation on every row. L15 Tomb of Annihilation modal rows annotated. L16 false `[DRAW PRICE]` on a cast row. L17 no hold row on Casting decisions; `repeated N times` row lag. L18 crack-back floor anchoring / decline-vs-hold near-synonyms / 2 parser refusals (deck125 review seqs). L19 the W69-BJ F1 diagnostic allows disjoint tap groups. Pins for every render change (positive, negative, echo shape).
