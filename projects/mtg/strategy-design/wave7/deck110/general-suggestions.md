# Deck-110 wave-7 — core-prompt (general-strategy) proposals

## Verdict: NO core-prompt edits from this seat.

Layer-routing: every item this wave routes to ENGINE/REPRESENTATION, PER-DECK, or MODEL-EXPERIMENTS
— nothing is deck-agnostic-and-losing at the core layer. The burden of proof is on core; this seat
does not meet it.

### Why nothing goes to core

- **The wave's headline is a REPRESENTATION win, not a prompt gap.** The re-equip marker
  (`AIPlayerGPT.cpp:1163-1173`) fired and the no-op rate collapsed 50%->6%. The fix lives in the
  engine's option-rendering, exactly where the wave-6 synthesis routed it. Core prompt did nothing
  and needed to do nothing.

- **The lone marker disobedience is a PLAN/CHOICE MISMATCH (vs131 s33)** — the pilot typed the wrong
  option index after correctly reasoning for a different one. The skill already classes this as a
  reply-protocol/model defect (Step-2). It routes to MODEL-EXPERIMENTS / reply-protocol, not a core
  sentence. A louder core "read the option numbers carefully" line would be a generic scold with no
  measured deck-agnostic base — REJECTED for core.

- **The vs44 counter loss is META/DECK-CONSTRUCTION**, not a prompt defect. The stack-surfacing +
  labeled-counter representation (shipped waves ago) is working as intended — it revived the
  opponent's counters against affinity's discrete threats. Core is correct; the deck list is the
  owner. No core change.

- **cast-nothing 25%** is concentrated in the two stall/Lightmine WINS where holding is correct (the
  vs140 double-Lightmine game's guide ORDER is "cast nothing / don't attack"). Not a deck-agnostic
  passivity signal; the core act-floor is fine. No change.

### Standing shortlist item (not this seat's call, flagged for the synthesis agent)
The core `hand -> stack` cast-happened anchor was put on the wave-6 REMOVAL shortlist (0 cast no-ops
cross-deck; Hellrider fixed at source). deck110's corpus REINFORCES the removal case: 0 fallbacks, 0
engine no-ops across 188 decisions — the anchor's target bug did not manifest for this seat either.
This is one more seat's worth of evidence for the deliberate A/B removal, not a wave edit. Single
seat cannot cut a core line; recorded for the cross-deck tally.
