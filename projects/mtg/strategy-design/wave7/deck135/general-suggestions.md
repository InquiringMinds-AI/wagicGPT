# Deck-135 wave-7 — core-prompt / general-strategy proposals (layer-routed)

## Verdict: NO core edits from deck135 this wave.

Every fixable item this seat produced routes to PER-DECK, ENGINE/REPRESENTATION, or a
CONSTRUCTION flag — not to the deck-agnostic core. Argued per candidate:

- **Force-of-Negation scoping (the wave-6 over-fire) → PER-DECK, resolved there.** The
  guide's named-board-wipe scoping was obeyed 9/10 this wave. Still single seat; the
  wave-6 rejection of promoting "a free reaction still costs a card" to core stands. Core
  burden-of-proof unmet (every deck would pay for a line only Force-class cards need).
  Keep on the Appendix watch; promote only if a second free-alt-cost reaction deck
  (Force of Will / Solitude class) reproduces the over-fire AND the per-deck scope fails.

- **Phantom-lethal board-read anchor → already correctly routed (representation +
  per-deck stopgap), no core change.** Core already carries "count this turn's damage only
  from creatures actually in the list" and "trust the board over the plan." Those held
  this wave (0 recurrences). A rule present-and-holding does not earn a second core
  paragraph. Keep the class on the convergence watch; it remains single-seat.

- **Stale-plan around an uncastable card (vs44) → MODEL-EXPERIMENTS + CONSTRUCTION, not
  core.** The core anti-deferral / plan-number-binding lines are present; the failure is a
  manabase strand feeding a stale plan, which no core sentence fixes. Route: construction
  flag (manabase) + model residual. Cheap per-deck list-anchor reinforcement only (shipped
  in this wave's strategy.txt).

- **Fetch re-offer tax → ENGINE LEDGER, not core.** Deck-agnostic in shape but it is a
  representation/engine defect (an activation re-offered every priority window), not a
  prompt-text problem. See notes.md.

- **Reflexive high-life blocking (standing convergence watch) → NO support from deck135.**
  Only 1 bundled `blockers` decision this corpus (vs131), and it was a low-life survival
  block. No new evidence to promote a core sentence; keep per-deck / on watch as wave-6
  left it.

## Reused threshold note (for the synthesis agent)
Where a proposal needs a life threshold, deck135's Rule #2 already reuses the core 10-/8-
life family (its survival-block line keys on "10 life or less"). No new number minted.
