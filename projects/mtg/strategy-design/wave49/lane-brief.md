# Wave-49 step-1 lane brief (from wave48/engine-ledger.md — THE DOCKET; read your items there IN FULL)

Base: master 6819183f7 (+ wave-48 synthesis/boundary commits; no engine change after 6819183f7).
Baselines: PARSETEST 1655/0; suite 1137/0 + 37 AI/0, exactly 1 `W43-1 ASSERT`, 0 timeouts.
Doctrine: enforce legality without constraining choice; every parse/render change ships
PARSETEST cases (positive, must-NOT-match negative, echo shape); every engine change ships a
suite pin; byte-splice Windows-1252 files; MEMORY CAP on every wagic run; state a falsifiable
prediction; report what you did NOT verify.

Lane R — combat screens (render): D1 B-row `may block` ranged + factored parenthetical;
  D13 `#n` sort interleave (group identical text across the lexicographic `#1/#10` order on
  A/B rows too); D9 vigilance `[attacking]` double-count in the blockers header.
Lane S — reply parsing: D2 E1 replacement name-vs-index validation (`named_row_not_offered`),
  last-coded-line rule for `ATTACK:`/`BLOCKS:` tails, `answer_replaced` truthful; D3 repeat
  count grammar (`x17`, `N=17`, `N = 17`, `17 times`, `, 17)`) + no-count re-ask/receipt;
  D8 one re-ask for index-past-menu with unmatched name, exit row says it moves on to combat.
Lane T — mana/engine: D4 auto-tap pays generic from the least-flexible sources (mono-colour
  first, most-flexible last) — TRACE the payment order before scoping; D11 rows say what
  paying TAPS (creature mana sources, `taps you out` only when true; the tapped-Hive beholder
  row); D10 edict/sweeper rows carry the `{right now:}` fact they turn on.
Lane U — pricing/narration: D5 converter PAIR loop clause + direction sentence (R12 HIGH);
  D6 `[DRAW PRICE:]` on draw:X / Clue / opposing cast-trigger rows + DRAW FORECAST; D7 carried
  plan expires when it names no action (immortal 48 windows); D12 `//` back-face marker.
Not this wave: D14-D19 (LOW), D20 frontend grouping (separate human-seat lane, owner item).
