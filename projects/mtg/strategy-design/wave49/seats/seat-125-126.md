# Wave-49 seat: deck125 (3/6) + deck126 (4/6)

Corpus `matchups-20260827-094106` - SIXTH fair-hand corpus, first on lanes R/S/T/U (+ Vita
fixes); binary master 7e94544f1. Guides = wave-48 reviewer revisions (87103fd01) + boundary pass
(a4d27c299). deck125 **479** decisions / 6 games, deck126 **272** / 6 games (751, excluding
gamestart/gameend). The deck123-vs-deck162 dead-loop game is excluded from every corpus-wide number
per the brief; none of my games touch it.

**Health (my 12 seat logs).** Fallbacks **1 / 751 (0.13%)**: `deck125 vs130` seq 101
`unparsed_reply` - a 409-char decode-collapse reply (CJK garbage, `coded_answers: 0`), Baka took
the 2-row window (Verdict at a 0 header / cast nothing; the narration shows no Verdict cast).
`answer_replaced` **0/751** on my seats; `commit_retracted` 0. Other seats' fallbacks seen while
scanning: 2 `unparsed_reply` (deck123 vs130 seq 31, deck146 vs125 seq 82), 1 `stale_echo` (deck146
vs152 seq 54), and lane S's new re-ask kinds `repeat_count_reask` (deck123 vs125 seq 11) and
`named_row_reask` (deck146 vs126 seq 9) - reported, not adjudicated here. Latency deck125 p50
4.6-16.6 s per game / max 96.4 s (`vs162` seq 31); deck126 p50 8-45 s / max **727.3 s** (`vs123`
seq 62, parsed). Prompt chars: deck125 max **34,333** (`vs123` seq 13 - the brief's known record,
dissected under HIGH #2), next 27,723 (`vs123` seq 73, an upkeep priority window - game-log
growth, nothing pathological); deck126 max 18,446 (`vs125` seq 104), blockers max **8,406**
(`vs152` seq 13). Multi-`CHOICE:` replies: deck125 2/479, deck126 1/272 (down from 9/174).

Every card fact below is read off `projects/mtg/bin/Res/sets/primitives/mtg.txt` with
`/usr/bin/grep` (Tribute to Hunger, Emrakul, Sanguine Bond, Exquisite Blood, Chromatic Lantern,
Lightmine Field, Sphinx's Revelation, Tovolar's Huntmaster, Devour Flesh, Pelakka Predation, Staff
of Nin, Elixir of Immortality, Shield Sphere, Forced Fruition) or quoted from the corpus's own
rendered strings; deck126's land count (24) and wall count (16) off `bin/Res/ai/baka/deck126.txt`.

**Results.** deck125 3W-3L: won vs146 (t36), vs123 (t50), vs130 (t54); lost vs162 (t23), vs152
(t18), vs126 (t60). deck126 4W-2L: won vs130 (t34), vs152 (t15), vs125 (t60), vs123 (t24); lost
vs162 (t13), vs146 (t14). Same records as wave 48 (3-3 / 4-2), n=6 - not attributed to edits.

## NEW-BASELINE record (mulligans)

| seat | offers | mulls taken | kept-hand land counts | bottoming |
|---|---|---|---|---|
| deck125 | 6 | **0** | 4, 2, 5, 5, 4, 2 (every coverage line named a card) | none |
| deck126 | 6 | **3** (single each: `vs123` 0-land seven, `vs152` 1-land seven, `vs146` 1-land seven) | 5, 3, 3, 3(after mull), 5(after mull), 3(after mull) | `vs152` seq 3 Chromatic Lantern (3 lands kept); `vs146` seq 3 Sunpetal Grove (5 lands kept); `vs123` seq 3 Sunpetal Grove (**2 lands kept** - Chapel + Savannah with Sorin, Guardian, Wall, Blood; won t24) |

No chain, no mull-to-4, no mull-to-zero. **No OWNER QUESTION arises.** The live deck126 mulligan
text still carried three floors ("KEEP whenever N is 2 or more", "(keeping 5) ... even ONE land is
a keep", "fewer than THREE lands ... cross them all off") - the `vs123` seq 3 bottom broke the
third and won; rewritten to the odds form under skill #119 (F15, my call, not the owner's).

---

# deck125 - Revelation Fracture (UW draw-go). 3W-3L

## Prediction set A - wave-48 guide edits (W38-W45)

**W38 (spell cast under Fruition + punisher at 7 x F x P >= life-1: 0; own-turn casts under the
pair: 0) - PASS 0/6 on own-turn casts, FAIL 1/1 on the counter.** `vs162` seq 27-32 (t18-t22,
life 10 -> 2, Forced Fruition + 2-3 Underworld Dreams + Ob Nixilis on the line, Staff at `leaves
3` on the menu every window): `Cast nothing` six times - the W8 text held. Seq 33 (life 2, Howling
Mine on the stack, price 7 x 1 x 4 = 28): Cancel cast, drew 7, dead. The seat was dead to the next
draw step regardless (4 damage per draw); a rule break with no game in it. Not re-worded.

**W39 (sweeper / Lightmine cast at a header second number of 0: 0) - FAIL 2 of 4 casts.** `vs126`
seq 60 (t34, life 35 vs 20, no clock on the seat): Final Judgment off `{right now: exiles 5 of
their creatures (0 able to attack), 0 of yours}` at header `5 are creatures, 0 of them without a
restriction` (five walls) after ~25 correct declines on that board. `vs130` seq 50 (t32): Lightmine
Field off `{right now: they control 0 creatures able to attack - deals 0 until they have an
attacker}` at a header of 2. Correct: `vs123` seq 13 (Verdict at 323), `vs152` seq 21 (Verdict at
3). Windows at 0 on both seats' turns: ~150, so the RATE is far below wave 48's 4/8 - but the
sixth corpus of the shape. Edited W17 (the row's own string named; #118 re-key).

**W40 (Staff from `leaves 4` or less / taps-out with a counter in hand: 0; plan line carries
"leaves 5 or more" N/N) - FAIL 3/6 casts; plan line 6/6 verbatim.** `vs152` seq 23 (`taps you
out`, Essence Scatter in hand - **the loss**: Tovolar's Huntmaster resolved next turn with no
window, its Wolves and a second Huntmaster took 18 -> 8 -> dead), `vs146` seq 30 (`taps you out`,
Fall of the Gavel), `vs130` seq 54 (`leaves 1`, Dream Fracture). Legal: `vs130` seq 63 (`leaves 3`,
no counter), `vs123` seq 89 (`leaves 12`), `vs126` seq 97 (`leaves 10`). Every breaking reply
echoed "resolve Staff of Nin from a row reading leaves 5 or more" - the rule in the plan and the
answer against it. Edited W15 (gate first in the plan sentence; the row strings named; the
contradicting "no answer needed: take it" bullet removed). Sixth wave.

**W41 (Elixir CAST below `leaves 5`: allowed; ACTIVATION to `Mana available: 2` or less with a
counter in hand: 0) - FAIL 5.** `vs126` seq 8 (3 -> 1 open, Scatter), 15 (4 -> 2, Scatter); `vs130`
seq 7 (3 -> 1, Scatter + Gavel), 15 (3 -> 1); `vs146` seq 35 (6 -> 4, three counters). None cost a
window (the spells that resolved into them were enchantments a lone Scatter could not counter).
Elixir casts at `leaves 0-1`: 4, all legal. Not edited beyond W15's plan sentence.

**W42 (counter on a `[defender]` / power-0 stack row: 0) - PASS 0/8.** `vs126` seq 22, 50, 54, 59,
76, 77, 81, 95 (Perimeter Captain, Battlement, Pride Guardian, Wall of Omens): all `Cast nothing`.
Counters cast: 22; targets 8 creatures power 2+ (incl. Master of the Feast, Bloodline Keeper,
Acererak x2, Siege-Gang x2, Silverquill Silencer x2), 3 planeswalkers (Sorin x2, Kaya, Lolth), 2
Triumphant Adventurer 1/1 deathtouch ("venture" - guide-correct), Thraben Doomsayer x2 ("{T}:"),
Fate Unraveler, 7 non-creature spells (Dictate, Idyllic Tutor x2, Vision Skeins, Pelakka Predation,
Soul Shatter, Damnation, Lightning Greaves, Devour Flesh, Lay Waste x2, Stone Rain, Hammer of
Bogardan, Chromatic Lantern x2, Exquisite Blood, Howling Mine). W11 holds.

**W43 (Path at a textless 1-power token above 12 life: 0; Path taken at life <= 12 with a power-2+
candidate: N/N) - FAIL 2 on tokens; UNTESTED on the second half; NEW break on a wall.** 7 Paths
cast: Luminarch Aspirant 1/1 ("At the beginning of combat" - correct), Katilda, Nadaar, a Spider
2/1 (the highest power offered beside Goblins, `vs146` seq 74), Siege-Gang Commander (`vs130` seq
29), and **Goblin tokens at `vs130` seq 34 and 41** (`Goblin #1-#3 (creature 1/1)` the only
candidates, life 21 and 17, five+ lands open; seq 31 had declined the same board). The W12 clause
did not hold - second corpus at 2. No window at life <= 12 offered a power-2+ target (lowest:
`vs162` seq 24 at 13 with only a Shield Sphere - and the seat Pathed the Sphere, `(artifact
creature 0/6) [defender]`). Edited W19 for the wall's second printed form; the token clause is
recorded, not re-worded (the rule already names "Goblin (creature 1/1)" and it fired 1/3 windows).

**W44 (X = 3 whenever the menu reaches 3: N/N; land drop N/N) - X PASS 8/8** (`vs146` 40, 77;
`vs123` 16, 22, 42, 52; `vs126` 99; `vs130` 94; below-3 menus took the max offered 5/5); **land
drop FAIL 4/88**: `vs146` seq 94, `vs130` seq 80, 98, 100 - all at 12+ lands with 7-8 cards in
hand, bare replies. Was 84/84. Recorded (first corpus), not edited.

**W45 (mulligan on a 2-5-land hand with a named coverage card: 0; keep without a counter or Path
after bottoming: 0; chain to 4: report) - PASS 0/6, 0 bottoming asks, 0 chains.**

**Revelation below six mana (no prediction, W-lineage): 5 casts** at `max affordable X=1/2` -
`vs146` 19, `vs130` 31/38/47, `vs152` 28. Edited W16 (re-keyed to the row's printed max X).

## The losses (deck125)

- **vs deck126 (t60) - the Emrakul suicide.** Both enchantments resolved by t10 while the seat's
  only counter was Essence Scatter (seq 19-21 held it for Sorin: countered; both Sorins and both
  later Lanterns and the second Blood were countered, 6 counters spent). The seat sat at 30-48
  life against a deck with NO clock (no Staff drawn by deck126, Sorin x2 countered), resolved its
  own Staff at seq 97 (t52) and pinged 20 -> 15. Seq 117 (t60): `Cast Emrakul` taken with the LOOP
  clause on the prompt; deck126's next window printed `Tribute to Hunger ... Emrakul, the Aeons
  Torn is sacrificed, you gain 15` and took it; 48 -> -12. STRATEGY (the guide's Emrakul absolute
  had no counter-shape). W14. The Final Judgment at seq 60 (W39) and the seq 8/15 Elixir
  activations were not the game.
- **vs deck152 (t18) - the Staff tap-out.** 7-card keep (5 lands, Final Judgment, Scatter), Scatter
  on Aspirant t5, Path x2 (Aspirant, Katilda), Judgment exiled by Elite Spellbinder, Verdict at 3
  (seq 21, correct). Seq 23 (t16): Staff at `taps you out` with Essence Scatter in hand ->
  Huntmaster #1 resolved t15... (JSONL turn is one off: log t17) with no window, 20 -> 8; Elixir +
  Staff ping at seq 25-26, Revelation X=2 at seq 28-29, dead to 4 Wolves + 2 Huntmasters. W15.
- **vs deck162 (t23) - no answer to four punishers.** Cancel x2 (Master of the Feast t6, Dictate
  t8) and Scatter (Fate Unraveler t12) spent correctly; Underworld Dreams x3, Ob Nixilis and Forced
  Fruition resolved with NO window because the hand held no counter that could target them (only
  Essence Scatter / nothing) - every `Cast nothing` from seq 27 on was W8-correct. 1-4 damage per
  draw step from t11; the seat never took combat damage. No guide content beyond the seq 24 Path on
  Shield Sphere (W19) and the seq 33 Cancel at 2 life (recorded).

## Other counted behaviour (deck125)

- Elixir library floor: min library 21 (`vs130` seq 81-84); the `<= 16` rule never fired. Elixir
  activations at life <= 15: `vs130` seq 67 (9 life), 102 (7); `vs152` seq 26 (8) - all taken.
- `[DRAW PRICE:]` reached Cancel/Elixir rows under Forced Fruition (`vs162` seq 27-33) - lane U's
  opposing cast-trigger price shipped on this seat (wave-48 MED #4 CLOSED). `DRAW FORECAST`
  present in all 21 `DRAW PUNISHERS` windows.
- Emrakul: cast 4 times (`vs146` 92 at `leaves 0`, won next turn; `vs123` 55 and 83, won; `vs126`
  117, lost). The extra turn IS given by the engine ("Turn 36 - YOUR turn ... Turn 37 - YOUR
  turn" in `vs146`).
- Counters at land destruction: `vs130` seq 20 (Stone Rain, Gavel), 60 (Lay Waste, Fracture), 106
  (Lay Waste, Gavel); guide silent; won at t54.

---

# deck126 - Sanguine Blood (WGB wall prison). 4W-2L

## Prediction set A - wave-48 guide edits (D36-D42)

**D36 (Tribute in EVERY `of which 1 is a creature` window with Tribute on the menu: N/N; 0 "if it
becomes" replies) - PASS 4/4, 0 replies.** `vs130` seq 6 (Dwarven Blastminer, t4), `vs146` seq 7
(Triumphant Adventurer, t5), `vs123` seq 54 (Thraben Doomsayer, t21 main 2 - the first window it
was offered), `vs125` seq 104 (Emrakul 15/15, gain 15 -> the loop, won at t60). F5 held; lane T's
row (`{right now: they control 1 creature - <name> is sacrificed, you gain N}`) was on every one.

**D37 (Tribute at `0 are creatures`: 0) - PASS 0/~120** (every row printed `at 0 this does
nothing`).

**D38 (Lantern with an entry 1-6 row on the menu, or with one out: 0; Lantern as the only row: N/N)
- FAIL 2 + 4.** Over a wall: `vs162` seq 5 (Wall of Omens), `vs146` seq 9 (Pride Guardian, cast
next window anyway). Second Lantern: `vs130` seq 34, `vs125` seq 73, 78, 100. Only-row Lanterns
taken: `vs130` seq 24, `vs125` seq 6 (2/2). Edited F13 (evidence + explicit "or cast nothing").

**D39 (both enchantments out, every offered Vampire attacks: N/N) - PASS 3/3** (`vs130` seq 39
`ATTACK: A1, A2, A3`, won). **Rule #2 as a whole FAILED 3 windows**: `vs152` seq 24, 25, 27 with
both out and `Cast Tribute to Hunger {right now: they control 7 creatures}` on the menu - Wall of
Omens, Battlement, `Cast nothing` ("wait for the opponent to attack") - then seq 28 took it and won.
Edited F10.

**D40 (Staff ping in every upkeep with Bond out: N/N) - UNTESTED.** No Staff resolved on any
deck126 seat (the one drawn was discarded to Pelakka Predation, `vs146` t9).

**D41 (one blocker on a menace A-line: 0; two menace attackers one-each: 0) - UNTESTED.** 4 blockers
windows (`vs152` seq 8, 13, 19; `vs146` seq 11), no menace attacker.

**D42 (mana-only skips per game; all-mana leaks 0) - FAIL 6 leaks.** Skips 0 / 0 / 44 / 0 / **265**
/ 132 (`vs130`, `vs162`, `vs152`, `vs146`, `vs125`, `vs123`). Leaks: `vs125` seq 66, 68, 75, 77,
85, 87 - each a `priority` window whose ONLY row is `Add 3 (or 4) green mana with Overgrown
Battlement [cost: Tap]`; three with an opposing counterspell on the stack (seq 66 Cancel on Sorin,
75 Dream Fracture on Lantern, 85 Cancel on Exquisite Blood), three with an EMPTY stack (68, 77,
87), each 14-16 KB, all answered `pass`. MED #3.

## The losses (deck126)

- **vs deck162 (t13) - the draw lock, with two guide breaks inside it.** Kept 3 lands + Sorin,
  Blood, Wall of Omens, Lantern. Seq 5 Lantern over Wall of Omens (F13); seq 7 Wall of Omens (+
  Pride Guardian) over `Cast Exquisite Blood ... taps you out` with the plan "ramp for Sorin"
  (F12); Blood resolved t9; seq 12 a SECOND Blood with the plan "Both enchantments are now on the
  battlefield" (F11) - Sanguine Bond never drawn, no Tutor drawn. Underworld Dreams (t8), Fate
  Unraveler (t10), Ob Nixilis (t12): 20 -> 2 by t13's draw step, Pride Guardian shot by Ob's -2.
  The seat declined Wall of Omens under `[DRAW PRICE:]` at seq 13/14/18/20/21 (correct). No guide
  lever on the lock itself; the two breaks cost tempo, not the game.
- **vs deck146 (t14) - a flood, a benched-rule block, and an ENGINE Tribute.** Mull to 6 (1-land
  seven), kept 5 lands + Guardian + Tribute. Seq 7 Tribute took Triumphant Adventurer (D36). Seq 9
  Lantern (F13), Guardian; seq 11 Pride Guardian on Nadaar `(your blocker dies, attacker lives)`
  at `you would be at 18`, no enchantment - died (F14). Seq 13 (t9 main 2): Tribute at `they
  control 2 creatures` (Nadaar + Goblin token) - **resolved with NO sacrifice and NO life gain**
  (HIGH #1). From t10 the hand was lands only (three Savannahs, a Scrubland drawn), no cast
  windows arose t11-t13, 24 -> 9 -> -5 to Nadaar + Silencer + Goblin.

## Other counted behaviour (deck126)

- Attack rule: 12 attackers windows, 12 sends, all on `[no creature they control can block this
  attacker]` rows (PASS). `vs123` seq 27 `ATTACK: A1, A2` with one A-row: A2 silently trimmed
  (R44 carried), the attack executed. Seq 51 named attackers by name (`Vampire #1, ...`) - parsed.
- Tribute at 2+ creatures (entry 8): cast `vs146` seq 13 (the engine event), `vs130` none; held
  `vs152` seq 11, 16-18 (Tutor / nothing above matched? seq 16-18 held at 3 creatures with nothing
  else on the menu - entry 8 says cast; 3 windows, life 28, no cost) - recorded.
- Battlement tap windows with a real menu: taken 0; the win-button game `vs125` cast Sorin x2 (both
  countered) at the top of Rule #7a - correct - and then had no trigger for 45 turns until Emrakul.
- Multi-CHOICE replies 1/272 (`vs125` seq 57); `post_answer_overrun` max 5,651 -> not re-measured.

---

# Prediction set B - engine lanes (where my seats had windows)

| lane | verdict | evidence |
|---|---|---|
| **R** fa1fddf02 - no blockers prompt > 15,000; no prompt > 30,000; B-row parenthetical repeats <= 2; header attacking <= creature count | **PASS on blockers (max 8,406, `deck126 vs152` seq 13); FAIL on the 30,000 ceiling by ONE prompt (34,333, `deck125 vs123` seq 13 - HIGH #2, a target-clause enumeration, not a B-row); B-row repeats 0/4 windows; vigilance header not exercised (Nadaar `[vigilance]` attacked alone `deck126 vs146` seq 11, header not double-counted at 1)** | 9 B-rows read, e.g. `B1. Perimeter Captain (0/4) [defender] [deals 0 - ...] - may block A1 (neither dies)` |
| **U** 2d6b16d8f - LOOP clause on every Bond+Blood board prompt; 0 non-`none` ATTACK into it; 0 "net 0"; Revelation/Clue rows priced; DRAW FORECAST wherever PUNISHERS; plan echo <= 5; 0 "game is lost" carries | **PASS on the clause (101/101 `deck125 vs126` windows from seq 19; deck126 own-side 38 + 5 + 94); 0 attacks into it (deck125 has none; deck126's attack `vs130` seq 39 is its own loop); "net 0" 0; `[DRAW PRICE:]` on Wall of Omens rows `deck126 vs162` seq 12-21 and on Cancel/Elixir rows under Forced Fruition `deck125 vs162` seq 27-33 (wave-48 MED #4 CLOSED); FORECAST 21/21 + 13/13; plan-echo max run 9 (`deck125 vs126`) and 5 (`vs130`) - the 5-echo expiry did NOT expire the deck125 plan (it names an action, "ping their face", so by the lane's own rule it carries; not a fail); "game is lost" 0** | the clause's owner-side wording is G1 in `deck126/general-suggestions.md` |
| **T** a2fd9684a - 0 casts tapping a colour source while a non-colour source could pay; creature-tapping casts pre-combat 0; edict casts at N=0 0/N; sweeper casts on `destroys 0` rows 0/N | **edict PASS 0/~120; sweeper `destroys 0` PASS 0/~150 - but a Final Judgment went off an `exiles 5 ... (0 able to attack)` row (`deck125 vs126` seq 60) and a Lightmine off a `deals 0 until they have an attacker` row (`deck125 vs130` seq 50): the two rows the prediction did not name; `{paying this taps:}` 0 occurrences on deck126 (UNTESTED / G2); `[this land is TAPPED` and `{tapping <name> now:` 0 (no such rows in these decks); payment order not traced** | the edict row `{right now: they control 1 creature - Emrakul, the Aeons Torn is sacrificed, you gain 15}` (`deck126 vs125` seq 104) is the corpus's clearest "row carries the fact" specimen - and the model took it |
| **S** 63e456fdf - off-menu-name replacements executed 0; false `answer_replaced` 0; `repeat_count_missing` without re-ask 0; index-past-menu without `named_row_reask` 0, >= 50% recovered | **PASS on my seats by absence (0 `answer_replaced`, 0 re-asks, 1 `unparsed_reply` that was garbage not an off-menu name); the exit row `Cast nothing right now (combat comes next this turn)` present in 239 first-main windows; `kCastAnsweredFact` not checked** | other seats' `named_row_reask` (deck146 vs126 seq 9) and `repeat_count_reask` (deck123 vs125 seq 11) exist - the lane's own reviewer adjudicates |
| Vita/human-seat fixes | not LLM-visible; no window | |

---

# Engine / render items (ranked, with repro)

Corpus root: `~/.Wagic/ai/gpt/selfplay-runs/matchups-20260827-094106/`

**HIGH - #1: an edict resolved with NO sacrifice and NO life gain against a two-creature board,
because a stale reveal zone from the previous turn's Pelakka Predation was consulted as the
choice set.** `1787841695-ai_baka_deck126-0x55ab47d01810-vs-ai_baka_deck146.jsonl` **seq 13** (t9
main 2, life 24): `Cast Tribute to Hunger {2}{b} {right now: they control 2 creatures - they choose
which one}` taken; the opponent controlled Nadaar, Selfless Paladin (3/3) and a Goblin token (1/1,
created by Lost Mine at t9). Seq 14 events: "You cast Tribute to Hunger / Your Tribute to Hunger
resolved and went to your graveyard / Opponent used: Gain life equal to its toughness with Tribute
to Hunger / Your revealed Savannah went to your hand / Your revealed Savannah went to your hand" -
no "died", no "You gained". The other seat's log
(`1787841695-ai_baka_deck146-0x55ab469c8240-vs-ai_baka_deck126.jsonl` seq 15 events) says it
outright: **"You revealed 2 cards but none was a legal target - took none / You used: Gain life
equal to its toughness with Tribute to Hunger / Opponent put the revealed Savannah into their hand
x2"**. One turn earlier (t9) that seat had cast Pelakka Predation (`reveal:type:*:opponenthand
revealzone(opponenthand) optionone ... target(*[manacost>=3]|reveal) reject ... optiontwo ...
all(*|reveal) moveto(hand)`) and taken Staff of Nin; the two Savannahs it revealed stayed in the
reveal zone until the Tribute's `notaTarget(creature|mybattlefield)` chooser fired, found "2
revealed cards, none a legal target", and took none. Rules: Tribute to Hunger's target must
sacrifice a creature (Oracle: "Target opponent sacrifices a creature. You gain life equal to that
creature's toughness"); a 3/3 or a 1/1 had to go and the seat had to gain 3 or 1. Inference (not
stepped in code): the `notaTarget(...)` sacrifice picker shares the reveal/choice machinery, and
Pelakka's option-two `moveto(hand)` did not empty `revealzone` at resolution - the "put back" only
executed when the next chooser ran. Repro shape for the suite: opponent resolves any
`revealzone(opponenthand)` reveal, next turn the revealed player casts Tribute to Hunger / Devour
Flesh / any `sacrifice!$ targetedplayer` edict.

**HIGH - #2: the `legal targets right now:` clause on a cast row is enumerated one target per
name, uncollapsed.** `1787841678-ai_baka_deck125-0x561c77675570-vs-ai_baka_deck123.jsonl` **seq
13** (t10 main 1): 34,333 chars, of which **28,755** are the row `3. Cast Path to Exile {w} ... -
legal targets right now: Lord of Lineage (creature 5/5) [...], Vampire #2 (creature 4/4) [flying,
doesn't untap during its controller's untap step], ..., Vampire #322 (creature 4/4) [...]` - 321
identical-text token entries at ~89 chars each, while the opponent battlefield line (630 chars)
collapsed the same board and the header read `329 permanents listed, of which 323 are creatures`.
Lane R's "no prompt > 30,000" fails on this record alone. Fix = the battlefield/A-row range
grammar on the target clause (`Vampire #2-#322 (creature 4/4) [...] x321`).

**MED - #3: the mana-only gate leaks a single-row `Add N green mana with Overgrown Battlement`
window - three times with an opposing spell on the stack and three times with an EMPTY stack.**
`1787841701-ai_baka_deck126-0x562af4e53780-vs-ai_baka_deck125.jsonl` seq 66 (stack: opponent's
Cancel targeting Sorin), 68 (empty), 75 (Dream Fracture on Lantern), 77 (empty), 85 (Cancel on
Exquisite Blood), 87 (empty); each `priority`, Main phase 1, options_text = that one row, 14-16
KB, all `pass`; `mana_only_windows_skipped: 265` in the same game, so the gate is live and these
six are its residue. The empty-stack three are the clean repro (something after a countered cast
- floating mana `Already in pool: {g}{b}{b}{w}` was on the seq 66 line - may be what defeats the
gate's "nothing but mana" test). Not traced in code.

**MED - #4: the sweeper row's `(K able to attack)` is 0 on the caster's own turn for
summoning-sick and tapped creatures, so it does not separate "five walls" from "323 fresh
tokens".** Same `vs123` seq 13 row: `destroys 323 of their creatures (0 able to attack)`; `vs126`
seq 60: `exiles 5 of their creatures (0 able to attack)`. The guide reads the header's "N of them
without a restriction against attacking" instead; `deck125/general-suggestions.md` G2 asks the
row to carry that count.

**LOW - #5: decode-collapse reply.** `1787841686-ai_baka_deck125-0x5584287653f0-vs-ai_baka_deck130.jsonl`
seq 101 (t52): 409 chars of CJK/punctuation garbage after 48.3 s, `unparsed_reply`, Baka took the
2-row window. Only such reply on my seats; `retry` did not fire (as designed for a parsed-nothing).

**LOW - #6: `ATTACK: A1, A2` with one A-row silently trimmed** (R44 carried).
`1787841703-ai_baka_deck126-0x55ce886fa020-vs-ai_baka_deck123.jsonl` seq 27: options_text has
`A1` only, reply `ATTACK: A1, A2`, `chosen_text: Vampire`, no `dropped` count on the record.

**LOW - #7: reveal-residue narration** ("Your revealed Savannah went to your hand" x2 at a
Tribute resolution, `vs146` seq 14 events) - the visible symptom of HIGH #1; not a separate fix.

**INFO - lane T's edict row worked at both ends:** `deck126 vs125` seq 104 printed `{right now:
they control 1 creature - Emrakul, the Aeons Torn is sacrificed, you gain 15}` and the seat took
it (win); the deck125 seat's prompt one window earlier (`vs126` seq 117) carried the LOOP clause
"ANY nonzero payment ... is fatal" and the seat cast Emrakul anyway - a guide gap (W14), not a
render gap.

**INFO - Emrakul's extra turn is real** (`deck125 vs146`: "Turn 36 - YOUR turn ... Turn 37 - YOUR
turn"); the primitive's auto= lines show only the annihilator, so it is implemented elsewhere.
Checked because the card text on the row promises it.

---

# Rotation evidence

- **deck125 - KEEP IN ROTATION.** 3-3; two of three losses had a guide lever (W14 new: the
  Emrakul counter-shape; W15: the Staff gate, sixth wave, now with the contradiction removed and
  the gate leading the plan line) and the third (vs162) had none (no counter that could target
  four punishers). Ceilings: X 8/8, walls-on-the-stack 0/8, draw-punisher Revelation 0/N,
  Emrakul-when-safe 3/3. Open: W39 (2 of 4 sweeper casts wrong, sixth wave, re-keyed to the row),
  W40 (3/6), land drop 84/88. Not a freeze candidate.
- **deck126 - KEEP IN ROTATION.** 4-2; the Tribute-at-1 rule reached ceiling (4/4, 0 deferral
  replies) one wave after 0/2; the win button failed 3 windows with the winning row on the menu
  (F10, first corpus of the shape); the pair-as-two-names belief (F11) and the entry-1 hesitation
  (F12) are new; STEP 1 hit its third corpus (F14, re-keyed); the mulligan section carried floors
  until this wave (F15). Re-test F10-F15 before any freeze talk.

# Lategame specimen candidate

Primary: `1787841678-ai_baka_deck125-0x561c77675570-vs-ai_baka_deck123.jsonl` **seq 13** (t10,
34,333 chars: one Path row of 321 enumerated Vampire tokens beside a collapsed battlefield line
and a `destroys 323 of their creatures (0 able to attack)` sweeper row - the two-emitter contrast
in one prompt). Alternate (ordinary lategame): `1787841701-ai_baka_deck125-0x562af3b37450-vs-ai_baka_deck126.jsonl`
**seq 117** (t60, 19,035 chars: 21 lands, both loop halves on the opponent line with the LOOP
clause, an Emrakul row at `leaves 6`, and the losing choice).

# Falsifiable predictions for wave 50

deck125:
- **W46** - `Cast Emrakul` taken while the opponent battlefield line shows Sanguine Bond or
  Exquisite Blood, or the log shows an opposing Tribute to Hunger / Devour Flesh: **0**; taken
  when none of those is present: **N/N**.
- **W47** - Staff of Nin cast from a row reading `taps you out` / `leaves 4` or less with a
  counterspell in hand: **0** (was 3/6); replies whose plan line carries "Staff of Nin only from a
  row reading leaves 5 or more" while the cast breaks it: **0**.
- **W48** - Sphinx's Revelation cast from a row reading `max affordable X=1` or `X=2` at life
  above 10: **0** (was 5); X = 3 whenever the menu reaches 3: **N/N**.
- **W49** - Supreme Verdict / Final Judgment cast at a header second number of 0: **0** (was 1/3);
  Lightmine Field off a `deals 0 until they have an attacker` row: **0** (was 1).
- **W50** - Path at a `[defender]` target of either printed form, or at a textless 1-power token
  above 12 life: **0** (was 1 + 2); Path taken at life <= 12 with a power-2+ candidate: **N/N**
  (UNTESTED three waves; report the window count).
- **W51** - land drop **N/N** (was 84/88); Elixir activation to `Mana available: 2` or less with a
  counter in hand: **0** (was 5). Regression detectors.

deck126:
- **D43** - with both enchantments on the seat's battlefield line and `Cast Tribute to Hunger
  {right now: they control N creatures}` (N >= 1) on the menu: taken in the FIRST such window
  **N/N** (was 1/4 windows); 0 replies containing "wait for the opponent to attack" in that state.
- **D44** - a second copy of Exquisite Blood or Sanguine Bond cast with the first on the
  battlefield line: **0** (was 1); replies saying "both enchantments" with one name absent: **0**.
- **D45** - Exquisite Blood / Sanguine Bond declined for a wall or Sorin when it is the missing
  name and on the menu: **0** (was 1, `vs162` seq 7).
- **D46** - Tribute at `1 is a creature`: **N/N** (was 4/4); at 0: **0**. Regression detectors.
- **D47** - Chromatic Lantern with an entry 1-6 row beside it or with one out: **0** (was 2 + 4).
- **D48** - a blocker assigned off a B-row whose every parenthesis reads `your blocker dies` at
  `you would be at 9` or more with no enchantment out: **0** (was 1, third corpus).
- **D49** - mulligans: a 2+-land hand with a named coverage card shipped: **0**; a keep of a 0-land
  hand: **0**; chains reaching 4: report.
- **D50** - all-mana single-row windows reaching the model: **0** (was 6); `mana_only_windows_skipped`
  per game reported.

# What I did NOT check

- I did not run wagic, touch `bin/Res`, `src/` or git, or read `game-*.stderr`; JSONL +
  `corpus-results.tsv` only. All guide work is under `wave49/`.
- HIGH #1's mechanism is inferred from two seats' narration and the two primitives' auto= lines;
  the reveal/choice code path was not stepped. The recorded fact (edict resolved, nothing
  sacrificed, nothing gained, opponent controlling two creatures) is from the logs.
- Lane T's payment order (which sources were tapped for which pips) - not traced; no
  `{paying this taps:}` string occurred on either seat, and I could not confirm whether the
  Battlement was ever tapped by an auto-payment (no `[tapped]` Battlement found on opponent-turn
  battlefield lines, which is consistent with either "never tapped" or "the clause is suppressed
  for defenders").
- Lane S's `kCastAnsweredFact` and the re-ask kinds - no window on my seats; the other seats'
  instances are cited, not adjudicated.
- Lane R's same-name `#1/#10` interleave - no A/B-row wide enough on my seats.
- Corpus-wide fallback rate and total decision count - not recomputed this wave (my 12 logs only:
  1/751).
- What Baka did in the `vs130` seq 101 fallback window (no `events` on the next record; the
  narration shows no Verdict cast, which is the right outcome either way).
- Scryfall: nothing cross-checked; every card fact is the primitive's text=/auto= line or the
  engine's rendered string.
