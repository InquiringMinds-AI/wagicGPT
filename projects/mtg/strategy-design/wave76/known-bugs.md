# Wave-76 known bugs (corpus matchups-20260910-141526-final: 21 games = 2 first-run + 19 reruns after pilot WEDGE #2, SINGLE binary archives/wagic-d372a1153-w76step1 = b4784ed05; sources wave76/engine-seat.md + wave76/deck<N>/review.md)

Invariant 000 held: 2,188/2,188 decisions with native reasoning, 0 heuristic fallbacks (second corpus running; the one `fallback` value is P14's first live re-ask, answered `x62`), 0 truncations, 0 hung, 21 natural finishes. The PILOT STALL tripwire fired ONCE, a TRUE positive 14 min after the engine froze (wave 73 burned 8 h); 19 matchups rerun as concurrent two-deck pools. Wave-76 items PAID: Q2 max prompt 80,805 → 30,375 B, 0 over 40 KB; Q3b 0 of 219 own-clock tags beside a live crack-back KILL (64 live); Q7 REFUTED verified independently (0/56 false under the record's turn, 30% under the raw observer turn); Q4a/Q4b, Q9 (deck130 Hammer 12 → 0), Q11 (42 `-6` rows / 52 emblem rows, 0 false targets), Q12 (112/112 hold-before-decline), Q14 (3/3 `[usable (tap for mana)]`), F3/F8/F10 PASS; Q1's referent fix holds on the priority seam (0 of 1,011 non-land pairs false, was 214/2,000); Q13's re-put bracket printed 109×, the 152v125 ten-call run did not recur. Identities hold: closes 18 = 13+0+5, replays 948/751, deviations 91, 37 async drops all re-asked. Wave-75 guide edits paid on every deck (deck123 reserve-VERDICT 14/14 declined, deck152 four edits firing, deck125 P5 board never recurred). All seven wave-76 guide EDITs installed (3858f4e67). Wins 152 5/6, 130 4/6, 162 3/6, 126 3/6, 146 2/6, 123 2/6, 125 2/6.

## R — routed to wave 77 (HIGH first)
- R1 HIGH — A WON, DETERMINISTIC LOOP IS PUT TO THE MODEL ONCE PER ITERATION: `126v123` deck126 t17 Main 1 seqs 30-61 — 32 consecutive priority windows while the seat's own PROVEN Sanguine Bond + Exquisite Blood loop resolved one point at a time (opp 17 → 1, seat 24 → 40), the screen printing `LOOP COMPLETE` every time, the model answering `pass` 32/32 (`declined this exact list 31 times`), ≈48 min of decode; 35 such windows at this seat. The board moves each link so no latch or the Q13 key can see it. LEGAL-OPTION BOUNDARY: the rows stay and nothing is auto-answered; the FIX is guidance + a hold path: under `lifeLoopProvenWin(self)` every acting row of the seat's own loop window carries `[your loop is resolving on its own - this row is not needed; HOLD (row N) covers every link until it ends or something changes]`, and the hold latch keyed on the action identity must actually hold across the links (verify with a fixture that walks 5 links after a hold: 0 asks). Measure `own_loop_windows_asked`. Annotation only — key-stability pin set. (engine-seat HIGH-1, deck126 HIGH-1)
- R2 HIGH — THE HOLD-CHECK BRACKET, THREE RESIDUALS AFTER Q1: (a) the LAND-DROP window shares the `"cast"` seam memory — ALL 22 false brackets in the corpus (15 false "every row above was also…", 7 wrong counts) are cast/land pairs, 0 of 1,011 non-land pairs (`125v162` deck162 seq 20; deck126 20/106 cast-seam false at `146` seq 33 / `162` seq 19 are this) — give the land-drop window its own seam key; (b) at streak 1 the bracket asserts "every row above was also on the menu (1 window in a row now)" with nothing yet compared — 11 of 43 streak-1 claims false vs 0 of 76 at streak ≥ 2 (`146v126` deck146 seq 29: rows 1 Kaya / 2 Lolth both new vs seq 25) — at the first asked window print "first window asked at this seam", never a comparison; (c) the `N rows above are new` COUNT is not a count of new rows: a reorder claims 3 new (`130v146` deck130 seq 11 → 14, identical set), a row that only DISAPPEARED claims 1 new (`130v162` seq 53 → 54, `130v126` seq 59 → 62) — count = rows of the current menu whose action key is absent from the last ASKED menu, order-independent; pins for all three; log `hold_check_ref_seq` on the record so the next seat can adjudicate without re-implementing the key. (engine-seat HIGH-2, deck146 HIGH, deck130 HIGH-2, deck126 MED)
- R3 HIGH — THE Q6 CONVERTER CLAUSE READS AS A COST STATEMENT AND THE MIRROR/OPPONENT BRANCHES ARE UNPRICED: `{their converter: ... ANY nonzero payment here is fatal}` on a creature-sacrifice row was read twice as being about the row's mana cost ("Shield Sphere costs 0, so it's safe") — `162v126` deck162 seq 20, lost the game; the clause never names the event (the toughness THEY gain → N off YOUR life through <converter>). Also: the seat's OWN converter is absent from the clause (`126v123` deck126 seq 23), and the Devour Flesh row prices the self-target branch fully and the OPPONENT branch not at all — no life figure, no converter clause (`123v126` deck123 seqs 70/71: handed a Bond opponent 4 life and took 4 off the seat; the four-wave-old edict misread's mechanism). Fix: one clause shape for every life-gain-on-sacrifice row: `[they gain N (its toughness) -> with their <converter> that is N off YOUR life]` / `[you gain N -> with your <converter> N off THEIRS]`, on BOTH branches of a modal edict; pins. (deck162 HIGH, deck126 MED, deck123 HIGH-1)
- R4 HIGH — THE BLOCKERS HEADER'S `you would be at -5 AT BEST (no assignment does better)` IGNORES THE BLOCKING LIFEGAIN THE SAME SCREEN METERS (`up to 4 life for you`; true best −1) — `126v146` deck126 seq 53; the seat quoted `no block saves you` and passed. Fix: the at-best figure folds in the metered lifegain (lifelink blockers, block triggers) and says so; pin. (deck126 HIGH-2)
- R5 HIGH — THE BATTLEFIELD LINE PRINTS NO ABILITY TEXT FOR ANY CREATURE: 3,892 creature entries corpus-wide, 0 glossed, while 2,734 non-creature entries carry `{effect:}` — `125v130` deck125 seq 7: `Dwarven Blastminer {1}{r} (1/1) [tapped…]` and nothing else; the model read it as a 1-power attacker while it land-destroyed the seat to 0 permanents by t17 (the same card's Path row at seq 23 prints the full `{target text:}`). Four deck125 guide rules key on text the board never shows. Fix: creature entries carry a compact `{effect:}` gloss of activated/triggered text (keywords already print), CAPPED (head of the text, ~120 B) — this is STATIC card text, not a board number, so no key pin is needed, but the Q2 size budget is: measure prompt bytes before/after on the 30 KB game. (deck125 HIGH B-1)
- R6 MED — CRACK-BACK COVER RESIDUALS: (a) a TARGETED-removal row (Hammer of Bogardan `{kills: Barrowin, Goblin}` under an 8-point crack-back) gets no cover clause while sweeps do — lane CO's named residual, and it lost `130v146` (deck130 seq 14); (b) the X sub-menu prices kills with no cover clause though the parent cast row has one (`123v130` seq 22); (c) the ATTACKERS menu prices attacking but never staying home — cast rows get `{crack-back cover: … you cover 3 of 6}`, attacker rows only prose, and the model invented the number (`162v152` deck162 seq 14). Fix: the cover family reaches targeted-removal rows (victims the row names), X sub-menu rows, and the attackers menu's `ATTACK: none`/hold-back line. (deck130 HIGH-1, engine-seat MED-2, deck162 MED)
- R7 MED — ATTACK TOTAL COUNTS UNTAPPED CREATURES, NOT LEGAL BLOCKERS: `123v152` deck123 seq 104 says `Their 3 untapped blockers` where only Elite Spellbinder can block 11 fliers, and the floor reads 16 where it is 20; `{crack-back cover:}` prints an unfiltered body count beside a legality-filtered damage figure (`152v123` deck152 seq 31 vs seq 32's single B-row). Fix: the aggregate counts bodies that can legally block at least one listed attacker (Q4a's knapsack already knows minBlockersRequired; feed it flying/reach/menace legality), and the cover clause's body count is the filtered one; pins. (deck123 MED-2, deck152 MED-1)
- R8 MED — Q13's BOARD CLAUSE IS INERT: 109 brackets, 5 print `nothing on the board has changed` (deck125 8/8 `the board HAS moved`, deck123 2 of 83, deck146 2 of 15) — CQ F4's full-state comparison is too strict (the serialized state changes on every tick). Fix: compare the ASK CACHE's board key (already normalised for replay) and say which of the two sentences is true; pin on the 152v125-class shape. (engine-seat MED-1, deck152/125/146/123)
- R9 MED — `plan_names_uncastable_zone_card` NEEDS A THIRD EXEMPTION: tutor / reanimation TARGET plans (deck126 42/42 are protocol-legal tutor-target plans; deck146 ~0/15 real — reanimation objects, spell targets, stack objects, opponents' hand cards, records with no PLAN line) — count only cards named as the seat's CAST intent (a verb of casting/playing next to the name), and skip records with no PLAN line; the graveyard-plan measure has a counter but no note. Measure only. (deck126 MED, deck146 MED, engine-seat LOW)
- R10 MED — THE X-ANNOUNCE WINDOW DROPS THE PLAN ECHO (3/3 on deck130 vs 128/155 asks) AND ITS WORKED EXAMPLE IS DRAWN FROM ROW 1: `130v162` deck130 seq 60 answered `CHOICE: 1 (X = 1)` on a row reading `kills THEIRS: none; YOURS: Dwarven Blastminer`. Fix: the plan echo renders on ANNOUNCE_X like every seam; the example uses a neutral form (`CHOICE: <n> (X = <n>)`), never a live row. (deck130 MED)
- R11 LOW — one lane: same-arm force-close RE-ARMS with an empty park are the whole `forced_close_unrecorded` 5 (Q8's bound never fired: refused/deferred/bound-hits 0) — bound same-arm re-arms the same way (waste only, no loss); a 2,374 B option row (`kills whichever you target:` roster — cap it like Q2's roster); the harness HARVEST keys on mtime (each one-game pool harvested 38 → 17 logs, other pools' in-flight files) — key it on the outdir's game manifest as tools/corpus-early-check.py does, in projects/mtg/tools/selfplay-harness.sh with a --selftest case; the plan echo repeats plans naming cards in the seat's own graveyard 4× in one game unannotated (deck162 LOW); the {X}-announcement payment loss fired ONCE this corpus, fully disclosed and chosen knowingly (`146v125` deck125 seq 179, 17 mana) — no defect, retire the docket item.
- R12 REPORT-ONLY (owner-ruled protocol territory — no lane): the reasoning TAIL — a median 56% of every decode falls after the model's first `CHOICE:` token, the protocol string `Write nothing else` re-quoted median 1 / max 8 times per decision, `reasoning_degenerate` 437/438 on deck125 a true signal driven by the prompt's protocol tail; `Output matches. Proceeds.` loops (deck152). `plan_line_missing` 91 corpus-wide, the dropped label in ~all (sixth wave). A Teferi +1 costs ~10 model calls (a chain, `152v126` seqs 36-47). Surfaced for the owner with the lategame specimen (wave76/lategame-specimen.txt); nothing in the protocol text changes.

Docket: {X}-announcement payment loss — RETIRED (fired once, disclosed, chosen). Still UNTESTED, carried: Q6 (0 converter-live sacrifice asks), Q10 ghostform removes, Q15b/F9, F2 positive (defer path never fired), F5, F6 (Erebos), Q3a's withdrawal (0 LOOP COMPLETE blockers headers), Q2's copy-row fold (0 copy rows), BS F2/F5/F6, CF F1/F2/F5/F8/F9, CM F1/F7, the reasoning-body halves of N14a/N14d/O20. Census rule (fourth wave): the brief's off-protocol / plan-line-missing / latency figures must be CORPUS-WIDE (the stats helper now prints a CORPUS-WIDE line — use it).
