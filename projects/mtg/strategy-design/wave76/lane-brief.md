# Wave-76 step-one lane brief (base = master after wave75 known-bugs; evidence = wave75/known-bugs.md §Q + wave75/engine-seat.md + wave75/deck<N>/review.md)

RULINGS THIS WAVE TOUCHES: 000 — reasoning only in the native channel; reply = PLAN then action, nothing else; no instruction licenses prose; no parser reads outside the two labelled parts (a present `CHOICE:` label on the PLAN's physical line is inside — P9); caps never bound reasoning; the golden protocol TEXT (reply-protocol.txt / kReplyProtocol) and the thinking regime are owner-ruled — render brackets and row text may change; Q16 is REPORT-ONLY. 00 — full games; a game answered by heuristics is not a corpus game. 3 — suite THREADS=1 0 failed (known flakes: lifeline, merrow_reejerey, intrepid_adversary_repeated_payment), PARSETEST 0 failed. Never remove, cap, or auto-answer a legal option (Q2's fold and Q13's bracket are DISPLAY — every option stays answerable; Q9 is MEASURE ONLY). No VPK. No AI attribution. NO CORPUS from a lane; no lane sends requests to the pilot this wave.

Standing mechanics: as wave71/lane-brief.md (worktree-only; `make -f Makefile.sdl -j8`; both link guards OK; binary freshness; every wagic run under `systemd-run --user --scope -q -p MemoryMax=4G -p MemorySwapMax=0 --`; suite THREADS=1 as a detached unit `w76-<LANE>-suite` waited with a FOREGROUND until-loop (no Monitor); count both failure strings; PARSETEST from bin; Edit tool for UTF-8 sources, byte-splice for MTGRules.cpp/GameObserver.cpp/primitives; U+FFFD 0; RED-on-base evidence + a falsifiable prediction per fix; commit with explicit paths, `git add -f` under bin/Res; write wave76/lane-<LANE>.md; touch `~/.gatelogs/W76_<LANE>_DONE`; weakest evidence). LESSON OF WAVE 74 stands: any NEW annotation carrying a board-derived number must be proven OUTSIDE every key (ask, async slot, hold latch, hold-check, option-set) with a PARSETEST pin that two windows differing only in that number yield identical keys, and the held set in a key pin is built the way the LIVE seam builds it (holdActionKeyRow via mLastMenuRows). LESSON OF WAVE 75: (1) an instruction's REFERENT must be something the reader can see — the hold-check bracket spoke of "the last window" meaning the last one the engine BUILT, while 3,402 built windows never reached the model; any bracket that refers to history refers to ASKED history; (2) a helper that hardcodes its input (the wave-74 stats script ignored its argument) reports the wrong corpus with full confidence — new tools take the run dir as an argument and echo it in their first output line; (3) a prediction's negative clause ("non-zero means lost") needs its own pin, not just the positive.

## Lane CN — the window/hold and meter family (Q1, Q7, Q8, Q13, Q9)
Q1 first (the HIGH): the hold-check memory keys on ASKED windows and the referent is worded as such; holdActionKeyRow strips every `{...}` annotation group (keeps `[cost:]`); pins seq 28→33 and 162 seqs 16/24; full key-stability pin set. Q7 at the member plumbing with a two-main-phase fixture through the real calls (the pure predicates are already green and prove nothing about the plumbing). Q8 arm bound + the SUPERSEDED rewrite in lane-CM.md. Q13 the cross-phase re-put measure + annotation-only bracket (outside every key). Q9 the two exemptions, the per-record field, and the stranded-card widening (measure only). Weakest-evidence section names what could not be reproduced from the translog (the engine's own reference set is not logged — consider logging the hold-check referent's seq on the record).

## Lane CO — combat and loop truth (Q3, Q4, Q5, Q6)
Q3 one shared stated-lethal predicate consulted by the combat header and the own-clock tag (and any survival clause the lane finds) — pins per lethal source; do NOT suppress the clock under a loop with no stated incoming loss. Q4 menace in the aggregate cover; the floor sentence requalified under an attack-trigger permanent (name it; find the trigger class in the engine, not a card list). Q5 the removal-row crack-back cover as a clause of the existing cover family. Q6 the converter clause on the sacrifice row. Every new clause with a number: key-stability pin set.

## Lane CP — row and prompt truth (Q2, Q10, Q11, Q12, Q14, Q15)
Q2 first (the HIGH): cap the `- THEIRS:` enumeration (count + distinct shapes) and fold identical `(copy N of M)` rows at the discard seam with the option set intact — pin the seq-394 shape under 12 KB with all five rows answerable and the answer→engine index mapping proven for the folded rows (the wave-72 O19/name-menu double-permutation finding is the hazard). Q10 removal forecast reads liveness like O1. Q11 up-to-N target list / no target on emblem rows. Q12 stable closing-row order on both seams. Q14 the usable tag by permanent type. Q15 cosmetics + the forced-close exclusion from `prose_outside_two_lines` + the Lightmine Field narration check (engine-level: is the script `foreach` per body? if so the narration is one event per body by design — report, do not change the script unless Oracle disagrees).

## Lane CQ — fix lane for the Astra review (wave76/codex-review.md) — seeded after step-one gate 1 — base = master f3eff598d (gate 1: 1289/0, 76 AI/0, PARSETEST 6112/0)
Ten findings, each CONFIRM (fixture/pin RED on base reproducing Astra's trigger, then fix) or REFUTE (a pin proving the trigger cannot occur, with the reason). Every pin exercises the LIVE caller path, not a helper fed assumed inputs (that is the review's recurring charge: "green pins encode helper assumptions"). Key-stability pin sets with the held set built as the live seam builds it.
- F1 HIGH Q1 (AIPlayerGPT.cpp:~29360, lane CN): the same-window early return in holdReopenNote bypasses staging after `w76HoldWindowNotAsked` erased the pending entry — a later changed menu earns a record but commits nothing, and a returning row is called "seen at the last asked window". Fix: the early return must not skip staging when the window will earn a record (commit on record, not on note reuse); pin the sequence Astra names ({A,B} asked → identical suppressed → {A} built same-seq → record → B returns).
- F2 HIGH Q8 (AIPlayerGPT.cpp:~17696, lane CN): a REFUSED force-close arm neither parks the decision nor schedules a re-ask — a 200 with reasoning and no answer then falls through `pollCompletionRetry` as success-with-empty-content and the HEURISTIC plays it. A refused arm must leave the decision PENDING (kChoicePending) and re-arm when the parked close on the other arm resolves (or arm a plain re-ask) — never the heuristic, never a permanent pending (deadlock is worse than a lost window: pin BOTH that the heuristic is not called and that the decision resolves within N ticks once the other arm clears). Pin through the caller, not the swap helper.
- F3 MED Q13 (AIPlayerGPT.cpp:~39834, lane CN): the "phase-free" re-put key still carries the phase inside the `decision` string (`Casting decision (Upkeep, opponent's turn)` vs `(Draw, ...)` at ~42670), so the 152v125 ten-window shape never matches across phases; the pin used an artificial phase-free string. Fix: strip the phase clause from the decision text in the key (or key on the seam id); pin with the LIVE decision strings.
- F4 MED Q13 (AIPlayerGPT.cpp:~40144, lane CN): the bracket asserts "nothing on the board has changed" from turn+question+rows only — no board snapshot compared; an unrelated permanent entering between asks makes the sentence false. Fix: store the board key (the ask cache's board-state key already exists) in the entry and require equality before printing the clause; else print only the re-put count. ALSO `mCrossPhaseRePuts++` runs before the pending-poll return → counted on every poll tick; count once per consumed ask.
- F5 HIGH Q5 (AIPlayerGPT.cpp:~2555, lane CO): `crackRemoved` counts a creature's whole crack-back contribution without regeneration/return checks — a regenerating 3/3 under Day of Judgment survives, the clause promises survival at 2 life. Fix: the victim classification reads the same liveness the `{removes:}`/O1 forecast reads (regeneration shield, indestructible, ghostform/undying return); pin a regenerating attacker = not subtracted, and the returning cases.
- F6 HIGH Q6 (AIPlayerGPT.cpp:~46437, lane CO): the converter clause prices their Sanguine Bond gain without consulting life-gain prohibitions — Erebos on the seat's side means no gain event, no Bond trigger, yet the row claims 6 off the seat. Fix: consult the same prohibition predicate wave-72 finding 9 fixed for LOOP COMPLETE (Erebos / `nolifegainopponent`); pin Erebos = no converter clause.
- F7 MED Q2 (AIPlayerGPT.cpp:~8750, lane CP): the roster's 12-group shape cap bounds SHAPES, but a group emits every body it holds — 100 Vampires alternating two P/T shapes (every second one with a +1/+1 counter) print all 100 because consecutive-rank runs never form. Fix: a group prints count + shape + at most K named handles (K small, e.g. 3) with `and N more` — never the full list; pin the alternating shape under a fixed byte bound; the residue stays NAMED as a count, never trimmed silently.
- F8 MED Q9 (AIPlayerGPT.cpp:~7342, lane CN): the self-recursion exemption scans ordinary `magicText` for `moveto(hand)` and exempts Unsummon (it bounces a TARGET, not itself). Fix: the exemption requires the ability to be a graveyard/exile-zone activated ability of the card itself (the ability's zone = graveyard/exile and its target = self, as Hammer's script has); pin Unsummon counted, Hammer exempt. Measure only.
- F9 MED Q15 (AIPlayerGPT.cpp:~7267, lane CP): `forced_close_prefill_echo` is assigned from the forced-close FLAG, not from a byte match against the prefill — new prose after a valid pair is mislabelled. Fix: classify as echo only when the off-protocol bytes are a prefix/substring of the supplied prefill trace; pin both cases.
- F10 MED Q1 (AIPlayerGPT.cpp:~85939, lane CN): the new PARSETEST pin REQUIRES unequal option-set keys for rows differing only inside a nested `{reserve: … {4} …}` annotation — it pins the forbidden drift as correct (CN flagged it "out of lane scope"). Fix: the shared option-set stripper (`stripRenderAnnotationsLc`) strips BALANCED groups too (the wave-74 lesson says every key must be stable across board-derived annotation numbers); rewrite the pin to require EQUAL keys; then run the full key-stability set and the whole PARSETEST corpus — any pin that assumed the first-`}` behaviour must be re-derived, not adjusted to pass.
Order: F2, F1, F10, F5, F6 (legal-option / deadlock / key / false verdict), then F7, F3, F4, F9, F8. Gate: suite THREADS=1 + PARSETEST + harness --selftest + instruction guard; lane report wave76/lane-CQ.md with per-finding CONFIRM/REFUTE, RED evidence, fix, GREEN, prediction, weakest evidence.
