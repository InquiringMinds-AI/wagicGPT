# Wave-76 step-3 brief (lean; REJECT is the expected verdict for anything not paid for by a decision the corpus shows going wrong. Amendment 332 stands: guides are decision-time instruction only, hard ceiling 20,000 bytes, no citations/history in a guide — evidence goes in YOUR review.md. Amendments 333 (heading = scope declaration) and 334 (no concessive clause for a rule's hard case) are drafted in wave74/strategy-writing-skill-v2-amendments.md, not yet folded in by the owner — apply them anyway.)

Seventh reasoning-on corpus (invariant 000). Wave 76 step one (wave75/known-bugs.md §Q → wave76/lane-{CN,CO,CP}.md + wave76/codex-review.md → lane-CQ.md, ten findings all CONFIRMED and fixed): CN window/hold — the hold-check bracket's memory commits only on windows that EARN A RECORD and its referent reads "the last window I asked you at this seam" (Q1 + CQ F1), `holdActionKeyRow` strips BALANCED brace groups (the wave-75 residual false-NEW was nesting, not unstripped braces), the main-phase-skipped meter's 36% was a stderr turn-index off-by-one — REFUTED, 0/94 false under the right mapping (Q7), a force-close arm is REFUSED/DEFERRED while the other arm's close is parked (kChoicePending, no decode bought, arms when the park clears or at 8 ticks — Q8 + CQ F2; counters `force_close_arms_refused/deferred/defer_bound_hits`), `crossphase_identical_reputs` + a prompt-only bracket naming the asked window, phase and the model's own decline, printed only when the BOARD key also matches (Q13 + CQ F3/F4), `plan_names_uncastable_zone_card` per record with self-recursion (a self-targeting graveyard/exile ability — Hammer) and cast-from-exile exemptions and the stranded/uncastable measures made independent (Q9 + CQ F8); CO combat/loop — one shared `w76StatedLethalOnScreen` predicate (stack / combat / crack-back KILL / loop + stated loss) consulted by the blockers header (withdraws `NOT lethal` under a proven opponent loop, naming why) and the own-clock tag (Q3), the ATTACK TOTAL aggregate cover is an exact knapsack over `minBlockersRequired()` so menace costs two bodies, and the floor sentence is requalified under an `attackPunisher`-class permanent of theirs (Q4), removal rows under a stated crack-back print `{crack-back cover:}` from the bodies they remove, regenerating/returning bodies NOT subtracted (Q5 + CQ F5), the forced-sacrifice row adds `- and with their <converter> that is N off YOUR life`, silent under a lifegain prohibition (Q6 + CQ F6); CP rows/prompt — the `- THEIRS:` victim roster prints count + distinct shapes + at most K names + `and N more` (Q2 + CQ F7), identical `(copy N of M in your hand)` rows fold at the discard seam with the option set intact (`copyTagOwnsRow` — the wave-75 diagnosis "the fold does not reach copy rows" was wrong: `splitRowHandle` precedence keyed them on a battlefield handle from their own roster), `{removes:}` reads ghostform liveness (Q10), up-to-N loyalty rows print the target list or `(choose up to N next)` and emblem rows no target (Q11), closing rows in one stable order, decline last (Q12), the 3-life land usable tag by permanent type (Q14), `forced_close_prefill_echo` by byte match against the prefill (Q15 + CQ F9), the shared option-set stripper strips balanced groups (CQ F10 — two inherited pins re-derived). Lightmine Field's N×1-damage narration is the script's own `foreach` — REPORTED, unchanged, docketed for a card-script lane. Records carry `thinking`, `reasoning`, `reasoning_chars`, `off_protocol_bytes`, `plan_line_missing`, `plan_steps_done`, `plan_names_uncastable_zone_card`, `http_error_body`, `reasoning_ngram_repeat`; gameend carries the counters below. Wave-75 guide edits for all seven decks are the LIVE guides (installed 2026-09-10 at 21d2203cf; each deck's before->after list is in wave75/deck<N>/review.md).

Repo /home/magi/Projects/wagicGPT, game dir projects/mtg. Use python for translogs; use /usr/bin/grep (bare grep drops matches). DO NOT edit anything under bin/Res, src/, or run git or wagic; write only under projects/mtg/strategy-design/wave76/. Never write mechanisms as the owner's positions. Report what you did NOT check.

CORPUS (wave 76, SINGLE binary archives/wagic-d372a1153-w76step1 = source b4784ed05; `--thinking on`, WAGIC_GPT_TIMEOUT=900, no operator token ceiling). INCIDENT: the first run (matchups-20260910-141526) hit the Spark pilot's silent engine wedge at 15:42 CDT, 1 h 27 min in; the new PILOT STALL tripwire fired at 15:56 (a TRUE positive, 14 min after the freeze) and stopped the run with 2 games finished (146v152, 152v162; start epochs 1789067756 / 1789067732); the container was restarted and the 19 stalled matchups RERUN as 19 concurrent two-deck pools (start epochs 1789074083-1789074169, all finished naturally). The corpus under review is the consolidated dir /home/magi/.Wagic/ai/gpt/selfplay-runs/matchups-20260910-141526-final (42 seat logs all with gameend, 0 timeout-tainted, merged results.tsv, askreplay/ 25 files); the stalled first-run seat logs are NOT in it. 21 games, all completed naturally, 0 HUNG/crashed; 2,188 decisions (wave 75: 2,919 — shorter games, 1.73 ask-decisions/turn vs 2.15) = ask 1,594 + priority 412 + attackers 94 + discard 40 + blockers 29 + reveal 13 + bottom 6, 100% with native reasoning (median 5,613 chars, max 21,437), 0 heuristic fallbacks (the single `fallback` value is one `repeat_count_reask` at a priority seam = P14's re-ask, a MODEL re-ask not a heuristic answer — engine seat (n)), 0 reply_truncated, 0 action_before_plan, 57 off_protocol_bytes>0 (3.6%), 74 plan_line_missing (4.6%), latency p50 92.4 s / p95 193.8 s; gameend sums: `ask_replays_reserved` 948 / `identical_ask_answers_reserved` 751, `hold_released_turn` 89, `hold_windows_skipped` 1,280 (cast 872 / priority 408), `stop_reached_windows_skipped` 18, `chain_windows_only_selfharm` 0 / `chain_selfharm_rows` 274 / `chain_acting_rows` 1,570 (+ `_cast` 327 / 421 / 854), `crossphase_identical_reputs` 111 (NEW), `force_close_arms_refused` 0 / `_deferred` 0 / `_defer_bound_hits` 0 (NEW — the F2 path never fired), `forced_close_unrecorded` 5, `phase2_answer_recovered` 13 / `_missing` 0, `main_phase_windows_skipped` 56, `mana_only_windows_skipped` 136, `own_turn_windows_skipped` 571, `reserve_decline_windows_noted` 28 / skipped 3, `plan_names_stranded_card` 1, `plan_names_uncastable_zone_card` 102 (wave 75: 68 — per record now), `protocol_deviation_replies` 91, `async_drops` 37, `repeat_annotated_takes` 4, `blocker_forecast_gang` 16 / multi 96 / collapsed 40 / rows 206; wins deck152 5/6, deck130 4/6, deck162 3/6, deck126 3/6, deck146 2/6, deck123 2/6, deck125 2/6 (wave 75: 152 5/6, 146 5/6, 162 3/6, 125 3/6, 126 2/6, 130 2/6, 123 1/6 — deck130 2→4, deck146 5→2; hands are real, do not read variance as guide effect).

## Per-deck agent (one per deck: 146, 152, 125, 126, 162, 123, 130)
Purpose (owner's words): read the transcripts of your deck, compare the LLM's actions to the
instruction set (the guide projects/mtg/bin/Res/ai/baka/deck<N>_strategy.txt + the general guide
wave62/general-strategy.md (wave-59 edition + R331; NOTE from the wave-63..67 syntheses: this file is NOT loaded at runtime (only deck<N>_strategy.txt is opened, AIPlayerGPT.cpp:15332) — the deck guide is the live surface) + the reply protocol in the prompt), find issues in the game, the
interface, and the guide, and surface anything else worth the core loop's attention.
Method: for each of your deck's 6 games (your seat's translog; the opponent's for context),
trace the decisions that decided the game. Classify each misplay PERCEPTION (the model misread a
true surface, or the surface was false/missing -> ENGINE/RENDER item, cite file+seq) vs STRATEGY
(read correctly, decided against the guide or against good play -> guide item). Note fallbacks,
re-asks, and any decision where the prompt's information was insufficient or wasteful (what a
better interface would have shown). Hands are real (mulligans, bottoming): do not read variance
as guide effect.
Output: ONE file, wave76/deck<N>/review.md: (1) game-by-game outcome + the deciding decisions with
seq citations; (2) engine/interface/card items ranked HIGH/MED/LOW with a concrete repro (file,
seq, the rendered line) — this is the primary deliverable; (3) guide verdict: KEEP as is (a
success verdict, state why) or EDIT — if EDIT, also write wave76/deck<N>/strategy.txt (the full
revised guide, start from the live one) and list each edit before->after with the seq that
paid for it IN review.md — the guide itself carries NO citation, count, or history (Amendment 332); hard ceiling 20,000 bytes, and an EDIT that grows the guide must say which rule earned its bytes; (4) OPTIONAL proposals, only when
your evidence supports them: general-guide changes (wave76/deck<N>/general-proposals.md) and
strategy-writing-skill changes (wave76/deck<N>/skill-proposals.md; current skill edition =
wave68/strategy-writing-skill-v2.md (the ~40 KB distillation, owner-installed 2026-09-06 after six Astra adversarial rounds; read it in full — it is short; the 1.3 MB wave68/strategy-writing-skill.md is lineage only) — read it before proposing; a proposal that
restates an existing amendment is noise). No prediction tables, no rotation evidence, no
docket bookkeeping. Final message: <= 10 lines.

## Engine-narration seat (one agent)
You read the whole corpus for the engine's sake, not the guides'. Deliverable wave76/engine-seat.md: (1) corpus census as last wave (games, decisions, decisions/turn vs wave 75's ~2.1, fallbacks by kind with genuineness, `async_drops`, `transport`, `deadline_pct`, reply shape, `reasoning_chars`, `reply_truncated`, `askreplay/`, `plan_steps_done`); (2) REGRESSION WATCH on the wave-76 changes — the section that matters most (SINGLE binary; 19 of the 21 games are RERUNS after the pilot wedge — see CORPUS; the two first-run games are 146v152 and 152v162): (a) the loop family stays dead — asks per (turn, phase) per seat; any phase > 10 asks: chain or re-put; (b) Q1 — the `[hold check: ...]` bracket now speaks of "the last window I asked you at this seam": for every record-adjacent pair at one seam, is the bracket's claim TRUE against the previous RECORD's rows (wave 75: 214 of 2,000 false against built windows) — count; the nested-annotation residual (`{reserve: ... {4} ...}`): any `N rows above are new` where only a nested annotation moved; (c) `forced_close_unrecorded` 5 with `force_close_arms_refused` 0 / `force_close_arms_deferred` 0 / `force_close_defer_bound_hits` 0 — the Q8/F2 path never fired, so what superseded these 5 closes? name each (game, arm, what happened next) and reconcile `phase2_answer_recovered` 13 + missing 0 against the stderr close count; did any decision fall to the heuristic (0 fallbacks says no — confirm per close); (d) Q13 — `crossphase_identical_reputs` 111: for a sample of 20, was the bracket printed, was its "nothing on the board has changed" clause printed (CQ F4 warns the board comparison may be too strict to ever print it — count prints vs re-puts), and was the re-put answered by the same decline; (e) Q7 — `main_phase_windows_skipped` 56: cross-reference each counted (turn, phase) against `Casting decision (Main phase N, YOUR turn)` headers using the record's turn (CN: the stderr now prints the record's turn — 0 false expected); (f) Q9 — `plan_names_uncastable_zone_card` 102 (wave 75: 68, and it is now per-record): sample 20 — real vs exempt (self-recursion, cast-from-exile) vs still-false; `plan_names_stranded_card` 1 vs reserve-VERDICT overrides on the taken row (deck123 had 4 uncounted last wave); (g) Q3 — 0 `NOT lethal: block only where the trade favors you` beside a live `LOOP COMPLETE`; 0 `reaches 0 in N more turns` beside a stated KILL (crack-back, stack, combat); the withdrawn header names why; (h) Q4 — every `Their N untapped blockers can cover every attacker` on a board with a menace/evasive attacker: true? every `At least N damage lands whatever they block` floor with an attack-punisher on their board: requalified? (i) Q5 — `{crack-back cover:}` on removal rows: present, arithmetic true, regenerating/returning bodies not subtracted; (j) Q6 — sacrifice rows under their converter carry the `off YOUR life` clause; none where the seat has a lifegain prohibition; (k) Q2 — max prompt bytes this corpus (wave 75: 80,805) and the count over 20 KB (562); every `- THEIRS:` roster: count + shapes + ≤K names + `and N more`; every folded copy row: was the answer mapped to the right engine index (echo/`chosen_text` agree); (l) Q10/Q11/Q12/Q14 — `{removes:}` on ghostform bodies says returns; up-to-N loyalty rows print the list or the choose-next note; closing rows always decline-last (0 reversed); 3-life land rows' usable tag by type; (m) Q15 — `forced_close_prefill_echo` vs `prose_outside_two_lines` by byte match; hold rows over 650 B; (n) P14 — the ONE `repeat_count_reask` (priority seam): quote the window, the bare-name answer, the re-ask and its answer — P14's first live firing; (o) `async_drops` 37 — all re-asked? `identical_ask_answers_reserved` 751 / `ask_replays_reserved` 948; `protocol_deviation_replies` 91 = `plan_line_missing` 91? (p) the harness — PILOT STALL fired ONCE, a TRUE positive (engine froze 15:42, tripwire 15:56); each rerun pool's "harvested 37 player-game logs" for a 1-game pool = the harvest keys on mtime and copies other pools' in-flight logs mid-game (the aggregation had to rebuild from the shared dir) — docket it as a harness item; (3) adjudicate EVERY prediction in wave76/lane-{CN,CO,CP,CQ}.md (CQ = the ten Astra findings) as PASS / FAIL / UNTESTED with counts and file+seq (UNTESTED is a real verdict), plus the carried UNTESTED docket (BS F2 Tribute second edict, BS F3/land latch `declined_face_latches` 0 for the 5th wave, BS F5 blockers ceiling, BS F6 distinct-index re-ask, CF F1 pending-deadlock, F2/F5-nonlifelink/F8 haste/F9, the reasoning-body halves of N14a/N14d/O20; {X}-announcement payment loss — count `dropped after its X was announced`); (4) engine/render/narration defects ranked HIGH/MED/LOW with repro — especially any new render clause stating a false fact (the loop-scope sentence, `[cost:]` on reveal rows, the ghostform gloss, the cannot-block subject); (5) the owner's lategame specimen (standing rule, alert-only): ONE late-turn decision (turn >= 25; 125v123 ran 67 turns), full `prompt` verbatim to wave76/lategame-specimen.txt with a 3-line header. Final message: <= 15 lines, HIGH items first.

## Synthesis (runs only if any deck agent wrote general-proposals.md or skill-proposals.md)
Read all proposals + engine-seat.md. Decide each proposal: ADOPT (edit wave76/general-strategy.md,
a copy of wave62's (the R331 edition; waves 63-67 wrote no edition; wave 68 installed wave68/strategy-writing-skill-v2.md as the current edition), or append a numbered amendment to wave76/strategy-writing-skill.md, a
byte-verbatim copy of wave59's with amendments appended — verify the prefix with cmp) or
REJECT with one line of reasons. Write wave76/synthesis-notes.md. If a deck agent's guide edit
contradicts an adopted general change, note it; do not edit deck guides yourself.

METHOD NOTE (from the wave-59 engine seat): the translog `phase` field is not the rendered phase — count phase-gated facts from the prompt text.
