# Wave-47 seat: deck125 (3/6) + deck126 (1/6)

Corpus `matchups-20260826-182155` - FOURTH fair-hand corpus, first on the wave-46 engine-seat fixes
(a7310f01c), the rendered hand count (1c71d5301) and lanes J/K/L/M. Binary master 5b1bf9668. Guides =
wave-46 reviewer revisions + boundary pass (verified: `deck125_strategy.txt` byte-identical to
`wave46/deck125/strategy.txt`; `deck126_strategy.txt` = `wave46/deck126/strategy.txt` + exactly the
C3 and C5 hunks). deck125 **413** decisions / 6 games, deck126 **199** / 6 games (612 total,
excluding gamestart/gameend).

**Health.** Fallback **11 / 5,644 corpus-wide (0.19%)**, inside the 0.10-0.28% baseline. On my seats:
deck125 **1** (`unparsed_reply`), deck126 **0**. `stale_echo` 0 on both (corpus-wide 1, at deck152).
`commit_retracted` 0. Latency deck125 p50 8.9 s / p90 22.0 / p99 54.9 / max **798.2**; deck126 p50
22.5 / p90 65.3 / p99 279.4 / max **800.8**. Both maxima are degenerate single events, not workload
(see engine item MED #3); the ~800 s pair suggests a client-side ceiling.
Prompt chars: deck125 mean 14,253 / p90 25,361 / max 35,559; deck126 mean 10,807 / p90 18,128 / max
**116,148** (one uncollapsed 431-row target menu - MED #4).

Every card fact below is read off `projects/mtg/bin/Res/sets/primitives/mtg.txt` with `/usr/bin/grep`;
every render string is quoted from the corpus; render predictions are adjudicated against the
emitter in `projects/mtg/src/AIPlayerGPT.cpp` (R14a read at ~2158-2210, the mana-only gate at
~11920-11946).

**Results.** deck125 3W-3L: won vs126 (t53), vs123 (t32), vs152 (t39); lost vs162 (t18), vs130
(t25), vs146 (t38). deck126 **1W-5L**: won vs146 (t20); lost vs130 (t15), vs162 (t12), vs152 (t16),
vs125 (t53), vs123 (t11). Per the new-baseline rule these are counts against n=6, not results, and I
am not attributing deck126's 5-1 -> 1-5 to the wave-46 edits. **On every decision class I can
measure, deck126's play got BETTER this wave** (Lantern 5/5 wrong -> 1/3 with the positive half
discharged; Battlement taps 1/16 -> 0/3; unpaid `Cast nothing` 24/24 -> 4/47; zero coloured-pip
overcommits for a second wave; 0 mulligans). The section "Why deck126 went 5/6 -> 1/6" below traces
each loss.

## NEW-BASELINE record (mulligans)

| seat | offers | mulls taken | kept-hand land counts | bottoming |
|---|---|---|---|---|
| deck125 | 6 | **1** (vs130) | 4, 3, 2, 4, 4 (after the mull), 4 | 1 (Island, vs130 seq 3) |
| deck126 | 6 | **0** | 4, 2, 3, 2, 4, 4 | 0 |

No mull-to-zero, no repeated mull, no chain reaching a third look. deck125's single mulligan was on
the exact branch its guide names (`Playing every land in this hand would not cover any spell in it`,
4 lands / 3 spells) and the re-keep came at the priced row `Mulligan (a keep after this one would
keep 5 cards)`. The one bottoming is guide-legal: an Island from a 4-land hand (rule 2, a duplicate;
leaves 3, clearing the three-land floor) - wave 46's bottoming violation did not recur. **No owner
question here.**

---

# deck125 - Revelation Fracture (UW draw-go). 3W-3L

## Prediction set A - wave-46 guide edits (W22-W29)

**W22 (land drop taken N/N, `Play no land right now` 0) - PASS, 93/93, 0 declines.** Denominator up
from 40 because lane L's land-drop ask no longer carries a PLAN request and the phase produces more
turn-level windows. Regression detector satisfied; not edited (#35).

**W23 (X == 3 in >= 15/17, none above 3) - VOID by its own terms, and the lane is now at ceiling.**
The prediction voided itself if the rendered own-hand count shipped before this corpus. It did
(below). Measured anyway: **12 X casts, 0 above 3**; X = 3 in **8 of 8** windows where 3 was
affordable, and the menu maximum in the other 4 (max 1, 1, 2, 2). The largest offered X was taken 0
times where anything smaller existed. Superseded by W30.

**W24 (Staff cast below the counter threshold: 0, n >= 10) - FAIL, 6/7, and the denominator stayed
at 7.** With the `{leaves N ...}` clause the measurement is now exact rather than inferred: `vs162`
seq 30 `leaves 2` (Cancel {1}{U}{U} + Fall of the Gavel {3}{W}{U} in hand), `vs126` seq 23 `leaves 1`
(Dream Fracture + Essence Scatter {1}{U}), `vs123` seq 14 `leaves 0` (Cancel), `vs123` seq 27
`leaves 1` (Cancel), `vs152` seq 13 and 16 `leaves 0` (Fall of the Gavel). The one clean cast,
`vs146` seq 22, held no counter. Four waves: 6/11, 3-4/7, 4/7, **6/7**. Fixed by W4 (read the row).

**W25 (sweeper into an all-defender/0-power board: 0; 0-creature-header casts stay 0) - FAIL on the
first half 4/13, PASS on the second at 0 of 64.** 107 sweeper windows, 13 casts. The four violations
are `vs126` seq 18, 36, 77, 84 and every one reads `0 of them without a restriction against
attacking`; all nine correct casts read 1 or more. **The new header discriminates 13/13.** Fixed by
W2. Wave 46 was 2/10, so the rate went up while the field that answers it was shipping.

**W26 (counters on a mana-only artifact: 0, take-rate >= 35/43) - PASS on the first half, take-rate
28/34.** 0 counters spent on a mana artifact (was 2/39) and 0 on a `[defender]`-tagged body (W18
holds a second wave). One counter did hit a wall - `vs126` seq 66, Cancel on `Perimeter Captain {w}
(creature 0/4) {target text: "Defender -- ..."}` - which is a PERCEPTION failure the render caused:
see engine item MED #2 and edit W6. Take-rate denominator fell with the window count; 82% take.

**W27 (Revelation cast under a "Whenever an opponent draws" permanent: 0) - PASS, 0/13.** Thirteen
windows offered Sphinx's Revelation with a `DRAW PUNISHERS on the battlefield: theirs - ...` line
present; none was cast. Wave 46 was 1/1, at 6 life, in a loss. The draw-is-damage stop plus lane J's
render is the cleanest guide+engine pairing on this seat.

**W28 (Emrakul taken N/N) - FAIL, 1/4.** `vs152` seq 86, 92, 93. Diagnosed as a rule collision, not
a visibility failure - see W7 and skill proposal S3.

**W29 (own-turn spends leaving the line below the counter threshold: 0) - FAIL, 12 identifiable**
(wave 46: 3). Measured off `{leaves N ...}` against the cheapest counterspell in the rendered hand
line, excluding counters cast in answer to the stack: Staff of Nin 6, Supreme Verdict 2, Path to
Exile 2, Lightmine Field 1, Final Judgment 1.

## The losses (deck125)

- **vs deck162 (t18) - a draw lock, and the guide answered the half it could.**
  `1787786518-...-vs-ai_baka_deck162.jsonl`. Every draw punisher that hit the stack was countered on
  sight: seq 15 Fall of the Gavel on Underworld Dreams, seq 22 Dream Fracture on Fate Unraveler,
  seq 28 Cancel on Underworld Dreams. Zero Revelations under a punisher (W27). What killed it was
  three Howling Mines + a Dictate of Kruphix + Ob Nixilis resolving in windows where no counter was
  in hand: 19 -> 9 in one draw step at turn 16. Two contributing misplays, both edited: seq 7 turn 6
  cast Lightmine Field for its whole four mana at an attacker count of 0 with Dream Fracture in hand
  (W3 + W4), and seq 30 turn 16 cast Staff of Nin at `leaves 2` holding two counters (W4). Neither
  is the loss; the loss is a matchup this deck has no answer to once the permanents resolve.
- **vs deck146 (t38) - THE guide loss of the seat, and it is Path to Exile.**
  `1787786536-...-vs-ai_baka_deck146.jsonl`. Twelve windows at life 12, 10, 5, 1 and 6 offered a
  legal Path target of printed power 2 or more (two Spiders 2/1, a Hive of the Eye Tyrant 3/3, a
  Nadaar 3/3) and every one was answered `Cast nothing right now` or spent elsewhere, with **three
  Path to Exile in hand** at seq 70-71. seq 74's reply gives the mechanism verbatim: *"You have 3
  Path to Exile in hand but no creatures to block the spiders if they attack, and the spiders are
  tapped"* - a `[tapped]` tag read as safety plus blocker reasoning in a deck with no creatures.
  **STRATEGY**; the render was complete. Fixed by W1.
- **vs deck130 (t25) - Ponza, 29 decisions, one bottoming.** Land destruction took the seat off its
  mana; no adjudicable rule failure and no new rule in it.

## Other counted behaviour (deck125)

- **Own-hand count (commit 1c71d5301): PASS on the render at 413/413 and PASS on the behaviour at
  0/1.** `Your hand (N cards):` renders on every prompt. Exactly one reply stated a hand size -
  `vs123` seq 40, *"My hand is 8 cards"*, against a rendered `Your hand (8 cards)`. **Correct.**
  Wave 46 was 3 of 4 wrong and it was the root cause of the X lane's 4/17. Discharged.
- **Staff pings:** every player-targeted ping window taken; the seat's clock is unbroken.
- **Elixir:** 15 activation windows, 9 taken; **minimum library across them 9**, so the
  `library <= 16` trigger had real windows for the first time in three waves and did not misfire.
- **`{target text:}` on every removal/counter option row: 100%.** Fifth consecutive wave.

---

# deck126 - Sanguine Blood (WGB wall prison). 1W-5L

## Prediction set A - wave-46 guide edits (D22-D28) and the boundary pass

**D22 (a blocker whose every attacker reads "dies" benched anyway with no enchantment and header
above 8: 0) - PASS as written, but its mirror failed.** 11 blockers windows. The one `BLOCKS: none`
of the corpus, `vs146` seq 20, was at a header reading **`you would be at 7`** - i.e. at 8 or less,
where the carve-out is OFF and the blocker (a lifelink Vampire) should have blocked. So D22 passes
on its literal wording and F4 fixes what it was pointing at.

**D23 (a blocker offered both "dies" and "lives" takes "dies"): 0 identified.** No window offered a
single blocker both shapes; F1's wave-46 STEP 1/STEP 2 rewrite has nothing against it this wave.
Report as **0 of 0 - no window**, not as a pass.

**D24 (Lantern at `{B} 2`+ or with one already out: 0; cast >= 1/1 at `{B} 1`/no {B}) - near-PASS on
the first half (1/3), PASS on the second, PASS on the duplicate half at 0.** 57 windows, 3 casts:
`vs123` seq 7 at `{B} 1` **correct**, `vs152` seq 12 at a colour clause with **no {B} entry at all**
(`{W} 4, {G} 3`) **correct**, `vs125` seq 17 at `{B} 4` wrong. A second Lantern was cast **0 times**
across ~40 windows with one already on the battlefield line. Wave 46: 5/5 wrong, 0/1 positive, 1
duplicate. F2's rewrite onto lane F's rendered per-colour count is the biggest single-rule
improvement on either seat and is **not** edited again (#35).

**D25 (`ATTACK: none` on a blocker-free A-line: 0; all Vampires attack when they outnumber N) -
UNTESTED on both halves.** 2 attackers windows, both a single Vampire with an explicit
`their untapped blockers:` list, both attacked. No bare A-line arose and the seat never had a Vampire
surplus. P-C3's D25 half is likewise **UNTESTED**: no `ATTACK: none` was chosen at all, so the C3
clause cannot have over-fired, and **0 replies asserted an attacker of theirs would be blocked by a
chosen attacker of ours**. P-C3 PASS on its second half, UNTESTED on its first.

**D26 (a land bottomed leaving fewer than three: 0) - UNTESTED.** 0 bottoming windows (0 mulligans).

**D27 (`Add N green mana with Overgrown Battlement` taken with no cast in the same decision: 0) -
PASS, 0/3** (was 1/16). Only 3 such windows reached the model at all.

**D28 (`BLOCKS: none` stays 0; D18's flying-Tribute conjunct reported) - FAIL 1/11 on the first half
(`vs146` seq 20, above), and D18 is UNTESTED a THIRD wave.** The conjunct needs their ONLY creature
to be `[flying]`: `vs130`'s Rorix Bladewing (6/5 flying, haste) was their only creature for the whole
game but **Tribute was never in hand** (0 Tribute windows in that game), and `vs162`'s Fate Unraveler
and `vs146`'s Silverquill Silencer are not flying. Not moved (#66).

**P-C5 (boundary pass, the totality close) - PASS, and this is the wave's cleanest guide result.**
107 cast windows; 47 answered `Cast nothing right now`. **38 of the 47 had nothing on the menu but a
second Chromatic Lantern**, which entry 7(b) forbids, and only **4** had a genuinely matched entry
available (three Pride Guardian declines in `vs146` at life 9-10, one in `vs125` seq 14). Wave 46's
same measure: **24 of 24**. The raw pass-rate rose 28% -> 44% because the menus were emptier, not
because the discipline slipped, and reading the raw rate alone would have condemned a working edit.

## Why deck126 went 5/6 -> 1/6 - what actually changed in its decisions

Traced per loss. Two of the five have an adjudicable guide mechanism; three do not.

1. **vs deck123 (t11, -4722 life) - NOT a decision.** The opponent assembled Bloodline Keeper's
   `Lord of Lineage` + `Intruder Alarm` into an unbounded token loop, reaching **1,540 creatures**
   and attacking with **1,184** of them. deck126 received priority thousands of times with nothing on
   the menu but `Add N green mana with Overgrown Battlement`; the auto-pass absorbed **8,610** of
   them (below). No guide answers this. HIGH engine item #1.
2. **vs deck125 (t53) - a control matchup, played out.** deck125 countered **six** of deck126's
   relevant spells: Sanguine Bond (seq 10, t9), Exquisite Blood (seq 13, t11), Sanguine Bond #2
   (seq 60, t31), Staff of Nin (seq 71, t35), Staff of Nin #2 (seq 95, t45), plus a Perimeter
   Captain. deck126 never had both enchantments on the battlefield in the same window (0 of 86
   decisions). No rule failed.
3. **vs deck152 (t16) - structural.** deck152 reached 66 life behind two lifelinking Intrepid
   Adversaries and a `Sigarda, Champion of Light (7/7) [trample, flying]` carrying
   `[NONE of your available blockers can block this attacker - it has flying and none of your
   available blockers has flying or reach]`. Sixteen walls, none with flying or reach. Tribute WAS
   cast, twice, correctly (seq 17 and 20).
4. **vs deck162 (t12) - GUIDE, and it is F1.** Their only creature was a Fate Unraveler (3/4), half
   of a Fate Unraveler + Underworld Dreams + Teferi's Puzzle Box draw lock; 17 -> 7 in one turn
   cycle. Tribute to Hunger - this deck's only removal, and clean at one opposing creature - was in
   the menu at seq 8, 9, 10 and 12 and was never cast; at seq 12 the seat cast Sanguine Bond instead,
   tapping out, and the Bond gained nothing for the rest of the game.
5. **vs deck130 (t15) - matchup, plus the same F1 shape untestable.** Rorix Bladewing (6/5 flying,
   haste) as their only creature against sixteen ground walls, under Ponza land pressure (down to one
   untapped source by turn 10). Tribute was never drawn.

**The one decision class that got worse and is mine:** Tribute to Hunger at an opponent creature
count of exactly 1 - **0 of 5** (F1) - plus one Tribute cast at a count of **0** (F2), and the
blocking allocation failures F3/F4. Everything else measured improved or held.

---

# Prediction set B - wave-47 engine lanes (lanes where my seats had windows)

| lane | verdict | evidence |
|---|---|---|
| **1c71d5301** own-hand count; replies stating a wrong own-hand size 0/N | **PASS on both** | `Your hand (N cards):` on **413/413** deck125 prompts and every deck126 prompt. Exactly one reply asserted a size (`deck125 vs123` seq 40, "My hand is 8 cards") and it was **right** (was 3/4 wrong). The X lane it was blocking went 4/17 -> 12/12. |
| **a7310f01c** `A choice is required` 0/N; `Cast ...` into cast-free menus 0/N; stale_echo on non-card menus 0; single-option loop windows > 100 s: 0 | **PASS on three, UNTESTED on the header** | `A choice is required` **0** on both seats (neither deck runs a modal spell) - UNTESTED here. Replies of the form `CHOICE: n (Cast ...)` into a menu with no `Cast` row: **0 / 612**. `stale_echo` **0** on both seats. No single-option window exceeded 100 s. |
| **J 2860f78c2** DRAW PUNISHERS line; `[DRAW PRICE:]`; X=0 callout + life/draw X preview | **PASS on the punisher line and the X preview; `[DRAW PRICE:]` UNTESTED** | `DRAW PUNISHERS on the battlefield: theirs - <names>. Every card YOU draw costs you 1 life to theirs. They fire on EVERY draw...` renders in **26** deck125 windows, and **0 of 13** Revelation-offering windows under it produced a cast. The X preview renders on the cast row (`{X pricing: max affordable X=9 (12 mana total); each point of X gains you 1 life and draws you 1 card}`) and on **every** menu row (`X = 3 {X pricing: X=3 - you gain 3 life and draw 3 cards}`), with the X = 0 row stating outright that the cast does nothing. **X = 0 answered where X >= 1 was affordable: 0/12.** Menus largest-first **12/12** (owner ruling). `[DRAW PRICE:` appears 0 times on my seats - no cycling card in either deck. |
| **K d30871747** converter folded into blocking/lifelink tags; affirmative no-blocker tag; zero-power STOPS scoped `THIS COMBAT` | **PASS on the scoping and the converter; the affirmative tag UNTESTED** | The zero-power tag now reads `[deals 0 - this block kills nothing, but it STOPS all of the damage from whichever attacker it blocks THIS COMBAT - each A-line above says how much]` - **7 windows on deck126, scoped every time**. Converter doubling is folded in and correct: `(your blocker dies, attacker lives (lifelink: you gain 1, and your converter takes 1 off them))` on `vs146` seq 34/40/45, where deck126 held a Sanguine Bond. The affirmative `[no creature they control can block this attacker]` did not arise on my seats (deck125 has no creature; deck126's two attack windows both had blockers). |
| **L 2a4ff28be** decline-cap clause; priced mulligan row + no carried plan; land-drop asks | **PASS on all three renders; PASS decisively on land-drop latency; the decline-cap behaviour UNTESTED** | `{if you pass here, this option is not offered again this turn}` renders (deck125 `vs126` seq 41 and 43, both on an Elixir activation, both passed - not a lethal-in-upkeep case, so the behavioural half is untested here). The priced row `Mulligan (a keep after this one would keep 6 cards)` / `(keeping 5 cards)` renders on **13/13** pregame keep/mulligan asks (deck125 7, deck126 6); **no chain reached a third look**; no seat neared keeping 4. Land-drop asks: **139 windows, p50 7.3 s, p90 15.0 s, max 27.7 s** (wave 45's was 453 s), and **0 declines in 93 deck125 + 17 deck126 turn-level drops**. |
| **M 8b6545b48** per-clause `{effect:}` budget; own-side `{effect:}` minus option-row permanents; blockers ranged collapse; `{c}` in colour sets; `N of them able to attack` header + `{leaves N untapped}` | **PASS on the effect budget, own-side effect, colour sets and both new fields; UNTESTED on the ranged collapse; the header's BEHAVIOURAL half FAILS on my seat** | No truncated loyalty ability (Sorin's three abilities render in full on every row). **Own bare permanents: 0 unexplained in 612 decisions** - every bare own permanent is either on an option row in the same prompt (Elixir 15, Lantern 57, Sorin 9, Staff 2) or a numbered LATER copy under the deliberate `{effect (each copy of this card does this): ...}` collapse (`Staff of Nin #2` x78). **`{1}`/`{x}`/`{c}` inside a colour set: 0 of 549 clauses.** Blockers ranged collapse: no window on my seats was wide enough - **UNTESTED**. The `N of them able to attack right now` / `N of them without a restriction against attacking` header renders on **1,196** battlefield lines, and the two wordings are a perfect complement by turn (534/64 and 64/534) - matching the emitter's `liveScope`. Its prediction "sweeper casts into all-defender boards 0/N" is **FAIL 4/13** (all four at that count = 0), and the deck125 Staff tap-out was re-measured at **6/7**, still at n=7. Both are guide failures against a correct render, fixed by W2 and W4. |
| **R7 prior** mana-only auto-pass; 8,673 corpus total | **PASS - the counter is sound and the gate is tighter than last wave** | See engine INFO below and `deck126/general-suggestions.md` G7. |
| Fair hands (lane A, carried) | **PASS** | Opening land counts 2-4 across 13 kept hands, including three 2-land sevens. |

## The mana-only counter, resolved

`mana_only_windows_skipped` totals **8,673** corpus-wide and **every one is at deck126's seat** -
8,610 (`vs123`) + 32 (`vs125`) + 30 (`vs152`) + 1 (`vs162`); deck125 0, every other seat 0.
Three findings, in the order the brief asks them:
1. **Did deck126's share explode?** Its share is 100%, as in wave 46. The magnitude did not: **8,610
   of the 8,673 (99.3%) are one game**, the deck123 token loop, where the opponent's 1,540-creature
   engine handed deck126 priority thousands of times with only a Battlement tap on the menu. Across
   the other five games the seat skipped **63** windows, against **180** across six games in wave 46.
   Per-game the rate went DOWN.
2. **Were windows carrying non-mana options skipped?** No, and this is now corpus-checkable rather
   than code-only: the complement measure is that **0 all-mana menus reached the model** this corpus
   (wave 46 leaked 3, at `vs123` seq 24 and `vs125` seq 28/30). The gate (`AIPlayerGPT.cpp`
   ~11930-11946) is unchanged and rejects on the FIRST option failing `isManaOnlyAction`, so a menu
   with any other action cannot reach the skip; the wave-46 leaks were it erring the safe way, and
   they are gone.
3. **Is the counter counting something else?** No. `mManaOnlyWindowsSkipped++` sits inside the gate's
   own branch, immediately before the `return NULL`, and has no other increment site in the file
   (`/usr/bin/grep -n` finds it at the JSON emitter and at that one line). It counts the same event
   it counted in wave 46. The number is large because one game was pathological, and that game is
   the HIGH engine item below, not a counter defect.

---

# Engine / render items (ranked, with repro)

Corpus root: `~/.Wagic/ai/gpt/selfplay-runs/matchups-20260826-182155/`

**HIGH - #1: a SECOND unbounded legal loop, and this one reached a gameend record - the corpus note
covers only the deck123-vs-deck162 game.**
`1787786558-ai_baka_deck123-0x55fa8add5b20-vs-ai_baka_deck126.jsonl`: **1,572 records, 1,550 of them
`priority`**, all one turn. seq 1567 reads
`Create vampire with Lord of Lineage [cost: Tap] [repeat: activated this turn 15 times already; you
control 1540 creatures]`; the attackers window at seq 1570 offers **1,525** options and 1,184 are
declared; deck126's gameend records **-4,722 life at turn 11**. Unlike the excluded game this one
TERMINATED, so it is inside every rate in this corpus and it is 1 of deck126's 5 losses.
Two things the ledger should carry from it that the excluded game does not show:
(a) the `[repeat: N times]` tag was PRESENT and did not stop it - and its own numbers disagree (15
activations against 1,540 creatures), so the tag is counting one ability among several that feed the
loop and understates what the pilot has done;
(b) from the OTHER seat, the loop is a denial-of-decision: deck126 received 8,610 priority windows in
one turn and was auto-passed through all of them, which is the auto-pass gate saving the run rather
than the loop being contained. The stopping affordance has to be structural.

**MED - #2: the stack line and the counter-target clause carry NO keyword tags, while the
battlefield line does.**
`1787786522-ai_baka_deck125-0x5555ed1493c0-vs-ai_baka_deck126.jsonl` **seq 66** (turn 33): the stack
reads `1 (top): opponent's Perimeter Captain {w} (creature 0/4) [spell]` and the counter row reads
`- can target on the stack: Perimeter Captain {w} (creature 0/4) {target text: "Defender -- Whenever
a creature you control with defender blocks, you may gain 2 life."}`. No `[defender]`. The same card
on the battlefield line, in the same corpus, reads `Perimeter Captain #1 {w} (0/4) [defender]`. Every
counter/removal rule in the pool is written in battlefield vocabulary, so a rule keyed to a tag is
unsatisfiable exactly where the decision is made - and this seat spent a Cancel accordingly. Same
defect class as the own-hand count: the fact is at the call site. Proposal in
`deck125/general-suggestions.md` G1; the guide is re-keyed onto `{target text:}` as a workaround
(W6) with a sunset the moment tags land.

**MED - #3: a degenerate decode burned 798 s and is recorded only as `unparsed_reply`.**
`1787786524-ai_baka_deck125-0x555dc22a73c0-vs-ai_baka_deck123.jsonl` **seq 39** (turn 22, 4 options,
25,138-char prompt): a **16,271-character** reply of one repeated token (`ai茧ai...`), no CHOICE
line, `latency_ms` 798,216, `fallback: unparsed_reply`. deck126's max is 800,769 ms
(`...vs-ai_baka_deck162.jsonl` seq 15, which DID answer), so an ~800 s ceiling exists somewhere in
the client. One event in 5,644 - an inference-layer item, not a prompt defect - but it deserves a
counter distinct from `unparsed_reply`, because an operator reading the ledger cannot currently tell
a 16 KB token-loop from an ordinary parse miss.

**MED - #4: target menus do not collapse identical rows, and produce the corpus's largest prompts by
4x.**
`1787786558-ai_baka_deck126-0x55fa89ab8040-vs-ai_baka_deck123.jsonl` **seq 18 and 19** (turn 10):
**432 and 431 options, 116,128 and 116,148 characters**, of which ~425 are
`Deal 1 damage with Staff of Nin targeting Vampire #N [opponent's battlefield] [cost: Tap] {card
text: "..."}` differing only in the index, each repeating the full card text of the same Staff. The
seat's next-largest prompt is 18 KB. Blockers were given a ranged collapse in lane M and battlefield
lines already collapse runs (`Human #1-#40 (1/1) ... x40` on deck125 `vs123` seq 18); target menus
were not. Both halves in `deck126/general-suggestions.md` G5. Correctness was unaffected (the model
answered option 431, the player, both times).

**LOW - #5: `{leaves N ...}` is on cast rows only, not on mana-costing activated-ability rows.**
`Life with Elixir of Immortality [cost: {2}, Tap]` prints its cost but not what it leaves, while
deck125's tap-out gate explicitly covers the Elixir "cast OR activated". 15 such windows this corpus.

**INFO - the two new deck125-facing fields both render exactly as their emitters describe, and both
are now load-bearing.** `battlefieldHeaderText`'s attacker count discriminated this seat's sweeper
casts **13/13**, and `{leaves N of your M untapped mana sources untapped}` made the Staff tap-out
lane measurable for the first time in four waves. Neither changed behaviour on its own - both needed
the guide rewritten onto them, which is what W2 and W4 do. The general lesson is in
`deck125/skill.md` S1.

---

# Rotation evidence

- **deck125 - KEEP IN ROTATION.** 3-3. What is left on this seat is now almost entirely
  *rule-shape*, not perception: three of this wave's seven edits move a rule from an arithmetic
  instruction onto a field the engine now prints on the very row being chosen (W2, W3, W4), one
  deletes a sentence the render has falsified (W5), one supplies a missing polarity (W1), one
  resolves a rule collision (W7), and one works around a real render gap (W6). Two of those - W1 and
  W4 - are the whole content of the two losses that had guide content. The lanes that were fixed this
  way already are at ceiling: land drop 93/93, X sizing 12/12, mana-artifact counters 0/28,
  draw-is-damage 0/13. Not a freeze candidate; the seat is converging and the mechanism is now
  understood well enough to predict.
- **deck126 - KEEP IN ROTATION, and the wave-46 freeze condition is NOT met but is also not the
  right test any more.** 1-5. The freeze condition was F1 (blocking) >= 5/6 and F2 (Lantern) at 0
  wrong casts. F2 came in at 1/3 with its positive half discharged - a large improvement but not 0.
  F1's survival test produced **no window that could adjudicate it** (D23: 0 of 0). What the corpus
  did surface is a class no previous wave reached: the seat's own removal spell has been unreachable
  in the ordered cast list since the boundary pass made that list total, and it went 0/5 in exactly
  the windows where it is strictly correct. That is one clean, testable edit (F1) with a second
  (F2's zero stop) and two combat gaps (F3 menace, F4 the carve-out floor). Re-test all four before
  considering a freeze; three of the five losses have no guide content at all and should not be
  counted against the seat.

# Lategame specimen candidate

Primary: `1787786558-ai_baka_deck126-0x55fa89ab8040-vs-ai_baka_deck123.jsonl` **seq 19** (turn 10,
**116,148 bytes**, 431 options) - four times the previous record. A single priority window on a board
of 1,540 opposing tokens: it exercises the uncollapsed target menu (MED #4), the battlefield-line run
collapse that DOES work on the same prompt, the ranged-collapse gap, and the whole prompt-assembly
budget at once. It is also the decision immediately before the seat lost 4,737 life, so it doubles as
the HIGH item's repro.
Alternate (ordinary lategame, deck125): `1787786524-ai_baka_deck125-0x555dc22a73c0-vs-ai_baka_deck123.jsonl`
**seq 16** (turn 14, 35,559 bytes, 86 options) - the largest natural prompt on either seat, a
priority window against an 86-creature Thraben Doomsayer board with the `Human #1-#40 ... x40` run
collapse live and a two-Staff target menu inside it.

# Falsifiable predictions for wave 48

deck125:
- **W30** - Sphinx's Revelation: X announced above 3 in **0** windows, and X == 3 in every window
  whose menu maximum is 3 or more. Report both denominators. (Was 12/12 and 0 above 3; this is now a
  regression detector, and it supersedes the voided W23.)
- **W31** - Supreme Verdict or Final Judgment cast in a window whose opponent battlefield header
  reads `0 of them without a restriction against attacking` (or `0 of them able to attack right
  now`): **0** (was 4/13). Report the denominator of sweeper windows.
- **W32** - Lightmine Field cast at that same number below 3: **0** (was 3/3).
- **W33** - a Staff of Nin, Elixir, sweeper, Lightmine Field or Sphinx's Revelation taken on the
  seat's own turn from a row reading `leaves 4` or less (or `casting this taps you out`) while any
  of Essence Scatter / Cancel / Dream Fracture / Fall of the Gavel is in the rendered hand line:
  **0** (was 12, of which 6 were Staff of Nin). Counters cast at something on the stack are excluded
  by the rule and from the count. Report the denominator; do not close the Staff sub-lane below a
  cumulative n = 10 across waves 47-48.
- **W34** - Path to Exile taken in **every** window at life 12 or less where at least one candidate
  reads printed power 2 or more and is not `[defender]` (was 1/13). Report N/N.
- **W35** - `Cast Emrakul, the Aeons Torn` taken in **N/N** windows where it is printed, including
  rows reading `leaves 0`/`leaves 1` (was 1/4).
- **W36** - a counterspell spent on a stack object whose `{target text:}` begins with "Defender" or
  whose printed power is 0: **0** (was 1/28). If the engine ships keyword tags on the stack line
  before this corpus, re-key the rule to the tag and report that instead.
- **W37** - land drop taken in **N/N** windows offered (was 93/93). Regression detector.

deck126:
- **D29** - Tribute to Hunger cast in the FIRST window where the opponent battlefield header reads
  `of which 1 is a creature` and Tribute is in the menu: **>= 1/1**, and taken in **every** such
  window: report N/N. (Was 0/5. This is F1's test and it is the seat's headline.)
- **D30** - Tribute to Hunger cast at a header reading `of which 0 are creatures`: **0** (was 1/1).
- **D31** - a blockers window in which one blocker is assigned to an attacker whose A-line carries
  the `[menace - only a block by TWO OR MORE ...]` tag: **0**, and a window in which every available
  blocker is assigned to a single attacker while another attacker without a two-or-more requirement
  is left unblocked: **0** (was 2/11 for the second, 2/11 for the first).
- **D32** - `BLOCKS: none`, or any blocker benched, in a window whose header reads `you would be at
  N` with N of 8 or less: **0** (was 1/11).
- **D33** - Chromatic Lantern cast at a per-colour clause reading `{B} 2` or more, or with a Lantern
  already on the battlefield line: **0** (was 1/3 and 0/~40). Report both denominators; the rule is
  NOT being edited this wave, so this is a pure hold test.
- **D34** - unpaid `Cast nothing right now` (a window where an entry 1-8 card other than a duplicate
  Chromatic Lantern was in the menu): **<= 4** and report N/M (was 4/47; wave 46 was 24/24).
- **D35** - report `mana_only_windows_skipped` per game AND the count of all-mana menus that reached
  the model. Expect the leak count to stay at **0** and the per-game skip count, excluding any game
  containing an unbounded loop, to stay under ~20.

# What I did NOT check

- I did not run wagic, did not touch `bin/Res`, `src/` or git, and did not read the `game-*.stderr`
  files (JSONL + results.tsv only). All my guide work is under `wave47/`.
- **Set-B lanes with no window on my seats and therefore NOT adjudicated:** the `A choice is
  required` header (neither deck runs a modal spell; corpus-wide count not taken - the deck146
  reviewer owns it), the `Cast Card Normally / cycling` annotation and `[DRAW PRICE:]` tags (no
  cycling card in either deck), lane K's affirmative no-blocker tag, lane L's decline-cap BEHAVIOUR
  (both rendered windows were ordinary Elixir passes, not lethal-in-upkeep), lane M's blockers ranged
  collapse (no window wide enough), and the K "life lost between attackers records 0" half.
- I did **not** verify that the `N of them without a restriction against attacking` count is correct
  against the engine's own `canAttack()` on a live board - I read `boardCreatureCanAttackNow` and
  `attackRestrictionFree` and confirmed the intent and the turn-scoping, and I confirmed the corpus
  is a perfect complement by turn (534/64), but I did not step the engine.
- The MED #3 degenerate decode was not investigated beyond reading the record; I did not check
  whether the ~800 s figure is a client timeout, and I did not look for the same shape at other
  seats beyond the corpus-wide fallback tally.
- I did not trace the wave-46 MED #3 (floating mana disappearing between decisions) - no window on
  this corpus reproduced it on my seats.
- **Primitives verified by `/usr/bin/grep` this wave:** Staff of Nin, Cancel, Dream Fracture, Essence
  Scatter, Fall of the Gavel, Supreme Verdict, Final Judgment, Sphinx's Revelation, Elixir of
  Immortality, Chromatic Lantern, Sanguine Bond, Exquisite Blood, Lightmine Field, Emrakul the Aeons
  Torn. Emrakul's primitive reads `mana={15}` while the option row read `{17}` - a cost-increase
  effect on that board (the same +2 appears on a Supreme Verdict row in the same menu), which I did
  NOT trace to its source card; I report both numbers. Perimeter Captain, Pride Guardian, Wall of
  Omens, Overgrown Battlement, Tribute to Hunger, Idyllic Tutor, Sorin and Urborg are carried from
  wave 46's verification, unchanged on disk. Cards I quote only from the engine's own
  `{effect:}` / `{card text:}` / `{target text:}` strings and did not open (they are what the model
  saw): Lord of Lineage, Intruder Alarm, Thraben Doomsayer, Fate Unraveler, Underworld Dreams, Ob
  Nixilis, Teferi's Puzzle Box, Howling Mine, Dictate of Kruphix, Silverquill Silencer, Sigarda,
  Intrepid Adversary, Brutal Cathar, Hive of the Eye Tyrant, Rorix Bladewing, Nadaar, Luminarch
  Aspirant, Master of the Feast, Shield Sphere. Nothing was cross-checked against Scryfall.
- I did not attempt to explain deck126's 5-1 -> 1-5 statistically; n=6 cannot separate a guide effect
  from hands, and three of its five losses have no guide content to attribute anything to.
