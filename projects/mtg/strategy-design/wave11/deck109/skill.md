# Skill contribution — deck109, wave 11 (VERIFICATION wave; hybrid-line close + one reply-protocol convergence)

**Mode: FREEZE + VERIFY.** The current skill is `wave10/strategy-writing-skill.md`. deck109's guide
is byte-identical to the live guide (the wave-10 hybrid line is now VERIFIED and stays as written).
This wave contributes ONE method refinement (a terminus close) and ONE convergence data point — no
new authoring headline, per the HIGH adopt-a-revision bar on a guide at terminus.

## Method refinement — a card-note stopgap that VALIDATES stays at FULL prominence when its representation backstop has not shipped

Sharpens the wave-10 headline-4 ladder (deck109's own contribution last wave: before routing an
offered=payable fabrication to representation/core, ask "is the SPECIFIC card-cost fact taught where
the pilot reads it?" — if a sibling card's parallel fact is OBEYED, add the same card-specific fact
for the misfiring card). Wave-11 supplies the END-TO-END CLOSE and the demotion caveat:

- **The close:** the deck109 Rakdos Cackler `{B/R}` card note (the interim guide rung shipped last
  wave) VALIDATED — the wave-10 decline/spiral class (deck131 seq18/19/21) went fully to zero
  (0 "lacking black mana" declines, 0 spiral markers), the two offered `{b/r}` windows both cast,
  and the pilot **echoed the guide fact verbatim in its reasoning** ("Rakdos Cackler costs {B/R},
  which is a hybrid cost payable with a single Red mana"). A card-note stopgap validates the same way
  a representation fix does: the WITNESSED defect-class goes to zero AT THE WITNESSING SEAT and the
  corrective fact appears in the reasoning.
- **The demotion caveat (the refinement):** do NOT demote a validated card-note stopgap while (a) the
  durable cross-deck REPRESENTATION backstop it stands in for has not shipped (here: the option-line
  hybrid annotation, Ledger Item A — verified 0 `- hybrid:` strings in the corpus, so the guide note
  is the SOLE carrier), OR (b) the class cleared on a THIN sample (2 offer windows). This is the
  card-note twin of the wave-8 conditional-surface-granularity demotion rule ("demote the stopgap the
  representation replaces ONLY when the representation's surface covers every case the stopgap
  covered") and the pause-aware demotion doctrine: absence of the representation = the stopgap is NOT
  double-covered, so it stays at full prominence. A validating stopgap whose replacement is unshipped
  is the *load-bearing* case, the mirror of the confirmed-fired terminus's demote-the-double-cover
  case. deck109 froze the line byte-identical accordingly.

## Convergence data point (route to synthesis, NOT the guide/core) — the UNLISTED-CARD-FIXATION reply-protocol sub-variant reaches a 2nd seat

The wave-10 deck110 P3 single-seat WATCH — *the plan fixates on an UNLISTED card and emits a
poorly-mapped/out-of-range index that contradicts its own sub-conclusion about the listed options* —
gets its **2nd witnessing seat** at deck109 vs135 seq12 (T7): with options {Pillar, Searing Spear,
Krenko's Command, Cast nothing}, the PLAN abandons its own previously-planned LISTED Krenko's Command
and fixates on Hellrider (NOT offered — uncastable), emitting `CHOICE: 5` (out of range for 4
options) -> `-1` -> heuristic. Per the wave-10 rule ("promote only with a 2nd seat"), this sub-variant
now has the 2nd seat. It is DISTINCT from the deck140 P2 answer-last trailing-index defect (there the
reasoning is CORRECT and only the index is wrong; here the reasoning reaches an ILLEGAL choice and the
out-of-range index faithfully encodes it, as if the unlisted card were an imagined extra option). The
sharper A/B target is the same family as deck140 P2's "reconcile the trailing index against the plan's
NAMED action / require echoing option TEXT not a bare integer" — but with the added guard that the
named action must be a LISTED option (reject-and-repair when the plan's named card is not in the menu).
Route to the code-appended reply-protocol layer; **never re-word the guide or add a core line over it**
(the convergence gate forbids a 2nd-seat item touching the core, and the guide already forbids
fixating on an uncastable card).

## Everything else held (freeze-checked, no method change)

- Attacker seam 26/26 full declaration, 0 intent-collapse — the answer-after-plan protocol's clean win
  on this seat continues.
- Chump-on-LETHAL-only OBSERVED OBEYED 1/1 (NOT-lethal -> no block); LETHAL branch + Obliterator
  absolute untested AGAIN (KEEP at full prominence — pause-aware demotion doctrine, untested guardrail
  against a known game-loser, 3rd consecutive zero-window wave).
- Phantom-lethal watch CLEAN (4 "lethal" assertions, all genuine or correctly hedged, development
  taken in every case).
- BURN face-default + Pillar `{R}` corrective 0-defect; token-maker/lifegain exceptions fired
  correctly (Young Pyromancer, Vault Skirge).
- The 3 empty_reply fallbacks are the known corpus-wide 120s-timeout class (240s shipped next corpus);
  none changed a game. Do not re-diagnose.

Core status (deck109 vote): **PASS.** No 2+-seat core candidate at this seat. The one convergence
(unlisted-card fixation) is a reply-protocol-layer item.
