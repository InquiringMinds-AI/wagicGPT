# Deck-135 skill contribution (wave 11) — method refinement for synthesis

This wave is a VERIFICATION wave (the wave-10 stack-target annotation fix at scale). Per the epistemics
rule, deck135 offers NO new guide-authoring headline and NO core edit. It contributes ONE method
refinement (2+-wave, completes an existing rung) plus watch-level notes. The current skill remains
`strategy-design/wave10/strategy-writing-skill.md`.

## Method refinement — VERIFYING A SHIPPED ANNOTATION-CLASS FIX has a SECOND-ORDER success signal beyond mis-cast->0: the REASONING-TAX COLLAPSE, and it can restore an ADJACENT decision faculty the annotation was silently corrupting.

Wave-8/9/10 built the confirmed-fired-representation terminus and its verification signals (marker-string
grep / option-appears / defect-class->0 / a REASONING-TAX DROP at annotated windows). Wave-10 deck135
diagnosed a NEW sub-species: an annotation that was firing on the WRONG option class — the permanent-
targeting "NO legal target right now" annotation was being applied to a COUNTERSPELL (Force of Negation),
whose target lives on the stack. That mis-applied annotation rendered a self-CONTRADICTION
(`NO legal target right now` AND `can target on the stack: X` at once) and drove ~470 words of oscillation.

The fix (suppress the permanent-target annotation on counter-type options) SHIPPED this wave. deck135
verifies it and the verification sharpens the terminus in two ways:

1. **The primary success signal for an annotation-SUPPRESSION fix is the contradiction string going to
   zero corpus-wide** (`grep 'NO legal target right now'` -> 0), the deletion-end analogue of "marker
   appears." Confirmed: 0 at seat, 0 corpus-wide.

2. **The load-bearing SECOND-ORDER signal is the REASONING-TAX COLLAPSE at the freed decision** — not a
   modest drop but a category change: ~470 words of self-negotiation (wave-10 deck140 s58) -> ~100 words
   of clean, correct reasoning (this wave deck140 s30-s32, Force on Pyroclasm). This is the wave-8 reasoning-
   tax-drop signal at its extreme: a contradiction in the option surface is a MULTIPLIER on a weak model's
   derivation length, so removing it doesn't just shorten the plan — it lets the model reach the RIGHT
   decision it was drowning in noise before. **Generalized rule:** when a shipped fix REMOVES a
   surface-level contradiction (not merely surfaces a fact), score it by BOTH (a) the contradiction string
   -> 0 AND (b) a reasoning-tax COLLAPSE at the affected decision, AND check whether an ADJACENT faculty
   the noise was corrupting has recovered — here Force TARGET DISCIPLINE (declined every 1-for-1 cleanly;
   cast Force only on a genuine board wipe), a discipline the wave-10 confusion had degraded into
   soft-target mis-Forces (Downsize/Elixir/Cyclonic Rift). A mis-applied annotation is not just tax; it can
   spread wrong ACTIONS, and the fix's benefit is measured across both surfaces.

Corollary retained (pause-aware, do not over-credit): the ENGINE resolution bug underneath the annotation
(free-alt-cost Force never counters) is UNCHANGED and was deliberately not fixed this corpus. The one clean
Force cast this wave was adjudicated mid-resolution, so the annotation-fix win is on TAX + DISCIPLINE, not
yet on a confirmed counter. This is the wave-9 layered-leak split in its cleanest form: the INTERFACE fix
(annotation) fired and validated; the underlying ENGINE fix is the separate, still-pending rung.

## Held from wave-10 (reconfirmed at this seat, no change)

- **Verify a shipped REPLY-PROTOCOL change by the STRUCTURAL head signal, distrust the old metric across a
  protocol boundary.** Answer-last stays clean: 0 numeric-headed replies across all 6 seat files; the
  `intent-collapse-metric.py` reversal-flag count remains an unreliable pointer (carried HARNESS note),
  not a defect count.
- **The 4096/timeout HARNESS class must not inflate a decision count.** Fallback type shifted
  unparsed_reply(2048) -> empty_reply(120s HTTP timeout, 240s fix shipped next corpus); all self-healed,
  none changed a game. Score annotation/develop obedience against the REASONING, and treat a fallback
  window as a harness item, never annotation-ignore.
- **Score a value-floor / reactive rule (Force) by DECISION-CORRECTNESS, not usage-rate.** 19 Force windows,
  2 cast-choices (1 real cast on a correct board wipe), 15 correct declines — the falling take-rate is
  IMPROVED play, not under-use.

## Convergence-watch (deck135 votes)

- **colored-pays-generic false belief: NO recurrence at this seat this wave** (the model self-corrected the
  inference in every "generic" window). The parked CORE candidate does not harden further from deck135; it
  still awaits a cross-seat 2nd deck to promote, and core stays PASS (the core body already carries the
  governing fact — a 3rd copy is the sanctioned-against escalation).
- **NO new guide line, NO core edit, NO demotion** (pause-aware: untested/still-leaking anchors are frozen
  at full prominence). Guide is byte-identical.
