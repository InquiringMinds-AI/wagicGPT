# Deck-135 wave-11 — general-strategy / core suggestions

## OBSERVATION 1 (CARRIED, NOT reinforced this seat) — "colored mana can't pay generic" is a CORE candidate; deck135 did NOT recur it this wave.

Status update on the wave-9/10 parked core candidate (a specific false mana-payment belief overriding the
core's already-stated "any color can pay a generic cost"). Wave-10 deck135 hardened it within-seat
(deck110 s21 declined offered `{1}{u}`/`{1}{g}` casts on the false belief). **This wave the belief did NOT
recur at the deck135 seat** — the model resolved the generic-payment inference correctly in every window,
including explicit self-corrections (deck131 s52, deck44 s28, deck133 s5; see findings Q6). This CONFIRMS
the wave-10 read that the belief is model-UNSTABLE (fires wrong on some long derivations, right on others),
not un-taught.

- **Core stays PASS.** The core prompt body already states the governing fact verbatim ("any color can pay
  a generic cost"). A 3rd/4th copy is the sanctioned-against escalation; deck135 does NOT propose a core
  edit.
- **Promotion trigger unchanged:** a CROSS-SEAT 2nd deck's reviewer finding the same offered-payable
  decline. **Synthesis grep** across all seat files: replies concluding a Cast-nothing / decline that cite
  an inability to pay a GENERIC pip with available COLORED mana ("no generic mana", "lack the generic {1}",
  "cannot pay {1}{X} because I have no colorless"). deck135 contributes ZERO such windows this wave.
- **Durable fix if it ever promotes = REPRESENTATION, not louder core prose:** an option-line
  affordability/payability breakdown (the engine already partly does this via offered=payable). This
  remains the project doctrine (representation beats instruction).

## OBSERVATION 2 (synthesis attribution) — the stack-annotation fix is a corpus-level WIN worth crediting explicitly.

deck135 is the seat that OWNS the Force-of-Negation annotation defect, and it VALIDATES the wave-10
stack-target annotation fix cleanly (findings Q1): contradiction string 0/corpus, Force reasoning-tax
~470w -> ~100w, Force target-discipline restored (mis-Forces of soft targets gone; correct board-wipe
Force on Pyroclasm; all 1-for-1s declined). Recommend synthesis record this in the wave-10-batch validation
table as CONFIRMED, alongside the standing note that the ENGINE resolution bug beneath it (ledger #1, free-
alt-cost Force never counters) is UNCHANGED and is next cycle's top engine item. The interface fix landed;
the engine fix has not.

## OBSERVATION 3 (WATCH, single-seat, does NOT clear the bar) — a Search-for-Azcanta / Azcanta card-fact hallucination.

deck44 s34 (findings Q5): the pilot believes Search for Azcanta transforms into a 4/4 FLYING CREATURE
blocker (Azcanta, the Sunken Ruin is a LAND) and fixates on the unoffered "transform" action, falling to
Cast nothing over offered develop spells. Single window, lost game — does NOT clear the HIGH adopt-a-
revision bar, and single-seat items cannot add/cut core lines (convergence gate). Flagged for synthesis
ONLY as a WATCH: if a 2nd seat/window shows a pilot treating Search for Azcanta or a transforming DFC as a
creature/blocker, the cheapest correct move (Method headline 4 — teach the specific card fact where the
pilot reads it) is a KEY CARDS micro-note that Search for Azcanta flips to a LAND (card advantage), not a
body. Not proposed for adoption this wave.

## No other general/core proposals.
Everything else routes to the engine ledger (Force resolution; fetch re-ask) or is model-side (uncastable-
plan / Azcanta hallucination) — see notes.md. The guide is FROZEN byte-identical.
