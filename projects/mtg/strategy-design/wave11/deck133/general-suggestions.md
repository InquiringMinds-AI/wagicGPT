# deck133 wave-11 — core-prompt (general-strategy.txt) proposals

**Verdict: PASS (no core prompt-body edit).** No 2+-seat core candidate at this seat.

deck133 produced no core candidate. The convergence gate forbids a single-seat item adding a core line, and
the freeze posture holds (core is on its 7th consecutive PASS). The core reflexive-blocking floor is
untestable here (0 blocker windows, structural). Layer-routing note: the one engine/model item this wave
(self-Fatal-Push via a stack-targeting rules misconception) goes to notes.md, NEVER prompt text.

## Single-seat parked item (NOT proposed — flagged for a 2nd-seat grep by synthesis)

**A legal-targets/own-targets annotation can be disobeyed IN THE REASONING via a stack-vs-battlefield rules
misconception — the model treats an instant (Fatal Push) as able to destroy an opponent creature SPELL still
on the stack. The durable lever is REPRESENTATION (name WHY the on-stack creature is not a legal target) +
MODEL, never a core prose line.**

Grounded in vs110 seq25 and vs135 seq45 (findings E): both cast Fatal Push at an opponent creature that was
mid-cast on the stack, reasoning explicitly that the on-stack creature was a valid target, and resolved onto
their own Bloodghast. The annotation (`the only legal targets are YOUR OWN right now`) was PRESENT and read;
the model fabricated a legal opponent target anyway.

Why PARKED, not proposed:
1. **Single seat, 2 instances**, both in WON games (non-deciding) — fails the convergence gate.
2. It is a **model rules-misconception**, not an instruction gap: the pilot had the correct annotation in
   front of it and reasoned past it (representation-beats-instruction wall). Louder core prose is the
   sanctioned-against escalation. The candidate durable fix is REPRESENTATION — an option-line refinement
   that names the on-stack creature as untargetable (e.g. surfacing that the opponent's incoming creature is
   a SPELL on the stack, which an instant cannot destroy) — routed to the engine ledger via notes.md #1.
3. The whole class is self-limiting here (both hits landed on Bloodghast, which returns via landfall).

**Reactivation trigger:** a SECOND seat this wave showing a legal-targets/own-targets annotation disobeyed
IN THE REASONING (a plan that fabricates a legal target the annotation excludes — especially an on-stack
creature-spell or another not-yet-a-permanent object). If it recurs cross-seat, promote the representation
refinement (name the excluded target's reason) to the engine ledger as a shipped item; still no core line.
