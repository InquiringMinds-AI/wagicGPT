# deck131 — general (core-prompt) suggestions, wave 11

## PASS.

No core prompt-body change proposed from this seat. Rationale:

- No 2+-seat core candidate originates here. Every wave-11 finding routes BELOW the core:
  - H1 (bare `BLOCKS: none` unparsed -> baka heuristic blocks Guttersnipe) is a HARNESS/PARSE
    defect (root fix in the reply parser), NOT a prompt-body item. Layer-routing is firm doctrine:
    never re-word the guide or core over a parse defect.
  - H2 (confusion-spiral -> 120s HTTP timeout) is the known corpus-wide HARNESS class; the 240s
    timeout fix ships next corpus.
  - H3 (unlisted-card fixation + out-of-range CHOICE) owns the code-appended REPLY-PROTOCOL layer.
  - H4 (block-when-not-lethal + first-strike math error) is an instance of the standing reflexive-
    blocking convergence class, already routed to REPRESENTATION (the blocker-seam lethal/`[not
    lethal]`/trade-outcome rider) and rejected as a core sentence 5x; the first-strike-knowledge
    sub-gap is single-seat WATCH, below the 2-seat promotion bar.

- The single-seat `damage>=toughness=dead` WATCH (wave-8 vintage) did NOT recur this wave; stays a
  WATCH, do NOT promote.

Core status inherited: PASS (would be an 8th consecutive-wave freeze). No prompt-body edit.
