# Skill delta (wave 11) — deck110 seat contribution

The CURRENT skill remains `strategy-design/wave10/strategy-writing-skill.md`. Wave 11 is a
VERIFICATION wave (guides at terminus, adopt bar HIGH). The deck110 seat proposes **NO new method
headline** and **NO cut/edit to any existing method line** — every wave-11 observation either CONFIRMS
an existing rung or is single-seat WATCH material that fails the convergence gate. Refinements below are
verification sharpenings offered to synthesis, not authoring changes.

## Confirmations of existing method (no change)

1. **Confirmed-fired-representation DEMOTION terminus + the COUNT-AGREEMENT shape (wave-10 Method
   headline 3) held a 2nd verification wave.** The artifact-tag completeness lever's success signal is
   still (a) surface complete/agrees AND (b) defect outcome-neutralized — re-derived count agrees with
   summary N in **211/211** windows (up from 177/177), and the fold-in reasoning artifact PERSISTS yet
   flips no decision (all Blast casts at surf>=3). The loop stays CLOSED; do NOT re-open with louder READ
   wording (escalate-at-a-wall anti-pattern). This is now a 4-wave-deep representation-beats-instruction
   wall — the demonstration that a complete, accurate, guide-pointed surface can coexist with a durable
   reasoning artifact when the artifact is outcome-neutral.

2. **The OUTCOME-DELTA no-op predicate (wave-10 Method headline 5) is re-validated as the correct fix
   shape by a NEW churn variant.** Wave-10 fired the equivalent-carriers bounce; wave-11 fired the
   two-Plating CONSOLIDATION-with-confused-reasoning (deck109 T8: both Platings onto Master while the plan
   believed the Ornithopter kept one). Different surface, same root: an identity-keyed no-op marker is
   blind wherever multiple equivalent sources/targets exist, and "move the second Plating onto a creature
   that already has one" reads as a gain. The single outcome-delta predicate ("moving here does not
   increase resulting power above the current carrier's") still catches every observed shape. Reinforces:
   engine ledger, NEVER a guide line — the guide already forbids it and the pilot has no surfaced gain
   signal to obey.

3. **The reply-protocol taxonomy's answer-LAST trailing mismatch (wave-10 Method headline 2) gains
   another 2nd-seat witness.** deck140 s6 at the deck110 seat: plan concludes HOLD the Blast, emits Cast
   Blast — the same ASYMMETRIC "cast the offered SPELL when the plan wanted to hold" failure deck140's
   own seat logged 4/4 last wave. The sharper A/B target (reconcile the trailing index against the plan's
   NAMED action / echo the option TEXT, not a bare integer) DOMINATES "accept the last bare number" here
   too. Route to the code-appended reply-protocol layer; never score a guide over it. The unlisted-card
   fixation SUB-variant (deck110 P3, wave-10) did NOT recur as a failure (deck131 s8/s11 resolved it
   correctly to "Cast nothing") — keep at WATCH, do not promote.

## Verification-method sharpening (offered to synthesis, additive)

- **When scoring an OBSERVED-VIOLATED guardrail's leak, separate the CORRECT multi-instance play from
  the churn leak before quantifying — the marker-obeyed rate is not the churn rate.** deck110's 20 equip
  picks include legitimate two-Plating stacks (deck140 s32/33, s46/47), a minor ground->flyer re-pick
  (deck135), and one costly confused consolidation (deck109). A raw "N equips, marker 90/0 obeyed" tally
  hides the churn entirely; a raw "count same-name consecutive equips" tally would falsely flag the
  correct two-Plating stacks as churn. Classify each equip run as correct-stack / correct-upgrade /
  churn against the OUTCOME DELTA (did resulting board power rise?), then quantify only the churn. This is
  the equip-seam instance of the wave-8 deck44 "score a value-floor rule by decision-correctness, not
  raw usage-rate" and the wave-7 "join the board state before scoring a raw tally."
