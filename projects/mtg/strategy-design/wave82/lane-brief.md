# Wave-82 step-one lane brief (base = master 35e1e82dc; evidence = wave81/synthesis-notes.md §2 merged list + the seven wave81/deck<N>/review.md + audit-2026-09/bug-list.md #13/#14)

This is STEP ONE of the owner's loop: "fix all known bugs, consider interface improvements that should give clearer and more complete information to the opponent llm allowing it to make better choices, improve parsing of those choices, consider structural improvements ... that reduce the inference turns required and or should produce more successful actions". A LIST, not a quota: a fix is validated when the false line no longer renders / the missing option or fact now renders / the fixture is green. Read the item's repro record (file + seq in synthesis-notes.md) BEFORE touching code; the corpus is `/home/magi/.Wagic/ai/gpt/selfplay-runs/matchups-20260917-191141-final` (records `kind: window` + `seam`; `prompt` = the full message the seat saw).

RULINGS IN FORCE: reasoning happens ONLY in the model's native reasoning channel (thinking on, always); the reply is the PLAN then the action line; the PARSER IS ROBUST — it reads the answer wherever it unambiguously is and rejects only an answer that PRECEDES the plan or is ambiguous; the golden protocol TEXT (`bin/Res/ai/gpt/reply-protocol.txt` / `kReplyProtocol`) is owner-ruled — do not change it; render brackets, row text, row NAMES and headers are loop-owned and may change; NEVER remove, cap, or auto-answer a legal option (a hold row may be re-named or annotated, never suppressed; a decline may be ADDED); the surface owes truth ("a true statement in the wrong scope is a lie"); ask/replay keys = BOARD STATE + QUESTION (any new number in a row must be proven outside every key — PARSETEST pin: two windows differing only in it yield identical keys); diagnostics compile-gated `#if defined(_DEBUG) || defined(WAGIC_DEVLOGS)`; no pilot config changes; NO corpus, NO requests to the pilot from a lane; no VPK; NO AI attribution in commits; never commit CLAUDE.md.

MECHANICS: work ONLY in your worktree `worktrees/lanes/w82-<LANE>` (branch `w82-lane-<LANE>`, seeded from 35e1e82dc with objs-sdl; run `make -f Makefile.sdl -j8` from its projects/mtg — the first build relinks; both link-time guards (ctor-init, reply-instructions) must print OK). GPT layer = five TUs: `src/AIPlayerGPT.cpp` (prompt facts + presentation), `AIPlayerGPTParse.cpp`, `AIPlayerGPTSeams.cpp`, `AIPlayerGPTTranslog.cpp`, `AIPlayerGPTTransport.cpp` (the ctor), `include/AIPlayerGPTInternal.h`; PARSETEST corpus = `AIPlayerGPTSelfTest.cpp`. EVERY wagic run under `systemd-run --user --scope -q -p MemoryMax=4G -p MemorySwapMax=0 --` (a runaway is a contained red test: audit the loop you touched). PARSETEST: `WAGIC_GPT_PARSETEST=1 ./wagic` from bin — baseline **7314 passed / 0 failed**; add cases for every render string you change (positive + negative + the echo shape of any new bracket). Suite THREADS=1 as a detached unit `systemd-run --user --unit=w82-<LANE>-suite -p MemoryMax=4G -p MemorySwapMax=0 --working-directory=<worktree>/projects/mtg/bin bash -c 'unset WAYLAND_DISPLAY DISPLAY; WAGIC_HEADLESS=1 WAGIC_TESTSUITE=1 WAGIC_TESTSUITE_THREADS=1 ./wagic > ~/.gatelogs/w82-<LANE>-suite.log 2>&1; touch ~/.gatelogs/W82_<LANE>_SUITE_DONE'` (rm the done-file first; wait with a FOREGROUND `until [ -f … ]; do sleep 10; done`, never a Monitor); baseline **1314 tests / 82 AI, 0 failed** except the known flake `intrepid_adversary_repeated_payment` (fails ~6/10 solo on the accepted binary — a failure with ANY other name is yours). Count both `==Test Failed !==` and `==Test timed out`. Edit the GPT TUs and AIPlayerBaka.cpp with the Edit tool (UTF-8); MTGRules.cpp / GameObserver.cpp / primitives are CP1252 (GameObserver CRLF) — byte-splice only; `git diff | /usr/bin/grep -c $'\357\277\275'` must be 0. Use /usr/bin/grep. A fixture that needs the GPT seat's live caller: the development-build stub `WAGIC_GPT_STUB` + `tools/w81-dk-window-probe.sh` shows the pattern (TestSuiteAI is the HEURISTIC — a suite fixture on a GPT-only path tests the component next to the defect). Commit on your branch with EXPLICIT paths, one commit per item (`git add -f` for anything under bin/Res). Write `wave82/lane-<LANE>.md` (per item: what was false/missing → what renders now, the repro record re-rendered through the live composition if you can, PARSETEST/suite counts, WEAKEST EVIDENCE). Touch `~/.gatelogs/W82_<LANE>_DONE` when your branch is committed and gated. Final message ≤ 12 lines. If an item turns out to be a design call the owner must make, say so in the lane file and skip it — do not park a question in a document as if it were answered.

## Lane EA — parser, transport, harness, tools (`AIPlayerGPTParse.cpp`, `AIPlayerGPTTransport.cpp`, `AIPlayerGPTTranslog.cpp`, `projects/mtg/tools/*`, `AIPlayerBaka.cpp`/`GameObserver.cpp` only for #13's seed)
1. **H7** — the corpus's only fallback: reply = the single line `PLAN: 4 (Cast nothing right now)` (deck50 vs 125 seq41; recovery seq42 `answer_label_absent: false`, `executed_by: heuristic`). Under the ruling this is unambiguous: no plan exists for the answer to precede, and `4 (<short name>)` matches exactly one row. Read it. General rule: when no action-label line exists and a labelled line's content is exactly `<row> (<that row's short name>)` (or the bare row number + short name) matching one row, it is the answer. And when the parser still cannot read a reply with deadline left, RE-ASK (the pilot had 891 s); the heuristic answers only when the deadline is gone. PARSETEST: the verbatim reply (positive), a genuinely ambiguous sibling (negative), and an answer-before-plan sibling (rejected).
2. **H6** — the reasoning-budget overrun retry re-asks with `max_tokens_reasoning: 0` (deck125 vs 152 seq23 `reasoning_chars` 20,683; vs 162 seq300 26,808; both `reasoning_budget_hit: true`, `retry: 1`). This violates the native-channel ruling. Keep thinking ON across the retry — raise the budget on retry rather than zero it (the owner's default posture is GENEROUS limits, tighten only on evidence). Records must show `thinking: on` on the retry.
3. **M3** — `async_drops` 9 → 52 with no per-record trace. Add an `async_drop` side record (seam, `window_seq`, what was discarded, why: board drifted / window closed / …) so a seat can adjudicate each drop from the translog; `corpus-stats.py` sums the new kind (census must still RECONCILE: round trips + non-round-trip kinds = record sum). Then say from the records what the 52 were.
4. **Bug-list #14** — `tools/corpus-early-check.py` counts `kind == 'ask'`; since P11 every decision record is `kind: window` + `seam`. Fix it to count windows by seam (and any other reader in tools/ still keyed on the old kinds — grep them all); run it on the wave-81 corpus dir and show real numbers.
5. **M13** — `tools/check-reply-instructions.py` matches only `*_strategy.txt`, so a staged `strategy.txt` gets zero coverage and passes vacuously. Match `strategy.txt` too and print the guide count on every run.
6. **Bug-list #13** — the heuristic A/B gate is UNPAIRED (`tools/baka-ab.sh` seeds each game by launch second). Add `WAGIC_SELFPLAY_SEED` to the engine's selfplay seeding (find where the selfplay RNG / deck-shuffle seed is taken from time — see audit-2026-09/step1-lane.md, which has the LD_PRELOAD time() shim that proved paired games trace-identical) and a `--paired` mode to `baka-ab.sh` that records each accepted game's seed and replays the same seeds on the candidate; `baka-ab-compare.py --paired` compares per-game outcomes (same seed → same or different winner), not per-deck rates. Prove: two runs of one binary with the same seed set are trace-identical; the compare tool's selftest covers the paired path. Do NOT run the full gate — 20 paired games is the proof.

## Lane EB — the casting menu, land rows, hold rows, target/discard/reveal windows (`AIPlayerGPT.cpp` presentation + `AIPlayerGPTSeams.cpp`)
1. **H1** — on every folded casting menu the `Land drop:` header still says `The land drop is its OWN decision (a "Land drop:" question with its own Play options), so the absence of a land from the choices below does not mean the drop is gone` while a `Play <land>` row sits below it (deck130 44/44 windows; deck152 vs 130 seq26: `PLAN: Play Plains … CHOICE: 5 (Cast nothing right now)` — the drop was lost). Replace the header with text true for the fold: the land row IS the drop this window; declining plays no land in this window (the folded decline row already says so). `WAGIC_GPT_LAND_SEPARATE=1` (the P9 revert switch) must still render the old header with the old regime — keep both paths true.
2. **H2** — both regimes run at once: a standalone `Land drop:` ask (options `['Play Marsh Flats', 'Play no land right now']`, deck123 vs 152 seq34/37) still fires when no casting menu exists. Decide and document ONE regime: when the only playable thing is a land, is the standalone ask the fold's degenerate case (fine — then its header must say so and telemetry must tag which shape the window was: add a `land_shape: folded|standalone` field), or should it route through the same menu builder? Make the two shapes consistent and tagged; the owner reads the P9 revert criterion off the next corpus's counts, so the tag is required.
3. **H8** — the hold row's SHORT NAME `Hold priority` is read in its Magic sense (25 takes across the corpus; deck123 vs 130 seq78 `PLAN: Hold priority to block Rorix … CHOICE: 3 (Hold priority)` — it forfeited the turn's windows and the game). Rename the row for what it does (e.g. `Stop asking me this turn`); the body text is already right — no new body words. The parser must accept the new name AND the old one (echo tolerance; PARSETEST both). Keys unchanged.
4. **H9** — a hold taken at the seat's OWN End step silences every window of the opponent's whole next turn (deck125 vs 162 seq211: T28 Underworld Dreams + Teferi's Puzzle Box resolved unasked; `hold_windows_skipped` 277 that game). The row's own words say "later THIS TURN"; its scope is "until your next turn begins". Make the scope match the words: a hold expires when the turn it was taken in ends. The DK re-open logic is unchanged.
5. **H10** — a damage spell's target ask prints `(this ask has no pass row)` over rows that all SURVIVE (deck130 vs 162 seq11); once the cast row is taken every "decline" rule in the guide is unreachable. Least-invasive true fix first: the CAST row carries the target verdict when every legal target survives / is the caster (`[every legal target survives - casting this kills nothing]`), so the decision is made where a decline exists. If you can also add a decline that unwinds the cast without engine surgery, do; if that needs the engine, record it as an item and stop.
6. **M10** — the hold row is offered on a menu where every other row is a zero-mana loyalty ability while the screen prints LETHAL (deck146 vs 152 seq36). Do NOT suppress it (ruling); print a refusal-style marker on the hold row when every non-pass row costs no mana (`[nothing here costs mana - holding gives away free actions]`).
7. **M9** — the X row's `best X` badge names a survivable X on a row every stop refuses (deck125 vs 162 seq296) and `x_cast_row_refusal_markers` fired 0 times. Apply DL's refusal-first marker shape to this badge: when the cast is refused by NET life, the row leads with the refusal and the badge does not recommend an X.
8. **M14** — the Upkeep animation row (`[Upkeep offer: this animation lasts only until end of turn, and the same row is offered again in your main phase]`) does not say the animation cannot block on the opponent's turn (deck146 vs 152 seq45 planned to). Add the block clause.
9. **M4** — discard / bottom / reveal seams print bare card names (deck123 vs 162 seq41: eight bare names, two identical Keepers unmarked). Give those rows the same annotations the cast rows carry: cost, `copy N of N`, `dead right now` / castable, `{spare:}` where the seam is cleanup discard. Keys: these are answers keyed by row identity — prove the annotations stay outside the key.
10. **M5** — a tutor/search window lists the whole library, 45 rows unfiltered (deck123 vs 130 seq45, `finds only an enchantment card`; deck125 seq9, 48 rows). Mark which rows are LEGAL for the search (never remove the rows — the engine's legal set is what it is; if the engine offers illegal rows, say so on them) and collapse identical entries the way P3 collapses hand entries (with the count).
11. **M12 (data, not code)** — six guide literals render 0 times corpus-wide: `blocking can leave them as high as`, `unless the board changes` (as the hold row's words), `you SACRIFICE this`, `[no cast row now: it must have a target`, `LIFE-TO-DAMAGE CONVERTER`, `LOOP`. deck152 and deck125 re-keyed their own guides; sweep the OTHER pool guides (`bin/Res/ai/baka/deck{146,130,123,162,50}_strategy.txt`) — re-key each rule to the LIVE string (the live forms are in synthesis-notes.md M12; `[no cast row now: no legal target - HELD]`, `FORCED sacrifice OF YOUR OWN CARD`, the four live `ATTACK TOTAL:` forms, the live hold-row text after your H8 rename). Same byte count or smaller; ≤ 20,000 B; `check-reply-instructions.py` OK; `git add -f`. For `LIFE-TO-DAMAGE CONVERTER` / `LOOP`: find the emitter — if it is reachable in the pool, leave the rules; if it cannot render for any pool deck, say so in the lane file and leave the rules (a guide-content call is the review seat's, not yours).

## Lane EC — combat verdicts, the narration register (P5), library/mill facts (`AIPlayerGPT.cpp` facts + presentation, `AIPlayerGPTSeams.cpp` for the blockers/attackers seams)
1. **H3** — the attacker row's `their untapped blockers` 1-on-1 verdict ignores damage already marked on the attacker, and no line outside the GAME LOG shows marked damage (deck130 vs 123 seq72: Rorix 6/5 with 4 marked, battlefield prints `(6/5) [untapped]`, verdict `(you kill it, your attacker lives)`; the seat attacked and lost its win condition). Two fixes, both: print marked damage on the battlefield/attacker line (`(6/5, 4 damage marked)`) and subtract it inside every 1-on-1 result. Prove the number stays outside every key.
2. **H4** — the blockers seam prices an attacker's lifelink only as a cost of BLOCKING (`A2. Intrepid Adversary (6/4) deals 6 [lifelink]` bare; the gain only in the B-row brace `they gain 6 from this block only`), so the menu reads as if declining avoids the gain (deck162 vs 152 seq11: `BLOCKS: none`, took 11 instead of 5). Put both branches and the owner on the A-line (`[lifelink: THEY gain 6 if it connects, blocked or not — a block only changes who takes the 6]`), drop `from this block only` from the B-row brace. Same emitter family as R331.
3. **H5 (engine half)** — no deck-out clause on the seat's OWN library line (deck125 vs 50 seq305: opponent line carries `DECK-OUT IS IN RANGE`, own line is bare `Your library: 11 cards`; the seat decked at 59 life), and no cast/X row prices the mill the cast itself triggers (seq282 option 12 promises `13 left`, library reads 5 after four Memory Erosions fire). Render the own-library deck-out clause with the same arithmetic the opponent line gets (draws per turn incl. Howling Mine / Puzzle Box / punishers), and price mill-on-cast on the row (`{library: 13 -> 5 after Memory Erosion x4}`).
4. **M1** — P5 collapsed damage batches print the batch's FIRST life total and claim losslessness (`… 1 damage to you (now 30) [x12 - … only the numbers in them differ]`, seat at 19 after; four seats). End the collapse with the batch's final total and the per-line value when constant: `Underworld Dreams -> 12 x 1 damage to you, 31 -> 19`. The "only the numbers differ" clause goes.
5. **M2** — opponent spell casts render with no actor prefix inside the seat's own turn block (`cast Emeria's Call; cast Cancel; your Emeria's Call was COUNTERED by Cancel`; 30+ lines in deck123's logs). Prefix the actor when it is not the turn's owner (`opp cast Cancel`), as the old register did (`WAGIC_GPT_NARRATION_V1=1` shows it).
6. **M6** — an activated ability's damage renders as the attacker's combat damage on the `Attack:` line while the attacker was BLOCKED (deck130 vs 123 seq67: `Attack: Siege-Gang Commander … -> Siege-Gang Commander: 2 damage, opp 4; … used Deal 2 damage with Siege-Gang Commander -> the opponent`). Combat damage on the Attack line only; the activation's line carries its own result.
7. **M7** — the same draw step narrated twice with different counts (`T13 (opp): drew.` then `Draw: … drew 6`; deck162 vs 50 seq15, 7 turns). When a turn gets a `Draw:` block the turn line carries no draw.
8. **M8** — damage that fails to kill a creature prints no outcome (`Starstorm -> 4 damage to Rorix Bladewing` — it survived, nothing says so; deck123 vs 130 seq75). The register's rule is "damage carries the new life total"; give creatures the equivalent: `-> 4 damage to Rorix Bladewing (survives, 4 marked)` / `(dies)`. Pairs with H3.
9. **M11** — `verdict_clamped` storms: 44 of 108 `hold_verdict_safer_ignored` are one game clamping two faces that say the same thing (`you survive the stack` over `nothing lethal on the stack`; deck146 vs 125 hold_event seq140+, bursts per Staff ping). Fold the two faces into one so equal-meaning faces do not clamp.

After all three lanes: the orchestrator merges (`bash projects/mtg/tools/merge-lane.sh <branch>` + `python3 projects/mtg/tools/check-merge-inits.py`), runs the hermetic gate (make -B, suite THREADS=1, PARSETEST) and the heuristic A/B gate (`bash projects/mtg/tools/baka-ab.sh <bin-dir> ~/.gatelogs/w82-baka 10 4` vs `archives/baka-ab/62a5733ec` — and, once EA's `--paired` lands, paired), archives the binary, then ROTATION and the corpus. NOT in this step one (separately scheduled, owner-ruled A/B only): P1/P2 — moving invariant sentences / card text into the system prompt.
