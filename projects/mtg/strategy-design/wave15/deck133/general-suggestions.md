# deck133 wave-15 — core-prompt (general-strategy) proposals

**PASS — no core-prompt change proposed from this seat.**

Rationale (layer-routed, per the binding doctrine that a single seat cannot add or cut core lines):

- The core prompt's decision surface performed correctly wherever the model reasoned directly:
  name-echo compliance held, the impact-cast/Thoughtseize/Gray-Merchant judgment obeyed the guide,
  and the beatdown line executed cleanly (vs135 WON, vs44 WON via a GM drain chain the heuristic even
  rescued at two unparsed windows).

- The wave's three findings all route BELOW or BESIDE the core prompt, never into it:
  1. Crash-fix + absent-echo-route validation -> confirmation only (finding 1). No prose lever.
  2. self-Fatal-Push via a card-presence hallucination (finding 2) -> an ENGINE/REPRESENTATION item
     (suppress a removal cast whose only legal targets are friendly). A core-prompt line telling the
     model "don't cast removal at your own creatures" would be present-and-disobeyed (the option
     annotation already says "only legal targets are YOUR OWN" and the model still cast it under a
     hallucination) — the durable fix is structural suppression, not more prompt text. Notes #A.
  3. stale_echo name-superstring false positive (finding 3) -> a HARNESS reconcile item. Notes #B.

- The deck's losses were structural (no early blocker, no reach, self-damage suite) — deck-
  construction items (notes #D), not core-prompt gaps. A symmetric core prompt is blind to win-rate
  anyway; decision-quality was the signal and it was clean.

Nothing here reaches the core prompt. PASS.
