# general-suggestions.md - deck17 (wave-15)

Cross-deck / core-prompt / representation candidates surfaced at this seat. None is a
core-line change on single-seat evidence (per doctrine); listed for the synthesis pass to
cross-reference against other seats.

## Representation candidate (cross-seat interest): HYBRID-PIP AFFORDABILITY annotation.
The hybrid false-belief taxed deck17 in every game and caused 2 under-deployments. The core
"payable if listed" rule fixes the OUTCOME but not the TAX. A cheap option-line annotation -
e.g. render a hybrid cost the model can't misread, or a one-word "(pay with any color)" /
"(payable)" tag on Cast options - would cut the oscillation at the representation layer for
EVERY hybrid-heavy deck (deck17, deck44, and any future one), not just via per-deck prose.
This is the colored-pays-generic family's cousin; promotion trigger should be a 2nd seat
(deck44 is the obvious cross-witness - its guide already carries hybrid explanations, meaning
the belief is a known multi-deck cost). Route: representation, NOT core prose.

## Latency/tax is the dominant loss driver at this seat, and it is model-produced.
deck17 lost all 3 games to adjudication, at least 2 from grinding slowly while even/ahead on
tempo. The proximate cause was reply LENGTH (thousand-token hybrid oscillations + degenerate
repetition), not wrong final choices. This reinforces the corpus-wide note that adjudications
are a latency artifact - but here the latency is partly AUTHORABLE-DOWN: a guide fact that
pre-empts the hybrid re-derivation should shorten replies materially. Worth measuring reply
length at hybrid-cast windows next corpus as a guide-fired signal (tax collapse), the way the
skill scores annotation-suppression fixes.

## Core PASS vote (this seat).
No 2+-seat core candidate originates here. The core prompt's land/cast/steal-target-decline/
block-when-favorable rules all fired correctly. deck17's needs are all deck-specific (hybrid,
defenders, attack floor, steal priority) - guide-layer, not core. Core = PASS from deck17.
