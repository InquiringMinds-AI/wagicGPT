# General suggestions — deck44, wave 15

Core prompt: **PASS** (single-seat items cannot add a core line per the convergence gate). This seat surfaced
one HIGH cross-deck engine/harness item and confirmed a prior fix; neither is guide-prose.

## 1. (TOP, cross-deck, ENGINE/HARNESS) Confirm whether combat asks were intentionally removed

Between the wave-13 corpus (`matchups-20260715-183228`) and this one (`matchups-20260716-040138`), **all
attacker- and blocker-declaration asks disappeared corpus-wide** (wave-13 deck44 had a `Blockers` phase +
`ATTACK:` declarations; this corpus has 0 of either at any seat — phases are only Main phase 1/2, Upkeep,
Combat damage). Combat is now auto-resolved. This is load-bearing:
- It directly caused deck44's two closest losses: vs110 and vs21 both ended **opponent at 1 life, deck44 dead
  9 -> -2 on the crackback**, because the auto-attacker tapped all of deck44's creatures every combat, leaving
  nothing to block the return swing — a hold-a-blocker/chump line the guide prescribes but the pilot was never
  offered.
- It makes the block/lethal/attack-selection prose in **every** deck guide inert.
Ask the owner: is auto-combat intended? If yes -> schedule a corpus-wide trim of block/attack prose (core/method
change, synthesis-owned). If no -> it's a harness regression; restoring combat asks re-arms the existing guide
lines. Either way, gate all combat-guidance work on this answer.

## 2. (cross-deck) The absent-echo staleness route is validated — keep it

The wave-14 `stale_echo -> choice=-1` route CLOSED the wave-13 self-destroy class at this seat (2 correct
catches, 0 self-destroys, 4/4 target sub-menus correct). No change requested; recording the positive
verification so synthesis can mark the reconcile-arm item resolved unless another seat shows a WRONGFUL
downgrade (an echo that DID name an offered option yet got routed to heuristic — none here).

## 3. (construction, for the decklist owner — NOT a guide/core item)
deck44's blue-source density is the pinch: a Swamp-heavy board + a double-blue Counterspell / `{1}{u}` Arcane
Denial load left it unable to pay `{U}{U}` to counter a sweeper (vs140 s22, lost its creature to Pyroclasm).
The counters earn their slots (they carried the vs135 win and the vs140 Wrath counter) — the lever is a bit
more blue density or a black-castable interaction, not fewer counters. Same signature flagged waves 5-13.

## 4. (representation, LOW) Forced sub-menu framing — de-prioritized
Header + own-vs-opp annotation on target/sacrifice sub-menus is still a reasonable cross-deck nicety, but at
deck44 it is now cosmetic (the safety case closed). Rank it below item 1.
