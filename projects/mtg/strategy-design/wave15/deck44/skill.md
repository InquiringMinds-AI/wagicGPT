# Skill contribution — deck44, wave 15 (single-seat; no core/guide edits)

CURRENT skill = `strategy-design/wave13/strategy-writing-skill.md`. deck44 guide is FROZEN byte-identical
(11th wave, md5 `bd097b0c4cf8524fb6cc30b75312cabe`). Per the convergence gate a single seat cannot add/cut a
method/core line. Below: one method re-witness that RESOLVES a prior watch, and one HIGH cross-cutting watch
for synthesis (the harness change). No guide-prose deltas.

## Method — RESOLUTION of wave-13's open self-destroy/reconcile watch (promote to CLOSED)

Wave-13 raised "absent `(name)` echo must not fall back to the raw CHOICE index" after a self-GFTT destroyed
deck44's own creature. This corpus VERIFIES the wave-14 fix at this seat: 2 absent-echo events both routed to
`stale_echo -> choice=-1` and **refused the index-1 cast** (vs140 s22 refused an own-only GFTT the pilot did
NOT want; vs135 s48 refused a stale land-echo). 0 self-destroys corpus-wide; 4/4 removal target sub-menus
resolved to the opponent side. **Skill lesson:** the "score self-casts at the TARGET SUB-MENU, not only the
parent OWN-ONLY option" verification signature (wave-13 addition) now has a clean pass — keep it in the
checklist as the confirming grep (`Choose the target` / `[your battlefield]` `chosen_text`), but the class it
was written to catch is closed. This is a re-witness that a shipped fix held, not a new method rung.

## Watch (HIGH, cross-deck) — verify the COMBAT-ASK SURFACE before trusting any guide's block/lethal prose

**New, deck-agnostic, structural.** This corpus has **0 attacker/blocker-declaration asks at any seat**
(phases: only Main phase 1/2, Upkeep, Combat damage); the wave-13 corpus had a `Blockers` phase + in-main
`ATTACK:` declarations. Combat is now auto-resolved. Consequence for the skill: **a guide can only instruct
decisions the harness actually surfaces as asks.** Any guide's BLOCKING section, LETHAL-CHECK, and
attack-selection lines are INERT under auto-combat and can only pollute the pilot's reasoning. Add to the
skill's pre-analysis step a **surface audit**: enumerate the distinct `phase` values (and whether block/attack
asks exist) in the corpus BEFORE crediting or writing any combat-decision guidance. If auto-combat is
confirmed intended corpus-wide, a method-level change follows (trim block/attack prose from every guide) —
that is a synthesis/core decision, explicitly NOT a single-seat edit. Promote from watch to core action once
a 2nd+ seat confirms the same absence (it will — the surface is global).

## Watch (LOW, cross-deck) — forced sub-menu framing is now cosmetic-priority, not safety-priority

Wave-13 flagged unframed target/sacrifice sub-menus as a self-harm driver. With the reconcile fix + the model
reliably reading `[opponent's battlefield]`, the SAFETY case is gone at this seat (4/4 correct). The header +
own-vs-opp annotation improvement remains a nice-to-have for reasoning clarity but should be re-prioritized
BELOW the combat-surface item. Keep as a diagnostic watch; do not spend a guide line on it.

## Convergence-gate note
All items single-seat and DIAGNOSTIC/re-witness. Core prompt and deck44 guide both FROZEN. The one HIGH item
(combat-ask surface) is deck-agnostic and routes to synthesis/engine, not to deck44 guide prose.
