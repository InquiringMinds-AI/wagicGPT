# Wave-72 known bugs (corpus matchups-20260907-163626, 21/21 natural, 0 hung; sources wave72/engine-seat.md + wave72/deck<N>/review.md)

Invariant 000 held: 1,910/1,910 with native reasoning, 0 truncations, 0 hangs, 3 fallbacks (both noop fires GENUINE and recovered — the wave-71 false-fire class is closed). Window model: no legal option lost; the 13.9%-of-windows double ask is still paid (see N1). Closed by wave 72: Puzzle Box line 3/3, stale `[castable now]` 123→0, `Hold priority:` head 1,352→0, LOOP COMPLETE 0→67, token names gone, copies-not-public rendered, priority hold priced (deck126 holds over activations 4/4 → 4/42). Wave-71 guide edits mostly PAID (146, 126, 130, 152 each won a game the prior edit targeted). All seven wave-72 guide EDITs installed.

## N — routed to wave 73 (HIGH first)
- N1 HIGH — `sibling_window_asks_skipped` 0: `gptHoldCoversSiblingWindow` compares `serializeGameState()`, and BT M13 / BX F3 put the casting-decision stamp (`[no cast row now: you already answered…]`) INSIDE that state, so the cast and priority menus of one window never share a board (w71: 19 identical, w72: 0). Also BX F1's subset rule can never hold — the two menus are disjoint by construction. Fix: the sibling key is the board WITHOUT the answered-tag, and the covering rule is "the held menu's rows ⊇ the sibling's ACTING rows OR the sibling has no acting rows the model has not seen" — decide and state; every skip counted with both option sets recorded.
- N2 HIGH — THE HOLD FAMILY (four seats, one mechanism): a HOLD's release is "until a row changes", and a once-per-turn row is byte-identical every turn, so (a) 152v162 seq 34 one hold silenced Teferi's loyalty rows for the REST OF THE GAME (died at 0 with 6 unused turns); (b) 123v130 seq 31 a hold over a Create row = zero tokens for 15 turns; (c) 125v130 seq 46 an own-turn hold skipped from t27 to t31 (18→6 life); (d) deck146: 36 of 66 priority windows advertise "0 = pass priority" with NO pass row printed (`AIPlayerGPT.cpp:~35146` prints it only when `baseIndex == 1`), so HOLD is taken as a pass 12 times. Fix: a hold RELEASES at the start of the holder's next turn when any suppressed row is a once-per-turn/loyalty/repeat activation (or simply: every hold expires at the holder's untap — count `hold_released_turn`); the priority window ALWAYS prints an explicit pass row; the hold row text says it lasts past this turn.
- N3 HIGH — the stated stop is never parsed: `repeatRowStopClause` rendered 0 times over 379 Doomsayer prompts because `repeatPlanStopAndCurrent` needs rigid `stop <digit>` AND `m <digit>`; the repeat row's bracket asks for "your stop count" in WORDS, and the models wrote "stop count 24" / "reached the stop (24". The golden protocol is owner-ruled — do NOT touch it. Fix inside the RENDER: the repeat row's bracket states the literal form (`write stop=N; M=N in your PLAN line`), and the parser, reading ONLY the PLAN line, accepts the labelled forms `stop=N`, `stop N`, `stop count N`, `stop at N`, `M=N`, `M N`, `M is N` (a label + number on the PLAN line is not prose reading). Pin each form; deck123's guide already emits `stop=`/`M=`.
- N4 HIGH — rung menus with ≥10 rows sort LEXICOGRAPHICALLY (152v125 seq 62: 1/1, 10/10, 11/11 … 2/2; `CHOICE: 14 (becomes a 14/14)` → index 14 is the 9/9 → echo_index_conflict). All 5 such menus mis-ordered. Sort numerically.
- N5 HIGH — `LOOP COMPLETE` header text contradicts the `LOOP SCOPE:` clause in the same prompt ("a number that reads as life to them is lethal to you" vs "this number is life THEY lose… does not enter it"): 162v126 seq 29 the model held and forfeited its only removal (0–28). Also `[LOOP RUNNING … the HOLD row answers all of them at once]` printed on a menu whose rows were Ob Nixilis −2 activations outside the chain. One scope wording; the bracket only on rows IN the chain.
- N6 HIGH — trigger-chain windows: 130v162 turn 10 = 20 windows for one unstoppable trigger chain (hold row re-opens on each link's shrinking damage figure). The `[LOOP RUNNING]` collapse is scoped to the life loop only; generalise to any chain whose links the seat cannot answer (no legal response row other than pass/hold) — no legal option lost because there is none.
- N7 HIGH — the Predation/search reveal list gates on mana VALUE and prints NO mana cost on any row (146v125 seq 14: took Emrakul over Supreme Verdict, swept at seq 39). Print the cost on reveal/search rows.
- N8 MED — `replay_run` is per-window on one path and a game total on the other (max 90 reads as a breached cap; true max run 11); the 63%-majority `repeat_ask_reserved` path is unbounded. Unify; bound both.
- N9 MED — hold-row headline prices the activation but the "cannot take it later this turn" clause sits ~600 chars deep in the brace (125 seq 36/101: holds whose PLAN names their own +1). Lead with it.
- N10 MED — worked-example truncation at `substr(0,48)` (`:~36038`) emits unbalanced quotes/parens: 79 of 279 malformed, 20 reasonings call the prompt a typo. Cut at a token boundary and balance.
- N11 MED — loyalty clause still truncated in the BOARD FRAME (92 prompts) though fixed on the rows (BV M16).
- N12 MED — `reserve_decline_windows_noted` 0 over 48 reserve rows (BV M9c note not wired); declined-count clause still loses ~25% of windows for a THIRD wave (BW M21 "fixed at render" did not move it — re-open with a measured cause).
- N13 MED — land-drop seam has no plan-vs-board name check (126v152 seq 16: the only decline in 49 windows, off a hallucinated hand); the attackers seam prints one — reuse it.
- N14 MED — animated Lair of the Hydra sent into a tag naming its killer (152v146 seq 30→32): price `{if this dies while animated you lose the land}`; a `ghostform` counter printed bare while the trade tag says "(both die)"; MDFC back face named with no link to the hand card; "held back, it CANNOT block: X" printed beside "their untapped blockers: X".
- N15 MED — the noop re-ask said "You chose row 6" when the model wrote `CHOICE: 14` (short-name match rescued it) — quote what was MATCHED and why.
- N16 MED — a main-1 casting window did not fire on 152v146 t18 with two `[castable now]` cards — investigate (window model N1 territory).
- N17 LOW — opponent-turn ask share halved 15.0%→6.8% (M21's predicted direction; watch); `plan_line_missing` 3.8% all dropped labels (report only; protocol owner-ruled); 2 replies echo the prompt's `YOUR PLAN:` (render heading — re-check BR L11's quotation form).
UNTESTED carried: {X}-announcement payment loss (0 fires corpus-wide this wave — keep the docket, don't build on a zero), BS F1/F2/F3/F5/F6, BP land latch, BV M9c.
