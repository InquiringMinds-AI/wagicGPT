# Wave-72 step-one lane brief (base = master after wave71 known-bugs; evidence = wave71/known-bugs.md §M + wave71/engine-seat.md + wave71/deck<N>/review.md)

RULINGS THIS WAVE TOUCHES (skill invariants): 000 — reasoning only in the native channel; reply = PLAN then action, nothing else; no instruction licenses prose; no parser reads outside the two lines (a re-ask that restates the same labelled question with the refused row named is allowed); caps never bound reasoning; protocol TEXT / thinking regime / harness flags are owner-ruled (report, don't change). 00 — corpora complete full games. 3 — suite 0 failed (lifeline + intrepid_adversary_repeated_payment + merrow_reejerey are the known flaky/single-thread-only failures), PARSETEST 0 failed. Never remove, cap, or auto-answer a legal option — collapsing a window whose answer is ALREADY GIVEN (a reached stop, a mana-only window) is not removal. No VPK. No AI attribution.

Standing mechanics: as wave71/lane-brief.md (worktree-only; `make -f Makefile.sdl -j8`; both link guards OK; binary freshness; every wagic run under `systemd-run --user --scope -q -p MemoryMax=4G -p MemorySwapMax=0 --`; suite THREADS=1 as a detached unit `w72-<LANE>-suite` waited with a FOREGROUND until-loop, no Monitor; count both failure strings; PARSETEST from bin; Edit tool for UTF-8 sources, byte-splice for MTGRules.cpp/GameObserver.cpp/primitives; U+FFFD 0; RED-on-base evidence for every fix + a falsifiable prediction; commit with explicit paths, `git add -f` under bin/Res; write wave72/lane-<LANE>.md; touch `~/.gatelogs/W72_<LANE>_DONE`; report weakest evidence; no corpora).

## Lane BT — harness, no-op class, window budget (M1, M2, M10, M13, M14, M22)
M1 one-line harvest fix + a selftest that a sidecar file is harvested. M2: `rowSaysNoOp`/the zero-verdict census treat a PERMANENT's prospective/conditional zero (`until`, `when`, `equips nothing` with no creature) as NOT a no-op — pin Lightmine Field and Lightning Greaves rows negative, Supreme Verdict on an empty board positive; the `NO LIVE CAST ROW` header must not claim it either; the re-ask names the refused row and its verdict (one labelled line). M10: a repeat row whose stated stop is reached is not re-asked again this turn (skipped like a mana-only window, counted `stop_reached_windows_skipped`); fixture from 123v152 seq 28-38. M13: `[castable now]` only when the casting decision is open. M14: the L4 branch issues no ask when no instant-speed cast candidate exists; count `own_turn_windows_skipped`. M22: the ask-cache refusal is scoped per window (state+question), reset when the window changes; pin.

## Lane BU — opponent-turn response windows (M4, M5) — SHARED PATH, highest risk
Decide and implement ONE window model: the model is asked ONCE per window where an instant-speed cast is legal; the priority window and the sibling ask are not both issued (M4). Then M5: the GPT seat must receive response windows on the OPPONENT's turn (their combat — after attackers, after blockers — and their end step) whenever `hasInstantResponse` is true; today deck130 saw 4 opponent-turn windows in 353 decisions. Fixture RED on base: opponent attacks, seat holds Starstorm-like instant with mana, the seat's model must be asked after attackers. Report the decisions/turn cost with the prediction; do not regress the 0-hang result (BP's no-progress arm must stay silent).

## Lane BV — render truth A (M6, M7, M8, M9, M12, M16, M17, M18)
M6 hold row priced on the priority seam when an activation is live (positive/negative pins per seam). M7 name-choice list: no token names; render "copies not yet public: N"; no visible-first sort. M8 perspective normalisation on non-self `{effect:}` lines ("they gain" / "their opponent" — the converter paragraph's existing rule). M9 reserve: an unused land drop counts as a source; lead the STRANDS verdict with its consequence; carry the reserve decline across upkeep→draw and count it. M12 legend-rule header names the rule and which copy STAYS. M16 loyalty row text never truncated into the card-text tail. M17 reveal/search rows: legendary/duplicate annotation + destination zone + the reveal ask states the carried plan may not apply. M18 crack-back clause on the casting seam + arithmetic on the tap-you-out row.

## Lane BW — render truth B + Puzzle Box (M3, M11, M15 report, M19, M20, M21, M23)
M3 fixture with Teferi's Puzzle Box on each side; `abilityEmptiesHandOf` from the pending ability's payload; pin. M11 repeatable-row-vs-life-trend line ("3 per turn vs their +3.75 per turn: never closes"). M15 MEASURE per seam what precedes an unlabelled plan line (report only; no protocol change). M19 loop-half salience: header for a completed pair, none for an unclosable one. M20 animatable tail states the cost and "not a creature now". M21 declined-count windows: find the render cause of the 26% mis-answers (deck125 review A-4) and fix it. M23: Silverquill life tax in the P-list; `HOLD PRIORITY:` row head made non-label-shaped (the four comparison sites BR docketed); dungeon room header.

---

## Lane BX — fix lane for the Astra review (wave72/codex-review.md)
Worktree `worktrees/lanes/w72-BX` (branch `w72-lane-BX`, base = master after the review commit; merged step one 32ab3855c, PARSETEST 5457/0). Invariants 000, 3; never remove/auto-answer a legal option. For EACH of the nine findings: reproduce RED on base (PARSETEST case or fixture) or refute with counter-evidence; then fix.
- F1 HIGH (a casting-seam HOLD suppresses the priority seam whose rows — cycling, activations — the held menu never showed): sibling suppression only when the sibling's LEGAL ROW SET is a subset of what the held menu showed (compare option texts, not just the board); otherwise the sibling asks. Flip the pin at ~77340 to carry both option sets.
- F2 HIGH (a stale `mStatedStop` gets dated by an unrelated new plan's `mPlanSetTurn`; no ownership discriminator): the stop carries its OWN set-turn (`mStatedStopTurn`), set only when a stop is parsed from THIS reply's PLAN line; the collapse requires the stop stated this turn; `their stop=…` never populates it. Pins: new plan without a stop → no collapse; opponent-owned stop → no collapse.
- F3 MED (`[castable now]` answered-tag: a second open casting decision in the same phase; the stamp set before validation at ~38348): the tag reads "answered" only when no casting decision is OPEN (the ask being rendered is itself a casting decision → open); stamp after validation.
- F4 HIGH (token copies of REAL card names removed from the naming menu — CR 201.4: a token's name that is also a card name is legal): filter only tokens whose name is NOT a card name in the card database (collection lookup by name), never by `isToken` alone. Pin: a token copy of Grizzly Bears stays; a "Goblin" token goes.
- F5 MED (public copies declared permanently uncastable: graveyard-return, flashback, cast-from-exile, bounce): the tag counts copies "not in a public zone" only; the header must not say "spent"/"cannot be cast again" — say where the public copies ARE; drop the exclusivity claim and its pin at ~77414.
- F6 MED (name-menu heuristic fallback permuted twice): the fallback answer is already in engine index space — do not apply `nameOrder` to it. Pin with a reversed order and a failed endpoint.
- F7 MED (a non-mana land — Dark Depths — counted as a mana source for the reserve fold): count the unused land drop only if the candidate land has a mana ability usable this turn; else no fold. Pin Dark Depths negative, Island positive.
- F8 MED ("every point is unblocked" with an untapped creature-land or instant-speed token maker available): the categorical claim only when no creature-land can animate and no instant-speed creature maker is castable/activatable; otherwise the conditional ("unless you animate X"). Fix the pin at ~77596.
- F9 MED (LOOP COMPLETE with a live lifegain prohibition — Erebos `nolifegainopponent`, or a disabled trigger): the header consults the same "can the loop operate" predicate as the loop-half warning — silent when it cannot.
Also: `own_turn_windows_skipped` (BT) and `sibling_window_asks_skipped` (BU) must count under the new F1 rule. Gates as standing (suite THREADS=1 — the multi-threaded gate flake on `ai_instant_own_end_step_w71bq` is NOT yours; it passes 5/5 solo); commit with explicit paths, no attribution; write wave72/lane-BX.md; touch `~/.gatelogs/W72_BX_DONE`.
