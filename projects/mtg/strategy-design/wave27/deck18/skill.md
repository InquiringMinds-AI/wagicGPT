# deck18 skill (wave-27 FIRST-GUIDED validation) - what the Step-0->guided pipeline teaches

deck18 is the SECOND clean first-guided validation after deck59 (the pipeline's namesake).
It converts three Step-0 skill CONFIRMATIONS/SHARPENINGs from wave-26 into VALIDATED
pipeline evidence: the wave-26 authoring choices produced the intended in-game behavior on
the next corpus, at 4/6 -> 5/6. Bar: proposals ride evidence; PASS is a success verdict.

## PIPELINE EVIDENCE #2 (after deck59): a Step-0-bis initial guide that targets three named
   leaks converts them, measured on the very next corpus.

deck59 established the loop (Step-0 guideless read -> initial guide -> first-guided
validation). deck18 reproduces it independently on a DIFFERENT deck archetype (pure
anthem/lord aggro, no color confound, no fabrication face):
- LEAK->DIRECTIVE conversion worked for all three: the trust-line collapsed the re-derivation
  tail (p95 10,923 -> 7,112; >10k spikes 8 -> 3), the Dodger evasion ATTACK ORDER made it
  swing every turn (the 14k impossible-block spiral vanished), the ground-only RACE off-case
  killed the illegal-flyer-block class at the exact matchup that produced it (vs137 Faerie:
  9.9k illegal-block spiral -> 1.1k "no blockers").
- TEACHING: the Step-0-bis method's per-leak targeting is now twice-validated. An initial
  guide written from a guideless read, with each leak stated as a phase-keyed DIRECTIVE
  (not a capability line), reliably converts. This strengthens the case that first-guided
  seats need only ONE validation corpus before rotation candidacy.

## CONFIRMATION - EVASION-AS-ATTACK-ORDER and the cross-phase restatement rung are VALIDATED.

Wave-26 sharpened rule 5: for a printed evasion keyword, state it TWICE (capability line +
explicit attack order keyed to the board fact) because "a deck FACT stated for one phase
does not reach the phase where it decides." deck18 wave-27 is the live proof: the model that
knew Dodger's evasion at mulligan but forgot it at combat (Step-0) now applies it at the
attack seam every turn. The dual-statement authoring rule is CONFIRMED effective, not just
plausible. Keep it as the standard treatment for can't-be-blocked-by / fear / flying /
unblockable across all decks.

## SHARPENING PROMOTED-TO-CONFIRMED - the "no flyers/reach -> can't block flyers, RACE"
   first-pass line is now LIVE-VALIDATED for ground-only decks.

Wave-26 flagged this as a first-pass SHARPENING to "promote to a general ground-only-deck
first-pass line if a second no-evasion aggro deck reproduces the illegal-flyer-block tax."
Update: rather than needing a second deck to reproduce the DEFECT, deck18 proved the FIX -
the same deck, same matchup, same flyer, with the line in the guide, produced the correct
race behavior and zero illegal-block tax. This is stronger evidence than a second reproduction
would have been: it shows the guide-lane line is SUFFICIENT for a ground-only deck. Bank it
as the standard first-pass line for any deck the read shows has no flying/reach in a pool
with flyers; a core-prompt line is NOT needed while the guide-lane handles it (see
general-suggestions candidate A downgrade).

## PASS - density, mulligan rung, sixth-face template unchanged.

- Density: the wave-26 guide (85 lines / ~5.9 KB, mid-band) validated with NO trim needed -
  all three leak sections earned their place (each measurably converted). This is a data
  point that a first-guided guide should NOT be trimmed on the validation corpus if all
  targeted leaks convert; trim only a leak the NEXT corpus proves dead. No leak proved dead.
- Mulligan (FIDELITY-vs-JUDGMENT): all 6 keeps correct again; surface remains effectively
  UNEXERCISED (mono-white trivial keeps). Carry as unexercised.
- Sixth face (symmetric-effect-asymmetric recursion): N/A (no sweepers/persist). Correctly
  not applied.

## NOTE for the synthesis agent - a new residual tax class surfaced, below the skill bar.

Whole-turn planning tax on trivial single-option decisions (vs137 seq12 "Play Plains" 7,112
chars; vs136 seq17 5,844). This is NOT the trust-the-rendered-number class (which the guide
fixed) - it is the model planning an entire turn's sequencing when the prompt asks a
single-option land drop. If this recurs across seats it may want a skill-level "answer the
decision in front of you; do not pre-plan the whole turn on a forced/trivial choice" rung.
Single-seat now; logged as L-18-1, tracking only.
