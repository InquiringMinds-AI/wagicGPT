# deck134 -> general-strategy (core prompt) proposals (wave 27): PASS

No core-prompt change proposed. Route to PASS (would be the 22nd consecutive core PASS if the
synthesis reviewers concur).

Rationale: every deck134 leak is DECK-SPECIFIC engine knowledge the per-deck guide carries, not a
deck-agnostic defect:
- The Map-crack pass leak is covered IN PRINCIPLE by two existing core lines ("sacrifice a
  fetchland the first time its activation is offered" + "activate your permanents' abilities").
  The failure was that the executor did not map an artifact-that-sacs-for-a-land onto "fetchland"
  - this is a per-deck naming gap (Proposal 1 in skill.md), NOT a missing core doctrine. Burden of
  proof is on core (every deck pays for each line); a broader core rewording ("any sac-for-value
  activation" ) risks over-firing on decks whose sac engines SHOULD be held. Keep it per-deck until
  a second deck shows the same artifact-fetch pass leak deck-agnostically.
- Counterspell timing, act-floor, role check, combat/lethal math, evoke-vs-body value, blink
  targeting - all either already in core (the first four, and they WORKED here: counters were
  sound) or too archetype-specific for core (blink/evoke).
- The reasoning-tax / latency spikes are a decode/serving property, not a prompt-doctrine gap; the
  guide mitigates by pre-answering the recurring derivations. No core text fixes latency.

Nothing here showed up deck-agnostic or is mechanically guaranteed to. Core PASS.
