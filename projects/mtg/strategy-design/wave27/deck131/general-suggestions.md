# deck131 wave-27 -- general-suggestions.md

PASS.

No core / general-prompt (shared decision-prompt) change PROPOSED from this seat. Every finding routes
cleanly by layer without touching the shared prompt:
- The favorable-block edit (wave-26 per-deck GUIDE rule) VALIDATED -- fired correctly when the model engaged;
  the 2 declines are model inconsistency on an already-correct line (WATCH), not a shared-prompt issue.
- The two priority fallbacks (vs134 s39/s40) decompose BENIGN: model plan-fixation ignoring the EXISTING,
  CORRECT shared-prompt re-derive note ("the actions your plan names are no longer among the options
  available right now - re-derive"), plus a cross-zone duplicate-name false-contradiction spiral. The
  shared-prompt instruction is CORRECT; the model ignored it once. Non-decisive (won 32/9 adj). No core
  representation gap -- the option list, board, and stale-plan note were all rendered correctly.
- The Guttersnipe tapped-read guard had NO recurrence (round 5) -- nothing to route.
- The BLOCKS parser correctly recovered a self-corrected block (vs22 s22) -- the shared parser behaved WELL
  (took the model's corrected final line). No core change.
- c1/c2/c3/c5 UNEXERCISED at this counter/bounce/basics-only seat; c4 corpus gate PASSED (no dropped/mis-
  picked LEGITIMATE reply; both fallbacks were model-side unparsed of a nonexistent option).

The general prompt's battlefield, mulligan, bottom, combat, and priority representations are CORRECT and
complete here; the residuals are model comprehension / harness-absorbed fallbacks, not defects the core
prompt could address.

**THREE below-bar candidates flagged for the synthesis agent (NOT proposals; do NOT break this PASS):**

1. **Affirmative untapped-blocker tagging** (carried, evidence did NOT strengthen). Tapped-read misread had
   ZERO recurrence this corpus (round 5). Still single-SEAT, still correct-not-defective representation.
   Surface ONLY on cross-SEAT convergence (a 2nd seat reporting a combat tapped-read misplay). Absent that,
   WATCH.

2. **Cross-zone duplicate-name disambiguation** (NEW below-bar, d1-adjacent). The d1 #N handles fixed
   BATTLEFIELD same-name collisions (validated + used correctly at this seat). But same-name copies across
   HAND / LIBRARY / GRAVEYARD are NOT disambiguated, and the model manufactured a false "log-vs-board
   contradiction" from distinct copies in the s40 spiral (209s). Single incident, non-decisive. A shared-
   representation candidate ONLY if cross-seat convergence shows a cross-zone same-name confusion flipping a
   decisive decision. Absent that, WATCH -- extending #N to non-battlefield zones is a large surface for a
   single non-decisive incident.

3. **Plan-fixation vs the stale-plan re-derive note** (NEW below-bar). At a single-option priority whose sole
   legal action was not the model's planned action, the model twice restated the plan (a nonexistent option)
   despite the shared prompt's explicit re-derive note. The note is CORRECT; the model ignored it. NOT core-
   fixable by adding text (it would be shout-louder at a correct instruction). Surface to synthesis ONLY if
   another reviewer reports plan-fixation-past-the-re-derive-note flipping a DECISIVE decision. Absent that,
   WATCH.

None of the three is a proposal; all are convergence-gated. The seat's answer is PASS.

This is the 22nd consecutive general-prompt PASS from this seat (wave-26 was the 21st).
