# deck137 general-suggestions (wave 27) -- CORE-layer candidates only (bar high: 22nd consecutive PASS)

## CORE PASS -- 22nd consecutive. No new core PROPOSAL this wave.
(Lineage per the wave-26 skill reconciliation: wave-24 = 19th, wave-25 = 20th, wave-26 = 21st,
wave-27 = 22nd. The general/core prompt is unchanged and needs no edit from this seat.)

The wave-27 headline finding (convoke casts do not resolve) is an ENGINE/HARNESS defect, not a core
prompt-surface issue -- routed to notes.md as HIGH / step-1. It does not touch the general prompt.

## Below-bar items updated this wave:

### RESOLVED at this seat -- R-DUPLICATE-NAME-INSTANCE (same-named-permanent disambiguation).
Wave-26 carried this below-bar as a single-witness representation candidate (deck137 vs18 s36). The
d1 fix shipped `#N` instance handles and they WORK: they render on board/A/B/target lines and the
model binds them correctly (vs131 s27 dup-Faerie block, vs22 s29 dup-Giant blocks, vs93 dup-Faerie
attacks). Remove from deck137's accumulating list -- resolved here. The corpus-general core question
(does it hold at a deck running MANY identical names?) now lives with the deck93 reviewer (36 Rats);
that is where a 2nd-deck promotion signal would come from, not here.

### Still below-bar (unchanged) -- "trust the engine-offered legal set both ways, do not re-derive."
Witnessed at deck137 across two seams (attackers wave-25 s21, blockers wave-26 s36) but still ONE
deck. This corpus the deck-guide clause worked (vs131 s27), so no new failure to add. Promotion bar
remains a SECOND DECK exhibiting the "re-derived a condition the engine already computed and overrode
a legal offer" failure. Deck reviewers: flag any such override at your seat.

### New (below-bar, methodology) -- resolution-verification for deferred-action classes.
See skill.md PROPOSAL 1. This is a REVIEW-METHOD rung (verify the deferred ACTION resolved, not the
adjacent decisions) rather than a core prompt change. Not a general-prompt proposal; recorded here so
core reviewers apply the check to any deferred class they audit. If a second deferred class at another
deck is found silently failing under a "harmless" label, that corroborates the method's value but
still would not touch the general prompt.

## Standing core items unchanged at this seat:
- c4 parser hardening: PASS -- no reversal drop; two illegal-block-shape replies recovered to the
  first legal B:A pair (vs18 s58, vs22 s39). Clean corpus toward c4 firing-where-it-should confidence.
- c2 ETB pay-or-tap annotations: self-describing at every shockland menu, all parsed. No core action.
