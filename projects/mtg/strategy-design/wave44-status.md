# Wave-44 status (step-1 near-complete; PAUSED at owner wrap-up 2026-08-24; resume Thursday)

## MERGED on master (chain through 663a16961, pushed):
- Lane A: FAIR HANDS BY DEFAULT (owner ruling verbatim in Rules.cpp; =0 legacy-forensics
  escape; FAIRAI follows; harness banner). First corpus after this = NEW BASELINE
  (not hand-comparable with waves <=43; mulligans exist in translogs for the first time).
- Lane B: X-pricing per-side headline (opponent-first), removal {target text:},
  Day/Night CURRENT SITUATION line.
- Lane C: teferieffect/w42CounterLabel fix, dungeon zone-tag suppression, reveal
  false-frame fix (previousZone attribution), plan-note gate RESURRECTED (dead since
  wave-35: 34% fire rate all-false on attackers; now option-rows-only + "resolve" verb
  + narrow-window shapes), menace DEFENDER probe DISCHARGED (3 green pins + 1 RED
  unregistered: Baka never gang-blocks menace — #W45 seed), loyalty lost-(now N),
  lifelink/DS clauses in trade verdicts.

## IN FLIGHT: the lethal-basis fix lane (owner-authorized "fine. fix it."):
one-token GuiCombat autoaffectDamage toughness->life; two pre-registered assert flips
(20->18, 17->15); three new pins (shock_then_trample, DS+protection composite,
fs_kills_then_all_face — last one previously unverified, red = separate defect);
stop-and-revert on any other suite movement; forecast-drift check.

## NEXT (the loop, when owner returns / usage resets THURSDAY):
1. Merge the lethal lane (FROM THE MAIN TREE — the worktree-merge trap hit 5x this
   session; cd /home/magi/Projects/wagicGPT FIRST, always).
2. Rebuild + merged gate (expected: PARSETEST 1080+23(B)+45(C)+lethal-lane's = watch
   exact sums; suite ~1129/0+37/0-ish — count is registry-dependent, 0-failed + exact
   fixture deltas is the invariant; 'W43-1 ASSERT' must be 0).
3. Push; archive binary to ~/wagic-archives/.
4. LAUNCH THE FIRST FAIR-HAND CORPUS (pool unchanged 146,152,125,126,162,123,130;
   canonical recipe; done-files on ~/.gatelogs NOT /tmp). Review carries: new-baseline
   discipline (do NOT attribute hand-variance to edits), first-ever mulligan evidence
   (guides' floors face reality; if mull-to-zero appears WITH guides, the seam-floor
   question goes to the owner), wave-43 guide-edit predictions (seats files), wave-44
   lane predictions (lane reports + wave43/engine-ledger.md).
5. Wave-45 seeds so far: Baka menace gang-block (red pin exists); #W44-9 mana-window
   auto-pass economics; counter-preview false affordance (needs repro locus); CS-024a;
   manarestriction sweep; observers' remaining LOW items.

## OWNER-DECISION LOG (this session): fair hands DEFAULT (done); lethal fix authorized
(in flight); {room effect:} = deliberate keep (closed); construction OUT OF SCOPE;
VPKs owner-initiated only. QUEUED FOR NEXT REQUESTED VPK: menace legality, damage-order
window, lethal-basis fix, fair hands, all wave-43/44 render+engine work (everything
since 9f47ce024).
