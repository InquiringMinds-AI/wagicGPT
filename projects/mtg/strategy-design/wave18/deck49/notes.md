# notes.md — deck49 (Dragons) engine/representation ledger + rotation verdict (wave 18)

## Engine / representation ledger items (status after this corpus)

### E-49a — "(player, life N)" suffix-strip in the echo matcher — CLOSED (validated).
Shipped in the wave-17 batch (item e). This corpus: 17/17 burn/player target menus resolved to the
correct player target with ZERO wrongful downgrades (was the deck21 s4 wrongful stale_echo last
wave). The player-target label no longer defeats the matcher. Close.

### E-49b — target sub-menu role not recognized — CLOSED (validated by TARGET CHOICE framing).
The wave-17 TARGET CHOICE framing (item a: names the pending effect, "not a cast or phase step")
resolved this. This corpus: 0 stale_echoes (was 7, ALL at this seam), model narrates the menu role
explicitly ("TARGET CHOICE for Lava Spike... I need to target," vs110 s11). The representation fix
did the work the guide's Rule #2 prose was reaching for → Rule #2 mechanics trimmed this wave
(representation-beats-instruction). Close.

### E-49c — beneficial-aura enemy-only suppression — WORKED this corpus, but being REPLACED (owner ruling).
Enemy-only Goblin War Paint was never offered all corpus, so the fatal wave-17 C4 loops had zero
recurrence (masked, not proven-solved). Per the OWNER RULING the suppression is replaced next corpus
with a WARNING ANNOTATION — so enemy-only War Paint WILL reappear. Two actions carried forward:
  1. KEEP the guide's never-aura-opponent rule (it pairs with the coming annotation; brief-mandated).
  2. The annotation should mirror the STYLE the engine already ships on the own-target side (vs35 s9:
     "the only legal targets are YOUR OWN right now") — see general-suggestions.md G-49b for the
     proposed enemy-only wording. Re-validate C4 next wave when the mask lifts.
This corpus is a HIGH-ledger evidence point FOR the annotation redesign (suppression prevented the
loop AND removed a legal play — both halves of the ruling's rationale confirmed at one seat).

### Card-script divergences (Oracle-verification queue) — UNCHANGED, still queued.
No card was mis-executed in a game-losing way this corpus. The wave-17 list stands (Bloodmark Mentor
engine 1/1 vs real 2/1; War Paint engine {1}{R}/+2+2 vs real {R}/+1+1; Crucible engine {3}{R} vs real
{2}{R}{R}; Rorix toughness unconfirmed — this corpus renders it 6/5, likely fine; Ashenmoor Liege
engine punisher-on-target vs real self-harm trigger). The guide describes ENGINE values throughout.
Queue, do not rush.

## Rotation verdict

**NOT a rotation candidate — the validation produced a warranted REVISION (new-work signal).**
Per Step 0-ter, a validated guide that produces a revision carries a new-work signal by construction.
This wave's revision:
  - COMPRESSED Rule #1 (mana-is-trivial) — the C1 class is DEAD (0 fabricated locks), so the verbose
    explanation was flattened to the load-bearing sentence (rule 5: trim validated-dead).
  - TRIMMED Rule #2's target-menu MECHANICS — redundant with the engine TARGET CHOICE framing that
    now carries the representation; reduced to a two-line pointer that keeps "the opponent's face is a
    valid target." (representation-beats-instruction; apportionment discipline in skill.md.)
  - ADDED a flood/no-curve OFF-CASE line ("WHEN YOUR CURVE FAILS YOU, THE BURN IS YOUR DECK") — the
    losing corpus lives in the off-case (rule 6); addresses the two grind losses (vs62, vs14) and the
    two mild burn-holds (vs14 s10, vs35 s10 held a face-able Lightning Bolt for "cast nothing").
  - KEPT unchanged: BURN-GOES-FACE (100% obeyed — trimming would risk the C3 regression), Crucible-
    needs-dragons (guide-cited decline), never-aura-opponent (pairs with the coming E-49c annotation),
    deploy-and-attack, mulligan.

**Re-validate next corpus**, focused on: (1) the trimmed Rule #2 — confirm stale_echoes stay at 0
without the mechanics prose; (2) the flood off-case — confirm the mild burn-hold dies; (3) C4 —
enemy-only War Paint reappearing under the annotation redesign (the masked class). The record is
expected to keep moving on the pool, not the guide; judge by taught-class survival and tax, per rule 1.
