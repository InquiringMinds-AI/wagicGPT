# deck14 (Deep Blue) — general-suggestions.md — core-prompt proposals (layer-routed)

## Verdict: CORE PASS from this seat. No new core-body line proposed.

Single-seat doctrine forbids adding/cutting a core line from one seat. Every deck14 finding routes
BELOW the core (guide) or to the engine/representation ledger. Detail:

1. **Combat FORMAT DRIFT (vs62 s34/s37, vs135 s26) — NOT core.** The reply protocol is code-appended
   after the template and already states the `BLOCKS: B#:A#` / `ATTACK: A#` format with examples.
   The failure is the model ignoring it under a 10k-char reasoning ramble, not a missing instruction.
   Durable fix = engine parser accepts card-name assignments (notes.md E1) + guide's short-answer
   discipline line. A third format restatement in core is the sanctioned-against escalation.

2. **Durdle-into-defense (vs35, 0 attackers) — GUIDE, not core.** This is a deck-specific mislabel
   (tempo deck that looks like control). The core's general "be aggressive" cannot carry a per-deck
   countermand; that is exactly what the initial guide is for (rule #1).

3. **Invented option number (vs110 s8) — engine/representation, not core.** Core already carries the
   option-list-authority meta-rule verbatim (per wave-17 synthesis, core line 36). This is a
   present-and-disobeyed distrust-the-list instance in the OVER-reach direction; a fresh restatement
   is the representation-beats-instruction wall. Route to the ledger, keep core frozen.

4. **stale_echo (vs21 s24) — KNOWN-OPEN, no action.** TARGET-CHOICE framing validated corpus-wide;
   this seat's single stale echo is a correct catch of a moved-on bounce target. Note only.

## OWNER-RULING check (effectBadOrGood suppression hiding a correct play)
No spot at this seat where a legal play was HIDDEN by the wave-16/17 own-only-BAD or opponent-only-
GOOD suppression such that it changed the outcome. deck14's steal/bounce are opponent-targeted and
beneficial-to-self; they were offered and used normally. Nothing to flag for the annotation redesign.
