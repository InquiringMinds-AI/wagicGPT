# general-prompt (core) suggestions from deck102 - wave 20

Layer-routing enforced; single-seat items CANNOT add/cut core lines (binding doctrine). This seat produced
NO core add. Rationale per candidate:

## PASS 1 - symmetric-effect self-harm -> GUIDE, not core.
The Pox/Smallpox-while-behind class (Headline 2) is deck-specific (WHICH cards are symmetric is a per-deck
fact) and the core ALREADY carries the general principle in the cost-weighing clause ("weigh each action by
what it COSTS you against what it gains ... paying life ... to advance your plan is that card doing its job
... Decline only when what you give up is clearly worth more than what you gain"). The gap is instantiation,
not a missing principle - the pilot did not know these specific cards are payoff-cards without the payoff.
Correct route is the deck guide's SYMMETRIC-TRAP fire-gate (shipped in strategy.txt). No core change.

## PASS 2 - the discard-pick reveal misrepresentation -> ENGINE/representation ledger (notes.md), not core.
The "top N cards of your library" mislabel and subset-framing on a pick-ONE effect is a HARNESS/prompt
defect upstream of any strategy text - routed to notes.md E1. A core-prompt line cannot fix a mislabeled
menu; loosening reasoning around it would only paper the defect. Not a core item.

## PASS 3 - the losing-position spiral -> GUIDE off-case, do NOT double-layer to core.
The wave-18 rung is explicit: the GUIDE off-case behavior branch is the shipped lever, and its core cousin
(a general "the outcome is forced, state the choice and pass" brake) is a single-seat-each CORE WATCH -
"measure whether the guide branch collapses the spiral before adding any core line - do not double-layer."
deck102 is one more seat of the SAME spiral; per that rung it reinforces the GUIDE off-case (shipped) and
does NOT justify a core line. Recorded as continued support for the existing core WATCH only.

## Restatement-ban compliance
The deck102 guide deliberately does NOT restate: the mana/interface contract, the general combat
arithmetic, ON THE STACK, target-by-impact, or the reply-format protocol - all core-carried. Its lines are
the deck-specific instantiations the core cannot carry (the capability table, the Tergrid engine, the
symmetric-trap fire-gate, the named finishers, the Thoughtseize pick-ONE surface, reanimation lines). One
interim/deck-fact line flagged for eventual promotion: the SYMMETRIC-COST fire-gate - if a second symmetric
deck appears, consider a single core sentence ("a spell whose cost line shows YOU losing life or sacrificing
is symmetric - do not fire it while behind unless you have the payoff") so it need not be re-authored per
deck.
