# deck14 (Deep Blue) — general-suggestions.md — core-prompt proposals (layer-routed)

## Verdict: CORE PASS from this seat. No new core-body line proposed.

Single-seat doctrine forbids adding/cutting a core line from one seat, and everything deck14 found this
wave routes BELOW core (per-deck guide diff) or to the engine/representation ledger. Detail:

1. **Bounce-on-the-stack misconception (vs62 s18 misplay + vs35 s27 / vs27 s11 / vs102 s9 fallbacks) —
   ENGINE/REPRESENTATION + GUIDE, not core.** Core already carries the option-list-authority meta-rule and
   the general "trust the offered list." The residual is a DECK-SPECIFIC rules gap (bounce cannot touch a
   spell on the stack) on a bounce-heavy deck (4 Boomerang + 4 Unsummon) — exactly what the per-deck guide
   is for (handled as a constrained diff in strategy.txt). The DURABLE fix is representation/engine (notes
   E1: stop offering the bounce as a "respond now" answer to a stack spell, and/or a menu header that the
   list is battlefield-permanents-only). Present-and-disobeyed => a core restatement hits the
   representation-beats-instruction wall. Core frozen.

2. **Combat token-cap truncation (vs62 s28, vs27 s47, vs27 s42) — DECODE/HARNESS, not core.** The reply
   protocol in core already states the coded short-answer format; the failure is the reply exhausting the
   token cap before emitting ANY line, which no prose restatement in core can fix. Route to decode-side
   (notes E2). No core action.

3. **Invented board object (vs27 s47 phantom Vodalian Zombie 7/7) — MODEL/DECODE, not core.** Occurs inside
   a truncated hopeless-board ramble; outcome-neutral. Not a prompt-authoring matter. Watch (notes).

## OWNER-RULING check (effectBadOrGood suppression -> annotation replacement) — ONE data point this wave
The warning annotation ("the only legal targets are YOUR OWN right now (warning: this would harm your own
side)") RENDERED at this seat for the first time (vs62 s18) — wave-19 it was absent, so this is a positive
render validation. It was DISOBEYED: the model cast the self-harming Unsummon anyway (bounced its own Lord
of Atlantis) because the on-stack false belief overrode the warning. Per the skill's annotation-scoring
rung: the annotation is CORRECT and OBEYED-in-nothing (the leak is a rules MISCONCEPTION reasoning straight
past it), so the residual routes to REPRESENTATION/MODEL (name WHY the on-stack spell is not a legal target
— stack-vs-battlefield), NOT to a louder or different annotation, and NOT to core. The annotation itself is
accurate; do not change it. Flagged for the annotation-redesign owner as a "correct-but-reasoned-past"
instance, matching the corpus-wide pattern (deck133 Fatal-Push-on-stack lineage).

## Brief focus items — all resolved to guide/engine layer, none to core
Reveal partition, attacker-sparing, and discard-pick surfaces are owned by other seats. deck14's own
perception-bundle validations (options_text fixed; salvage inapplicable to truncated-before-line; #N not
observed) are engine/observability items (notes), with no core implication.
