# deck27 skill.md — proposals for the strategy-writing skill (wave-20)

Seat: Zombies (UB tribal), FIRST GUIDED corpus (Step 0-ter validation). One reviewer = single-seat
by default; each item tagged with promotion status. Layer-routing enforced: single-seat items
CANNOT add/cut a skill core line.

## Step 0-ter confirmations (this first-guided seat re-fired existing rungs — no new headline)

- **Tax-collapse-is-the-primary-signal (rule 1) CONFIRMED as a discriminator, not just a caveat.**
  This seat's record rose 2/6->3/6 while the tax stayed FLAT (2.1%->2.2% fallback, 6->6 giants).
  Reading the record first would have mis-scored the guide as "working"; the flat tax + the
  taught-class ledger correctly says the win-path fired but the off-case/obedience tax did not
  move. Clean worked example that the tax gauge and the record can DISAGREE, and the tax gauge is
  the load-bearing read. No skill change — reinforces the existing rung.

- **"Died but tax-signature persists = the tax RELOCATED" (rule 4) CONFIRMED, combat-hallucination
  variant.** The wave-19 imagined-attacker class (deck14 s9, block a nonexistent A2) DIED — zero
  recurrence. But the giant-reply count did not drop; the tax relocated to (a) an ahead-position
  decode DEGENERATION (vs49 s16, a repetition loop, not over-reasoning) and (b) a
  representation-defect-induced spiral (vs62 s6, below). Same lesson: re-classify survivors by
  MECHANISM, do not read a stable giant count as "the fix failed." No skill change.

- **"Write the off-case even if the winning corpus never needed it" (rule 6) CONFIRMED, and it is
  where THIS guide's one revision lives.** All three wins ran the evasion win-path and never
  engaged the long grind. The single loss that was guide-addressable (vs14, durdle to the turn cap)
  lived entirely in an off-case the first guide's anti-durdle prose did not cover: it scoped the
  durdle to EARLY piecemeal deployment, not to holding buffed attackers in a LATE ground stall.
  The surgical revision extends the existing rule to the late-stall face. No skill change — a
  textbook application of the rung.

## WATCHES (single-seat and/or not-yet-promotable)

- **WATCH (2nd instance, SAME opponent -> does NOT promote): the FREE-BLOCK DECLINE class.**
  wave-19 flagged this pending a second CLEAN seat. wave-20 re-fired it — but on the SAME opponent
  (deck49) and the SAME card (Bloodmark Mentor, their first-strike lord): vs49 s5 declined
  `(you kill it, your blocker lives)` on the enemy anthem. Because the promotion bar is a DIFFERENT
  deck, this stays a WATCH. Crucial observation for the skill's model-vs-representation doctrine:
  the guide rule that names this EXACT case ("especially against an opponent LORD or anthem-giver
  (e.g. first strike)") is present, maximally explicit, and was disobeyed anyway — while the SAME
  model TOOK the equivalent block three plies later (s9) and correctly declined a chump-with-lord
  (vs102 s21). This is strong evidence for the existing skill principle that a correct-and-explicit
  representation the model ignores is a MODEL-OBEDIENCE gap that MORE PROSE cannot close — do not
  let a reviewer "fix" it by re-wording an already-correct rule. Promote to a skill rung only on a
  DIFFERENT deck where an explicit favorable-block rule is disobeyed.

- **WATCH (2nd instance): the AHEAD-position giant is a DECODE degeneration, not over-reasoning.**
  wave-19 deck110 s20 and wave-20 deck27 vs49 s16 are both ahead-position giants, but s16's tail is
  a literal repetition loop ("No, it's a Spirit?" xN), not a value-hunt. Candidate skill note: when
  an ahead-giant's tail is DEGENERATE REPETITION rather than reasoning, route it to the model/decode
  ledger (sampling/repetition-penalty), NOT to a guide "keep it simple" line — the guide line cannot
  prevent a decode loop, and stacking more guide prose is wasted bytes. Hold pending a third instance
  before proposing a rung; recorded here so the synthesis agent sees the 2-seat pattern.

## No new core-prompt candidate from this seat
Every item is single-seat and/or owns a layer below core (guide off-case, engine annotation, model
decode). See general-suggestions.md for the layer routing.
