# deck27 general-suggestions.md — core-prompt proposals (wave-20)

Layer-routing enforced: single-seat items CANNOT add or cut a core line; engine/representation
items go to the notes.md ledger, never into core prompt text. This seat proposes **NO core-prompt
change.** Rationale and routing below.

## Core prompt: PASS (no change proposed)

Nothing this seat found is both (a) 2+-seat convergent AND (b) properly core (not a guide off-case,
engine annotation, or model-decode item). Detail:

1. **Free-block decline (vs49 s5)** — 2nd instance but SAME opponent as wave-19; does not promote.
   And it is NOT a core-prose candidate regardless: the representation already surfaced the
   favorable trade correctly and the model disobeyed a maximally-explicit guide rule. A core line
   "take favorable blocks" would teach against a signal the model already has and ignores. Stays in
   the GUIDE (unchanged — already explicit) + flagged as a MODEL-OBEDIENCE watch (skill.md/notes.md).

2. **Ahead-position decode degeneration (vs49 s16)** — a repetition-loop decode failure, not a
   strategy-judgment issue, so it does not belong in core strategy prose. Routed to the MODEL/DECODE
   ledger (notes.md). The guide's "keep it simple when ahead" line is retained but is not expected
   to prevent a decode loop.

3. **Behind losing-spiral (vs62 s18) and the durdle-to-cap (vs14)** — both already own lower layers:
   the behind-spiral is handled at the GUIDE off-case layer (wave-18 ruling: do not double-layer into
   core); the durdle late-stall face is handled by the ONE surgical GUIDE revision this wave. Neither
   is a core candidate.

## Confirmations for the synthesis agent (no text change)

- **Trust-the-rendered-number** held again at this seat with ZERO mana fallbacks on a two-color
  hybrid base — a supporting data point (Nth) that mana false-belief risk scales with color
  COMMITMENT, not two-color complexity. Reinforces the existing rule; no new prose.
- **The evasion-first / lords-are-the-engine win-path is deck-specific**, not portable core prose —
  it belongs entirely in the per-deck guide (this deck's win path fired in all 3 wins). Correctly
  kept out of core.
- **Recursion-allocation (recur BEST not a 1/1) is deck-specific** and validated DEAD-in-a-good-way
  this wave (the model never returned the 1/1). Guide-layer, not core.

## Engine/representation items produced (routed to notes.md ledger, NOT core)

- **Wither trade-annotation defect** (headline): a 2-wither blocker (Oona's Gatewarden) blocking an
  attacker with toughness > 2 is annotated "(both die)" but the attacker SURVIVES shrunk. Seqs vs135
  s13, vs102 s18, vs62 s6 — the vs62 s6 case directly CAUSED a fallback. VERIFY-then-fix.
- (Reinforcement) options_text present in every combat record; #N duplicate-name disambiguation
  fired correctly (vs49 s19). No ledger defect there — recorded as positive validation.
