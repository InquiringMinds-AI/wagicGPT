# deck136 (Yarok of Pranks) -> general-suggestions.md (core-prompt proposals)
# Wave-29. Layer: core = the shared symmetric system prompt / prompt architecture.

## PASS (#24 consecutive)
No core-prompt defect observed at the deck136 seat this wave. Specifics:
- PARSER held on every well-formed reply. The 2 fallbacks are NOT parser/architecture
  faults: (1) seq17 vs93 = a stale-answer retracted_choice (the async self-healing path
  working as designed - the pilot's intended pass was preserved by the heuristic);
  (2) seq39 vs122 = an unparsed_reply where the MODEL produced a Rankle-mode answer to a
  priority window (a content/context bleed from the preceding sequential modal, routed to
  notes.md as a light watch). Neither indicts the core prompt or the reply protocol.
- The deciding-fact-rides-the-option architecture worked: option lines carried rules text
  and the pilot reasoned off them correctly (Ritual "destroys MV3 or less", Legion's End
  "MV<=2") - the MV3 valuation win (findings Job 1) is partly the guide and partly the
  option-borne card text doing its job.
- The one transient mana-miscount (seq16) self-corrected within the same reply and is not
  a prompt-representation defect (the Mana Available line was correct; the model briefly
  mis-added black pips, then re-read and fixed itself).
- No representation gap surfaced that a core change would address; the actionable items
  are one engine seam bug (N-136a) and one guide clarification, both layer-routed away
  from core.
NO core change proposed.
