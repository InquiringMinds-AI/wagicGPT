# deck122 -> general-strategy (core prompt) suggestions (wave 29)

PASS (24th consecutive expected).

Nothing from the deck122 seat routes to the symmetric core prompt. Every deck122 finding is
either deck-specific (life-as-engine, Felidar 40-line, Serra/Divinity thresholds, Sorin +1
chain -> strategy.txt), a skill-layer routing pattern (alt-win-condition teach, resource
reframe -> skill.md), or an engine/representation item (Felidar alt-win live-probe -> notes.md).

Corpus was clean from the core-prompt's seat: ZERO fallbacks across all 6 deck122 seats, no
parser contradictions, no unparsed/retracted/deferred records, blocker/attacker bundles
parsed correctly. The core prompt's protocol, narration, and choice-formatting handled a
lifegain deck, a planeswalker, may-triggers, forced-exile, and conditional-P/T renders
without a single seam failure. No core change indicated.

Layer-routing note for synthesis: the two most important deck122 findings (life-threshold
play, alt-win pursuit) are STRATEGY teaches that belong in the deck guide, NOT the core
prompt -- do not generalize "value your life / pursue alt-wins" into the symmetric core
(it would be noise for the aggro/control decks that do not care).
