# deck148 -> general (core) prompt suggestions (wave 29)

## CORE PROMPT: PASS (24th consecutive expected).

This seat routes the core prompt to PASS. Nothing in the deck148 corpus is a deck-agnostic core
defect, and the burden of proof is on core (every deck pays for each line). Reasoning:

- The equipment surface, mulligan behavior, and general option comprehension all worked off the
  core prompt with no guide - the core handled a brand-new archetype (equipment aggro) competently
  on its first exposure. Re-teaching any of it would be pure tax.
- The two real leaks are DECK-SPECIFIC and route PER-DECK (guide), not core:
  1. Attack-floor passivity is posture-dependent - promoting "attack every turn" to core would
     misplay every control deck (the standing per-deck routing rule). Stays in the guide.
  2. The bare-Armament-Master-is-a-blank fact is a single card's engine idiosyncrasy - per-deck.

## The ONE cross-seat item that is NOT core-promotable yet (routes to SKILL/CORE ledger, not a core
edit this wave): L-18-1 planning-tax / N-18e.

My f6 record (vs139 seq31) is the second live witness of the losing-position over-computation spiral
(first was deck18 vs93 seq20, wave-28): the model recomputed identical lethal math ~6 times in a
PROVABLY LOST position and truncated at the token ceiling. The wave-28 note already stages the right
home for this: a SKILL/CORE rung "answer the decision in front of you; when the position is lost or
you are far ahead on a low-stakes/forced choice, commit briefly - do not compute a multi-turn or
repeated lethal plan." This wave ADDS a data point (a THIRD archetype, a genuinely lost blockers
seam) but is still the same single family - do NOT mint a core sentence off one more witness. The
N-18e HARNESS safety that catches the truncation ALREADY WORKS (my f6 validated it - safe phase
default "no blockers" taken correctly). So the residual is purely the MODEL over-computation, whose
eventual home is a core/skill line once it has cross-family agreement. Flag for promotion WATCH;
core stays PASS.
