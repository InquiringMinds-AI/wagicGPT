# deck137 -> general-strategy (core prompt) proposals -- wave 29

## PASS. No core-prompt change proposed. (24th consecutive core PASS from this seat.)

Nothing this corpus routes to the SHARED core prompt. Rationale by layer:

- The open defect (offer-then-can't-pay convoke: March X-spell DEFERS, Loxodon fixed-cost SILENT
  no-ops) is ENGINE, not prompt -- routed to notes.md (N-137-MARCH-XCONVOKE updated to PARTIALLY-
  CLOSED; N-137-LOXCONVOKE-SILENT-NOOP recurred + unified under the same root). A core-prompt line
  cannot make an unpayable convoke payment complete.
- The behavior lever this wave (an X>=2-3 affordability threshold for casting March -- X=0 makes
  0 Soldiers per the card's own `auto=this(X=0) donothing`) is DECK-SPECIFIC to March, the only
  X-convoke spell in the pool. It lives in the deck137 guide, not the core prompt. Generalizing it
  would add noise for every non-convoke, non-X seat.
- Comprehension surfaces the core prompt owns (instance handles #N, land-identity tags, adventure
  bodies, shockland ETB menus, blockers legality, the X-announce step) all rendered clean and were
  bound correctly this corpus -- 0 unparsed, 0 empty, 0 stale_echo, 0 retracted, 0 illegal
  gang-piles across all 6 logs. The X-announce step itself (new via f1) parsed cleanly when it
  fired (vs139 `X = 1`). Nothing to add or fix.

The core prompt is doing its job at this seat: every decision surface the model needed was present,
legal, and correctly disambiguated -- including the NEW X-announce seam. The residual failures are
one (partially-fixed) engine defect and zero prompt defects. Core: PASS.
