# Wave-79 step-3 brief (lean; REJECT is the expected verdict for anything not paid for by a decision the corpus shows going wrong. Amendment 332 stands: guides are decision-time instruction only, hard ceiling 20,000 bytes, no citations/history in a guide — evidence goes in YOUR review.md. Amendments 333 (heading = scope declaration) and 334 (no concessive clause for a rule's hard case) are now FOLDED into wave68/strategy-writing-skill-v2.md §4.1 — the current edition.)

OWNER RULINGS 2026-09-12 (verbatim, all in force): (1) "no such rule exists. Explicitly the rule is to 'make the parser robust'. And that 'any plan, must precede action.'" — the reply parser reads the answer wherever it unambiguously is (lane DD, merged AFTER this corpus at 5bc856d84 — this corpus ran WITHOUT it); the protocol TEXT is unchanged; (2) pilot config changes: "No."; (3) lategame specimens: "Stop producing these." — write none; (4) the strategy-writing skill is loop-managed.

Tenth reasoning-on corpus (invariant 000). Wave 79 step one (wave78/known-bugs.md §T → wave79/lane-{CZ,DA,DB}.md + wave79/codex-review.md → lane-DC.md, eleven findings all CONFIRMED and fixed): CZ window/hold — T2 the stop-reached collapse identity = action-key set + stop NUMBER, the wave-72 `planTurn == nowTurn` clause dropped (`stop_reached_reputs_collapsed`); T3 the CG shape: every numbered row reduces to its ACTION key in the ask/async key tail, and the key's board half is the seam scope + (DC F1) a LEGAL-CONTINUATION DIGEST — live `canTarget` target identities per row + untapped-source count — so life ticks, log lines and `{…}` groups no longer mint a new question but a new target / grown X / changed cost does (`ask_key_continuation_differs`); the hold latch re-opens only on ESCALATION (`hold_verdict_safer_ignored`) and (DC F2) on a NEW threat identity carried in the THREATENED marker (`hold_reopened_new_threat`); T4 the `[own loop verdict:]` line is RENDERED (`own_loop_verdict_lines_rendered`; DC F10 counted at the send, suppressed windows clear the face; `own_loop_verdict` on the record); T12 only ` #1` strips and (DC F9) rank shifts are not identity; T13 the drain promise scoped to THIS stack; T14 stderr names wall-miss/force-close/phase-2 figures, `declined_face_latches` retired; DA combat truth — T5 an exact DP `w79BestBlockDamage` (trample spill, menace, and (DC F3) BOTH damage steps for first/double strike; two suite fixtures prove it in the engine's damage code; past 12×12 or with an out-of-model ability the optimality claim AND the death badge are dropped), GANG BLOCK states its spill, no header prints two disagreeing verdicts; T6 the cover subtracts from the header's own total; T7 `UNLESS <source> untaps them` (DC F8: seat-owned or both-sides LIVE triggers only); T9 sacrifice rows price the spent blocker (DC F4: no double count) and ATTACK TOTAL folds the punisher it names (DC F5: "N power survives the field", no life projection); T16 `ON THE STACK` includes queued punisher damage, `[untapped]` face; DB render/bytes/tools — T8 `HELD: …castable the moment a legal target appears` on instants with no target + keep-X prices the cheapest COUNTERSPELL (DC F6: a spell whose own cast effect counters); T10 hoist by BODY, narration trim 24/20 KB → 16/12 KB; T11 gloss clause priority (triggered/activated before keywords; DC F7: dependent clauses kept together) and the fallback cut closes `(`/`"`; T15 `+0` face; T17 corpus-stats reconciles (engine-answered off the round-trip sum), TURN SOURCES line, `prompt-byte-census.py`. Records carry `thinking`, `reasoning_chars`, `off_protocol_bytes`, `plan_line_missing`, `hold_check_ref_seq`/`_window`, `protocol_deviation`, `own_loop_verdict`, `forced_close`/`recovery` kinds; gameend carries the counters below. Wave-78 guide edits for all seven decks are the LIVE guides (installed 2026-09-11 at a37b2d5ce; each deck's before->after list is in wave78/deck<N>/review.md).

Repo /home/magi/Projects/wagicGPT, game dir projects/mtg. Use python for translogs; use /usr/bin/grep (bare grep drops matches). DO NOT edit anything under bin/Res, src/, or run git or wagic; write only under projects/mtg/strategy-design/wave79/. Never write mechanisms as the owner's positions. Report what you did NOT check.

CORPUS (wave 79, SINGLE binary archives/wagic-5af6f7a95-w79step1 = source b1e1db7be; `--thinking on`, WAGIC_GPT_TIMEOUT=900, no operator token ceiling). No incident: one run, 21 concurrent, all 21 games finished naturally in 5 h 16 min; the pilot never stalled. Corpus under review: /home/magi/.Wagic/ai/gpt/selfplay-runs/matchups-20260912-000748-final (42 seat logs all with gameend, 0 timeout-tainted, results.tsv, askreplay/ 29 files; census = `python3 projects/mtg/tools/corpus-stats.py <dir>`, output also at ~/.gatelogs/w79-stats.out — CENSUS CHECK RECONCILES). 21 games, 0 HUNG/crashed; record-kind sum 2,230 = ask 1,696 + priority 360 + attackers 84 + discard 26 + forced_close 25 + blockers 16 + reveal 12 + bottom 10 + recovery 1; `protocol_replies` 2,203 (wave 78: 2,629; 1.86 decisions per results.tsv turn), 100% with native reasoning (median 5,835 chars, max 21,069), **0 heuristic fallbacks on the ask seam** (wave 78: 7; the only fallback is 1 `reveal`/`engine_answered`); 0 reply_truncated, 0 action_before_plan; CORPUS-WIDE off_protocol_bytes>0 87 (3.9%), plan_line_missing 99 (4.5%), `protocol_deviation` compliant 2,104 / `unlabelled_plan` 87 / `plan_absent` 12 / `answer_label_absent` 0, latency p50 94.4 s (ask seam p95 196.3 s; wave 78: 78.3 s); gameend sums: `ask_replays_reserved` 1,084 / `identical_ask_answers_reserved` 0 (wave 78: 953), `ask_key_continuation_differs` 118 (NEW), `hold_released_turn` 88, `hold_windows_skipped` 1,598 (cast 1,080 / priority 518; wave 78: 2,816), `hold_verdict_safer_ignored` 59 (NEW) / `hold_reopened_new_threat` 0 (NEW), `stop_reached_windows_skipped` 8 / `stop_reached_reputs_collapsed` 5 (wave 78: 44 / 0), `stack_drain_windows_asked` 30, `own_loop_windows_asked` 4 / `own_loop_verdict_lines_rendered` 63 (NEW), `forced_close_events` 25 / `forced_close_unrecorded` 12, `phase2_answer_recovered` 1 / `_missing` 0, `crossphase_identical_reputs` 172 / `crossphase_board_unchanged` 26, `menu_pass_no_progress_suppressed` 1 (NEW), `main_phase_windows_skipped` 31, `mana_only_windows_skipped` 129, `own_turn_windows_skipped` 560, `reserve_decline_windows_noted` 19 / skipped 0, `plan_names_stranded_card` 3, `plan_names_uncastable_zone_card` 23, `protocol_deviation_replies` 99, `async_drops` 15, `repeat_annotated_takes` 2, `identical_option_asks_resolved` 7, `blocker_forecast_gang` 14 / multi 26 / collapsed 0 / rows 72, `chain_windows_only_selfharm` 48 / `chain_selfharm_rows` 167 / `chain_acting_rows` 902 (+ `_cast` 252 / 291 / 949); wins deck152 5/6, deck162 4/6, deck146 4/6, deck130 4/6, deck126 3/6, deck125 1/6, deck123 0/6 (wave 78: 146/126/152/123 4/6, 125 3/6, 162 2/6, 130 0/6 — deck123 4→0, deck130 0→4; hands are real, do not read variance as guide effect).

## Per-deck agent (one per deck: 146, 152, 125, 126, 162, 123, 130)
Purpose (owner's words): read the transcripts of your deck, compare the LLM's actions to the
instruction set (the guide projects/mtg/bin/Res/ai/baka/deck<N>_strategy.txt + the general guide
wave62/general-strategy.md (wave-59 edition + R331; NOTE from the wave-63..67 syntheses: this file is NOT loaded at runtime (only deck<N>_strategy.txt is opened, AIPlayerGPT.cpp:15332) — the deck guide is the live surface) + the reply protocol in the prompt), find issues in the game, the
interface, and the guide, and surface anything else worth the core loop's attention.
Method: for each of your deck's 6 games (your seat's translog; the opponent's for context),
trace the decisions that decided the game. Classify each misplay PERCEPTION (the model misread a
true surface, or the surface was false/missing -> ENGINE/RENDER item, cite file+seq) vs STRATEGY
(read correctly, decided against the guide or against good play -> guide item). Note fallbacks,
re-asks, and any decision where the prompt's information was insufficient or wasteful (what a
better interface would have shown). Hands are real (mulligans, bottoming): do not read variance
as guide effect.
Output: ONE file, wave79/deck<N>/review.md: (1) game-by-game outcome + the deciding decisions with
seq citations; (2) engine/interface/card items ranked HIGH/MED/LOW with a concrete repro (file,
seq, the rendered line) — this is the primary deliverable; (3) guide verdict: KEEP as is (a
success verdict, state why) or EDIT — if EDIT, also write wave79/deck<N>/strategy.txt (the full
revised guide, start from the live one) and list each edit before->after with the seq that
paid for it IN review.md — the guide itself carries NO citation, count, or history (Amendment 332); hard ceiling 20,000 bytes, and an EDIT that grows the guide must say which rule earned its bytes; (4) OPTIONAL proposals, only when
your evidence supports them: general-guide changes (wave79/deck<N>/general-proposals.md) and
strategy-writing-skill changes (wave79/deck<N>/skill-proposals.md; current skill edition =
wave68/strategy-writing-skill-v2.md (the ~40 KB distillation, owner-installed 2026-09-06 after six Astra adversarial rounds; read it in full — it is short; the 1.3 MB wave68/strategy-writing-skill.md is lineage only) — read it before proposing; a proposal that
restates an existing amendment is noise). No prediction tables, no rotation evidence, no
docket bookkeeping. Final message: <= 10 lines.

## Engine-narration seat (one agent)
You read the whole corpus for the engine's sake, not the guides'. Deliverable wave79/engine-seat.md: (1) corpus census as last wave (games, decisions, decisions/turn vs wave 78's ~2.1, fallbacks by kind with genuineness, `async_drops`, `transport`, `deadline_pct`, reply shape, `reasoning_chars`, `reply_truncated`, `askreplay/`, `plan_steps_done`); (2) REGRESSION WATCH on the wave-79 changes — the section that matters most (SINGLE binary, single run, no reruns; LESSON OF WAVE 78: every lane predicted a NON-ZERO counter — a 0 is a FAIL unless you show the state never arose): (a) FALLBACKS — 0 on the ask seam (first since wave 76): confirm from the records (every `fallback` field), and quote the one `reveal`/`engine_answered` (zero-eligible?); `protocol_deviation` classes compliant 2,104 / `unlabelled_plan` 87 / `plan_absent` 12 / `answer_label_absent` 0 — the label-less shape did NOT occur this wave (deck125's guide edit 4 paired the label with the form): state that the guide was the cause; lane DD's parser fix (merged after this corpus, 5bc856d84) is therefore untested by this corpus — say so; (b) T2/CZ — `stop_reached_reputs_collapsed` 5 (predicted 20-35) / `stop_reached_windows_skipped` 8 (wave 78: 44): find every stop-reached re-put chain; did the collapse fire on each; is any collapse a LOST LEGAL OPTION (the owner's condition — an option the seat could have taken that the collapsed window hid); why 5 not 20+ — fewer chains this wave, or the arm still refusing; (c) T3/CZ + DC F1 — THE CG SHAPE: count same-seam same-turn same-phase re-asks that are byte-identical or identical after stripping `{…}`/`[…]` (wave 78: 135 + 98 same-phase); predicted 0; `ask_key_continuation_differs` 118 (NEW — each is a window whose legal continuation changed under identical rows: sample 20 and verify a target/X/cost/stack change actually occurred); `identical_ask_answers_reserved` 0 (wave 78: 953) vs `ask_replays_reserved` 1,084 — explain the swap (did the replay path move counters, or did replays stop); any REPLAYED answer that was WRONG for the new board (DC F1's aliasing risk — the owner's rule: a cached answer replays only when legal continuations are identical); (d) hold latch — `hold_verdict_safer_ignored` 59 (NEW): sample 15 — was the ignored safer verdict truly safer, and did any hold survive a board where a re-open was owed; `hold_reopened_new_threat` 0 (DC F2; predicted > 0) — show whether a new equal-rank threat ever arose (if never, UNTESTED; if it did, FAIL); `hold_windows_skipped` 1,598 (wave 78: 2,816); `crossphase_board_unchanged` 26 (wave 78: 4) — quote 5; `crossphase_identical_reputs` 172; (e) T4/CZ + DC F10 — `own_loop_verdict_lines_rendered` 63 / `own_loop_windows_asked` 4: every rendered `[own loop verdict:` line — which face (IDLE/RESOLVING/THREATENED/UNPROVEN), TRUE against the stack, on an ASKED prompt (F10: none on a suppressed window), did the seat act on it; `menu_pass_no_progress_suppressed` 1 (quote); (f) T5/T6/T9/DA + DC F3/F4/F5 — blockers headers: every `AT BEST` figure — is it beaten by any legal assignment (trample spill, menace, first/double strike — the DP now models both damage steps; past 12×12 the claim is dropped: count those); `GANG BLOCK` spill clauses true; no header prints two disagreeing verdicts; `{crack-back cover:}` subtracts from the header's total; sacrifice-cost rows price the spent blocker without double-counting (quote 5); ATTACK TOTAL folds punishers with 'N power survives' and no life projection; `blocker_forecast_*` 72/26/14/0; (g) T7/DA + DC F8 — every `UNLESS <source> untaps them` clause: the source is the seat's or a both-sides live trigger, never a spent ETB; (h) T8/DB + DC F6 — every `HELD:` note on an instant with no target; keep-X naming a COUNTERSPELL (not Ertai-class, not Path); (i) T10/DB — prompt bytes: per-deck max and count over 20 KB (wave 78: deck125 44.4%, deck130 max 29,641; DB predicted deck130 max ≈ 22 KB, corpus mean 15.3 → 13.8 KB); the narration trim at 16/12 KB — did any prompt lose the seat's own last PLAN, a stated stop, or the loop state; hoisted card text once per BODY; (j) T11/DB + DC F7 — `{effect:}` glosses: sample 30, 0 mid-clause cuts, `(...more)` after whole units, dependent clauses kept together (Chrome Mox class); (k) T12/T13/T14/CZ + DC F9 — `[hold check:` brackets: claims true against `hold_check_ref_seq`; `N gone` true; rank shifts (`#3`→`#2`) not counted as new; the drain clause scoped to THIS stack; `forced_close_events` 25 / `forced_close_unrecorded` 12 / `phase2_answer_recovered` 1 — reconcile against stderr; `declined_face_latches` gone from gameend (retired); (l) S12/T15 — `Opponent life trend:` gained/lost/net true, `+0` face printed when one side is zero; (m) `async_drops` 15 (wave 78: 46) — all re-asked; `mana_only_windows_skipped` 129 (wave 78: 367); latency p50 94.4 s (wave 78: 78.3) with 2,203 decisions (wave 78: 2,629) — what made decisions fewer and slower (prompt bytes? the collapse? reasoning_chars median 5,835); `plan_line_missing` 99 (4.5%); wins: deck123 0/6 (wave 78: 4/6) and deck125 1/6 (3/6) — hands are real, but say whether any wave-79 render change appears in their deciding decisions; (n) the harness — no tripwire, 0 HUNG, the corpus took 5 h 16 min; (3) adjudicate EVERY prediction in wave79/lane-{CZ,DA,DB,DC}.md (DC = the eleven Astra findings; lane-DD.md is post-corpus — mark its prediction UNTESTED) as PASS / FAIL / UNTESTED with counts and file+seq (UNTESTED is a real verdict), plus the carried UNTESTED docket (BS F2 Tribute second edict, (BS F3 retired), BS F5 blockers ceiling, BS F6 distinct-index re-ask, CF F1 pending-deadlock, F2/F5-nonlifelink/F8 haste/F9, the reasoning-body halves of N14a/N14d/O20; {X}-announcement payment loss — count `dropped after its X was announced`); (4) engine/render/narration defects ranked HIGH/MED/LOW with repro — especially any new render clause stating a false fact (the drain clause, the UNPROVEN/THREATENED loop faces, the conditional-counter clause, the GAINED/LOST split, the hoisted card text, the `(...more)` cuts); (5) RETIRED by owner ruling 2026-09-12 — write NO lategame specimen. Final message: <= 15 lines, HIGH items first.

## Synthesis (runs only if any deck agent wrote general-proposals.md or skill-proposals.md)
Read all proposals + engine-seat.md. Decide each proposal: ADOPT (edit wave79/general-strategy.md,
a copy of wave62's (the R331 edition; waves 63-67 wrote no edition; wave 68 installed wave68/strategy-writing-skill-v2.md as the current edition), or append a numbered amendment to wave79/strategy-writing-skill.md, a
byte-verbatim copy of wave59's with amendments appended — verify the prefix with cmp) or
REJECT with one line of reasons. Write wave79/synthesis-notes.md. If a deck agent's guide edit
contradicts an adopted general change, note it; do not edit deck guides yourself.

METHOD NOTE (from the wave-59 engine seat): the translog `phase` field is not the rendered phase — count phase-gated facts from the prompt text.
