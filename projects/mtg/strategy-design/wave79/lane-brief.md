# Wave-79 step-one lane brief (base = master after wave78 known-bugs; evidence = wave78/known-bugs.md §T + wave78/engine-seat.md + wave78/deck<N>/review.md)

RULINGS THIS WAVE TOUCHES: 000 — reasoning only in the native channel; reply = PLAN then action, nothing else; no instruction licenses prose; no parser reads outside the two labelled parts (a present `CHOICE:` label on the PLAN's physical line is inside — P9); caps never bound reasoning; the golden protocol TEXT (reply-protocol.txt / kReplyProtocol) and the thinking regime are owner-ruled — render brackets and row text may change; T18 is REPORT-ONLY and T1 is MEASURE-ONLY pending the owner's ruling — NO parser tolerance for an unlabelled answer line. 00 — full games; a game answered by heuristics is not a corpus game. 3 — suite THREADS=1 0 failed (known flakes: lifeline, merrow_reejerey, intrepid_adversary_repeated_payment), PARSETEST 0 failed. Never remove, cap, or auto-answer a legal option (R1's loop guidance is an ANNOTATION plus the model's own hold row — nothing is collapsed or auto-answered; R9 is MEASURE ONLY). No VPK. No AI attribution. NO CORPUS from a lane; no lane sends requests to the pilot this wave.

Standing mechanics: as wave71/lane-brief.md (worktree-only; `make -f Makefile.sdl -j8`; both link guards OK; binary freshness; every wagic run under `systemd-run --user --scope -q -p MemoryMax=4G -p MemorySwapMax=0 --`; suite THREADS=1 as a detached unit `w79-<LANE>-suite` waited with a FOREGROUND until-loop (no Monitor); count both failure strings; PARSETEST from bin; Edit tool for UTF-8 sources, byte-splice for MTGRules.cpp/GameObserver.cpp/primitives; U+FFFD 0; RED-on-base evidence + a falsifiable prediction per fix; commit with explicit paths, `git add -f` under bin/Res; write wave79/lane-<LANE>.md; touch `~/.gatelogs/W79_<LANE>_DONE`; weakest evidence). LESSON OF WAVE 78: (1) a pin can pass while the live path renders NOTHING (S6: pinned rendered bytes, 0 renders in 42 logs) and a fix can be right at the helper while the caller's gate never admits the real case (S3: `planTurn == nowTurn`, and 40/44 windows lacked the repeat row the gate demanded) — every fix ships with a CORPUS-DERIVED fixture: the actual prompt/record pair that motivated the item, driven through the LIVE caller, plus a predicted NON-ZERO counter value in the next census; a counter that reads 0 after the fix is a FAIL, not "untested"; (2) a key-stability claim is proven only on the corpus's own re-asked pairs — the CG shape survived four waves of green per-row pins because no pin ever took a real re-asked prompt pair as its input. LESSON OF WAVE 74 stands: any NEW annotation carrying a board-derived number must be proven OUTSIDE every key (ask, async slot, hold latch, hold-check, option-set) with a PARSETEST pin that two windows differing only in that number yield identical keys, and the held set in a key pin is built the way the LIVE seam builds it (holdActionKeyRow via mLastMenuRows). LESSON OF WAVE 77: (1) a verdict the engine computes but pushes only into a KEY is invisible to the model — every verdict that should change a decision must be RENDERED, and its pin must assert the rendered bytes, not the key (S6); (2) a field named `_seq` that is not a record seq wastes eight seats — name telemetry by what it is and pin the join (S7); (3) the census must sum every record kind, including the new ones a lane adds. LESSON OF WAVE 76: (1) a bracket that prints a NUMBER or a COUNT must be pinned on the cases where the count is zero, one, and a reorder — Q1 shipped with the referent fixed and the count still wrong on reorders and disappearances; (2) a fix-lane finding whose fix is "compare more state" (CQ F4) can make a clause fail-closed into silence — pin that the positive clause still prints on the shape it was built for; (3) a clause that carries a number about LIFE must name the EVENT that moves it, or the model reads it as a statement about the row's mana cost (R3). LESSON OF WAVE 75: (1) an instruction's REFERENT must be something the reader can see — the hold-check bracket spoke of "the last window" meaning the last one the engine BUILT, while 3,402 built windows never reached the model; any bracket that refers to history refers to ASKED history; (2) a helper that hardcodes its input (the wave-74 stats script ignored its argument) reports the wrong corpus with full confidence — new tools take the run dir as an argument and echo it in their first output line; (3) a prediction's negative clause ("non-zero means lost") needs its own pin, not just the positive.

## Lane CZ — the window/hold family (T2, T3, T4, T13, T12, T14)
T2 the S3 collapse: stop persists across the turn boundary via the seat's own PLAN; identity = action-key set unchanged since the decline + printed M at/past the stop; no repeat row required; board numbers that create no row are not identity; fixture = `123v152` seqs 16→17 prompts through the live caller; predict `stop_reached_reputs_collapsed` > 0. T3 the CG shape: take ≥5 byte-identical and ≥5 strip-identical re-asked pairs from the corpus (engine-seat.md names them) as PARSETEST fixtures, drive them through the LIVE ask-cache, async-slot and hold-latch key builders, name the differing byte per pair, and make identical action-key sets at one seam+turn yield identical keys; the hold latch must survive a life tick and a stack-top change that creates no row; `126` seqs 89-191 (77 windows on one turn) is the acceptance case; predict byte-identical re-asks → 0. T4 the own-loop verdict: add `own_loop_verdict` to the record; find why the live render path emits nothing on the corpus board (`126v125`); RED on base with the corpus board; predict ≥1 rendered line per own-loop window. T13 the drain promise; T12 the occurrence-index over-merge (Staff of Nin #2 = new) + the cost-change sentence; T14 stderr `0 unrecorded` + retire `declined_face_latches`; name why phase-2 recovery read 0.
## Lane DA — combat truth (T5, T6, T7, T9, T16)
T5 AT BEST under trample/menace = the true knapsack (spill = power − assigned toughness; menace two blockers), the gang-block clause states the spill, a header never prints two disagreeing verdicts — fixtures = `146v152` seqs 21/26, `162v152` seq 19, `146v123` seq 19 boards. T6 the crack-back cover subtracts from the header's total or names its base — `162v146` seq 15. T7 the tap forecast under untap triggers (Intruder Alarm) — `152v123` seqs 17/18. T9 sacrifice rows price the spent blocker; ATTACK TOTAL folds the punisher it names (Lightmine Field) — `130v146` seqs 53/55, `126v125` seq 83. T16 `ON THE STACK` includes queued punisher damage; `[untapped]` affirmative face. Every fixture = the corpus board; every clause pinned key-stable on the JOINED menu (ask + async + hold keys).
## Lane DB — render truth, bytes and tools (T8, T10, T11, T15, T17, T1 measure)
T8 the held-counter note ("held — castable when a target appears") + keep-X prices the cheapest COUNTERSPELL in hand — `125v162` seq 52, `125v130` seq 315. T10 hoist by body not instance; narration trim by bytes (trigger 16 KB, keep the newest); per-deck byte census before/after on the corpus prompts. T11 gloss clause priority (triggered/activated before keywords) + the fallback cut closes open `(`/`"` — Barrowin, Sigarda, Ranger Class, Triumphant Adventurer. T15 `+0 gained` face. T17 corpus-stats.py: engine_answered/latency −1 records off the round-trip sum, CENSUS CHECK reconciles on the wave-78 corpus; harness banner does not count a zero-eligible engine-answered window as a fallback; the late-check turn source. T1: NO parser change; keep the measure; add per-record `answer_label_absent` on the recovery record so seats can join it.
## Lane DC — fix lane for the Astra review (wave79/codex-review.md) — seeded after step-one gate 1
