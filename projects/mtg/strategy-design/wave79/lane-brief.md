# Wave-79 step-one lane brief (base = master after wave78 known-bugs; evidence = wave78/known-bugs.md §T + wave78/engine-seat.md + wave78/deck<N>/review.md)

RULINGS THIS WAVE TOUCHES: 000 — reasoning only in the native channel; reply = PLAN then action, nothing else; no instruction licenses prose; no parser reads outside the two labelled parts (a present `CHOICE:` label on the PLAN's physical line is inside — P9); caps never bound reasoning; the golden protocol TEXT (reply-protocol.txt / kReplyProtocol) and the thinking regime are owner-ruled — render brackets and row text may change; T18 is REPORT-ONLY and T1 is MEASURE-ONLY pending the owner's ruling — NO parser tolerance for an unlabelled answer line. 00 — full games; a game answered by heuristics is not a corpus game. 3 — suite THREADS=1 0 failed (known flakes: lifeline, merrow_reejerey, intrepid_adversary_repeated_payment), PARSETEST 0 failed. Never remove, cap, or auto-answer a legal option (R1's loop guidance is an ANNOTATION plus the model's own hold row — nothing is collapsed or auto-answered; R9 is MEASURE ONLY). No VPK. No AI attribution. NO CORPUS from a lane; no lane sends requests to the pilot this wave.

Standing mechanics: as wave71/lane-brief.md (worktree-only; `make -f Makefile.sdl -j8`; both link guards OK; binary freshness; every wagic run under `systemd-run --user --scope -q -p MemoryMax=4G -p MemorySwapMax=0 --`; suite THREADS=1 as a detached unit `w79-<LANE>-suite` waited with a FOREGROUND until-loop (no Monitor); count both failure strings; PARSETEST from bin; Edit tool for UTF-8 sources, byte-splice for MTGRules.cpp/GameObserver.cpp/primitives; U+FFFD 0; RED-on-base evidence + a falsifiable prediction per fix; commit with explicit paths, `git add -f` under bin/Res; write wave79/lane-<LANE>.md; touch `~/.gatelogs/W79_<LANE>_DONE`; weakest evidence). LESSON OF WAVE 78: (1) a pin can pass while the live path renders NOTHING (S6: pinned rendered bytes, 0 renders in 42 logs) and a fix can be right at the helper while the caller's gate never admits the real case (S3: `planTurn == nowTurn`, and 40/44 windows lacked the repeat row the gate demanded) — every fix ships with a CORPUS-DERIVED fixture: the actual prompt/record pair that motivated the item, driven through the LIVE caller, plus a predicted NON-ZERO counter value in the next census; a counter that reads 0 after the fix is a FAIL, not "untested"; (2) a key-stability claim is proven only on the corpus's own re-asked pairs — the CG shape survived four waves of green per-row pins because no pin ever took a real re-asked prompt pair as its input. LESSON OF WAVE 74 stands: any NEW annotation carrying a board-derived number must be proven OUTSIDE every key (ask, async slot, hold latch, hold-check, option-set) with a PARSETEST pin that two windows differing only in that number yield identical keys, and the held set in a key pin is built the way the LIVE seam builds it (holdActionKeyRow via mLastMenuRows). LESSON OF WAVE 77: (1) a verdict the engine computes but pushes only into a KEY is invisible to the model — every verdict that should change a decision must be RENDERED, and its pin must assert the rendered bytes, not the key (S6); (2) a field named `_seq` that is not a record seq wastes eight seats — name telemetry by what it is and pin the join (S7); (3) the census must sum every record kind, including the new ones a lane adds. LESSON OF WAVE 76: (1) a bracket that prints a NUMBER or a COUNT must be pinned on the cases where the count is zero, one, and a reorder — Q1 shipped with the referent fixed and the count still wrong on reorders and disappearances; (2) a fix-lane finding whose fix is "compare more state" (CQ F4) can make a clause fail-closed into silence — pin that the positive clause still prints on the shape it was built for; (3) a clause that carries a number about LIFE must name the EVENT that moves it, or the model reads it as a statement about the row's mana cost (R3). LESSON OF WAVE 75: (1) an instruction's REFERENT must be something the reader can see — the hold-check bracket spoke of "the last window" meaning the last one the engine BUILT, while 3,402 built windows never reached the model; any bracket that refers to history refers to ASKED history; (2) a helper that hardcodes its input (the wave-74 stats script ignored its argument) reports the wrong corpus with full confidence — new tools take the run dir as an argument and echo it in their first output line; (3) a prediction's negative clause ("non-zero means lost") needs its own pin, not just the positive.

## Lane CZ — the window/hold family (T2, T3, T4, T13, T12, T14)
T2 the S3 collapse: stop persists across the turn boundary via the seat's own PLAN; identity = action-key set unchanged since the decline + printed M at/past the stop; no repeat row required; board numbers that create no row are not identity; fixture = `123v152` seqs 16→17 prompts through the live caller; predict `stop_reached_reputs_collapsed` > 0. T3 the CG shape: take ≥5 byte-identical and ≥5 strip-identical re-asked pairs from the corpus (engine-seat.md names them) as PARSETEST fixtures, drive them through the LIVE ask-cache, async-slot and hold-latch key builders, name the differing byte per pair, and make identical action-key sets at one seam+turn yield identical keys; the hold latch must survive a life tick and a stack-top change that creates no row; `126` seqs 89-191 (77 windows on one turn) is the acceptance case; predict byte-identical re-asks → 0. T4 the own-loop verdict: add `own_loop_verdict` to the record; find why the live render path emits nothing on the corpus board (`126v125`); RED on base with the corpus board; predict ≥1 rendered line per own-loop window. T13 the drain promise; T12 the occurrence-index over-merge (Staff of Nin #2 = new) + the cost-change sentence; T14 stderr `0 unrecorded` + retire `declined_face_latches`; name why phase-2 recovery read 0.
## Lane DA — combat truth (T5, T6, T7, T9, T16)
T5 AT BEST under trample/menace = the true knapsack (spill = power − assigned toughness; menace two blockers), the gang-block clause states the spill, a header never prints two disagreeing verdicts — fixtures = `146v152` seqs 21/26, `162v152` seq 19, `146v123` seq 19 boards. T6 the crack-back cover subtracts from the header's total or names its base — `162v146` seq 15. T7 the tap forecast under untap triggers (Intruder Alarm) — `152v123` seqs 17/18. T9 sacrifice rows price the spent blocker; ATTACK TOTAL folds the punisher it names (Lightmine Field) — `130v146` seqs 53/55, `126v125` seq 83. T16 `ON THE STACK` includes queued punisher damage; `[untapped]` affirmative face. Every fixture = the corpus board; every clause pinned key-stable on the JOINED menu (ask + async + hold keys).
## Lane DB — render truth, bytes and tools (T8, T10, T11, T15, T17, T1 measure)
T8 the held-counter note ("held — castable when a target appears") + keep-X prices the cheapest COUNTERSPELL in hand — `125v162` seq 52, `125v130` seq 315. T10 hoist by body not instance; narration trim by bytes (trigger 16 KB, keep the newest); per-deck byte census before/after on the corpus prompts. T11 gloss clause priority (triggered/activated before keywords) + the fallback cut closes open `(`/`"` — Barrowin, Sigarda, Ranger Class, Triumphant Adventurer. T15 `+0 gained` face. T17 corpus-stats.py: engine_answered/latency −1 records off the round-trip sum, CENSUS CHECK reconciles on the wave-78 corpus; harness banner does not count a zero-eligible engine-answered window as a fallback; the late-check turn source. T1: NO parser change; keep the measure; add per-record `answer_label_absent` on the recovery record so seats can join it.
## Lane DC — fix lane for the Astra review (wave79/codex-review.md) — seeded after step-one gate 1

## Lane DC — fix lane for the Astra review (wave79/codex-review.md) — seeded after step-one gate 1 — base = master 244d8b5f8 (gate 1: 1293/0, 76 AI/0, PARSETEST 6810/0)
Eleven findings, each CONFIRM (fixture/pin RED on base reproducing Astra's trigger THROUGH THE LIVE CALLER PATH, then fix) or REFUTE (a pin proving the trigger cannot occur, with the reason). The principal blocker is F1: CZ's T3 replaced the ask/async board half with a turn+phase scope, so two windows with the same action-key set but DIFFERENT LEGAL CONTINUATIONS now share a key and the cached answer is replayed. The owner's rule cuts both ways: a board NUMBER that creates no row is not identity — but a legal option (a new target, a grown X ceiling, a changed cost) IS. The corpus's re-asked pairs (life tick, log line, `{...}` group) must still key EQUAL after the fix; Astra's shapes must key UNEQUAL. T1 stays MEASURE ONLY.
- F1 HIGH T3 (AIPlayerGPT.cpp:~44662, ~18566, ~43540; lane CZ): the ask key and async-slot key now alias windows whose legal continuations differ — a second legal target for Doom Blade entering in the same phase, an X ceiling that grew, a cost that changed under an unchanged row — and the priority seam at ~43540 reads identical normalised rows after a SUCCESSFUL reusable activation as "no progress" and forces a pass. Fix: the key's board half = the seam scope + a LEGAL-CONTINUATION DIGEST: per cast/activation row the set of legal target instance handles (ids, not previews), the X ceiling for X rows, the cost groups (already in the action key), and the stack's object identities (ids, not text) — no life totals, no log, no annotation text; the "no progress" pass at ~43540 requires that the activation's continuation digest is unchanged too, and never fires after a successful activation that changed the board. Pins: the corpus pairs `123v126` 93→96 and `123v125` 59→62 still EQUAL; Doom Blade one-target vs two-target UNEQUAL; X ceiling 3 vs 4 UNEQUAL; a successful activation that created a token does not force a pass — all through the live key builders on the JOINED menu and the live `askModel` cache path.
- F2 HIGH T3 (AIPlayerGPT.cpp:~32559; lane CZ): the hold latch's rank clamp keeps a hold over a THREATENED loop after the answered threat resolves and a NEW threat targets a different component — the marker is face-only so equal rank never re-opens. Fix: the marker = face + the threatened component's and threatening object's NAMES (names, not numbers); a THREATENED face whose threat identity differs from the latched one re-opens; safer faces still do not. Pins: same threat → held; new threat, same rank → re-opened; RESOLVING after THREATENED → held.
- F3 HIGH T5 (AIPlayerGPT.cpp:~30208; lane DA): the DP sets `exactAssignment = true` while ignoring first strike and double strike (4/4 double-strike trampler vs a 0/3 blocker at 4 life: DP says survive at 3, you die; a 6/1 first-strike blocker kills a 6/6 trampler before damage, beating the DP's "best case"). Fix: model the first-strike damage step in the DP (first/double strikers deal in step 1; a blocker killed in step 1 assigns nothing; double strikers deal again in step 2 with trample spill recomputed against remaining toughness); if any involved creature has an ability outside the model (protection, prevention, damage redirection), `exactAssignment = false` and the wording drops both the optimality claim AND the death badge everywhere it is read, including the hold latch's death-verdict key. Pins: both Astra boards; the two wave-79 trample fixtures stay green.
- F4 HIGH T9 (AIPlayerGPT.cpp:~27551; lane DA): the sacrifice clause subtracts lost blocker cover from a total that never credited blockers — 6 life, Mogg Fanatic + a 0/6 vs 5/5 + 2/1, sacrifice Fanatic to kill the 2/1: prints −1 (death) while declining every block leaves 1. Fix: lost cover = (header's projected life WITH the blocker) − (projected life WITHOUT it) computed by the same blockers floor the header uses, applied to the header's own base; never subtract twice; pin Astra's board = "you would be at 1".
- F5 HIGH T9 (AIPlayerGPT.cpp:~7484; lane DA): the folded ATTACK TOTAL sentence says surviving attackers' power "reaches them" and subtracts it from their life while an untapped 5/6 can block the survivor. Fix: the fold states "N power survives the field" and projects life only through the existing blocker-aware attack forecast (which already knows their untapped blockers); no life projection without it; pin the 1/1 + 5/5 into Lightmine Field + a 5/6 = no "reaches them" life figure.
- F6 MED T8 (AIPlayerGPT.cpp:~46346; lane DB): keep-X's counterspell candidate accepts `fizzle` anywhere in any script — Ertai, Wizard Adept (`{2}{U}{U}{T}:fizzle`) is reserved at its 3-mana CASTING cost as "a counterspell". Fix: a counterspell candidate = an instant (or flash) spell whose own cast effect counters (the `fizzle` in the spell's main `auto=`, not inside a `:` activated-ability clause); pin Ertai vs Counterspell vs Mana Leak through the live candidate selection.
- F7 MED T11 (AIPlayerGPT.cpp:~703; lane DB): clause priority drops a clause another kept clause depends on — Chrome Mox prints the mana clause "of any of the exiled card's colors" without the imprint clause at the 120/140 B budgets. Fix: dependency = a kept clause that refers to "the exiled card", "that card/creature", "it", "if you do", "this way" requires its antecedent clause; keep both or omit both, then `(...more)`; pin Chrome Mox at both budgets.
- F8 MED T7 (AIPlayerGPT.cpp:~7187; lane DA): the UNLESS-untaps scan matches script substrings without ownership or availability — their Village Bell-Ringer's spent ETB `untap all(creature|mybattlefield)` is named as a source that can untap YOUR creatures. Fix: a source counts only if (a) the seat controls it or its effect names the seat's battlefield/all creatures of both players, and (b) it is a LIVE trigger or activation (not an ETB of a permanent already in play); pin Village Bell-Ringer (theirs, in play) = no clause; own Intruder Alarm = clause; their Intruder Alarm = clause.
- F9 MED T12 (AIPlayerGPT.cpp:~32667; lane CZ): ordinals above ` #1` are kept as identity while `instanceHandle` recomputes rank from battlefield order — removing an earlier tapped Staff turns `Staff of Nin #3` into `#2` and re-opens a hold with no new legal action. Fix: the occurrence index keys on the permanent's INSTANCE id (stable), not its rank; a rank shift with no row change = 0 new / 0 gone; a genuinely new copy = 1 new; pin #3→#2 rank shift vs a real second copy.
- F10 MED T4 (AIPlayerGPT.cpp:~47456, gate at ~47483-47487; lane CZ): `own_loop_verdict_lines_rendered` and the face stamp are populated BEFORE the live hold gate discards the window — a suppressed window counts as rendered and leaves a stale face for a later record. Fix: count and stamp at the splice into the PROMPT that is actually SENT (after every skip gate), clear the face when the window is suppressed; pin: a proven loop + a standing cast hold → 0 rendered, no face on the next record; an asked window → 1 rendered.
- F11 LOW T17 (corpus-stats.py:~288; lane DB): the tool's explanation calls the record `turn` "this SEAT's turn count" but `translogTurn` is `observerTurn + 1` for both seats. Fix: state the true relation after reading where results.tsv's `turn` comes from (the harness's gameend line vs the record's observer turn) and explain the 1,016 vs 974 gap correctly; pin nothing false.
Astra's side-checks (no action): the DA lower-cased key expectation is consistent with CZ's normalisation; ctor merge coherent; golden protocol unchanged; no unlabelled-answer acceptance; DA's KEY pin uses supplied row strings (make it build the joined menu through the live collector as part of F1's pins).
Order: F1, F2, F3, F4, F5 (aliasing / lost re-open / false survival ×3), then F10, F9, F6, F8, F7, F11. Gate: suite THREADS=1 + PARSETEST + harness --selftest + corpus-stats --selftest + instruction guard; lane report wave79/lane-DC.md with per-finding CONFIRM/REFUTE, RED evidence, fix, GREEN, prediction (a NON-ZERO counter), weakest evidence.
