# Wave-79 known bugs (corpus matchups-20260912-000748-final: 21 games, ONE run, no pilot incident, 5 h 16 min, SINGLE binary archives/wagic-5af6f7a95-w79step1 = b1e1db7be; sources wave79/engine-seat.md + wave79/deck<N>/review.md)

Invariant 000 held: 2,203/2,203 protocol replies with native reasoning, 21 natural finishes, 0 truncations, 0 hung, 0 tripwires, **0 heuristic answers on the ask seam** (first since wave 76; the one `engine_answered` reveal is a zero-eligible window). `answer_label_absent` 6 → 0: deck125's guide edit 4 was the cause of the label-less line; lane DD's robust parser (owner ruling 2026-09-12) is merged at 5bc856d84 and UNTESTED by this corpus. Prediction scoreboard (engine seat): 45 PASS / 8 FAIL / 7 UNTESTED. PAID: the CG shape 192 → 3 byte-identical same-phase re-asks; all 118 `ask_key_continuation_differs` sampled = real moved continuations (52/52 recoverable pairs); 16/16 blockers best-case figures correct, 111/111 crack-back cover totals agree, 1,690/1,693 hold-check claims true; keep-X names a counterspell 62/62 (Path 0/62, was 10/47); deck125 prompts over 20 KB 44.4% → 11.8%, deck130 max 29,641 → 22,357 B; `+0` face; `UNLESS <source> untaps` 32/32 both-sides live (deck126); the stop-reached collapse fired 5/5 and hid nothing (deck123); wave-78 guide edits paid at 162 (Ob Nixilis carve-out beat the deck that beat it twice), 152, 125 (all four), 130 (B/C/D), 146 (1 and 3). Wins 152 5/6, 162/146/130 4/6, 126 3/6, 125 1/6, 123 0/6 (deck123's slide is variance — five games had no engine; deck130's rise came with a decision-priced loss only at 162).

## U — routed to wave 80 (HIGH first)
- U1 HIGH — THE X-ANNOUNCEMENT PAYMENT LOSS IS LIVE, and its cause is the PLAN ECHO: the plan-step trimmer marks a cast DONE at the CAST-ROW answer, so the `Choose an option for <card>` / X menu that COMPLETES the cast echoes a plan with the cast clause deleted (`130v162` seqs 31→32: "Cast Starstorm X=2 to kill Fog Bank, then attack…" became "then attack with Rorix…" and the seat took `Decline - do nothing` on 6 already-tapped sources; 120 of 275 deck130 echoes lose their leading clause); deck125 did the same twice (`126` seqs 44→45, `152` seqs 9→10: announced Sphinx's Revelation under its own stop, then Declined at the X menu — 5 and 4 mana burned, the only two `dropped after its X was announced` events, both in games deck125 lost); the modal `Decline` row omits the stranded-mana clause the X decline row prints (`130v123` seqs 26/33). Fix: a cast step stays in the echo until the cast's completing menu resolves (mark DONE at the completing seam, not the cast row); every decline row on a completing menu prints "your N sources are SPENT either way"; pins on the corpus pairs; predict `dropped after its X was announced` 0 and 0 leading-clause loss in echoes at completing menus.
- U2 HIGH — THE HOLD-LATCH COUNTERS ARE UNAUDITABLE: `hold_verdict_safer_ignored` 59 and `hold_reopened_new_threat` 0 have no per-record trace, and the verdict marker rows `[crack-back verdict:` / `[stack death verdict:` render 0 times in 2,203 prompts (keys only — the wave-77 lesson again); DC F2 is UNTESTED by default (engine seat) while deck125 names a state where a NEW threat arrived under a live hold and nothing re-opened (`125v162` seq 199: Underworld Dreams + Forced Fruition). Fix: per-record `hold_safer_ignored_face` and `hold_reopen_reason` fields; the crack-back and stack-death verdicts RENDER as prompt lines (the same fix S6 needed); a fixture from `125v162` seq 199 through the live latch; predict `hold_reopened_new_threat` > 0 with a record naming the threat.
- U3 HIGH — CRACK-BACK COVER COUNTS A BODY THE ROW DOES NOT LEAVE: Acererak's ETB bounce (4 windows) and Silverquill Command's dead "return creature" mode (`146v162` seq 32) are credited as "adds 1 body" while the same row says the body leaves / the mode is dead; the true covers were twice DISBELIEVED (`146v162` seqs 35/37, ~2,000 reasoning chars each) because the clause names no mechanism; T9's spent-blocker clause is attached to the `{removes N from the CRACK-BACK…}` relief clause so it fires only on attacker-KILL rows — face/own-target `[cost: …, Sacrifice]` rows under a printed CRACK-BACK still price no blocker (`130v152` seqs 52/54); DC F4's new string never rendered (F4 FAIL, positive half). Fix: the cover credits a body only if the row's own resolution leaves it on the battlefield (ETB bounce, dead modes, sacrifice-as-cost excluded); the clause names the mechanism ("N blockers absorb M"); the spent-blocker clause attaches to EVERY sacrifice-cost row under a crack-back; pins on the four corpus boards.
- U4 HIGH — THE TAP BILL SPLITS: several one-name `{paying this taps: X}` clauses on one row where the payment taps three attackers and no clause states more than two (`152v125` seq 38) — the LETHAL COUNT rule counts names in that clause. Fix: one clause per row naming every tapped attacker; pin.
- U5 HIGH — THE CASTING MENU'S FORMAT EXAMPLE IS BUILT FROM ROW 1 (`e.g. "CHOICE: 1 (Cast Dictate of Kruphix)"`) while the priority menu uses a neutral placeholder; deck162's reasoning quoted the example three times and "picked Dictate as it's first in the menu"; the seat chose the example's row in 62/139 windows. Fix: neutral placeholder on every seam; pin; predict the row-1 pick rate falls to the priority menu's.
- U6 HIGH — A CYCLING ROW'S `[DRAW PRICE:]` PRICES THE DRAW HALF ONLY: with `DISCARD PUNISHERS … Liliana's Caress … costs you 2 life` printed on the same screen, cycling printed 1 and charged 3, twice (`130v162` seqs 35/37: 19 → 13 for two Mountains, game lost at exactly 0). Fix: a cycle is a discard AND a draw — price both; pin.
- U7 HIGH — THE `board sweep:` MARKER ARGUES ONE SIDE: it names the engine the sweep takes off THEIRS (`Fate Unraveler - a DRAW PUNISHER`) and nothing for the maker it takes off YOURS — deck123 cast Damnation with its own Thraben Doomsayer in the `M of yours` list and died two turns short (`123v162` seq 42; near-control seq 74 with no editorial, declined). Fix: the marker names both sides' named losses (engines/makers of yours by name); pin.
- U8 HIGH — `crossphase_board_unchanged` 4 → 26 (22 on deck123): full model calls on a board the bracket itself certifies unchanged, because the ask key includes phase — a dead list crosses every phase at full price (worst run `123v130` seqs 72-80, 746 s). Astra (wave 79 F1) warned that cross-phase collapse loses phase-gated options. Fix: replay the cached answer across phases ONLY when the bracket certifies board-unchanged AND the action-key set is identical AND no row's legality is phase-gated (no sorcery-speed row differs); otherwise ask; measure `crossphase_replayed` and pin the phase-gated case as still asked.
- U9 MED — CG RESIDUAL: 3 byte-identical + 19 strip-identical same-phase re-asks remain (T3a/b FAIL literal; 192/235 → 3/19); `ask_replays_reserved` 1,344 → 1,084 while `identical_ask_answers_reserved` 953 → 0 — the population moved from the latch path to the cache path (engine seat 2(c)). Fix: name the 22 pairs' differing bytes; the counters' rename/merge so the census is comparable.
- U10 MED — OWN-LOOP RESIDUALS: `[own loop verdict: your life loop is a proven win]` printed 4× on a board with NO starter while the same prompt said the only gain source cannot block the flier killing it (`126v130` seqs 31-36, the game deck126 lost); 7 `[own loop verdict: none]` writes counted as rendered lines (63 vs 56; land-drop seam missed by DC F10); the HOLD-ROW absolute and the RESOLVING verdict contradict on one menu (`126v125` seqs 45/46); THREATENED/UNPROVEN faces never rendered (UNTESTED). Fix: "proven win" requires a live starter (name it) or prints the gap; count only rendered non-`none` lines at every seam; the hold row and the verdict agree.
- U11 MED — COUNTER TARGET ASK OFFERS THE SEAT'S OWN STACK SPELL with no warning and the cast row's `can target on the stack:` under-reports the target set (`125v146` seqs 104/105: Cancel named the seat's own Dream Fracture, two counters spent for nothing); 23 prompts print `the stack is not empty (see ON THE STACK above)` with NO stack block (`125v162` seq 199). Fix: own-spell rows marked `[your own spell - never a counter target]`; the target roster matches the cast row; the stack sentence prints only with the block.
- U12 MED — GLOSS/RENDER RESIDUALS: `(...more)` still cuts mid-clause 2/34 (`146` seqs 22 "with menace and (...more)", 54 "emblem with (...more)"); Sigarda carries both `...` and `(...more)` (`126v152` seq 15); Teferi's Puzzle Box renders `{feeds: an amount that is not fixed (read the card)}` while the hand size is on screen (14 vs 6 unpriced, `162v152` seq 18); `[legendary:` omits current loyalty (`162v125` seq 41 — 5 mana on a loyalty-5 copy); Sorin's decklist gloss drops the −2 and −6 in all six games (deck126); mode liveness names the mode not the object (deck146). Fix each; pins.
- U13 MED — THE 21-ROW ADVERSARY MENU is still ASKED when the repeat cost pays for 0 payments, i.e. all 21 rows have one outcome (`152`, 4/207 calls); Teferi +1 chains. Fix: a menu whose rows all resolve to one outcome is answered by the engine (no legal option lost — they are the same option); measure.
- U14 MED — A BLOCKERS HEADER PRINTS "NOT lethal" while the same prompt forecasts compulsory lethal draw-punisher damage (`146v162` seq 29); deck152's one loss was a STRATEGY block at 18 life vs two 1/1s (`152v130` seq 24 — every surface true; guide edited). Fix: the blockers header's lethal verdict includes the forecast non-combat damage the seat cannot avoid this turn cycle; pin.
- U15 MED — COUNTERS OVERSHOOT THEIR RENDERED POPULATION by a fixed margin: own-loop 63 vs 56, cross-phase 172 vs 170, drain 30 vs 18 (deck126 6 vs 3) — counted before the send at some seams. Fix: every render counter increments at the send (one helper, every seam); pin per seam.
- U16 MED — BYTES MOVED TO deck146: mean 15.0 KB, 22.7% over 20 KB, 45 of the corpus's 99 `plan_line_missing`; deck125 max 25,471 B; the hold-row boilerplate is ~1.4 KB of every prompt (deck146). Fix: fold the hold-row boilerplate to one line; measure deck146 with prompt-byte-census.py.
- U17 MED — LATENCY p50 78 → 94 s with FEWER decisions (2,629 → 2,203) and a flat reasoning median (5,835): unexplained. Measure: per-seam latency vs prompt bytes vs reasoning_chars; a corpus-wide line in corpus-stats.py.
- U18 REPORT-ONLY (owner-ruled territory): `plan_line_missing` 99 (4.5%; deck162 7.4%, deck146 45/99), `protocol_deviation_replies` 99; T13c FAIL literal = 2 cross-seam drain pairs; DA's DP trample/menace/12×12 arms never exercised; T16 untested (no queued draw); DD untested (0 label-less replies).

Docket: still UNTESTED, carried: DP trample/menace/12×12 arms (DA), T16, DD's robust parser, THREATENED/UNPROVEN faces, DC F2 (until U2 makes it auditable), the cross-phase collapse's phase-gated-option risk (U8 pins it), the reasoning-body halves. Census rule: the record-kind sum must reconcile (it does: 2,230 = 2,203 + 25 + 1 + 1).
