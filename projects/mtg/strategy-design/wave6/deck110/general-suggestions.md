# General (core-prompt) suggestions from deck-110 wave-6

Layer-routed proposals for the shared `system_prompt.txt`. deck110 produced NO strong
core-prompt change this wave (its losses are variance/deck-construction, and its two
model-fixable leaks are deck-specific and handled in the guide). One weak-signal item and
one confirmation:

## PASS (no core change proposed). Rationale:
- The re-equip no-op is an ENGINE-marker bug (notes #E1), not a prompt gap. The core already
  carries the two-power done-signal + "equipping the same creature again gains nothing" (line
  27). Adding louder core text would repeat the wave-5 mistake — the pilot READS the cue and
  disobeys; the option-line marker is the fix, and it must be fixed in code, not prose.
- Counter-aware sequencing is deck-specific (affinity's go-wide answer to counters is not a
  universal prior — a control deck plays around counters oppositely). Keep it in the guide.

## WEAK SIGNAL (single-seat, route to synthesis for cross-deck convergence only):
**Attack-seam survival clause.** The core combat block (line 29) teaches the reverse lethal
check ("keep back enough blockers to survive it - a creature needed to survive the swing back
stays home, and do not tap out into a lethal swing back"). deck110 vs109 shows the pilot
attacked with a needed blocker at 8 life anyway and died to the crackback — so the core line
EXISTS but LOST to a local pattern. Per the skill's no-restatement caveat, this earns a
DECK-GUIDE reinforcement tied to the named situation (added to THE RACE), NOT a core rewrite,
UNLESS another seat this wave shows the same "tapped out my blocker attacking" pattern — then
the synthesis agent should consider strengthening the core line's phrasing from "do not tap
out into a lethal swing back" to also cover "do not ATTACK with a creature you need to block."
Flagged for convergence check only; single-seat cannot cut core.

## CONFIRMATION (no change): the ON THE STACK / labeled-counter representation is working as
intended on the OTHER side — deck44 used it to counter deck110's Etched Champion. That is the
platform functioning correctly, not a defect. No action.
