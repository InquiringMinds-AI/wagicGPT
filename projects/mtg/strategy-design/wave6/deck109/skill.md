# Proposed revisions to the strategy-writing skill (from deck109 wave-6)

**Net: no changes proposed. The skill's machinery held up cleanly again; this seat is a validation.**

Two skill mechanisms fired exactly as designed this wave:

- **The stopgap lifecycle correctly RETIRED the Hellrider fallback rule.** The skill's triple
  discipline (line, defect+citation, removal condition) + the "wave-N+1 author knows which stopgaps
  are still needed" procedure (check: engine fix shipped? corpus shows defect gone? core absorbed?)
  drove a confident deletion: commit 353f1a150 shipped, 2/2 Hellrider casts resolved (incl. the
  formerly-vetoing light-board case), the defect is gone → delete the guide fallback. Keep the
  procedure verbatim. Bonus confirmation of the skill's warning that a fixed-defect stopgap becomes
  attention tax: this one had gone further and was actively MISFIRING (the pilot cited "I must have
  missed casting Hellrider" to justify a Cast-nothing pass) — a concrete instance of a retired stopgap
  turning from inert to harmful, exactly why the lifecycle mandates deletion, not benign retention.

- **The per-seam positive-floor rule + the negative-framing ruling were VINDICATED by prediction.**
  Wave-5 replaced deck109's tail-end "Do not block... race" DON'T with a positive blocker-seam floor
  keyed to the 8-life threshold. The skill predicted this would fix the reflexive-high-life-blocking
  leak; this corpus confirms it (4 high-life blocks → 0). This is the "a decision SEAM needs its own
  positively-framed floor, not a terminal prohibition" lesson landing. No edit.

- **OBEYED-BUT-LOSING mode** handled the 133 loss correctly: high obedience metrics (26/26 full
  attacker commitment, 16/16 burn face, 0 durdle), opponent dead-adjacent at 6-7 (not near 20) → route
  the loss to deck construction, freeze the obeyed spine, resist the reflexive rewrite. Worked as
  written.

No method line was found wanting; nothing to add or cut.
