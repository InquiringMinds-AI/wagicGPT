# Card-Script Potential Issues

This ledger records suspicious behavior found during lexicon conformance testing. Entries do not authorize production fixes. “Potential” means the behavior may still be a fixture-driving or harness problem; “confirmed” means a deterministic witness distinguishes actual behavior from the documented semantics.

| ID | Status | Surface | Observation | Evidence / next check |
|---|---|---|---|---|
| CS-001 | **Fixed** | Rot Farm Skeleton payload | Four `{m}` payments mill four cards, but `all(this) moveTo(myBattlefield)` left the resolving source in the graveyard: the `all(this)` chooser does not find a source that sits in the graveyard. Card-script fix — bare `moveTo(myBattlefield)` acts on the source directly (the Reassembling Skeleton idiom); Oracle re-verified (DGM #98, returns untapped). | Witness promoted from `_known_failures.txt` to both green registries; suite 985+4/0. |
| CS-002 | Potential | `{q}` and `{q(target)}` activation | Crackleburr probe untapped the source and both selected blue creatures, but did not spend `{UR}{UR}`, create a stack action, or return the target. Source re-click and ability-choice variants produced the same result. | Reproduce manually or add test-harness tracing around extra-cost confirmation before classifying parser, engine, card-script, or driver. |
| CS-003 | Potential / harness gap | Emerge and subtype offering | Automated fixtures never expose the alternative-cost menu because every production primitive gates it with `otherrestriction=compare(ishuman)~morethan~0`, while TestSuite players are AI. Payment semantics therefore remain untested rather than failed. | Reproducers are listed in `test/lexicon/_potential_tests.txt`. Add a TestSuite human-player mode or validate manually after the lexicon test pass. |
| CS-004 | **Fixed — loader crash** | Positive islandhome boundary | Real segfault, nothing to do with islandhome: `Rules::getCardByMTGId` global first-match order (all of player 0's zones, then player 1's) returned player 0's already-placed Island when loading player 1's — `putInZone` from player 1's library then returned NULL and `Spell(NULL)` crashed. Any fixture (or saved game-state) with the same card id on both sides triggered it. | Fixed: `getCardByMTGId` gained a preferred-player parameter used by both initGame loaders (TestSuiteAI.cpp + Rules.cpp), plus a NULL-guard replacing the crash with a logged skip. Fixture (with one added phase advance) promoted to the green registries. |
| CS-004 | Potential | `{n}` Ninjutsu cost | In the unblocked-attacker window, the fixture selects Ninja of the Deep Hours and its Raging Goblin return-cost object, but no ability is committed: mana remains, the Goblin deals its normal damage, and the Ninja stays in hand. Re-clicking the source does not change the result. | Reproducer is listed in `_potential_tests.txt`. Trace `Ninja::tryToSetPayment`/confirmation or validate the exact GUI activation protocol manually. |
| CS-005 | Potential | Dynamic `{value:...}` mana | Circular Logic targets an opponent spell whose controller has two graveyard cards. The spell resolves, but only one mana is consumed instead of the expected two. The trace resolves Circular Logic before the payment menu and may be evaluating its own controller's graveyard after the counterspell moves there. | `mana_dynamic_value_graveyard.txt` in `_potential_tests.txt`. Trace `WParsedInt` binding/evaluation time and `pay[[...]]` payer selection. |
| CS-006 | **Partially fixed — root cause found** | `suspend(N)=` upkeep progression | Breakpoint-verified: the upkeep tick (MTGSuspendRule::receiveEvent) fires and the cast-on-zero trigger (Counters::removeCounter) runs — the mechanism works. The real defect was DATA: 4 suspend cards (Rift Bolt, Ancestral Vision, Mindstab, Plunder) embedded their target in the auto= line instead of the standard `target=` field, so the delayed cast stacked WITHOUT a target and fizzled (the cast path builds its chooser from `target=` only). All 4 converted to the standard idiom (Lightning Bolt / Ancestral Recall / Choking Sands references); Rift Bolt's normal cast regression-tested green (`cast_rift_bolt_normal.txt`). | REMAINING: the suite-driver click protocol for suspend-casting a targeted spell (chooser + alt-cost menu interplay) wedges scripted runs — the end-to-end suspend fixture stays in `_potential_tests.txt` pending a GUI confirmation of the click sequence. |
| CS-007 | Potential / harness | Threaded suite stability | One 191-test conformance run aborted after successful fixtures with `std::length_error: basic_string::_M_create`; no fixture assertion identified the source. Earlier 186-test runs completed normally. | Rerun the same registry and retain occurrence context. If reproducible, isolate by registry partition and inspect shared threaded logging/state. |
| CS-008 | Potential / harness | `{chosencolor}` generated mana ability | Sol Grail and Coldsteel Heart complete their color-choice menu, but subsequent automated source/menu clicks do not commit the generated tap-for-mana ability, leaving the mana pool empty. | `mana_chosen_color_persists.txt` in `_potential_tests.txt`. Validate manually or trace `GenericChooseTypeColorName`, `AASetColorChosen`, and action-layer registration after the choice resolves. |
| CS-009 | Potential | `@phasedin(this)` source trigger | Teferi's Imp phases out at its controller's Turn 2 untap, but its Turn 4 phase-in produces no trigger stack action and does not draw the library card. | `trigger_phasedin_draws.txt` in `_potential_tests.txt`. Check whether phased-source trigger suppression remains active while `WEventCardPhasesIn` is dispatched, and compare a non-source listener if one exists. |
| CS-010 | Potential / harness | Missing completion accounting | `exquisite_blood_i953.txt` enters a reciprocal Exquisite Blood/Sanguine Bond trigger loop and never emits a test result, but the runner exits 0 and reports only the other completed registry entries. | Require every loaded registry entry to reach pass/fail/timeout accounting; a suite must not report success when a worker remains unfinished. The finite `trigger_lifelost_amount` fixture replaces it in conformance. |
| CS-011 | Potential | `@coinflipped` / Puppet's Verdict | Puppet's Verdict resolves with a top library card available, but creates neither a coin-flip action nor an `@coinflipped` branch; both the 2-power and 3-power creatures survive. | `trigger_coinflipped_branch.txt` in `_potential_tests.txt`. Trace the temporary `all(*[zpos=1]|mylibrary) transforms(...)` grant and verify whether its target selector finds the top card before `flipacoin` resolves. |
| CS-012 | Potential / harness | Targeted `@energizedof` payload | Fabrication Module's activation grants energy and its energized trigger reaches a target chooser, but selecting Fathom Mage or Grizzly Bears does not apply the +1/+1 counter. Territorial Gorger's non-targeted energized listener works. | Compare targeted trigger-payload resolution after `TrplayerEnergized` with ordinary targeted triggered abilities; preserve a dedicated reproducer if manual activation behaves the same way. |
| CS-013 | Potential / harness | Defender declaration with legal co-attacker | In a combat-attacker fixture, declaring Raging Goblin and then clicking Benevolent Ancestor leaves the phase at attackers and the Goblin deals no damage; clicking the defender first similarly prevents the legal attack. | `basic_defender_rejects_attack.txt` in `_potential_tests.txt`. Compare manual invalid-attacker handling and inspect whether a rejected click clears existing attacker selections. |
| CS-014 | **Resolved — engine correct, fixture bug** | Horsemanship matching blocker | The blocker predicate (`MTGCardInstance::canBlock`) is correct; the block committed and both creatures died. The observed 3 life loss was Cao Ren's own ETB ("you lose 3 life") firing when `[INIT]` places him — misread as combat damage from an unblocked attacker. | Fixture expectation corrected (defender at 17 after setup); promoted to the green registries. |
| CS-015 | **Resolved — engine correct, fixture bug** | Skulk equal-power boundary | The skulk compare is already strict (`power > opponent->power`); equal power blocks fine. The original fixture's Fogwalker has an ETB *target chooser* ("target creature doesn't untap") that fires at `[INIT]` placement and consumes the scripted clicks, desyncing the whole script. General lesson: **fixtures must use vanilla-where-possible creatures — `[INIT]` placement fires ETB triggers, and a pending chooser eats `[DO]` clicks.** | Fixture rewritten around Furtive Homunculus (vanilla skulk); promoted to the green registries. |
| CS-016 | **Fixed — data** | Legions of Lim-Dûl primitive identity | Generalized by a full sweep: 11 set-file `primitive=` references used accented names (UTF-8) where the primitive identity convention is unaccented ASCII — 8× Lim-Dûl family (ICE/ALL/TSP), Márton Stromgald (ICE), Séance (SIS). All corrected to the unaccented primitive names; ICE 2466 now resolves (regression `data_ice_lim_dul_resolves.txt`). Séance Board / Clavileño / Arna Kennerüd remain unresolved because those cards have NO primitive at all (unimplemented). | Suite 989+4/0. |
| CS-017 | Confirmed / flaky | `generic/cytoplast_manipulator_i1085.txt` intermittent failure | Fails ~4/60 repetitions, at identical rates in windowed and WAGIC_HEADLESS runs (so not display- or mode-dependent): the gain-control intermittently doesn't happen (battlefield "expected 2, got 1"). Likely the threaded-suite race family (CS-007). | Reproduce with a probe registry (the test's path repeated 60×) run via `WAGIC_TESTSUITE_FILE`; a probe run reliably shows several failures. |

Add new observations here even when unconfirmed. Preserve a deterministic failing fixture in `_known_failures.txt` only after the expected semantics and driver protocol are sufficiently established.
