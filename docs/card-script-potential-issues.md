# Card-Script Potential Issues

This ledger records suspicious behavior found during lexicon conformance testing. Entries do not authorize production fixes. “Potential” means the behavior may still be a fixture-driving or harness problem; “confirmed” means a deterministic witness distinguishes actual behavior from the documented semantics.

| ID | Status | Surface | Observation | Evidence / next check |
|---|---|---|---|---|
| CS-001 | **Fixed** | Rot Farm Skeleton payload | Four `{m}` payments mill four cards, but `all(this) moveTo(myBattlefield)` left the resolving source in the graveyard: the `all(this)` chooser does not find a source that sits in the graveyard. Card-script fix — bare `moveTo(myBattlefield)` acts on the source directly (the Reassembling Skeleton idiom); Oracle re-verified (DGM #98, returns untapped). | Witness promoted from `_known_failures.txt` to both green registries; suite 985+4/0. |
| CS-002 | **FIXED (engine + suite infra, 2026-07-09)** | `{q}` and `{q(target)}` activation | Two real defects, neither the hypothesized lifecycle seam. (1) Suite: `tappedinplay:` was ASSERT-only — an [INIT] use silently no-opped, so the "tapped creatures" premise never existed and the untap costs were rules-correctly unpayable. [INIT] `tappedinplay:N` now taps the first N battlefield cards (`TestSuiteAI.cpp initGame`, `tap(true)` = no triggers). (2) Engine: `UnTapTargetCost`/`TapTargetCost` inherited `ExtraCost::canPay()==1`, so the activation was OFFERED with zero valid candidates and the target clicks silently never committed (the observed select-without-commit). Both now override `canPay()` = at least one candidate the chooser accepts in the right tap state. RESIDUAL: per-cost `canPay` cannot count SIBLING requirements (a cost needing two red creatures still reacts when only one exists) — relevant to the P2 legal-action-set work. | Witnesses green: `cost_untap_crackleburr.txt` (positive) + `cost_untap_gate_untapped.txt` (gate regression). |
| CS-003 | **Classified — harness gap by design** | Emerge and subtype offering | Not an engine defect: the production primitives deliberately gate these menus on `compare(ishuman)` and TestSuite players are AI, so the suite structurally cannot exercise them. Closing as untestable-in-suite; the paths need either a TestSuite human-player mode or one-off GUI validation. ⚠ LLM-TRACK IMPLICATION: `ishuman` (WParsedInt.cpp:1461) is false for ALL AI players including AIPlayerGPT — the LLM opponent is never OFFERED emerge/offering casts on these 15 cards. Once the GPT modal-menu seam is trusted, consider relaxing the gate for GPT-piloted players (first confirm why it exists — presumably Baka mishandled these menus). | Reproducers remain listed in `_potential_tests.txt` for whoever adds human-mode. |
| CS-004 | **Fixed — loader crash** | Positive islandhome boundary | Real segfault, nothing to do with islandhome: `Rules::getCardByMTGId` global first-match order (all of player 0's zones, then player 1's) returned player 0's already-placed Island when loading player 1's — `putInZone` from player 1's library then returned NULL and `Spell(NULL)` crashed. Any fixture (or saved game-state) with the same card id on both sides triggered it. | Fixed: `getCardByMTGId` gained a preferred-player parameter used by both initGame loaders (TestSuiteAI.cpp + Rules.cpp), plus a NULL-guard replacing the crash with a logged skip. Fixture (with one added phase advance) promoted to the green registries. |
| CS-004b | **RESOLVED — not a bug (fixture protocol), 2026-07-09** | `{n}` Ninjutsu cost | `testDestroy` instrumentation disproved the reap hypothesis: the ability lives in the layer all game (601 ticks, reap=0). The click gate told the real story: ninjutsu is an INTERRUPT-WINDOW activation — the blockers-step phase advance goes on the stack (`AddNextGamePhase`) and the attacker activates during the interrupt (`no`/`yes` … `endinterruption`), floating mana from lands INSIDE the window (INIT manapool drains at phase changes). Also hit the same-name click wedge (two `Island`s resolve to one card — use distinct land names). Engine correct end to end. | Witness green: `cost_ninjutsu_return_attacker.txt` (rewritten protocol). |
| CS-005 | **RESOLVED — not a bug (fixture misread the Oracle), 2026-07-09** | `{value:}` in `pay[[...]]` | Empirical probes cleared the engine on both axes. Zone: the granted pay-ability rides on the TARGET spell, so the primitive's `{value:type:*:opponentgraveyard}` (payer's opponent = CL's caster) is the rules-correct graveyard — the fixture had put the cards in the WRONG graveyard ("your graveyard" = Circular Logic's caster's, not the target controller's). Timing: with the caster's graveyard at 2 the engine charges exactly 2, and with it empty charges 0 — no self-count (CL still on the stack while resolving). The old fixture also had impossible arithmetic (expected a 2-mana payment with 1 floating). | Witnesses green: `mana_dynamic_value_graveyard.txt` (pay 2) + `mana_dynamic_value_graveyard_empty.txt` (pay 0, self-count regression). |
| CS-006 | **Partially fixed — root cause found** | `suspend(N)=` upkeep progression | Breakpoint-verified: the upkeep tick (MTGSuspendRule::receiveEvent) fires and the cast-on-zero trigger (Counters::removeCounter) runs — the mechanism works. The real defect was DATA: 4 suspend cards (Rift Bolt, Ancestral Vision, Mindstab, Plunder) embedded their target in the auto= line instead of the standard `target=` field, so the delayed cast stacked WITHOUT a target and fizzled (the cast path builds its chooser from `target=` only). All 4 converted to the standard idiom (Lightning Bolt / Ancestral Recall / Choking Sands references); Rift Bolt's normal cast regression-tested green (`cast_rift_bolt_normal.txt`). | REMAINING: the suite-driver click protocol for suspend-casting a targeted spell (chooser + alt-cost menu interplay) wedges scripted runs — the end-to-end suspend fixture stays in `_potential_tests.txt` pending a GUI confirmation of the click sequence. |
| CS-007 | **WATCH — not reproducible at head, 2026-07-09** | Threaded suite stability | 8 consecutive full-suite runs at `WAGIC_TESTSUITE_THREADS=16` (~8,000 test executions): zero aborts, zero failures, no `length_error`. The original abort predates this session's thread-relevant fixes (worker runaway cap, per-instance suite RNG, `genrand`/Conjure/randomcard migrated off process-global `std::rand`). KNOWN RESIDUALS, kept under this row: (a) one default-thread run once reported a 1001-test COUNT (vs 1000) — a rare double-count in threaded accounting; (b) seeded threaded runs showed one boundary-seed outcome flip (17↔18) that single-threaded runs don't — one scheduling-dependent RNG draw remains somewhere. Neither produces false failures today. Re-open on any recurrence with the log retained. | Stress logs were in scratchpad; the recipe is 8× full suite at THREADS=16 watching rc + tail. |
| CS-008 | **Resolved — fixture protocol (+ new wart CS-020)** | `{chosencolor}` generated mana ability | The mechanism works end-to-end: casting opens TWO menus (accept the as-enters choice, then the 5-color pick); the original fixture answered only the first, so the color was never chosen and no producer existed. With `choice 0` twice at cast, the granted `{T}: add {chosen}` producer persists across turns and produces correctly. | Fixture corrected and promoted (suite 995+4/0). The dissection surfaced CS-020: the consumed "Choose a color" ability keeps reacting forever. |
| CS-009 | **Fixed — engine** | `@phasedin(this)` source trigger | Real engine bug, bigger than the trigger: the phasing flip POLLED `mCurrentGamePhase == UNTAP` from the per-tick state pass, but the untap step can transit within a single tick, so the poll usually missed the window — a phased-out card often never phased back in (permanent phase-out; instrumentation showed 1 phase-out event, 0 phase-in events across 4 upkeeps). Fix: phasing now happens synchronously in `GameObserver::phasingPhase()`, called from `nextGamePhase()` as the untap step begins and BEFORE `untapPhase()` (CR 702.26 — a card that phases in still untaps). | Witness promoted; suite 991+4/0. |
| CS-010 | **Fixed (worker path)** | Missing completion accounting | Worker games now carry a 1M-update cap: a game that cannot end (infinite trigger loop, wedged modal state) is FAILED loudly as "Test timed out (game never ended)" instead of spinning the worker forever / vanishing from accounting. Residual: the few tests the MAIN thread runs per suite have no cap yet (a main-thread hang is at least loud — the whole run visibly stalls). Also added `WAGIC_TESTSUITE_THREADS=<n>` for single-threaded discrimination runs. | Suite 994+4/0 at default and threads=2. |
| CS-011 | **Fixed — card script** | `@coinflipped` / Puppet's Verdict | The primitive was an indirect contraption: grant the TOP LIBRARY CARD a `@coinflipped` listener plus a do-nothing `flipacoin`, and compare flip-result globals. Rewritten to the direct, corpus-proven construct: `flipacoin winability all(creature[power<=2]) destroy winabilityend loseability all(creature[power>=3]) destroy loseabilityend flipend` (same shape as Bottle of Suleiman). 12/12 repetitions green covering both flip outcomes. | Witness promoted to the green registries; suite 991+4/0. |
| CS-012 | **Fixed — engine** | Targeted `@energizedof` payload | Reproducer built (`trigger_energized_targeted_counter.txt`): the trigger's StackAbility goes on the stack and RESOLVES WITHOUT ever opening/waiting on its target chooser — clicks before resolution are consumed as interrupt-declines, so the counter payload fizzles targetless. Not a suite artifact (the wait never happens, so GUI play cannot answer either), and not payload word order (leading `target(...)` form fails identically). ROOT CAUSE UNDERSTOOD: creature-ETB payloads (bare `auto=`, e.g. Flametongue Kavu) route through the SPELL flow, which acquires targets at cast; `@event` trigger payloads route through GenericTriggeredAbility::resolve, which has NO target-acquisition step — a one-shot payload resolves immediately with an empty chooser and fizzles. Systemic and now MEASURED: 417 primitives use the plain (non-may) `@trigger:target(...)` shape vs 175 may-wrapped (which work via MayAbility). Spot checks fizzle 2/2 across unrelated trigger families: Fabrication Module (@energizedof) and Akoum Hellkite landfall (@movedTo — dealt 0 damage). The ability$!...!$ grant subfamily (Abyssal Specter discards) may work through the granted-activated path — verify before counting it broken. FIXED: `GenericTriggeredAbility::resolve` now routes payloads carrying an unfilled interactive action-TC (targetter set, not a TriggerTargetChooser) through `MayAbility(must=true)` — the same machinery the parser builds for `may target(...)` payloads — so the controller acquires the target; no legal target = the trigger correctly does nothing. Validated: both witnesses green (Fabrication Module counter, Akoum Hellkite landfall damage), full suite 994+4/0 with zero regressions, Baka self-play one-shot completes clean with the menus live. CS-005 (Circular Logic resolving before its payment menu) may be the same resolve-without-wait family. | Reproducer in `_potential_tests.txt`. |
| CS-013 | **RESOLVED — not a bug (fixture card choice), 2026-07-09** | Defender declaration with legal co-attacker | Benevolent Ancestor is not a vanilla defender: its `{T}: prevent:1 target(anytarget)` activated ability REACTS to the click and arms its target chooser, which blocks phase advance (`getCurrentTargetChooser()` gates `userRequestNextGamePhase`) — the "cleared attack / stuck phase" was the card's own ability working. With a vanilla defender (Wall of Stone) an illegal attacker click is a clean no-op: the declared goblin keeps attacking and connects. Another instance of the "fixtures that test the card, not the harness" lesson. | Witnesses green: `basic_defender_rejects_attack.txt` (rewritten vanilla) + `combat_illegal_attacker_click_noop.txt` (new: declared attacker survives an illegal co-click). |
| CS-014 | **Resolved — engine correct, fixture bug** | Horsemanship matching blocker | The blocker predicate (`MTGCardInstance::canBlock`) is correct; the block committed and both creatures died. The observed 3 life loss was Cao Ren's own ETB ("you lose 3 life") firing when `[INIT]` places him — misread as combat damage from an unblocked attacker. | Fixture expectation corrected (defender at 17 after setup); promoted to the green registries. |
| CS-015 | **Resolved — engine correct, fixture bug** | Skulk equal-power boundary | The skulk compare is already strict (`power > opponent->power`); equal power blocks fine. The original fixture's Fogwalker has an ETB *target chooser* ("target creature doesn't untap") that fires at `[INIT]` placement and consumes the scripted clicks, desyncing the whole script. General lesson: **fixtures must use vanilla-where-possible creatures — `[INIT]` placement fires ETB triggers, and a pending chooser eats `[DO]` clicks.** | Fixture rewritten around Furtive Homunculus (vanilla skulk); promoted to the green registries. |
| CS-016 | **Fixed — data** | Legions of Lim-Dûl primitive identity | Generalized by a full sweep: 11 set-file `primitive=` references used accented names (UTF-8) where the primitive identity convention is unaccented ASCII — 8× Lim-Dûl family (ICE/ALL/TSP), Márton Stromgald (ICE), Séance (SIS). All corrected to the unaccented primitive names; ICE 2466 now resolves (regression `data_ice_lim_dul_resolves.txt`). Séance Board / Clavileño / Arna Kennerüd remain unresolved because those cards have NO primitive at all (unimplemented). | Suite 989+4/0. |
| CS-017 | **FIXED — engine + fixture, 2026-07-09** | `generic/cytoplast_manipulator_i1085.txt` intermittent failure | Root cause found via seed sweep: the card's graft lines carry `genrandN` restrictions — community AI heuristics baked into the CARD SCRIPT (e.g. `compare(genrand20)~equalto~0` = 1-in-20 chance the AI moves a counter to an opponent creature on ETB). The bears' INIT placement fires that trigger; on unlucky rolls a may-menu opened and desynced the fixture (~3/60 ≈ 1/20, matching the observed rate). TWO fixes: (1) engine — `genrand` in `WParsedInt` drew from **global `std::rand()`**, immune to the fixture `seed` directive and interleaved across suite worker threads; it now draws from the seeded per-observer generator (single-threaded seeded runs are now fully deterministic); (2) fixture — `seed 1` pinned, 60/60 green threaded + single-threaded. RESIDUAL: one scheduling-dependent draw remains in threaded mode (a boundary seed flipped 17↔18 across runs) — folded into CS-007. ishuman-lens note: these `genrand`+`ishuman` script gates auto-decide graft moves for every non-human player INCLUDING AIPlayerGPT — the LLM never gets the choice (P2/P3 material). | Witness: pinned fixture, 30× repetition green in both modes. |

| CS-018 | **FIXED — engine, 2026-07-09** | `phasedoutbonus=` phase-out effect | The `WEventCardPhasesOut` ctor fired the bonus but forced `oneShot=true` onto what the parser wraps as `MayAbility(must)` for interactive payloads — a one-shot `resolve()` on a MayAbility does nothing, so the discard silently never happened (the CS-019 disease at another site). Fix: interactive payloads are now `addToGame`d so the may-machinery runs its menu; and the whole interaction chain gets `clickableWhilePhased` (new MTGAbility flag, propagated by the copy ctor through clones), exempting it from `ActivatedAbility::isReactingToClick`'s `isPhased` gate — the source IS phased out when the chooser runs, and this is a trigger consequence, not an activation. Also drops the old leaked `card->clone()` parse source. Non-interactive phasedoutbonus lines keep the original stack-fired oneShot path. | Witness green: `trigger_phasedin_draws.txt` rewritten to Oracle-correct expectations (forest discarded at phase-out via `choice 0` + card click, island drawn at phase-in, hand = island only). Suite 1000+4/0. |
| CS-019 | **FIXED — card script, 2026-07-09** | Casting of Bones fidelity | Rescripted to two trigger lines: `@movedto(graveyard) from(mytgt|Battlefield):reject notaTarget(*|myhand)` + `...:draw:3` (discard registered FIRST so LIFO resolves it AFTER the draw). The plain `reject notaTarget` payload takes the parser's catch-all `GenericTargetAbility + MayAbility(must=true)` wrap — the discard is a FORCED menu (`choice 0`, then click the hand card). With the old `transforms(...) forever` contraption gone the aura also correctly dies with its creature. Learned along the way: `&&`-chained interactive payloads fizzle (the 61 copy-pasted `if(andAbility)` resolve sites have no target-acquisition — recorded as the CS-012 disease one layer deeper), and same-event trigger lines resolve LIFO (registration order reversed). | Witness green: `_spot/08_casting_of_bones.txt` (draw 3, forced discard, aura dies). Suite 1000+4/0. |
| CS-020 | **RESOLVED — no longer reproducible, 2026-07-09** | Consumed as-enters choice keeps reacting | Probed at head: the wrapper clone (`GenericActivatedAbility` "Choose a color", `forceDestroy=1`) hits `testDestroy` exactly once and is reaped; only the granted mana producer persists. A later tap-activation is a DIRECT click — no menu opens. The stale chooser was real when observed mid-CS-008 but the CS-012 MayAbility rework evidently retired it. | Witness: `mana_chosen_color_persists.txt` now runs WITHOUT a menu answer at the later activation — a regressed stale chooser would open a menu there and desync the script. |
| CS-021 | **FIXED — card script, 2026-08-23** | Delver of Secrets castable-or-flippable menu in HAND | Owner live report (Vita): "delver of secrets doesn't trigger on the first upkeep after playing it." The `@each my upkeep` trigger is SOUND — it fires on the first upkeep after the card is cast (verified scripted-suite and Baka selfplay, both ASPHASES postures). The real defect was one zone earlier: `anyzone={0}:doubleside(backside)` made "Flip Side" an ACTIVATED ability in every zone, so clicking Delver in hand opened `Cast Card Normally / Flip Side / Cancel`, and Flip Side turned the hand card into Insectile Aberration — a 3/2 flier for {U} with no upkeep trigger. No MTG rule lets a transforming DFC flip outside the battlefield (the engine's own doubleside parser already refuses it ON the battlefield); the line is dropped. NOTE: 59 OTHER transform-DFC fronts carry the same `anyzone={0}:doubleside(backside)` line and have the same illegal hand-flip; AIPlayerGPT already papers over it for the LLM seat with the N-152e "display toggle only" prompt note. Class sweep not done — open. | Witnesses green: `delver_of_secrets_cast_upkeep.txt` (one-click cast, first upkeep transforms — RED before the fix: the extra menu cancelled the cast), `delver_of_secrets_no_instant.txt` (non-instant top, no transform). |
| CS-022 | **FIXED — card script, 2026-08-23** | Black Vise ETB opponent-chooser | Owner live report (Vita): Vise "doesn't seem to be applying damage when its supposed to." Black Vise was the ONLY card of its family scripted as `auto=name(choose opponent) notaTarget(opponent) doNothing` + `@each targetedplayer upkeep:...targetedpersonshand...` — the trigger's `who=-3` branch fires ONLY when `source->playerTarget` is set, i.e. only if the player answers the as-enters chooser by clicking the opponent's avatar. Left unanswered the chooser WEDGES the game (reproduced: fixture never advances past the cast); answered wrongly/cancelled it leaves an inert Vise for the rest of the game. Its two functional identicals, Iron Maiden and Viseling, use the target-free `@each opponent upkeep:damage:type:*:opponenthandminus4minusend opponent` — rules-equivalent in this strictly 2-player engine, where the chosen opponent is necessarily the only opponent. Black Vise now matches them. | Witnesses green: `black_vise_seven_cards.txt` (7 cards —> 3 damage, no clicks — the same click-free script WEDGED before the fix), `black_vise_four_cards.txt` (4 —> 0), `black_vise_own_upkeep.txt` (never fires on the controller's own upkeep), plus the pre-existing `black_vise.txt` with its chooser answers removed. |

Add new observations here even when unconfirmed. Preserve a deterministic failing fixture in `_known_failures.txt` only after the expected semantics and driver protocol are sufficiently established.

- **CS-022a (scope note, 2026-08-23):** Black Vise's rescript to `@each opponent upkeep`
  (Iron Maiden form) is exact in every shipping game mode (all modes load exactly 2 seats;
  `Player::opponent()` is singular engine-wide). IF multiplayer ever lands, "each opponent"
  cards (Iron Maiden, Viseling) stay correct but Black Vise must be re-scoped to its
  chosen player - the old chooser script was equally 2-player-bound and encoded no real
  multi-opponent choice.
